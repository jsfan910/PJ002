# E-001 staging 首次部署紀錄（T-0027）

- 撰寫：dev-ops（Claude Sonnet 5）
- 日期：2026-09-19
- 任務卡：`tasks/T-0027-staging-deploy.md`

## 結論摘要

**staging 尚未部署成功**。`deploy-staging.yml` 已在 main 綠燈後真實觸發兩次，兩次都在第一個階段（`auth`，Workload Identity Federation）失敗，錯誤訊息一致：

```
google-github-actions/auth failed with: failed to generate Google Cloud federated token for //iam.googleapis.com/***:
{"error":"unauthorized_client","error_description":"The given credential is rejected by the attribute condition."}
```

**根因（已用本機已登入的 `gcloud` 唯讀查詢確認，未修改任何設定）**：GCP 端 WIF provider（`github-pool`／`github-provider`）的 `attribute-condition` 目前的值是**字面佔位符**，並未替換成實際倉庫：

```
attributeCondition: assertion.repository == '<owner>/<repo>'
```

正確值應為：

```
assertion.repository == 'jsfan910/PJ002'
```

因為條件字面比對永遠不成立（沒有任何 GitHub 倉庫的名字是字串 `<owner>/<repo>`），所以**任何**倉庫（包括正確的 `jsfan910/PJ002`）換發的 OIDC token 都會被拒絕。這正是 `README.md`「部署與 secrets」第 4 步 ② 指令裡 `--attribute-condition="assertion.repository == '<owner>/<repo>'"` 的角括號佔位符未被替換所致——已在本次收尾把這個真實案例補進 README 新增的「GCP 指令在哪裡執行」一節，提醒日後任何人設定 WIF 時務必替換。

**這是 GCP 設定問題，不是 workflow 程式問題**：`deploy-staging.yml`、`monitor-health.yml`、`infra/cloudrun-service.yaml`、`scripts/*.sh` 皆未變更任何部署邏輯，本卡只新增 README 一段說明與 06 文件三處實作紀錄。**agent 已依規定不索取、不代填、不修改任何 GCP IAM／安全性設定**，只做唯讀查詢定位根因；修正指令列在下方「使用者要做的事」，需使用者自行執行。

## 時間軸

| 時間（+08:00） | 事件 |
|---|---|
| 09-19 約 15:33 | Leader 派工 T-0027；main 推送至 `7d620f6`（含 `f524697`），觸發 CI 與 Deploy Staging |
| 09-19 15:50:25 | commit `7d620f6` 時間戳（`git log`） |
| 09-19 ～15:5x | `CI #2`（`35430410446`）與 `QA Tests (T-0021) #2`（`35430410444`）綠燈 |
| 09-19 ～15:5x | `Deploy Staging #1`（`35430432261`）觸發，21 秒後失敗（auth 階段） |
| 09-19 ～15:5x | `Deploy Staging #2`（`35430463010`）觸發，18 秒後失敗（auth 階段，同一錯誤） |
| 09-19 15:51 | dev-ops 開工，讀取任務卡與必讀文件 |
| 09-19 ～15:5x | 開瀏覽器查 Actions，定位兩次 run 皆為 auth 階段失敗 |
| 09-19 ～15:5x | 以本機已登入 `gcloud`（帳號 `excalibur.star@gmail.com`，專案 `pj002-509106`）唯讀查詢：WIF provider attribute-condition、SA IAM binding、專案角色、Artifact Registry、Secret Manager、Cloud Run services 清單 |
| 09-19 ～15:5x | 確認根因為 attribute-condition 字面佔位符；其餘設定（SA 四個角色、workloadIdentityUser 綁定、Artifact Registry repo、三個 Secret Manager secret 皆有 enabled 版本）全部正確 |
| 09-19 ～16:0x | 查 `monitor-health.yml`：0 workflow runs（尚未觸發，cron 延遲或需等待，agent 無法登入 GitHub 手動 `workflow_dispatch`） |
| 09-19 ～16:0x | 補 README「GCP 指令在哪裡執行」一節；更新 06 文件第 1 章 staging 列、5.4 演練紀錄、新增 6.5 首次 run 紀錄；本報告 |

## 各階段結果

| 階段 | 狀態 | 說明 |
|---|---|---|
| CI（lint/unit/build/integration） | ✅ 綠燈 | `CI #2` https://github.com/jsfan910/PJ002/actions/runs/35430410446 |
| QA Tests (T-0021) | ✅ 綠燈 | https://github.com/jsfan910/PJ002/actions/runs/35430410444 |
| deploy-staging：auth（WIF） | ❌ 失敗 | 見上方錯誤訊息；兩次 run 一致 |
| deploy-staging：migrate | ⏸ 未執行 | 卡在 auth 之前 |
| deploy-staging：build & push | ⏸ 未執行 | 同上 |
| deploy-staging：deploy | ⏸ 未執行 | 同上；`gcloud run services list --region asia-east1` 回傳空清單，證實從未有 revision 部署成功 |
| deploy-staging：verify | ⏸ 未執行 | 同上；staging 網址尚不存在 |
| staging 網址驗證（/health、/、/api/v1/todos） | ⏸ 無法執行 | 沒有可用網址，見下「acceptance 對照」 |
| 三處回填（STAGING_BASE_URL、06 §1、04 servers） | ⏸ 無法執行 | 沒有真實網址可填；已在 06 §1 記錄目前狀態與根因 |
| 回滾演練（06 §5.4） | ⏸ 無法演練 | 前提「已部署的 staging 服務、至少兩個 revision」不成立（目前 0 個 revision） |
| monitor-health.yml 排程與採樣 | ⏸ 尚無 run | 已確認 cron 語法（`*/5 * * * *`）正確且已在 main 上，GitHub 尚未觸發首次排程；`STAGING_BASE_URL` 為空時本工作流會以 0/0 略過，不會誤判 |

## 遇到的問題與解法

1. **WIF attribute-condition 字面佔位符未替換**（見上方「結論摘要」）。解法：使用者執行下方修正指令後，重新觸發 `deploy-staging.yml`。
2. **本機 `gcloud`／`gcloud.cmd` 不在 PATH**：Git Bash 下 `gcloud` 找不到指令；PowerShell 下 `gcloud.cmd` 同樣找不到。改用完整路徑 `"$env:LOCALAPPDATA\Google\Cloud SDK\google-cloud-sdk\bin\gcloud.cmd"` 執行唯讀查詢成功（帳號已於先前 `gcloud auth login` 完成，agent 未代登入，只使用既有登入狀態）。**建議使用者把該路徑加入使用者 PATH**，之後可直接打 `gcloud.cmd`（PowerShell／cmd.exe）或 `gcloud`（Git Bash，若安裝時已建立 POSIX 包裝）。
3. **`monitor-health.yml` 尚無 run**：GitHub 排程工作流在剛併入 main 後常有數分鐘至數十分鐘的觸發延遲，非程式問題。agent 無 GitHub 登入，無法手動 `workflow_dispatch`；已在 06 §6.5 記錄為「遠端待驗」。

## 使用者後續要做的事（依優先序）

1. **修正 WIF attribute-condition**（唯一已知阻擋根因；本步驟屬安全性設定變更，agent 依規定不代為執行）：

   Git Bash：
   ```bash
   gcloud iam workload-identity-pools providers update-oidc github-provider \
     --project="pj002-509106" --location="global" --workload-identity-pool="github-pool" \
     --attribute-condition="assertion.repository == 'jsfan910/PJ002'"
   ```

   PowerShell（若 `gcloud.cmd` 不在 PATH，前面加完整路徑）：
   ```powershell
   gcloud.cmd iam workload-identity-pools providers update-oidc github-provider `
     --project="pj002-509106" --location="global" --workload-identity-pool="github-pool" `
     --attribute-condition="assertion.repository == 'jsfan910/PJ002'"
   ```

   修正後建議立即唯讀確認（不會修改任何東西）：
   ```bash
   gcloud iam workload-identity-pools providers describe github-provider \
     --project="pj002-509106" --location="global" --workload-identity-pool="github-pool" \
     --format="value(attributeCondition)"
   # 預期輸出：assertion.repository == 'jsfan910/PJ002'
   ```

2. **重新觸發 `deploy-staging.yml`**：到 https://github.com/jsfan910/PJ002/actions/workflows/deploy-staging.yml 登入後點 **Run workflow**（`workflow_dispatch`），或對失敗的 run 頁面點 **Re-run all jobs**。agent 無 GitHub 登入，這一步需使用者手動點擊。

3. **部署成功後回填三處**（dev-tl 審核時或使用者自行）：
   - 從該次 run 的 `GITHUB_STEP_SUMMARY`（或 `gcloud run services describe todo-app --region asia-east1 --format="value(status.url)"`）取得 `*.run.app` 網址。
   - GitHub repository variable `STAGING_BASE_URL`（Settings → Secrets and variables → Actions → Variables）填入該網址。
   - `docs/specs/06_部署架構與CICD.md` 第 1 章 staging 列、`docs/specs/04_API規格.yaml` 的 `servers` 區塊 staging 項，兩處都把佔位符換成實際網址（下一輪 dev-ops 或審核者可代填，不含憑證）。

4. **三條 staging 驗證**（回填網址後執行，憑證用環境變數，不入報告）：
   ```bash
   curl -sS -o /dev/null -w "%{http_code}\n" "$STAGING_BASE_URL/health"          # 期望 200，無憑證
   curl -sS -o /dev/null -w "%{http_code}\n" "$STAGING_BASE_URL/"                # 期望 401，無憑證
   curl -sS -o /dev/null -w "%{http_code}\n" -u "$STAGING_BASIC_AUTH_USER:$STAGING_BASIC_AUTH_PASSWORD" "$STAGING_BASE_URL/api/v1/todos"   # 期望 200，帶憑證
   ```

5. **回滾演練**（06 §5.4，Gate 2 前置條件）：待第一次成功部署後，執行一次會啟動失敗的版本驗證 startup probe 擋下、再部署一版能啟動但行為有誤的版本，用 `scripts/rollback-staging.sh <revision>` 或 `gcloud run services update-traffic` 切回，記錄耗時與驗證輸出於 06 §5.4。

6. **monitor-health.yml 首次採樣**：可等 cron 自然觸發（每 5 分鐘），或到 https://github.com/jsfan910/PJ002/actions/workflows/monitor-health.yml 登入後點 **Run workflow** 立即跑一次。`STAGING_BASE_URL` 填好之前，取樣會顯示 `::notice::` 並以 0/0 略過，不算成功採樣。

## 唯讀查詢佐證（不含任何憑證值）

```text
gcloud iam workload-identity-pools providers describe github-provider ... --format="yaml(attributeCondition,attributeMapping,oidc)"
→ attributeCondition: assertion.repository == '<owner>/<repo>'
  attributeMapping: {attribute.repository: assertion.repository, google.subject: assertion.sub}
  oidc.issuerUri: https://token.actions.githubusercontent.com

gcloud iam service-accounts get-iam-policy github-deployer@pj002-509106.iam.gserviceaccount.com ...
→ roles/iam.workloadIdentityUser 綁定 principalSet .../attribute.repository/jsfan910/PJ002   （正確，非佔位符）

gcloud projects get-iam-policy pj002-509106 --filter="bindings.members:github-deployer@..." --format="table(bindings.role)"
→ roles/artifactregistry.writer, roles/iam.serviceAccountUser, roles/run.admin, roles/secretmanager.secretAccessor   （四個角色皆已授予）

gcloud artifacts repositories describe todo-app --location=asia-east1 ...
→ 存在，DOCKER 格式，0MB（尚無映像推送，符合從未部署成功的事實）

gcloud secrets list --project=pj002-509106
→ basic-auth-pass, basic-auth-user, database-url（三個皆存在）

gcloud secrets versions list <name> --project=pj002-509106
→ 三個 secret 皆至少一個 enabled 版本

gcloud run services list --project=pj002-509106 --region=asia-east1
→ （空清單，尚無任何 Cloud Run 服務／revision）
```

## Acceptance 對照

| acceptance 條目 | 結果 |
|---|---|
| deploy-staging 在 main 上綠燈（附 run 網址與各階段狀態） | ❌ 紅燈 ×2（`35430432261`、`35430463010`），已找出原因為 GCP WIF attribute-condition 設定問題，修正指令已列於本報告與 README |
| staging 網址三條 curl 驗證 | ⏸ 無法執行（網址不存在） |
| 三處回填 | ⏸ 無法執行（無真實網址）；06 §1 已記錄目前狀態與根因 |
| 回滾演練並記錄於 06 §5.4 | ⏸ 前提不成立（0 個 revision），06 §5.4 已記錄現況與待辦 |
| monitor-health.yml 至少 1 次成功採樣 | ⏸ 尚無 run（GitHub 排程延遲，非程式問題），06 §6.5 已記錄 |

**本卡判定：blocked**，阻擋原因為使用者 GCP 一次性設定中的一個字元級錯誤（WIF attribute-condition 未替換佔位符），修正後預期可自動解除本卡全部剩餘項目（下一輪僅需重新觀察 run 結果並補齊回填與演練，不需重新設計）。

---

## r1 續（2026-09-19 16:05～16:20，WIF 修正後重新驗證）

使用者已執行 `update-oidc` 把 attribute-condition 改為 `assertion.repository == 'jsfan910/PJ002'`；Leader 推送 main 至 `95137b8`，`deploy-staging.yml` 第三次真實觸發。

### 結果摘要

**WIF 問題已解除，staging 首次部署成功**，但發現第二個、性質不同的阻擋點：Secret Manager 的 `basic-auth-user` 值含混入的換行字元，導致 verify 階段的認證呼叫失敗。

| Run | 網址 | 結果 |
|---|---|---|
| `#3` https://github.com/jsfan910/PJ002/actions/runs/35431202802 | commit `95137b8` | Failure（僅最後一步失敗），耗時 1m 55s |

各步驟狀態：`checkout`／`setup-node`／`npm ci`／`npm run build`／`auth（WIF）`／`setup-gcloud`／`gcloud auth configure-docker`／`migrate`／`docker build & push`／`gcloud run deploy`／`verify（取服務網址）`／`verify（輪詢 /health 至 200）`／`回填提示` 全部 **成功**；只有 `verify（帶憑證呼叫 /api/v1/todos 確認 200）` **失敗**（`curl -f` exit code 22，即 HTTP 非 2xx）。

### staging 網址與驗證

- **網址：`https://todo-app-dpevsdhdva-de.a.run.app`**（revision `todo-app-00001-tfq`，100% 流量）
- Agent 自行以 curl 重新驗證（未使用任何憑證，或僅使用刻意錯誤的憑證確認錯誤訊息型態，未輸入任何真實憑證）：

```bash
curl -sS -o /dev/null -w "HTTP_CODE=%{http_code}\n" "https://todo-app-dpevsdhdva-de.a.run.app/health"
# HTTP_CODE=200，body {"status":"ok"}

curl -sS -o /dev/null -w "HTTP_CODE=%{http_code}\n" "https://todo-app-dpevsdhdva-de.a.run.app/"
# HTTP_CODE=401，body {"code":"E_UNAUTHORIZED","message":"Missing or bad formatted authorization header",...}

curl -sS -o /dev/null -w "HTTP_CODE=%{http_code}\n" "https://todo-app-dpevsdhdva-de.a.run.app/api/v1/todos"
# HTTP_CODE=401（無憑證，符合預期）

curl -sS -o /dev/null -w "HTTP_CODE=%{http_code}\n" -u "bogus:bogus" "https://todo-app-dpevsdhdva-de.a.run.app/api/v1/todos"
# HTTP_CODE=401，body {"code":"E_UNAUTHORIZED","message":"Invalid username or password",...}
```

**`/health` 無憑證 200、`/` 無憑證 401 兩條驗收條件已通過。** 第三條（帶正確憑證 200）無法由 agent 完成，因為 agent 不持有真實憑證；但用「刻意帶錯誤憑證」測出的錯誤訊息是 `Invalid username or password`（不是 `Missing or bad formatted authorization header`），證明應用程式的 Basic Auth 比對邏輯本身正常運作，問題出在**兩邊憑證字串不一致**，不是程式或部署缺陷。

### 根因排查（Secret Manager，唯讀，未顯示任何憑證明文）

用位元組計數比對每個 secret「原始讀出」與「去除 `\n`／`\r` 後」的長度差異，藉此判斷是否混入換行字元，**全程未印出任何憑證內容**：

```bash
GCLOUD="/c/Users/excal/AppData/Local/Google/Cloud SDK/google-cloud-sdk/bin/gcloud.cmd"
for n in basic-auth-user basic-auth-pass database-url; do
  RAW=$("$GCLOUD" secrets versions access latest --secret="$n" --project="pj002-509106" | wc -c)
  STRIPPED=$("$GCLOUD" secrets versions access latest --secret="$n" --project="pj002-509106" | tr -d '\n\r' | wc -c)
  echo "$n raw_bytes=$RAW stripped_bytes=$STRIPPED diff=$((RAW-STRIPPED))"
done
```
```text
basic-auth-user raw_bytes=12 stripped_bytes=9 diff=3
basic-auth-pass raw_bytes=9 stripped_bytes=9 diff=0
database-url raw_bytes=147 stripped_bytes=147 diff=0
```

**結論**：`basic-auth-pass`、`database-url` 兩個 secret 乾淨；**`basic-auth-user` 的 `latest` 版本（版本 2，版本 1 已 disabled）仍含 3 個換行／回車字元**。這會讓 Cloud Run 容器啟動時注入的 `BASIC_AUTH_USER` 環境變數尾端帶著看不見的字元，與 GitHub secret `STAGING_BASIC_AUTH_USER`（若使用者輸入時沒有同樣的雜訊）不相等，Basic Auth 因而判定「使用者名稱或密碼錯誤」。**這正是 README「GCP 指令在哪裡執行」一節先前提醒的『secret 值用 `[IO.File]::WriteAllText` 避免多寫入換行』案例在本卡的真實重現。**

### 使用者要做的事

1. **重新建立 `basic-auth-user` 的乾淨版本**（PowerShell，沿用 README 已有寫法；`<你的使用者名稱>` 換成實際值，不含前後空白或換行）：
   ```powershell
   $gcloud = "$env:LOCALAPPDATA\Google\Cloud SDK\google-cloud-sdk\bin\gcloud.cmd"
   [System.IO.File]::WriteAllText("$PWD\basic-auth-user.txt", "<你的使用者名稱>", [System.Text.Encoding]::UTF8)
   & $gcloud secrets versions add basic-auth-user --project="pj002-509106" --data-file="$PWD\basic-auth-user.txt"
   Remove-Item basic-auth-user.txt
   ```
   並確認這個值與 GitHub repository secret `STAGING_BASIC_AUTH_USER` 完全相同（大小寫、無多餘空白）。
2. **重新部署以套用新的 secret 版本**：Cloud Run 的 `--set-secrets ...:latest` 是在容器**啟動時**解析，新增 secret 版本不會讓現有 revision 自動生效。到 https://github.com/jsfan910/PJ002/actions/workflows/deploy-staging.yml 登入後點 **Run workflow**，或對 run `#3` 按 **Re-run failed jobs**。
3. **重新部署成功後**：
   - GitHub repository variable `STAGING_BASE_URL` 填入 `https://todo-app-dpevsdhdva-de.a.run.app`（Settings → Secrets and variables → Actions → Variables）。
   - 確認 `verify（帶憑證呼叫 /api/v1/todos 確認 200）` 步驟綠燈。
4. **回滾演練（06 §5.4）**：目前只有 1 個 revision（`todo-app-00001-tfq`），修正後的重新部署會產生 revision 2，屆時可用 `scripts/rollback-staging.sh` 或 `gcloud run services update-traffic` 在 revision 1／2 之間演練一次並記錄於 06 §5.4（下一輪處理）。
5. **`monitor-health.yml`**：`STAGING_BASE_URL` 回填後，等 cron（每 5 分鐘）自然觸發，或登入 GitHub 後手動 `workflow_dispatch` 立即跑一次確認採樣成功。

### Acceptance 對照（更新）

| acceptance 條目 | 結果 |
|---|---|
| deploy-staging 在 main 上綠燈 | ⚠️ 大幅進展：auth／migrate／build&push／deploy／verify(URL)／verify(/health) 全綠；僅 verify(帶憑證 /api/v1/todos) 紅燈，根因已定位（Secret Manager `basic-auth-user` 混入換行） |
| staging 網址三條 curl 驗證 | ✅ `/health` 無憑證 200；✅ `/` 無憑證 401；⏸ `/api/v1/todos` 帶正確憑證 200（agent 無真實憑證無法測，錯誤訊息已證明是憑證不一致而非程式缺陷） |
| 三處回填 | ✅ 06 §1、04 servers 已回填真實網址；⏸ GitHub variable `STAGING_BASE_URL` 待使用者網頁設定（值已列於上方） |
| 回滾演練並記錄於 06 §5.4 | ⏸ 仍差一個 revision，06 §5.4 已記錄現況 |
| monitor-health.yml 至少 1 次成功採樣 | ⏸ 待 `STAGING_BASE_URL` 回填後才有意義；截至收尾時仍 0 run |

**本卡本輪判定：blocked**（維持），阻擋原因已從「WIF 設定」精確化為「Secret Manager `basic-auth-user` 含混入換行字元，與 GitHub secret 不一致」，修法已備妥且屬使用者需自行執行的憑證值變更，agent 未讀取、未顯示任何憑證明文。相較上一輪，**部署管線本身已證明可跑通**，剩餘全部是使用者端的一個資料清理動作 + 重新觸發。
