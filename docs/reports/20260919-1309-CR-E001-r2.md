# Code Review 報告（第 2 輪複審）：T-0020 P0 全量 Code Review（E-001）

<!-- 檔名：docs/reports/20260919-1309-CR-E001-r2.md -->
<!-- 通過門檻：阻擋級 = 0。本輪為 r2 複審，不改寫 r1 報告 docs/reports/20260919-1146-CR-E001.md。 -->

- 審查者：qa-cr（round 2）
- 審查範圍：**只複審 r1 的 13 項發現是否確實修正 + 回歸**，不重審全量
  - 基準 commit：`main` `beb3f0b`（＝ r1 審的 `153410b` ＋ 修正卡 T-0024／T-0025／T-0026 合併後的 `4b96b20` ＋ T-0021 自動化測試合併 `beb3f0b`）；複審期間 dev-tl 再推進到 `e7abe4f`（T-0021 收尾），本報告的 lint／build 結論以 `e7abe4f` 為最終狀態（見 N-2），受審的修正碼本身（`src/**`、`public/**`、`Dockerfile`、compose、workflows）在 `beb3f0b`→`e7abe4f` 之間**未變動**（`e7abe4f` 只動 `tests/e2e`、`tsconfig*`、`package*.json`、`qa-tests.yml`、`README`／`CHANGELOG`／`.gitignore`／T-0021 卡與交接檔）
  - 複審檔案：`src/plugins/basic-auth.ts`、`src/server.ts`、`src/app.ts`、`src/db/migrate.ts`、`Dockerfile`、`docker-compose.yml`、`package.json`、`tsconfig.test.json`、`.github/workflows/{ci,deploy-staging,monitor-health}.yml`、`public/assets/todo-store.js`、`public/assets/todo-view.js`、`tests/integration/{startup-migrate,auth-basic,todos-api}.test.ts`、`docs/specs/06_部署架構與CICD.md` §7、`docs/templates/code_review_report.md`、`README.md`
  - **平行作業注意**：複審期間 dev-tl 正在根目錄進行 T-0021 收尾（工作區有未 commit 變更）。本報告凡受此影響之處皆標明觀測時間與當時的 commit／工作區狀態，見「回歸檢查」N-2。
- 對照文件：r1 報告 `docs/reports/20260919-1146-CR-E001.md`（B-1、B-2、S-1～S-11 的修正建議欄）、`tasks/E-001-todo-app.md`（2026-09-19T12:03:12／12:22:24／12:59:14 三段 Leader 裁決）、`tasks/T-0024`／`T-0025`／`T-0026` 與其 dev-tl 審核紀錄
- 結論：**通過**（阻擋級 **0** 項、r1 的 13 項**全部已修正**；本輪新增建議級 **2** 項，其中 N-2 已於本輪結束前由 dev-tl 的 `e7abe4f` 關閉，實際待辦僅 N-1；另觀察 **1** 項）

---

## 結論：阻擋級 = 0

| r1 編號 | 一句話 | 判定 |
|---|---|---|
| B-1 | `/health` 豁免比對的點節段／百分號編碼繞過 | **已修正**（真實容器 + 原始 request-target 實測，13 種變體全部正確） |
| B-2 | 啟動時未自動套用 migration | **已修正**（`docker compose down -v` → `up --build` 乾淨環境實測，自動建表、`GET /api/v1/todos` 回 200 `[]`） |
| S-1～S-11 | 建議級 11 項 | **全部已修正**（逐項證據見下表） |

- 本輪**未發現任何阻擋級**問題，r1 的兩項阻擋皆以「真實邊界」（真實容器 + 未經正規化的 request-target、乾淨 volume 的容器啟動）而非 `app.inject` 複驗。
- 新增 2 項建議級（N-1 並行 migration 無互斥鎖；N-2 lint 涵蓋範圍與 T-0021 e2e 的交互，**已由 dev-tl `e7abe4f` 關閉**）與 1 項觀察（N-3），皆不阻擋本卡通過。

---

## 逐項複審（r1 的 13 項）

### 阻擋級

| # | 判定 | 修正處（main `beb3f0b`） | 證據 |
|---|---|---|---|
| **B-1** `/health` 豁免比對正規化繞過 | **已修正** | `src/plugins/basic-auth.ts:53-64` `stripQueryAndHash()` 改為純字串切割（只找 `?`／`#` 取較小索引後 `slice`），`:70-75` `isExemptPath()` 以 `=== "/health"` 完全相等比對；全檔已無 `new URL(` | ① 對**真實容器**（`crr2-app-1`，非 `app.inject`）以 `curl --path-as-is` 送 13 種 request-target：r1 的四個繞過變體 `/foo/../health`、`/.%2e/health`、`/%2e%2e/health`、`/assets/%2e%2e/%2e%2e/health` 全部由 **403 → 401**，`/health` 與 `/health?x=1` 仍 200（輸出見 §3）。② 另以 `node:http`／`net` 直送 8 種畸形 request-target（含絕對式 `http://localhost:8097/health`、`/health;x=1`、`/health/.`、`/..;/health`、`/health%23/...`）均為 401 或僅回 `/health` 本文，**未發現任何新的豁免繞過**（§4）。③ 回歸測試已內建：`tests/integration/auth-basic.test.ts:196-219` 四個變體以**真實 TCP**（`http.request` 的 `path` 逐字送出，檔頭並明確記載「`app.inject` 會先 `new URL()`，永遠會誤判成豁免命中」）斷言 401 + `E_UNAUTHORIZED` |
| **B-2** 啟動時未自動套用 migration | **已修正** | `src/server.ts:35-56`：`start()` 在 `app.listen` 之前呼叫 `applyMigrationsOrExit()`（專用池跑完即 `end()`），失敗 `app.log.error` 後 `process.exit(1)`；`Dockerfile:43` runtime 階段補 `COPY migrations ./migrations` | ① **乾淨環境實測**：獨立 compose 專案 `crr2`（自用埠 8097／5439）先 `down -v` 移除 volume，再 `up -d --build`；容器啟動後**未執行任何 migrate 指令**，`GET /api/v1/todos` 直接回 `200 []`，`\dt` 顯示 `todos`／`schema_migrations` 已建立，`schema_migrations` 有 `001`（§2）。② 時序正確：`applied_at 05:03:20.601Z` 早於 `Server listening`（`time:1789794200661` = `05:03:20.661Z`）**60ms**，確認 migration 在監聽之前完成。③ 失敗路徑：以不存在的 DB（`localhost:5999`）啟動，記錄「啟動時套用 migration 失敗，服務不會啟動監聽」後 `NODE_EXIT=1`，且 18099 埠無監聽（§5）。④ 回歸測試 `tests/integration/startup-migrate.test.ts` 以**與 `src/server.ts` 相同的啟動路徑**（`import { start } from "../../dist/server.js"`）對隔離 schema（`startup_migrate_test`，每次 DROP／CREATE）驗證 200 `[]`；本輪實跑**確實執行未被 skip**（`skipped 0`，該測試 959.27ms 通過，§6） |

### 建議級

| # | 判定 | 修正處 | 證據 |
|---|---|---|---|
| **S-1** ajv 型別脅迫 | **已修正** | `src/app.ts:39-43` `ajv.customOptions.coerceTypes: false` | 對容器實測：`POST {"title":123}` → **400** `{"code":"E_VALIDATION","message":"body/title must be string","details":{"field":"title","rule":"type"}}`；`PATCH {"isCompleted":null}` → **400** `body/isCompleted must be boolean`；`PATCH {"isCompleted":"true"}` → **400**。正常流程未受影響：`POST` 合法標題 201、`PATCH {"isCompleted":true}` 200、`?status=active`／`?status=completed` 200、`DELETE` 204、UUID 格式錯 400（§7）。整合測試含專責案例「PATCH：isCompleted 送 null 回 400（CR S-1）」 |
| **S-2** 豁免不分 HTTP 方法 | **已修正** | `basic-auth.ts:70-73`：非 `GET`／`HEAD` 直接不豁免 | 容器實測未帶憑證：`GET /health` 200、`HEAD /health` 200、`POST`／`PUT`／`PATCH`／`DELETE`／`OPTIONS /health` 皆 **401**（§3）；`auth-basic.test.ts:221` 有對應斷言 |
| **S-3** workflow secrets 進命令列 | **已修正** | `.github/workflows/deploy-staging.yml:135-145`：改宣告 `env: BASIC_USER／BASIC_PASS` 後以 `-u "$BASIC_USER:$BASIC_PASS"` 引用 | `grep -n "secrets\." deploy-staging.yml` 僅 5 處命中，全部位於 `with:`（WIF 的 `workload_identity_provider`／`service_account`）或 `env:`（migrate 與 verify 兩步）區塊，**`run:` 內已無任何 `${{ secrets.* }}` 展開** |
| **S-4** `tsc` 不檢查 `tests/` | **已修正** | 新增 `tsconfig.test.json`；`package.json:15` `lint` 串接 `tsc --noEmit -p tsconfig.test.json` | 本輪 `npm run lint` 確實對 `tests/**/*.ts` 求值（這正是 N-2 能被當場抓出的原因）。**S-4 的修正同時是 N-2 的前提**：若 `tests/` 仍不受檢查，T-0021 的 e2e 型別問題會無聲進 main |
| **S-5** `load()` 競態 | **已修正** | `public/assets/todo-store.js:109` `requestSeq`、`:134` 取號、`:139`／`:145` 成功與失敗兩條路徑皆比對 `seq !== requestSeq` 後回 `{stale:true}` 不寫 state | 讀碼確認成功與**例外**路徑都做了 stale 判定（只防成功路徑是常見漏洞，此處沒有漏）；單元測試 76/76 全綠（含 `todo-store.test.mjs` 的先發後到案例）；dev-tl 已於真實瀏覽器以人為延遲 1500ms 製造先發後到並複驗 |
| **S-6** 編輯中草稿被重繪吃掉 | **已修正** | `todo-view.js:48` 模組層 `editingDraft`、`:164` `input.value = editingDraft ?? todo.title`、`:172` `input` 事件同步、`:137`／`:185`／`:195` 進入與離開編輯模式時重置 | 讀碼確認初始化、同步、重置三點齊備（進入編輯即重置，避免上一筆草稿串到下一筆）；`innerHTML`／`outerHTML`／`insertAdjacentHTML`／`document.write`／`eval(` 對 `public/`＋`src/` 掃描仍為 **NO MATCH**（修正未引入 XSS 風險）。**無自動化測試**一項經 dev-tl 裁決接受並交 qa-at r2 補 E2E，r2 追認此切分 |
| **S-7** 監測 `curl -f` 吞掉狀態碼 | **已修正** | `.github/workflows/monitor-health.yml:47`：`curl -sS -o /dev/null -w "%{http_code} %{time_total}" ... \|\| echo "000 0"`，已無 `-f` | 讀碼確認真實狀態碼（含 4xx／5xx）會寫入 `health-samples.csv`，只有 curl 連不上才落到 `000`；`SUCCESS_COUNT` 仍以 `= "200"` 判定，NFR-003 的判讀規則可用 |
| **S-8** `.env.example` 佔位值與凍結 SD 字面矛盾 | **已修正（依 Leader 裁決改以模板例外處理）** | `docs/templates/code_review_report.md:37` 新增第 4 類已知例外：「`.env.example` 與測試檔的本機佔位值（Leader 裁決 D-01；值必須是明顯佔位如 `dev`、`localhost`）」 | 與 Leader 2026-09-19T12:03:12 裁決②一致（不改凍結 SD 文字）。本輪憑證掃描即依四類例外判讀，無需再逐輪重新論證 |
| **S-9** Cloud Run 參數三處飄移 | **已修正** | `docs/specs/06_部署架構與CICD.md` §7「三處必須同步的參數表」 | 抽查三列與現況一致：容器埠 `containerPort: 8080` vs `--port 8080` ×2；min／max-instances `0`／`2` 三處一致；`--set-secrets` 三個 Secret Manager 名稱與 `infra/cloudrun-service.yaml` 的 `secretKeyRef`、`scripts/deploy-staging.sh:67` 逐字相同。兩項飄移（`--allow-unauthenticated` 只在兩支 gcloud 指令、probe 只在 IaC 檔）已**明列並附影響說明與補救指令**，符合 r1 的修正建議「不得無聲飄移」 |
| **S-10** compose db 埠綁死 | **已修正** | `docker-compose.yml:16` `"${POSTGRES_HOST_PORT:-5432}:5432"` | 本輪**即以此驗證**：`POSTGRES_HOST_PORT=5439`＋`PORT=8097` 起獨立專案，與他人正在使用的 5432／8080 並行不撞（`docker ps` 顯示兩組服務同時健康運作，§2） |
| **S-11** `npm run dev` 必然失敗／`engines` 太寬 | **已修正** | `package.json:7-9` `engines.node: ">=22.18"`、`:11-12` `dev` 拆為 `dev:build`（`tsc -w`）與 `dev:run`（`node --watch dist/server.js`）；`README.md:111` 常用指令表改列兩支 script 並標「需開兩個終端機」、`:131` 但書說明原 `dev` 失敗原因 | 讀碼與 README 逐行確認；`npm run dev` 已不存在（不會再有「列在指令表卻必然失敗」的指令）。拆兩支 script 而非引入 `concurrently` 一事，由 package.json 擁有者 dev-tl 裁決、Leader 於 12:59:14 追認，r2 接受（新增相依屬規格變更，成本高於收益） |

**小計：13 / 13 全部已修正，無「部分修正」或「未修正」。**

---

## 回歸檢查（修正是否引入新問題）

| # | 嚴重度 | 檔案:行號 | 說明 | 修正建議 |
|---|---|---|---|---|
| **N-1** | 建議 | `src/db/migrate.ts:76-105`（`runMigrations`），觸發點 `src/server.ts:35-56` | B-2 把 migration 從「pipeline 單次執行」改成「**每個應用實例啟動時都執行**」，但 runner 沒有任何互斥機制。對**全新資料庫**且**兩個以上實例同時冷啟**時：兩者都讀到 `schema_migrations` 為空 → 都執行 `001`（DDL 本身冪等，`CREATE TABLE/INDEX IF NOT EXISTS`、`DROP TRIGGER IF EXISTS`）→ 但 `INSERT INTO schema_migrations (version)` 會有一方撞 PK 唯一鍵 → `runMigrations` 拋例外 → 該實例 `process.exit(1)`。實務衝擊有限（部署工作流的 migrate 階段先跑，正常情況下實例啟動時已是 no-op；`max-instances 2`；Cloud Run 會重試），但這是 B-2 修正**新引入**的失敗模式，且 P1 增加 `002` 之後（expand-contract、多實例滾動更新）命中機率會上升 | `runMigrations` 開頭取一把 session 級 advisory lock、結束釋放：`SELECT pg_advisory_lock($1)` / `pg_advisory_unlock($1)`（固定 key，例如 `hashtext('todo-app-migrations')`），或把 `INSERT` 改為 `ON CONFLICT (version) DO NOTHING` 並在其後重新確認版本已存在。建議列 P1 或與 `002` 同卡處理，不必為此開 P0 修正卡 |
| **N-2**（**已於本輪結束前關閉**） | 建議（存查用，現況無待辦） | `tsconfig.test.json:10`（`include: ["tests/**/*.ts"]`）× `tests/e2e/**`（T-0021 於 `beb3f0b` 合併） | S-4 讓 `npm run lint` 開始檢查 `tests/**/*.ts`，而 T-0021 合併進來的 `tests/e2e/*.e2e.test.ts` 需要 DOM 全域型別與 `@playwright/test`。**2026-09-19T13:02 對 committed `beb3f0b` 實跑 `npm run lint` 為紅**（`LINT_EXIT=2`，24 個錯誤，全部集中在 `tests/e2e/*`：`Cannot find name 'document'`、`Cannot find name 'HTMLInputElement'`、`TS18048 possibly undefined`、以及尚未裝套件時的 `Cannot find module '@playwright/test'`）。`ci.yml:35-36` lint job 正是 `npm run build` → `npm run lint`，故該 commit 上 CI lint job 會失敗。13:08 於工作區（dev-tl 尚未 commit）轉綠，**13:12 dev-tl 已 commit `e7abe4f`「T-0021 收尾」**，內含 `tsconfig.e2e.json`（新）、`tsconfig.test.json` 的 `"exclude": [..., "tests/e2e"]`、`package.json` 的 `lint` 串上 `tsc --noEmit -p tsconfig.e2e.json` 三處；對該 commit 重跑 `npm run build && npm run lint` 皆為 **0**。**本項已關閉，不需任何後續動作**，保留紀錄是因為它揭示了一個會再犯的結構：`tsconfig.test.json` 的 `include` 是 `tests/**/*.ts`，日後任何新增在 `tests/` 下、需要不同 `lib`／型別環境的測試都會重演同一條紅燈 | 已由 `e7abe4f` 解決。日後在 `tests/` 新增需要不同型別環境的測試時，比照 `tsconfig.e2e.json` 的做法：新增專屬 tsconfig、在 `tsconfig.test.json` 的 `exclude` 排除、並把新 tsconfig 串進 `lint`；三處要同一個 commit 一起改 |

### N-3 觀察（不列為發現）

`T-0024` 的 acceptance 寫「S-1：POST 送 `{"title":123}` 回 400、`{"isCompleted":null}` 回 **400**」。實測 `POST {"title":"x","isCompleted":null}` 回 **201**（`isCompleted` 為 `false`）。原因不是型別脅迫復發，而是 `CreateTodoRequestSchema`（`src/schemas/todo-schema.ts:55-65`）只宣告 `title` 且 `additionalProperties: false`，ajv 的 `removeAdditional` 在型別檢查前就把 `isCompleted` 移除——這與 r1 檢查清單已接受的「夾帶 `id`／`createdAt` 被忽略」是同一個行為，也符合 `04_API規格.yaml` 的 `CreateTodoRequest` 只有 `title`。**S-1 在真正相關的位置（`PATCH` 的 `isCompleted`、兩處 `title`）已確實回 400**，故判定 S-1 已修正；僅記錄「該卡 acceptance 字面與實際行為不符」，供日後寫驗收條件時取 `PATCH` 案例而非 `POST` 案例。dev-tl 審核時實際驗的也是 `PATCH isCompleted null`，判斷正確。

---

## 檢查清單（r2 範圍：只複核受修正影響的項目）

- [x] **安全（認證邊界）** — 豁免比對已無任何正規化；13 種 request-target（含四個 r1 繞過變體）＋ 8 種畸形 raw target 實測皆正確；未帶憑證取靜態資產仍 401；401 仍附逐字 `WWW-Authenticate: Basic realm="staging"`；豁免限 `GET`／`HEAD`。
- [x] **符合資料庫設計／migration forward-only** — `001` 內容未變更（仍為 forward-only、無 down、DDL 冪等）；新增的只是「啟動時呼叫」與容器內 `COPY migrations`；runner 交易寫法未動。**唯一新風險見 N-1（並行無互斥鎖）**。
- [x] **錯誤處理與輸入驗證** — 關閉 `coerceTypes` 後錯誤契約不變（`E_VALIDATION` + `details{field,rule}`）；migration 失敗走 `app.log.error` + `exit(1)`，不會半開；容器日誌抽驗未見任何憑證值（26 筆 `authorization` 字樣全部是 `@fastify/basic-auth` 的固定錯誤訊息字面值 `Missing or bad formatted authorization header`，非標頭內容，`redact` 未失效）。
- [x] **無硬編碼密鑰／環境值** — 以 dev-tl 補強樣式（含可選引號）對全部受追蹤檔案重掃（含 T-0021 新進的 `tests/e2e`／`tests/integration/qa`／`tests/perf` 與 `qa-tests.yml`）：命中全部落在模板的四類已知例外內（CI 佔位值 `ci`、`id-token: write`、`--set-secrets` 參照語法、D-01 本機佔位值 `dev`／`localhost`），**真實憑證 0 筆**。
- [x] **測試品質** — r1 指出的三個缺口全部補上：點節段豁免測試（真實 TCP，4 條）、未套用 migration 的啟動回歸測試（`startup-migrate.test.ts`，本輪確認**真的執行、非 skip**）、`tests/` 納入 `tsc` 檢查。unit 76/76、integration 56/56，`skipped 0`。
- [x] **遵守 ADR／規格** — B-2 的修法（啟動時自動 migration + pipeline 階段保留為雙重保險）符合 `05_資料庫設計.md` §3 第 4 條與 SD §7 NFR-008 對策④，並對應 Leader 撤回 T-0013-③ 的裁決；`$PORT` 不寫死、非 root、多階段建置等 ADR-0001／0005 要件未被修正破壞（容器以 `USER appuser` 正常啟動並讀 `PORT=8097`）。
- [ ] 不適用 — API 規格逐端點全量比對、DB DDL 逐行比對、40 條「假設與決策」表態：r1 已完成且本輪修正未觸及其結論，依 Leader「r2 只複審修正處與回歸」裁決不重做。

---

## 驗證輸出

環境：Node v24.15.0、npm 11.12.1、Windows 11、Docker Desktop。**自用資源**：compose 專案名 `crr2`、app 埠 `8097`、db 埠 `5439`（`POSTGRES_HOST_PORT`），與他人正在使用的 `8080`／`5432` 完全隔離；收尾已 `down -v`（見 §8）。

### §1 `npm run build` → `npm run lint`

```bash
npm run build
```
```text
> todo-app@0.1.0 build
> tsc -p tsconfig.json

BUILD_EXIT=0
```

```bash
npm run lint    # 13:02，對 committed main beb3f0b（工作區尚無 dev-tl 的 T-0021 收尾變更）
```
```text
> todo-app@0.1.0 lint
> eslint . && tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.test.json

tests/e2e/nfr-compat.e2e.test.ts(50,11): error TS2584: Cannot find name 'document'. ...
tests/e2e/us001-create.e2e.test.ts(55,29): error TS2304: Cannot find name 'HTMLInputElement'.
tests/e2e/us008-spa.e2e.test.ts(115,13): error TS2584: Cannot find name 'document'. ...
（全部 24 筆錯誤皆位於 tests/e2e/*，src/ 與 tests/unit、tests/integration 零錯誤）
LINT_EXIT=2
```

```bash
npm run lint    # 13:08，dev-tl 於工作區補上 tsconfig.e2e.json 與 exclude 後重跑
```
```text
> todo-app@0.1.0 lint
> eslint . && tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.test.json && tsc --noEmit -p tsconfig.e2e.json

LINT_EXIT=0
```

判讀見 N-2：**紅燈成因不在 T-0024～T-0026 的修正，而在 S-4（lint 擴及 tests/）與 T-0021（e2e 檔）合併的交互**；dev-tl 的修法正確，惟須確實 commit。

### §2 B-2：乾淨環境（`down -v` → `up --build`）自動 migration

```bash
docker compose -p crr2 --env-file <自用 env> down -v     # 先清掉 volume，確保是全新資料庫
docker compose -p crr2 --env-file <自用 env> up -d --build
docker compose -p crr2 logs app
curl -sS -u dev:dev -w "\nHTTP=%{http_code}\n" http://localhost:8097/api/v1/todos   # 未執行任何 migrate 指令
docker exec crr2-db-1 psql -U dev -d todo -c "\dt"
docker exec crr2-db-1 psql -U dev -d todo -c "SELECT version, applied_at FROM schema_migrations"
```
```text
# down -v
Volume crr2_todo-db-data  Removed          ← 確認是全新 volume

# up
Container crr2-db-1  Healthy
Container crr2-app-1 Started

# app 啟動日誌
{"level":30,"time":1789794200661,"pid":1,...,"msg":"Server listening at http://127.0.0.1:8097"}

# GET /api/v1/todos（未跑過任何 migrate）
[]
HTTP=200            ← r1 此處為 500 relation "todos" does not exist

# \dt
 public | schema_migrations | table | dev
 public | todos             | table | dev

# schema_migrations
 version |          applied_at
---------+-------------------------------
 001     | 2026-09-19 05:03:20.601436+00   ← 早於 listening（05:03:20.661Z）60ms
```

同時確認 S-10：`docker ps` 顯示 `crr2-app-1 0.0.0.0:8097->8097/tcp`、`crr2-db-1 0.0.0.0:5439->5432/tcp` 與他人的 `soft-dev-team-260919-*`（8080／5432）**同時運作互不影響**。

### §3 B-1／S-2：對真實容器以 `curl --path-as-is` 直送 request-target

```bash
for P in /health "/health?x=1" "/health/" /healthz-secret "//health" \
         "/api/v1/todos/../health" "/foo/../health" "/.%2e/health" \
         "/%2e%2e/health" "/assets/%2e%2e/%2e%2e/health" \
         "/%2e%2e/assets/api-client.js" "/health%00" "/HEALTH"; do
  printf "%-34s -> " "$P"; curl -sS --path-as-is -o /dev/null -w "%{http_code}\n" "http://localhost:8097$P"
done
for M in GET HEAD POST PUT PATCH DELETE OPTIONS; do
  printf "%-8s -> " "$M"; curl -sS -X "$M" -o /dev/null -w "%{http_code}\n" "http://localhost:8097/health"
done
```
```text
/health                            -> 200   ← 豁免（預期）
/health?x=1                        -> 200   ← 查詢字串不影響（預期）
/health/                           -> 401
/healthz-secret                    -> 401   ← 無前綴比對
//health                           -> 401
/api/v1/todos/../health            -> 401
/foo/../health                     -> 401   ← r1 為 403（已越過認證），現已修正
/.%2e/health                       -> 401   ← r1 為 403，已修正
/%2e%2e/health                     -> 401   ← r1 為 403，已修正
/assets/%2e%2e/%2e%2e/health       -> 401   ← r1 為 403，已修正
/%2e%2e/assets/api-client.js       -> 401
/health%00                         -> 401
/HEALTH                            -> 401   ← 大小寫不豁免

GET      -> 200
HEAD     -> 200
POST     -> 401   ← S-2（r1 為 404，等於可探測路由存在）
PUT      -> 401
PATCH    -> 401
DELETE   -> 401
OPTIONS  -> 401
```

### §4 B-1 加驗：`node:http`／`net` 直送畸形 request-target（尋找新的繞過）

```bash
node rawprobe.mjs     # http.request({path: <逐字>})
node sockprobe.mjs    # net socket 直接寫 "GET <target> HTTP/1.1"
```
```text
"/health"                                  -> 200 {"status":"ok"}
"/health#x"                                -> 200 {"status":"ok"}          ← 豁免命中，但實際服務的仍是 /health 本文
"/health#/../assets/api-client.js"         -> 200 {"status":"ok"}          ← 未洩漏任何受保護內容
"/health%23/../assets/api-client.js"       -> 401 E_UNAUTHORIZED
"/assets/api-client.js#/health"            -> 401 E_UNAUTHORIZED
"/health;x=1"                              -> 401 E_UNAUTHORIZED
"/health/."                                -> 401 E_UNAUTHORIZED
"/health/.."                               -> 401 E_UNAUTHORIZED
"/..;/health"                              -> 401 E_UNAUTHORIZED
"http://localhost:8097/health"（絕對式）    -> 401 E_UNAUTHORIZED
"//health"                                 -> 401 E_UNAUTHORIZED
"/health?"                                 -> 200 {"status":"ok"}
"/health#"                                 -> 200 {"status":"ok"}
"/health\t"                                -> 400 Bad Request（Node HTTP 解析器擋下，未進應用）
```

結論：剝除 `#` 之後才比對的三個案例雖然命中豁免，但路由實際服務的仍是 `/health` 本身（回 `{"status":"ok"}`），**不存在「豁免命中卻取得受保護內容」的組合**，B-1 的修法在邊界上是收斂的。

### §5 B-2 失敗路徑：migration 失敗即非 0 結束且不監聽

```bash
DATABASE_URL="postgres://dev:dev@localhost:5999/nodb" PORT=18099 ... node dist/server.js
```
```text
{"level":50,...,"err":{"type":"AggregateError","code":"ECONNREFUSED",
 "stack":"... at async ensureMigrationsTable (.../dist/db/migrate.js:46:5)
          at async runMigrations (.../dist/db/migrate.js:59:5)
          at async applyMigrationsOrExit (.../dist/server.js:34:9)
          at async start (.../dist/server.js:50:5)"},
 "msg":"啟動時套用 migration 失敗，服務不會啟動監聽"}

NODE_EXIT=1
netstat | grep 18099 → 18099 free      ← 確認未監聽（不會半開）
```

### §6 測試：unit 76/76、integration 56/56，`startup-migrate` 未被 skip

```bash
npm run test:unit
NODE_ENV=test PORT=8097 DATABASE_URL="postgres://dev:dev@localhost:5439/todo" \
  BASIC_AUTH_USER=dev BASIC_AUTH_PASSWORD=dev LOG_LEVEL=silent npm run test:integration
```
```text
# test:unit
ℹ tests 76
ℹ pass 76
ℹ fail 0
ℹ skipped 0

# test:integration
ℹ tests 56
ℹ pass 56
ℹ fail 0
ℹ skipped 0
ℹ duration_ms 1314.8834

# 其中（確認真的執行，非 skip）
✔ 以與 src/server.ts 相同的啟動路徑（start()）啟動，對未套用 migration 的資料庫，
  GET /api/v1/todos 回 200 []（CR B-2，非 500） (959.2696ms)
✔ PATCH /api/v1/todos/{id}：isCompleted 送 null 回 400（CR S-1：關閉 ajv coerceTypes，不得脅迫成 false 後放行）
```

註：`test:integration` 的 glob 在 dev-tl 的 T-0021 收尾（當時未 commit）中由 `tests/integration/**/*.test.ts` 改為 `tests/integration/*.test.ts`（qa 子目錄另走 `test:integration:qa`），本輪 56 項即為此範圍，與 T-0024～T-0026 合併時的 56 項可比。

### §7 S-1 與契約回歸抽驗（真實容器 8097）

```text
POST {"title":123}                -> 400 {"code":"E_VALIDATION","message":"body/title must be string",
                                          "details":{"field":"title","rule":"type"}}     ← r1 為 201
POST {"title":"x","isCompleted":null} -> 201（removeAdditional 先移除未宣告欄位，見 N-3）
POST {"title":"CR-r2 regression"} -> 201
PATCH {"isCompleted":true}        -> 200
PATCH {"isCompleted":"true"}      -> 400 body/isCompleted must be boolean
PATCH {"isCompleted":null}        -> 400 body/isCompleted must be boolean                ← r1 為脅迫成 false
PATCH {"title":123}               -> 400 body/title must be string
GET ?status=active                -> 200（回傳僅未完成項）
GET ?status=completed             -> 200
GET /api/v1/todos/abc             -> 400（UUID 格式錯，Leader 12:03 裁決③追認）
DELETE                            -> HTTP/1.1 204 No Content（無 Content-Type、無 body）
GET /api/v1/todos（未帶憑證）      -> 401 + www-authenticate: Basic realm="staging"
GET /assets/api-client.js（未帶憑證）-> 401
```

### §8 收尾清除

```bash
docker compose -p crr2 --env-file <自用 env> down -v
docker ps; docker volume ls --filter name=crr2; netstat -ano | grep -E "8097|5439"
```
```text
Container crr2-db-1 Removed / Volume crr2_todo-db-data Removed / Network crr2_default Removed
docker ps → soft-dev-team-260919-app-1（8080）、soft-dev-team-260919-db-1（5432）   ← 他人服務未受影響
docker volume ls --filter name=crr2 → （無）
netstat → 僅剩 TIME_WAIT 的客戶端連線，無任何 LISTENING
```

---

## 對三張修正卡 dev-tl 審核宣稱的核實

| 卡 | dev-tl 宣稱 | 核實結果 |
|---|---|---|
| T-0024 | 四個點節段變體與 `POST /health` 401、乾淨環境自動建表、migration 失敗 `exit(1)` 且不監聽、coerceTypes 關閉、lint 0／unit 75／integration 56 | **屬實**。本輪以真實容器與 raw request-target 複驗四個變體與方法限制皆正確；乾淨 volume 自動建表、失敗路徑 `NODE_EXIT=1` 且埠未監聽皆重現。unit 本輪為 76（T-0026 的 store 測試併入後），非矛盾 |
| T-0025 | actionlint 0、`run:` 內 `secrets.` 零命中、monitor 無 `curl -f`、`POSTGRES_HOST_PORT=5437` 生效、`tsconfig.test.json` 使 lint 涵蓋 tests、06 參數表與三處現況一致 | **屬實**（`POSTGRES_HOST_PORT` 本輪以 5439 再次驗證生效；參數表抽查三列一致）。**唯一追加**：`tsconfig.test.json` 的 `include` 在 T-0021 合併後會掃到 `tests/e2e`，造成 lint 紅燈，見 N-2——此為合併交互，非 T-0025 當時的缺陷 |
| T-0026 | lint 0、`todo-store.test.mjs` 25/25、`innerHTML` 0、11 個 `data-testid` 不變、瀏覽器實測先發後到與草稿保留 | **屬實**。讀碼確認 `requestSeq` 在成功與例外兩條路徑都做 stale 判定、`editingDraft` 初始化／同步／重置三點齊備；`innerHTML` 等五個樣式對 `public/`＋`src/` 仍 NO MATCH |

---

## 下一步建議

1. **N-2 已關閉，無需動作**：dev-tl 的收尾 commit `e7abe4f` 已含三處修法，對該 commit 重跑 `npm run build`（0）與 `npm run lint`（0）皆通過，main 的 CI lint job 可恢復綠燈。日後在 `tests/` 新增需要不同型別環境的測試時，須比照 `tsconfig.e2e.json` 的三處同步做法。
2. **N-1 列 P1 待辦**（或與 `002` migration 同卡）：`runMigrations` 加 advisory lock 或 `ON CONFLICT DO NOTHING`，避免多實例同時冷啟時其中一個 `exit(1)`。
3. 寫後續卡的 acceptance 時，型別嚴格性一律以 `PATCH` 案例為準（`POST` 的未宣告欄位會先被 `removeAdditional` 移除，見 N-3）。
4. T-0020 本卡：阻擋級 = 0，建議 Leader 於 qa-at r2／qa-uat r2 回報後一併收進 T-0023 測試總結。
