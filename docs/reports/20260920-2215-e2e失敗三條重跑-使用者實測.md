# e2e 失敗三條重跑（TC-037／TC-011／TC-016 × 6 project × 3 輪）：使用者實測紀錄

- 執行者：使用者（自有機器）；Leader 於 2026-09-20T22:15:23+08:00 轉錄自對話，逐字保留
- 環境：staging https://todo-app-dpevsdhdva-de.a.run.app（最新 revision，應用碼自 ddc66bb 起零變更）
- 指令：node node_modules\@playwright\test\cli.js test -c tests\e2e\playwright.config.ts -g "TC-037|TC-011|TC-016" --reporter=dot，連跑 3 輪（每輪 18 條 = 3 TC × 6 project）

```text
=== run 1 ===
  1 failed
    [chromium-390x844] › tests\e2e\us004-delete.e2e.test.ts:62:1 › TC-037：刪除已被刪除的待辦不產生系統錯誤畫面
  17 passed (58.7s)
=== run 2 ===

Running 18 tests using 1 worker
··················
  18 passed (24.2s)
=== run 3 ===

Running 18 tests using 1 worker
··················
  18 passed (25.2s)
```

- 統計：TC-011、TC-016 於 3 輪 × 6 project 全過（18/18 各），前次失敗判為暫時性網路逾時的初判得到佐證。
- TC-037：3 輪 18 次中 1 敗（chromium-390x844，run 1）；加上完整回歸的 1 敗（msedge-1280x800），合計 24 次執行 2 敗，跨兩種瀏覽器引擎，且 run 1 整體耗時 58.7s（其餘 24～25s），顯示當時網路較慢。run 2 起 test-results/ 已被 Playwright 清空，run 1 的 error-context 不可得；完整回歸那次的錯誤為「等待 todo-item（TC-037：待刪除）的 todo-item-delete 逾時」。

## Leader 初判（非事實來源，交 qa-lead 於 T-0048 定案）

測試原始碼 tests/e2e/us004-delete.e2e.test.ts 第 62～73 行的前置步驟：

```ts
const todo = await apiCreate("TC-037：待刪除");
await page.goto("/");
const item = page.getByTestId("todo-item").filter({ hasText: "TC-037：待刪除" });
await apiDelete(todo.id); // 背景已被刪除
page.once("dialog", (dialog) => dialog.accept());
await item.getByTestId("todo-item-delete").click();
```

`page.goto("/")` 只等到 load 事件，不等清單的 API 回應；接著立刻 `apiDelete`。網路較慢時，前端的清單請求在 apiDelete 之後才回應（或回應已不含該筆），項目從未出現在畫面上，`click` 因此逾時。這是測試前置步驟的競態（缺一行 `await expect(item).toBeVisible()` 再 apiDelete），與 T-0038 的 stale 行為無關（本 TC 不經前端建立資料，沒有動作被取代的情境）。初判為**測試設計缺陷（flaky test），非產品缺陷**；建議由 qa-lead 判定後列 P1 測試修正項（tests/e2e 由 qa-at 維護）。
