---
doc: REPORT
title: 監測補強：NFR-003 採樣來源、GitHub cron 診斷、TC-080/TC-090 直接量測
epic: E-001
task: T-0031
author: dev-ops
created: 2026-09-19T18:04:19+08:00
---

# 監測補強：E-001（T-0031）

## 1. 時間軸

| 時間（+08:00） | 事件 |
|---|---|
| 17:50:14 | 開工，讀任務卡與必讀清單 |
| 17:51:08 | GitHub API 查 cron run 數（`total_count=1`，與 T-0030 報告一致） |
| 17:51~18:01 | 逐項核對 cron 診斷四個假設（分支／repo 活動／語法／首次排程延遲） |
| 17:53:49~17:55:07 | 建立 Cloud Monitoring uptime check（`todo-app-health`），過程中踩到 Git Bash `/health` 路徑轉換的坑並排除 |
| 17:59:17~18:03:16 | 背景執行 `scripts/measure-deploy-downtime.sh`（240 秒）同時以 `gcloud run services update` 觸發新 revision，量測 TC-080／TC-090 |
| 18:01Z（UTC 10:01） | 判讀指令首次實跑，取得 14 個樣本（全 `true`） |
| 18:04:19 | 完成 06 第 6 章更新與本報告 |

## 2. GitHub cron 診斷結論

**結論：`monitor-health.yml` 本身設定無缺陷（分支 `main`、workflow `state=active`、cron 語法 `*/5 * * * *` 正確、repo 為公開且持續有推送），零自動觸發的成因判斷為 GitHub 排程系統對新註冊 workflow 的啟動延遲，超出可控範圍。本卡未修改 `monitor-health.yml`。**

逐項證據（實際輸出）：

```bash
curl -sS "https://api.github.com/repos/jsfan910/PJ002/actions/workflows/monitor-health.yml/runs?per_page=50" \
  | grep -oE "\"total_count\": *[0-9]+"
```
```text
"total_count": 1
```
（實查時間 2026-09-19T18:01:50+08:00；workflow 註冊於 GitHub 的 `created_at` 為 `2026-09-19T07:49:44Z`（15:49:44+08:00），距診斷時間已 **2 小時 12 分**，理論排程次數約 26 次，實際 `event=schedule` 次數 **0**。）

```bash
curl -sS "https://api.github.com/repos/jsfan910/PJ002" | grep -oE "\"default_branch\": *\"[a-z]+\""
curl -sS "https://api.github.com/repos/jsfan910/PJ002/actions/workflows/361932810"
```
```text
"default_branch": "main"
{"id":361932810,"name":"Monitor Health","path":".github/workflows/monitor-health.yml","state":"active","created_at":"2026-09-19T07:49:44.000Z","updated_at":"2026-09-19T07:49:44.000Z"}
```

```bash
curl -sS "https://api.github.com/repos/jsfan910/PJ002" \
  | grep -oE "\"pushed_at\": *\"[0-9T:Z-]+\"|\"created_at\": *\"[0-9T:Z-]+\"|\"private\": *(true|false)"
```
```text
"private": false
"created_at": "2026-09-19T06:58:29Z"
"pushed_at": "2026-09-19T09:49:21Z"
```

詳細逐項排除過程見 `docs/specs/06_部署架構與CICD.md` §6.8.1。**不採用**「cron 語法需與 push 同時觸發才生效」之類的猜測性修法，因為分支、路徑、語法、repo 活動四項證據都正常，沒有找到可歸因的設定缺陷；勉強調整 cron 分鐘偏移（避開整點）預期效益低（無法解釋連續 2 小時 12 分鐘全數落空），故不列為本卡變更。

## 3. Cloud Monitoring uptime check 建立紀錄

- **check id**：`todo-app-health-aMAlP5dfKv0`
- **建立時間**：`2026-09-19T09:55:07Z`（UTC）＝ **`2026-09-19T17:55:07+08:00`**
- **規格**：`https://todo-app-dpevsdhdva-de.a.run.app/health`、HTTPS、port 443、週期 5 分鐘（`300s`）、逾時 10 秒（`10s`）、4 個檢查地區（`ASIA_PACIFIC`／`USA_OREGON`／`USA_IOWA`／`EUROPE`，≥ 3）、期望狀態碼 `200`、不帶憑證
- **可重跑腳本**：`infra/uptime-check.sh`（`create`／`list`／`describe` 三個子命令）
- **Git Bash 踩坑**：`--path=/health` 在 Git Bash 下被 MSYS 路徑轉換攔截，導致 gcloud.cmd 整串指令解析失敗；加 `MSYS_NO_PATHCONV=1` 解決。已記錄在腳本註解與本節，供之後任何人在 Git Bash 下呼叫 gcloud 時參考（PowerShell 不受影響）。

實際建立輸出：
```text
Created uptime [projects/pj002-509106/uptimeCheckConfigs/todo-app-health-aMAlP5dfKv0].
{
  "checkerType": "STATIC_IP_CHECKERS",
  "displayName": "todo-app-health",
  "httpCheck": { "acceptedResponseStatusCodes": [{"statusValue": 200}], "path": "/health", "port": 443, "requestMethod": "GET", "useSsl": true },
  "monitoredResource": { "labels": { "host": "todo-app-dpevsdhdva-de.a.run.app", "project_id": "pj002-509106" }, "type": "uptime_url" },
  "name": "projects/pj002-509106/uptimeCheckConfigs/todo-app-health-aMAlP5dfKv0",
  "period": "300s",
  "selectedRegions": ["ASIA_PACIFIC", "USA_OREGON", "USA_IOWA", "EUROPE"],
  "timeout": "10s"
}
```

## 4. 判讀指令與首次實跑輸出

指令（`monitoring.googleapis.com/uptime_check/check_passed`，依 `check_id` 過濾，24 小時窗）：

```bash
GCLOUD="C:/Users/excal/AppData/Local/Google/Cloud SDK/google-cloud-sdk/bin/gcloud.cmd"
TOKEN=$("$GCLOUD" auth print-access-token)
END=$(date -u "+%Y-%m-%dT%H:%M:%SZ")
START=$(date -u -d "-24 hours" "+%Y-%m-%dT%H:%M:%SZ")
CHECK_ID="todo-app-health-aMAlP5dfKv0"
PROJECT="pj002-509106"
FILTER='metric.type="monitoring.googleapis.com/uptime_check/check_passed" AND metric.label.check_id="'"${CHECK_ID}"'"'
curl -sS -G "https://monitoring.googleapis.com/v3/projects/${PROJECT}/timeSeries" \
  -H "Authorization: Bearer ${TOKEN}" \
  --data-urlencode "filter=${FILTER}" \
  --data-urlencode "interval.startTime=${START}" \
  --data-urlencode "interval.endTime=${END}" \
  --data-urlencode "view=FULL"
```

首次實跑（check 建立後約 6 分鐘，樣本仍少，依任務卡要求即使樣本少仍貼出）：

```text
checker_location=usa-oregon:   6 點，09:59:20Z ~ 10:00:50Z，全部 boolValue=true
checker_location=usa-iowa:     6 點，09:59:20Z ~ 10:00:10Z，全部 boolValue=true
checker_location=eur-belgium:  2 點，10:00:50Z ~ 10:01:00Z，全部 boolValue=true
（asia-pacific 尚無資料點，屬新建立 check 的正常初期現象）
```

**本次窗內成功率 ＝ 14/14 ＝ 100%**（樣本量遠低於 Gate 2 門檻的 288 次，僅證明判讀指令可用）。**正式 Gate 2 判讀請於 2026-09-20T17:55:07+08:00 之後，以完整 24 小時窗重跑本指令並統計。**

## 5. TC-080／TC-090 直接量測結果

腳本：`scripts/measure-deploy-downtime.sh`（每 1 秒 `curl /health`，不帶憑證，記錄狀態碼與時間）。因本卡尚未合併，依任務卡建議路徑：`gcloud run services update` 加無害環境變數觸發新 revision，同時背景執行量測腳本。

```text
觸發時間：2026-09-19T09:59:43Z（UTC）
gcloud run services update todo-app --region asia-east1 --update-env-vars "DEPLOY_PROBE_T0031=2026-09-19T09:59:43Z"
→ Deploying... Creating Revision.................done. Done.
→ 新 revision：todo-app-00008-kcs（lastTransitionTime 2026-09-19T09:59:46.414475Z）
```

量測窗：`2026-09-19T09:59:17.746Z` ~ `2026-09-19T10:03:16.670Z`（134 個樣本）。

```text
awk -F, 'NR>1{print $2}' deploy-downtime.csv | sort | uniq -c
    134 200
```
```text
最長連續失敗次數（約秒數，取樣間隔 ~1s）：0
全程無非 200 回應。
```

**結論：最長連續不可用秒數 ＝ 0 秒**，遠低於 60 秒門檻。量測窗內另有一個並行 revision（`todo-app-00009-cg7`，`10:00:11Z` 產生），判斷為共用 staging 環境中其他任務卡並行部署／回滾操作所致；不影響本次 `/health` 全程 200 的結論。

**TC-080 範圍說明**：本卡任務卡 acceptance 對 TC-080 的定義限定為「以每 1 秒輪詢 `/health` 記錄不可用秒數」，不含需 Basic Auth 憑證的 `/api/v1/todos` 資料筆數比對（該比對需讀取 Secret Manager 應用憑證明文，超出本卡「不索取、不代填、不讀取憑證明文」的限制，且 Claude Code 權限分類器已直接拒絕本卡嘗試以 `gcloud secrets versions access` 取值供 `curl -u` 使用的操作）。本節 0 秒不可用結果補強 TC-080 既有三項間接證據之一；資料層 id 集合直接比對仍待下一輪有憑證的執行者補做。

## 6. 給 qa-lead 的 NFR-003 判讀方式與起算時間

- **主要來源**：Cloud Monitoring uptime check `todo-app-health`（check id `todo-app-health-aMAlP5dfKv0`），第 4 節指令。
- **起算時間**：`2026-09-19T17:55:07+08:00`（uptime check 建立時間）。**Gate 2 的 24 小時判讀時間點：2026-09-20T17:55:07+08:00 之後**。
- **備援來源**：`monitor-health.yml`（若後續開始穩定產生排程樣本），可交叉比對但不作為 Gate 判準分母。
- **未達標時判讀規則**：沿用 06 §6.3；uptime check 額外可用 `checker_location` 維度區分「單一地區網路問題」與「服務本身異常」。
- **TC-080**：本卡已補做 `/health` 直接量測（0 秒不可用）；資料層 id 集合比對仍待補做，建議排入下一輪測試卡。

## 7. 需要 Leader 裁決的事

無（cron 診斷未發現可修缺陷，依裁決 B 已改用 uptime check 為主要來源；TC-080 範圍依任務卡定義完成，資料層直接比對留待下一輪）。
