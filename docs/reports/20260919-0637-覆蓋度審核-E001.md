---
doc: COVERAGE-REVIEW
title: SD 覆蓋度反向審核報告
epic: E-001
task: T-0005
round: 1
author: plan-ba
model: Claude Opus 5（1M context）
reviewers: [leader]
created: 2026-09-19T06:52:00+08:00
---

# 覆蓋度審核報告：E-001 待辦事項 Web 應用（T-0005 r1）

以**需求方視角**反向確認設計端（SA／SD／API）沒有漏掉任何使用者故事。
本報告不改 SD、API、DB、部署、ADR、SA —— 發現的問題一律寫成建議，交 Leader 決定是否開修正卡。

審核依據：`tasks/T-0005-coverage.md` 的 6 條 acceptance、`.claude/agents/plan-ba.md`「審核他人時（審 SD 覆蓋度）」判準（**任一 P0 US 無對應即退回**）、`tasks/E-001-todo-app.md`「Leader 裁決紀錄」全部三段。

---

## 1. 檢查範圍與數量

| 對象 | 數量 | 來源 | 核對方式 |
|---|---|---|---|
| 使用者故事 US | **13 則**（P0 十則 US-001~US-010、P1 三則 US-011~US-013） | `docs/specs/01_需求規格書_SRS.md` 第 3 章 | 與 `traceability.md` 主表 `diff` 集合比對 |
| 使用案例 UC | 14 則 | `docs/specs/02_系統分析書_SA.md` 第 1 章 | 與 `traceability.md` `diff` 集合比對 |
| 驗收條件 AC | 62 條 | SRS 第 3 章 | 逐 US 去重計數 vs `traceability.md` AC 對照表 |
| 非功能需求 NFR | 8 條 | SRS 第 4 章 | 與 `traceability.md` NFR 追溯表集合比對 |
| SD 模組 | 21 個（後端 13、前端 4、DevOps 4） | `docs/specs/03_系統設計書_SD.md` 第 3 章 | 與 `traceability.md` 引用集合雙向差集 |
| API 路徑 / operation | 5 條路徑 / 9 個 operation | `docs/specs/04_API規格.yaml` | 與 `traceability.md` 引用路徑比對 |
| Leader 裁決項 | 11 項 Q ＋ 10 項 O ＋ NFR-001 ＋ N-001 | `tasks/E-001-todo-app.md` | 逐項確認已落到規格或已登錄 |

**逐 US 檢查了 13 則，覆蓋率 13/13 = 100%。**

---

## 2. 逐 US 對照結果（SD 模組表 × API 端點）

判定方法分兩層，兩層都要過：

1. **欄位層**：`traceability.md` 主表的 `SD 模組`、`API 端點` 兩欄不得為空或 `—`。
2. **內容層**（比 T-0004 審核更嚴）：把 SD 第 3 章模組表「對應 UC」欄的**範圍寫法 `UC-00a~UC-00b` 展開**後反查，該 US 的 UC **必須至少被一個模組明載**。只有欄位填了字但 SD 端查無此 UC，仍算缺口。

| US | 優先級 | UC | SD 模組（SD 端明載其 UC 者） | API 端點 | 判定 |
|---|---|---|---|---|---|
| US-001 新增待辦 | P0 | UC-001 | BE-04、BE-05、BE-06、FE-02、FE-03 | `POST /api/v1/todos` | ✅ 有對應 |
| US-002 檢視清單 | P0 | UC-002 | BE-04、BE-05、BE-06、FE-02、FE-03、FE-04 | `GET /api/v1/todos` | ✅ 有對應 |
| US-003 編輯待辦 | P0 | UC-003 | BE-04、BE-05、BE-06、FE-03 | `PATCH /api/v1/todos/{todoId}` | ✅ 有對應 |
| US-004 刪除待辦 | P0 | UC-004 | BE-04、BE-05、BE-06、FE-03、FE-04 | `DELETE /api/v1/todos/{todoId}`（204） | ✅ 有對應 |
| US-005 切換完成 | P0 | UC-005 | BE-04、BE-05、BE-06、FE-03 | `PATCH /api/v1/todos/{todoId}`（設定目標狀態，冪等） | ✅ 有對應 |
| US-006 依狀態篩選 | P0 | UC-006 | BE-04、BE-05、BE-06、FE-03 | `GET /api/v1/todos?status=all\|active\|completed` | ✅ 有對應 |
| US-007 顯示建立時間 | P0 | UC-007 | BE-04、BE-06、FE-04 | `GET /api/v1/todos`、`GET /api/v1/todos/{todoId}` 的 `createdAt` | ✅ 有對應 |
| US-008 單頁前端 | P0 | UC-008 | BE-09、FE-01、FE-04 | `GET /`、`GET /assets/*`（靜態頁，見註 ①） | ✅ 有對應 |
| US-009 REST API | P0 | UC-009 | BE-01、BE-03、BE-04 | 全部 `/api/v1/*` ＋ 統一錯誤物件 `components/schemas/Error` | ✅ 有對應 |
| US-010 staging 部署 | P0 | UC-010、UC-011 | BE-02、BE-07、BE-08、OPS-01~OPS-04 | `GET /health`（唯一未保護路徑） | ✅ 有對應 |
| US-011 使用者註冊 | P1 | UC-012 | BE-10、BE-11、BE-12 | `POST /api/v1/auth/register` | ✅ 有對應 |
| US-012 登入與登出 | P1 | UC-013 | BE-10、BE-11、BE-13 | `POST /api/v1/auth/login`（登出無端點，見註 ②） | ✅ 有對應 |
| US-013 資料隔離 | P1 | UC-014 | BE-13、BE-05 | 全部 `/api/v1/todos*`（橫切授權，見註 ③） | ✅ 有對應 |

**缺口數 0。無任何 P0 或 P1 US 缺設計對應，因此依 `.claude/agents/plan-ba.md` 的審核判準，不退回。**

### 註：三個「看起來像缺口、實際不是」的情況

- **註 ①（US-008／UC-008）**：`UC-008` 在 `04_API規格.yaml` 的出現次數為 **0**。這是**正確的** —— OpenAPI 描述的是 API 契約，不描述靜態頁；靜態頁由 `BE-09 static-hosting` 供應，落點在 SD 第 5 章端點表第 2 列（`/`、`/assets/*` → UC-008、UC-010）。**列此一筆是為了讓下一位審核者不必重複追問。**
- **註 ②（US-012 登出）**：登出無伺服器端端點，是 Leader 裁決 **O-006**（登出＝用戶端清除 JWT）的結果，非遺漏。
- **註 ③（US-013／UC-014）**：UC-014 是**橫切授權**而非獨立端點，端點落點為全部 `/api/v1/todos*`，模組落點為 `BE-05`／`BE-13`（SD §3.1），實作規格見 SD §5 設計要點第 4 點與 §6.2。惟 `04_API規格.yaml` 尚未在任一 operation description 標註 UC-014 —— 這是**文字標註缺失，不是設計缺失**，詳見 §4 的 D-1。

---

## 3. 缺口清單

### 3.1 設計覆蓋缺口

**無。**

13 則 US（含全部 10 則 P0 與 3 則 P1）在 SD 模組表與 API 端點表皆有對應；`traceability.md` 孤兒檢查四項（US↔UC、UC↔US、US↔模組／端點、模組↔US）重跑後全部為「無」。

### 3.2 追溯矩陣一致性缺口

**無。**七組一致性核對（US 集合、優先級、UC 集合、模組集合、API 路徑、AC 數量、特殊驗證 AC）全部相符，指令與真實輸出見 `worklog/handoff/20260919-0637-T0005-r1-plan-ba.md` B 段。

### 3.3 文件一致性瑕疵（非覆蓋缺口，但需修）

共 **4 項**，全部為文字層級，逐項見 §4、§5。其中 D-1、D-2 為審核者 plan-sa 於 T-0004 標給 plan-sd 的項目，**不在本卡 outputs 內，本輪未動**。

---

## 4. 對 SD／API 的修正建議（交 plan-sd，本卡不改）

| # | 問題 | 證據 | 建議修法 | 優先級 |
|---|---|---|---|---|
| **D-1** | `UC-014` 未在 `04_API規格.yaml` 任一 operation description 標註。其餘 12 則有端點的 UC 皆有標註 | `grep -c "UC-014" docs/specs/04_API規格.yaml` → **0**；對照 `UC-005` → 4、`UC-003` → 3、`UC-007` → 3 | 在 `getTodoById`／`updateTodo`／`deleteTodo` 三個 operation 的 description 各補一句「（P1）UC-014：以使用者身分隔離存取，跨使用者回 404」 | 低（不影響 Gate 1） |
| **D-2** | SD §9 開放問題 `SD-02` 的文字與 `05_資料庫設計.md` 實際 SQL 位置對不上。SD 寫「做成 `002_*.sql` migration 的**第一行** `TRUNCATE todos;`」，實際 SQL 在 `CREATE TABLE users` 之後（`05_*.md` 第 178 行） | SD 第 404 行原文 vs `05_資料庫設計.md` §2 | 把 SD-02 的「第一行」改為「**同一個 migration 內、`ALTER ... SET NOT NULL` 之前**」。語意本就相同，只是文字會讓開發者以為有兩種寫法 | 低（P1 才執行） |
| **D-3** | SD 第 3 章模組表「對應 UC」欄比 `traceability.md` **窄** 6 處，兩份文件對同一事實有兩種寬度 | 展開範圍寫法後比對：`FE-04` 未列 UC-001／003／005／006；`FE-02`／`FE-03` 未列 UC-008；`BE-05`／`BE-06` 未列 UC-009；`BE-06`／`BE-12` 未列 UC-014 | 二選一：(a) 補齊「對應 UC」欄（`FE-04` → UC-001~UC-008、`FE-02`／`FE-03` → 加 UC-008、`BE-05`／`BE-06` → 加 UC-009、`BE-06`／`BE-12` → 加 UC-014）；(b) 在欄位標題加註「僅列主要 UC，完整對應以 `traceability.md` 為準」。**本角色偏好 (a)**，因為 dev-tl 是拿這一欄反查工作項的 | 低 |
| **D-4** | （**已確認非缺口，僅登錄以免重複追問**）`UC-008` 在 `04_API規格.yaml` 出現次數為 0 | 同 D-1 的 grep 輸出 | **不需修改**。OpenAPI 不描述靜態頁；UC-008 的端點落點在 SD 第 5 章端點表，已足夠 | — |

> 方向說明（D-3）：6 處差異**全部是 `traceability.md` 較寬、SD 較窄**，即不存在任何 US 覆蓋不足的情形。這是「兩份文件的詳盡度不同」，不是「設計漏了」。

---

## 5. 對 SRS 的修正建議

### 5.1 本輪已修（在本卡 outputs 授權範圍內）

| # | 修改 | 依據 |
|---|---|---|
| S-0 | **NFR-003 量測方式改寫**為兩級：①Gate 2 門檻＝24 小時採樣（每 5 分鐘一次，共 288 次，成功率 ≥ 99%，即允許失敗 ≤ 2 次）；②正式環境目標＝連續 7 天，不作 Gate 2 門檻。同時把量測方式欄中過時的端點暫名 `GET /healthz` 對齊為 plan-sd 定名的 `GET /health` | Leader 裁決 **O-009**（2026-09-19T05:57:38+08:00） |
| — | SRS frontmatter `version` **0.1 → 0.2**、`updated` 更新 | 任務卡 acceptance 第 5 條 |

**其餘 7 條 NFR 與全部 US／AC 一字未動**，以 `git diff` 證明：全檔僅 3 行變更（version、updated、NFR-003 該列）。

### 5.2 本輪未修，需 Leader 決定（**本卡 outputs 明文限定 SRS 僅改 NFR-003，故未擅動**）

| # | 問題 | 影響 | 建議 |
|---|---|---|---|
| **S-1** | **SRS 第 6 章「假設與待確認事項」表的 Q-001~Q-011 狀態欄仍為「待確認」**，而 `01a_需求澄清紀錄.md` 已於本輪全數更新為「已裁決（Leader）」。同一事實在兩份文件有兩種狀態 | SRS 開頭自述「本文件僅寫已確認為事實的需求」，狀態欄過時會讓下游誤以為還有 11 項未定 | 授權 plan-ba 以一張小卡（或追加本卡 outputs）把第 6 章 11 列狀態欄改為「已裁決（Leader）」、確認來源欄改為 `tasks/E-001-todo-app.md#Leader 裁決紀錄`。**純文字，估 10 分鐘** |
| **S-2** | **NFR-003「需求」欄的括號數字自相矛盾**：原文寫「成功率 ≥ 99%（每 5 分鐘一次，允許失敗 ≤ 100 次／2016 次）」。7 天 × 288 次／天 = 2016 次，**99% 成功率對應的允許失敗上限是 ≤ 20 次，不是 100 次**；100／2016 ≈ 4.96%，實際等於 95% 成功率 | 這是**量化 NFR 的內部矛盾**，測試團隊寫 TC 時會撞到兩個互斥的判準。這是我在 T-0002 寫 SRS 時的算術錯誤，由本輪反向審核發現 | 本輪已在**量測方式欄**寫死 Gate 2 的可執行判準（288 次、允許失敗 ≤ 2 次），矛盾不會傳到 Gate 2。但**需求欄的「≤ 100 次／2016 次」仍須改為「≤ 20 次／2016 次」**。與 S-1 同卡處理即可 |
| **S-3** | **AC-009-5 的 Given 前提已不成立**：原文「Given 前端與 API 部署於不同來源」，而 Leader 裁決 **O-004 為同源** | 已由 `traceability.md`「特殊驗證方式的 AC」表以兩條規範意圖判準替代（無 `Access-Control-Allow-Origin` 標頭、無跨來源預檢），plan-sa 於 T-0004 審核中已表態接受 | 建議在 SRS frozen 前於 AC-009-5 加一行備註指向 `traceability.md` 該表，**不改 AC 原文**（原文保留可追溯裁決前後的差異）。與 S-1 同卡處理 |

> 三項皆為**文字與數值層級**，不涉及範圍、不涉及設計、不需重跑 T-0003／T-0004。

---

## 6. 對 Gate 1 的建議

### 建議：**有條件通過**

**條件（4 項，全部為文字修正，預估合計 ≤ 30 分鐘，可由 Leader 於 Gate 1 當場批准後併成一張小卡執行）**：

1. **S-1** — SRS 第 6 章 Q-001~Q-011 狀態欄同步為「已裁決（Leader）」。（plan-ba）
2. **S-2** — SRS NFR-003 需求欄的「允許失敗 ≤ 100 次／2016 次」改為「≤ 20 次／2016 次」，消除與「≥ 99%」的矛盾。（plan-ba）
3. **D-1** — `04_API規格.yaml` 三個 operation description 補標 UC-014。（plan-sd）
4. **D-2** — SD §9 SD-02 的「第一行」改為「同一個 migration 內、`ALTER ... SET NOT NULL` 之前」。（plan-sd）

### 為何是「有條件通過」而不是「通過」

Epic 的 Gate 1 判準是「**規格包全部通過 DoD ＋ 追溯矩陣孤兒檢查三項為「無」**」。

- 孤兒檢查三項：**全部為「無」，已重跑確認**（另加第四項模組↔US 反向檢查亦為「無」）。判準達成。
- DoD 的「非功能需求全部量化並附量測方式」：**S-2 是一條已量化但數值自相矛盾的 NFR**。矛盾會直接傳給測試團隊（同一條 NFR 有 99% 與 95% 兩個判準），因此我不願以「通過」掩蓋它。但它只需改一個數字，不構成重做。
- DoD 的「假設攤開、待確認項不得當事實」：**S-1 使 SRS 正文對外顯示 11 項仍待確認**，而實際已全數裁決。同樣只需改狀態欄。

### 為何不是「不通過」

- **沒有任何 P0 或 P1 US 缺設計對應**（缺口數 0）。角色檔的退回判準是「任一 P0 US 無對應即退回」，此判準未被觸發。
- 四項條件**全部是文字或單一數值**，無一需要重新設計、重開 UC、或改動 API 契約的語意。
- 設計端的實質品質在 T-0004 審核中已由 plan-sa 逐條驗過（6/6 acceptance 通過、32 條 BR 全有落點、13 條 BR 抽驗落點正確）。本輪反向審核從需求側再驗一次，結論一致。

### 併同提醒 Leader 的既有指派（非本輪新增，僅避免遺漏）

- 標給 **dev-tl** 的兩項實作註記（`/health` 豁免須比對 pathname 而非 `req.url`；`title` 須先 `trim()` 再驗長度再交 DB），出自 `worklog/handoff/20260919-0628-T0004-r1-plan-sa.md`，屬開發階段，不影響 Gate 1。
- 標給 **dev-ops** 的回滾演練（`06_部署架構與CICD.md` §5.4）為 **Gate 2 前置條件**，不是 Gate 1 條件。
- `traceability.md` 的 `TC-ID` 欄與 NFR 追溯表的 `對應測試` 欄**刻意留空**，由 qa-lead 於測試計畫卡填。孤兒檢查的後兩項（US↔TC）因此尚未成立，**依 Epic 明文不屬 Gate 1 三項判準**。

---

## 7. 本報告的可重現性

本報告的每一個數字都來自實際執行的指令，指令與**真實輸出**完整貼在 `worklog/handoff/20260919-0637-T0005-r1-plan-ba.md` B 段「驗證方式」。
逐 US 覆蓋度的展開比對腳本（會把 SD 的 `UC-00a~UC-00b` 範圍寫法展開後反查）亦保留在該段，接手者可直接重跑。
