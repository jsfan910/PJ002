# Code Review 報告：T-0020 P0 全量 Code Review（E-001）

<!-- 檔名：docs/reports/20260919-1146-CR-E001.md -->
<!-- 通過門檻：阻擋級 = 0。建議級由 dev-tl／Leader 決定是否本輪修。 -->

- 審查者：qa-cr（round 1）
- 審查範圍：`main` HEAD `153410b`（程式碼內容 = `df4071b` 之後 T-0011～T-0018 合併後的最終狀態）
  - 受審檔案：`src/**`（14 檔）、`public/**`（5 檔）、`tests/**`（10 檔）、`migrations/001_create_todos.sql`、`Dockerfile`、`docker-compose.yml`、`.github/workflows/**`（3 檔）、`infra/cloudrun-service.yaml`、`scripts/**`（3 檔）、`package.json`、`tsconfig.json`、`eslint.config.js`、`.env.example`、`.gitignore`、`README.md`
- 對照文件：`docs/specs/03_系統設計書_SD.md` §3／§5／§6／§7／§8、`docs/specs/04_API規格.yaml`、`docs/specs/05_資料庫設計.md` §2／§3／§4、`docs/specs/02_系統分析書_SA.md` §5（BR）、`docs/specs/adr/ADR-0001~0005`、`tasks/E-001-todo-app.md`（Leader 裁決 D-01／O-001~O-009／Q-001~Q-011）
- 結論：**退回**（阻擋級 **2** 項、建議級 **11** 項）

---

## 結論：阻擋級 = 2

| # | 阻擋項 | 一句話 |
|---|---|---|
| B-1 | `/health` 豁免比對做了 URL 正規化，點節段路徑可繞過全站 Basic Auth | `src/plugins/basic-auth.ts:45` |
| B-2 | migration 未在應用啟動時自動套用，README 方式一起來的環境所有待辦 API 回 500 | `src/server.ts:10`／`Dockerfile:49`／`docker-compose.yml:25` |

兩項皆已**實跑重現**，輸出見「驗證輸出」§5、§6。建議的修正卡切分見本報告最後一節。

---

## 發現

### 阻擋級

| # | 嚴重度 | 檔案:行號 | 說明 | 修正建議 |
|---|---|---|---|---|
| B-1 | **阻擋** | `src/plugins/basic-auth.ts:45-52`（`extractPathname`／`isExemptPath`），影響 `:74-85` 的 onRequest hook | 豁免比對用 `new URL(rawUrl, "http://localhost").pathname`。WHATWG URL 解析器會**正規化點節段，且會先把 `%2e` 解碼為 `.` 再判斷**，因此 `/foo/../health`、`/.%2e/health`、`/%2e%2e/health`、`/assets/%2e%2e/%2e%2e/health` 的 pathname 都等於 `/health`，**未帶憑證即通過 onRequest hook**（實測回 403，403 是 `@fastify/static` 的路徑穿越防護擋下的，不是 401）。這牴觸 SD §6.2 列為「實作硬性要求（CR 判準）」的「豁免比對**必須是完全相等的字串**，禁止前綴比對、萬用字元或正則」——此處比對的是**正規化後**的字串而非請求原文；也正是任務卡背景「特別檢查：`/health` 豁免比對（pathname、**點節段**）」要抓的情形。目前尚未取得受保護內容（路由比對走的是**原始** URL，不存在「原始等於 `/api/v1/todos`、正規化等於 `/health`」的字串），但認證邊界現在只靠第三方 `send` 的穿越防護守住，且只要日後新增任何靜態設定或路由形狀就可能變成真實曝露 | 豁免比對改成**只剝除查詢字串與 hash，不做任何路徑正規化**：`const q = rawUrl.indexOf("?"), h = rawUrl.indexOf("#"); let end = rawUrl.length; if (q >= 0) end = Math.min(end, q); if (h >= 0) end = Math.min(end, h); return rawUrl.slice(0, end) === "/health";`。同時在 `tests/integration/auth-basic.test.ts` 補四個點節段變體（`/foo/../health`、`/.%2e/health`、`/%2e%2e/health`、`/assets/%2e%2e/%2e%2e/health`）斷言 **401** |
| B-2 | **阻擋** | `src/server.ts:10-20`（未呼叫 `runMigrations`）、`Dockerfile:49`（`CMD ["node","dist/server.js"]`）、`docker-compose.yml:25-41`（app 服務無 migrate 前置）、`README.md:24-33`（「方式一：Docker Compose（推薦）」無 migrate 步驟） | `docs/specs/05_資料庫設計.md` §3「migration runner 行為規格」第 4 條明文「於**應用啟動時自動執行**（NFR-008：使用者不需手動下 migration 指令）」，SD §7 NFR-008 對策④同文。實作把 runner 寫在 `src/db/migrate.ts`（`T-0013` 交接檔自陳「尚未接進啟動流程」），啟動流程從未呼叫它。**實測**：對乾淨資料庫啟動應用後 `GET /api/v1/todos` 回 500（`relation "todos" does not exist`）。加重情形：`.env` 的 `DATABASE_URL` 指向 compose 內部主機名 `db:5432`，使用者在宿主機執行 `npm run migrate` 會連不上，而 README 沒有提供在 compose 內執行 migration 的指令——**使用者依 README 走完全部步驟，拿到的是一個所有待辦功能都 500 的服務**，NFR-008「15 分鐘、卡關點 0」不可能成立。CI 之所以全綠是因為 `ci.yml:98` 在 `test:integration` 之前單獨跑了一次 `npm run migrate`，恰好掩蓋了這個缺口 | 二擇一並補 README：①（建議）`src/server.ts` 在 `app.listen` 之前呼叫 `runMigrations(createPool(config))`，失敗即以非 0 結束（同時滿足 05 §3 第 4 條字面要求）；② `docker-compose.yml` 的 app 服務改用 `command: sh -c "node dist/db/migrate.js && node dist/server.js"`。另外務必在 `tests/integration/` 補一條「對未套用 migration 的資料庫啟動後 `GET /api/v1/todos` 不得為 500」的回歸測試，否則此缺口會再次被 CI 的獨立 migrate 步驟掩蓋 |

### 建議級

| # | 嚴重度 | 檔案:行號 | 說明 | 修正建議 |
|---|---|---|---|---|
| S-1 | 建議 | `src/schemas/todo-schema.ts:55-82`；生效點在 `src/routes/todos.ts:70-100` | Fastify 的 ajv 預設 `coerceTypes: true`，因此 `POST /api/v1/todos` 送 `{"title": 123}` 會被**脅迫成字串 `"123"` 並回 201 建立成功**（實測）。`04_API規格.yaml` 的 `CreateTodoRequest.title` 宣告為 `type: string`，嚴格讀應回 400。同理 `{"isCompleted": null}` 會被脅迫為 `false` | 於路由註冊處關閉脅迫（`app.setValidatorCompiler` 或建立 app 時 `ajv: { customOptions: { coerceTypes: false } }`），或由 Leader 裁決「接受 ajv 預設脅迫」並在 04 規格註記，避免下一輪 CR 重新論證 |
| S-2 | 建議 | `src/plugins/basic-auth.ts:50-52`、`74-78` | 豁免**不分 HTTP 方法**。SD §5 表格與 §6.2「未保護路徑清單」寫的是 **`GET /health`**。實測 `POST /health` 未帶憑證回 404（而非 401），等於未認證者可以探測路由是否存在 | `isExemptPath` 改為同時檢查 `request.method === "GET" \|\| request.method === "HEAD"` |
| S-3 | 建議 | `.github/workflows/deploy-staging.yml:139-141` | secrets 被 `${{ }}` 直接展開進 `run:` 的命令列（`curl -u "${{ secrets.STAGING_BASIC_AUTH_USER }}:${{ secrets.STAGING_BASIC_AUTH_PASSWORD }}"`）。同一檔 `:75-83` 的 migrate 步驟已用正確寫法（`env:` 宣告後以 `"$VAR"` 引用），兩種寫法並存代表這是疏漏而非取捨。GitHub 官方 hardening 指南要求 `run:` 內一律經中介環境變數 | 改為 `env: { BASIC_USER: ${{ secrets.STAGING_BASIC_AUTH_USER }}, BASIC_PASS: ${{ secrets.STAGING_BASIC_AUTH_PASSWORD }} }` 後以 `-u "$BASIC_USER:$BASIC_PASS"` 引用 |
| S-4 | 建議 | `tsconfig.json:21-22`（`include: ["src/**/*.ts"]`、`exclude: [..., "tests"]`） | `npm run lint` 的 `tsc --noEmit` **完全不檢查 `tests/`**。10 個測試檔的型別錯誤要到執行期才炸，等於「測試品質」的靜態防線缺一角 | 新增 `tsconfig.test.json`（`extends` 主檔、`noEmit: true`、`include: ["tests/**/*.ts"]`），並把 `npm run lint` 改成 `eslint . && tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.test.json` |
| S-5 | 建議 | `public/assets/todo-store.js:126-137`（`load`），受影響者 `:143-220` 全部 action | `load()` 沒有請求序列化。快速連續切換篩選（或連點 toggle）時，先發出的 `listTodos` 回應可能後到並覆蓋較新的結果，畫面內容與 `state.filter` 不一致——BR-011「事實來源永遠是後端」在競態下被破壞 | 在 store 內維護單調遞增的 `requestSeq`，`load()` 開頭取號、`await` 回來後比對號碼，只有最新一次才 `setState({todos})` |
| S-6 | 建議 | `public/assets/todo-view.js:146-186`（`renderEditingItem`）、`:257-263`（`render`） | 編輯模式期間任何 store 通知都會整段重繪該 `<li>`，輸入框被重建並回復為 `todo.title`，**使用者已鍵入但尚未儲存的文字會被吃掉**（例如編輯中另一筆的 toggle 成功觸發 `load()`） | 在模組層保存 `editingDraft` 字串，`renderEditingItem` 以 `editingDraft ?? todo.title` 初始化 `input.value`；或編輯期間跳過該筆的重繪 |
| S-7 | 建議 | `.github/workflows/monitor-health.yml:47` | `curl -fsS ... \|\| echo "000 0"`：`-f` 會讓任何 4xx／5xx 直接失敗，於是**真實狀態碼被丟掉、一律記成 `000`**。SD §7 NFR-003 的「判讀規則」要求能區分應用缺陷與平台事件，`000` 讓 CSV 失去這個能力 | 拿掉 `-f`（`curl -sS -o /dev/null -w "%{http_code} %{time_total}"` 本來就會輸出實際狀態碼），僅在 curl 真的連不上時才落到 `000` |
| S-8 | 建議 | `.env.example:21-31`（3 處佔位值）；對照 `docs/specs/03_系統設計書_SD.md:261` | SD §6.2 寫「`.env.example` **只寫名稱**」，實作寫了 `dev/dev` 等值。這已由 Leader 裁決 **D-01** 明文允許（`tasks/E-001-todo-app.md:102`），但**凍結規格的文字沒有同步**，等於留下一處「規格與實作字面矛盾」給下一輪 CR 或 P1 的人重新論證 | Leader 走一次規格變更請求，把 D-01 回寫進 SD §6.2 該行（或加註「見 D-01」） |
| S-9 | 建議 | `infra/cloudrun-service.yaml:36-103` vs `.github/workflows/deploy-staging.yml:96-114` vs `scripts/deploy-staging.sh` | Cloud Run 參數在三處各寫一份。目前三者一致，但 `livenessProbe` 只存在於 IaC 檔、`--allow-unauthenticated` 只存在於兩支 gcloud 指令，已經開始飄移。T-0018 交接檔「假設與決策 5」已自陳此取捨 | 在 `06_部署架構與CICD.md` 加一張「三處必須同步的參數表」，或在 CI 加一步用 `gcloud run services describe` 比對實際 revision 與 IaC 檔 |
| S-10 | 建議 | `docker-compose.yml:15-16` | db 服務把 5432 直接綁死到宿主機。本機多專案／多 worktree 並行時必撞埠（T-0018 交接檔已實際遇到並自行改用 5433 迴避） | 改為 `"${POSTGRES_HOST_PORT:-5432}:5432"`，並在 `.env.example` 加一行說明 |
| S-11 | 建議 | `package.json:11`（`"dev": "node --watch src/server.ts"`）、`:18`（`"verify:health": "node scripts/verify-health.ts"`）、`:8`（`engines.node: ">=22"`）；README 常用指令表 `README.md:85` | `npm run dev` **必然失敗**：`src/server.ts` 以 `./app.js` 規格匯入，Node 的型別剝除不會對應回 `.ts`（實測 `ERR_MODULE_NOT_FOUND: ... src\app.js`）。README 的「已知平台限制」已對 `migrate`／`test` 記了同一原因，卻漏了 `dev`，而 `dev` 仍列在常用指令表中無任何但書。另 `verify:health` 直接以 `node` 跑 `.ts`，依賴 Node 22.18+ 才預設開啟的型別剝除，`engines` 只寫 `>=22` | `dev` 改為 `tsc -w` ＋ `node --watch dist/server.js` 兩段（或 `node --watch --experimental-strip-types` 搭配把匯入改回無副檔名不可行，故建議前者）；`engines.node` 收緊為 `>=22.18`；README 常用指令表為 `dev` 補上與 `migrate` 相同的但書 |

嚴重度定義：
- **阻擋**：正確性錯誤、安全漏洞、違反設計書／API 規格、資料遺失風險、缺少必要測試。
- **建議**：可維護性、命名、重複、效能非關鍵路徑。

---

## 檢查清單

- [x] **符合 API 規格（路徑、方法、狀態碼、schema）** — 五個端點逐項核對 `04_API規格.yaml`：`GET /api/v1/todos` 回陣列（含空陣列）、`POST` 201、`GET /{id}` 200／404、`PATCH` 200 且至少一欄否則 400、`DELETE` **204 且無本文**（實測回應無 `Content-Type`、無 body）、`/health` 200 固定 `{"status":"ok"}`、401 附 `WWW-Authenticate: Basic realm="staging"`（逐字相符，未附 `charset`）。`updatedAt` 未進回應契約（O-008）✓。UUID 格式錯 400、格式對查無資料 404（SD §5 第 6 點分界）✓。無任何 CORS 標頭（實測 `curl -I` 無 `access-control-*`，O-004／BR-015）✓。**唯一偏差**：請求型別脅迫，見 S-1。
- [x] **符合資料庫設計（欄位、約束、索引）** — `migrations/001_create_todos.sql` 與 05 §3 DDL 草案逐行一致：六欄、`CHECK (char_length(btrim(title)) BETWEEN 1 AND 200)`、`idx_todos_created_at_desc (created_at DESC, id DESC)`、`created_at` 不可變觸發器並同時維護 `updated_at`、`is_completed` 刻意不建索引、無 `deleted_at`／`completed_at`／`description` 等被裁決排除的欄位。`schema_migrations(version text PK, applied_at timestamptz)` 與 05 §2 一致。migration 為 forward-only、無 `down`、runner 於單一交易內執行檔案全文後才寫版本（`src/db/migrate.ts:88-102`）✓。**唯一偏差**：runner 未接進啟動流程，見 B-2。
- [x] **錯誤處理與輸入驗證** — BE-03 為唯一錯誤出口（`src/plugins/error-handler.ts:142-145` 直接對根實例 `setErrorHandler`／`setNotFoundHandler`，未經 `app.register` 子情境，因此涵蓋 auth／static／routes 三個手足）；`E_INTERNAL` 訊息為固定字面值（`:36`、`:95`、`:113`），5xx 一律無 `details`（`src/schemas/error-schema.ts:58`、`:74` 雙重強制）；驗證 `details` 只含 `{field, rule}` 不回射輸入原文（`:57-72`）。雙層驗證齊備：schema `minLength/maxLength/format/minProperties`（`todo-schema.ts`）＋ 業務層 **trim 早於長度檢查**（`todo-service.ts:43-58`，實測 201 字元回 400 而非 500）＋ DB CHECK 第三道。
- [x] **安全（注入、認證授權、敏感資料不落日誌）** — SQL **全參數化**：`todo-repository.ts` 五段 SQL 皆為純字面常數，`grep` 檔內無 `${` 插值（05 §3 單一判準）✓，值一律走 `values` 陣列。Basic Auth 以 `crypto.timingSafeEqual` 常數時間比對，長度不同時仍跑一次等長比較（`basic-auth.ts:33-41`）✓。hook 掛根實例，靜態資產未帶憑證實測 401 ✓。靜態檔 `Cache-Control: no-store, private`（實測）✓。pino `redact: ["req.headers.authorization"]`（`app.ts:32`）✓，且實測日誌中無任何 Authorization 內容。5xx 的堆疊／SQL 只進日誌、回應本文不含（實測：日誌有 `relation "todos" does not exist` 與完整堆疊，回應只有 `{"code":"E_INTERNAL","message":"Internal Server Error","requestId":"req-2"}`）✓。`requestId` 與 pino `reqId` 同值（實測皆為 `req-2`，NFR-005）✓。UUID 不洩漏：`/health` 只回固定字面值、404／401 皆不含識別碼 ✓。XSS：`grep -rn "innerHTML\|outerHTML\|insertAdjacentHTML\|document.write\|eval("` 對 `public/`＋`src/` **無輸出**，前端全走 `textContent`／`createElement` ✓。**兩項偏差**：B-1（豁免比對）與 S-2（豁免不分方法）、S-3（workflow secrets 進命令列）。
- [x] **單元測試存在且有意義（非只測 happy path）** — 實跑 75 單元 ＋ 47 整合 = **122 項全綠**，覆蓋面確實超出 happy path：trim 後 201 字元、全空白標題、夾帶 `id`／`createdAt` 被忽略、PATCH 空 body、404 不得建立資料、冪等重送、500 筆同秒建立的 tie-break 決定性、DB CHECK 第二道防線、錯誤契約逐欄比對、`requestId`／`reqId` 關聯、前端 store 每個 action 的 `ApiError`／`NetworkError` 失敗分支。**缺口三處**（已各自開在 B-1／B-2／S-4）：① 無任何點節段豁免測試（正是 B-1 沒被攔下的原因，而任務卡「特別檢查」明列「點節段」）；② 無「未套用 migration 時的行為」測試，CI 的獨立 migrate 步驟把 B-2 掩蓋成綠燈；③ `tests/` 不受 `tsc` 檢查。另 `public/assets/todo-view.js` 無自動化測試（無 DOM 測試環境），此點**不適用**於本卡——依 `07_測試計畫.md` 由 qa-at／qa-uat 以真實瀏覽器覆蓋。
- [x] **無硬編碼密鑰／環境值** — 以 dev-tl 補強樣式（含可選引號）對**全部受追蹤檔案**掃描，輸出見「驗證輸出」§4。14 筆命中逐筆判讀後**真實憑證 0 筆**：3 類已知假陽性（`ci.yml` 佔位值 ×3、`id-token: write` ×1、`--set-secrets "ENV=secret-name:version"` ×3）＋ D-01 明文許可的本機佔位值（`.env.example` ×3、`tests/integration/todo-repository.test.ts:37` 的 localhost 預設值 ×1）＋ 純結構假陽性（`src/config.ts:16` 型別宣告、`auth-basic.test.ts:37/39` 讀環境變數與函式參數、`todos-api.test.ts:53` 同、`error-handler.test.ts:110` 刻意偽造的外洩字串用來斷言**不**外洩）。另掃私鑰／服務帳號金鑰樣式（`BEGIN PRIVATE KEY`／`credentials_json`／`AKIA`／`ghp_`／`AIza`）**零命中**；`git ls-files` 確認 `.env` 未進版控、`.gitignore` 已含 `.env` 與 `.env.*`（保留 `!.env.example`）。**建議**：把「D-01 本機佔位值」正式列為第 4 類已知例外（S-8）。
- [x] **遵守 ADR** — ADR-0001（Fastify＋TypeScript＋ESM＋Node 22-alpine，`package.json`／`Dockerfile:17,30` 釘選 ✓）；ADR-0002（PostgreSQL 16、`pg`、無 ORM、參數化、`ORDER BY created_at DESC, id DESC` tie-break、`pg.Pool max: 5` ✓ `src/db/pool.ts:21`）；ADR-0003／0005（Cloud Run：`$PORT` 不寫死 ✓ `src/server.ts:15`＋`config.ts:98`，`min-instances 0`、startup probe `/health`、回滾為 `update-traffic` 切 revision ✓ `scripts/rollback-staging.sh`）；ADR-0004（同源部署、不註冊 CORS 外掛、`CORS_ALLOWED_ORIGINS` 填 `*` 即啟動失敗 ✓ `config.ts:76-78`，前端一律相對路徑 ✓ `api-client.js:17`）。Dockerfile 非 root（`addgroup/adduser`＋`USER appuser`，`:41-43`）✓、多階段、runtime `npm ci --omit=dev` ✓。workflow **無長期金鑰**：只用 WIF（`google-github-actions/auth@v2` ＋ `id-token: write`），`grep -nE "credentials_json|service_account_key|\.json$"` 零命中 ✓。

---

## 對 T-0011～T-0018 開發者「假設與決策」逐條表態

### T-0011 dev-ops（5 條）

| # | 決策 | 表態 | 理由 |
|---|---|---|---|
| 1 | 測試 script 改用明確 glob 取代目錄寫法 | **接受** | 已在本機 Node v24.15.0 重現目錄寫法的失敗；glob 寫法效果等價，兩個 script 實跑皆能遞迴抓到全部測試檔（75＋47 項） |
| 2 | `redact` 由 T-0011 在 Fastify 建構參數先寫入 | **接受** | `redact` 確實只能在建構時指定，等 WI-02 沒有插入點。實測日誌不含 Authorization |
| 3 | Dockerfile 暫不 `COPY public` | **接受（已被後續卡收斂）** | 當時 `public/` 不存在，理由成立；現況 `Dockerfile:26,38` 兩階段皆已補上，無殘留 |
| 4 | `.env.example` 新增三個 compose-only 的 `POSTGRES_*` | **接受** | `postgres:16-alpine` 確實需要這三個變數初始化，且與 `DATABASE_URL` 一致；不影響應用讀取的變數集合 |
| 5 | 以改寫 `config.ts` 型別宣告＋`ci.yml` 加雙引號消除掃描假陽性 | **接受，附但書** | 兩處功能等價已核實。但書：加引號是「讓掃描不命中」而非「讓內容更安全」，這是掃描樣式的弱點；本輪已改用補強樣式（含可選引號）重掃，`ci.yml` 三處仍會命中並被列為已知例外，做法正確 |

### T-0012 dev-be（5 條）

| # | 決策 | 表態 | 理由 |
|---|---|---|---|
| 1 | `registerErrorHandler` 直接對根 `app` 呼叫，不經 `app.register` | **接受（且是本專案的關鍵正確決定）** | Fastify 封裝機制確實會讓子情境的 `setErrorHandler` 對手足外掛失效。實測 auth（hook）、static（404）、routes（400/404/500）三種來源的錯誤皆走同一個 handler、同一個契約結構 |
| 2 | `src/app.ts` 多了一行 import | **接受** | 沒有 import 就無法編譯，是不可迴避的伴隨變更；五個 ANCHOR 與既有行未動 |
| 3 | schema 驗證只取第一條違規 | **接受** | 04 規格的錯誤範例皆為單欄位，SD §6.1 未要求聚合；聚合屬規格變更 |
| 4 | 非 `AppError` 例外依 `statusCode` 對照，非 4xx 一律 `E_INTERNAL` | **接受** | 保守方向正確（誤判只會更嚴格、不會洩漏）。實測 `@fastify/basic-auth` 自身的 401 確實落在這條 fallback 並正確轉成 `E_UNAUTHORIZED` |
| 5 | `E_EMAIL_TAKEN`/409 只預留常數 | **接受** | P1 範圍，任務卡明示 |

### T-0013 dev-be（6 條）

| # | 決策 | 表態 | 理由 |
|---|---|---|---|
| 1 | `setCompleted` 為 `updateTodo` 薄封裝 | **接受** | 05 §3 本來就只給一條 update 語句；共用同一條 `COALESCE` SQL 正是 O-002 冪等語意的來源 |
| 2 | `createdAt` 維持 `Date`，不在 repository 轉字串 | **接受** | 格式化單點落在 `TodoResponseSchema` 的 `format: date-time`；實測回應為 `2026-09-19T03:53:49.086Z`，符合 BR-004，且全鏈路只有一處格式化邏輯 |
| 3 | 以最小介面 `Queryable` 注入而非依賴 `pg.Pool` 型別 | **接受** | 讓 20 個 repository／service 單元測試完全不需 DB，這是本專案單元測試層次乾淨的主因 |
| 4 | migration 用 `pool.connect()` 取單一 client 跑交易 | **接受（必要且正確）** | `pool.query()` 每次可能拿到不同連線，交易必須綁同一條連線。`migrate.ts:88-101` 的 `BEGIN`／檔案全文／`INSERT`／`COMMIT`／例外 `ROLLBACK`／`finally release` 寫法無誤 |
| 5 | 500 筆 tie-break 測試以顯式 `created_at` 插入 | **接受** | 真實時鐘幾乎不可能造出完全相同的微秒時間戳，顯式指定是唯一能可靠逼出 tie-break 的方法；`created_at` 只在 `BEFORE UPDATE` 被觸發器保護，INSERT 顯式指定不違反任何約束 |
| 6 | 額外新增 `tests/unit/todo-repository.test.ts`（超出 outputs） | **接受** | 已在交接檔與任務卡 outputs 同步登錄，非隱藏變更；與整合測試覆蓋層次不同，是無 DB 階段的回歸防線 |

### T-0014 dev-fe（6 條）

| # | 決策 | 表態 | 理由 |
|---|---|---|---|
| 1 | `index.html` 暫不加 `<script>` | **接受（已被 T-0017 收斂）** | 當時腳本不存在；現況 `index.html:86` 已補上單一 module script |
| 2 | `ApiError`／`NetworkError` 兩個具名例外 | **接受** | AC-008-3 需要「伺服器回報的錯誤」與「連不上伺服器」兩種不同文案，兩個類別比單一例外＋錯誤碼更貼合「具名例外」的字面要求，且 store 的 `toDisplayError` 直接受益 |
| 3 | `listTodos()` 省略 `status` 而非明送 `all` | **接受** | 04 規格 `StatusFilter` 有 `default: all`，兩者對後端等價；省略更貼合 BR-010「不寫入網址」的精神 |
| 4 | 204 直接 return `undefined`，完全不呼叫 `.json()` | **接受** | 實測 DELETE 回應確實無 `Content-Type` 無 body，對空本文呼叫 `.json()` 會拋例外；以狀態碼提前 return 是最穩的寫法 |
| 5 | `.add-form__input` 改 `flex:1 1 auto` ＋ `min-width` ＋ `height` | **接受** | `flex-basis` 在主軸方向改變時語意會從寬度變高度，原寫法在 390×844 下確實是缺陷；此修正屬正確性而非偏好 |
| 6 | 不用 `.claude/launch.json` 預覽，改用暫存目錄的最小靜態伺服器 | **接受** | 根目錄必須停在 main 且不得寫入非本卡 outputs；已確認埠號釋放，倉庫內無殘留檔案 |

### T-0015 dev-be（6 條）

| # | 決策 | 表態 | 理由 |
|---|---|---|---|
| 1 | 自行組 `WWW-Authenticate`，不用外掛的 `authenticate` 選項 | **接受** | 外掛會附 `, charset="UTF-8"`，與 ADR-0004 第 1 點與 04 規格的 `Basic realm="staging"` 逐字不符。實測標頭逐字相符 |
| 2 | 認證失敗一律經全域 error-handler 序列化 | **接受** | 對應 dev-tl 在 T-0012 的交辦；外掛自身的 `FST_BASIC_AUTH_MISSING_OR_BAD_AUTHORIZATION_HEADER` 訊息為固定字面值不含內部細節，落到 fallback 分支後仍是合契約的 `E_UNAUTHORIZED`，判斷正確 |
| 3 | `registerBasicAuth`／`registerStatic` 為直接呼叫的一般函式 | **接受** | 與 WI-02 一致；兩個第三方外掛本身以 `fastify-plugin` 打破封裝，hook 確實掛在根實例。實測靜態資產未帶憑證回 401，是這個決定成立的證據 |
| 4 | **豁免用 `new URL(...).pathname` 取 pathname，不用字串切割** | **不接受** | 這正是 B-1。理由本身（「`URL` 是標準 API、比手動字串處理更不易寫錯」）看似合理，但 `URL` 的 pathname 是**正規化後**的結果——它不只剝除查詢字串，還會摺疊 `..`／`.` 並先把 `%2e` 解碼再摺疊，使 `/foo/../health`、`/%2e%2e/health` 等四種形式的請求略過整個認證 hook（實測 403 而非 401）。交接檔自陳的驗證只有 `/health?cb=1` 與 `/health/` 兩例，兩例都不會暴露正規化行為。SD §6.2 的硬性要求是「完全相等的**字串**」比對，而本實作比對的不是請求原文。應改回只剝除 `?`／`#` 的字串切割並補點節段測試 |
| 5 | 靜態託管明確關閉快取（`no-store, private`） | **接受** | 交接檔自陳「可能過度設計」，但受 Basic Auth 保護的內容若被中介快取保留，確實是繞過認證的實質風險；成本為零，方向正確 |
| 6 | `basic-auth.ts` 全檔改用 `//` 行註解以通過 `grep -nE "startsWith\|RegExp\|\*"` | **接受，附但書** | 檔案內容確實不含前綴比對或正則，改註解風格不影響語意。但書：這說明 WBS §1.5 那條 grep 判準是**字面形狀檢查而非語意檢查**——它讓 `basic-auth.ts` 通過了，卻完全抓不到 B-1 這種「用標準 API 做了正規化」的真實繞過。建議 Leader 把該驗收改為「四個點節段變體必須回 401」的行為斷言 |

### T-0016 dev-be（5 條）

| # | 決策 | 表態 | 理由 |
|---|---|---|---|
| 1 | **UUID 格式錯採 400，不採提示詞的 404** | **接受（判斷正確）** | `04_API規格.yaml` 的 `TodoId` 參數說明、SD §5 第 6 點「分界寫死」、WBS §1.6 驗收指令三者一致指向 400，且凍結規格優先於提示詞單行敘述。角色協定「規格就是合約」，此處依合約行事並完整記錄，是正確做法。**Leader 仍宜補一筆裁決追認**，讓下一輪不必重新論證 |
| 2 | 連線池建在 `routes/todos.ts` 並以 `onClose` 收尾 | **接受，附但書** | 不碰非本卡檔案的理由成立，`onClose` 確實讓池的生命週期綁定 app 實例（整合測試每個 case 各建一個 app 也因此不漏連線）。但書：長期而言「資料層生命週期由路由模組擁有」與 SD §3.1 BE-01「app-bootstrap 負責註冊優雅關機、排空連線池」的職責劃分不一致；**若 B-2 依建議①在 `server.ts` 接 migration，順手把池上移到 bootstrap 是自然的收斂點**，可併入同一張修正卡 |
| 3 | UUID 格式驗證留在 schema，service 不重驗 | **接受** | 對應 SD §5 第 6 點的分界；WBS §1.6 也只要求業務層對 `title` 再驗一次 |
| 4 | PATCH「至少一個欄位」在 schema 與業務層各驗一次 | **接受（且必要）** | 不只是防禦性重複：ajv 的 `minProperties` 在 `removeAdditional` **之前**求值，所以 `{"id":"abc"}` 會通過 schema 但被移空。實測確認此時是業務層那一道擋下並回 `E_VALIDATION`（`details.rule = minProperties`）——沒有業務層這一道，這個請求會帶著空 patch 打到 DB |
| 5 | `createdAt` 全程 `Date`，只在 response schema 序列化 | **接受** | 與 T-0013 決策 2 一致，格式化維持單點；實測回應為合規 ISO 8601 |

### T-0017 dev-fe（7 條）

| # | 決策 | 表態 | 理由 |
|---|---|---|---|
| 1 | 不改 `styles.css`，動態元素改用行內 `style`（引用既有 `:root` 變數） | **接受，附但書** | 任務卡 outputs 與角色協定皆禁止改 outputs 以外的檔案，判斷正確。但書：`todo-view.js:42-50` 現在有三份重複的按鈕樣式字串，屬可維護性負債；P1 或樣式卡時應收斂回 CSS class（不必本輪修） |
| 2 | `todo-view.js` 兼任 controller | **接受** | 任務卡 outputs 無獨立 controller 檔，SD §3.2 的資料流圖也只畫到 `todo-view`；檔內已明確分界事件綁定區與 render 函式群，且**實測 `todo-view.js` 未出現任何 `fetch`／api-client 匯入**，單向資料流的硬規則沒有被這個合併破壞 |
| 3 | JS 關閉新增輸入框的原生 `required` | **接受** | 原生提示會繞過 `error-message` 區，與 AC-001-2 的呈現位置不一致；且全空白字串本來就不受 `required` 擋，統一走同一條錯誤路徑是正確的 |
| 4 | 異動失敗時不自動重新載入清單 | **接受（理由紮實）** | `load()` 開頭就 `setState({error: null})`，自動重載確實會在同一 tick 把錯誤訊息洗掉，使用者看不到——這是對自家 store 行為的正確推論，不是偷懶 |
| 5 | 新增／編輯一律先本地 trim 驗證才送出 | **接受** | 對應 SD §6.3 的雙層驗證（前端擋、後端仍驗），也避開「原始 >200、trim 後 ≤200」被 schema 擋成 400 的邊界 |
| 6 | `renderError` 每次重建「重試」按鈕 | **接受** | `textContent = ...` 必然清空子節點，重建是必要的；單一元素的建立成本可忽略，清單才是效能重點且已用 `DocumentFragment` |
| 7 | 以 `.env.example` 複製出的 `dev/dev` 做本機驗證，驗畢 `docker compose down -v` | **接受** | D-01 允許；`git ls-files` 確認 `.env` 未進版控 |

### T-0018 dev-ops（6 條）

| # | 決策 | 表態 | 理由 |
|---|---|---|---|
| 1 | 階段順序依 06 §3.2 逐字（auth → migrate → build&push → deploy → verify） | **接受** | 06 為凍結權威規格且明載 migrate 先行是 expand-contract 的 expand 階段；WBS 的驗收也只要求 migrate 在 deploy 之前。實作符合兩者 |
| 2 | Secret Manager 採小寫連字號命名 | **接受** | 06 §4.1 把此決定留給後續卡而 T-0011 未處理，由實際建立部署 IaC 的卡決定並記錄是合理的歸屬；命名同時避開掃描關鍵字，副作用正向 |
| 3 | README PowerShell 改用 `[System.IO.File]::ReadAllLines()` | **接受** | 已附 PowerShell 5.1 對「UTF-8 無 BOM ＋ 中文註解」誤判編碼的逐字重現（25 行 vs 30 行），這是修掉一個真缺陷而非偏離裁決；且避免在 README 重複列出佔位值造成兩處維護 |
| 4 | `verify:health` 逾時取 06 的 5 分鐘而非 WBS 的 10 分鐘 | **接受** | 06 後於 WBS 修訂且附理由（Cloud Run 冷啟 1–3 秒），取後修訂的凍結文件正確 |
| 5 | `infra/cloudrun-service.yaml` 定位為文件化 IaC，事實來源為工作流參數 | **接受，附但書** | ADR-0005「後果」段確實把取捨授權給 dev-ops，理由（IaC 檔無法自動代入 `${{ github.sha }}`）成立。但書：三處參數已開始飄移，見 S-9 |
| 6 | 本機驗證改用 8081~8084／5433 以避開 T-0017 佔用的埠 | **接受** | 只是測試時的 host 埠參數，未改動 `docker-compose.yml`／README；平行作業下這是正確的協作行為（本輪 qa-cr 亦沿用同一原則，改用 8098／8099 並自建獨立資料庫） |

**合計 40 條，接受 39 條、不接受 1 條（T-0015 第 4 條 → 阻擋項 B-1）。**

---

## 驗證輸出（核實各卡交接檔宣稱）

環境：Node v24.15.0、npm 11.12.1、Windows 11。Postgres 由平行作業（T-0022 qa-uat）已啟動的 `docker compose`（`soft-dev-team-260919-db-1`，healthy）提供，**本輪未 `docker compose down`**，以免中斷他人服務；本輪自建的驗證資料庫 `crcheck` 與自用埠 8098／8099 皆已於收尾清除（見 §6 末）。

### §1 `npm run lint`

```bash
npm run lint
```
```text
> todo-app@0.1.0 lint
> eslint . && tsc --noEmit -p tsconfig.json

（無任何輸出，exit 0）
```

### §2 `npm run build`

```bash
npm run build
```
```text
> todo-app@0.1.0 build
> tsc -p tsconfig.json

BUILD_EXIT=0
dist/ 產物：app.js config.js db/ plugins/ repositories/ routes/ schemas/ server.js services/
```

### §3 `npm run test:unit` 與 `npm run test:integration`

```bash
npm run test:unit
NODE_ENV=test PORT=8080 DATABASE_URL="postgres://dev:dev@localhost:5432/todo" \
  BASIC_AUTH_USER=dev BASIC_AUTH_PASSWORD=dev LOG_LEVEL=silent npm run test:integration
```
```text
# test:unit
ℹ tests 75
ℹ pass 75
ℹ fail 0
ℹ duration_ms 161.7931

# test:integration
ℹ tests 47
ℹ pass 47
ℹ fail 0
ℹ duration_ms 1046.333
```

**核實結論**：各卡交接檔宣稱的 lint／build／unit／integration 全綠**屬實**。合計 122 項測試，本輪重跑零失敗。

### §4 憑證掃描（dev-tl 補強樣式，含可選引號，全部受追蹤檔案）

```bash
# 樣式（寫入暫存檔後以 grep -f 使用，避免 shell 引號問題）：
# (password|secret|database_url|api[_-]?key|token)[[:space:]]*[:=][[:space:]]*["']?[A-Za-z0-9/+_.-]{2,}
git ls-files src public tests migrations infra scripts .github Dockerfile docker-compose.yml \
  package.json .env.example README.md eslint.config.js tsconfig.json \
  | tr "\n" "\0" | xargs -0 grep -nEi -f credscan.txt
```
```text
.env.example:21:DATABASE_URL=postgres://dev:dev@db:5432/todo
.env.example:23:POSTGRES_PASSWORD=dev
.env.example:29:BASIC_AUTH_PASSWORD=dev
.github/workflows/ci.yml:69:          POSTGRES_PASSWORD: "ci"
.github/workflows/ci.yml:81:      DATABASE_URL: "postgres://ci:ci@localhost:5432/todo"
.github/workflows/ci.yml:83:      BASIC_AUTH_PASSWORD: "ci"
.github/workflows/deploy-staging.yml:26:  id-token: write # WIF 用：向 GCP 換取短期憑證
.github/workflows/deploy-staging.yml:114:            --set-secrets "DATABASE_URL=database-url:latest,BASIC_AUTH_USER=basic-auth-user:latest,BASIC_AUTH_PASSWORD=basic-auth-pass:latest"
README.md:230:   ...（--set-secrets 的三個 Secret Manager 名稱）
scripts/deploy-staging.sh:67:  --set-secrets "DATABASE_URL=database-url:latest,BASIC_AUTH_USER=basic-auth-user:latest,BASIC_AUTH_PASSWORD=basic-auth-pass:latest"
src/config.ts:16:  basicAuthPassword: string;
tests/integration/auth-basic.test.ts:37:const PASSWORD = process.env.BASIC_AUTH_PASSWORD;
tests/integration/auth-basic.test.ts:39:function basicAuthHeader(user: string, password: string): string {
tests/integration/todo-repository.test.ts:37:const DATABASE_URL = process.env.DATABASE_URL ?? "postgres://dev:dev@localhost:5432/todo";
tests/unit/todos-api... tests/unit/error-handler.test.ts:110:  const error = new AppError("E_INTERNAL", "DATABASE_URL=postgres://leaked", {
```

逐筆判讀（14 筆）：

| 命中 | 分類 | 判定 |
|---|---|---|
| `ci.yml:69,81,83` | 已知例外①：CI service container 佔位值（Leader 裁決 dev-tl-②） | 非真命中 |
| `deploy-staging.yml:26` | 已知例外②：WIF 必要語法 `id-token: write` | 非真命中 |
| `deploy-staging.yml:114`、`scripts/deploy-staging.sh:67`、`README.md:230` | 已知例外③：`--set-secrets "ENV=secret-name:version"`，右側為 Secret Manager 名稱不是值 | 非真命中 |
| `.env.example:21,23,29` | Leader 裁決 **D-01** 明文許可的本機 compose 佔位值（`dev`／`dev`） | 非真命中（建議正式列為第 4 類例外，見 S-8） |
| `tests/integration/todo-repository.test.ts:37` | 同上性質：localhost 預設連線字串，僅在未設環境變數時生效 | 非真命中 |
| `src/config.ts:16` | 型別宣告（`: string` 的 `string` 是型別關鍵字） | 結構假陽性 |
| `auth-basic.test.ts:37,39`、`todos-api.test.ts:53` | 讀 `process.env.*` 與函式參數名 | 結構假陽性 |
| `error-handler.test.ts:110` | 刻意偽造的外洩字串，用來斷言 500 回應**不**含它 | 結構假陽性 |

```bash
# 私鑰／長期服務帳號金鑰樣式
git ls-files | tr "\n" "\0" | xargs -0 grep -nE "BEGIN (RSA |EC |OPENSSH |PGP )?PRIVATE KEY|AKIA[0-9A-Z]{16}|ghp_[A-Za-z0-9]{20,}|AIza[0-9A-Za-z_-]{20,}"
```
```text
（零命中；唯一含 service_account 字樣者為 deploy-staging.yml:66 的
 service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}，是 WIF 的服務帳號 email 參照，非金鑰）
```

**核實結論**：倉庫中**無任何真實憑證**，各卡交接檔的宣稱屬實。

### §5 阻擋項 B-1 重現：點節段繞過 Basic Auth

```bash
# 以獨立埠 8099 起一份服務，全部請求皆「未帶任何憑證」，curl --path-as-is 不做客戶端正規化
for P in /health "/health?x=1" "/health/" /healthz-secret "//health" \
         "/api/v1/todos/../health" "/foo/../health" "/.%2e/health" \
         "/%2e%2e/health" "/assets/%2e%2e/%2e%2e/health" "/%2e%2e/assets/api-client.js"; do
  printf "%-34s -> " "$P"; curl -sS --path-as-is -o /dev/null -w "%{http_code}\n" "http://localhost:8099$P"
done
```
```text
/health                            -> 200   ← 預期（唯一豁免）
/health?x=1                        -> 200   ← 預期（查詢字串不影響）
/health/                           -> 401   ← 預期
/healthz-secret                    -> 401   ← 預期（無前綴比對）
//health                           -> 401   ← 預期
/api/v1/todos/../health            -> 401   ← 預期（正規化為 /api/v1/health）
/foo/../health                     -> 403   ← 【缺陷】未帶憑證卻不是 401
/.%2e/health                       -> 403   ← 【缺陷】
/%2e%2e/health                     -> 403   ← 【缺陷】
/assets/%2e%2e/%2e%2e/health       -> 403   ← 【缺陷】
/%2e%2e/assets/api-client.js       -> 401   ← 預期（正規化後不等於 /health）
```
```bash
node -e "for (const u of ['/foo/../health','/%2e%2e/health','/.%2e/health']) console.log(JSON.stringify(u),'->',new URL(u,'http://localhost').pathname);"
```
```text
"/foo/../health" -> /health
"/%2e%2e/health" -> /health
"/.%2e/health"   -> /health
```

四個 403 的來源是 `@fastify/static` 的路徑穿越防護——**請求已經越過 Basic Auth 的 onRequest hook**，否則應與其他未帶憑證的請求一樣回 401。

### §6 阻擋項 B-2 重現：乾淨資料庫上待辦 API 全 500

```bash
docker exec soft-dev-team-260919-db-1 psql -U dev -d postgres -c "CREATE DATABASE crcheck"
NODE_ENV=development PORT=8099 DATABASE_URL="postgres://dev:dev@localhost:5432/crcheck" \
  BASIC_AUTH_USER=dev BASIC_AUTH_PASSWORD=dev LOG_LEVEL=info node dist/server.js &
curl -sS -o /dev/null -w "%{http_code}\n" http://localhost:8099/health
curl -sS -u dev:dev -w "\nHTTP=%{http_code}\n" http://localhost:8099/api/v1/todos
```
```text
# /health（未帶憑證）
200

# GET /api/v1/todos（帶正確憑證）
{"code":"E_INTERNAL","message":"Internal Server Error","requestId":"req-2"}
HTTP=500

# 伺服器日誌（同一筆請求）
{"level":50,...,"reqId":"req-2","err":{"type":"DatabaseError","message":"relation \"todos\" does not exist",
 "stack":"error: relation \"todos\" does not exist\n    at ...\\node_modules\\pg-pool\\index.js:45:11\n
 at async listTodos (file:///.../dist/repositories/todo-repository.js:80:20)",
 "code":"42P01",...},"msg":"relation \"todos\" does not exist"}
{"level":30,...,"reqId":"req-2","res":{"statusCode":500},"responseTime":22.75,"msg":"request completed"}
```

這一次執行同時**正面驗證**了四件事（皆通過）：`/health` 不查資料庫仍回 200（NFR-003 對策②）；500 回應本文不含堆疊／SQL／內部路徑（BR-026、NFR-002②）；堆疊與 SQL 只進日誌（SD §6.5）；回應的 `requestId` 與日誌的 `reqId` 為同一值 `req-2`（NFR-005）。

收尾清除：

```text
taskkill /PID 4132 /F      → 成功: 處理程序 PID 4132 已經終止了。
DROP DATABASE crcheck      → DROP DATABASE
netstat | grep 8099        → 8099 free
docker ps                  → soft-dev-team-260919-app-1 Up / soft-dev-team-260919-db-1 Up (healthy)   ← 他人服務未受影響
```

### §7 契約與邊界行為抽驗（對既有 compose 服務，8080）

```bash
curl -u dev:dev -X PATCH -d "{\"id\":\"abc\"}"  .../api/v1/todos/00000000-0000-4000-8000-000000000000
curl -u dev:dev -X PATCH -d "{}"               .../api/v1/todos/00000000-0000-4000-8000-000000000000
curl -u dev:dev "/api/v1/todos?bogus=1"
curl -u dev:dev -X POST  -d "{\"title\":123}"  .../api/v1/todos
curl -u dev:dev -X POST  -d "{bad"             .../api/v1/todos
curl -u dev:dev -X DELETE -D - .../api/v1/todos/<剛建立的 id>
```
```text
PATCH {"id":"abc"}  -> 400 {"code":"E_VALIDATION","message":"at least one of title or isCompleted must be provided",
                            "requestId":"req-5","details":{"field":"body","rule":"minProperties"}}
                        ← 由業務層擋下（schema 的 minProperties 在 removeAdditional 之前求值），
                          證明 T-0016 決策 4「雙重驗證」是必要的而非贅餘
PATCH {}            -> 400 {"code":"E_VALIDATION","message":"body must NOT have fewer than 1 properties",
                            "requestId":"req-6","details":{"field":"(root)","rule":"minProperties"}}
GET ?bogus=1        -> 200（未宣告查詢參數被 removeAdditional 移除；規格未禁止）
POST {"title":123}  -> 201 {"id":"63805ac8-...","title":"123","isCompleted":false,"createdAt":"2026-09-19T03:53:49.086Z"}
                        ← 【S-1】ajv coerceTypes 預設 true，數字被脅迫成字串
POST 壞 JSON        -> 400 {"code":"E_VALIDATION","message":"Body is not valid JSON but content-type is set to 'application/json'",...}
DELETE              -> HTTP/1.1 204 No Content（無 Content-Type、無 body）✓ O-001
```
上述測試建立的那一筆待辦已於同一輪以 DELETE 清除，未殘留於 T-0022 的驗證資料。

### §8 XSS 與 SQL 拼接的單一判準

```bash
grep -rn "innerHTML\|outerHTML\|insertAdjacentHTML\|document.write\|eval(" public/ src/
grep -n "\${" src/repositories/todo-repository.ts   # SQL 字串內的樣板插值
```
```text
NO MATCH (good)
（repository 內 SQL 字串皆為純字面常數，無任何 ${ 插值）
```

### §9 `npm run dev` 實測（S-11）

```bash
node src/server.ts
```
```text
Error [ERR_MODULE_NOT_FOUND]: Cannot find module '...\src\app.js' imported from ...\src\server.ts
```

---

## 建議的修正卡切分（Leader 建卡用）

| 建議卡 | 含括發現 | 角色 | 為何這樣切 | 驗收建議 |
|---|---|---|---|---|
| **修正 ①：`/health` 豁免比對改為原文字串比對** | **B-1**、S-2 | dev-be | 兩者都只動 `src/plugins/basic-auth.ts` 的同一個函式與同一段 hook，一起改一起測最省來回；且都屬「豁免條件的精確定義」同一個心智 | `tests/integration/auth-basic.test.ts` 新增 5 條斷言：`/foo/../health`、`/.%2e/health`、`/%2e%2e/health`、`/assets/%2e%2e/%2e%2e/health` 皆回 **401**，`POST /health` 回 **401**；既有 `/health`、`/health?cb=1` 仍 200，`/health/`、`/healthz-secret` 仍 401 |
| **修正 ②：啟動時自動套用 migration** | **B-2**、T-0016 決策 2 的但書（連線池上移） | dev-be（或 dev-ops，視採①或②方案） | 這是「應用啟動流程」單一職責；若採 `server.ts` 方案，順手把 `createPool` 從 `routes/todos.ts` 上移到 bootstrap，可同時收斂 SD §3.1 BE-01 的職責劃分，避免兩次改同一區 | 對**全新**資料庫執行 `docker compose up -d`（不另跑 migrate）後，`GET /api/v1/todos` 回 **200 `[]`**；新增一條整合測試覆蓋此情境；README「方式一」步驟不需新增任何 migrate 指令即可走通 |
| **修正 ③：CI／腳本與開發體驗清理（小卡，可合併進 ①②任一輪）** | S-3、S-4、S-7、S-10、S-11 | dev-ops | 全部是 workflow／`package.json`／`tsconfig`／`docker-compose.yml` 的小改，彼此無耦合，合一張卡比開四張省 | `npm run lint` 涵蓋 `tests/`；`npm run dev` 可實際啟動；`grep -n "secrets\." .github/workflows/deploy-staging.yml` 確認 `run:` 內不再出現 `${{ secrets.* }}` |
| **修正 ④：前端競態與編輯草稿（可延至 P1）** | S-5、S-6 | dev-fe | 兩者都在 store／view 的同一條「重繪時機」路徑上 | 快速連切三次篩選後，清單內容與 `aria-pressed` 為真的按鈕一致；編輯中觸發他筆 toggle，編輯框內已鍵入文字不被清掉 |
| **規格同步（非程式碼，Leader 直接處理）** | S-8、S-9、T-0016 決策 1 的追認 | Leader／plan-sd | 三件都是「文件與裁決的一致性」，不需要開發者 | SD §6.2 對 `.env.example` 的敘述與 D-01 一致；06 有三處 Cloud Run 參數的同步表；裁決紀錄新增一列追認「UUID 格式錯 = 400」 |

**S-1（型別脅迫）** 需先由 Leader 裁決方向（關閉 `coerceTypes` 或接受並在 04 規格註記），再決定是否併入修正 ①。
