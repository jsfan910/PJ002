# 測試總結報告（驗收簽核）：E-001 待辦事項 Web 應用 **round 4（D-017 關閉、D-018 立案、TC-090 有效重測）**

<!-- 檔名：docs/reports/20260920-2145-測試總結-E001-r4.md -->

- 提報者：qa-lead（Claude Opus 5 (1M context)，T-0046 r1）
- 時間：2026-09-20T21:44:04+08:00（系統實查）
- 取代：`docs/reports/20260920-1806-測試總結-E001-r3.md`（r3）。**凡與本報告衝突者以本報告為準**；r3 未變更的結論在本報告以「沿用 r3」明確標註，不重新量測也不重新宣稱。
- 受測版本：`main` `9c471a7`（程式碼落點見下）
- 受測環境：**staging** `https://todo-app-dpevsdhdva-de.a.run.app`（Cloud Run `asia-east1`，Neon Postgres），判讀當下流量 100% 在 revision **`todo-app-00020-z8j`**（§7.1 實查）
- **建議：有條件可發布**（條件見第 5 節）。依 `07_測試計畫.md` §4 的**字面**規定仍為「不可發布」（第 1 項 99.05% < 100%），兩種讀法的差異已由使用者於 2026-09-20T18:35 的 Gate 2 r2 裁決① **A（有條件通過）** 處置，本輪不重開此爭點。

---

> ## 本輪與 r3 的實質差別（四件事）
>
> 1. **D-017 關閉**：使用者於自有機器對 staging（revision `00019-k5q`）重跑 TC-067／TC-009 × 4 project × 5 輪，**40／40 全過**，達成 Leader 所定「至少 5 次連續全過」的關閉條件。**TC-009、TC-067 由「部分通過」改判通過。**
> 2. **D-018 立案並關閉（S2）**：`deploy-staging` 的 verify 只打 service URL、不驗新 revision 是否接到流量，使 **15 個 revision 連續 28 小時 11 分未上線而每次 run 全綠**。T-0045 已修（主判準改讀 `status.traffic` 加總 = 100），並經**兩次**真實部署驗證。
> 3. **退出準則第 12 項換證據**：T-0031 的「revision `00008` 切換 0 秒不可用」量測**無效**（流量根本沒切），AT r3 的 TC-090 兩次量測**存疑**；改引 T-0045 事故復原後的真實部署重測 **226／226 樣本、0 秒**。
> 4. **第 1 項由 102／105（97.14%）升為 104／105（99.05%）**，唯一缺口為 **TC-091 Firefox 兩組（工具限制，非產品缺陷）**。
>
> ## 本輪新發現的兩件事（r3 未記載，會改變判斷）
>
> - **A. 現行上線的前端程式碼，其完整回歸證據不存在。** r3 的成立前提是「r2 → r3 之間未改任何 `src/`／`public/` 程式碼」。**這個前提在 r4 已不成立**：T-0038 改了 `public/assets/todo-store.js`（+79／−11 行），T-0043 新增 `public/favicon.ico` 並改 `public/index.html`。而 AT r3、UAT r4、NFR-001／NFR-007 的 staging 數字全部量在 **`00003-lt2`**（2026-09-19 16:05 的 main，**不含**這兩項修正）之上。現行碼在 staging 上實際被重跑過的，**只有** TC-067／TC-009 這兩條（40／40）。詳見 §4 R-14。
> - **B. `QA Tests` workflow 已連續 8 次紅燈**（run #17 ~ #24，2026-09-20T10:51:38Z 起至今）。`integration (tests/integration/qa)` job 失敗，`e2e`／`perf` 兩個 job 為 `workflow_dispatch` 專用故於 push 事件 `skipped`。**關鍵事實：轉紅前後的程式與測試碼完全相同** —— `git diff --name-only e948d54 6b27611 -- src public tests package.json package-lock.json .github/workflows/qa-tests.yml` 輸出**為空（0 行）**，因此**不是本 repo 的程式碼回歸**；但失敗訊息需 repo admin 權限才能讀（匿名 API 403），**本卡無法定根因，故不立缺陷**，列 §4 R-16 與 §6 裁決事項 A。
>
> **這兩件事都不改變任何一格退出準則的「達成／未達成」判定**，但它們改變了**若干格數字的證據強度**，因此逐格在「本輪來源」欄標註清楚，不以「沿用」掩蓋前提已變。

---

## 1. 退出準則達成情況

依 `docs/specs/07_測試計畫.md` §4 逐條對照。**第 1 ~ 7 項為擋關項**（第 2 項除外，P1 於 Gate 2 後執行）。「本輪來源」欄標明該格數字是**本輪實測**、**本輪改判**還是**沿用 r3**。

| # | 門檻 | 目標 | 實際值 | 本輪來源 | 達成 |
|---|---|---|---|---|---|
| 1 | **P0 測試案例通過率** | 100%（分母 105） | **通過 104、部分通過 1、阻擋 0、不通過 0 → 104 / 105 = 99.05%**。<br>部分通過 1：**TC-091**（相容性 4／6，Firefox 兩組工具限制）。<br>**本輪改判 2 條**：TC-009、TC-067 由「部分通過」轉**通過**（D-017 關閉，40／40）。<br>**r3 → r4 之間另有 1 條改判**：TC-080 由「部分通過」轉**通過**（使用者 Gate 2 r2 裁決②追認，2026-09-20T18:35，已落 `20_測試案例.md` 與 `traceability.md`）。<br>分解見 §1.1 | **本輪改判**（TC-009、TC-067）＋ **沿用使用者裁決**（TC-080）；其餘 102 條沿用 r3 | ❌ **未達標**（缺口 1 條，**且該條不是產品缺陷**：Firefox 二進位檔於 Windows `CreateProcess` 層級啟動失敗） |
| 2 | P1 測試案例通過率 | ≥ 95%（Gate 2 後執行，不擋關） | **未執行**：TC-106 ~ TC-123 共 18 條，已執行 **0 條**。P1 開卡待 Leader 裁決（Gate 2 r2 裁決③ A：先清待辦再開 P1） | 沿用 r3 | ➖ 不適用（依計畫略過） |
| 3 | **阻擋級 CR 發現** | 0 筆 | **0 筆**。<br>**但證據基礎已變（r3 的說法在本輪不再成立）**：現行 CR 仍為 r2（`20260919-1309-CR-E001-r2.md`，覆蓋到 `b4b9d41`）；其後 **T-0038／T-0043 改動了 `public/assets/todo-store.js`、`public/index.html`、`public/favicon.ico`**，這三處**未經 qa-cr 正式輪次**，只經 dev-tl 初審。<br>**本卡據 `07` §5.2 逐條核對後仍判 0 筆**：§5.2 列舉的阻擋級判準為 BR-003／009／012／013／016／018／020／022／023／026、SD 6.3（SQL 樣板字串插值）、SD 6.2（豁免比對非完全相等字串），而 T-0038 的變更範圍是**前端跨動作操作序號（`opSeq`／`beginOp`）取代 `requestSeq`**、T-0043 是**新增一個靜態檔與一行 `<link>`**，兩者皆不觸及上述任一條。dev-tl 初審另實跑了 lint、unit 78／78、`/health` 豁免七變體（`/./health`、`/%2e/health`、`//health`、`/health/`、`/HEALTH` 皆 401）與密鑰掃描（無命中） | **本輪重新論證**（r3 的「程式未變更故沿用」已失效） | ✅ 達成（**列 R-14：現行前端碼未經 qa-cr 正式輪次**） |
| 4 | **無 S1／S2 缺陷處於開放狀態** | 0 筆 | **開放 S1 = 0 筆、開放 S2 = 0 筆**（`docs/specs/24_缺陷清單.md`，本輪登錄 **18 筆** D-001 ~ D-018）。<br>S1 一筆（D-001）`closed`；**S2 兩筆（D-002、D-018）皆 `closed`** —— D-018 為本輪新立，T-0045 已修並經真實部署驗證。<br>**開放 S3 = 1 筆**（僅 D-014 migration 互斥鎖，經 Leader 裁決列 P1、不擋 Gate 2）；**D-017 本輪關閉**，開放 S3 由 2 降為 1。開放 S4 = 0 | **本輪更新**（D-017 closed、新增 D-018） | ✅ 達成 |
| 5 | **UAT：全部 P0 US 通過** | 10 / 10 | **10 / 10 通過，且為 staging 真實環境**（UAT r4 `20260919-1656-UAT-E001-r4.md`）。r3（haiku）報「8/10 阻擋」之結論已由 Leader 證偽為工具操作問題，**本報告不採信 r3**。<br>**本輪附註**：UAT r4 執行於 2026-09-19，當時 staging 服務的是 `00003-lt2`，**不含 T-0038／T-0043 的前端變更**，未對現行碼重做（列 R-14） | 沿用 r3 ＋ **本輪補註適用範圍** | ✅ 達成 |
| 6 | **NFR-003：24 小時採樣成功率** | ≥ 99%（連續 24 小時） | **100.0000%**（1156 / 1156 次取樣，原始資料點 6936 / 6936 `true`、`false` 0）。窗 `2026-09-19T09:55:07Z ~ 2026-09-20T10:04:05Z`（24h08m58s）；4 個檢查地區各 289 次；逐地區最大取樣間隔 270 ~ 280 秒 < 300 秒週期，無漏採。<br>**本輪必要附註（因 D-018）**：該 24 小時窗內，staging 實際服務的**自始至終是 `00003-lt2`**（流量釘死期間）。因此這 100% 證明的是「**P0 程式在 staging 連續 24 小時可用**」—— `00003-lt2` 即 2026-09-19 16:05 的 main，含 P0 全功能與 CR r2 全部修正，故**結論對 P0 成立、不因事故失效**；但它**不是**對現行 `00020-z8j` 的 24 小時可用性量測 | 沿用 r3（數字未重測）＋ **本輪新增適用範圍附註** | ✅ **達成（100% ≥ 99%）** |
| 7 | **README 15 分鐘內跑起** | ≤ 15 分 00 秒且卡關點 0 | **方式二（本機 Node）：0 分 14 秒，卡關點 0 個**；**方式一（Docker Compose）：0 分 27 秒，卡關點 0 個**（AT r1／r2 逐步計時，乾淨目錄 `git archive HEAD`）。**qa-lead 已核對 qa-at 記錄之實際耗時數字**（14 秒／27 秒，非「大致 OK」），對 900 秒門檻裕度分別為 64 倍與 33 倍。staging 段（TC-081）由 T-0027 真實部署佐證，Leader 2026-09-19T17:30:10 裁決③認可 | 沿用 r3 | ✅ 達成 |
| 8 | **NFR-001 效能** | 清單 P95 < 500ms；寫入 P95 < 800ms | **staging 實測**（AT r3，`-c 10`、暖身 10 秒後取樣 60 秒）：**GET 清單 P95 = 156.4 ms**、**POST P95 = 315.6 ms**、**PATCH P95 = 158.5 ms**、**DELETE P95 = 316.9 ms**。四段全 PASS，裕度 ≥ 2.5 倍。<br>**本輪附註**：量測於 `00003-lt2`；T-0038／T-0043 只改前端靜態資產，**未動 `src/`**（`git diff --name-only e948d54 HEAD -- src` 為空），故後端 API 的 P95 結論不受影響 | 沿用 r3 ＋ **本輪核對變更範圍** | ✅ 達成（staging 實測） |
| 9 | **NFR-007 容量** | 500 筆下清單 API < 2 秒、前端可互動 < 3 秒 | **清單 API（staging 實測）：5 次中位數 116.2 ms**（陣列長度 714 筆）→ PASS。**前端可互動：staging 未在 500 筆情境下量測**；本機實測 **166 ms**（502 筆）→ PASS | 沿用 r3 | ✅ 達成（**前端 500 筆子項仍僅本機實測**，列殘留風險 R-4） |
| 10 | **NFR-004 相容性 6 組全過** | 6 / 6 | **4 / 6**。Chromium 1280×800／390×844 ＋ Edge 1280×800／390×844 四組通過（staging）。**Firefox 2 組仍阻擋**：T-0041 於本機 docker compose 重跑，**68／68 全部於啟動階段失敗**（`browserType.launch: spawn UNKNOWN`）；已 `--force` 重新下載 122 MiB 二進位檔排除損毀假設、同環境 Chromium 對照組 4／4 通過、以 Node `child_process.spawn` 直呼 `firefox.exe` 重現同一錯誤（`errno: -4094`），確認為 Windows `CreateProcess` 層級失敗。**判工具／執行環境限制，非產品缺陷**（E-B-09） | 沿用 r3 ＋ **本輪納入 T-0041 新證據** | ❌ 未達標（非擋關項；Leader 2026-09-19T12:32:40 已裁決於非沙盒環境補跑，T-0041 `blocked`，留待使用者於自有機器執行） |
| 11 | **追溯矩陣孤兒檢查六項皆「無」** | 六項皆「無」 | **六項皆「無」**。**本輪在改完 traceability 之後重跑**：主表 13 列、UC／SD 模組／API 端點／TC-ID 四欄為 `—` 的列數皆 **0**；US 集合 `US-SET-IDENTICAL-OK`、UC 集合 `UC-SET-IDENTICAL-OK`、TC 集合 `TC-SET-IDENTICAL-OK`（123 vs 123，孤兒 TC 無、捏造 TC 無、重複引用 TC 無）；模組集合雙向差集皆空（21 vs 21）。逐 US 條數 13／10／12／9／11／12／7／7／13／11／6／6／6 = 123，與 r3 逐一相符。實際輸出見 §7.3 | **本輪重跑** | ✅ 達成 |
| 12 | **部署不可用時間 < 60 秒** | < 60 秒 | **0 秒**。<br>**證據已於本輪整批更換（因 D-018）**：<br>❌ **T-0031「revision `00008-kcs` 切換時 134 樣本全 200」判定為無效證據** —— 當時流量釘死在 `00003-lt2`，`00008-kcs` 從未接流量，量到的是「舊版本全程持續服務」而非「切流量很快」。<br>⚠️ **AT r3 的 TC-090（228 樣本、覆蓋 08:40~08:59 UTC 兩次部署事件）判定為存疑** —— 同一期間流量同樣釘死，是否真的涵蓋一次流量切換無法回溯確認，**不再作為本項主要證據**。<br>✅ **現行主要證據：T-0045 事故復原後的真實部署重測** —— `deploy-staging` run [`35513187463`](https://github.com/jsfan910/PJ002/actions/runs/35513187463)（`95bc4ca`，13:20:01Z~13:22:23Z）`conclusion=success`，期間以 ~1 秒間隔輪詢 `/health` 取得 **226 個樣本全部 200、最長連續非 200 秒數 = 0**；`services describe` 唯讀複查 `todo-app-00019-k5q,100`（新 revision 取得 100% 流量）。<br>✅ **本卡獨立補證（第三次真實部署）**：run `35513953747`（`712b4a3`，13:35:28Z~13:37:36Z）`conclusion=success`，流量自動跟隨至 `todo-app-00020-z8j`；該部署窗（13:30~13:42Z）兩個 uptime check 共 7 條時間序列 **72 個資料點全 `true`、`false` 0**；本卡另於 13:37:26Z（**部署進行中**）不帶憑證打 `/health` 得 **200**（§7.1、§7.2） | **本輪實測＋換證據** | ✅ 達成（0 秒，遠低於 60 秒門檻） |

### 1.1 第 1 項的分解（105 條 P0 案例逐類清點）

| 類別 | 條數 | TC-ID | 說明 |
|---|---|---|---|
| **通過** | **104** | r3 的 101 條 ＋ **TC-080**（使用者裁決②追認）＋ **TC-009**、**TC-067**（本輪改判） | r3 已判通過的 101 條全部維持（unit 層 11 條的追溯狀態見 R-6，本輪已由「整體佐證」升級為「可逐條追溯」）。<br>**TC-080**：使用者於 Gate 2 r2 裁決② **B** 追認間接證據判通過（2026-09-20T18:35），已落 `20_測試案例.md` TC-080 列附註與 `traceability.md` US-010 列。<br>**TC-009／TC-067**：D-017 修正（T-0038）於流量復原後真正上線，使用者對 `00019-k5q` 重跑 4 project × 5 輪 **40／40 全過** → 改判通過。 |
| **部分通過** | **1** | TC-091 | 相容性 4／6：Chromium×2、Edge×2 通過；**Firefox 2 組為 Windows `CreateProcess` 層級啟動失敗**，T-0041 已排除二進位檔損毀與服務未就緒兩項假設，判工具／環境限制。**不是產品的預期結果不成立。** |
| **阻擋／未判定** | **0** | — | — |
| **不通過** | **0** | — | **沒有任何一條 P0 測試案例的預期結果不成立** |

合計核對：104 ＋ 1 ＋ 0 ＋ 0 = **105** ✅

### 1.2 r3 → r4 的逐項變化（只列有變的；未列出者皆「沿用 r3」）

| # | r3（2026-09-20 18:06） | r4（2026-09-20 21:44） | 變化性質 |
|---|---|---|---|
| 1 | 101 通過／**4 部分**／0 阻擋／0 不通過＝**96.19%** | **104 通過／1 部分／0 阻擋／0 不通過＝99.05%** | 三條改判通過：TC-080（使用者裁決②）、TC-009／TC-067（D-017 關閉，40／40）。缺口由 4 條減為 **1 條**，且該條為工具限制 |
| 3 | 0 筆；理由為「r2 → r3 之間未改任何 `src/`／`public/` 程式碼，故沿用」 | 0 筆；**理由改為「依 `07` §5.2 逐條核對 T-0038／T-0043 的變更範圍，皆不觸及阻擋級判準」** | **判定不變，論證基礎更換**。r3 的「程式未變更」前提在 r4 已不成立 |
| 4 | 登錄 17 筆；開放 S1／S2 = 0；**開放 S3 = 2**（D-014、D-017） | 登錄 **18 筆**；開放 S1／S2 = 0；**開放 S3 = 1**（僅 D-014） | **D-017 關閉**；**新增 D-018（S2／closed）**。開放 S1／S2 仍為 0，第 4 項續達成 |
| 6 | 100.0000%（1156／1156），無附註 | 數字不變；**新增適用範圍附註**：該 24 小時窗服務的是 `00003-lt2`（P0 程式），結論對 P0 成立，非對現行 `00020-z8j` 的量測 | **數字未變，證據涵義被釐清** |
| 10 | 4／6；Firefox 2 組沙盒 `spawn UNKNOWN` | 4／6；**納入 T-0041 的三項排除證據**（`--force` 重下載、Chromium 對照組、`child_process.spawn` 直呼重現） | 判定不變，**「非產品缺陷」的論證強度提高** |
| 11 | 六項皆「無」（r3 重跑） | 六項皆「無」（**r4 於改完 traceability 後重跑**，逐 US 條數與 r3 逐一相符） | 無變化，本輪重新驗證 |
| 12 | 0 秒；證據＝AT r3 228 樣本 ＋ T-0031 134 樣本 | 0 秒；**證據整批更換**＝T-0045 真實重測 226 樣本 ＋ 本卡補證第三次部署（72 資料點、部署中 `/health` 200） | **判定不變，但舊證據其中一份判無效、一份判存疑**；這是本輪最實質的證據面變動 |
| — | R-3（D-017 開放）、R-6（unit 層無法逐條追溯）、R-13（cron 備援不可靠） | **三項皆關閉**（見 §4） | 三項殘留風險解除 |
| — | 無 | **新增 R-14（現行前端碼缺完整回歸證據）、R-15（歷史 staging 結論的適用範圍）、R-16（`QA Tests` 8 連紅）** | 本輪新發現 |

---

## 2. 各報告連結（現行版本）

| 類別 | 路徑 | 結論 |
|---|---|---|
| CR r2（現行） | `docs/reports/20260919-1309-CR-E001-r2.md` | 通過：阻擋級 0。**覆蓋到 `b4b9d41`；其後的前端變更未經 qa-cr 輪次**（R-14） |
| AT r3（staging） | `docs/reports/20260919-1719-AT-E001-r3.md` | integration:qa 42 條 36 過／1 敗（TC-079 已裁決接受 302）／5 skip；e2e 136 條 133 過／3 敗（即 D-017，**現已修復**）；perf 全 PASS。**量測於 `00003-lt2`**；其中 TC-090 一項因 D-018 改列存疑 |
| **AT r4（Firefox 補跑，本機）** | `docs/reports/20260920-1901-AT-E001-r4-firefox.md` | Firefox 2 組 68／68 啟動失敗，判工具限制；TC-091 維持 4／6 |
| UAT r4（staging） | `docs/reports/20260919-1656-UAT-E001-r4.md` | staging：P0 十則 **10／10** 通過。**執行於 `00003-lt2`**（R-14） |
| UAT r3 | `docs/reports/20260919-1641-UAT-E001-r3.md` | **結論已被證偽，不採信** |
| **D-017 staging 重跑（使用者實測）** | `docs/reports/20260920-2132-D017-staging重跑-使用者實測.md` | **40／40 全過**（revision `00019-k5q`）。**執行者為使用者本人，非 agent**；本卡據此關閉 D-017 |
| **流量釘死事故報告** | `docs/reports/20260920-2042-流量釘死事故-E001.md` | 事故時間軸、影響範圍、pipeline 修法、TC-090 真實重測 226／226 |
| **事故規格紀錄** | `docs/specs/06_部署架構與CICD.md` §6.10.1 ~ §6.10.4 | 根因、`status.latestReadyRevisionName` 僅能作輔助佐證的理由、verify 修法 |
| 監測補強（NFR-003 來源切換） | `docs/reports/20260919-1804-監測補強-E001.md` | uptime check 建立（起算 2026-09-19T17:55:07+08:00） |
| 缺陷清單 | `docs/specs/24_缺陷清單.md` | **18 筆**（開放 S1／S2 = 0、開放 S3 = 1）；**本輪異動：D-017 closed、新增 D-018** |
| 追溯矩陣 | `docs/specs/traceability.md` | **本輪更新主表 US-001（TC-009）、US-008（TC-067）、US-010（TC-090）三格狀態欄** |
| 測試總結 r3（被本報告取代） | `docs/reports/20260920-1806-測試總結-E001-r3.md` | 歷史對照 |
| Gate 2 報告 r2（含使用者裁決） | `docs/reports/20260920-1822-Gate2-E001-r2.md` | ① A 有條件通過 ② B 追認 TC-080 ③ A 先清待辦再開 P1 |

---

## 3. 本輪的審核對象與採信原則

本卡**不是** CR／AT／UAT 任一張卡的 reviewer；本輪做四件事：① D-017 關閉判定；② D-018 立案、定嚴重度與關閉判定；③ 退出準則第 1、12 項的重算與換證據；④ 追溯矩陣狀態欄更新與孤兒檢查重跑。

**採信原則**：

- **第 11、12 項與 staging 現況為本卡親自實跑**（§7.1 ~ §7.3），不採信任何人的轉述。
- **D-017 的關閉證據是使用者實測，本卡無 staging Basic Auth 憑證、未親自執行**。依 CLAUDE.md 安全鐵則不索取、不讀取憑證值。本報告據實記為「使用者實測」，**不冒充為本卡實測**。該紀錄由 Leader 逐字轉錄，形式為 5 段 `8 passed` 的 Playwright 原始輸出，與關閉條件（≥ 5 次連續全過）可直接對照。
- **第 1 ~ 10 項中未標「本輪」者一律沿用 r3**，且本輪**額外核對了沿用的前提是否仍成立**（`git diff --name-only e948d54 HEAD -- src public tests ...`）；前提已變者（第 3、5、6、8 項）逐格加註適用範圍，不靜默沿用。

---

## 4. 殘留風險

| # | 風險 | 影響 | 現狀與處置 |
|---|---|---|---|
| R-1 | ~~NFR-003 的 24 小時採樣只走了 0.87 小時~~ | — | **已於 r3 解除** |
| **R-2** | **TC-080 沒有「同一批資料跨越一次部署前後」的直接比對** | NFR-006 在 staging 的直接證據缺一角；目前靠三項間接證據＋本機 TC-093 | **維持，列 P1 staging 測試輪補做**。使用者已於 Gate 2 r2 裁決② **B** 追認判通過（文件已一致：`20_測試案例.md`、`traceability.md` 均已更新），**但證據形式仍是間接的**。**本輪另有新增理由必須補做**：事故報告「給 qa-lead 的重驗清單」明載「TC-080 先前比對到的是舊版本 `00003-lt2` 的資料層行為，需重驗」。補做需 staging 憑證，agent 不代做 |
| ~~R-3~~ | ~~D-017：TC-067／TC-009 在 staging 間歇性失敗~~ | — | **本輪關閉**。T-0038 修正上線後 40／40 全過，D-017 `closed` |
| **R-4** | **NFR-007「前端可互動 < 3 秒」子項在 staging 未於 500 筆情境量測** | 第 9 項有一半是本機數字（166 ms／502 筆） | 沿用 r3：本機對 3000 ms 門檻裕度 18 倍，風險低；建議併入下次 staging 測試窗補量 |
| **R-5** | **真冷啟動（閒置後首次載入）的使用者感知秒數未量測** | `min-instances = 0`，ADR-0005 預期 1 ~ 3 秒 | 沿用 r3（24 小時內 1156 次 `/health` 皆在 10 秒逾時內回 200，風險低） |
| ~~R-6~~ | ~~unit 層 11 條 P0 TC 無法逐條追溯~~ | — | **本輪關閉**（依 Leader 2026-09-20T19:57 裁決「以可逐條追溯關閉」）。T-0042 已於 `tests/unit/**` 為 11 條補標 `[TC-xxx]` 測試名稱前綴，`grep -rn "TC-001" tests/unit` 一行可定位，跑一次測試即產生追溯證據。<br>**關閉但須明載的保留**：**11 條中有 6 條為「部分覆蓋」**（TC-001、TC-003、TC-019、TC-039、TC-048、TC-059），dev-tl 已逐條寫明未被斷言的子句 —— TC-001「repository 寫入方法零次被呼叫」未斷言且 `"\t\n "` 未單獨測；TC-003「`id` 為 UUID 格式」「`createdAt` 為建立當下時間」unit 層驗不到（由 DDL 產生）；TC-019 `""` 未單獨測、「更新方法零次被呼叫」未斷言；TC-039「回傳物件 `title`／`createdAt` 不變」在 service 層未完整比對；**TC-048 步驟 1、3 在後端不落在 `todo-service`**（`listTodos` 為純轉發零驗證，實際落點為 `todo-schema.ts` 的 `StatusFilterQuerystringSchema`，屬路由 schema／整合層，unit 層改標在前端 store 的等效斷言）；TC-059「字串以 `Z` 或 `+00:00` 結尾」未斷言。**其餘 5 條（TC-002、012、020、030、038）為完全覆蓋**。**補測列 P1**（T-0042 受 outputs 限制「只加 TC-ID、不改斷言」，未補） |
| **R-7** | **Firefox 兩組相容性仍未跑（NFR-004 4／6）** | Gecko 引擎零覆蓋 | 沿用 Leader 裁決。**T-0041 已在本機重試並失敗**（`blocked`），三項假設（二進位檔損毀、服務未就緒、Playwright 設定）皆已排除，錯誤在 Windows `CreateProcess` 層。**唯一剩餘路徑：使用者於自有機器執行**（指令見 AT r4 報告）。這是第 1 項到不了 100% 的**唯一**原因 |
| **R-8** | **D-014（`runMigrations` 無互斥鎖）仍開放（S3）** | 多實例同時對全新資料庫冷啟時一方 `process.exit(1)` | 沿用 Leader 裁決列 P1。現況 `min-instances = 0`、staging 單實例，P0 不受影響。建議 P1 補一條「多實例並行冷啟」TC |
| **R-9** | **OBS-7：多個 QA agent 並行寫入同一 staging 會互相清空資料** | 曾造成 AT r3 的 TC-080 基準資料被清空等假性失敗 | 沿用 Leader 裁決「staging 測試卡錯開時段」。建議再加資料隔離（各角色固定標題前綴，清空型步驟只刪自己的） |
| **R-10** | **staging Basic Auth 憑證曾貼入對話** | 憑證外洩風險 | **本報告第四次重申此項尚未執行**。Gate 2 已由使用者判「有條件通過」，**輪換是其中一項明列條件**，且發布前必做（Secret Manager 加新版 ＋ GitHub secrets 更新 ＋ 重新部署）。T-0039 已完成 secret 釘版本，輪換後只需更新版本號即可生效 |
| **R-11** | **TC-095 的 15 分鐘實測由 AI agent 執行，非「未參與開發者」** | NFR-008 的人因成分未被真實使用者驗證 | 14 秒／27 秒對 900 秒門檻裕度極大；列已知限制，不建議為此阻擋 |
| **R-12** | **NFR-003 的 24 小時只涵蓋「低流量的一天」** | 該期間無真實使用者負載 | 沿用 r3：NFR-001 的負載測試（`-c 10`、60 秒）已證明併發下仍達標，合併看風險低；正式環境仍應依 `06` §6.2 走「連續 7 天」目標 |
| ~~R-13~~ | ~~`monitor-health.yml` cron 備援不可靠~~ | — | **本輪關閉**。兩路處置皆已落地：①T-0040 已把 `06` §6.1 的 cron 改為「保溫與人工抽查用，不具備援能力」的定位；②T-0044 已建立**第二個 uptime check** 作為真備援 —— 本卡實查確認 `todo-app-health-backup-yvjsI-2xna0` 存在且正在取樣（部署窗內 `usa-virginia` 12 點、`sa-brazil-sao_paulo` 12 點、`apac-singapore` 7 點，全 `true`，見 §7.2） |
| **R-14（新增）** | **現行上線的前端程式碼缺少完整回歸證據** | AT r3（e2e 136 條、perf、integration:qa 42 條）與 UAT r4（10 則 US）全部量在 `00003-lt2`，**不含 T-0038 的 `public/assets/todo-store.js`（+79／−11 行，跨動作操作序號重寫）與 T-0043 的 `public/index.html`／`favicon.ico`**。現行碼在 staging 上被重跑過的只有 TC-067／TC-009 兩條 | **中度風險**。已有的緩解：①T-0038 的變更僅限前端狀態時序，`src/` 零變更（本卡以 `git diff` 核實），故後端 API 的 integration／perf 結論不受影響；②dev-tl 初審已實跑 lint、unit 78／78、e2e 目標條目 5 輪、修正前後因果隔離對照、密鑰掃描；③CI（`ci.yml`）在現行 `712b4a3` 上 **lint／unit／integration／docker build 四個 job 全綠**（§7.4）；④使用者 staging 40／40。**未緩解的部分：完整 e2e 136 條與 UAT 10 則未對現行碼重跑**。列 §6 裁決事項 B |
| **R-15（新增）** | **歷史 staging 結論的適用範圍需逐項認定（D-018 後遺症）** | 2026-09-19 16:33 ~ 2026-09-20 20:44 共 28 小時 11 分，staging 一律服務 `00003-lt2`。這段期間產出的**所有** staging 結論，其受測標的都不是「當時的 main」 | **本輪已逐項認定完畢**，結論如下，供後人不必重新推導：<br>**仍有效**：AT r3 的 integration:qa／e2e／perf、UAT r4、NFR-003 24 小時採樣、TC-078／079／081/082／083／084／105 —— 因 `00003-lt2` 即 2026-09-19 16:05 的 main，含 P0 全功能與 CR r2 全部修正，這些結論對「P0 程式」成立。<br>**無效**：T-0031 的 `00008-kcs` 部署不可用量測。<br>**存疑、已換證據**：AT r3 的 TC-090。<br>**需重驗**：TC-080（資料層比對打到的是舊版本行為）。<br>**風險**：此認定依賴「`00003-lt2` 的內容等同 2026-09-19 16:05 的 main」這個前提，本卡未以映像 digest 回推 commit 驗證（需 registry 存取），依據為事故報告與 `06` §6.10 的記載 |
| **R-16（新增）** | **`QA Tests` workflow 已連續 8 次紅燈（run #17 ~ #24），且原因不明** | `integration (tests/integration/qa)` job 於每次 push 失敗；該 job 用的是 workflow 內的 `postgres:16-alpine` service container 與 `ci/ci` 帳密，**不依賴 staging**，因此失敗不是憑證或 staging 問題。`e2e`／`perf` 兩個 job 為 `workflow_dispatch` 專用，於 push 事件 `skipped`（設計如此，非連帶失敗）。**後果：P0 的 42 條 QA 層 integration TC 目前在 CI 上是紅的**，而退出準則第 1 項採信的是 AT r3 的人工執行結果 | **未處置**。本卡可確定的事實：①轉紅起點為 run #17（`6b27611`，2026-09-20T10:51:38Z），前一次全綠為 run #16（`e948d54`，2026-09-19T10:47:26Z）；②**兩者之間 `src`／`public`／`tests`／`package.json`／`package-lock.json`／`qa-tests.yml` 的 `git diff --name-only` 輸出為空（0 行）** —— 程式與測試碼完全相同，**排除本 repo 的程式碼回歸**；③失敗訊息需 repo admin 權限（匿名 `/actions/jobs/{id}/logs` 回 403），本卡讀不到。<br>**因無失敗訊息、無法定根因與嚴重度，依 `07` §5.3 不立缺陷**（且逾本卡 outputs）。**列 §6 裁決事項 A，建議 Leader 開一張 dev-ops／qa-at 卡由有權限者讀 log** |

---

## 5. 建議

### **有條件可發布**

Gate 2（P0）已由使用者於 2026-09-20T18:35 裁決① **A 有條件通過**。本輪相對該裁決的進展與剩餘條件：

**已消解的條件（2 / 3）**：

1. ~~TC-080 定案~~ → 使用者裁決② **B** 追認通過，文件已一致（`20_測試案例.md`、`traceability.md`）。**但補做直接比對仍列 P1**（R-2）。
2. ~~D-017 列管~~ → **已關閉**（40／40）。

**剩餘條件（發布前必須完成）**：

1. **立刻輪換 staging Basic Auth 憑證**（R-10，已延宕四輪）。這是使用者裁決①明列的條件，也是唯一與安全相關的一項。
2. **Release Notes v0.1.0 須附註更正 staging 現況**（見 §6 裁決事項 D）。現行 Release Notes 所述的 staging 狀態成文於流量釘死期間，與事實不符。
3. **`QA Tests` 8 連紅須有結論**（R-16／裁決事項 A）。不是因為它會改變任何一格判定，而是因為**「CI 綠燈」這個訊號目前對 QA 層 integration 是失效的**，而 D-018 的教訓正是「全綠不等於沒事」。

**列管但不擋發布**：Firefox 2 組（R-7，第 1 項達 100% 的唯一障礙，需使用者自有機器）、TC-080 直接比對（R-2）、unit 層 6 條部分覆蓋補測（R-6）、D-014（R-8）、`06` §5.1 回滾流程規格文字（plan-sd P1 卡）、NFR-007 前端 500 筆 staging 補量（R-4）。

**理由（一句）**：七項擋關項中**第 2 ~ 7 項全部達標**，第 1 項 99.05% 的**唯一**缺口 TC-091 是 Windows `CreateProcess` 層級的瀏覽器啟動失敗、已排除三項替代假設、**與產品程式碼無關**；105 條 P0 中**不通過 0 條、阻擋 0 條**，開放 S1／S2 缺陷 0 筆，且本輪把 Gate 2 前置裡最弱的一環（部署不可用時間的證據其實建立在「流量從未切換」之上）**主動推翻並以兩次真實部署重建**。

**若要求二擇一（只能寫「可發布」或「不可發布」）**：依 `07` §4 字面仍為 **不可發布**（第 1 項 < 100%）。該結構性矛盾已由使用者裁決① A 處置，本輪不重開；但須注意**第 1 項現在距離 100% 只剩 Firefox 兩組**，一旦使用者在自有機器跑完並通過，**第 1 項即為 105／105 = 100%，字面與實質首次一致**，屆時可出一份無條件的 Gate 2 r3。這是本輪最值得追求的收尾。

---

## 6. 給 Leader 的裁決事項

### 裁決事項 A：`QA Tests` 連續 8 次紅燈，要不要在發布前查清楚？

1. **核心爭議**：QA 層 42 條 integration 測試在 CI 上**已經紅了 8 次、橫跨約 2 小時 44 分的所有 push**，而我們讀不到失敗訊息（需 repo admin）。實質影響：**我們目前無法從 CI 判斷 P0 的 QA 測試是否還會過**；退出準則第 1 項採信的是 AT r3 於 2026-09-19 的人工執行結果，那份結果與現在的 CI 訊號互相矛盾，而我們不知道哪一邊對。更重要的是，D-018 剛剛才教過一次「每次 run 全綠但其實沒上線」；現在的情況是反面版本 ——「每次 run 全紅但我們照樣簽核」。兩者的共同病灶是**把自動化訊號當背景噪音**。
2. **證據觀點**：①轉紅起點 run #17（`6b27611`，10:51:38Z），前一次全綠 run #16（`e948d54`，09-19 10:47:26Z）；②**兩者之間 `src`／`public`／`tests`／`package.json`／`package-lock.json`／`qa-tests.yml` 的 `git diff --name-only` 為空（0 行）**，程式與測試碼逐字相同 → **不是程式碼回歸**，最可能是 runner 映像、`npm ci` 解析到的傳遞相依、`postgres:16-alpine` tag 漂移或時序不穩；③該 job 使用 workflow 內建的 postgres service container 與 `ci/ci` 帳密，**不依賴 staging 或任何 secret**，所以不是 T-0039 的 secret 釘版本造成；④`e2e`／`perf` 的 `skipped` 是設計（`workflow_dispatch` 專用），**不是連帶失敗**；⑤`ci.yml` 在同一個 `712b4a3` 上 lint／unit／integration／build **四個 job 全綠**（§7.4），所以基本健康度是好的；⑥Leader 已於 2026-09-20T19:01 把「run #17 job log 需 admin 權限」交給使用者，**至今未回**。
3. **選項**：
   - **選項 1：開一張小卡（dev-ops 或 qa-at），由有權限者讀 run #24 的 job log 並定性**；若為產品回歸則立缺陷並擋發布，若為環境／相依漂移則釘版本或記為已知噪音。代價：一張小卡，約半小時；需要使用者提供 log 或授權。
   - **選項 2：本地重現代替讀 log** —— 在本機以同一組環境變數（`postgres:16-alpine`、`DATABASE_URL=postgres://ci:ci@localhost:5432/todo`）跑 `npm run test:integration:qa`，看是否重現。代價：本機環境與 runner 不同，**不重現也不能證明沒問題**，但成本最低且不需任何權限。
   - **選項 3：暫不處理**，記為殘留風險 R-16，發布後再看。代價：發布時我們對 QA 層 integration 的唯一依據是 30 小時前的人工執行；若那 8 次紅燈其實是真回歸，會在 P1 開卡後才被發現，且屆時要從 8 個 commit 裡回推。
4. **我的建議：選項 2 先做（零權限、10 分鐘），不重現再走選項 1。** 理由：`git diff` 為空這項證據已經把「程式碼回歸」的機率壓得很低，本地重現是最快的證偽／證實手段；只有在本地也紅的時候才真的需要 admin log。
5. **推翻成本**：**低、可回頭**。兩個選項都不改任何已判定的數字；若查出是真回歸，受影響的是第 1 項與第 3 項，需重出 r5，但所有 staging 實測數字（第 5 ~ 12 項）皆可沿用。若查出是環境噪音，只需在 R-16 補一行結論並關閉。

### 裁決事項 B：現行上線的前端碼沒有完整回歸證據，要不要在發布前補跑一輪？

1. **核心爭議**：我們準備簽核的程式，和我們拿到 10／10 UAT 與 136 條 e2e 結果的程式，**不是同一份**。T-0038 重寫了前端的跨動作操作序號邏輯（`public/assets/todo-store.js`，+79／−11 行），T-0043 動了 `public/index.html`。這兩項在 staging 上被驗過的只有 TC-067／TC-009 兩條。實質影響：如果 T-0038 的時序重寫在**別的**操作路徑上引入了迴歸（例如快速連點篩選、編輯中重繪 —— 正是 D-007／D-008 當初的失效模式），我們現在**不會知道**。
2. **證據觀點**：①`git diff --name-only e948d54 HEAD -- src` **為空** → 後端零變更，integration／perf／效能結論不受影響，風險面**只在前端**；②D-007（請求序列化競態）與 D-008（編輯中重繪吃掉草稿）都曾是真實缺陷，且**與 T-0038 改的是同一塊狀態通知機制**，這是風險最高的巧合；③dev-tl 初審已做因果隔離（拉 main 建獨立副本對照）、5 輪 e2e 目標條目、unit 78／78，**但 `-g "TC-067|TC-009"` 是目標式執行，不是完整套件**；④`ci.yml` 四 job 全綠，但 `ci.yml` 不含 e2e；⑤使用者的 40／40 是 staging 真實環境，但同樣只有那兩條。
3. **選項**：
   - **選項 1：補跑一輪完整 e2e（chromium + msedge 四組，136 條）對現行 staging**，由使用者在自有機器執行（已證明可行，40／40 就是這樣跑的），**順便把 Firefox 兩組一起跑掉**。代價：使用者約 15 ~ 20 分鐘；需 staging 獨占時段（OBS-7）。**額外收益：若 Firefox 通過，第 1 項直接 105／105 = 100%。**
   - **選項 2：只補跑 US-001 ~ US-007 的前端相關子集**（TC-008 ~ 011、026、027、053 ~ 055、065 ~ 069 等直接受 store 狀態通知影響者）。代價：較快，但「哪些算相關」是判斷題，漏掉的正是最危險的。
   - **選項 3：不補跑**，以 dev-tl 初審 ＋ 40／40 ＋ CI 四 job 全綠作為足夠證據，列 R-14 殘留風險發布。代價：把一次未經完整驗證的前端重寫送進發布；若日後出問題，我們的紀錄會顯示「知道但沒做」。
4. **我的建議：選項 1，且與 Firefox 合併成同一次操作交給使用者。** 理由：它同時解掉本輪最大的證據缺口（R-14）和第 1 項到不了 100% 的唯一障礙（R-7），**一次 20 分鐘的操作可以讓 Gate 2 從「有條件通過」變成無條件通過**，性價比在本輪所有待辦裡最高。
5. **推翻成本**：**低**。若使用者排不出時間，退選項 3 即可，本報告所有數字不變，只需在 R-14 標明「已提請、未執行」；反向亦然。若補跑後發現迴歸，則立缺陷、退 dev-fe，第 1 項下修 —— 但那正是我們該知道的事。

### 裁決事項 C：D-018 的「來源」欄偏離 `07` §5.3 的列舉，請追認

1. **核心爭議**：`07` §5.3 規定缺陷的「來源」限 unit／integration／e2e／perf／CR／UAT 六類。**D-018 不屬於任何一類** —— 它是 Leader 在排查使用者的異常回報時，實查 `gcloud run services describe` 發現的維運事故。若嚴格照字面，它**不算缺陷**，而那顯然荒謬：一個讓 15 次部署全部沒上線、28 小時無人察覺的問題，不登錄在缺陷清單裡，退出準則第 4 項就會繼續顯示「一切正常」。
2. **證據觀點**：①§5.3 的實質要件是五項齊備（嚴重度＋可重現步驟＋對應 TC＋來源＋狀態），D-018 **五項全有**，唯一不合的是來源欄的**列舉窮盡性**；②§5.3 的立法意旨是「缺少重現步驟或對應 TC 的不算缺陷」，針對的是**證據不足**，不是**發現管道**；③本 Epic 的缺陷清單第 2 節早有前例 —— E-B-01 ~ E-B-09 的「環境阻擋」就是依 Leader 2026-09-19T07:40:06 裁決新增的類別，§5.3 同樣沒列；④我已在 D-018 的來源欄據實寫明偏離並標註待追認，未靜默通過。
3. **選項**：
   - **選項 1：Leader 當場追認**「維運事故實查」為合法來源，記入 Epic 裁決紀錄。代價：無，一行字。
   - **選項 2：走規格變更請求改 `07` §5.3**，把來源欄改為開放列舉（「包含但不限於」）並補「維運事故」一類。代價：一張 plan-sd 小卡；但它讓判準與事實一致，下個 Epic 也受益。
   - **選項 3：不追認，把 D-018 降級為「觀察（OBS）」**。代價：**不可接受** —— 開放 S1／S2 的計數會因此漏掉一個 S2，退出準則第 4 項就不再可信。
4. **我的建議：選項 1 立刻追認，並把選項 2 併入已排定的 plan-sd P1 規格同步卡**（該卡已要處理 `06` §5.1 的回滾流程文字，同一張卡一起改）。理由：追認零成本且立刻讓第 4 項的計數站得住；規格文字修正交給已經存在的卡，不另開新卡。
5. **推翻成本**：**極低**。不追認也只是把 D-018 改記為 OBS，但那會讓缺陷計數失真，我會在報告裡明載反對。

### 裁決事項 D：Release Notes v0.1.0 需附註更正 staging 現況

1. **核心爭議**：Release Notes v0.1.0 於 2026-09-20T19:57（T-0042）定版並打了 tag，**成文時間正好落在流量釘死期間**。它所描述的「staging 現況」是根據「deploy-staging 全綠」推論出來的，而那個推論在當時**是假的** —— 實際服務的是 `00003-lt2`，不含 v0.1.0 宣稱已上線的任何一項修正。實質影響：**一份已打 tag 的發布文件，記載了與事實不符的部署狀態**；若照現狀發布，等於把 D-018 的錯誤結論固化進對外文件。
2. **證據觀點**：①事故報告「影響範圍」第 1 點明載「T-0031／T-0039／T-0038／T-0043／v0.1.0 定版皆未真正上線，儘管對應的 run 全數綠燈」；②Leader 於 2026-09-20T20:41:17 的 Epic 裁決第 ④ 點已明白寫出「Release Notes v0.1.0 所述 staging 現況需附註更正」；③事故已完全復原，現行 `00020-z8j` 確實含全部修正（本卡 §7.1 實查流量 100%）—— **需要更正的是敘述，不是產品**；④tag `v0.1.0` 已推 origin，**不應也不必重打**。
3. **選項**：
   - **選項 1（建議）：在 Release Notes v0.1.0 追加一段「勘誤／staging 現況更正」**，寫明：定版當下 staging 實際服務 `00003-lt2`、成因為 D-018、已於 2026-09-20 20:44 復原、現行 revision 為 `00020-z8j` 且含全部修正，並連到事故報告與 `06` §6.10。指派 dev-tl（Release Notes 的權責角色）。代價：一張小卡或併入既有卡；tag 不動，勘誤以附錄形式存在。
   - **選項 2：重出 v0.1.1**，把 staging 現況寫對。代價：為一段敘述錯誤升一個版本號，版本語意變髒；且 v0.1.0 的錯誤敘述仍留在歷史裡沒被更正。
   - **選項 3：不動**。代價：對外文件與事實不符，且這正是 D-018 的後遺症被固化的方式 —— 我們剛剛才花一張卡把它挖出來。
4. **我的建議：選項 1。** 理由：錯的是敘述不是產品，勘誤是最小且最誠實的修法；重打版本號會讓「v0.1.0 到底有沒有上線」變得更難查，與更正目的相反。
5. **推翻成本**：**極低**。勘誤是純追加，隨時可再補充或改寫；若日後決定重出 v0.1.1，這段勘誤也正好是變更說明的素材。

---

## 7. 本卡實際執行的驗證輸出（貼實際輸出）

> **憑證聲明**：本節所有指令**皆未使用 staging Basic Auth 憑證**。`curl` 一律不帶憑證（`/health` 為 `06` §3.2 明載的唯一豁免路徑）；`gcloud` 為**唯讀** `describe`／`list`／`monitoring`，使用本機已登入的 GCP 帳號，**不涉及、不讀取、不寫入任何應用層憑證值**。需憑證的驗證（TC-080 直接比對、staging UI 重放、D-017 e2e 重跑）本卡一律未執行，見 §7.5。

### 7.1 staging 現況實查（不帶憑證 ＋ 唯讀 describe）

```bash
date "+READOUT_LOCAL=%Y-%m-%dT%H:%M:%S%z"; date -u "+READOUT_UTC=%Y-%m-%dT%H:%M:%SZ"
B="https://todo-app-dpevsdhdva-de.a.run.app"
curl -sS -o /dev/null -w "GET /health -> %{http_code} time=%{time_total}s\n" "$B/health"
curl -sS -o /dev/null -w "GET /          -> %{http_code}\n" "$B/"
curl -sS -o /dev/null -w "GET /favicon.ico -> %{http_code}\n" "$B/favicon.ico"
MSYS_NO_PATHCONV=1 "$GCLOUD" run services describe todo-app --region asia-east1 --project pj002-509106 \
  --format="yaml(spec.traffic,status.traffic,status.latestReadyRevisionName,status.latestCreatedRevisionName)"
```
```text
READOUT_LOCAL=2026-09-20T21:37:26+0800
READOUT_UTC=2026-09-20T13:37:26Z
--- 1. 不帶憑證打 /health 與 / ---
GET /health -> 200 time=0.297171s
GET /          -> 401
GET /favicon.ico -> 401
--- 2. 現行 revision 與流量（gcloud 唯讀 describe，不涉應用層憑證）---
spec:
  traffic:
  - latestRevision: true
    percent: 100
status:
  latestCreatedRevisionName: todo-app-00020-z8j
  latestReadyRevisionName: todo-app-00020-z8j
  traffic:
  - latestRevision: true
    percent: 100
    revisionName: todo-app-00020-z8j
EXIT=0
```

**判讀**：

- **`spec.traffic` 為 `latestRevision: true`** → D-018 的根因狀態（釘定具名 revision）**確已解除**，流量一律跟隨最新 revision。
- **`status.traffic` 的 100% 在 `todo-app-00020-z8j`，且等於 `latestCreatedRevisionName` 與 `latestReadyRevisionName`** → 最新建立的 revision 確實在服務，**三者一致**。
- 保護規則正確：`/health` 豁免回 200、`/` 與 `/favicon.ico` 皆 401（`/favicon.ico` 的 401 而非 404，本身即證明 Basic Auth 覆蓋到靜態資產；該檔是否存在需帶憑證才驗得到，**本卡未驗**，由使用者 40／40 中 msedge TC-009 全過間接佐證）。
- **本次讀數時刻 13:37:26Z 正落在 `deploy-staging` run `35513953747` 的執行區間內（13:35:28Z ~ 13:37:36Z）** —— 即**部署進行中不帶憑證打 `/health` 得 200**。這是本卡對退出準則第 12 項的一筆附帶現場證據。

**注意（重要，本卡發現）**：判讀當下的現行 revision 是 **`00020-z8j`**，而使用者重跑 D-017 的標的是 **`00019-k5q`**。兩者的**應用程式碼完全相同**，已核實：

```bash
git diff --stat 95bc4ca 712b4a3                       # 00019 → 00020 之間的 main 變更
git diff --name-only 95bc4ca 712b4a3 -- src public | wc -l
```
```text
 CHANGELOG.md | 2 +-
 docs/reports/20260920-2042-流量釘死事故-E001.md | 35 ++++-
 docs/reports/20260920-2132-D017-staging重跑-使用者實測.md | 37 +++++
 docs/specs/06_部署架構與CICD.md | 46 +++++-
 tasks/T-0045-traffic-pinned-fix.md | 7 +-
 tasks/T-0046-test-summary-r4.md | 57 +++++++
 tasks/_todo.md | 3 +-
 worklog/handoff/20260920-2042-T0045-r1-dev-ops.md | 83 +++++++++-
 worklog/handoff/20260920-2059-T0045-r1-dev-tl.md | 170 +++++++++++++++++++++
 worklog/handoff/20260920-工作交接.md | 1 +
 10 files changed, 425 insertions(+), 16 deletions(-)
0
```

→ `src/` 與 `public/` 變更檔案數 = **0**，僅文件與任務卡。**因此 D-017 在 `00019-k5q` 上的 40／40 結論，對現行服務的 `00020-z8j` 同樣成立。**

### 7.2 D-018 修法的獨立複查：第三次真實部署（本卡補證，退出準則第 12 項）

```bash
MSYS_NO_PATHCONV=1 "$GCLOUD" run revisions list --service todo-app --region asia-east1 \
  --project pj002-509106 --limit 4 \
  --format="table[no-heading](metadata.name,metadata.creationTimestamp,status.conditions[0].status,spec.containers[0].image)"
curl -sS "https://api.github.com/repos/jsfan910/PJ002/actions/workflows/deploy-staging.yml/runs?per_page=5"
```
```text
todo-app-00020-z8j  2026-09-20T13:37:08.302411Z  True  ...todo-app@sha256:eb758bee...
todo-app-00019-k5q  2026-09-20T13:21:55.672504Z  True  ...todo-app@sha256:40b1ffc9...
todo-app-00018-xq4  2026-09-20T12:19:12.835503Z  True  ...todo-app@sha256:d0a90622...
todo-app-00017-qg4  2026-09-20T12:10:28.130626Z  True  ...todo-app@sha256:4fed79dc...

--- deploy-staging 最近 5 個 run（id / event / status / conclusion / head_sha / created / updated）---
35513953747 workflow_run completed success 712b4a3 2026-09-20T13:35:28Z 2026-09-20T13:37:36Z
35513187463 workflow_run completed success 95bc4ca 2026-09-20T13:20:01Z 2026-09-20T13:22:23Z
35510215055 workflow_run completed success e02f706 2026-09-20T12:17:56Z 2026-09-20T12:19:25Z
35509795862 workflow_run completed success 854c843 2026-09-20T12:09:02Z 2026-09-20T12:10:41Z
35509297796 workflow_run completed failure 5a3599d 2026-09-20T11:58:29Z 2026-09-20T11:59:06Z
```

部署窗內的 uptime check 取樣（**兩個 check 皆納入**；窗 13:30:00Z ~ 13:42:00Z 完整涵蓋 run `35513953747`）：

```bash
curl -sS -G "https://monitoring.googleapis.com/v3/projects/pj002-509106/timeSeries" \
  -H "Authorization: Bearer $(gcloud auth print-access-token)" \
  --data-urlencode 'filter=metric.type="monitoring.googleapis.com/uptime_check/check_passed"' \
  --data-urlencode "interval.startTime=2026-09-20T13:30:00Z" \
  --data-urlencode "interval.endTime=2026-09-20T13:42:00Z" \
  --data-urlencode "view=FULL" --data-urlencode "pageSize=2000"
```
```text
--- 現有 uptime checks ---
projects/pj002-509106/uptimeCheckConfigs/todo-app-health-aMAlP5dfKv0         todo-app-health
projects/pj002-509106/uptimeCheckConfigs/todo-app-health-backup-yvjsI-2xna0  todo-app-health-backup

--- 部署窗 2026-09-20T13:30:00Z ~ 2026-09-20T13:42:00Z 取樣統計 ---
timeSeries objects: 7
  todo-app-health-aMAlP5dfKv0 | apac-singapore  true=11 false=0
  todo-app-health-aMAlP5dfKv0 | eur-belgium  true=12 false=0
  todo-app-health-aMAlP5dfKv0 | usa-iowa  true=7 false=0
  todo-app-health-aMAlP5dfKv0 | usa-oregon  true=11 false=0
  todo-app-health-backup-yvjsI-2xna0 | apac-singapore  true=7 false=0
  todo-app-health-backup-yvjsI-2xna0 | sa-brazil-sao_paulo  true=12 false=0
  todo-app-health-backup-yvjsI-2xna0 | usa-virginia  true=12 false=0
TOTAL points true=72 false=0
```

**判讀**：

- **D-018 的修法在連續兩次真實部署上成立**：`35513187463`（T-0045 驗的那次，→ `00019-k5q` 100%）與 `35513953747`（**本卡獨立觀察到的第三次**，→ `00020-z8j` 100%），兩次 `conclusion` 皆 `success`，且流量皆自動跟隨到新建的 revision。**這是「verify 現在真的會驗流量」的第二個獨立樣本**，本卡未依賴 T-0045 的任何轉述。
- **第三次部署期間零不可用**：兩個 uptime check 共 7 條時間序列、72 個資料點**全 `true`、`false` 0**；加上本卡自己在 13:37:26Z（部署進行中）取得的 `/health` 200。
- **但退出準則第 12 項的主要證據仍記為 T-0045 的 226／226**：本節的 72 個資料點是 Monitoring 的 10 秒展開點、取樣密度遠低於 1 秒輪詢，且 12 分鐘窗內的資料可能尚未完整寫入（各地區點數 7 ~ 12 不等即為此故）。**本節定位為獨立佐證，不取代主要量測。**
- **R-13 連帶關閉**：`todo-app-health-backup-yvjsI-2xna0`（T-0044 建立的第二個 uptime check）確實存在且正在取樣，檢查地區（`usa-virginia`、`sa-brazil-sao_paulo`、`apac-singapore`）與主 check 刻意錯開，**真備援已成立**。

### 7.3 追溯矩陣孤兒檢查重跑（退出準則第 11 項）

本輪**在改完 traceability 主表三格狀態欄之後**重跑（以 Node 依「未跳脫的 `|`」切欄，避開 US-006 列 API 欄內含 `\|` 的陷阱；另以「第 3 欄為 `P0`／`P1`」過濾出主表 13 列，排除同檔內其他以 `| US-0xx |` 開頭的對照表）：

```bash
node orphan_t0046.js   # 讀 traceability.md 主表、01_需求規格書_SRS.md、02_系統分析書_SA.md、03_系統設計書_SD.md、20_測試案例.md
```
```text
主表 US 列數 = 13 （應 13）
UC 欄為 — 的列數 = 0 （應 0）
SD 模組欄為 — 的列數 = 0 （應 0）
API 端點欄為 — 的列數 = 0 （應 0）
TC-ID 欄為 — 的列數 = 0 （應 0）

[1][2] US 集合：SRS 13 則 vs 主表 13 則
US-SET-IDENTICAL-OK（diff 為空）

[3][4] UC 集合：SA 14 則 vs 主表 UC 欄 14 則
UC-SET-IDENTICAL-OK（diff 為空）

[5][6] TC 集合：20_測試案例.md 123 條 vs 主表展開 123 條
只在測試案例檔（孤兒 TC）：無
只在追溯矩陣（捏造 TC）：無
TC-SET-IDENTICAL-OK（diff 為空）
被多於一則 US 引用的 TC：無

逐 US 條數：US-001=13 US-002=10 US-003=12 US-004=9 US-005=11 US-006=12 US-007=7 US-008=7 US-009=13 US-010=11 US-011=6 US-012=6 US-013=6  合計=123

模組集合：SD 全檔 21 個 vs 主表引用 21 個
只在主表（捏造模組）：無
只在 SD（孤兒模組）：無
```

**判讀**：本輪只改主表三格的「狀態」欄文字（US-001 的 TC-009、US-008 的 TC-067、US-010 的 TC-090），**未新增／刪除任何 US、UC、模組、端點或 TC**。上述輸出逐項確認第 1 ~ 6 項皆為「無」；逐 US 條數 13／10／12／9／11／12／7／7／13／11／6／6／6 與 `traceability.md` §孤兒檢查第 5 項所記、以及 r3 的重跑結果**三方逐一相符**。**六項皆「無」，第 11 項達成。**

### 7.4 本輪新發現的兩項核對（§前言 A、B 的原始輸出）

**(1) 現行上線碼與歷史 staging 測試標的的差異（R-14）**

```bash
git log --since="2026-09-19 15:00" --oneline --name-only -- src public
git diff --name-only e948d54 HEAD -- src public tests package.json .github/workflows/qa-tests.yml
```
```text
d8a7427 T-0038: 合併 D-017 修正 — 前端錯誤訊息顯示時序競態
938a759 T-0043: 補 public/favicon.ico 消除 Edge TC-009 因 /favicon.ico 404 誤判 console error
public/favicon.ico
public/index.html
1ab2a53 T-0038: 修正 D-017 前端錯誤訊息時序競態（跨動作操作序號取代 requestSeq）
public/assets/todo-store.js

--- e948d54..HEAD ---
public/assets/todo-store.js
public/favicon.ico
public/index.html
tests/unit/todo-repository.test.ts
tests/unit/todo-service.test.ts
tests/unit/todo-store.test.mjs
```

→ **`src/` 零變更**（清單中無任何 `src/` 檔案），變動全在 `public/`（3 檔）與 `tests/unit/`（3 檔，T-0042 的 TC-ID 標註）。

**(2) `QA Tests` 8 連紅，且轉紅前後程式碼逐字相同（R-16）**

```bash
curl -sS ".../actions/workflows/qa-tests.yml/runs?per_page=24"        # run_number / event / conclusion / head_sha / created_at
curl -sS ".../actions/runs/35513907679/jobs"                          # 最新一次紅燈 run 的 job 明細
git diff --name-only e948d54 6b27611 -- src public tests package.json package-lock.json .github/workflows/qa-tests.yml | wc -l
```
```text
24 push failure 712b4a3 2026-09-20T13:34:34Z
23 push failure 95bc4ca 2026-09-20T13:18:59Z
22 push failure e02f706 2026-09-20T12:16:27Z
21 push failure 854c843 2026-09-20T12:08:08Z
20 push failure 5a3599d 2026-09-20T11:57:16Z
19 push failure 7dab35c 2026-09-20T11:38:17Z
18 push failure bfe4693 2026-09-20T11:18:56Z
17 push failure 6b27611 2026-09-20T10:51:38Z     <-- 轉紅起點
16 push success e948d54 2026-09-19T10:47:26Z     <-- 最後一次全綠
（run 1 ~ 16 皆 success，略）

--- run 35513907679（#24）的 job ---
jobs 3
- integration (tests/integration/qa) | completed failure
    step: 執行 tests/integration/qa（依序，避免共用資料庫互相干擾） -> failure
- e2e (Playwright) | completed skipped
- perf (NFR-001／NFR-007) | completed skipped

--- git diff --name-only e948d54 6b27611 -- src public tests package.json package-lock.json .github/workflows/qa-tests.yml ---
0
```

對照組：同一個 `712b4a3` 上 **`ci.yml` 四個 job 全綠**

```bash
curl -sS ".../actions/runs/35513907735/jobs"
```
```text
- lint        | success   （npm ci → build → lint → redocly lint OpenAPI 全部 success）
- integration | success   （postgres service container → migrate → test:integration 全部 success）
- build       | success   （docker build success）
- unit        | success   （npm run test:unit success）
```

**判讀**：`git diff` 輸出 **0 行** → 轉紅前後 `src`／`public`／`tests`／相依鎖檔／workflow 定義**逐字相同**，**排除本 repo 的程式碼回歸**。`e2e`／`perf` 的 `skipped` 經查 `qa-tests.yml` 第 19 ~ 33 行確認為設計（兩者僅 `workflow_dispatch` 觸發），**非連帶失敗**。`integration-qa` 使用 workflow 內建的 `postgres:16-alpine` service container 與 `ci/ci` 帳密（同檔第 40 ~ 60 行），**不依賴 staging 或任何 secret**，故與 T-0039 的 secret 釘版本無關。失敗訊息需 repo admin 權限（匿名 `/actions/jobs/{id}/logs` 回 403），**本卡讀不到，因此不立缺陷、不臆測根因**，列 R-16 與裁決事項 A。

### 7.5 本卡未能執行的驗證（誠實揭露）

| 項目 | 為什麼沒做 | 影響與替代 |
|---|---|---|
| **D-017 的 staging e2e 重跑** | 需 staging Basic Auth 憑證。**CLAUDE.md 安全鐵則：agent 不讀取、不索取、不寫入任何憑證值**；任務卡亦明令不索取、不使用 | **引用使用者實測紀錄**（40／40，`docs/reports/20260920-2132-D017-staging重跑-使用者實測.md`），來源已於 D-017 列與本報告各處註明為「使用者執行」。本卡**不宣稱為自身實測** |
| **TC-080 直接比對** | 同上，需憑證建立基準資料 | 維持使用者裁決②的追認判定；**補做仍列 P1**（R-2），且事故後新增「需重驗」理由 |
| **完整 e2e 136 條 ／ UAT 10 則對現行碼重跑** | e2e 需 staging 憑證；UAT 需瀏覽器操作並通過 Basic Auth | **未做**，列 R-14 與裁決事項 B，提請由使用者於自有機器執行（與 Firefox 兩組合併） |
| **Firefox 2 組相容性** | 沙盒與本機皆 `browserType.launch: spawn UNKNOWN`（T-0041 已排除三項假設） | 沿用 Leader 既有裁決（E-B-09），列 R-7 |
| **`QA Tests` 失敗訊息判讀** | `/actions/jobs/{id}/logs` 匿名存取回 403，需 repo admin | **不臆測根因、不立缺陷**；已取得的間接證據（`git diff` 為空、job 名單、`ci.yml` 對照組）全部寫入 §7.4，交裁決事項 A |
| **`00003-lt2` 的映像 digest 回推 commit** | 需 Artifact Registry 存取與映像標籤比對，逾本卡範圍 | R-15 的認定依據為事故報告與 `06` §6.10 的記載，已於該列註明此前提未經本卡獨立驗證 |
| NFR-001／NFR-007／CR 重測 | `src/` 本輪零變更（§7.4 實證），重測不會產生新資訊 | 標「沿用 r3」並加註量測標的為 `00003-lt2`，來源報告見第 2 節 |
