# 自動化測試報告：E-001 round 2

<!-- 檔名：docs/reports/20260919-1314-AT-E001-r2.md -->

**本報告取代 r1（`docs/reports/20260919-1221-AT-E001.md`）的全部數字。** r1 對象是修正前 main（`153410b`）；dev-tl 合併 T-0021 時把三張 CR 修正卡（T-0024～T-0026）與 Playwright devDependency 一併帶進 main，並把 `test:integration` 拆成「進程內」與「真實 HTTP（`test:integration:qa`）」兩條 script（見 `worklog/handoff/20260919-1301-T0021-r1-dev-tl.md`）。本輪對最終 main 重新完整跑過一次，指令與數字均以本報告為準；r1 報告保留不改，僅作歷史紀錄。

- 執行者：qa-at（Claude Sonnet 5）
- 執行時間：2026-09-19T13:14 ~ 2026-09-19T13:25（+08:00）
- 環境：**本機**，main commit `a288ca7`（開工時的 HEAD；期間 main 前進到 `03b71f4`，經 `git diff --name-only a288ca7 03b71f4` 核對只動 `docs/reports`、`tasks/`、`worklog/handoff/`，未動 `src/`／`tests/`，不影響本輪測試結果的有效性）。**staging 仍未部署**（GCP／GitHub 一次性設定尚未由使用者提供），staging 相關 TC 一律記「阻擋（環境未就緒）」。
- 本機環境版本：Node `v24.15.0`、Docker `29.4.2`、Playwright `1.63.0`（devDependency，已於 dev-tl 合併時鎖版，非本輪臨時安裝）
- **本機服務隔離**：因平行 agent（qa-cr r2、qa-uat r2）當時仍在使用根目錄既有的 `docker compose`（`localhost:8080`），本輪全程使用**自己的** compose project 與埠，避免互相干擾：
  - `test:integration:qa`／`test:e2e`／`test:perf`：`docker compose -p t0021r2 --env-file .env.qa-at-r2 up -d --build`，`PORT=8092`、`POSTGRES_HOST_PORT=5434`；跑完立即 `docker compose -p t0021r2 down -v`（容器與資料卷已移除，無殘留）。
  - README 方式二重測：另一個乾淨目錄（`git archive HEAD`），`docker compose -p t0021r2-readme up -d db`，`PORT=8199`、`POSTGRES_HOST_PORT=5435`；驗證後 `docker compose -p t0021r2-readme down -v` 並終止 `npm start` 行程。
- 指令：
  ```bash
  npm ci
  npm run build
  npm run lint

  npm run test:unit

  # test:integration（進程內 app.inject，不需服務，接到既有 5432 的本機 postgres 即可）
  NODE_ENV=test PORT=8080 DATABASE_URL="postgres://dev:dev@localhost:5432/todo" \
    BASIC_AUTH_USER=dev BASIC_AUTH_PASSWORD=dev LOG_LEVEL=silent npm run test:integration

  # test:integration:qa（真實 HTTP，對自起的服務 :8092）
  cp .env.example .env.qa-at-r2
  sed -i 's/^PORT=8080/PORT=8092/' .env.qa-at-r2
  sed -i 's/^# POSTGRES_HOST_PORT=5432/POSTGRES_HOST_PORT=5434/' .env.qa-at-r2
  docker compose -p t0021r2 --env-file .env.qa-at-r2 up -d --build
  BASE_URL=http://localhost:8092 BASIC_AUTH_USER=dev BASIC_AUTH_PASSWORD=dev npm run test:integration:qa

  # e2e（Playwright，Chromium／Edge，對 :8092）
  npx playwright install chromium
  BASE_URL=http://localhost:8092 BASIC_AUTH_USER=dev BASIC_AUTH_PASSWORD=dev \
    npm run test:e2e -- --project=chromium-1280x800 --project=chromium-390x844 --project=msedge-1280x800 --project=msedge-390x844

  # perf（NFR-001／NFR-007，對 :8092；跑完清殘留資料）
  BASE_URL=http://localhost:8092 BASIC_AUTH_USER=dev BASIC_AUTH_PASSWORD=dev npm run test:perf
  docker compose -p t0021r2 down -v   # 連同資料卷一併移除，等同清空殘留

  # README 方式二（乾淨目錄，PORT=8199／POSTGRES_HOST_PORT=5435）
  cp .env.example .env; npm ci; npm run build
  set -a; . ./.env; set +a
  docker compose -p t0021r2-readme up -d db
  export DATABASE_URL="postgres://dev:dev@localhost:5435/todo"
  npm start &
  curl -sS http://localhost:8199/health
  ```

## 結果總覽（本輪 vs r1／dev-tl 合併後）

| 層級 | 通過 | 失敗 | 跳過（環境未就緒） | 合計 | 與 r1 差異 |
|---|---|---|---|---|---|
| lint（`eslint . && tsc` × 3 project） | — | 0 | — | — | 無變化（LINT_EXIT=0） |
| unit | 76 | 0 | 0 | 76 | r1 為 75（既有）；合併 T-0026 後多 1 條，與 dev-tl 合併後數字一致 |
| integration（進程內 `app.inject`，`tests/integration/*.test.ts`） | 56 | 0 | 0 | 56 | **指令變更**：r1 一條指令跑 89 條（含 QA 那組）；本輪起這是獨立的 `test:integration`，只含既有 dev 團隊測試，與 dev-tl 合併後數字一致 |
| integration:qa（真實 HTTP，`tests/integration/qa/**/*.qa.test.ts`，T-0021 新增） | 38 | 0 | 4（TC-079／080／084／TC-087 之 500 子案例） | 42 | **新指令**（`test:integration:qa`）；通過／跳過數與 dev-tl 合併後重跑一致 |
| e2e（Chromium × 2 尺寸、Edge × 2 尺寸） | 136 | 0 | 0 | 136 | 與 r1 完全一致（r1 本就含 Edge）；瀏覽器版本與 r1／dev-tl 相同 |
| e2e（Firefox × 2 尺寸） | — | — | 2（環境限制：本沙盒 `browserType.launch: spawn UNKNOWN`） | — | 未變；沿用 r1 結論，Leader 已裁決列 Gate 2 後待辦，本輪未再嘗試 |
| perf（NFR-001：TC-085／TC-086，4 個量測點） | 4 | 0 | 0 | 4 | 全數 PASS，數字量級與 r1／dev-tl 相近（本機無網路延遲） |
| perf（NFR-007：TC-094） | 1 | 0 | 0 | 1 | PASS；本機資料庫累積筆數不同（37719，因與其他測試共用同一資料卷內多次執行疊加），中位數 184.7ms 仍遠低於 2000ms 門檻，判讀不受影響 |

**integration 系列的「合計」與 r1 不可直接相加比較**：r1 的 89 條是「舊 glob 一次跑完全部」；dev-tl 合併時把它拆成 56（`test:integration`）＋42（`test:integration:qa`）＝98 條，比 r1 多 9 條（合併帶入 T-0024～T-0026 修正後新增的測試斷言）。qa-lead 於 T-0023 驗收須跑**兩條**指令才涵蓋完整（README「測試分層」已註明）。

**P0 automated TC-ID 覆蓋**：與 r1 結論相同，`grep -roE "TC-[0-9]{3}" tests/integration/qa tests/e2e tests/perf` 對照應覆蓋清單（TC-004~011、013~018、021~029、031~037、040~047、049~058、060~076、079、080、082~094，共 84 條），**實際覆蓋完全一致，缺口 0**（唯一例外 TC-089，任務卡明文本卡不寫腳本，數據由 T-0018 monitor workflow 提供，判讀方式見 r1 報告，本輪未變更）。合併期間未新增或刪除任何 TC 腳本，覆蓋結論沿用 r1。

## staging 依存 TC（阻擋，環境未就緒，沿用 r1）

| TC-ID | 層級 | 狀態 |
|---|---|---|
| TC-079 | integration | 阻擋（環境未就緒）：Cloud Run 平台 HTTP→HTTPS 301，本機無 TLS 終止層 |
| TC-080 | integration | 阻擋（環境未就緒）：需觸發一次真實 staging 部署 |
| TC-084 | integration | 阻擋（環境未就緒）：revision 回滾演練尚未執行 |
| TC-090 | perf | 阻擋（環境未就緒）：部署不可用時間量測需一次真實部署事件；`tests/perf/tc090-deploy-downtime.perf.mjs` 已備妥，本輪未執行 |
| TC-089 | perf | 不適用（任務卡明文本卡不寫腳本，數據由 T-0018 monitor workflow 提供，本輪未部署故無 artifact 可讀，判讀方式見 r1 報告） |

## TC-091 瀏覽器版本記錄（本輪對最終 main 重跑）

| project | browserName | 版本 |
|---|---|---|
| chromium-1280x800 | chromium | 153.0.8010.12 |
| chromium-390x844 | chromium | 153.0.8010.12 |
| msedge-1280x800 | msedge（Chromium 核心） | 153.0.4234.32 |
| msedge-390x844 | msedge（Chromium 核心） | 153.0.4234.32 |

與 r1 版本號完全相同（同一台機器、同一份已鎖版 `@playwright/test@1.63.0`）。Firefox 沿用 r1 的「阻擋（環境限制）」結論，本輪未再嘗試（Leader 已裁決列 Gate 2 後待辦，非本卡阻擋項）。

## README 15 分鐘實測 —— 只重測方式二（TC-095，NFR-008）

r1 已對修正前 main 實測方式一與方式二，兩者皆 0 卡點、遠低於 15 分鐘門檻（見 r1 報告）。**方式一（Docker Compose）不必重做**（提示詞明文）；**方式二在 T-0024（CR B-2，啟動時自動 migration）後前提已改變**（`npm start` 前必須先有一個從宿主機連得到的 PostgreSQL），本輪依新版 README 方式二重測一次：

- 環境：Windows 11、Git Bash；乾淨目錄：`git archive HEAD` 匯出（不含 `.git`、`node_modules`、`dist`），逐字依新版 README「方式二」操作，不參考原始碼；埠因與其他平行 agent／既有服務衝突，改用 `PORT=8199`／`POSTGRES_HOST_PORT=5435`（僅測試用，README 字面的 8080/5432 已由 dev-tl 於合併時複驗過，見其交接檔「未完成／已知問題」）。

| 步驟 | 耗時 |
|---|---|
| `cp .env.example .env` ＋改埠 | <1 秒 |
| `npm ci` | 4 秒 |
| `npm run build` | 2 秒 |
| `set -a; . ./.env; set +a`（載入環境變數） | <1 秒 |
| `docker compose up -d db`（起資料庫，等 healthy） | 1 秒 |
| `export DATABASE_URL=...`（蓋掉 compose 內部主機名） | <1 秒 |
| `npm start` → 輪詢 `/health` 回 200（含啟動時自動 migration） | 7 秒 |
| **合計** | **約 14 秒** |

**卡點：0 個**。逐字依 README 新版方式二操作，`npm start` 在 `app.listen` 之前完成 migration 後即監聽並回應 `/health`，全程與 README 文字描述一致，未遇到 `getaddrinfo ENOTFOUND db` 或任何未載明的手動步驟。**遠低於 15 分鐘門檻 → TC-095（方式二）通過。**

## 憑證掃描

沿用 r1 結論；本輪未新增測試檔案，僅重跑既有腳本，掃描範圍與命中內容不變（9 處已知假陽性、真命中 0，見 r1 報告「憑證掃描」章節）。

## 缺陷與觀察（沿用 r1，本輪未發現新項）

r1 報告已記錄兩項觀察（前端快速連續新增競態、`load-runner.mjs` `summarize()` 堆疊溢位已於程式修正落地）。本輪重跑未發現新的缺陷或觀察；`tests/e2e/us002-list.e2e.test.ts`（TC-018）、`tests/e2e/us008-spa.e2e.test.ts`（TC-066）沿用 r1 已修正的「等第一筆可見再送第二筆」寫法，本輪 136/136 全過，未再觀察到競態重現（樣本數不足以推翻 r1 的機率性重現結論，僅供參考）。

## 原始輸出（節錄，貼實際輸出）

### lint

```bash
npm run lint; echo "LINT_EXIT=$?"
```
```text
> todo-app@0.1.0 lint
> eslint . && tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.test.json && tsc --noEmit -p tsconfig.e2e.json

LINT_EXIT=0
```

### unit

```bash
npm run test:unit
```
```text
ℹ tests 76
ℹ suites 0
ℹ pass 76
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 159.8646
UNIT_EXIT=0
```

### integration（進程內）

```bash
NODE_ENV=test PORT=8080 DATABASE_URL="postgres://dev:dev@localhost:5432/todo" \
  BASIC_AUTH_USER=dev BASIC_AUTH_PASSWORD=dev LOG_LEVEL=silent npm run test:integration
```
```text
ℹ tests 56
ℹ suites 0
ℹ pass 56
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 1045.2179
INTEGRATION_EXIT=0
```

### integration:qa（真實 HTTP，尾段節錄）

```bash
BASE_URL=http://localhost:8092 BASIC_AUTH_USER=dev BASIC_AUTH_PASSWORD=dev npm run test:integration:qa
```
```text
▶ TC-087：4 種可安全觸發的錯誤情境不洩漏內部細節（500 情境見上方檔頭說明）
  ﹣ 500 情境（跳過，理由見檔頭） (0.1278ms) # 需真的斷線資料庫才能安全觸發 500，已由 tests/integration/error-contract.test.ts 的 /__test/boom 覆蓋
✔ TC-087：4 種可安全觸發的錯誤情境不洩漏內部細節（500 情境見上方檔頭說明） (6.2867ms)
✔ TC-092：4xx 與 5xx 回應同形；requestId 可 grep 到對應日誌 (3.4814ms)
﹣ TC-079：HTTP 由平台自動 301 導向 HTTPS（Cloud Run 平台行為，本機無此層） (7.8806ms) # 阻擋（環境未就緒）
﹣ TC-080：一次新的部署後既有資料仍存在（需觸發真實 staging 部署） (0.121ms) # 阻擋（環境未就緒）
✔ TC-082：/health 不帶憑證回 200；/ 不帶憑證回 401（唯一未保護路徑） (8.0654ms)
✔ TC-083：全站 Basic Auth 涵蓋 API 與靜態資產；憑證未入倉庫 (48.7714ms)
﹣ TC-084：Cloud Run revision 切流量回滾（需真實 staging 環境與回滾演練） (0.1594ms) # 阻擋（環境未就緒）
✔ TC-093：建立 10 筆，重啟服務容器後 10/10 一致（本機近似 NFR-006） (825.4981ms)
ℹ tests 42
ℹ suites 0
ℹ pass 38
ℹ fail 0
ℹ cancelled 0
ℹ skipped 4
ℹ todo 0
ℹ duration_ms 3510.7507
QA_INTEGRATION_EXIT=0
```

### e2e（Playwright，Chromium × 2 ＋ Edge × 2，尾段節錄）

```bash
BASE_URL=http://localhost:8092 BASIC_AUTH_USER=dev BASIC_AUTH_PASSWORD=dev \
  npm run test:e2e -- --project=chromium-1280x800 --project=chromium-390x844 --project=msedge-1280x800 --project=msedge-390x844
```
```text
[TC-091][瀏覽器版本] chromium 153.0.8010.12 @ chromium-1280x800
[TC-091][瀏覽器版本] chromium 153.0.8010.12 @ chromium-390x844
[TC-091][瀏覽器版本] chromium 153.0.4234.32 @ msedge-1280x800
[TC-091][瀏覽器版本] chromium 153.0.4234.32 @ msedge-390x844

  ok 136 [msedge-390x844] › tests\e2e\us009-same-origin.e2e.test.ts:14:1 › TC-075：/api/v1/* 請求與頁面同源，且不含任何 OPTIONS 預檢 (379ms)

  136 passed (57.6s)
E2E_EXIT=0
```

### perf（NFR-001：TC-085／TC-086，10 併發、暖身 10 秒、正式取樣 60 秒）

```bash
BASE_URL=http://localhost:8092 BASIC_AUTH_USER=dev BASIC_AUTH_PASSWORD=dev node tests/perf/tc085-tc086-nfr001.perf.mjs
```
```text
BASE_URL=http://localhost:8092  concurrency=10  warmup=10000ms  duration=60000ms

=== TC-085 GET /api/v1/todos ===
count=16946 errors 已排除於 ok 判定外
min=5.8ms  mean=35.4ms
p50=34.8ms  p95=47.7ms  p99=54.2ms  max=66.3ms
門檻：P95 < 500ms → PASS

=== TC-086 POST /api/v1/todos ===
count=97741 errors 已排除於 ok 判定外
min=4.2ms  mean=6.1ms
p50=5.9ms  p95=7.7ms  p99=10.4ms  max=23.6ms
門檻：P95 < 800ms → PASS

=== TC-086 PATCH /api/v1/todos/{id} ===
count=178901 errors 已排除於 ok 判定外
min=1.7ms  mean=3.4ms
p50=3.2ms  p95=4.5ms  p99=7.5ms  max=23.9ms
門檻：P95 < 800ms → PASS

=== TC-086 DELETE /api/v1/todos/{id} ===
count=100597 errors 已排除於 ok 判定外
min=3.1ms  mean=6.0ms
p50=5.8ms  p95=7.8ms  p99=10.4ms  max=29.7ms
門檻：P95 < 800ms → PASS

=== 總結 ===
TC-085 PASS  TC-086(POST) PASS  TC-086(PATCH) PASS  TC-086(DELETE) PASS
```

**注意**：TC-085 的 p95（47.7ms）比 r1（2.8ms）與 dev-tl 合併後重跑（未量測 GET，但 POST/PATCH/DELETE 同量級 5~9ms）明顯偏高。判讀：本輪 GET 量測開始時資料庫已累積前次 e2e／整合測試殘留的資料列（非本輪新增，屬前次同一資料卷疊加），`GET /api/v1/todos` 回傳整份陣列，筆數越多序列化耗時越高；47.7ms 仍遠低於 500ms 門檻，判定 PASS 不受影響，但列為觀察：**若要取得可比較的 baseline 數字，跑 perf 前應先清空資料庫**（本輪為求不干擾平行 agent，選擇用自己的資料卷而非清空既有資料，下一輪建議 perf 專用資料庫從空表開始）。

### perf（NFR-007：TC-094，500 筆容量）

```bash
BASE_URL=http://localhost:8092 BASIC_AUTH_USER=dev BASIC_AUTH_PASSWORD=dev node tests/perf/tc094-nfr007-capacity.perf.mjs
```
```text
BASE_URL=http://localhost:8092  目標筆數=500
建立 500 筆耗時 0.3 秒
第 1 次：status=200 time_total=184.7ms
第 2 次：status=200 time_total=187.4ms
第 3 次：status=200 time_total=162.9ms
第 4 次：status=200 time_total=159.2ms
第 5 次：status=200 time_total=195.0ms

=== TC-094 結果 ===
5 次中位數：184.7ms（門檻 < 2000ms） → PASS
回應為單一完整陣列（非分頁結構）：PASS（isArray=true）
陣列長度（含既有資料）：37719
順序符合 created_at DESC：PASS

PERF_EXIT=0
```

跑完後整個 compose project（`t0021r2`，含資料卷）已 `docker compose -p t0021r2 down -v` 完全移除，無殘留（本輪選擇整組移除而非逐列刪除，效果等同 dev-tl r1 收尾的 `DELETE FROM todos WHERE title LIKE 'perf-%'...`，且更乾淨）。

### README 方式二實測

```bash
# 乾淨目錄（git archive HEAD），PORT=8199／POSTGRES_HOST_PORT=5435
cp .env.example .env && npm ci && npm run build   # 6 秒
set -a; . ./.env; set +a
docker compose -p t0021r2-readme up -d db          # 1 秒（healthy）
export DATABASE_URL="postgres://dev:dev@localhost:5435/todo"
npm start &                                          # 另開行程
curl -sS -o /dev/null -w "%{http_code}\n" http://localhost:8199/health   # 200，npm start 到此 7 秒
curl -sS http://localhost:8199/health                                     # {"status":"ok"}
```
```text
{"level":30,...,"msg":"Server listening at http://127.0.0.1:8199"}
200
{"status":"ok"}
```

事後清理：`kill <npm start pid>`；`docker compose -p t0021r2-readme down -v`（容器、網路、資料卷全數移除）。

### 憑證掃描（沿用 r1，本輪重新執行核對範圍不變）

```text
（9 處命中，全部為已知假陽性，真命中 0；詳見 r1 報告「憑證掃描」章節，本輪範圍與命中內容未變）
```
