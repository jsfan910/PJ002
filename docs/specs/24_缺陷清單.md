---
doc: DEFECTS
epic: E-001
author: qa-lead
updated: 2026-09-20T21:45:00+08:00
---

# 缺陷清單：E-001 待辦事項 Web 應用

本檔為退出準則第 4 項（`07_測試計畫.md` §4）的唯一事實來源：**`severity ∈ {S1, S2}` 且 `status ≠ closed` 的筆數**即為「開放 S1／S2 缺陷數」。
嚴重度定義見 `07_測試計畫.md` §5.1；「阻擋級 CR」的判準見 §5.2；缺陷成立要件（嚴重度＋可重現步驟＋對應 TC＋來源＋狀態）見 §5.3。

**目前計數（2026-09-20T21:45:00+08:00，qa-lead 於 T-0046 核實；上一版為 T-0030 的 2026-09-19T17:38:24）**

| 指標 | 值 |
|---|---|
| 登錄缺陷總數 | **18**（D-001 ~ D-018） |
| **開放 S1**（`severity = S1` 且 `status ≠ closed`） | **0** |
| **開放 S2**（`severity = S2` 且 `status ≠ closed`） | **0**（D-002、**D-018** 兩筆 S2 皆 `closed`） |
| 開放 S3 | **1**（僅 D-014 `runMigrations` 無互斥鎖，經 Leader 2026-09-19T13:15:01 裁決列 P1、不擋 Gate 2）。**D-017 已於本輪關閉** |
| 開放 S4 | 0 |
| 已關閉（closed） | **16**（本輪新增 D-017、D-018 兩筆） |
| 已關閉（wontfix，記於 closed 之外） | 1（D-015） |
| 另登錄「環境阻擋」項（依 Leader 裁決不計為缺陷） | 9（E-B-01 ~ E-B-09，見第 2 節）；其中 **8 條已於 2026-09-19 staging 部署後解除**，**1 條仍阻擋**（E-B-09 Firefox；T-0041 於本機重跑仍 68／68 啟動失敗，判工具限制） |

**退出準則第 4 項（`07` §4）據此為 0 筆**，達成。

**T-0046（2026-09-20）變更摘要**：
- **D-017 → `closed`**：使用者於自有機器對 staging revision `todo-app-00019-k5q` 重跑 TC-067／TC-009 × 4 project × 5 輪 **40／40 全過**，達成 Leader 所定「至少 5 次連續全過」的關閉條件（證據 `docs/reports/20260920-2132-D017-staging重跑-使用者實測.md`，非本卡實測）。修正任務卡 T-0038。
- **新增 D-018（S2／closed）**：`deploy-staging` verify 只打 service URL、不驗新 revision 是否接到流量，使 15 個 revision 未上線而每次 run 全綠；T-0045 已修（主判準改讀 `status.traffic` 加總 = 100）並經兩次真實部署驗證。嚴重度判定理由與對測試結論的影響見該列。
- 開放 S3 由 2 降為 **1**；登錄總數 17 → 18。

**T-0030（2026-09-19）變更摘要**：新增 D-016（TC-079 規格文字，S4／closed）、D-017（TC-067／TC-009 間歇性，S3／open／P1）、OBS-7（staging 並行存取干擾，觀察非缺陷）；E-B-01 ~ E-B-08 因 staging 於 T-0027 部署成功而解除，狀態逐條更新。

---

## 1. 缺陷

| D-ID | 嚴重度 | 標題 | 重現步驟 | 預期／實際 | 對應 TC | 來源 | 狀態 | 修正任務卡 |
|---|---|---|---|---|---|---|---|---|
| D-001 | **S1** | `/health` 豁免比對做了 URL 正規化，點節段／百分號編碼可繞過全站 Basic Auth（`src/plugins/basic-auth.ts:45-52`） | 1. `docker compose up -d --build` 起服務 2. 不帶任何憑證執行 `curl -sS --path-as-is -o /dev/null -w "%{http_code}" "http://localhost:<PORT>/foo/../health"` 3. 同樣方式再送 `/.%2e/health`、`/%2e%2e/health`、`/assets/%2e%2e/%2e%2e/health` | 預期：四者皆 **401**（SD §6.2 硬性要求「豁免比對必須是完全相等的字串」）／實際（r1）：四者皆 **403**——已越過認證 hook，僅被 `@fastify/static` 的路徑穿越防護擋下 | TC-082、TC-083 | CR（r1 B-1） | **closed** | T-0024 |
| D-002 | **S2** | migration 未在應用啟動時自動套用，依 README 方式一起來的環境所有待辦 API 回 500（`src/server.ts:10-20`、`Dockerfile:49`、`docker-compose.yml:25-41`） | 1. `docker compose down -v` 清空資料卷 2. `docker compose up -d --build` 3. **不執行任何 migrate 指令**，`curl -sS -u <user>:<pass> http://localhost:<PORT>/api/v1/todos` | 預期：`200 []`（`05_資料庫設計.md` §3 第 4 條「於應用啟動時自動執行」、SD §7 NFR-008 對策④）／實際（r1）：`500`，日誌為 `relation "todos" does not exist` | TC-004、TC-014、TC-095 | CR（r1 B-2） | **closed** | T-0024 |
| D-003 | S3 | ajv 預設 `coerceTypes: true` 使錯誤型別被脅迫後放行（`src/schemas/todo-schema.ts:55-82`，生效點 `src/routes/todos.ts:70-100`） | 1. 服務啟動 2. `POST /api/v1/todos` body `{"title":123}` 3. `PATCH /api/v1/todos/{id}` body `{"isCompleted":null}` | 預期：兩者皆 **400 `E_VALIDATION`**（`04_API規格.yaml` 宣告 `type: string`／`boolean`）／實際（r1）：步驟 2 回 **201** 且標題存成 `"123"`；步驟 3 被脅迫成 `false` 後回 200 | TC-005、TC-006、TC-023 | CR（r1 S-1） | **closed** | T-0024 |
| D-004 | S3 | `/health` 豁免不分 HTTP 方法，未認證者可藉此探測路由是否存在（`src/plugins/basic-auth.ts:50-52`、`74-78`） | 1. 服務啟動 2. 不帶憑證 `curl -sS -X POST -o /dev/null -w "%{http_code}" http://localhost:<PORT>/health` | 預期：**401**（SD §5、§6.2 未保護路徑只列 `GET /health`）／實際（r1）：**404**——等於回答了「此路徑不存在 POST 路由」 | TC-082 | CR（r1 S-2） | **closed** | T-0024 |
| D-005 | S3 | workflow 把 secrets 以 `${{ }}` 直接展開進 `run:` 命令列（`.github/workflows/deploy-staging.yml:139-141`） | 1. 開啟該檔第 139~141 行 2. `grep -n "secrets\." .github/workflows/deploy-staging.yml`，檢視命中是否落在 `run:` 區塊內 | 預期：`run:` 內無任何 `${{ secrets.* }}` 展開，一律經 `env:` 中介（GitHub hardening 指南；同檔 `:75-83` 已是正確寫法）／實際（r1）：`curl -u "${{ secrets.* }}:${{ secrets.* }}"` 直接進命令列 | TC-083 | CR（r1 S-3） | **closed** | T-0025 |
| D-006 | S3 | `npm run lint` 的 `tsc --noEmit` 完全不檢查 `tests/`（`tsconfig.json:21-22`） | 1. 於 `tests/` 任一 `.ts` 測試檔寫入一個明顯型別錯誤 2. 執行 `npm run lint` | 預期：lint 失敗（測試碼的型別防線成立）／實際（r1）：`LINT_EXIT=0`——10 個測試檔的型別錯誤要到執行期才炸 | TC-092（測試品質橫切，無專屬 TC；判準來源為 `07_測試計畫.md` §2「測試品質」檢查項） | CR（r1 S-4） | **closed** | T-0025 |
| D-007 | S3 | 前端 `load()` 無請求序列化，先發後到的回應會覆蓋較新結果（`public/assets/todo-store.js:126-137`） | 1. 開啟清單頁 2. 對 `GET /api/v1/todos` 注入人為延遲（例如 1500ms）僅套用於第一次請求 3. 快速連續點擊「未完成」→「全部」兩個篩選 | 預期：畫面內容永遠與 `state.filter` 一致（BR-011「事實來源永遠是後端」）／實際（r1）：先發出的回應後到並覆蓋，畫面顯示的是舊篩選的結果 | TC-053、TC-054、TC-055 | CR（r1 S-5） | **closed** | T-0026 |
| D-008 | S3 | 編輯模式期間的重繪會吃掉使用者已鍵入、尚未儲存的文字（`public/assets/todo-view.js:146-186`、`:257-263`） | 1. 清單至少兩筆 2. 點第一筆「編輯」，在輸入框鍵入新文字但**不儲存** 3. 點第二筆的完成狀態核取方塊（觸發 store 通知與 `load()`） | 預期：第一筆編輯框內的草稿保留／實際（r1）：該 `<li>` 整段重繪，輸入框被重建並回復為 `todo.title`，草稿消失 | TC-026、TC-027 | CR（r1 S-6） | **closed** | T-0026 |
| D-009 | S3 | 監測腳本 `curl -f` 吞掉真實狀態碼，NFR-003 判讀規則失效（`.github/workflows/monitor-health.yml:47`） | 1. 開啟該行 2. 以任一回 4xx／5xx 的端點模擬：`curl -fsS -o /dev/null -w "%{http_code} %{time_total}" <url> \|\| echo "000 0"` | 預期：CSV 記錄真實狀態碼（4xx／5xx 可與「連不上」區分，SD §7 NFR-003 判讀規則要求）／實際（r1）：`-f` 使任何 4xx／5xx 直接失敗，一律落到 `000` | TC-089 | CR（r1 S-7） | **closed** | T-0025 |
| D-010 | S4 | `.env.example` 的佔位值與凍結 SD §6.2「只寫名稱」字面矛盾（`.env.example:21-31` vs `03_系統設計書_SD.md:261`） | 1. 對照 `.env.example` 第 21~31 行與 SD 第 261 行 | 預期：兩者不互相矛盾（或矛盾處已有權威註記）／實際（r1）：Leader 裁決 D-01 已允許本機佔位值，但凍結規格文字未同步，留給下一輪 CR 重新論證 | TC-083 | CR（r1 S-8） | **closed**（依 Leader 2026-09-19T12:03:12 裁決②改以 CR 模板第 4 類已知例外處理，不改凍結 SD 文字） | T-0025 |
| D-011 | S3 | Cloud Run 參數在三處各寫一份且已開始飄移（`infra/cloudrun-service.yaml:36-103` vs `deploy-staging.yml:96-114` vs `scripts/deploy-staging.sh`） | 1. 逐一比對三處的容器埠、min／max-instances、`--set-secrets` 名稱、`livenessProbe`、`--allow-unauthenticated` | 預期：三處一致，或有一張「必須同步的參數表」使飄移不會無聲發生／實際（r1）：`livenessProbe` 只在 IaC 檔、`--allow-unauthenticated` 只在兩支 gcloud 指令 | TC-078、TC-084 | CR（r1 S-9） | **closed** | T-0025 |
| D-012 | S3 | `docker-compose.yml:15-16` 把 db 的 5432 綁死宿主機，多專案／多 worktree 並行必撞埠 | 1. 已有一組 compose 佔用宿主機 5432 2. 於另一個 compose 專案 `docker compose -p <其他名稱> up -d` | 預期：可用環境變數改宿主機埠而不改檔／實際（r1）：埠寫死，第二組必然啟動失敗 | TC-095 | CR（r1 S-10） | **closed** | T-0025 |
| D-013 | S3 | `npm run dev` 必然失敗卻仍列在 README 常用指令表且無但書；`engines.node` 過寬（`package.json:11`、`:8`、`README.md:85`） | 1. 乾淨環境 `npm ci` 2. `npm run dev` | 預期：指令可用，或 README 標明與 `migrate`／`test` 相同的平台但書／實際（r1）：`ERR_MODULE_NOT_FOUND: ... src\app.js`（Node 型別剝除不會把 `./app.js` 對應回 `.ts`） | TC-095 | CR（r1 S-11） | **closed** | T-0025 |
| D-014 | S3 | `runMigrations` 無互斥機制，多實例同時對全新資料庫冷啟時其中一方 `process.exit(1)`（`src/db/migrate.ts:76-105`，觸發點 `src/server.ts:35-56`） | 1. 準備一個全新（無 `schema_migrations`）資料庫 2. 同時啟動兩個以上應用實例指向同一資料庫 3. 觀察兩者日誌與結束碼 | 預期：兩個實例皆正常監聽（migration 併發安全）／實際：兩者都讀到 `schema_migrations` 為空並執行 `001`，`INSERT INTO schema_migrations (version)` 一方撞 PK 唯一鍵 → `runMigrations` 拋例外 → 該實例 `process.exit(1)` | TC-080（最接近；**現有 TC 未涵蓋「多實例並行冷啟」，建議 P1 補一條**） | CR（r2 N-1） | **open** | 未建卡——Leader 2026-09-19T13:15:01 裁決「列 P1 待辦，與 `002` migration 同卡處理」（Cloud Run `min-instances = 0`、staging 單實例，P0 不受影響） |
| D-015 | S4 | 前端 fetch 未設 `credentials: "include"`，導致（r1 的）自動化瀏覽器工具取不到 Basic Auth 快取 | 1. 以自動化瀏覽器工具導向 `http://<user>:<pass>@localhost:<PORT>` 2. 觀察頁面初始化時 `GET /api/v1/todos` 是否帶 `Authorization` | 預期：清單頁正常載入／實際（r1）：工具環境下回 401 `E_UNAUTHORIZED`，畫面顯示「無法連線到伺服器」 | TC-008、TC-016、TC-026、TC-034、TC-045、TC-053、TC-066 | UAT（r1） | **wontfix**（非產品缺陷，見下方理由） | 不建卡 |
| D-016 | S4 | **規格文字**寫死「HTTP→HTTPS 由平台回 **301**」，Cloud Run／Google Frontend 實際回 **302**（`docs/specs/20_測試案例.md:156` TC-079 判準「回 **`301`**（Google 前端固定回 301，非『3xx 皆可』；ADR-0005 決定第 7 點）」、`docs/specs/06_部署架構與CICD.md:45` 「HTTP 請求由 Google 前端 301 導向 HTTPS」） | 1. `curl -sSI "http://todo-app-dpevsdhdva-de.a.run.app/health"` 2. 讀第一行狀態碼與 `location` 標頭 | 預期（規格字面）：`HTTP/1.1 301 Moved Permanently`／實際（qa-at r3 2026-09-19T17:19 與 qa-lead T-0030 於 2026-09-19T17:32 各自獨立實測）：`HTTP/1.1 302 Found`，`location: https://todo-app-dpevsdhdva-de.a.run.app/health`。**導向行為與 HTTPS 強制的規範意圖完全成立**，僅狀態碼字面不符；此為 Cloud Run 平台行為，團隊零程式碼可改 | TC-079 | AT（r3，staging） | **closed** —— Leader 2026-09-19T17:30:10 裁決①「**接受 302 等效**（意圖是強制 HTTPS），本輪判 TC-079 **通過**並註記」。本筆以「規格文字待修」登錄，非產品缺陷，故關閉不擋 Gate 2 | 未建卡 —— Leader 裁決「SRS／`06`／`20_測試案例.md` 文字修正列 **P1『規格變更請求』**」；文字建議：判準改為「回 **3xx**（301 或 302 皆視為合規）且 `Location` 為對應的 `https://` 網址」 |
| D-017 | **S3** | e2e **TC-067／TC-009 對 staging 間歇性失敗**：前端錯誤訊息元素（`getByTestId("error-message")`）在真實網路延遲下有時維持 `hidden`，斷言逾時（`public/assets/todo-view.js` 錯誤狀態更新與 `public/assets/todo-store.js` 的請求／狀態通知時序疑有競態） | 1. `export BASE_URL=https://todo-app-dpevsdhdva-de.a.run.app` 2. `npm run test:e2e -- --project=msedge-1280x800 --project=msedge-390x844 -g "TC-067\|TC-009"` 3. **重複執行 3 ~ 5 次**（單次執行可能全過；本機 docker compose／HTTP-1.1 從未重現，僅對 staging 重現） | 預期：TC-067 步驟 2 顯示可讀錯誤訊息（BR-029）、TC-009 空白標題顯示可讀錯誤（BR-030），每次執行皆通過／實際（AT r3，乾淨單獨執行、已排除並行污染）：136 條中 **3 條失敗**（`[msedge-1280x800] TC-067`、`[msedge-390x844] TC-009`、`[msedge-390x844] TC-067`），失敗率約 2 ~ 4 成；失敗訊息 `expect(locator).toBeVisible() failed / Locator: getByTestId("error-message") / Expected: visible / Received: hidden / Timeout: 5000ms`；重跑驗證 4 條中 1 過 3 敗 | TC-067、TC-009 | AT（r3，staging） | **closed**（qa-lead 於 T-0046，2026-09-20T21:4x 判定） | **T-0038**（dev-fe，已合併 main 並經 T-0045 事故復原後真正上線）。<br>**關閉條件與實際值**：Leader 2026-09-19T17:30:10 裁決②所定之關閉條件為「修正後重跑 TC-067／TC-009 **至少 5 次連續全過**」。實際：**40／40 全過**（TC-067、TC-009 × 4 個 project〔chromium-1280x800、chromium-390x844、msedge-1280x800、msedge-390x844〕× 5 輪，每輪 8 tests，`8 passed`×5）。<br>**證據來源**：`docs/reports/20260920-2132-D017-staging重跑-使用者實測.md`（**由使用者本人於自有機器對 staging 執行並逐字轉錄；本卡 qa-lead 無 staging Basic Auth 憑證，未親自執行，不宣稱為本卡實測**）。受測標的為 revision **`todo-app-00019-k5q`**（含 T-0038 修正）。<br>**關閉的前置事實**：2026-09-20 20:3x 的前一次重跑（msedge 全敗）**不計為未達關閉條件** —— 當時流量仍釘死在 `todo-app-00003-lt2`（不含 T-0038／T-0043），打到的是修正前的版本，見 D-018 與 `docs/specs/06_部署架構與CICD.md` §6.10。<br>**TC-009 於 msedge 4 輪全過亦連帶證明 `/favicon.ico` 404 已消除**（T-0043）。 |
| D-018 | **S2** | **`deploy-staging` 的 verify 只打 service URL，不驗證本次新建的 revision 是否真的接到流量**：回滾演練把 `spec.traffic` 由 `latestRevision: true` 改為釘定具名 revision 後，之後每一次 `gcloud run deploy` 都會建立新 revision 並通過 startup probe，但**流量不會自動切過去**；verify 只確認「service URL 有回應」，而舊 revision 一直健康地回應著，因此 **15 個 revision（`00004-7p5` ~ `00018-xq4`）連續 28 小時 11 分未上線，而每一次 run 都判定為全綠**（`.github/workflows/deploy-staging.yml`、`scripts/deploy-staging.sh` 的 verify 步驟；根因路徑見 `docs/specs/06_部署架構與CICD.md` §6.10.1、§6.10.3） | 1. 對已部署的 Cloud Run 服務執行 `gcloud run services update-traffic <svc> --region <r> --to-revisions <某個舊 revision>=100`（等同 `scripts/rollback-staging.sh` 的回滾演練，**且不執行還原**）2. 確認 `gcloud run services describe <svc> --format="yaml(spec.traffic)"` 已由 `latestRevision: true` 變成 `revisionName: <舊 revision>` 3. 對 `main` 推送任意會觸發 `deploy-staging` 的提交 4. 等待該 run 結束，讀其 `conclusion` 5. 執行 `gcloud run services describe <svc> --format="csv[no-heading](status.traffic.revisionName,status.traffic.percent)"`，比對實際接流量的 revision 是否為本次新建者 | 預期：步驟 4 的 run 應判**失敗**（或自動把流量切到新 revision 後才判成功），因為本次部署的產物並未實際對外服務／實際（修正前，2026-09-19 16:33 ~ 2026-09-20 20:44 共 15 次）：步驟 4 每一次皆 `conclusion=success`，步驟 5 回傳的永遠是 `todo-app-00003-lt2,100`。**Leader 於 2026-09-20T20:41:17 以人工比對才發現**，自動化零告警 | TC-090（本缺陷直接使 TC-090 的既有量測失效，見下方「對測試結論的影響」）、TC-084（回滾演練為觸發路徑；演練程序本身正確，缺的是「演練後歸位」） | **維運事故實查**（Leader 2026-09-20T20:41:17 於使用者重跑 D-017 異常時實查 `services describe` 發現）。**註**：`07_測試計畫.md` §5.3 列舉的來源為 unit／integration／e2e／perf／CR／UAT 六類，本筆不屬其中任一；依 §5.3 的實質要件（嚴重度＋可重現步驟＋對應 TC＋來源＋狀態五項齊備）成立為缺陷，來源欄據實記為「維運事故實查」，**此為對 §5.3 列舉的偏離，已在測試總結 r4 §6 裁決事項 C 提請 Leader 追認** | **closed**（T-0045 已修並經真實部署驗證，2026-09-20） | **T-0045**（dev-ops，dev-tl 兩階段審核，合併 `f015ea1`）。<br>**修法**：verify 主判準改為直接讀 `status.traffic`、**加總本次新 revision 拿到的百分比並要求等於 100**；`status.latestReadyRevisionName` 相等僅作輔助佐證（dev-tl 初審 r1 指出單看後者會漏判分流／canary，已更正）；未達 100% 即自動 `update-traffic --to-latest` 並重新驗證，仍未達才判紅。`scripts/rollback-staging.sh` 新增 `--to-latest` 還原模式與檔頭警語；README 同步。詳見 `06` §6.10.4。<br>**驗證**：真實 `deploy-staging` run [`35513187463`](https://github.com/jsfan910/PJ002/actions/runs/35513187463)（`95bc4ca`）`conclusion=success`，`services describe` 唯讀複查 `todo-app-00019-k5q,100`。qa-lead 於 T-0046 另獨立複查**第二次**修正後的真實部署 run `35513953747`（`712b4a3`，2026-09-20T13:35:28Z~13:37:36Z UTC）：`conclusion=success`，流量已自動跟隨至 `todo-app-00020-z8j`（`spec.traffic: latestRevision: true`、`status.traffic` 100%），**修法在連續兩次真實部署上成立**（輸出見測試總結 r4 §7.1）。<br>**嚴重度判定理由（依 `07` §5.1）**：**不判 S1** —— 全程無系統不可用（舊 revision `00003-lt2` 持續正常服務，24 小時 uptime check 1156／1156 成功）、無資料遺失（Neon 與運算實例分離，切流量不觸碰資料層）、無安全漏洞（Basic Auth 與 `/health` 豁免行為未受影響）。**判 S2（主要功能失效，無繞過）** —— ①失效的是部署管線 verify 步驟的**核心職能**（確認「本次部署的產物真的在服務」），該職能**完全不生效**而非部分退化；②**無繞過**：管線內不存在任何替代訊號會發現此狀態，15 次 run 全綠、零告警，只能靠人工比對；③後果是「綠燈即上線」這個團隊賴以判斷的前提在 28 小時內持續為假，並因此**使既有測試結論失效**（見下）。**不判 S3** —— S3 的要件是「有繞過」，本筆無。<br>**對測試結論的影響（qa-lead 於 T-0046 定案）**：①**T-0031 §6.8.4「revision `00008-kcs` 部署期間 0 秒不可用」的量測無效** —— 流量根本未切，量到的是「舊版本全程持續服務」而非「切流量很快」；②**AT r3 的 TC-090 兩次量測（228 樣本）是否真的涵蓋一次流量切換存疑**，不再作為第 12 項的主要證據；③**退出準則第 12 項改引 T-0045 的真實部署重測**（226／226 樣本、0 秒）；④**P0 功能面的既有結論仍然有效** —— `00003-lt2` 是 2026-09-19 16:05 的 main（含 P0 全功能與 CR r2 修正），AT r3／UAT r4／NFR-003 24 小時採樣皆對「P0 程式」成立（見測試總結 r4 §1 第 6 項附註）。<br>**殘留**：`06` §5.1 的回滾流程**規格文字**尚未寫入「演練收尾必 `--to-latest` 還原」（腳本與 README 已有，規格未同步），已列 P1 規格同步卡（plan-sd），見 `tasks/_todo.md`。 |

### D-015 判定理由（qa-lead 於 T-0023 依 Leader 2026-09-19T12:18:14 裁決「交 qa-lead 以兩造證據裁定」）

兩造證據：
- qa-uat r1 主張 `public/assets/api-client.js:64-68` 缺 `credentials: "include"`。
- dev-tl 在 T-0017 審核時於真實 Chrome 逐 AC 實測 34 條全過；同源 `fetch` 的預設值為 `credentials: "same-origin"`，本來就會帶上瀏覽器已持有的 HTTP 認證。

qa-lead 本輪**親自複驗**（2026-09-19T13:53，自用 compose 專案 `qal23`、埠 8093）：先以 `http://dev:dev@localhost:8093/` 載入一次建立認證快取，再以**裸網址** `http://localhost:8093/` 重新載入 —— 清單頁正常載入（`location.href` 確認為不含帳密的 `http://localhost:8093/`），新增、切換完成、三種篩選全部成功，無任何 401 或錯誤橫幅。

結論：**成因是自動化瀏覽器工具的首次認證握手方式，不是應用程式缺陷**。依 `07_測試計畫.md` §5.3，產品端無可重現的偏差，故記 **wontfix**，並依 Leader 裁決以「阻擋（工具）」的性質處理，**不計入開放 S1／S2**。UAT r2 沿用「先帶帳密載入再用裸網址」的方式後，P0 十則 10/10 通過。

---

## 2. 環境阻擋登錄（依 Leader 2026-09-19T07:40:06 對 T-0009 的裁決第 6 項：staging 未就緒的 TC 記「阻擋（環境未就緒）」而非「不通過」，**不計為缺陷**）

| E-ID | 嚴重度 | 標題 | 重現步驟 | 預期／實際 | 對應 TC | 來源 | 狀態 | 修正任務卡 |
|---|---|---|---|---|---|---|---|---|
| E-B-01 | 不適用（環境阻擋） | HTTP 由平台 301 導向 HTTPS 無法驗證 | 1. `BASE_URL=$STAGING_BASE_URL npm run test:integration:qa` 2. 觀察 TC-079 | 預期：`curl -sSI http://<staging>/` 回 301 至 https／實際：staging 未部署，本機 docker compose 無 TLS 終止層，測試 skip | TC-079 | AT（r2） | **已解除（T-0030）** —— staging 已部署，AT r3 實測回 **302**（非 301）；Leader 2026-09-19T17:30:10 裁決①接受 302 等效，**TC-079 判通過**，規格文字落差另記 D-016 | 阻擋成因已消失（T-0027 部署完成）；規格文字修正列 P1 |
| E-B-02 | 不適用（環境阻擋） | 一次新的部署後既有資料仍存在（NFR-006 staging 側） | 1. 觸發一次真實 staging 重新部署 2. 比對部署前後的 10 筆持久化集 | 預期：10/10 一致／實際：無 staging 可部署，測試 skip（本機以 TC-093 容器重啟近似，已通過） | TC-080 | AT（r2） | **部分解除（T-0030）** —— staging 已部署，AT r3 於兩次真實部署（`deploy-staging` run `35432964533`／`35432984799`）期間取得**間接證據**（部署期間 `/health` 228 樣本零中斷、Neon Postgres 與 Cloud Run 運算分離、回滾切 revision 不動資料層）；**「同一批資料跨越一次部署前後」的直接比對尚未取得**（AT r3 基準資料被同輪 `test:integration:qa` 的 TC-014 清空） | **TC-080 判「部分通過」**；建議下一次真實部署事件安排單獨時段補做直接比對（見測試總結 r2 殘留風險 R-2） |
| E-B-03 | 不適用（環境阻擋） | Cloud Run revision 切流量回滾演練 | 1. 於已部署的 staging 建立至少兩個 revision 2. 執行 `scripts/rollback-staging.sh` 3. 驗證流量已切回 | 預期：回滾成功且 `06_部署架構與CICD.md` §5.4 演練紀錄已回填日期與結果（Gate 2 前置條件）／實際：staging 未部署，演練未執行，測試 skip | TC-084 | AT（r2） | **已解除（T-0030）** —— T-0027 已於 2026-09-19 16:33 對真實 revision 執行「切走再切回」演練（`todo-app-00002-sn7` ↔ `todo-app-00003-lt2`，9.689s／10.355s），`06` §5.4 演練紀錄已回填；**TC-084 判通過** | 無（已完成） |
| E-B-04 | 不適用（環境阻擋） | NFR-003 健康檢查 24 小時採樣成功率 | 1. staging 部署完成且 `monitor-health.yml` 已啟用 2. 選定連續 24 小時採樣窗 3. 下載 artifact 統計成功次數／實際取樣次數 | 預期：成功率 ≥ 99%（Leader 裁決 O-009：Gate 2 門檻為 24 小時，非 7 天）／實際：**未執行 —— staging 未部署，無任何 artifact 可讀** | TC-089 | AT（r2） | **阻擋解除、改為「採樣中」（T-0030）** —— `monitor-health.yml` 已在真實 staging 上執行並有成功 run；但 24 小時窗未滿（起算 2026-09-19 16:46，至 17:38 僅 **0.87 小時**），且 `cron`（`*/5 * * * *`）截至查詢時**尚未產生任何排程 run**（GitHub API `total_count = 1`，唯一 run 為 `workflow_dispatch`） | **TC-089 未判定（採樣中）**；判讀時間 2026-09-20T16:46 之後；門檻：滿 24 小時且成功率 ≥ 99%。**若 cron 持續不觸發，須另立維運卡調查**（見測試總結 r2 裁決事項 B） |
| E-B-05 | 不適用（環境阻擋） | 單次部署造成的不可用時間 < 60 秒 | 1. 啟動 `tests/perf/tc090-deploy-downtime.perf.mjs`（每 5 秒輪詢 `/health`） 2. 觸發一次部署 3. 計算最長連續失敗時長 | 預期：< 60 秒／實際：腳本已備妥但無部署事件可量測，未執行 | TC-090 | AT（r2） | **已解除（T-0030）** —— AT r3 於兩次真實部署事件期間以 5 秒間隔輪詢 `/health`，**228 樣本全部 200、最長連續失敗 0 秒**（門檻 < 60 秒）；**TC-090 判通過** | 無（已完成） |
| E-B-06 | 不適用（環境阻擋） | staging 以瀏覽器完成 US-001~US-007 全部操作 | 1. 以瀏覽器開啟 staging 網址並通過 Basic Auth 2. 依序執行新增、檢視、編輯、刪除、切換、篩選、檢視建立時間 | 預期：七項操作全部成功／實際：staging 未部署，UAT r2 以**本機替代**完成同等操作（10/10 通過） | TC-078 | UAT（r2） | **已解除（T-0030）** —— UAT r4（T-0029 r2，`docs/reports/20260919-1656-UAT-E001-r4.md`）已於真實 staging 以瀏覽器完成 US-001 ~ US-007 全部操作；**TC-078 判通過** | 無（已完成） |
| E-B-07 | 不適用（環境阻擋） | 依 README 可本機跑起**並可重現一次 staging 部署** | 1. 依 README 於本機跑起（已完成，見 TC-095） 2. 依部署文件的一次性設定清單重現一次 staging 部署 | 預期：兩段皆完成且卡關點為 0／實際：**本機段通過**（AT r2 方式二 14 秒、0 卡關點）；**staging 重現段阻擋**，雲端憑證須由使用者提供 | TC-081 | UAT（r2） | **已解除（T-0030）** —— 本機段早已通過（AT r2 方式二 14 秒、0 卡關點）；staging 重現段由 **T-0027 依部署文件實際完成一次 staging 部署**（`docs/reports/20260919-1551-部署紀錄-E001.md`，過程中發現的 README 修正已回填）佐證，Leader 2026-09-19T17:30:10 裁決③認可；**TC-081 判通過** | 無（已完成） |
| E-B-08 | 不適用（環境阻擋） | staging 環境本身可用（HTTPS、`/health` 免認證、完整新增→編輯→刪除） | 1. 以瀏覽器開啟 staging 網址通過 Basic Auth 2. 確認網址列為 HTTPS 3. 直接開 `/health` 4. 完成一次新增→編輯→刪除 | 預期：四項全成立／實際：staging 未部署；本機等效項（`/health` 200、`/` 401、CRUD）已由 TC-082／TC-083 與 UAT r2 通過，**HTTPS 一項無法以本機替代** | TC-105 | UAT（r2） | **已解除（T-0030）** —— UAT r4 US-010 於 staging 實測：`/health` 不帶憑證 200、`/` 不帶憑證 401、帶憑證 200、錯誤密碼 401，網址列全程 HTTPS，並完成新增→編輯→刪除；qa-lead 於 T-0030 以不帶憑證獨立複驗三條（200／401／401）相符；**TC-105 判通過** | 無（已完成） |
| E-B-09 | 不適用（工具／環境限制） | Firefox 兩個 e2e project 無法啟動 | 1. `npm run test:e2e -- --project=firefox-1280x800 --project=firefox-390x844` | 預期：兩組通過（NFR-004 相容性 6 組全過）／實際：本沙盒 `browserType.launch: spawn UNKNOWN`，兩組未執行；Chromium 2 組與 Edge 2 組共 4 組通過 | TC-091 | AT（r1、r2） | **阻擋（工具／環境限制）** | Leader 2026-09-19T12:32:40 裁決「於非沙盒環境補跑，列 Gate 2 後待辦」 |

---

## 3. 觀察（不構成缺陷，供日後維護與 P1 取用）

| # | 內容 | 來源 |
|---|---|---|
| OBS-1 | `T-0024` 的 acceptance 字面寫「`POST {"isCompleted":null}` 回 400」，實際為 **201**：`CreateTodoRequestSchema` 只宣告 `title` 且 `additionalProperties: false`，ajv 的 `removeAdditional` 在型別檢查之前就把該欄移除。**非型別脅迫復發**，與 CR r1 已接受的「夾帶 `id`／`createdAt` 被忽略」是同一行為。日後寫型別嚴格性的驗收條件一律取 `PATCH` 案例 | CR r2 N-3 |
| OBS-2 | `tsconfig.test.json` 的 `include` 為 `tests/**/*.ts`；日後任何新增在 `tests/` 下、需要不同 `lib`／型別環境的測試都會重演 lint 紅燈。做法：新增專屬 tsconfig、於 `tsconfig.test.json` 的 `exclude` 排除、並把新 tsconfig 串進 `lint`，**三處同一個 commit 一起改** | CR r2 N-2（已由 `e7abe4f` 關閉） |
| OBS-3 | `tests/unit/**` 與 `tests/integration/*.test.ts`（dev 團隊撰寫）**未在測試碼中標註 TC-ID**，因此 `grep -roE "TC-[0-9]{3}" tests/` 只命中 79 個 TC-ID（全部來自 `tests/integration/qa`、`tests/e2e`、`tests/perf`）。對應到 TC-001~003、012、019、020、030、038、039、048、059 十一條 unit 層 P0 案例時，只能靠「unit 套件 76/76 全綠」整體佐證，無法逐條追溯。**建議 P1 於 unit／dev 整合測試的 `test()` 標題補 TC-ID** | qa-lead T-0023 實查 |
| OBS-4 | AT r2 報告「P0 automated TC-ID 覆蓋 ⋯ 共 84 條」的**計數有誤**：其所列區間展開後為 80 條，扣掉明文不寫腳本的 TC-089 後實際為 **79 條**，與 `grep` 去重結果逐一相符。**覆蓋內容正確，僅數字寫錯**，不影響結論 | qa-lead T-0023 實查 |
| OBS-5 | CR r2 稱 `src/plugins/basic-auth.ts`「全檔已無 `new URL(`」，實查仍有 **2 處命中**，但兩處皆在**註解**（第 10、47 行，用於說明為何不得使用）。**可執行碼確實已無 `new URL(`**，結論成立，僅措辭不精確 | qa-lead T-0023 實查 |
| OBS-6 | perf 量測前若不清空資料庫，`GET /api/v1/todos` 的 P95 會隨既有筆數上升（AT r2 為 47.7ms vs 先前 2.8ms，兩者皆遠低於 500ms 門檻）。**建議 perf 走專用空資料庫**以取得可比較的 baseline | AT r2 |
| **OBS-7** | **多個 QA agent 並行對同一份 staging 做寫入型測試會互相清空資料，造成假性失敗與難以歸因**（**觀察，非缺陷**：應用程式行為正確，反而正確重現了 AC-003-5／AC-004-4 的 404 錯誤處理）。實證兩則：①UAT r4 建立一筆後 0.1 ~ 3 秒內 `GET` 即回 404（`POST → 201`，第 1 ~ 3 次 `GET` 200，第 4 次起 404），清單筆數在重新整理間持續增減；②AT r3 的 `test:integration:qa` TC-014 以 `list.json().map(todo => deleteTodo(todo.id))` **清空 staging 全部既有資料**（BR-028 驗證的必要手段），連帶把自己的 TC-080 基準資料與 qa-uat 的資料一起刪掉；AT r3 另自承曾同時跑兩份 e2e 造成 `Expected: 3 Received: 4` 的競態假性失敗。**處置**：Leader 2026-09-19T17:30:10 已裁決「之後 staging 測試卡錯開時段」。**建議再加一層資料隔離**：清空型步驟改為只刪自己建立的資料（各角色固定標題前綴），或為 staging 測試加獨占鎖，使「錯開時段」不必完全依賴人工排程 | UAT r4、AT r3（qa-lead 於 T-0030 彙整編號） |
