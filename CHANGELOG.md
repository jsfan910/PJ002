# CHANGELOG

格式：每個合併的任務卡一行 `T-#### 摘要`，最新在上。由 dev-tl 於合併時寫。

## [Unreleased]

- T-0039 維運：Secret Manager 釘具體版本、verify 失敗自動重試一次（落實 06 §6.7 建議一、建議二與 CLAUDE.md 安全鐵則。`deploy-staging.yml` 新增 `resolve secret versions` 步驟，對 `database-url`／`basic-auth-user`／`basic-auth-pass` 各跑 `gcloud secrets versions list --filter="state=ENABLED" --sort-by="~createTime" --limit=1` 取目前啟用中的最新版本號，`--set-secrets` 改組成 `NAME=secret:N`（不再 `:latest`），三個版本號寫入 `GITHUB_STEP_SUMMARY` 供稽核與回滾比對，可用選填 repository variables `SECRET_VERSION_DATABASE_URL`／`SECRET_VERSION_BASIC_AUTH_USER`／`SECRET_VERSION_BASIC_AUTH_PASS`（只填版本號、不填值）覆寫，任一 secret 無 ENABLED 版本即 `::error::` 中止；原 `gcloud run deploy`＋三個 verify 步驟合併為單一步驟 `gcloud run deploy ＋ verify（含一次自動重試）`，以 `deploy_revision`／`get_service_url`／`verify_health`／`verify_auth` 四個 bash 函式封裝，帶憑證呼叫 `/api/v1/todos` 第 1 次失敗時自動以相同參數與相同釘定版本重新部署、重新輪詢 `/health`、再驗一次，仍失敗才 `::error::` 判紅，重試上限固定 1 次（無迴圈），`retried`／`retry_result` 兩個 output 讓 STEP_SUMMARY 區分「重試後通過」與「重試後仍失敗」；`scripts/deploy-staging.sh` 同步加入 `resolve_version()`，但刻意不內建自動重試（本機手動執行本身即人工介入，理由寫入檔頭與 README）；`infra/cloudrun-service.yaml` 三處 `secretKeyRef.key` 改 `"<SECRET_VERSION>"` 佔位符並附查版本號指令；README「部署與 secrets」新增「釘定 Secret Manager 版本與 verify 自動重試」一節與三個選填 variables；06 僅新增 §6.9 實作紀錄，§1～§6.8 文字不動。dev-tl 初審獨立重跑全部驗收指令：`git diff --stat main...` 僅含 outputs＋任務卡＋交接檔、`grep -n "secret:"` 於 workflow 零命中 `:latest`（僅 docker image tag 保留 `latest`，屬既有無關用法）、`bash -n scripts/deploy-staging.sh` 通過、`node -e` 讀檔 255 lines、`npx yaml-lint` 成功、自行下載 actionlint v1.7.12 對三個 workflow 跑出 EXIT=0 零 finding、`${{ secrets.* }}` 僅出現於 `env:` 區塊而 `run:` 內零命中（T-0025 CR S-3 規則未破）、倉庫無任何 secret 值且無 `gcloud secrets versions access`、`src/**` 零改動故 SD §6.2 的 `/health` 唯一豁免比對邏輯未受影響。**已知未同步**：06 §7「三處必須同步的參數表」的「機密」列與變更紀錄表仍寫 `:latest`，依本卡 outputs 限制留給 T-0040（plan-sd）調整規格文字。<br>**⚠ 本列所述內容雖已合併進 main（`05c9a1e`），但合併後的真實 `deploy-staging` run `35506278351` 紅燈，任務卡已退回 rework（round 2），機制尚未在 CI 上生效**：新增的 `resolve secret versions` 步驟以 exit code 1 失敗、其後五步全部 skipped、STEP_SUMMARY 無任何版本號。根因實查：`gcloud secrets versions list` 需要 `secretmanager.versions.list` 權限，該權限只在 `roles/secretmanager.viewer`；而部署服務帳號 `github-deployer@pj002-509106.iam.gserviceaccount.com` 依 README 只被授了 `roles/run.admin`／`roles/artifactregistry.writer`／`roles/iam.serviceAccountUser`／`roles/secretmanager.secretAccessor` 四個角色，`secretAccessor` 的 `includedPermissions` 僅 `resourcemanager.projects.get;resourcemanager.projects.list;secretmanager.versions.access`。r1 的本機實測之所以通過，是因為本機 gcloud 用的是專案擁有者帳號、與 CI 的 WIF 服務帳號不是同一個身分。**在使用者補授 `roles/secretmanager.viewer`（或改以 repository variables `SECRET_VERSION_*` 直接指定版本號）之前，staging 無法部署新版本**（既有 revision 仍正常服務）。r2 具體修法見 `tasks/T-0039-secret-pin-verify-retry.md` 審核紀錄與 `worklog/handoff/20260920-1848-T0039-r1-dev-tl.md`）
- T-0039 r2 維運：`resolve secret versions` 失敗改留可讀 `::error::`、README／06 補 `roles/secretmanager.viewer`（修 r1 真實 run `35506278351` 暴露的兩個問題。①**錯誤被吞**：`.github/workflows/deploy-staging.yml` 與 `scripts/deploy-staging.sh` 的 `resolve_version()` 原本寫 `out="$(gcloud secrets versions list …)"`，gcloud 非 0 時外層 `set -euo pipefail` 直接中止 job，使用者只看得到 `Process completed with exit code 1.`、看不到原因；改為 `if ! out="$(… 2>&1)"; then` 顯式捕捉 stdout+stderr 後判斷，失敗時輸出含 gcloud 原始訊息與修復指引（需 `roles/secretmanager.viewer`，或改填 `SECRET_VERSION_*` repository variable 略過查詢）的 `::error::` annotation 再 `return 1`，三個呼叫端 `V_DB`／`V_USER`／`V_PASS` 改為 `… || exit 1` 承接；`out` 為空（secret 存在但無 ENABLED 版本）另有獨立 `::error::`；本機腳本同步套用同一修法，訊息走一般 stderr。②**權限缺口未寫進文件**：README「部署與 secrets」建立服務帳號的角色迴圈（Bash 與 PowerShell 各一處）補上 `roles/secretmanager.viewer` 並註明 `secretmanager.secretAccessor` 只含 `versions.access`、不含 `versions.list`，「釘定 Secret Manager 版本與 verify 自動重試」一節新增「前置權限（必讀）」子段寫明補跑指令、失敗行為與「IAM 授權屬使用者雲端專案安全設定，不由 CI 或 agent 代執行」；PowerShell 迴圈順手改用 `gcloud.cmd`；06 新增 §6.9.1 追記 run `35506278351` 紅燈事實、兩組 IAM 實查輸出與「本機 gcloud 身分 ≠ CI 的 WIF 服務帳號身分，本機能列版本不代表 CI 能列」的教訓（不覆蓋 §6.9 的「離線驗證通過」，避免第二份真相）；`infra/cloudrun-service.yaml` 本輪零改動（靜態範本、不含 gcloud 呼叫，與 r1 失敗無關）。dev-tl 初審 r2 獨立重跑：`git diff --stat main...` 僅含 outputs＋任務卡＋交接檔、`grep -n "secret:"` 於 workflow 零命中（殘留 `latest` 三處皆為 docker image tag 與註解文字）、`bash -n` 通過、`node -e` 268 lines、`npx yaml-lint` 成功、以 mock `gcloud` 從 workflow 原檔 `sed` 抽出 `resolve_version()` 實跑三條路徑（PERMISSION_DENIED → 印出完整 `::error::` 且 EXIT=1／無 ENABLED 版本 → 另一條 `::error::` 且 EXIT=1／正常 → 回傳版本號 EXIT=0，override 分支不呼叫 gcloud 直接回 `42`），證實 r1 的「黑盒 exit 1」已消除；另以唯讀指令複驗 IAM 現況仍為四個角色、仍缺 `secretmanager.viewer`，未執行任何 IAM 變更、未執行 `gcloud secrets versions access`。合併推送（merge commit `3345022`，HEAD `bfe4693`）後觸發的真實 `deploy-staging` run **`35507519302` 仍紅燈**，失敗點同樣是第 8 步 `resolve secret versions`、第 9～13 步 skipped、STEP_SUMMARY 無版本號——**但失敗形態已改變**：`check-runs/106069733881/annotations` 除 `Process completed with exit code 1.` 外，多出一則 failure annotation `無法列出 secret database-url 的版本（gcloud 原始訊息：ERROR: (gcloud.secrets.versions.list) PERMISSION_DENIED: Permission 'secretmanager.versions.list' denied on resource …）`，r1 時**只有** exit code、沒有任何原因。**r2 要修的東西已在真實 CI 上驗證生效**；任務卡判 `blocked`（非 rework），`blocked_reason` 為「等待使用者 IAM 授權或填版本變數」，worktree 與分支保留不清理。**在使用者補授 `roles/secretmanager.viewer`，或設定 repository variables `SECRET_VERSION_DATABASE_URL=1`／`SECRET_VERSION_BASIC_AUTH_USER=3`／`SECRET_VERSION_BASIC_AUTH_PASS=1`（只填版本號、不填值）之前，staging 無法部署新版本**；既有 revision 仍正常服務。詳見 `worklog/handoff/20260920-1915-T0039-r2-dev-tl.md`）
- T-0031 監測補強：NFR-003 採樣來源改 Cloud Monitoring uptime check、GitHub cron 診斷、TC-080／TC-090 直接量測（新增 `infra/uptime-check.sh`——`create`／`list`／`describe` 三個子命令，內含 Git Bash 下 `--path=/health` 被 MSYS 路徑轉換吃掉、須加 `MSYS_NO_PATHCONV=1` 的踩坑紀錄；新增 `scripts/measure-deploy-downtime.sh`——每秒 `curl /health` 落 CSV 並以 awk 算最長連續失敗秒數；`06_部署架構與CICD.md` 僅動第 6 章：§6.1 監控由兩層改三層、正式量測來源改為 uptime check `todo-app-health`（check id `todo-app-health-aMAlP5dfKv0`，`/health`、443/SSL、週期 `300s`、逾時 `10s`、4 個檢查地區、期望 200、不帶憑證），`monitor-health.yml` 降為備援且明註「僅供交叉比對、不作 Gate 判準分母」，§6.2 **NFR-003 採樣起算改以 uptime check 建立時間 `2026-09-19T17:55:07+08:00` 為準**（判讀時間點 2026-09-20 同時刻後），新增 §6.8 完整記錄 cron 診斷、建立紀錄、判讀指令（`timeSeries.list` 取 `uptime_check/check_passed`）與 TC-080／TC-090 量測；**`.github/workflows/monitor-health.yml` 未動**——分支 `main`、workflow `state=active`、cron 語法、repo 公開且活動頻繁四項逐一實查排除，零自動觸發判為 GitHub 對新註冊 workflow 的排程啟動延遲，無可歸因的設定缺陷故不做猜測性修改；`docs/reports/20260919-1804-監測補強-E001.md`。dev-tl 初審重跑全部驗收指令：uptime check 參數六項全符、判讀指令 24h 窗 70 點全 `true`＝100%、cron `total_count=1` 且唯一 run 為 `workflow_dispatch`、TC-080 輪詢 134/134 回 200 最長不可用 0 秒並以 `gcloud run revisions list` 佐證 `todo-app-00008-kcs`（env=7，多出量測用 `DEPLOY_PROBE_T0031`）確為真實 revision 切換、憑證掃描零真實命中。合併收尾由 dev-tl 訂正 06 §6.8.4 一處貼錯的部署輸出（原誤寫 `revision [todo-app-00003-lt2]`——該 revision 實為 08:33:27Z 的 T-0027 回滾演練產物；`lastTransitionTime` 末三碼誤取自並行的 `00009-cg7`），結論 0 秒不可用不受影響。**NFR-003 的 24 小時正式判讀待 2026-09-20T17:55:07+08:00 後由 qa-lead 重跑 §6.8.3 指令統計**；TC-080 資料層 id 集合比對需 Basic Auth 憑證，留下一輪有憑證的測試卡補做）
- T-0027 staging 首次部署、網址回填、回滾演練、監測啟動（`deploy-staging.yml` 在 main 上跑通：run `35431202802` re-run attempt 2 全綠，含 `verify（帶憑證 /api/v1/todos）`；staging 網址 `https://todo-app-dpevsdhdva-de.a.run.app`，`/health` 無憑證 200、`/` 無憑證 401 經 dev-tl 實測複驗；三處回填完成——`06_部署架構與CICD.md` 第 1 章環境清單、`04_API規格.yaml` 的 `servers` staging（url 樣板一併由 `https://todo-app-staging-{hash}-{region}.run.app` 更正為 `https://todo-app-{hash}-{region}.a.run.app`，`hash: dpevsdhdva`／`region: de`，redocly lint 0 errors）、GitHub variable `STAGING_BASE_URL` 由使用者設定；**回滾演練實際執行並通過**——`scripts/rollback-staging.sh` 切到 `todo-app-00002-sn7` 9.689s、切回 `todo-app-00003-lt2` 10.355s，兩次 `/health` 皆 200、連續 3 次無 5xx，遠低於 §5.1 的 2 分鐘目標，紀錄於 06 §5.4；06 新增 §6.5／§6.6／§6.7 記錄三次真實 run、WIF attribute-condition 字面佔位符根因、verify 401 事件收尾與兩點維運建議（secret 參照釘版本、verify 失敗自動重試，**本卡未動任何 workflow**，留待維運改善小卡）；README 新增「GCP 指令在哪裡執行」一節（Cloud Shell／本機 SDK、Windows 下 `gcloud.cmd`、佔位符務必替換、`<(echo …)` 改寫暫存檔、secret 用 `[IO.File]::WriteAllText` 避免多寫換行、跨殼層管線量測不可靠應先 `--out-file` 落地再量）；`docs/reports/20260919-1551-部署紀錄-E001.md`。合併收尾由 dev-tl 修正 06 四處文字：§5.4 首行「尚未實際演練」與同節演練紀錄自相矛盾、§6.6 把已證偽的「`basic-auth-user` 含換行」寫成既定成因未加更正標記、第 1 章未寫 Leader 紀錄的真實成因（revision `00001-tfq` 執行實例解析到的 secret 值有誤）、§6.7 的 revision 歸屬與 `gcloud run revisions list` 的 `DEPLOYED BY` 實查相反。**acceptance 第 5 條 `monitor-health.yml` 至少 1 次成功採樣未達成**（`total_count: 0`，GitHub 排程首次觸發延遲且 agent 無法 `workflow_dispatch`），移交使用者／Leader 追蹤，24 小時採樣由 T-0028 判讀）
- T-0021 自動化測試：整合／E2E／效能腳本與 AT 報告（`tests/integration/qa/**` 10 個 spec ＋ `lib/http-client.ts`，一律走真實 HTTP 打 `$BASE_URL`，換成 staging 網址即可原碼重跑；`tests/e2e/**` 9 個 spec ＋ `playwright.config.ts`，6 個 project＝Chromium／Edge／Firefox × 1280×800／390×844，`httpCredentials` 讀環境變數；`tests/perf/**` 自寫負載產生器與 NFR-001／NFR-007／TC-090 三支腳本；`.github/workflows/qa-tests.yml` 新檔三個 job，未動 `ci.yml`；`docs/reports/20260919-1221-AT-E001.md`。合併收尾由 dev-tl 補：`@playwright/test` 釘選 `1.63.0` 列入 devDependencies（Leader 裁決 2026-09-19T12:32:40+08:00），新增 `test:integration:qa`／`test:e2e`／`test:perf` 三個 script 並把 `test:integration` 的 glob 收斂為 `tests/integration/*.test.ts`——`app.inject` 那組不需服務在跑、QA 那組必須先起服務，兩者前提不同不可混在同一個 script；新增 `tsconfig.e2e.json` 讓 `tests/e2e` 拿得到 DOM 型別而 Node 側測試拿不到，`npm run lint` 改跑三個 tsc project；`.gitignore` 補 Playwright 產物）
- T-0026 CR 修正（前端）：S-5 `load()` 請求序列化防競態、S-6 編輯中草稿不被重繪吃掉（`public/assets/todo-store.js` 加模組實例私有的單調遞增 `requestSeq`，`load()` 開頭取號、`await` 回來後成功與失敗兩分支皆比對，非最新一次回 `{ok, stale:true}` 且不寫入 state，快速切換篩選不再被先發後到的舊回應覆蓋（BR-011）；`public/assets/todo-view.js` 加模組層 `editingDraft`，`renderEditingItem` 以 `editingDraft ?? todo.title` 初始化並在 `input` 事件同步，進入編輯／儲存／取消三處重置，編輯期間他筆更新觸發整段重繪不再吃掉未儲存文字；`tests/unit/todo-store.test.mjs` 補先發後到測試共 25 條；`index.html`／`api-client.js`／`styles.css` 未動、11 個 `data-testid` 不變、`innerHTML` 仍為 0）
- T-0025 CR 修正（DevOps）：S-3／S-4／S-7／S-9／S-10／S-11（`deploy-staging.yml` 的 verify 步驟改以 `env:` 宣告後 `-u "$BASIC_USER:$BASIC_PASS"` 引用，`run:` 內不再直接展開 secrets；`monitor-health.yml` 拿掉 `curl -f`，4xx／5xx 的真實狀態碼進 CSV、僅連不上才 `000`（NFR-003 判讀規則）；新增 `tsconfig.test.json` 並讓 `npm run lint` 一併對 `tests/**/*.ts` 跑 `tsc --noEmit`（因此 `lint` 前需先 `npm run build`，`ci.yml` 的 `lint` job 同步補上）；`docker-compose.yml` 的 db 埠改為 `"${POSTGRES_HOST_PORT:-5432}:5432"` 並於 `.env.example`／README 加說明；`package.json` 的 `dev` 拆為 `dev:build`（`tsc -w`）＋ `dev:run`（`node --watch dist/server.js`）、`engines.node` 收緊為 `>=22.18`；`06_部署架構與CICD.md` 新增第 7 節「三處必須同步的參數表」對照 IaC 檔／workflow／腳本並標註兩項既有飄移）
- T-0024 CR 修正（後端）：阻擋級 B-1／B-2 與建議級 S-1／S-2（`src/plugins/basic-auth.ts` 移除 `new URL().pathname`，豁免比對改為只剝除 `?`／`#` 的字串完全相等且不做任何正規化，`/foo/../health`／`/.%2e/health`／`/%2e%2e/health`／`/assets/%2e%2e/%2e%2e/health` 未帶憑證一律 401（SD §6.2 硬性要求）；豁免同時限定 `GET`／`HEAD`，`POST /health` 回 401；`src/server.ts` 啟動邏輯抽成可匯入的 `start()`，在 `app.listen` 之前呼叫 `runMigrations`（05 §3 第 4 條、NFR-008），失敗記錄原因後以非 0 結束且不監聽，pipeline 的 migrate 階段保留為雙重保險；`src/app.ts` 建構 Fastify 時 `ajv.customOptions.coerceTypes: false`，`{"title":123}`／`{"isCompleted":null}` 一律 400；`Dockerfile` runtime 階段補 `COPY migrations ./migrations`；新增 `tests/integration/startup-migrate.test.ts` 回歸測試，點節段測試改走真實 TCP 以避開 `light-my-request` 的正規化）
- T-0018 staging 部署（Cloud Run）、健康監測、回滾程序（`.github/workflows/deploy-staging.yml` 以 `workflow_run` 監聽 CI 於 `main` 全綠後觸發，階段順序固定 auth（WIF，無長期金鑰）→ migrate → build & push 至 Artifact Registry（SHA＋latest 雙標籤，部署一律用 SHA）→ `gcloud run deploy` → verify（輪詢 `/health` 每 10 秒上限 5 分鐘，再帶憑證驗 `GET /api/v1/todos` 200）；`.github/workflows/monitor-health.yml` `cron */5` 連續取樣 3 次、間隔 20 秒、不帶憑證、不附查詢字串，結果上傳 artifact 供 NFR-003 統計，連續 3 次全失敗才標紅；`infra/cloudrun-service.yaml` 文件化 IaC（minScale 0／maxScale 2／並行 80／逾時 60s，機密一律 Secret Manager 參照，probe 指向 `/health`）；`scripts/verify-health.ts`（`npm run verify:health` 的實作）與 `scripts/deploy-staging.sh`／`rollback-staging.sh` 本機等效；README 補「部署與 secrets」章節與方式二的 `.env` 載入步驟；06 僅回填第 1 章網址、5.4 演練紀錄、新增 6.4 實作紀錄三處。**實際部署、回滾演練與 24 小時採樣待使用者提供 GCP 專案與 GitHub repo**）
- T-0017 前端狀態與呈現（單向資料流）（`public/assets/todo-store.js` 為唯一可變更狀態處，`load`／`setFilter`／`add`／`updateTitle`／`setCompleted`／`remove`／`retry` 七個 action，任何異動成功後以目前 filter 整份重取清單（BR-011）、filter 預設 `all` 且不寫網址不持久化（BR-010）、`setCompleted` 直送目標值不做取反（O-002）、送出前先 `trim` 驗長度 1~200；`public/assets/todo-view.js` 兼任 controller，一律 `createElement`＋`textContent`（BR-013，XSS 原樣顯示）、`Intl.DateTimeFormat` 轉本地時區 `yyyy-mm-dd HH:mm`（BR-004）、`DocumentFragment` 單次插入（NFR-007）、空狀態／載入中／錯誤訊息＋重試按鈕、刪除前 `window.confirm` 二次確認（BR-008）；`public/index.html` 僅新增一行 module script 進入點，既有 11 個 `data-testid` 未動；`tests/unit/todo-store.test.mjs` 24 條）
- T-0016 待辦 API 與業務規則（`src/routes/todos.ts` 註冊 `/api/v1/todos` 五端點、只做 schema 驗證與序列化，連線池於註冊時建立一次並隨 app `onClose` 關閉；`src/services/todo-service.ts` 為業務規則唯一落點，`title` 先 `trim()` 再驗長度 1~200（201 字元回 400 而非 500）、PATCH 至少一個可更新欄位、查無資料一律 `E_NOT_FOUND`；`src/schemas/todo-schema.ts` 對齊 04 的 `Todo`／`CreateTodoRequest`／`UpdateTodoRequest`，`additionalProperties: false` 搭配 `removeAdditional` 落實 BR-012「忽略」，`createdAt` 以 `format: date-time` 單點序列化、`updatedAt` 不進契約；DELETE 回 204 無本文、PATCH 為設定目標狀態（冪等非 toggle）、狀態篩選與新到舊排序在後端、UUID 格式錯 400 而格式對查無 404）
- T-0015 全站 Basic Auth、`/health` 唯一豁免、靜態託管（`src/plugins/basic-auth.ts` 全域 `onRequest` hook 掛根實例、涵蓋靜態檔與全部後續路由；豁免比對 pathname 且為完全相等字串，`/health?cb=1` 放行而 `//health`／`/health/`／`/healthz-secret` 一律 401；`crypto.timingSafeEqual` 常數時間比較、帳密只來自環境變數；401 附 `WWW-Authenticate: Basic realm="staging"` 並經全域 error-handler 序列化為 `Error` schema；`src/plugins/static.ts` 以 `@fastify/static` 供應 `public/`、`prefix: "/"`、無目錄列表、`Cache-Control: no-store, private`）
- T-0014 前端版面與 API 用戶端（`public/index.html`／`styles.css` 單頁骨架，flexbox ＋ 單一 768px 斷點、44×44 點擊區、11 個穩定 `data-testid`；`public/assets/api-client.js` 對應五個 `/api/v1/todos*` 端點，相對路徑不寫死網域、不碰 DOM、統一錯誤物件轉具名例外 `ApiError`／`NetworkError`；無框架無打包器；Dockerfile 兩階段各補一行 `COPY public ./public`）
- T-0013 資料層：連線池、migration runner、todo repository（`migrations/001_create_todos.sql` 逐字採用 05 §3 DDL；`src/db/pool.ts` `max: 5`；`src/db/migrate.ts` 單一交易、`schema_migrations` 記錄版本、forward-only；`src/repositories/todo-repository.ts` 全參數化查詢、`snake_case↔camelCase` 唯一轉換點、清單含 `id` tie-break）
- T-0012 統一錯誤處理器與錯誤契約（`src/plugins/error-handler.ts` 全域 `setErrorHandler`／`setNotFoundHandler`；`src/schemas/error-schema.ts` 的 `AppError`／`buildErrorBody` 對齊 04 的 `Error` schema；5xx 固定字面值 `Internal Server Error` 且不含 `details`；`requestId` 與 pino `reqId` 同值）
- T-0011 專案骨架 ＋ CI ＋ 本機可跑 `/health` ＋ README 初版（Fastify 5／TypeScript ~5.6／ESM；`src/app.ts` 五個註冊錨點；`src/config.ts` 一次驗證 P0 全部環境變數；Cloud Run 形狀的 Dockerfile 與本機 docker-compose；`ci.yml` lint／unit／build／integration 四階段不推送）
