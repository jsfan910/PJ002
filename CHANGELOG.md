# CHANGELOG

格式：每個合併的任務卡一行 `T-#### 摘要`，最新在上。由 dev-tl 於合併時寫。

## [Unreleased]

- T-0021 自動化測試：整合／E2E／效能腳本與 AT 報告（`tests/integration/qa/**` 10 個 spec ＋ `lib/http-client.ts`，一律走真實 HTTP 打 `$BASE_URL`，換成 staging 網址即可原碼重跑；`tests/e2e/**` 9 個 spec ＋ `playwright.config.ts`，6 個 project＝Chromium／Edge／Firefox × 1280×800／390×844，`httpCredentials` 讀環境變數；`tests/perf/**` 自寫負載產生器與 NFR-001／NFR-007／TC-090 三支腳本；`.github/workflows/qa-tests.yml` 新檔三個 job，未動 `ci.yml`；`docs/reports/20260919-1221-AT-E001.md`。合併收尾由 dev-tl 補：`@playwright/test` 釘選 `1.63.0` 列入 devDependencies（Leader 裁決 2026-09-19T12:32:40+08:00），新增 `test:integration:qa`／`test:e2e`／`test:perf` 三個 script 並把 `test:integration` 的 glob 收斂為 `tests/integration/*.test.ts`——`app.inject` 那組不需服務在跑、QA 那組必須先起服務，兩者前提不同不可混在同一個 script；新增 `tsconfig.e2e.json` 讓 `tests/e2e` 拿得到 DOM 型別而 Node 側測試拿不到，`npm run lint` 改跑三個 tsc project；`.gitignore` 補 Playwright 產物）
- T-0026 CR 修正（前端）：S-5 `load()` 請求序列化防競態、S-6 編輯中草稿不被重繪吃掉（`public/assets/todo-store.js` 加模組實例私有的單調遞增 `requestSeq`，`load()` 開頭取號、`await` 回來後成功與失敗兩分支皆比對，非最新一次回 `{ok, stale:true}` 且不寫入 state，快速切換篩選不再被先發後到的舊回應覆蓋（BR-011）；`public/assets/todo-view.js` 加模組層 `editingDraft`，`renderEditingItem` 以 `editingDraft ?? todo.title` 初始化並在 `input` 事件同步，進入編輯／儲存／取消三處重置，編輯期間他筆更新觸發整段重繪不再吃掉未儲存文字；`tests/unit/todo-store.test.mjs` 補先發後到測試共 25 條；`index.html`／`api-client.js`／`styles.css` 未動、11 個 `data-testid` 不變、`innerHTML` 仍為 0）
- T-0025 CR 修正（DevOps）：S-3／S-4／S-7／S-9／S-10／S-11（`deploy-staging.yml` 的 verify 步驟改以 `env:` 宣告後 `-u "$BASIC_USER:$BASIC_PASS"` 引用，`run:` 內不再直接展開 secrets；`monitor-health.yml` 拿掉 `curl -f`，4xx／5xx 的真實狀態碼進 CSV、僅連不上才 `000`（NFR-003 判讀規則）；新增 `tsconfig.test.json` 並讓 `npm run lint` 一併對 `tests/**/*.ts` 跑 `tsc --noEmit`（因此 `lint` 前需先 `npm run build`，`ci.yml` 的 `lint` job 同步補上）；`docker-compose.yml` 的 db 埠改為 `"${POSTGRES_HOST_PORT:-5432}:5432"` 並於 `.env.example`／README 加說明；`package.json` 的 `dev` 拆為 `dev:build`（`tsc -w`）＋ `dev:run`（`node --watch dist/server.js`）、`engines.node` 收緊為 `>=22.18`；`06_部署架構與CICD.md` 新增第 7 節「三處必須同步的參數表」對照 IaC 檔／workflow／腳本並標註兩項既有飄移）
- T-0024 CR 修正（後端）：阻擋級 B-1／B-2 與建議級 S-1／S-2（`src/plugins/basic-auth.ts` 移除 `new URL().pathname`，豁免比對改為只剝除 `?`／`#` 的字串完全相等且不做任何正規化，`/foo/../health`／`/.%2e/health`／`/%2e%2e/health`／`/assets/%2e%2e/%2e%2e/health` 未帶憑證一律 401（SD §6.2 硬性要求）；豁免同時限定 `GET`／`HEAD`，`POST /health` 回 401；`src/server.ts` 啟動邏輯抽成可匯入的 `start()`，在 `app.listen` 之前呼叫 `runMigrations`（05 §3 第 4 條、NFR-008），失敗記錄原因後以非 0 結束且不監聽，pipeline 的 migrate 階段保留為雙重保險；`src/app.ts` 建構 Fastify 時 `ajv.customOptions.coerceTypes: false`，`{"title":123}`／`{"isCompleted":null}` 一律 400；`Dockerfile` runtime 階段補 `COPY migrations ./migrations`；新增 `tests/integration/startup-migrate.test.ts` 回歸測試，點節段測試改走真實 TCP 以避開 `light-my-request` 的正規化）
- T-0018 staging 部署（Cloud Run）、健康監測、回滾程序（`.github/workflows/deploy-staging.yml` 以 `workflow_run` 監聽 CI 於 `main` 全綠後觸發，階段順序固定 auth（WIF，無長期金鑰）→ migrate → build & push 至 Artifact Registry（SHA＋latest 雙標籤，部署一律用 SHA）→ `gcloud run deploy` → verify（輪詢 `/health` 每 10 秒上限 5 分鐘，再帶憑證驗 `GET /api/v1/todos` 200）；`.github/workflows/monitor-health.yml` `cron */5` 連續取樣 3 次、間隔 20 秒、不帶憑證、不附查詢字串，結果上傳 artifact 供 NFR-003 統計，連續 3 次全失敗才標紅；`infra/cloudrun-service.yaml` 文件化 IaC（minScale 0／maxScale 2／並行 80／逾時 60s，機密一律 Secret Manager 參照，probe 指向 `/health`）；`scripts/verify-health.ts`（`npm run verify:health` 的實作）與 `scripts/deploy-staging.sh`／`rollback-staging.sh` 本機等效；README 補「部署與 secrets」章節與方式二的 `.env` 載入步驟；06 僅回填第 1 章網址、5.4 演練紀錄、新增 6.4 實作紀錄三處。**實際部署、回滾演練與 24 小時採樣待使用者提供 GCP 專案與 GitHub repo**）
- T-0017 前端狀態與呈現（單向資料流）（`public/assets/todo-store.js` 為唯一可變更狀態處，`load`／`setFilter`／`add`／`updateTitle`／`setCompleted`／`remove`／`retry` 七個 action，任何異動成功後以目前 filter 整份重取清單（BR-011）、filter 預設 `all` 且不寫網址不持久化（BR-010）、`setCompleted` 直送目標值不做取反（O-002）、送出前先 `trim` 驗長度 1~200；`public/assets/todo-view.js` 兼任 controller，一律 `createElement`＋`textContent`（BR-013，XSS 原樣顯示）、`Intl.DateTimeFormat` 轉本地時區 `yyyy-mm-dd HH:mm`（BR-004）、`DocumentFragment` 單次插入（NFR-007）、空狀態／載入中／錯誤訊息＋重試按鈕、刪除前 `window.confirm` 二次確認（BR-008）；`public/index.html` 僅新增一行 module script 進入點，既有 11 個 `data-testid` 未動；`tests/unit/todo-store.test.mjs` 24 條）
- T-0016 待辦 API 與業務規則（`src/routes/todos.ts` 註冊 `/api/v1/todos` 五端點、只做 schema 驗證與序列化，連線池於註冊時建立一次並隨 app `onClose` 關閉；`src/services/todo-service.ts` 為業務規則唯一落點，`title` 先 `trim()` 再驗長度 1~200（201 字元回 400 而非 500）、PATCH 至少一個可更新欄位、查無資料一律 `E_NOT_FOUND`；`src/schemas/todo-schema.ts` 對齊 04 的 `Todo`／`CreateTodoRequest`／`UpdateTodoRequest`，`additionalProperties: false` 搭配 `removeAdditional` 落實 BR-012「忽略」，`createdAt` 以 `format: date-time` 單點序列化、`updatedAt` 不進契約；DELETE 回 204 無本文、PATCH 為設定目標狀態（冪等非 toggle）、狀態篩選與新到舊排序在後端、UUID 格式錯 400 而格式對查無 404）
- T-0015 全站 Basic Auth、`/health` 唯一豁免、靜態託管（`src/plugins/basic-auth.ts` 全域 `onRequest` hook 掛根實例、涵蓋靜態檔與全部後續路由；豁免比對 pathname 且為完全相等字串，`/health?cb=1` 放行而 `//health`／`/health/`／`/healthz-secret` 一律 401；`crypto.timingSafeEqual` 常數時間比較、帳密只來自環境變數；401 附 `WWW-Authenticate: Basic realm="staging"` 並經全域 error-handler 序列化為 `Error` schema；`src/plugins/static.ts` 以 `@fastify/static` 供應 `public/`、`prefix: "/"`、無目錄列表、`Cache-Control: no-store, private`）
- T-0014 前端版面與 API 用戶端（`public/index.html`／`styles.css` 單頁骨架，flexbox ＋ 單一 768px 斷點、44×44 點擊區、11 個穩定 `data-testid`；`public/assets/api-client.js` 對應五個 `/api/v1/todos*` 端點，相對路徑不寫死網域、不碰 DOM、統一錯誤物件轉具名例外 `ApiError`／`NetworkError`；無框架無打包器；Dockerfile 兩階段各補一行 `COPY public ./public`）
- T-0013 資料層：連線池、migration runner、todo repository（`migrations/001_create_todos.sql` 逐字採用 05 §3 DDL；`src/db/pool.ts` `max: 5`；`src/db/migrate.ts` 單一交易、`schema_migrations` 記錄版本、forward-only；`src/repositories/todo-repository.ts` 全參數化查詢、`snake_case↔camelCase` 唯一轉換點、清單含 `id` tie-break）
- T-0012 統一錯誤處理器與錯誤契約（`src/plugins/error-handler.ts` 全域 `setErrorHandler`／`setNotFoundHandler`；`src/schemas/error-schema.ts` 的 `AppError`／`buildErrorBody` 對齊 04 的 `Error` schema；5xx 固定字面值 `Internal Server Error` 且不含 `details`；`requestId` 與 pino `reqId` 同值）
- T-0011 專案骨架 ＋ CI ＋ 本機可跑 `/health` ＋ README 初版（Fastify 5／TypeScript ~5.6／ESM；`src/app.ts` 五個註冊錨點；`src/config.ts` 一次驗證 P0 全部環境變數；Cloud Run 形狀的 Dockerfile 與本機 docker-compose；`ci.yml` lint／unit／build／integration 四階段不推送）
