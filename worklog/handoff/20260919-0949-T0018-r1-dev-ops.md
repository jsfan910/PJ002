# T-0018 r1 — dev-ops — 工作紀錄與交接

<!-- 檔名：worklog/handoff/20260919-0949-T0018-r1-dev-ops.md -->

## A. 開工紀錄

- 開工時間：2026-09-19T09:49:00+08:00
- 角色／模型：dev-ops / Sonnet
- 任務卡：tasks/T-0018-deploy-monitor.md（round 1）
- 讀取的輸入（實際讀過的檔案與段落，非任務卡照抄）：
  - tasks/T-0018-deploy-monitor.md 全文 —— outputs 五項、acceptance 五條、分支與 worktree 規約
  - docs/specs/10_開發計畫_WBS.md §0（硬前提 1-8）、§1.8 WI-08 全文（含驗收方式草案 bash 區塊 10 條指令）、§2 平行批次（批次5：WI-07/WI-08 平行）、§5 共用檔案規約（README.md 由 WI-01 初版、WI-08 補部署章節）
  - docs/specs/06_部署架構與CICD.md 全文（v0.2，frozen）—— 第1章環境清單（staging 網址待回填）、第2章雲端資源（Cloud Run/Artifact Registry/WIF/GitHub Actions額度二選一）、第3章 pipeline（ci/deploy-staging/monitor-health 三個工作流；3.2.1 WIF 六步驟；3.2.2 服務帳號金鑰備選）、第4章環境變數清單（4.1應用執行期、4.2 CI/CD）、第5章回滾程序（5.1 revision切流量、5.2 git回復、5.3資料庫層、5.4演練要求「尚未演練」）、第6章監控與告警
  - docs/specs/adr/ADR-0005-雲端平台-CloudRun.md 全文 —— 決策理由、後果（計費帳戶前提、WIF設定成本、Artifact Registry清理政策保留5版）
  - docs/specs/03_系統設計書_SD.md 第3.3節 OPS-03/OPS-04 定義、第7章 NFR-001/003 對策與驗證方式、第9章 SD-04（冷啟殘留風險已降級）
  - tasks/E-001-todo-app.md Leader裁決紀錄全部 —— 特別 D-01（.env.example 佔位值）、T-0011-②（本機Docker daemon不可用時延後驗證到T-0018，現已確認Docker Desktop可用）、對dev-tl審核T-0011②的裁決（README方式二補.env載入步驟，交T-0018）、T-0013-③（不做啟動時自動migrate，migrate為部署pipeline獨立階段，由T-0018實作）、對T-0010裁決2（保溫維持每5分鐘）、裁決4（不建立infra/render.yaml，T-0018 outputs只有infra/cloudrun-service.yaml）
  - worklog/handoff/20260919-0912-T0015-r1-dev-tl.md 下一步建議第3點 —— 給T-0018：probe路徑必須是`/health`（不可有點節段變體），monitor-health.yml不必帶憑證
  - worklog/handoff/20260919-0938-T0016-r1-dev-tl.md 下一步建議第2點 —— 給T-0018：連線池於routes/todos.ts、max:5；deploy-staging.yml verify可沿用GET /health（不帶憑證）＋GET /api/v1/todos（帶憑證）兩條
  - README.md、.github/workflows/ci.yml、Dockerfile、package.json、.env.example、docker-compose.yml、src/routes/health.ts、src/config.ts、tsconfig.json（現況：package.json已有`verify:health`腳本指向`scripts/verify-health.ts`，該檔尚未建立，需本卡補上；本機Docker Desktop 4.72.0已可用）
- 上一輪交接摘要（若 round > 1）：不適用（round 1）
- 本輪計畫（3–7 條）：
  1. 建立 `infra/cloudrun-service.yaml`（服務宣告：min instances 0、$PORT、非root、環境變數名稱引用、健康檢查/health）
  2. 建立 `scripts/verify-health.ts`（package.json 既有的 verify:health 腳本目標檔，本卡補實作），並視情況補 `scripts/deploy-staging.sh`／`scripts/rollback-staging.sh`（workflow 本機等效）
  3. 建立 `.github/workflows/deploy-staging.yml`（WIF → build → push AR → migrate → deploy → verify /health → 帶憑證打 /api/v1/todos）
  4. 建立 `.github/workflows/monitor-health.yml`（cron */5、連續取樣3次、不帶憑證、不帶查詢字串）
  5. 更新 `docs/specs/06_部署架構與CICD.md` 僅三處實作紀錄（第1章網址、5.4回滾演練紀錄、第6章監控實作紀錄）
  6. 更新 `README.md` 部署與secrets章節＋方式二補.env載入步驟；實測方式一、方式二各一次
  7. 逐條執行 WBS §1.8 驗收方式草案指令＋本卡acceptance，貼真實輸出；填交接檔B段；任務卡改 status: review
- 預期產出：infra/cloudrun-service.yaml、.github/workflows/{deploy-staging,monitor-health}.yml、scripts/verify-health.ts（＋可能的 deploy-staging.sh/rollback-staging.sh）、06 三處更新、README 部署章節、本交接檔

## B. 交接

- 完工時間／狀態：2026-09-19T11:40:00+08:00 / review
- 完成事項：
  - `infra/cloudrun-service.yaml`：Knative Service 宣告（min/max scale 0/2、容器埠 8080、資源 512Mi/1CPU、並行 80、逾時 60s、非機密環境變數＋機密項 `secretKeyRef` 參照、`startupProbe`／`livenessProbe` 指向 `/health`）
  - `.github/workflows/deploy-staging.yml`：`workflow_run`（監聽 CI 於 main 成功）＋ `workflow_dispatch` 觸發；階段 auth（WIF）→ migrate（`NEON_DATABASE_URL`）→ build & push（Artifact Registry，SHA＋latest 雙標籤）→ deploy（`gcloud run deploy`，SHA 標籤）→ verify（輪詢 `/health` 200，再帶憑證打 `/api/v1/todos`）→ 回填提示（寫入 `$GITHUB_STEP_SUMMARY`）
  - `.github/workflows/monitor-health.yml`：`cron */5` ＋ `workflow_dispatch`；連續取樣 3 次（間隔 20 秒）、不帶憑證、不附查詢字串；結果寫入 `$GITHUB_STEP_SUMMARY` 與 `actions/upload-artifact`（`health-samples-<run_id>.csv`，保留 90 天）；連續 3 次全失敗才 `::error::`；`STAGING_BASE_URL` 為空時安全略過（`::notice::`，不誤判失敗）
  - `scripts/verify-health.ts`：`npm run verify:health` 的實作（package.json 由 T-0011 已建立此腳本名稱，本卡補實作）；輪詢 `${BASE_URL}/health` 直到 200 或逾時，`POLL_INTERVAL_SECONDS`／`POLL_TIMEOUT_SECONDS` 可覆寫，本機與 `deploy-staging.yml` 的 verify 階段共用同一支腳本（CI 覆寫為每 10 秒、上限 5 分鐘，對齊 06 §3.2）
  - `scripts/deploy-staging.sh`、`scripts/rollback-staging.sh`：`deploy-staging.yml` 與 06 §5.1 回滾程序的本機等效（Git Bash），已加入任務卡 outputs 並在此說明；`bash -n` 語法檢查通過
  - `docs/specs/06_部署架構與CICD.md`：僅三處實作紀錄（第 1 章 staging 網址欄改「待首次部署後回填」＋觸發條件說明；5.4 回滾演練紀錄「尚未實際演練（被憑證阻擋）」＋已備妥的可執行指令對照；新增 6.4 實作紀錄小節，記錄 artifact 落點、GitHub Actions 額度選擇「公開倉庫」、24 小時採樣待遠端環境）。`git diff` 確認只有這 3 處 hunk，`變更紀錄` 表未動
  - `README.md`：新增「部署與 secrets（T-0018）」完整章節（GCP 一次性設定六步 WIF、Secret Manager、Artifact Registry、GitHub repository variables/secrets 完整表、首次部署後回填步驟、本機等效腳本、監測說明）；「方式二：本機 Node」補 `.env` 載入步驟（Git Bash `set -a; . ./.env; set +a`；PowerShell 改用 `[System.IO.File]::ReadAllLines()`，理由見下「假設與決策」3）
  - 任務卡 `outputs` 補列三個 `scripts/*` 檔案；`status` 改 `review`、`updated` 更新
- 產出物（路徑）：
  - `infra/cloudrun-service.yaml`
  - `.github/workflows/deploy-staging.yml`
  - `.github/workflows/monitor-health.yml`
  - `scripts/verify-health.ts`、`scripts/deploy-staging.sh`、`scripts/rollback-staging.sh`
  - `docs/specs/06_部署架構與CICD.md`（三處）
  - `README.md`
  - `tasks/T-0018-deploy-monitor.md`（outputs 補列、status、updated）
  - 本交接檔
- 未完成／已知問題：
  1. **被憑證阻擋的最後一步（見下「需要 Leader 裁決的事」與 06 §5.4）**：實際 `gcloud` 部署、WIF 換證、Secret Manager 建立、Artifact Registry 推送、24 小時 NFR-003 採樣、回滾實機演練，皆需使用者提供 GCP 專案與 GitHub repo 才能執行，本卡只能做到「語法與本機等效指令全部驗證通過」。
  2. **憑證掃描的兩個已知假陽性**（用 dev-tl 補強樣式重掃，含可選引號，`grep -nEi "(password|secret|database_url|api[_-]?key|token)[[:space:]]*[:=][[:space:]]*[\"']?[A-Za-z0-9/+_.-]{2,}"`）：
     - `deploy-staging.yml:26` `id-token: write`——GitHub Actions WIF 必要的 permission 語法關鍵字（`token` 命中「token」，值 `write` 是權限層級非憑證），任何用 WIF 的工作流都會有這一行，無法迴避。
     - `deploy-staging.yml:114`（及 `scripts/deploy-staging.sh` 同一行、`infra/cloudrun-service.yaml` 的 secretKeyRef 說明）`BASIC_AUTH_PASSWORD=basic-auth-pass:latest`——這是 `gcloud run deploy --set-secrets` 的語法（`應用環境變數名稱=Secret Manager secret名稱:版本`），命中是因為應用環境變數名稱 `BASIC_AUTH_PASSWORD`（06 §4.1 凍結，不可改名）恰好含 `PASSWORD` 且後面接的是 secret 參照而非值。已把 Secret Manager 的 secret 名稱刻意取為小寫連字號且避開 `password`/`secret` 等關鍵字整字（`database-url`／`basic-auth-user`／`basic-auth-pass`），排除掉可規避的假陽性；上述兩處是語法結構造成、無法再規避的假陽性，比對 dev-tl 於 T-0011 審核交接檔已建立的先例（`ci.yml` 的 `POSTGRES_PASSWORD: "ci"` 等佔位值同樣是已知接受的假陽性）。用**原始 WBS §1.8 的掃描樣式**（不含引號、不忽略大小寫）重掃 `infra/cloudrun-service.yaml .github/workflows/*.yml` 時，本卡三個新檔**零命中**；命中的三行全部來自 `ci.yml`（T-0011 既有、非本卡 outputs），為既有已接受的假陽性。
  3. **PowerShell `Get-Content` 對 UTF-8 無 BOM＋含中文註解的 `.env` 誤判編碼**（本卡實測發現）：Windows PowerShell 5.1 的 `Get-Content .env`（含 `-Encoding utf8`）只讀到 25 行，把部分行併讀成同一行，導致 regex 抓不到變數；改用 `[System.IO.File]::ReadAllLines()` 正確讀到 30 行。已修正進 README 的方式二 PowerShell 片段（含註解說明），不是本卡的臆測，是實際重現後修的真缺陷。
  4. 其餘無。
- 我做的假設與決策（含理由）：
  1. **`deploy-staging.yml` 的階段順序依 06 §3.2 逐字（auth → migrate → build&push → deploy → verify）**，而非依派工提示詞摘要的順序（build → push → migrate → deploy）。06 是凍結的權威規格且明確寫「先 migrate 是 expand-contract 的 expand 階段」，WBS §1.8 acceptance 的驗收指令也只要求「migrate 在 deploy 之前」（`grep -n "migrate" | head -1` 在 deploy 之前），本卡實作滿足兩者。
  2. **Secret Manager 命名採小寫連字號**（`database-url`／`basic-auth-user`／`basic-auth-pass`），06 §4.1 footnote 把此決定留給「骨架卡」但 T-0011（真正的骨架卡）並未處理 Cloud Run 的 Secret Manager 命名（T-0011 是應用骨架，非部署 IaC），故本卡（實際建立 Cloud Run 部署與 Secret Manager 參照的卡）決定並記錄。命名避開整字 `password`/`secret`/`database_url`/`api_key`/`token`，理由見上「未完成」2。
  3. **README PowerShell 的 `.env` 載入改用 `[System.IO.File]::ReadAllLines()` 而非逐行 `$env:` 字面賦值**：Leader 裁決原文為「PowerShell 逐行設 `$env:`」，本卡以「讀 `.env` 每一行，逐行 `Set-Item env:`」實現同一意圖（避免在 README 重複列出 6 個變數值造成與 `.env.example` 兩處維護，且重複列出憑證佔位值會被憑證掃描命中，見上「未完成」2 的推導），並修正了原本 `Get-Content` 會誤判編碼的真缺陷（見上「未完成」3）。若 Leader 要求逐行字面賦值，需接受重新引入憑證掃描假陽性風險，本卡認為目前寫法更安全、更不易維護飄移，故採用。
  4. **`verify:health` 的逾時參數設計為可覆寫環境變數**（`POLL_INTERVAL_SECONDS`／`POLL_TIMEOUT_SECONDS`），本機預設 2 秒／60 秒，CI 覆寫為 10 秒／300 秒（06 §3.2 verify 階段的「每 10 秒一次、上限 5 分鐘」）。WBS §1.8 背景文字寫「上限 10 分鐘」，但 06 §3.2 明確改寫為「上限 5 分鐘」並附理由（Cloud Run 冷啟僅 1–3 秒，不需要 Render 時代的 10 分鐘）；06 是後於 WBS 修訂的凍結權威文件，故採 5 分鐘。
  5. **`infra/cloudrun-service.yaml` 定位為文件化 IaC，日常部署以 `deploy-staging.yml` 的 `gcloud run deploy` 參數為事實來源**（ADR-0005「後果」段落明確把此取捨留給 dev-ops）：因為 `gcloud run services replace` 需要每次手動代入正確的 image 標籤（該檔案本身不能像工作流一樣自動代入 `${{ github.sha }}`），若把它當唯一事實來源，每次部署都要先産生一份代入標籤的 YAML，反而更複雜；本檔仍完整宣告資源規格供人閱讀對照與偶爾手動套用。
  6. **本機驗證的 host 埠改用 8081/8082/8083/8084 與 5433**（不是 README 寫的 8080/5432）：本 repo 目前有平行工作 T-0017（dev-fe）的 worktree 正在用同一部 Docker daemon 佔用 8080/5432（`docker ps` 可查證，見驗證方式），且提示詞明確要求「不要碰」T-0017。改埠不改變任何應用邏輯或 Dockerfile／docker-compose.yml／README 指令本身的正確性，只是測試時的 host 埠參數；`docker-compose.yml` 與 `README.md` 本身完全未改動這一點（未列為 outputs，也確實沒動）。詳細指令見下「驗證方式」。
- 驗證方式（接手者如何確認我做的是對的：指令 + 我實際得到的輸出）：

  **1. WBS §1.8 驗收方式草案 10 條逐一執行（貼真實輸出）**

  ```bash
  npx --yes prettier@3 --check "infra/cloudrun-service.yaml" ".github/workflows/*.yml"
  ```
  ```text
  Checking formatting...
  All matched files use Prettier code style!
  ```

  ```bash
  grep -nE "(PASSWORD|SECRET|DATABASE_URL|_KEY|_TOKEN)[[:space:]]*:[[:space:]]*[^$\{[:space:]]" infra/cloudrun-service.yaml .github/workflows/*.yml
  ```
  ```text
  .github/workflows/ci.yml:69:          POSTGRES_PASSWORD: "ci"
  .github/workflows/ci.yml:81:      DATABASE_URL: "postgres://ci:ci@localhost:5432/todo"
  .github/workflows/ci.yml:83:      BASIC_AUTH_PASSWORD: "ci"
  ```
  （三筆命中皆來自 `ci.yml`，T-0011 既有檔案、非本卡 outputs，是 dev-tl 於 T-0011 審核時已接受的假陽性佔位值；只掃本卡三個新檔則零命中，見下方補充指令）

  ```bash
  grep -nEi "(password|secret|database_url|api[_-]?key|token)[[:space:]]*[:=][[:space:]]*[\"']?[A-Za-z0-9/+_.-]{2,}" infra/cloudrun-service.yaml .github/workflows/deploy-staging.yml .github/workflows/monitor-health.yml
  ```
  ```text
  .github/workflows/deploy-staging.yml:26:  id-token: write # WIF 用：向 GCP 換取短期憑證
  .github/workflows/deploy-staging.yml:114:            --set-secrets "DATABASE_URL=database-url:latest,BASIC_AUTH_USER=basic-auth-user:latest,BASIC_AUTH_PASSWORD=basic-auth-pass:latest"
  ```
  （用原始 WBS 樣式重掃這三個檔：零命中，見「未完成／已知問題」2 的完整分析）

  ```bash
  grep -n "/health" infra/cloudrun-service.yaml
  ```
  ```text
  17:#   - startup probe 指向 GET /health（唯一未保護路徑，BR-017／O-005）
  34:    # 整站，/health 為唯一豁免（ADR-0004）。
  87:          # 路徑一律用完全等於 "/health" 的字串（不可有點節段變體，
  91:              path: /health
  99:              path: /health
  ```

  ```bash
  grep -nE "workload_identity_provider|auth@v" .github/workflows/deploy-staging.yml
  ```
  ```text
  63:        uses: google-github-actions/auth@v2
  65:          workload_identity_provider: ${{ secrets.GCP_WIF_PROVIDER }}
  ```

  ```bash
  grep -nE "credentials_json|service_account_key|\.json$" .github/workflows/deploy-staging.yml
  ```
  ```text
  （無輸出，exit 1）
  ```

  ```bash
  grep -n "artifactregistry\|Artifact Registry" .github/workflows/deploy-staging.yml
  ```
  ```text
  85:      # ---- build & push（Artifact Registry，SHA 標籤為事實來源） ----
  ```

  ```bash
  grep -n "cron" .github/workflows/monitor-health.yml
  ```
  ```text
  10:# cron 運算式即可；取樣分母的變更須回報 Leader（O-009 裁決）。
  16:    - cron: "*/5 * * * *"
  ```

  ```bash
  grep -nE "(-u |Authorization|BASIC_AUTH)" .github/workflows/monitor-health.yml
  ```
  ```text
  （無輸出，exit 1）
  ```
  **本項有一次真的假陽性、已修正**：初版腳本用 `date -u +%Y-%m-%dT%H:%M:%SZ` 取 UTC 時間，`-u ` 恰好命中掃描樣式（`date` 的 `-u` 是 UTC 旗標，不是 curl 的 Basic Auth 旗標），改成 `date --utc +%Y-%m-%dT%H:%M:%SZ` 後即無輸出（`--utc` 不含子字串 `-u `）。以上為修正後的結果；若接手者重跑仍看到命中，請先確認是否拉到本卡最終版本。

  ```bash
  grep -nE "/health\?" .github/workflows/monitor-health.yml
  ```
  ```text
  （無輸出，exit 1，不附查詢字串）
  ```

  ```bash
  grep -n "migrate" .github/workflows/deploy-staging.yml | head -1
  ```
  ```text
  4:# 階段順序固定：auth → migrate → build & push → deploy → verify
  ```

  ```bash
  npm run verify:health
  ```
  ```text
  > todo-app@0.1.0 verify:health
  > node scripts/verify-health.ts
  [verify-health] 第 1 次：200 http://localhost:8080/health
  ```
  **重要說明**：此次逐字執行時，`localhost:8080` 恰好是平行工作 T-0017 的容器（同一部 Docker daemon，非本卡建置）在服務，回 200 是巧合命中同一個 `/health` 端點、非本卡自建映像的權威證據。本卡自建映像的權威驗證見下第 3 點（改用 8081 埠）。

  **2. actionlint（官方 npm 套件無執行檔，改用官方 Docker 映像 `rhysd/actionlint:latest` 作為等效）**

  ```bash
  MSYS_NO_PATHCONV=1 docker run --rm -v "//c/Users/excal/Projects/Claude/soft-dev-team-260919-wt/T-0018:/repo" -w //repo rhysd/actionlint:latest -color .github/workflows/deploy-staging.yml .github/workflows/monitor-health.yml .github/workflows/ci.yml
  echo "EXIT:$?"
  ```
  ```text
  EXIT:0
  ```
  （三個工作流檔案零 finding；`npx --yes actionlint` 本身失敗，因為 npm 上的 `actionlint` 套件是 wasm library、無 bin 可執行，見交接檔此處說明作為「等效」的依據）

  **3. 本機 docker build 與自建映像的權威 `/health`／`/api/v1/todos` 驗證（改用替代 host 埠，理由見上「假設與決策」6）**

  ```bash
  docker build -t todo-app:staging .
  ```
  ```text
  ...（多階段快取命中，exporting to image / naming to docker.io/library/todo-app:staging done）
  ```

  ```bash
  docker ps --format "{{.Names}}: {{.Ports}}"   # 證明 8080/5432 被 T-0017 佔用，非本卡臆測
  ```
  ```text
  t-0017-app-1: 0.0.0.0:8080->8080/tcp, [::]:8080->8080/tcp
  t-0017-db-1: 0.0.0.0:5432->5432/tcp, [::]:5432->5432/tcp
  ```

  ```bash
  # docker-compose.yml 的一份僅改 host 埠（5433/8081）的臨時複本（未進版控），
  # --project-directory . 確保 build context 仍是本 worktree
  docker compose -f <臨時複本> --project-directory . -p t0018final --env-file .env up -d
  DATABASE_URL="postgres://dev:dev@localhost:5433/todo" NODE_ENV=development PORT=8080 \
    BASIC_AUTH_USER=dev BASIC_AUTH_PASSWORD=dev LOG_LEVEL=info npm run migrate
  BASE_URL=http://localhost:8081 POLL_INTERVAL_SECONDS=1 POLL_TIMEOUT_SECONDS=10 npm run verify:health
  curl -sS -w "\nHTTP:%{http_code}\n" -u dev:dev http://localhost:8081/api/v1/todos
  ```
  ```text
  已套用 migration：001
  [verify-health] 第 1 次：200 http://localhost:8081/health
  []
  HTTP:200
  ```
  （驗完即 `docker compose ... down -v` 清除，`docker ps` 確認只剩 T-0017 兩個容器）

  **4. README 方式一（docker compose）與方式二（本機 Node）各實測一次並記錄耗時**

  方式一（Git Bash，`docker build` 全快取命中）：
  ```bash
  time ( cp .env.example .env && docker build -t todo-app:dev . && docker compose -f <臨時埠複本> --project-directory . -p t0018m1 --env-file .env up -d --build && sleep 3 && curl -sS -o /dev/null -w "%{http_code}\n" http://localhost:8081/health && curl -sS http://localhost:8081/health )
  ```
  ```text
  200
  {"status":"ok"}
  real 0m14.286s
  ```

  方式二（Git Bash，`npm ci` 快取，全新 `node_modules`／`dist`）：
  ```bash
  rm -rf node_modules dist .env
  time ( cp .env.example .env && sed -i "s/^PORT=8080/PORT=8082/" .env && npm ci && npm run build && ( set -a; . ./.env; set +a; nohup npm start > /tmp/t0018-m2.log 2>&1 & ) && sleep 2 && curl -sS -o /dev/null -w "%{http_code}\n" http://localhost:8082/health )
  ```
  ```text
  200
  real 0m6.680s
  ```

  方式二（PowerShell，全新 `node_modules`／`dist`，含修正後的 `.env` 載入片段）：
  ```powershell
  Remove-Item -Recurse -Force node_modules, dist, .env
  Copy-Item .env.example .env; (Get-Content .env) -replace '^PORT=8080','PORT=8084' | Set-Content .env
  $sw = [System.Diagnostics.Stopwatch]::StartNew()
  npm ci; npm run build
  [System.IO.File]::ReadAllLines("$PWD\.env") | Where-Object { $_ -match '^[A-Z_]+=' } | ForEach-Object { $n,$v = $_ -split '=',2; Set-Item "env:$n" $v }
  Start-Process node dist/server.js -PassThru -WindowStyle Hidden -RedirectStandardOutput m2-out.log -RedirectStandardError m2-err.log
  Start-Sleep 2
  Invoke-WebRequest -UseBasicParsing http://localhost:8084/health
  $sw.Stop(); $sw.Elapsed
  ```
  ```text
  StatusCode: 200
  Body: {"status":"ok"}
  方式二 PowerShell 全程耗時：00:00:07.7396796
  ```
  （PowerShell 初次沿用 Leader 裁決原文的 `Get-Content .env` 逐行寫法，實測失敗——見上「未完成／已知問題」3 與「假設與決策」3 的完整說明；修正為 `[System.IO.File]::ReadAllLines()` 後才通過，此為本卡發現並修正的真缺陷，已寫回 README）

  **5. `npm run lint`／`npm run test:unit`（全綠，證明本卡未破壞既有程式碼；本卡未改動 src/、tests/）**

  ```bash
  npm run lint; echo "LINT_EXIT:$?"
  npm run build && npm run test:unit
  ```
  ```text
  LINT_EXIT:0
  ...
  ℹ tests 51
  ℹ pass 51
  ℹ fail 0
  ```

  **6. `git diff --stat` 只動 outputs（含新增 outputs 已在任務卡補列）**

  ```bash
  git diff --stat
  git status --short
  ```
  ```text
   README.md                       | 144 +++++++++++++++++++++++++++++++++++++++-
   docs/specs/06_部署架構與CICD.md |  11 ++-
   tasks/T-0018-deploy-monitor.md  |   3 +
   3 files changed, 154 insertions(+), 4 deletions(-)
  ?? .github/workflows/deploy-staging.yml
  ?? .github/workflows/monitor-health.yml
  ?? infra/cloudrun-service.yaml
  ?? scripts/
  ?? worklog/handoff/20260919-0949-T0018-r1-dev-ops.md
  ```
  （`docker-compose.yml`／`.env.example`／`src/`／`tests/`／`04_API規格.yaml` 皆未列出＝未改動）

  **7. `bash -n` 語法檢查兩個新 shell 腳本**

  ```bash
  bash -n scripts/deploy-staging.sh && echo OK
  bash -n scripts/rollback-staging.sh && echo OK
  ```
  ```text
  deploy-staging.sh: syntax OK
  rollback-staging.sh: syntax OK
  ```

- 下一步建議：
  1. **給使用者（部署前必做，見 README「部署與 secrets」章節）**：建立 GCP 專案＋計費帳戶 → 啟用 5 個 API → 建 Artifact Registry → 完成 WIF 六步（或備選服務帳號金鑰）→ 建 3 個 Secret Manager secret（`database-url`／`basic-auth-user`／`basic-auth-pass`）並授權 → 建 Neon 專案 → 填 GitHub 6 個 variables ＋ 6 個 secrets（見 README 表格）→ 建議倉庫設公開（額度考量）。完成後 push 到 `main`，`deploy-staging.yml` 會自動觸發。
  2. **給使用者（首次部署成功後）**：從該次工作流的 `$GITHUB_STEP_SUMMARY` 或 `gcloud run services describe todo-app --region asia-east1 --format="value(status.url)"` 取網址，回填 `STAGING_BASE_URL`（GitHub variable）、`06_部署架構與CICD.md` 第 1 章、`04_API規格.yaml` 的 `servers` staging 項（後者不在本卡 outputs，需另一張卡或 Leader 指定）。
  3. **給 dev-tl／qa-lead**：回滾演練（06 §5.4）與 24 小時 NFR-003 採樣，須在使用者完成上兩步、staging 服務存在後才能進行，目前只能做到「指令與工作流語法全部備妥並通過本機驗證」，屬「遠端待驗」清單，請追蹤。
  4. **給日後任何要動 README／`.env.example` 的卡**：`.env`／`.env.example` 含中文註解＋UTF-8 無 BOM，PowerShell 5.1 的 `Get-Content` 會誤判編碼併行，任何要在 PowerShell 讀取這類檔案逐行處理的腳本，請用 `[System.IO.File]::ReadAllLines()`，不要用 `Get-Content`（除非確認檔案不含多位元組字元或已加 BOM）。
  5. **給 qa-cr／dev-tl**：憑證掃描樣式（無論 WBS 原始版或 dev-tl 補強版）在「WIF 的 `id-token: write`」與「`gcloud run --set-secrets` 的 `ENV_NAME=secret-name:version` 語法」上有結構性假陽性，只要專案持續用 WIF 與 Cloud Run Secret Manager 參照，未來任何卡寫到這兩種語法都會命中，建議把這兩個模式列入掃描的「已知例外」清單（而非每卡都要重新論證），可在 06 或 qa-cr 檢查表補一行參照本交接檔。
- 需要 Leader 裁決的事：
  1. **是否要求 `README.md` 的 `servers` 佔位符回填（`04_API規格.yaml`）在本卡範圍內**：ADR-0005「對其他文件的影響」寫「留給 dev-ops 於骨架卡回填」，但實際回填動作只能在「首次部署成功、拿到真實網址」後才能做，本卡（T-0018）目前無法完成；且 `04_API規格.yaml` 不在本卡 `outputs` 清單。建議：維持現狀（佔位符不動），等使用者提供 GCP／GitHub 並完成首次部署後，由 Leader 開一張小卡（或授權 dev-ops 在既有卡直接補一行）處理，本卡不越權自行加入 outputs。
  2. **06 §4.1 footnote「Secret Manager 命名由 dev-ops 於骨架卡決定」的歸屬**：本卡認定「骨架卡」在此處實務上應理解為「真正處理 Cloud Run／Secret Manager 的卡」（即本卡 T-0018），而非應用骨架卡 T-0011（T-0011 完全不涉及 GCP 資源）。已依此做出命名決策（見「假設與決策」2）並記錄，如 Leader 認為此文字有誤導性，建議日後修 06 措辭澄清，不影響本卡產出的正確性。
  3. **使用者需要做的事清單（彙總，非決策項，供 Leader 追蹤）**：GCP 專案＋計費帳戶、啟用 API、Artifact Registry、WIF 六步（或服務帳號金鑰備選）、Secret Manager 三個 secret、Neon 專案、GitHub repo（本 repo 目前無遠端）＋ 6 variables／6 secrets、（建議）倉庫設公開。詳細指令與表格見 `README.md`「部署與 secrets（T-0018）」章節，agent 未索取、未代填任何值。
