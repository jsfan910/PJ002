# T-0045 r1 — dev-ops — 工作紀錄與交接

## A. 開工紀錄

- 開工時間：2026-09-20T20:42:05+08:00
- 角色／模型：dev-ops / sonnet
- 任務卡：tasks/T-0045-traffic-pinned-fix.md（round 1）
- 讀取的輸入（實際讀過的檔案與段落，非任務卡照抄）：
  - tasks/E-001-todo-app.md#2026-09-20T20:41:17 事故段 — 事實：spec.traffic 100% 釘死在 todo-app-00003-lt2，revision 00004～00018（15 個）從未接流量；成因 09-19 演練 `update-traffic --to-revisions ...=100` 後未 `--to-latest` 還原。
  - docs/specs/06_部署架構與CICD.md#5.1、#5.4 — 回滾流程原理（revision 切流量）與 09-19 演練紀錄（演練本身成功，但未記載演練後必須還原）。
  - docs/specs/06_部署架構與CICD.md#3.2 — deploy＋verify 單一步驟現況：verify 只驗 `/health` 與帶憑證 `/api/v1/todos`，未驗新 revision 是否真的接到流量（此即 D-018 根因）。
  - docs/specs/06_部署架構與CICD.md#6.9、#6.9.1 — T-0039 secret 版本釘定與 verify 自動重試的實作模式（本卡沿用同一種「函式化＋GITHUB_STEP_SUMMARY 記錄」寫法）。
  - .github/workflows/deploy-staging.yml、scripts/deploy-staging.sh — 現行 `gcloud run deploy` 不帶任何流量旗標；deploy_revision/get_service_url/verify_health/verify_auth 函式結構。
  - scripts/rollback-staging.sh — 現行只有「列清單」與「切到指定 revision」兩種模式，無 `--to-latest` 還原模式，也無演練後必須還原的警語。
  - scripts/measure-deploy-downtime.sh — T-0031 的 /health 輪詢量測腳本，背景執行、CSV 輸出、算最長連續非 200 秒數。
  - docs/specs/20_測試案例.md#TC-090 — 部署不可用時間 < 60 秒門檻。
- 本輪計畫（3–7 條）：
  1. 背景啟動 measure-deploy-downtime.sh（≥180 秒）量測復原前後，執行 `update-traffic --to-latest` 復原流量，貼前後 `services describe` 輸出。
  2. 修 deploy-staging.yml 與 scripts/deploy-staging.sh：deploy 後檢查 `status.latestCreatedRevisionName` 與 `status.traffic`，未接 100% 流量時自動 `update-traffic --to-latest` 並重新 verify；STEP_SUMMARY 記錄 revision 與流量百分比。
  3. rollback-staging.sh 新增 `--to-latest` 模式與檔頭警語；README 同步。
  4. 復原後以空 commit 推 main 觸發真實部署，背景輪詢 /health，貼 run 結果、STEP_SUMMARY、TC-090 量測結果。
  5. 寫 06 §6.10 事故時間軸與事故報告 docs/reports/20260920-HHmm-流量釘死事故-E001.md（含給 qa-lead 的重驗清單）。
  6. 全程不讀取／不輸出 secret 值；只 add 任務卡 outputs。
- 預期產出：.github/workflows/deploy-staging.yml、scripts/deploy-staging.sh、scripts/rollback-staging.sh、README.md（回滾小節）、docs/specs/06_部署架構與CICD.md（§6.10）、docs/reports/20260920-HHmm-流量釘死事故-E001.md、本交接檔。

## B. 交接

- 完工時間／狀態：2026-09-20T21:26:24+08:00 / review
- 完成事項：
  - 復原流量：背景量測 200 秒（1 秒一次）後執行 `gcloud run services update-traffic todo-app --region asia-east1 --to-latest`，貼前後 `services describe` 輸出；152/152 樣本皆 200、最長連續非 200 秒數 = 0。
  - Pipeline 修法：`.github/workflows/deploy-staging.yml`、`scripts/deploy-staging.sh` 於 deploy 後新增流量驗證，主判準讀 `status.traffic` 加總新 revision 拿到的百分比並要求等於 100，`status.latestReadyRevisionName` 是否等於該 revision 併作輔助佐證；未達 100% 自動 `update-traffic --to-latest` 並重新驗證，仍未達才判紅。
  - `scripts/rollback-staging.sh` 新增 `--to-latest` 還原模式與檔頭警語；README「部署與 secrets」章節同步。
  - dev-tl 初審 r1 判 R-1（判準需用 `status.traffic` 而非 `latestReadyRevisionName` 代理指標）、R-2（06 §7 標題被 §6.10 插入動作吞併）兩項阻擋，已於同一 worktree 追加 commit（`78dca59`）修正，重跑 `actionlint`／`yaml-lint`／`bash -n` 皆通過，`grep "^## "` 核對 06 章節結構已與 main 一致。
  - dev-tl 複審合併修正進 main（`5b28b13`）後，Leader 推送 main（`95bc4ca`）觸發真實 `deploy-staging` run，dev-ops 推送前背景啟動 300 秒量測並全程涵蓋；run `35513187463` `conclusion=success`，`services describe` 唯讀複查確認新 revision `todo-app-00019-k5q` 拿到 100% 流量，226/226 樣本皆 200、最長連續非 200 秒數 = 0（TC-090 重測通過）。
  - 06 §6.10（事故時間軸、影響範圍、復原、pipeline 修法、真實部署重測、給 qa-lead 的重驗清單）、事故報告 `docs/reports/20260920-2042-流量釘死事故-E001.md` 皆已補上真實 run 結果，無佔位段落殘留。
- 產出物（路徑）：
  - `.github/workflows/deploy-staging.yml`
  - `scripts/deploy-staging.sh`
  - `scripts/rollback-staging.sh`
  - `README.md`
  - `docs/specs/06_部署架構與CICD.md`（§6.10、§變更紀錄 T-0045 列）
  - `docs/reports/20260920-2042-流量釘死事故-E001.md`
  - 本交接檔
- 未完成／已知問題：
  - TC-080（`/api/v1/todos` 資料筆數直接比對）需有 Secret Manager 憑證的執行者於下一輪測試卡補做，本卡依安全鐵則未讀取憑證，已列入 06 §6.10.4 給 qa-lead 的重驗清單。
  - 06 §5.1 規格文字本身（例如補述「演練後須還原」的正式流程文字）屬規格變更，未在本卡範圍內修改，已於 06 §變更紀錄 T-0045 列註明留待 P1 規格同步卡由 plan-sd 處理。
  - GitHub Actions 原始 job log 因匿名 API 需登入（`/actions/jobs/{id}/logs` 回 403）而無法讀取，已改以 `gcloud run services describe`（唯讀）複查 run 結果佐證，效力已於 06 §6.10.3、事故報告中說明。
- 我做的假設與決策（含理由）：
  - 判準改為「主判準讀 `status.traffic` 加總目標 revision 百分比要求 =100，`latestReadyRevisionName` 降為輔助佐證」——採 dev-tl 初審 r1（R-1）的具體改法，理由是 `latestReadyRevisionName` 為 Cloud Run 未文件化行為且會漏判分流／canary 情境，`status.traffic` 才是 acceptance 明文要求的依據。
  - 「以空 commit 觸發真實部署」實際執行方式改為「Leader 於根目錄推送 main」（而非 dev-ops 自己在根目錄空 commit），因根目錄當時由 dev-tl 合併 T-0044 中，dev-ops 不得觸碰根目錄；此變更由 Leader 於對話中直接安排，效果等同任務卡 acceptance 第 4 條要求的「一次真實部署＋量測」。
  - 全程未讀取、未輸出任何 secret 值；`rollback-staging.sh --to-latest` 實跑驗證時 `STAGING_BASIC_AUTH_USER`／`STAGING_BASIC_AUTH_PASSWORD` 未匯出，驗證 2/3（帶憑證）依設計正常跳過。
- 驗證方式（接手者如何確認我做的是對的：指令 + 我實際得到的輸出）：
  ```bash
  GCLOUD="C:/Users/excal/AppData/Local/Google/Cloud SDK/google-cloud-sdk/bin/gcloud.cmd"
  MSYS_NO_PATHCONV=1 "$GCLOUD" run services describe todo-app --region asia-east1 --project pj002-509106 \
    --format="yaml(spec.traffic,status.traffic,status.latestReadyRevisionName,status.latestCreatedRevisionName)"
  ```
  ```yaml
  spec:
    traffic:
    - latestRevision: true
      percent: 100
  status:
    latestCreatedRevisionName: todo-app-00019-k5q
    latestReadyRevisionName: todo-app-00019-k5q
    traffic:
    - latestRevision: true
      percent: 100
      revisionName: todo-app-00019-k5q
  ```
  ```bash
  curl -sS "https://api.github.com/repos/jsfan910/PJ002/actions/runs/35513187463" | grep -oE '"conclusion": *"[^"]+"|"run_started_at": *"[^"]+"|"updated_at": *"[^"]+"'
  ```
  ```text
  "run_started_at": "2026-09-20T13:20:01Z"
  "updated_at": "2026-09-20T13:22:23Z"
  "conclusion": "success"
  ```
  ```bash
  # scripts/measure-deploy-downtime.sh 300 秒量測輸出（涵蓋上述 run 全程）
  ```
  ```text
  最長連續失敗次數（約秒數，取樣間隔 ~1s）：0
  全程無非 200 回應。
  226 200（awk 統計，226/226 皆 200）
  ```
  ```bash
  cd "C:\Users\excal\Projects\Claude\soft-dev-team-260919-wt\T-0045"
  bash -n scripts/deploy-staging.sh scripts/rollback-staging.sh
  /c/Users/excal/AppData/Local/Temp/actionlint_test/actionlint.exe -color .github/workflows/deploy-staging.yml .github/workflows/ci.yml .github/workflows/monitor-health.yml
  grep -n "^## " docs/specs/06_部署架構與CICD.md
  ```
  ```text
  （bash -n 與 actionlint 皆無輸出、EXIT=0）
  22:## 1. 環境清單
  38:## 2. 雲端資源清單（依 ADR-0005）
  100:## 3. Pipeline 階段（GitHub Actions）
  185:## 4. 環境變數清單（只列名稱與用途，值不入版控）
  237:## 5. 回滾程序（必須實際演練過一次，記錄日期與結果）
  315:## 6. 監控與告警
  845:## 7. 三處必須同步的參數表（T-0025，CR S-9，實作紀錄）
  869:## 變更紀錄
  ```
- 下一步建議：
  - qa-lead 依 06 §6.10.4／事故報告的重驗清單安排 TC-080（需憑證，由有權限者執行）、D-017、TC-009 的重新驗證。
  - plan-sd 於下一張 P1 規格同步卡，將「回滾演練後必須 `--to-latest` 還原」補進 06 §5.1 的正式流程文字（本卡只在 §6.10 記錄事故與在腳本／README 加警語，未動 §5.1 本文）。
  - dev-tl 複審本輪修正與真實部署結果，通過後合併、更新 CHANGELOG、清理 worktree／分支。
- 需要 Leader 裁決的事：
  - 無。
