# T-0021 r2 — qa-at（對最終 main 重跑自動化測試） — 工作紀錄與交接

<!-- 檔名：worklog/handoff/20260919-1314-T0021-r2-qa-at.md（時間 = 開工時間，取實查系統時間） -->
<!-- 規則：開工立即寫 A 段；完工或中止時寫 B 段。同一輪同一角色一個檔。 -->

## A. 開工紀錄

- 開工時間：2026-09-19T13:14:00+08:00
- 角色／模型：qa-at / Claude Sonnet 5
- 任務卡：tasks/T-0021-automated-tests.md（round 2）
- 讀取的輸入（實際讀過的檔案與段落，非任務卡照抄）：
  - `.claude/agents/qa-at.md` 全文 — 職責、必讀、必寫、完成定義、通用協定（開工／收尾／禁止）
  - `docs/reports/20260919-1221-AT-E001.md`（r1 報告）全文 — r1 對修正前 main（`153410b`）的數字：integration 89（85 過／4 阻擋）、e2e 136/136（含 Firefox 阻擋 2 組）、perf PASS、README 方式一 27 秒／方式二 6 秒、憑證掃描 0 真命中
  - `worklog/handoff/20260919-1301-T0021-r1-dev-tl.md` 全文 — dev-tl 合併後對含 CR 修正的 main 重跑數字（unit 76/76、integration 56/56、qa-integration 38 過／4 阻擋、e2e Chromium 68/68、perf PASS、actionlint 0）；「下一步建議」1：script 拆成 `test:integration`＋`test:integration:qa`、Edge 建議補跑、AT 報告需更新；「假設與決策」1～6（glob 收斂原因、tsconfig.e2e.json、Playwright 鎖版 1.63.0）
  - `README.md`「常用指令」表與「已知平台限制」全部條目 — 新 script（`test:integration`、`test:integration:qa`、`test:e2e`、`test:perf`）、方式二新前提（啟動時自動 migration、需可連到的 Postgres、`.env` 不自動載入）
  - `tasks/E-001-todo-app.md` 最後三段裁決（12:32:40、12:59:14、13:12:54）— T-0021 r1 裁決（Playwright 鎖版、Firefox 列 Gate 2 後待辦）；修正卡合併完成（main `4b96b20`）；T-0021 合併與追認（main `e7abe4f`，追認 dev-tl 兩項偏離，明文「qa-at r2：對最終 main 重跑三類並更新 AT 報告」）
  - `tasks/T-0021-automated-tests.md` 全文 — acceptance 六條、審核紀錄表 r1 兩列（dev-tl 技術初審通過並合併；leader rework→r2）
- 上一輪交接摘要（round 1 → r2，非退回，是「技術初審通過並合併」後的延續輪）：
  - 原因：r1 報告對象是修正前 main（`153410b`），dev-tl 合併時把三項 CR 修正（T-0024～T-0026）與 Playwright devDependency 一併帶進 main，且把 `test:integration` 拆成「進程內」與「真實 HTTP（`test:integration:qa`）」兩條 script；r1 報告的指令與數字已過時（dev-tl 交接檔「未完成／已知問題」2 明文指出，且 leader 派工提示詞要求 r2 專屬報告，不改 r1）
  - 本輪要做：對最終 main（`a288ca7`）重跑 lint／test:unit／test:integration／test:integration:qa／test:e2e（Chromium＋Edge）／test:perf，README 方式二依新版重測一次；staging 相關 TC 仍記阻擋
- 本輪計畫（3–7 條）：
  1. 用自己的 docker compose project 名稱／埠（避免動到 qa-cr r2、qa-uat r2 正在用的 `localhost:8080`），起本機服務
  2. `npm ci`、`npm run build`、`npm run lint`
  3. `npm run test:unit`、`npm run test:integration`（進程內，不需服務）
  4. `npm run test:integration:qa`（真實 HTTP，對自起的服務）
  5. `npm run test:e2e -- --project=chromium-*` 與 `--project=msedge-*`（記錄版本號）
  6. `npm run test:perf`（跑完清殘留資料）
  7. README 方式二依新 README 重測一次記錄耗時；產出 r2 報告；填交接檔 B 段、任務卡 status: review；commit
- 預期產出：`docs/reports/20260919-HHmm-AT-E001-r2.md`、任務卡審核紀錄表新增一列、本交接檔 B 段

## B. 交接

- 完工時間／狀態：2026-09-19T13:25:04+08:00 / **review**（reviewer 仍為 qa-lead，T-0023 驗收）
- 完成事項：
  1. 對最終 main（開工時 HEAD `a288ca7`；期間 main 前進到 `03b71f4`，已用 `git diff --name-only a288ca7 03b71f4` 核對只動 `docs/reports`／`tasks/`／`worklog/handoff/`，未動 `src/`／`tests/`，不影響本輪測試結果有效性）重跑：`npm ci`、`npm run build`、`npm run lint`、`npm run test:unit`、`npm run test:integration`（進程內）、`npm run test:integration:qa`（真實 HTTP）、`npm run test:e2e`（Chromium × 2 尺寸＋Edge × 2 尺寸）、`npm run test:perf`（NFR-001／NFR-007）。
  2. README 15 分鐘實測：只重測**方式二**（依提示詞，方式一 r1 已 0 卡點不必重做；方式二在 T-0024 後前提改變，需重測）。乾淨目錄（`git archive HEAD`）逐字依新版 README 操作，**合計約 14 秒、0 卡點**，遠低於 15 分鐘門檻。
  3. 全程用自己的 docker compose project（`t0021r2`／`t0021r2-readme`）與埠（8092/5434、8199/5435），未動到平行 agent（qa-cr r2、qa-uat r2）當時在用的 `localhost:8080` 既有服務；跑完後 `docker compose down -v` 完全移除，無殘留容器／資料卷。
  4. 產出 r2 專屬報告 `docs/reports/20260919-1314-AT-E001-r2.md`，開頭已註明「取代 r1 數字」；r1 報告 `docs/reports/20260919-1221-AT-E001.md` 未改動。
  5. 任務卡：`status` 改為 `review`、`updated` 更新；審核紀錄表未動（由審核者填）。
- 產出物（路徑）：
  - `docs/reports/20260919-1314-AT-E001-r2.md`（本輪 AT 報告，取代 r1 數字）
  - `tasks/T-0021-automated-tests.md`（`status: review`、`updated`）
  - 本交接檔
  - 無測試碼變更（本輪未修改 `tests/**`，重跑即通過，無需修正測試寫法）
- 未完成／已知問題：
  1. **Firefox 兩組 e2e 仍未跑**：沿用 r1／dev-tl 結論（本沙盒 `browserType.launch: spawn UNKNOWN`，環境限制非產品缺陷）。Leader 已裁決列為 Gate 2 後待辦，本輪未再嘗試。
  2. **TC-089／090（NFR-003 24 小時採樣、部署不可用時間）、TC-079／080／084（真實部署／回滾）仍為「阻擋（環境未就緒）」**：staging 尚未部署，與前兩輪結論相同。
  3. **perf 的 TC-085 GET P95（47.7ms）比 r1（2.8ms）明顯偏高**（仍遠低於 500ms 門檻，PASS 不受影響）：判讀是本輪 perf 資料卷累積了較多殘留列（陣列長度 37719），`GET` 回整份陣列序列化耗時隨筆數增加；已在 r2 報告「原始輸出」節下方加註觀察，建議下一輪 perf 前先清空專用資料庫再量測 baseline。**本輪已用 `docker compose -p t0021r2 down -v` 整組移除該資料卷，不會遺留給下一輪。**
  4. **偵測到一個與本輪無關的孤兒行程**：`localhost:8098` 被一個既有 `node.exe`（PID 36548，啟動時間 12:18:48）佔用，推測是先前 r1 README 實測留下未關閉的行程（r1 報告記錄用過 8096/8098）。本輪未觸碰（不確定是否仍被其他 agent 依賴），改用 8199 埠避開；記錄於此供 Leader 或後續清理參考，非本卡阻擋項。
- 我做的假設與決策（含理由）：
  1. **README 方式二重測用另一個獨立埠（8199/5435）而非 r1 用過的 8096/8098**：因 8098 已被孤兒行程佔用（見上），改用未被占用的埠，效果不受影響（README 本身埠可配置，不影響驗證結論）。
  2. **`test:integration`（進程內）沿用既有共用 `localhost:5432` 資料庫**（而非自己的 compose db）：因該測試不需要「服務在跑」，只需要一個可連線的 Postgres；使用既有共用 db 執行後核對筆數（4 筆，測試自身建立的 500 筆已於測試內清理），未對其他平行 agent 造成可觀察影響，故未額外起專用 db。
  3. **perf 與 test:integration:qa／e2e 使用自己的 compose project（`t0021r2`）而非既有共用服務**：因這三者需要「服務長時間運行」且 perf 會產生大量資料異動，若用共用服務會干擾同時在跑的 qa-cr r2／qa-uat r2；跑完後完整 `down -v` 移除，避免遺留。
  4. **不重做 README 方式一**：依提示詞明文「r1 已 0 卡點，不必重做」。
  5. **本輪未修改任何 `tests/**` 檔案**：重跑全部通過，未發現測試碼因合併產生的過時之處（dev-tl 已在合併時處理型別檢查相關的 3 處小修，見 r1 dev-tl 交接檔），故無需再修。
- 驗證方式（接手者如何確認我做的是對的：指令 + 我實際得到的輸出）：
  ```bash
  npm run lint
  ```
  ```text
  LINT_EXIT=0
  ```
  ```bash
  npm run test:unit
  ```
  ```text
  ℹ tests 76  ℹ pass 76  ℹ fail 0  ℹ skipped 0
  ```
  ```bash
  NODE_ENV=test PORT=8080 DATABASE_URL="postgres://dev:dev@localhost:5432/todo" \
    BASIC_AUTH_USER=dev BASIC_AUTH_PASSWORD=dev LOG_LEVEL=silent npm run test:integration
  ```
  ```text
  ℹ tests 56  ℹ pass 56  ℹ fail 0  ℹ skipped 0
  ```
  ```bash
  BASE_URL=http://localhost:8092 BASIC_AUTH_USER=dev BASIC_AUTH_PASSWORD=dev npm run test:integration:qa
  ```
  ```text
  ℹ tests 42  ℹ pass 38  ℹ fail 0  ℹ skipped 4
  ```
  ```bash
  BASE_URL=http://localhost:8092 BASIC_AUTH_USER=dev BASIC_AUTH_PASSWORD=dev \
    npm run test:e2e -- --project=chromium-1280x800 --project=chromium-390x844 --project=msedge-1280x800 --project=msedge-390x844
  ```
  ```text
  [TC-091][瀏覽器版本] chromium 153.0.8010.12 @ chromium-1280x800
  [TC-091][瀏覽器版本] chromium 153.0.8010.12 @ chromium-390x844
  [TC-091][瀏覽器版本] chromium 153.0.4234.32 @ msedge-1280x800
  [TC-091][瀏覽器版本] chromium 153.0.4234.32 @ msedge-390x844
  136 passed (57.6s)
  ```
  ```bash
  BASE_URL=http://localhost:8092 BASIC_AUTH_USER=dev BASIC_AUTH_PASSWORD=dev npm run test:perf
  ```
  ```text
  TC-085 PASS  TC-086(POST) PASS  TC-086(PATCH) PASS  TC-086(DELETE) PASS
  TC-094：5 次中位數 184.7ms（門檻 < 2000ms） → PASS
  ```
  完整原始輸出（含逐條 TC 通過紀錄）已貼於 `docs/reports/20260919-1314-AT-E001-r2.md` 末「原始輸出」節，本檔不重複貼整份以免造成第二份真相。
- 下一步建議：
  1. **給 qa-lead（T-0023 驗收）**：重跑指令改用 r2 報告「指令」節；TC-ID 覆蓋比對不受影響。
  2. **給後續 perf 執行者**：若需要可比較的 baseline P95 數字，建議先清空 perf 專用資料庫（或用全新資料卷）再量測，避免像本輪 TC-085 GET 因既有資料列數不同而使 p95 數字在各輪間無法直接比較（雖仍遠低於門檻，不影響 PASS 判定）。
  3. **給 Leader／dev-ops**：`localhost:8098` 有一個孤兒 `node.exe`（PID 36548）殘留，疑為 r1 README 實測遺留未關閉，建議找空檔手動確認後終止（本輪未動，怕誤殺其他 agent 行程）。
- 需要 Leader 裁決的事：
  -（無）
