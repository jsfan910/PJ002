# T-0019 r1 — qa-lead — 工作紀錄與交接

<!-- 檔名：worklog/handoff/20260919-0745-T0019-r1-qa-lead.md（開工時間取實查系統時間 date "+%Y%m%d-%H%M" = 20260919-0745） -->

## A. 開工紀錄

- 開工時間：2026-09-19T07:45:48+08:00
- 角色／模型：qa-lead / Claude Opus 5
- 任務卡：tasks/T-0019-testplan-sync.md（round 1，status 已為 in_progress；depends_on: [T-0010]）
- 讀取的輸入（實際讀過的檔案與段落，非任務卡照抄）：
  - `.claude/agents/qa-lead.md` 全文 — 本角色通用協定（開工四步、收尾五步、禁止事項：不擴大範圍、不動 outputs 以外的檔案）。
  - `tasks/T-0019-testplan-sync.md` 全文 — 五條 acceptance、outputs 三檔、驗收方式（grep -c Render 與重跑 T-0009 的 V1～V3）。
  - `tasks/E-001-todo-app.md#Leader 裁決紀錄` — 「對測試計畫（T-0009）7 項的裁決」（第 1 項授權補填 NFR 追溯表「對應測試」欄、第 2 項授權改主表「狀態」欄、第 3 項 NFR-004 改讀為「最新版即可」、第 5 項開本卡同步 Cloud Run 於 6.1／R-4／TC-079／084／089）與「對 T-0010 四項的裁決」（第 2 項保溫維持每 5 分鐘、第 3 項 04 的 staging servers 改 `*.run.app` 形式、第 4 項不建 `infra/render.yaml`）。
  - `worklog/handoff/20260919-0719-T0009-r1-qa-lead.md#B` 全文 — 八格 NFR→TC 對應內容（裁決事項第 1 點）、五處受 Cloud Run 影響位置（裁決事項第 5 點：07 第 6.1 節、07 R-4、TC-084、TC-089、TC-079）、V1～V3 驗證指令原文與當時輸出（123／123／0／TC-SET-IDENTICAL-OK）。
  - `docs/specs/06_部署架構與CICD.md`（Cloud Run 版 v0.2）第 1 章環境清單（staging 網址格式 `https://<service>-<hash>-<region-code>.a.run.app`、由 dev-ops 首次部署後回填三處）、第 2 章資源清單（平台 301、`min-instances = 0`、Artifact Registry 保留 5 版）、第 3.3 節（保溫每 5 分鐘、Cloud Run 無 15 分鐘休眠門檻、冷啟 1–3 秒）、第 5.1 節（revision 切流量回滾指令與四步驗證）、第 5.4 節（演練要求：啟動失敗與行為錯誤兩種要分開演練）、第 6.1／6.3 節（內建指標 `Container startup latency`／`Container instance count`／`Request count`、判讀規則、冷啟已不足以造成取樣失敗）。
  - `docs/specs/adr/ADR-0005-雲端平台-CloudRun.md` 全文 — 冷啟 1–3 秒、revision 回滾、`min instances = 0` 為免費前提、HTTPS 由 Google 前端 301 導向、保溫性質降級為「錦上添花」。
  - `docs/specs/07_測試計畫.md` 第 4 章第 6 項、第 6.1 節、第 7 章 R-4／R-5 — 待改寫的四處實際原文與行號。
  - `docs/specs/20_測試案例.md` TC-079（第 155 行）、TC-084（第 160 行）、TC-089（第 174 行）、TC-091（第 176 行） — 待改寫四條的實際原文。
  - `docs/specs/traceability.md` 全文 — NFR 追溯表八格現況（全為 `—`）、主表 13 列「狀態」欄現況（全為「…；待 TC」）、第 75 行的填寫責任說明。
- 上一輪交接摘要（若 round > 1）：不適用（round 1）。
- 本輪計畫（3–7 條）：
  1. 07 第 6.1 節 staging 列與第 4 章第 6 項、第 7 章 R-4 改為 Cloud Run 判準（冷啟 1–3 秒、revision 切流量回滾、保溫每 5 分鐘）。
  2. 20 的 TC-079（平台自動 301）、TC-084（Cloud Run revision 切流量回滾）、TC-089（Cloud Run 判讀依據）改寫。
  3. traceability NFR 追溯表「對應測試」欄八格依交接檔所列填實；主表 13 列「狀態」欄改「UC／模組／端點／TC 全欄已定稿（T-0019）」；其他欄一字不動。
  4. 依裁決 3 把 NFR-004 改為「最新版即可」：TC-091 備註與 07 第 7 章 R-5 同步。
  5. 全檔核對 07／20 是否殘留「30–50 秒」冷啟或 Render 字樣，列出行號；07、20 frontmatter `version` 進版、`updated` 更新。
  6. 實跑驗證：grep -n Render 07／20、重跑 T-0009 V1～V3、grep 狀態欄，真實輸出貼入 B 段。
  7. 任務卡 `status: review`、`updated` 更新；只 `git add` 三個 outputs、任務卡、本交接檔，於 main commit。
- 預期產出：
  - `docs/specs/07_測試計畫.md`（第 4 章第 6 項、6.1 節、R-4、R-5、frontmatter）
  - `docs/specs/20_測試案例.md`（TC-079、TC-084、TC-089、TC-091、frontmatter）
  - `docs/specs/traceability.md`（NFR 追溯表「對應測試」欄八格 ＋ 主表「狀態」欄 13 格 ＋ frontmatter `updated`）
  - 本交接檔 B 段

## B. 交接

- 完工時間／狀態：2026-09-19T07:49:36+08:00 / review
- 完成事項：
  - **`07_測試計畫.md`（4 處內容 ＋ frontmatter）**
    1. 第 4 章退出準則第 6 項（NFR-003）：判讀語句由「區分應用缺陷與 **Render Free 休眠**／平台事件」改為「區分應用缺陷與**平台事件**，判讀依據為 Cloud Run 內建指標（`Request count` 依回應碼分組、`Container startup latency`、`Container instance count`）與 revision 事件紀錄，以及該時段 GitHub 排程的取樣間隔是否異常拉長」（同步 `06` 第 6.3 節）。
    2. 第 6.1 節環境表 staging 列：環境名改「GCP Cloud Run ＋ Artifact Registry ＋ Neon（ADR-0005）」；存取方式由 `https://<service>.onrender.com` 改為 `*.run.app` 格式（`https://<service>-<hash>-<region-code>.a.run.app`，平台決定、無法事先寫死）；注意事項補上「測試腳本一律讀 `$STAGING_BASE_URL`，不得寫死網址」與平台特性四項（`min-instances = 0`、`asia-east1`、**冷啟 1–3 秒**、HTTP 平台 301、**回滾為 revision 切流量**），並把提供者改標為 T-0018。
    3. 第 7 章 R-4：由「Render Free 冷啟可能造成 NFR-003 誤報」改寫為「**Cloud Run 冷啟造成 NFR-003 的回應時間離群值**」——冷啟 1–3 秒已不足以讓 `curl` 逾時、基本上不再造成取樣失敗；保溫維持**每 5 分鐘**（Leader 對 T-0010 第 2 項的裁決）且性質降為「錦上添花」；NFR-001 暖身 10 秒的規定保留不動。
    4. 第 7 章 R-5：依 Leader 對 T-0009 的裁決第 3 項，標為**已解除**，改寫為「NFR-004 改讀為**最新版即可**，前一版不要求；qa-at 不需人工補測前一版，僅須記錄實際執行的三個瀏覽器版本號」。
    5. frontmatter：`version` 0.1 → **0.2**（附變更說明註解）、`updated` → `2026-09-19T07:45:48+08:00`。
  - **`20_測試案例.md`（4 條 TC ＋ frontmatter）**
    1. **TC-079**：改為「HTTP 由**平台自動 301** 導向 HTTPS（Cloud Run 平台行為，團隊零程式碼）」。步驟改用 `$STAGING_BASE_URL` 推導明文 HTTP 位址；預期結果由「`301`（或 3xx）」**收斂為固定 `301`**（依 ADR-0005 決定第 7 點：Google 前端以 301 導向），並註明「此判準驗的是平台設定未被改壞，不是應用程式碼」。
    2. **TC-084**：標題由「平台層回滾」改為「**Cloud Run revision 切流量回滾**（`06` 第 5.1 節首選路徑）」。前置條件補上「兩種失敗須分開演練」（啟動失敗由 startup probe 擋住不切流量／啟動成功但行為錯誤才走 5.1）；步驟新增 `gcloud run services describe ... --format="value(status.traffic)"` 確認流量分佈、以及重複 3 次 `curl -sSI` 確認無持續 5xx；預期結果新增「該 revision `percent` 為 `100`」「演練紀錄須含 revision 名稱與 commit SHA」「回滾操作本身 < 2 分鐘」，並註明無 `gcloud` 時可用 Cloud Console 的 Manage traffic 等效路徑（故 `gcloud` 非必要條件）。
    3. **TC-089**：預期結果中的「Render Free 休眠／平台事件」改為「**平台事件**」，判讀依據改列 Cloud Run 三個內建指標與 revision 事件紀錄；並明確寫下反向規則「**不得再以「休眠」解釋失敗**」（Cloud Run 無固定休眠門檻、冷啟 1–3 秒不足以逾時，只造成 `time_total` 離群值），另補「`Container instance count` 持續 > 0 表示保溫生效」。
    4. **TC-091**：備註依 Leader 裁決第 3 項改寫為「NFR-004 的『最新兩個版本』**改讀為最新版即可，前一版不要求**；自動化只跑執行當下可取得的版本，**qa-at 不需人工補測前一版**，僅須記錄三個瀏覽器實際版本號」，並附裁決時戳。判準本體（6 組全過、各一張截圖與版本號）不變。
    5. frontmatter：新增 `version: 0.2`（原檔無 `version` 欄，註解已說明 0.1 = T-0009 r1 初版）、`updated` → `2026-09-19T07:45:48+08:00`。
  - **`traceability.md`（8 ＋ 13 格 ＋ 3 句說明 ＋ frontmatter，共 26 行）**
    1. 「非功能需求追溯」表 `對應測試` 欄**八格全部填實**（依 Leader 裁決第 1 項、內容與 T-0009 交接檔所列一致）：NFR-001 → TC-085、TC-086；NFR-002 → TC-079、TC-087、TC-088、TC-110、TC-116；NFR-003 → TC-089、TC-090；NFR-004 → TC-091；NFR-005 → TC-092；NFR-006 → TC-093；NFR-007 → TC-094；NFR-008 → TC-095。已交叉核對與 `20_測試案例.md` 第 8 章「NFR → TC 覆蓋對照」完全一致（見 [V5]、[V7]）。
    2. 主表 13 列 `狀態` 欄由「UC／模組／端點已定稿（T-0005 r1）；待 TC」改為「**UC／模組／端點／TC 全欄已定稿（T-0019）**」（依 Leader 裁決第 2 項）。
    3. frontmatter `status` 註解與 `updated` 更新。
    4. 三句直接描述上述兩欄填寫狀態的說明文字同步（理由見「假設與決策」第 1 點）。
    5. **`UC-ID`／`SD 模組`／`API 端點`／`TC-ID`／`設計對策落點`／`驗證責任` 等其他欄位一字未動**（[V8] 逐行計數 26 增 26 減，恰為上述項目）。
- 產出物（路徑）：
  - `docs/specs/07_測試計畫.md`
  - `docs/specs/20_測試案例.md`
  - `docs/specs/traceability.md`
  - `tasks/T-0019-testplan-sync.md`（`status: review`、`updated`）
  - `worklog/handoff/20260919-0745-T0019-r1-qa-lead.md`（本檔）
- 未完成／已知問題：
  - **`traceability.md` 的 `設計對策落點` 欄仍殘留兩處 Render 時代敘述**：NFR-002 列寫「平台 HTTPS ＋ 301（**ADR-0003**）」（ADR-0003 已 superseded by ADR-0005）、NFR-003 列寫「**Render health check** 零停機輪替」（Cloud Run 的對應機制是 **startup probe ＋ revision 流量切換**）。該欄由 **plan-sd** 於 T-0004 填寫、且**不在本卡 outputs 授權範圍**（任務卡明載「其他欄不動」），故未改。**需 Leader 裁決是否併入 plan-sd 的修正 commit**（見「需要 Leader 裁決的事」第 1 點）。此為文字標註瑕疵，不影響任何 TC 判準（實際判準已在 07／20 同步為 Cloud Run）。
  - 07 第 4 章退出準則表與 20 的統計欄「實際值」仍全為待填，待 CR／AT／UAT 執行後於測試總結報告回填（T-0009 即如此，非本卡範圍）。
  - staging 尚未存在，`STAGING_BASE_URL` 尚未回填；TC-079／084／089 的實際執行仍須等 T-0018 完成。
- 我做的假設與決策（含理由）：
  1. **改了 `traceability.md` 的三句說明文字**（第 16 行「唯一未定稿欄位為 `TC-ID`」、第 17 行末「`TC-ID` 留空，由 qa-lead 於測試計畫卡填」、第 75 行「`對應測試` 欄…**留空由 qa-lead 於測試計畫卡填**」）。任務卡的「其他欄不動」指的是**表格欄位**；這三句是直接描述「我這次填的那兩欄尚未填」的敘述，不改會在同一份檔案裡留下兩個互相矛盾的事實來源（欄位說已填、說明說留空）。改動限於陳述該兩欄的填寫狀態與出處，**未新增任何需求或判準**。若 Leader 認為仍逾越，回退成本為 3 行。
  2. **TC-079 的預期結果從「`301`（或 3xx）」收斂為固定 `301`**。ADR-0005 決定第 7 點與 `06` 第 2 章都寫明「HTTP 請求由 Google 前端以 **301** 導向 HTTPS」，是平台的確定行為而非實作選擇。留著「或 3xx」等於讓一個已確定的值保有模糊空間，斷言會變弱。
  3. **TC-084 新增 `gcloud ... describe --format="value(status.traffic)"` 與「重複 3 次 `curl -sSI`」兩個步驟**，對應 `06` 第 5.1 節第 3、4 步。理由：Render 時代的「平台層回滾」只能驗結果，Cloud Run 的 revision 模型讓「流量真的切到哪一個 revision」可被直接查證；不查流量分佈就無法區分「回滾成功」與「切到另一個壞 revision」。同時保留「無 `gcloud` 時走 Cloud Console」的等效路徑，避免把 `gcloud` 變成 TC 的隱性前置條件（`06` 第 5.1 節明載 `gcloud` 非回滾必要條件）。
  4. **TC-089 寫入反向規則「不得再以『休眠』解釋失敗」**。Cloud Run 沒有固定休眠門檻，若不寫死，判讀者很可能沿用舊習慣把取樣失敗歸因於休眠，等於讓真正的應用缺陷被平台背鍋。此規則來自 `06` 第 6.3 節「冷啟已不足以讓 `curl` 逾時，基本上不再造成取樣失敗」。
  5. **07 的 R-4 不保留「Render 時代 30–50 秒」的對照文字**。原先寫了一句歷史對照，但任務卡 acceptance 第 1 條要求 `grep -c Render 07` 為 **0**（第 2 條才給 20 開「歷史備註須註明」的例外），故改為回指 `06` 第 6.3 節與 ADR-0005。歷史脈絡在 ADR-0003／ADR-0005 中完整保留，測試計畫不需要重述。
  6. **07 第 6.1 節補「測試腳本一律讀 `$STAGING_BASE_URL`，不得寫死網址」**。Cloud Run 的網址含平台產生的 hash、在首次部署前不存在，若容許寫死，所有 staging 側 TC 都會在部署後需要逐條改寫。此為 `06` 第 1 章回填義務的直接後果，非新增需求。
  7. **20 的 frontmatter 補 `version: 0.2`**（原檔無此欄）。任務卡要求「07、20 frontmatter version 進版」，20 無此欄故新增，並以註解標明 0.1 對應 T-0009 r1 初版，避免版號來歷不明。
  8. **`對應測試` 欄八格逐字照用 T-0009 交接檔所列內容**，未自行重新推導；填入後再以 `20` 第 8 章反向核對（[V7]），兩者完全一致。裁決第 1 項寫的是「內容依交接檔所列八格」，照辦即可，重新推導反而製造分歧風險。
  9. **主表狀態欄的文字逐字採用裁決／派工提示的「UC／模組／端點／TC 全欄已定稿（T-0019）」**，未加 `r1` 等後綴，13 列完全一致（[V4] 顯示唯一值 1 種、計數 13）。
- 驗證方式（接手者如何確認我做的是對的：指令 + 我實際得到的輸出）：

  ```bash
  # [A1] acceptance 第 1、2 條：07 與 20 的 Render 殘留
  grep -c "Render" docs/specs/07_測試計畫.md
  grep -c "Render" docs/specs/20_測試案例.md
  # [A1b] 順手核對：冷啟 30–50 秒、onrender 網址、Render Dashboard 回滾、render.yaml 的殘留
  grep -n "30–50\|30-50\|onrender\|Dashboard\|render.yaml" docs/specs/07_測試計畫.md docs/specs/20_測試案例.md
  # [A1c] Cloud Run 實際出現次數
  grep -c "Cloud Run" docs/specs/07_測試計畫.md docs/specs/20_測試案例.md
  ```

  ```text
  0                                   # 07：Render 殘留 0 處
  0                                   # 20：Render 殘留 0 處（無歷史備註需註明）
                                      # [A1b] 無輸出 = 無「30–50 秒」冷啟敘述、無 onrender 網址、無 Dashboard 回滾、無 render.yaml
  docs/specs/07_測試計畫.md:4         # [A1c]
  docs/specs/20_測試案例.md:4
  ```

  ```bash
  # [V1] 重跑 T-0009 的 V1：TC 總數、唯一性、重複、連號
  grep -c "^| TC-" docs/specs/20_測試案例.md
  grep -cE "^\| TC-[0-9]{3} " docs/specs/20_測試案例.md
  grep -oE "^\| TC-[0-9]{3} " docs/specs/20_測試案例.md | sort -u | wc -l
  grep -oE "^\| TC-[0-9]{3} " docs/specs/20_測試案例.md | sort | uniq -d | wc -l
  seq -f "TC-%03g" 1 123 | sort | comm -23 - <(grep -oE "^\| TC-[0-9]{3} " docs/specs/20_測試案例.md | grep -oE "TC-[0-9]{3}" | sort)
  ```

  ```text
  138        # 含 15 個表格的表頭列「| TC-ID |」（與 T-0009 相同）
  123        # 實際案例列 → TC 總數仍為 123
  123        # 唯一 TC-ID 數
  0          # 重複數
             # 缺號：無輸出 → 連號成立
  ```

  ```bash
  # [V2] 重跑 T-0009 的 V2：traceability 主表 TC 欄無「—」
  sed "s/[\]|/@PIPE@/g" docs/specs/traceability.md > /tmp/tr.md
  grep -E "^\| US-[0-9]{3} \| [^0-9]" /tmp/tr.md | awk -F "|" "{print \$2 \"=>\" \$8}"
  grep -E "^\| US-[0-9]{3} \| [^0-9]" /tmp/tr.md | awk -F "|" "{print \$8}" | grep -c "—"
  ```

  ```text
   US-001 => TC-001 ~ TC-011、TC-088、TC-096
   US-002 => TC-012 ~ TC-018、TC-093、TC-094、TC-097
   US-003 => TC-019 ~ TC-029、TC-098
   US-004 => TC-030 ~ TC-037、TC-099
   US-005 => TC-038 ~ TC-047、TC-100
   US-006 => TC-048 ~ TC-058、TC-101
   US-007 => TC-059 ~ TC-064、TC-102
   US-008 => TC-065 ~ TC-069、TC-091、TC-103
   US-009 => TC-070 ~ TC-077、TC-085 ~ TC-087、TC-092、TC-104
   US-010 => TC-078 ~ TC-084、TC-089、TC-090、TC-095、TC-105
   US-011 => TC-106 ~ TC-111（Gate 2 後）
   US-012 => TC-112 ~ TC-117（Gate 2 後）
   US-013 => TC-118 ~ TC-123（Gate 2 後）
  0          # 含「—」的列數 = 0（TC 欄未受本卡改動影響）
  ```

  ```bash
  # [V3] 重跑 T-0009 的 V3：TC 集合 diff（traceability 展開 vs 20_測試案例）
  grep -E "^\| US-[0-9]{3} \| [^0-9]" /tmp/tr.md | awk -F "|" "{print \$8}" \
    | sed "s/TC-\([0-9]*\) ~ TC-\([0-9]*\)/RANGE:\1:\2/g" | tr "、（）" "\n\n\n" \
    | grep -oE "(RANGE:[0-9]+:[0-9]+|TC-[0-9]{3})" \
    | awk -F ":" "{if (\$1==\"RANGE\") {for(i=\$2+0;i<=\$3+0;i++) printf \"TC-%03d\n\", i} else print \$0}" \
    | sort > /tmp/set_trace.txt
  grep -oE "^\| TC-[0-9]{3} " docs/specs/20_測試案例.md | grep -oE "TC-[0-9]{3}" | sort > /tmp/set_cases.txt
  wc -l < /tmp/set_trace.txt ; sort -u /tmp/set_trace.txt | wc -l ; wc -l < /tmp/set_cases.txt
  diff /tmp/set_trace.txt /tmp/set_cases.txt && echo "TC-SET-IDENTICAL-OK"
  ```

  ```text
  123
  123
  123
  TC-SET-IDENTICAL-OK     # diff 無輸出 → TC 集合與 traceability 一致（acceptance 第 5 條）
  ```

  ```bash
  # [V4] acceptance 第 3 條後半：主表狀態欄
  grep -E "^\| US-[0-9]{3} \| [^0-9]" /tmp/tr.md | awk -F "|" "{print \$9}" | sort | uniq -c
  grep -c "待 TC" docs/specs/traceability.md
  ```

  ```text
       13  UC／模組／端點／TC 全欄已定稿（T-0019）      # 13 列唯一值只有一種
  0                                                     # 「待 TC」殘留 0 處
  ```

  ```bash
  # [V5] acceptance 第 3 條前半：NFR 追溯表八格；並檢查引用的 TC 都真實存在
  grep -E "^\| NFR-[0-9]{3} " docs/specs/traceability.md | awk -F "|" "{print \$2 \"=>\" \$6}"
  grep -E "^\| NFR-[0-9]{3} " docs/specs/traceability.md | awk -F "|" "{print \$6}" | grep -c "—"
  grep -E "^\| NFR-[0-9]{3} " docs/specs/traceability.md | awk -F "|" "{print \$6}" \
    | grep -oE "TC-[0-9]{3}" | sort -u | comm -23 - /tmp/set_cases.txt
  ```

  ```text
   NFR-001 => TC-085、TC-086
   NFR-002 => TC-079、TC-087、TC-088、TC-110、TC-116
   NFR-003 => TC-089、TC-090
   NFR-004 => TC-091
   NFR-005 => TC-092
   NFR-006 => TC-093
   NFR-007 => TC-094
   NFR-008 => TC-095
  0          # 仍為「—」的格數 = 0（八格全部填實）
             # 無輸出 → 八格引用的 TC 全部存在於 20_測試案例.md，無幽靈 TC
  ```

  ```bash
  # [V6] 回歸：20 的 TC 列欄位數仍為 9 欄（awk NF=11），確認改寫未破壞表格結構
  sed "s/[\]|/@PIPE@/g" docs/specs/20_測試案例.md > /tmp/tc.md
  grep -E "^\| TC-[0-9]{3} " /tmp/tc.md | awk -F "|" "{if (NF!=11) print \$2 \" NF=\" NF}"
  ```

  ```text
             # 無輸出 → 123 列皆為完整 9 欄（TC-079／084／089／091 改寫後結構完好）
  ```

  ```bash
  # [V7] 交叉一致性：traceability 八格 vs 20 第 8 章 NFR → TC 對照
  sed -n "/^## 8\./,/^## 9\./p" docs/specs/20_測試案例.md | grep -E "^\| NFR-"
  ```

  ```text
  | NFR-ID | 類別 | 對應 TC | 可量測的門檻 |
  | NFR-001 | 效能 | TC-085、TC-086 | P95 < 500 ms（清單）／< 800 ms（寫入） |
  | NFR-002 | 安全 | TC-079（①）、TC-087（②）、TC-110（③，P1）、TC-116（④，P1）、TC-088（⑤） | 301 導向；5 種錯誤 grep 無輸出；bcrypt cost ≥ 10；五端點 401；零彈窗 ＋ `grep innerHTML` 無輸出 |
  | NFR-003 | 可用性 | TC-089、TC-090 | 24 小時採樣成功率 ≥ 99%；部署不可用 < 60 秒 |
  | NFR-004 | 相容性 | TC-091 | 3 瀏覽器 × 2 尺寸 = 6 組全過 |
  | NFR-005 | 可維運性 | TC-092 | 4xx 與 5xx 回應同形；以 `requestId` grep 到含五項的日誌 |
  | NFR-006 | 資料持久性 | TC-093 | 重新部署後 10/10 一致 |
  | NFR-007 | 容量 | TC-094 | 500 筆下 API < 2 秒、前端可互動 < 3 秒 |
  | NFR-008 | 可安裝性 | TC-095 | 總耗時 ≤ 15 分鐘且卡關點 = 0 |
  # 八格與此表逐列相同（NFR-002 的 TC 集合亦相同，僅本表另標①~⑤）
  ```

  ```bash
  # [V8] 只改了該改的：變更範圍
  git diff --stat docs/specs/07_測試計畫.md docs/specs/20_測試案例.md docs/specs/traceability.md
  git diff -U0 docs/specs/traceability.md | grep -cE "^\+[^+]"
  git diff -U0 docs/specs/traceability.md | grep -cE "^-[^-]"
  ```

  ```text
   docs/specs/07_測試計畫.md  | 12 +++++------
   docs/specs/20_測試案例.md  | 11 +++++-----
   docs/specs/traceability.md | 52 +++++++++++++++++++++++-----------------------
   3 files changed, 38 insertions(+), 37 deletions(-)
  26
  26
  # traceability 的 26 行 = 主表 13 列狀態欄 + NFR 表 8 格 + frontmatter 2 行 + 說明文字 3 句。其他欄位未動。
  ```

  **逐條核對任務卡 acceptance**：

  | # | acceptance | 結果 | 證據 |
  |---|---|---|---|
  | 1 | 07 第 6.1 節環境表與第 7 章 R-4 改為 Cloud Run（冷啟 1–3 秒、revision 回滾）；`grep -c Render 07` 為 0 | ✅ | 6.1 staging 列已改 Cloud Run ＋ `*.run.app` ＋ 冷啟 1–3 秒 ＋ revision 切流量；R-4 已改寫；[A1] = **0** |
  | 2 | TC-079、TC-084、TC-089 改寫為 Cloud Run 對應判準；`grep -c Render 20` 為 0（歷史備註除外須註明） | ✅ | TC-079 平台自動 301（收斂為固定 301）、TC-084 revision 切流量並驗流量分佈、TC-089 判讀依據改 Cloud Run 指標；[A1] = **0**，**無歷史備註需註明** |
  | 3 | traceability NFR 追溯表八格填實；主表狀態欄改「UC／模組／端點／TC 全欄已定稿（T-0019）」；其他欄不動 | ✅ | [V5]（八格填實、`—` 計數 0、無幽靈 TC）；[V4]（13 列唯一值 1 種、「待 TC」殘留 0）；[V8]（26 行變更全部可歸屬） |
  | 4 | NFR-004 依裁決 3 改寫 TC-091 備註與 07 R-5 為「最新版即可」 | ✅ | TC-091 備註已改為「最新版即可、前一版不要求、qa-at 不需人工補測」；07 R-5 標示裁決已解除並同步改寫 |
  | 5 | TC 總數仍 123、連號、TC 集合與 traceability 一致（重跑 T-0009 的 V1～V3） | ✅ | [V1] 123／123／0／缺號無輸出；[V2] `—` = 0；[V3] `TC-SET-IDENTICAL-OK`；另 [V6] 欄位結構未破壞 |

- 下一步建議：
  1. **Leader 裁決是否併修 `traceability.md` 的 `設計對策落點` 欄兩處 Render 殘留**（NFR-002 的「ADR-0003」、NFR-003 的「Render health check 零停機輪替」）。該欄屬 plan-sd，建議併入 plan-sd 於 T-0010 的修正 commit，或另開一張極小的文字同步卡；本角色不越界改。
  2. 提醒 **dev-ops（T-0018）** 三件事，否則 TC-079／084／089／090 無法判定：① 首次部署後回填 `STAGING_BASE_URL`（`06` 第 1 章的三處回填義務）；② 依 `06` 第 5.4 節完成回滾演練並回填日期與結果，**兩種失敗要分開演練**；③ Artifact Registry 清理政策**保留數不得少於 3**，否則 TC-084 的 revision 回滾路徑會因舊映像被清理而不可用。
  3. **排程提醒不變**：NFR-003 需連續 24 小時採樣，staging 部署完成日與 Gate 2 判定日之間至少隔 24 小時。
  4. **提醒 qa-at**：NFR-004 已改為「最新版即可」，執行 TC-091 時**不必**安排前一版的人工補測，但**必須**在測試報告中記錄三個瀏覽器的實際版本號（這是新的唯一證據要求）。
  5. T-0009 交接檔中的裁決事項第 5 點（本卡的來源）已全數處理完畢；其餘六點皆已由 Leader 於「對測試計畫（T-0009）7 項的裁決」結案，無遺留。
- 需要 Leader 裁決的事：
  1. **`traceability.md` 的 `設計對策落點` 欄是否併修**（詳見「未完成／已知問題」第 1 點與「下一步建議」第 1 點）。該欄由 plan-sd 於 T-0004 填寫，本卡 outputs 明載「其他欄不動」，故從嚴不動。內容若要改，建議為：NFR-002 的「（ADR-0003）」改為「（ADR-0005）」；NFR-003 的「Render health check 零停機輪替」改為「Cloud Run startup probe（`/health`）＋ revision 流量切換，零停機」。
  2. **本卡是否接受「改動 `traceability.md` 三句說明文字」**（「假設與決策」第 1 點）。若 Leader 判定逾越「其他欄不動」，本角色可於 r2 回退該 3 行，其餘產出不受影響。
