# T-0011 r1 — dev-tl — 工作紀錄與交接（審核）

<!-- 檔名：worklog/handoff/20260919-0817-T0011-r1-dev-tl.md -->

## A. 開工紀錄

- 開工時間：2026-09-19T08:14:15+08:00
- 角色／模型：dev-tl / Claude Opus 5（1M context）
- 任務卡：tasks/T-0011-project-skeleton.md（round 1）— **審核 T-0011**（依 `.claude/skills/review-round/SKILL.md`）
- 審核對象：分支 `task/T-0011-project-skeleton`，commit `dcd6def`，worktree `C:/Users/excal/Projects/Claude/soft-dev-team-260919-wt/T-0011`（所有驗證在該 worktree 內執行，根目錄全程停在 main）
- 讀取的輸入（實際讀過的檔案與段落）：
  - `.claude/agents/dev-tl.md` 全文 — 本角色審核他人的規約：實際 checkout、實際跑 build 與測試、輸出貼審核交接檔；退回須列「具體要改哪裡」
  - `.claude/skills/review-round/SKILL.md` — 一輪審核的六個步驟與判定規則（不採信交接檔的「通過」二字）
  - `tasks/T-0011-project-skeleton.md`（根目錄版，`status: in_progress`）— acceptance 六條、驗收方式、outputs 15 個路徑
  - `worklog/handoff/20260919-0744-T0011-r1-dev-ops.md`（worktree 版）B 段全文 — 完成事項、已知問題 6 點、假設與決策 6 條、驗證輸出、需 Leader 裁決 3 項
  - `docs/specs/10_開發計畫_WBS.md` §0（硬前提 1–8）、§1.1（WI-01 全文含驗收方式草案 11 條指令）、§3.3（CI 本地等價指令對照）、§5（共用檔案規約與 app.ts 錨點骨架）
  - `docs/specs/adr/ADR-0001` §「版本釘選」— 釘選定義為 Node `22-alpine`、TypeScript `~5.6`、Fastify `^5`（即 `^`／`~` range ＋ lock 檔，非 exact version）
  - `tasks/E-001-todo-app.md` 最後一段「2026-09-19T08:13:18+08:00 — 對 T-0011 三項的裁決」①②③，以及 D-01／D-02／D-03
- 上一輪交接摘要：無（r1 首審）
- 本輪計畫：
  1. 在 worktree 內重跑 WBS §1.1 全部驗收指令（Docker 兩條依 Leader 裁決 ② 跳過）
  2. 獨立驗證 dev-ops 三項「需裁決」主張是否屬實（Docker daemon、`node --test` 目錄寫法、`COPY public`）
  3. 逐檔審查 package.json／config.ts／app.ts／Dockerfile／ci.yml／.env.example／README
  4. 對交接檔「假設與決策」六條逐條表態
  5. 判定 done／rework，寫本檔 B 段
- 預期產出：本審核交接檔；若通過則合併、CHANGELOG、WBS §1.1／§3.3 修正（Leader 裁決 ③）

## B. 交接

- 完工時間／狀態：2026-09-19T08:22:00+08:00 / done（**初審通過，已合併 main**）
- 完成事項：
  - 在 worktree `C:/Users/excal/Projects/Claude/soft-dev-team-260919-wt/T-0011` 內實際重跑 WBS §1.1 驗收指令共 9 條（Docker 2 條依 Leader 裁決 ② 跳過，理由見下），真實輸出見「驗證方式」。
  - 額外做了 3 條 dev-ops 未做、但屬 acceptance 要求的驗證：① `config.ts` 缺值時啟動失敗的**實測**（acceptance 第 5 條隱含、WBS §1.1「缺必要項即啟動失敗並印出缺哪一個」）；② 獨立重現 `node --test tests/integration/`（目錄寫法）失敗以核實 Leader 裁決 ③ 的前提；③ 不受引號規避影響的補強憑證掃描。
  - 逐條判定 acceptance 六條：**全部通過**（見下）。
  - 對 dev-ops 交接檔「假設與決策」六條逐條表態：**六條全部接受**（見下）。
  - 合併：根目錄 main 執行 `git merge --no-ff task/T-0011-project-skeleton`；移除 worktree 與分支；更新 `CHANGELOG.md`（新建，依 `docs/templates/dev_13_CHANGELOG.md`）；依 Leader 裁決 ③ 修正 `docs/specs/10_開發計畫_WBS.md` §1.1 與 §3.3 的 `node --test` 寫法為 glob（version 0.2 → 0.3，變更紀錄一行）。
- 產出物（路徑）：
  - 本審核交接檔 `worklog/handoff/20260919-0817-T0011-r1-dev-tl.md`
  - `tasks/T-0011-project-skeleton.md`（`status: done`、`updated` 更新、審核紀錄表加一列）
  - `CHANGELOG.md`（新建）
  - `docs/specs/10_開發計畫_WBS.md`（§1.1、§3.3 的 `node --test` 寫法；version 0.3）

### acceptance 逐條判定

| # | acceptance | 判定 | 證據 |
|---|---|---|---|
| 1 | WBS §1.1 驗收指令每條實際執行且結果符合 | **通過（Docker 2 條依 Leader ② 免驗）** | 下方「驗證方式」9 條真實輸出；`docker info` 20 秒逾時（exit 124）佐證 daemon 確實不可用 |
| 2 | `npm run lint` 與相關測試全綠；不改 outputs 以外的檔案 | **通過** | lint exit 0；integration 2 pass／0 fail；unit 0 tests exit 0；`git diff main...HEAD --stat` 僅 14 個 outputs ＋ 任務卡 ＋ 交接檔（`.gitignore` 為既有檔且內容已符合，未修改，合理） |
| 3 | `src/app.ts` 含五錨點且順序固定 | **通過** | `grep -cE` 得 `5`；`grep -nE` 顯示順序為 error-handler(32) → auth(33) → healthRoutes(34) → static(35) → routes(36) → p1-auth-routes(37)，與 WBS §5 骨架逐字一致 |
| 4 | 倉庫不含任何憑證值 | **通過** | 官方掃描無輸出（exit 1）；另以不受引號影響的補強樣式重掃，命中僅 `.env.example` 的 `dev`（D-01 明文允許）與 `ci.yml` 的 CI service container 佔位值 `ci`，皆為明顯非真實憑證。見下方「小瑕疵 2」 |
| 5 | README 同時給 Git Bash 與 PowerShell 寫法，並明載 `type` 與 `module` 決定 | **通過** | README「技術決定」節載明 `type: module`（ESM）、`module`/`moduleResolution: NodeNext`，與 D-03、WI-04／WI-07 的 `*.test.mjs` 一致；「快速開始」兩種方式各有 Git Bash 與 PowerShell 兩段。見下方「小瑕疵 1」（方式二逐字照做會失敗，非阻擋級） |
| 6 | Dockerfile 為 Cloud Run 形狀 | **通過（靜態審查；實機 build 依 Leader ② 延後）** | `node:22-alpine` 多階段；`addgroup/adduser` ＋ `USER appuser` 非 root；`EXPOSE 8080` 僅文件用途、實際埠讀 `$PORT`（`src/config.ts` → `src/server.ts`，無任何寫死埠號）；全檔無 Render 專屬設定、無憑證值 |

### 對 dev-ops 交接檔「假設與決策」六條的表態

| # | 假設／決策 | 表態 | 理由 |
|---|---|---|---|
| 1 | `node --test` 改用 glob 取代 WBS 原文的目錄寫法 | **接受** | 我以同一台機器獨立重現：目錄寫法 exit 1（`test at tests\integration:1:1 ✖`），glob 寫法 2 pass exit 0。Leader 裁決 ③ 已認可；本次已同步修正 WBS §1.1 與 §3.3 文字 |
| 2 | pino `redact: ["req.headers.authorization"]` 由本卡在 `app.ts` 建構參數先寫入 | **接受** | `redact` 確實只能在 Fastify 建構時給定，WI-02 的 `setErrorHandler` 無插入點；此寫法不佔用任何錨點、不改動錨點順序，不影響 WI-02 的職責（錯誤序列化仍歸 WI-02）。WBS §1.2 仍要求 WI-02 檢查 `grep -n "redact" src/plugins/error-handler.ts` —— 見「下一步建議」2 |
| 3 | Dockerfile 刻意不 `COPY public ./public` | **接受** | 與 Leader 裁決 ① 完全一致（該行由 T-0014 補，T-0014 outputs 已追加 Dockerfile 僅該行）。若現在加入，`docker build` 會因來源目錄不存在而失敗 |
| 4 | `.env.example` 增加 `POSTGRES_USER`／`POSTGRES_PASSWORD`／`POSTGRES_DB` 三個 compose-only 變數 | **接受** | 官方 `postgres:16-alpine` 映像初始化確實需要這三個獨立變數；三者不進入 `src/config.ts` 的 P0 七項集合（已確認 `config.ts` 只讀七項），不擴散到應用程式契約；值為 D-01 允許的明顯佔位值，且與 `DATABASE_URL` 內容一致 |
| 5 | 憑證掃描兩處假陽性的消除手法（`config.ts` 改 tuple 回傳；`ci.yml` 佔位值加雙引號） | **接受，但附但書** | `config.ts` 改寫後可讀性未下降、型別安全不變（`tsc --noEmit` exit 0），可接受。`ci.yml` 加雙引號使掃描不再命中——**我以不受引號影響的樣式重掃並人工確認該三處確實只是 CI 用完即丟的 `ci`／`ci`，非真實憑證**，因此接受；但這代表 WBS §1.1 的掃描樣式可被引號規避，屬掃描規則的弱點而非本卡缺陷，見「下一步建議」3 |
| 6 | `tests/integration/health.test.ts` 匯入 `dist/` 而非 `src/`（需先 build） | **接受** | `rewriteRelativeImportExtensions` 確為 TS 5.7 能力，本專案依硬前提 7 釘選 `~5.6`，此限制客觀存在。`ci.yml` 的 integration job 已先 `npm run build` 再測，順序正確；README 已載明。屬後續卡須知，見「下一步建議」1 |

- 未完成／已知問題（皆非阻擋級，不退回）：
  1. **小瑕疵 1（README 方式二逐字照做會失敗）**：README「方式二：本機 Node」寫 `cp .env.example .env` → `npm ci` → `npm run build` → `npm start`，但 **Node 不會自動載入 `.env`**（需 `--env-file=.env`，或 shell 先 export）。我逐字照做的實測結果是啟動失敗並列出缺六個環境變數（輸出見下）。應用程式本身無誤——我以 `set -a && . ./.env && set +a` 載入環境變數後啟動，`/health` 回 200 與 `{"status":"ok"}`。此項不在阻擋清單（build／測試／憑證／錨點／config 驗證）內，故不退回，但**它直接衝擊 README 的 DoD「新 session 15 分鐘內跑起來」（qa-at TC-095 會實測）**，且在本機 Docker 不可用期間方式二是唯一可跑路徑。修法二選一（皆只改 README／package.json 一行）：`npm start` 改為 `node --env-file=.env dist/server.js`（並同步 `dev`／`migrate`），或 README 方式二補上 Git Bash `set -a; . ./.env; set +a` ／ PowerShell `Get-Content .env | ForEach-Object { ... }` 的載入步驟。見「需要 Leader 裁決的事」1。
  2. **小瑕疵 2（`ci.yml` 佔位值落在 D-01 文字之外）**：D-01 只寫「`.env.example` 可含本機 compose 佔位值」，未涵蓋 `ci.yml` 的 CI service container 佔位值。實質上兩者性質相同（用完即丟、非真實憑證），本次以此判準通過；建議 Leader 在裁決紀錄補一句把 CI 佔位值一併納入 D-01，免得後續卡與 qa-cr 重複爭議。
  3. **小瑕疵 3（dev-ops 留下常駐程序）**：審核時發現 port 8080 仍被 dev-ops 上一輪 `npm start &` 遺留的 node 程序（PID 32324）占用，導致第一次 curl 打到的是舊程序。我已 `Stop-Process` 終止該程序後以全新程序（PID 20100）重驗，驗畢亦已終止，port 8080 現為 free。**提醒所有開發卡：背景啟動的服務在收尾前必須結束。**
  4. `ci.yml` 的 `integration` job 目前執行必失敗（`npm run migrate` 的 `src/db/migrate.ts` 屬 WI-03／T-0013）。符合 WBS §3.3「建立者：WI-01（骨架）／WI-03（migrate）」的設計，非缺陷；T-0013 合併後才會轉綠。
  5. `docker build`／`docker compose up` 兩條未實測：我以 `timeout 20 docker info` 獨立確認 daemon 不可連線（exit 124 逾時），與 dev-ops 陳述相符。依 Leader 裁決 ② 不阻擋合併，延後到 CI（使用者提供 GitHub repo 後）與 T-0018。
  6. `ci.yml`／README 皆未經真實 GitHub Actions 驗證（本 repo 無遠端，WBS R-02）。
- 我做的假設與決策（含理由）：
  - **以「Leader 明列的阻擋級清單」為退回門檻**（build 失敗、測試紅、憑證入庫、錨點錯、config 缺驗證），五項皆未發生，故判 done。README 方式二的啟動缺陷雖為實質瑕疵，仍依「小瑕疵不退回」處置，轉為批次 2 的注意事項與 Leader 裁決項。
  - **`config.ts` 的 `CORS_ALLOWED_ORIGINS` 不列入 `REQUIRED_VARS` 屬合規**：acceptance／WBS 要求的是「一次宣告並驗證 P0 全部環境變數，**缺必要項**即啟動失敗」。該變數已被宣告、讀取、解析（含「永不得為 `*`」的防呆），留空是 O-004（同源部署）的正確預設，不是漏驗。
  - **版本釘選判準採 ADR-0001 原文**（Node `22-alpine`、TS `~5.6`、Fastify `^5` ＋ `package-lock.json`），故 `^`／`~` range 視為已釘選，不要求 exact version。`npm ci` 以 lock 檔還原 183 套件、0 弱點，可重現性成立。
  - **`.gitignore` 雖列於 outputs 但未被修改，視為通過**：該檔既有內容已含 `.env`、`.env.*`、`!.env.example`、`dist/`、`node_modules/`，無需變更；acceptance 要求的是「`grep -n "^\.env$" .gitignore` 有輸出」，已滿足。
- 驗證方式（接手者如何確認我做的是對的：指令 + 我實際得到的輸出）：

  全部指令在 `C:/Users/excal/Projects/Claude/soft-dev-team-260919-wt/T-0011`（分支 `task/T-0011-project-skeleton`，commit `dcd6def`）內執行。

  ```bash
  git log --oneline -3 && git status --short && git diff main...HEAD --stat
  ```
  ```text
  dcd6def T-0011: 專案骨架＋CI＋本機可跑 /health＋README 初版
  885c39b E-001: T-0010 審核 done；派工 T-0011/T-0019
  f13e177 T-0010: 雲端平台改 GCP Cloud Run（保留 Neon Free）
  （git status --short 無輸出＝工作樹乾淨）
   .env.example                                      |   36 +
   .github/workflows/ci.yml                          |   96 +
   Dockerfile                                        |   47 +
   README.md                                         |  116 +
   docker-compose.yml                                |   44 +
   eslint.config.js                                  |   23 +
   package-lock.json                                 | 2427 +++++++++++++++++++++
   package.json                                      |   33 +
   src/app.ts                                        |   40 +
   src/config.ts                                     |  105 +
   src/routes/health.ts                              |   15 +
   src/server.ts                                     |   40 +
   tasks/T-0011-project-skeleton.md                  |    4 +-
   tests/integration/health.test.ts                  |   50 +
   tsconfig.json                                     |   21 +
   worklog/handoff/20260919-0744-T0011-r1-dev-ops.md |  167 ++
   16 files changed, 3262 insertions(+), 2 deletions(-)
  ```

  ```bash
  npm ci && npm run lint
  ```
  ```text
  added 183 packages, and audited 184 packages in 3s
  57 packages are looking for funding
  found 0 vulnerabilities

  > todo-app@0.1.0 lint
  > eslint . && tsc --noEmit -p tsconfig.json

  LINT_EXIT:0
  ```

  ```bash
  rm -rf dist && npm run build && ls dist/server.js
  ```
  ```text
  > todo-app@0.1.0 build
  > tsc -p tsconfig.json

  BUILD_EXIT:0
  dist/server.js
  ```

  ```bash
  npx --yes @redocly/cli lint "docs/specs/04_API規格.yaml"
  ```
  ```text
  docs\specs\04_API規格.yaml: validated in 54ms
  Woohoo! Your API description is valid. 🎉
  You have 1 warning.      （operation-4xx-response，/health 僅 200 本就無 4xx，非本卡缺陷）
  REDOCLY_EXIT:0
  ```

  ```bash
  # Leader 裁決 ② 之前提的獨立佐證：daemon 確實不可用
  timeout 20 docker info
  ```
  ```text
  EXIT:124      （20 秒逾時，無任何輸出；docker build / docker compose up 兩條依裁決 ② 跳過）
  ```

  ```bash
  node --version
  node --test tests/integration/                       # WBS 原文（目錄）寫法
  node --test "tests/integration/**/*.test.ts"         # dev-ops 的 glob 寫法
  ```
  ```text
  v24.15.0

  （目錄寫法）
  ✖ failing tests:
  test at tests\integration:1:1
  ✖ tests\integration (53.9182ms)
    'test failed'
  EXIT_A:1

  （glob 寫法）
  ✔ GET /health 回 200 與固定的 {status:ok} (118.6645ms)
  ✔ GET /health 帶查詢字串仍回 200（pathname 比對，非 req.url 比對） (2.6054ms)
  ℹ tests 2
  ℹ pass 2
  ℹ fail 0
  EXIT_B:0
  ```

  ```bash
  npm run test:unit
  ```
  ```text
  ℹ tests 0
  ℹ pass 0
  ℹ fail 0
  UNIT_EXIT:0        （P0 單元測試由 WI-02 起陸續加入，符合本卡範圍）
  ```

  ```bash
  # config.ts 缺值即啟動失敗（acceptance 隱含條件，dev-ops 未實測，我補驗）
  rm -f .env
  env -u NODE_ENV -u PORT -u DATABASE_URL -u BASIC_AUTH_USER -u BASIC_AUTH_PASSWORD -u LOG_LEVEL node dist/server.js
  ```
  ```text
  Error: 缺少必要環境變數：NODE_ENV, PORT, DATABASE_URL, BASIC_AUTH_USER, BASIC_AUTH_PASSWORD, LOG_LEVEL（請對照 .env.example 補齊，複製為 .env 後重試）
      at loadConfig (.../dist/config.js:69:15)
  EXIT:1             （七項中六項必要變數全數點名，訊息可據以自行修復）
  ```

  ```bash
  # README「方式二」逐字照做 —— 失敗（小瑕疵 1）
  cp .env.example .env
  env -u NODE_ENV -u PORT -u DATABASE_URL -u BASIC_AUTH_USER -u BASIC_AUTH_PASSWORD -u LOG_LEVEL npm start
  ```
  ```text
  > todo-app@0.1.0 start
  > node dist/server.js

  Error: 缺少必要環境變數：NODE_ENV, PORT, DATABASE_URL, BASIC_AUTH_USER, BASIC_AUTH_PASSWORD, LOG_LEVEL（...）
  EXIT:1             （Node 不會自動讀 .env；需 --env-file=.env 或 shell 先載入）
  ```

  ```bash
  # 正確載入環境變數後啟動並驗 /health（先確認 8080 未被占用，見「已知問題 3」）
  set -a && . ./.env && set +a && node dist/server.js &
  curl -sS -o /dev/null -w "%{http_code}\n" http://localhost:8080/health
  curl -sS http://localhost:8080/health
  ```
  ```text
  {"level":30,"time":1789776989770,"pid":20100,"hostname":"AIFAN-26FF501","msg":"Server listening at http://127.0.0.1:8080"}
  {"level":30,...,"msg":"Server listening at http://172.30.32.1:8080"}
  {"level":30,...,"msg":"Server listening at http://192.168.1.103:8080"}
  200
  {"status":"ok"}
  （驗畢已 Stop-Process 終止 PID 20100，netstat 確認 PORT_8080_FREE）
  ```

  ```bash
  rm -f .env
  grep -rnE "(PASSWORD|SECRET|DATABASE_URL)[[:space:]]*[:=][[:space:]]*[A-Za-z0-9]" . --include=Dockerfile --include=docker-compose.yml --include=*.json --include=*.ts --include=*.yml
  grep -n "^\.env$" .gitignore
  grep -cE "ANCHOR:(error-handler|auth|static|routes|p1-auth-routes)" src/app.ts
  ```
  ```text
  （憑證掃描：無輸出）
  SCAN_EXIT:1
  15:.env
  GITIGNORE_EXIT:0
  5
  ```

  ```bash
  # 補強掃描：改用不受引號規避影響的樣式，排除 node_modules/dist
  grep -rnEi "(password|secret|database_url|api[_-]?key|token)[[:space:]]*[:=][[:space:]]*[\"']?[A-Za-z0-9/+_.-]{2,}" --include=Dockerfile --include=docker-compose.yml --include=*.yml --include=*.ts --include=*.js --include=.env.example . --exclude-dir=node_modules --exclude-dir=dist --exclude-dir=.git
  ```
  ```text
  ./.env.example:21:DATABASE_URL=postgres://dev:dev@db:5432/todo      ← D-01 允許的本機佔位值
  ./.env.example:23:POSTGRES_PASSWORD=dev                             ← 同上
  ./.env.example:29:BASIC_AUTH_PASSWORD=dev                           ← 同上
  ./.github/workflows/ci.yml:66:          POSTGRES_PASSWORD: "ci"     ← CI service container 用完即丟
  ./.github/workflows/ci.yml:78:      DATABASE_URL: "postgres://ci:ci@localhost:5432/todo"
  ./.github/workflows/ci.yml:80:      BASIC_AUTH_PASSWORD: "ci"
  ./src/config.ts:16:  basicAuthPassword: string;                     ← 型別宣告，非值
  （人工逐條確認：無任何真實憑證）
  ```

  ```bash
  # 錨點順序（不只計數）
  grep -nE "ANCHOR:|app.register" src/app.ts
  ```
  ```text
  32:  // [ANCHOR:error-handler]   ← WI-02 於此註冊 BE-03（必須最先）
  33:  // [ANCHOR:auth]            ← WI-05 於此註冊 BE-02 全域 hook（必須早於 static 與 routes）
  34:  app.register(healthRoutes); // WI-01 註冊 BE-07
  35:  // [ANCHOR:static]          ← WI-05 於此註冊 BE-09
  36:  // [ANCHOR:routes]          ← WI-06 於此註冊 BE-04
  37:  // [ANCHOR:p1-auth-routes]  ← P1：BE-10；並由 BE-13 取代 [ANCHOR:auth]
  （與 WBS §5 骨架逐字一致，順序正確）
  ```

  合併後於根目錄（main）：

  ```text
  （合併、worktree 移除、分支刪除、CHANGELOG／WBS 更新的實際輸出見合併 commit；
    main 新 HEAD 見 CHANGELOG.md 與 tasks/T-0011 審核紀錄表）
  ```

- 下一步建議（給批次 2：T-0012／T-0013／T-0014）：
  1. **跑整合測試前一定要先 `npm run build`**（測試匯入 `dist/`，非 `src/`）。T-0012 的 `tests/integration/error-contract.test.ts`、T-0013 的 `tests/integration/todo-repository.test.ts` 請沿用 `health.test.ts` 的寫法：匯入 `../../dist/...`、檔頭以 `process.env.X ??=` 給測試預設值，並用 `app.inject()` 而非真實 TCP 監聽。
  2. **T-0012 不必再設 pino `redact`**：它已在 `src/app.ts` 的 Fastify 建構參數內（`redact` 只能在建構時給）。WBS §1.2 的驗收指令 `grep -n "redact" src/plugins/error-handler.ts` 會無輸出——請審核 T-0012 時改查 `src/app.ts`，或由 T-0012 在 error-handler 內加註解指向 app.ts。**此點已知，不另開規格變更卡**。
  3. **憑證掃描樣式可被引號規避**（`KEY: "value"` 不會命中）。建議 qa-cr 的 CR 檢查表改用本檔「補強掃描」的樣式（含可選引號），並請 Leader 把「CI service container 佔位值」一併納入 D-01 文字。
  4. **T-0014 記得補 `Dockerfile` 的 `COPY public ./public`**（builder 與 runtime 兩階段皆要，Leader 裁決 ①，T-0014 outputs 已含 Dockerfile 僅該行）。加完後 `docker build` 才不會漏打包前端。
  5. **背景啟動的服務收尾前務必結束**（本輪發現 port 8080 被上一輪遺留程序占用）。驗完 `/health` 請用 `netstat -ano | grep ":8080"` ＋ `Stop-Process -Id <pid> -Force` 收乾淨。
  6. `package.json` 已定案，**後續卡一律不得改**；需要新套件先向 dev-tl 提出。`src/config.ts` 同樣鎖定，新環境變數走規格變更請求。
- 需要 Leader 裁決的事：
  1. **README「方式二：本機 Node」逐字照做無法啟動**（Node 不自動載入 `.env`，實測輸出見上）。不屬阻擋級故已放行合併，但直接影響 README DoD「新 session 15 分鐘內跑起來」（qa-at TC-095 實測），且本機 Docker 不可用期間方式二是唯一可跑路徑。請裁決修法與歸屬：(a) 開一張極小修正卡改 `package.json` 的 `start`／`dev` 為 `node --env-file=.env ...`（但 Cloud Run 上無 `.env`，需改用 `--env-file-if-exists`）；(b) 只改 README 補上載入環境變數的兩種 shell 寫法；(c) 併入 T-0018（README 部署章節）一起處理。**我的建議：(b)，且併入最近一張會動到 README 的卡，不另開卡。**
  2. **D-01 是否擴及 CI 佔位值**（`ci.yml` 的 `ci`／`ci`）：本次以「性質相同」判準放行，建議在裁決紀錄補一句定案，避免 qa-cr 重複爭議。
