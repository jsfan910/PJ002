# T-0028 r1 — dev-tl — 技術初審與合併・工作紀錄與交接

<!-- 檔名：worklog/handoff/20260919-1733-T0028-r1-dev-tl.md（時間 = 開工時間，取實查系統時間） -->
<!-- 本檔為「審核者交接檔」：dev-tl 對 qa-at 之 T-0028 做技術初審並執行合併；內容品質由 qa-lead 於 T-0030 續審。 -->

## A. 開工紀錄

- 開工時間：2026-09-19T17:33:22+08:00
- 角色／模型：dev-tl / Claude Opus 5
- 任務卡：tasks/T-0028-at-staging.md（round 1，status review，reviewer qa-lead）
- 讀取的輸入（實際讀過的檔案與段落，非任務卡照抄）：
  - `.claude/agents/dev-tl.md` — 初審判準（能 build、測試通過、無硬編碼密鑰）與「實際 checkout、實際跑、把指令與輸出貼進審核交接檔」的要求。
  - `tasks/T-0028-at-staging.md`（frontmatter + 驗收方式 + 審核紀錄）— 確認 status=review、reviewer=qa-lead、outputs 僅限 `docs/reports/*` 與 `tests/**` 小修。
  - `worklog/handoff/20260919-1642-T0028-r1-qa-at.md`（B 段全文）— 取得 e2e config 小修的判斷依據（最小重現腳本排除 QUIC／HTTP2／reload）、憑證處理方式、四項已知問題與兩項需裁決事項。
  - `git diff main...task/T-0028-at-staging`（含 `tests/e2e/playwright.config.ts` 全文 diff）— 確認變更面與唯一一處測試碼修改。
  - `tests/integration/qa/us010-deploy.qa.test.ts` L60–L94 — TC-083 憑證掃描的實際範圍（正則 `BASIC_AUTH_(USER|PASSWORD)\s*=\s*\S`，路徑限 `src/public/tests/.github/workflows/infra/scripts/docker-compose.yml`，排除 docs／worklog），用來判斷本卡文件內的掃描字串會不會讓 TC-083 反噬。
  - `.github/workflows/ci.yml` L30–L104 — 確認 push main 會跑 build／lint／test:unit／test:integration，據此判定 lint 既有錯誤的影響面。
- 上一輪交接摘要（round > 1）：不適用（本卡 round 1；本檔為審核方首次介入）。
- 本輪計畫：
  1. 檢查 diff 範圍（不得動 `src/`、`package.json`）。
  2. 憑證掃描（`pj002`／Basic base64／`BASIC_AUTH_*=` 字面值）。
  3. 審查唯一一處測試碼修改的技術合理性與副作用。
  4. `--no-ff` 合併至 main。
  5. 合併後於根目錄跑 build／lint／test:unit／test:integration。
  6. 任務卡補審核紀錄列、移除 worktree 與分支、push origin main。
- 預期產出：合併 commit、任務卡審核紀錄列、本交接檔。

## B. 交接

- 完工時間／狀態：2026-09-19T17:40:00+08:00 / done（T-0028 任務卡本身維持 `review`，待 qa-lead 於 T-0030 做內容品質終審）
- 完成事項：
  - 技術初審四項判準逐項核對（範圍、憑證、測試碼修改、合併後 CI 指令），結論**通過**。
  - `git merge --no-ff task/T-0028-at-staging` 合併至 main，無衝突（commit `cd2025e`）。
  - 合併後於根目錄實跑 build／lint／test:unit／test:integration（輸出見下）。
  - 任務卡 `tasks/T-0028-at-staging.md` 審核紀錄表加一列「dev-tl 技術初審通過並合併」；`status` 維持 `review`、`reviewer` 維持 `qa-lead`（未動）。
  - 移除 worktree `…-wt/T-0028`、刪除分支 `task/T-0028-at-staging`、push origin main。
  - CHANGELOG 未加（本卡產出為測試報告，非使用者可見變更）。
- 產出物（路徑）：
  - `tasks/T-0028-at-staging.md`（審核紀錄列、`updated`）
  - `worklog/handoff/20260919-1733-T0028-r1-dev-tl.md`（本檔）
  - 合併 commit `cd2025e`（帶入 `docs/reports/20260919-1719-AT-E001-r3.md`、`tests/e2e/playwright.config.ts`、`worklog/handoff/20260919-1642-T0028-r1-qa-at.md`）
- 未完成／已知問題：
  - **`npm run lint` 未綠，但與本卡無關**：唯一錯誤為 `scripts/gen-schedule.mjs:27:7 'teamOf' is assigned a value but never used`。該檔來自 main 既有 commit `b2fb97b`（「docs: 專案時程表（WBS + 甘特圖）與產生器」），於本次合併**之前**即已存在於 main，且不在 T-0028 的 diff 內。依角色協定「不改本卡 outputs 以外的檔案」，本卡未代修，列為需 Leader 裁決事項（見末段）。影響：`ci.yml` 的 `lint` job 在 push main 後會紅燈；`deploy-staging.yml` 只跑 `build`＋`migrate`＋`verify:health`，不受影響。
  - **`npm run migrate` 在本 shell 失敗**：`缺少必要環境變數：NODE_ENV, PORT, DATABASE_URL, BASIC_AUTH_USER, BASIC_AUTH_PASSWORD, LOG_LEVEL`。純屬本 shell 未載入 `.env`，非程式問題；`test:integration` 自行處理 migration，56/56 全綠可證。
  - qa-at 自列的四項已知問題（TC-080 無直接同批比對證據、e2e TC-067／TC-009 間歇性失敗、TC-079 302 vs 301、NFR-003 未滿 24 小時）屬**內容品質**範疇，本次技術初審不做判定，原封不動交 qa-lead（T-0030）。
- 我做的假設與決策（含理由）：
  - **`pj002` 字面檢查結果為「2 筆命中但非憑證，判定通過」**：提示詞要求 `grep -rn pj002` 於分支 diff 必須為 0。實測大小寫敏感命中 2 筆，皆在**文件敘述中引用掃描指令本身**（`docs/reports/…r3.md:241` 的 `git grep -nE "pj002|Basic …"`、`worklog/handoff/…qa-at.md:101` 的 `grep -inE "pj002user|pj002pass"`），不含任何帳密值；`grep -i` 另多出的 2 筆是 GitHub repo 名 `jsfan910/PJ002`。另核對：無 `pj002user`／`pj002pass` 值、無 `Basic <base64>`、diff 內 `BASIC_AUTH_*=` 只出現 `<由環境變數提供>` 與 `***` 佔位符。並回查 `tasks/E-001-todo-app.md:253`、`tasks/T-0027-staging-deploy.md:45` 早已含 GCP 專案 id `pj002-509106`（在 main 既有，非本卡帶入），故「全庫 pj002 歸零」本就不是現行事實基準；真正的判準是 TC-083 所定義的「Basic Auth 憑證字面值」，該判準為 0 命中。
  - **命中位置不會讓 TC-083 反噬**：TC-083 的 `git grep` 路徑限定 `src public tests .github/workflows infra scripts docker-compose.yml`，明文排除 docs／worklog；兩筆命中皆落在 docs／worklog，且 `tests/e2e/playwright.config.ts` 的新增內容不含任何 `BASIC_AUTH_*=`。合併後實跑 `test:integration` 全綠亦佐證。
  - **接受 `tests/e2e/playwright.config.ts` 的 `launchOptions: { args: ["--disable-http2", "--disable-quic"] }`**：屬任務卡 outputs 明文授權的「修正環境差異時小修」；不改測試斷言、不改被測程式、不影響本機 docker compose（HTTP/1.1）；qa-at 已以最小重現腳本逐一排除 QUIC／HTTP2／reload 三情境並連續 3 次驗證。註解已寫明成因與適用範圍，可追溯。**非阻擋級觀察**：此旗標是無條件套用（非依 BASE_URL 判斷），等同 e2e 永不走 HTTP/2／QUIC 路徑；對一個 Basic Auth + REST 的 todo app 無功能覆蓋損失，但若日後要驗證 HTTP/2 行為需改成條件式，已列入下一步建議。
  - **不代修 `scripts/gen-schedule.mjs` 的 lint 錯誤**：雖是一行未使用變數、修起來幾秒鐘，但該檔不在本卡 outputs，且為 Leader 側產出；平行作業中擅自改他人檔案會造成合併衝突與責任混淆。改為明確回報。
  - **維持 `status: review`、不改 `reviewer`**：本次僅為技術初審，內容品質（數字與原始輸出一致性、TC 證據抽查、NFR-003 重算）由 qa-lead 於 T-0030 負責，結案權不在 dev-tl。
- 驗證方式（接手者如何確認我做的是對的：指令 + 我實際得到的輸出）：

  ```bash
  # 1. 變更面：只動 docs/reports、tests/**、任務卡、交接檔；未動 src/、package.json
  git diff main...task/T-0028-at-staging --stat
  ```
  ```text
   docs/reports/20260919-1719-AT-E001-r3.md        | 241 ++++++++++++++++++++++++
   tasks/T-0028-at-staging.md                      |   4 +-
   tests/e2e/playwright.config.ts                  |  10 +-
   worklog/handoff/20260919-1642-T0028-r1-qa-at.md | 112 +++++++++++
   4 files changed, 364 insertions(+), 3 deletions(-)
  ```

  ```bash
  # 2. 憑證掃描（大小寫敏感 pj002）
  git diff main...task/T-0028-at-staging | grep -n "pj002"
  ```
  ```text
  247:+…額外跑一次 `git grep -nE "pj002|Basic [A-Za-z0-9+/=]{10,}"`，確認無命中（見交接檔）。
  397:+  cd worktree; grep -inE "pj002user|pj002pass" tasks/T-0028-at-staging.md tests/e2e/playwright.config.ts docs/reports/20260919-1719-AT-E001-r3.md worklog/handoff/20260919-1642-T0028-r1-qa-at.md
  （兩筆皆為「敘述掃描指令」的文字，不含帳密值；分別位於 docs/reports 與 worklog/handoff）
  ```

  ```bash
  # 3. 憑證值掃描（實際帳密／base64／字面值指派）
  git diff main...task/T-0028-at-staging | grep -inE "pj002user|pj002pass|Basic [A-Za-z0-9+/=]{10,}"
  git diff main...task/T-0028-at-staging | grep -nE "BASIC_AUTH_(USER|PASSWORD)\s*=\s*\S"
  ```
  ```text
  （第一道）397:+  cd worktree; grep -inE "pj002user|pj002pass" …   ← 僅指令字串本身，無值
  （第二道）
  24:+  export STAGING_BASIC_AUTH_USER=<由環境變數提供>
  25:+  export STAGING_BASIC_AUTH_PASSWORD=<由環境變數提供>
  353:+    STAGING_BASIC_AUTH_USER=*** STAGING_BASIC_AUTH_PASSWORD=*** npm run test:integration:qa
  ← 三筆皆為佔位符，且皆落在 docs/reports 與 worklog/handoff（TC-083 掃描範圍外）
  ```

  ```bash
  # 4. 合併（無衝突）
  git merge --no-ff task/T-0028-at-staging -F <訊息檔>
  ```
  ```text
  Merge made by the 'ort' strategy.
   docs/reports/20260919-1719-AT-E001-r3.md        | 241 ++++++++++++++++++++++++
   tasks/T-0028-at-staging.md                      |   4 +-
   tests/e2e/playwright.config.ts                  |  10 +-
   worklog/handoff/20260919-1642-T0028-r1-qa-at.md | 112 +++++++++++
   4 files changed, 364 insertions(+), 3 deletions(-)
   create mode 100644 docs/reports/20260919-1719-AT-E001-r3.md
   create mode 100644 worklog/handoff/20260919-1642-T0028-r1-qa-at.md
  ```

  ```bash
  # 5. 合併後 build（根目錄 main）
  npm run build
  ```
  ```text
  > todo-app@0.1.0 build
  > tsc -p tsconfig.json
  （無錯誤輸出，exit 0）
  ```

  ```bash
  # 6. 合併後 lint
  npm run lint
  ```
  ```text
  > todo-app@0.1.0 lint
  > eslint . && tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.test.json && tsc --noEmit -p tsconfig.e2e.json

  C:\Users\excal\Projects\Claude\soft-dev-team-260919\scripts\gen-schedule.mjs
    27:7  error  'teamOf' is assigned a value but never used  @typescript-eslint/no-unused-vars

  ✖ 1 problem (1 error, 0 warnings)
  （exit 1；唯一錯誤在 main 既有檔案，與 T-0028 無關 —— 下一格為歸屬證明）
  ```

  ```bash
  # 7. 證明該 lint 錯誤先於本次合併存在於 main
  git log --oneline -1 -- scripts/gen-schedule.mjs
  git merge-base --is-ancestor $(git rev-list -1 HEAD -- scripts/gen-schedule.mjs) 7cdfd69 && echo PRE-EXISTING-ON-MAIN-BEFORE-MERGE
  ```
  ```text
  b2fb97b docs: 專案時程表（WBS + 甘特圖）與產生器
  PRE-EXISTING-ON-MAIN-BEFORE-MERGE
  （7cdfd69 = 合併前的 main HEAD）
  ```

  ```bash
  # 8. 合併後 test:unit
  npm run test:unit
  ```
  ```text
  ℹ tests 76
  ℹ suites 0
  ℹ pass 76
  ℹ fail 0
  ℹ cancelled 0
  ℹ skipped 0
  ℹ todo 0
  ℹ duration_ms 169.0178
  ```

  ```bash
  # 9. 合併後 test:integration（需本機 DB；未起 DB 時 todos-api.test.ts 會以 500 假性失敗）
  docker compose up -d db
  npm run test:integration
  ```
  ```text
  NAME                        IMAGE                SERVICE   STATUS
  soft-dev-team-260919-db-1   postgres:16-alpine   db        Up (healthy)   0.0.0.0:5432->5432/tcp

  ℹ tests 56
  ℹ pass 56
  ℹ fail 0
  ℹ skipped 0
  （對照：未起 DB 時為 tests 56 / pass 36 / fail 11 / skipped 9，失敗全為 DB 連線導致的 500，非程式缺陷）
  ```

  ```bash
  # 10. 收尾：worktree 與分支
  git worktree remove "C:/Users/excal/Projects/Claude/soft-dev-team-260919-wt/T-0028"
  git branch -d task/T-0028-at-staging
  git worktree list
  ```
  ```text
  Deleted branch task/T-0028-at-staging (was 9248e68).
  C:/Users/excal/Projects/Claude/soft-dev-team-260919 cd2025e [main]
  （移除前 worktree 內 `git status --porcelain` 為空，無未提交內容遺失）
  ```

- 下一步建議：
  - **qa-lead（T-0030）**：內容品質終審請沿用 qa-at 自列的抽查建議（TC-084、TC-090 有完整原始輸出），並對 TC-079（302 vs 301）與 e2e TC-067／TC-009 間歇性失敗兩項決定是否編缺陷號；NFR-003 待採樣累積後重算。技術面本次已放行，無須重跑 build／unit／integration。
  - **e2e 網路旗標收斂**：`--disable-http2 --disable-quic` 目前無條件套用。若日後需要驗證 HTTP/2 行為，建議改為依 `BASE_URL` 是否為 staging 動態決定（例如只在非 localhost 時加旗標），並在該次變更的交接檔記錄本沙盒的原始症狀，避免旗標被當成永久設定而遺忘成因。
  - **`test:integration` 的 DB 前置條件**：`tests/integration/todos-api.test.ts` 在無本機 DB 時直接以 500 失敗，而同目錄的 `todo-repository.test.ts`、`startup-migrate.test.ts` 會友善 skip 並印出「請先 docker compose up -d db」。建議日後有人動到該檔時，順手補上相同的 skip 前置檢查，可省下接手者的誤判時間（非阻擋，不另開卡）。
- 需要 Leader 裁決的事：
  - **`scripts/gen-schedule.mjs:27` 的 lint 錯誤要由誰修**：一行未使用變數 `teamOf`，來自 main 既有 commit `b2fb97b`，會讓 `ci.yml` 的 lint job 在 push main 後紅燈（`deploy-staging` 不受影響）。dev-tl 依「不改本卡 outputs 以外的檔案」未代修。請 Leader 擇一：(a) Leader 自行於 main 補一個修正 commit；(b) 開一張小卡指派；(c) 明確指示 dev-tl 代修。建議 (a)，最省回合。
  - qa-at 原列的兩項裁決事項（TC-079 判準文字是否修訂規格、e2e TC-067／TC-009 是否立案指派 dev-fe）原封轉呈，屬內容／規格範疇，dev-tl 技術初審不做判定。
