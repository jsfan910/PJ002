# T-0043 r1 — dev-tl — 工作紀錄與交接（初審＋合併 T-0043／T-0038）

## A. 開工紀錄

- 開工時間：2026-09-20T19:26:38+08:00
- 角色／模型：dev-tl / Opus 5 (1M context)
- 任務卡：tasks/T-0043-favicon.md（round 1，審核 T-0043）
- 讀取的輸入（實際讀過的檔案與段落，非任務卡照抄）：
  - tasks/T-0043-favicon.md（分支版）— acceptance 4 條與「驗收方式」指令清單；outputs 為 `public/favicon.ico` 與（必要時）`public/index.html`；reviewer=dev-tl、branch=task/T-0043-favicon；分支上 status 已改 review、updated 2026-09-20T19:19:40+08:00（main 上的副本仍是 todo，合併後才會同步）。
  - worklog/handoff/20260920-1915-T0043-r1-dev-fe.md（分支版，122 行）— dev-fe 宣稱：Node 手刻 32bpp 32×32 ICO（4286 bytes、檔頭 00 00 01 00）、index.html 加一行 `<link rel="icon">`、compose 8083／5435 實測 401／200、e2e 4 project × 5 次 = 20/20、lint／unit 全綠；一則「假設與決策」主張 `image/vnd.microsoft.icon` 與 `image/x-icon` 為同一 MIME 別名。
  - worklog/handoff/20260920-1902-T0038-r1-dev-tl.md（我自己 r1 初審 T-0038 的紀錄）— acceptance 1／2／3／5 通過、4 條件通過（msedge TC-009 敗於 favicon 404 且 main 基準完全複現）；待辦：合併＋CHANGELOG＋worktree 清理。
  - tasks/T-0038-fix-d017-error-message.md — status: review、round 1、審核紀錄表已有 r1「初審通過，待合併」一列；outputs 為 todo-store.js／todo-view.js／tests/unit/todo-store.test.mjs。
  - .claude/skills/review-round/SKILL.md — 審核一輪的步驟與判定規則（不採信交接檔的「通過」二字，只採信自己重跑的輸出；合併與 worktree 清理須在主 repo 根目錄執行）。
  - docs/specs/03_系統設計書_SD.md 第 6 章（錯誤處理與安全設計）— 豁免路徑判準（只認字面 `/health`），本輪須實打變體確認新增靜態檔未鬆動保護。
- 上一輪交接摘要（若 round > 1）：無（round 1，本卡第一次審核）
- 本輪計畫（3–7 條）：
  1. 比對 `git diff --stat main...task/T-0043-favicon`，確認只動 outputs（＋任務卡＋交接檔），特別確認未改 `src/`、未改 `tests/e2e`。
  2. 獨立驗證 ICO 檔本身：檔頭 `00 00 01 00`、尺寸、bytes ≤ 5 KB；掃描是否含第三方來源痕跡。
  3. 在 worktree 實跑 `npm ci`／`npm run lint`／`npm run test:unit`，貼真實輸出。
  4. 以 `PORT=8083`／`POSTGRES_HOST_PORT=5435` 起 compose，實打：不帶憑證 `/favicon.ico` → 401；帶憑證 → 200 + ICO MIME；並依 SD §6 實打豁免路徑變體（`/health`、`/./health`、`/%2e/health`、`//health`、`/health/`、`/HEALTH`、`/favicon.ico` 變體）確認保護未被鬆動。
  5. 4 個 project（chromium／msedge × 1280x800／390x844）`-g "TC-009"` 連續 5 次，貼 5 次摘要。
  6. 逐條判 acceptance，對 dev-fe「假設與決策」逐條表態；寫審核紀錄。
  7. 通過則在根目錄依序 `git merge --no-ff` T-0043、T-0038，更新 CHANGELOG，推送 main 一次，兩卡改 done，清理兩個 worktree 與分支。
- 預期產出：worklog/handoff/20260920-1926-T0043-r1-dev-tl.md（本檔）、tasks/T-0043-favicon.md（審核紀錄＋status）、tasks/T-0038-fix-d017-error-message.md（審核紀錄＋status）、CHANGELOG.md。

## B. 交接

- 完工時間／狀態：2026-09-20T19:36:11+08:00 / done
- 完成事項：
  - **T-0043 初審 r1：acceptance 四條全數通過，判 done**。所有證據皆為我自己在 worktree `<根>-wt/T-0043` 重跑取得，未採信 dev-fe 交接檔的文字。
  - **ICO 獨立佐證非第三方檔案**：除 `file` 判為 `MS Windows icon resource - 1 icon, 32x32, 32 bits/pixel` 外，我另以結構算式反推——`ICONDIR 6 + ICONDIRENTRY 16 + BITMAPINFOHEADER 40 + XOR(32×32×4) + AND mask(32×32÷8) = 4286`，與實際檔長 **逐位元組吻合**，代表整個檔案是無壓縮的原始像素陣列，沒有 PNG 區塊、沒有 EXIF／版權中繼資料，與「下載來的圖示」的特徵不符。這是我能在不連外的前提下驗證「不得抓取第三方檔案」這條的最強證據。
  - **安全面依 SD 第 6 章判準實打，不以「反向代理擋掉了」放行**：豁免路徑 10 條（`/health` 200；`/./health`、`/%2e/health`、`/%2E/health`、`//health`、`/health/`、`/HEALTH`、`/api/v1/todos`、`/index.html`、`/styles.css` 全 401）；另補打 **favicon 自身 7 條路徑變體**（含 `/health/../favicon.ico` 這種「借道豁免路徑」的組合）**全 401**，確認新增一個靜態檔沒有在 `@fastify/static` 的 prefix 比對上開出任何側門，`/health` 仍是唯一豁免。
  - **T-0043 e2e**：compose `-p t43rev`（PORT=8083／POSTGRES_HOST_PORT=5435），4 project `-g "TC-009"` 連續 5 次 **20/20 全過**；並以應用日誌交叉佐證根因確已消除——全程 **0 筆 `Route not found`**、14 筆 `/favicon.ico` 皆 200 或 401（T-0038 初審時同條件為 21 筆 404 全對應 `/favicon.ico`、msedge 兩尺寸 5 次固定全敗）。
  - **合併順序刻意安排**：先 `git merge --no-ff task/T-0043-favicon`（merge commit `1107bbd`），再 `git merge --no-ff task/T-0038-fix-d017`（merge commit `d8a7427`）。因為 T-0038 r1 初審唯一的未過項就是 favicon 404，先合 T-0043 才能讓下一步的合併後複驗真的有意義。
  - **合併後在 main 上重建驗證（不是紙上推論）**：以 `git worktree add --detach` 拉合併後 main 的獨立副本，`npm ci`／`build`／`lint` EXIT=0、`test:unit` **78/78**；compose `-p mchk`（8083／5435）以 4 project `-g "TC-067|TC-009"` 連續 5 次 **8 passed × 5 = 40/40 全過**（T-0038 r1 時同條件為每次 6 過 2 敗、5 次完全一致）；應用日誌 323×200／20×201／19×204／114×401，**零筆 404、零筆 5xx**；SD 第 6 章的 17 條路徑複驗結果與合併前一致。**至此 T-0038 r1 的「條件通過」已被實際打掉，兩卡皆判 done**。
  - **合併衝突**：僅 `tasks/T-0038-fix-d017-error-message.md` frontmatter 的 `updated` 一欄（main 19:10:27 vs 分支 18:45:08），取 main 版；其餘 7 個檔案自動合併無衝突。
  - **CHANGELOG.md** 在 `## [Unreleased]` 最上方補兩列（T-0038、T-0043），依合併順序倒序排列（最新在上）。
  - **兩卡 status 改 done**、`updated` 同步為 2026-09-20T19:33:36+08:00，審核紀錄表各加一列。
  - **清理**：`git worktree remove` 兩個 worktree（皆無未提交變更，未用 `--force`）、`git branch -d` 兩個分支（皆已完全合併，未用 `-D`）、`git worktree prune`。`docker ps` 無殘留容器，兩組 compose 皆 `down -v`，兩份本機 `.env` 皆已刪除。
- 產出物（路徑）：
  - C:\Users\excal\Projects\Claude\soft-dev-team-260919\worklog\handoff\20260920-1926-T0043-r1-dev-tl.md（本檔）
  - C:\Users\excal\Projects\Claude\soft-dev-team-260919\tasks\T-0043-favicon.md（status→done、審核紀錄表加一列）
  - C:\Users\excal\Projects\Claude\soft-dev-team-260919\tasks\T-0038-fix-d017-error-message.md（status→done、審核紀錄表加一列）
  - C:\Users\excal\Projects\Claude\soft-dev-team-260919\CHANGELOG.md（`[Unreleased]` 補 T-0038、T-0043 兩列）
  - main 上的 merge commits：`1107bbd`（T-0043）、`d8a7427`（T-0038）
- 未完成／已知問題：
  - **staging 部署仍卡在 T-0039 的 IAM**：本次推送觸發的 `deploy-staging` run 預期會在第 8 步 `resolve secret versions` 以 `PERMISSION_DENIED: Permission 'secretmanager.versions.list' denied` 停住，其後步驟 skipped。依 Leader 2026-09-20 裁決，**此不計為 T-0043／T-0038 的失敗**；待使用者補授 `roles/secretmanager.viewer`（或設定 `SECRET_VERSION_*` repository variables）後重跑部署即可上 staging。
  - **D-017 尚未正式關閉**：該缺陷僅在 staging 重現（缺陷清單第 50 列明寫本機 docker compose 從未重現），本機 40/40 全過不足以關閉。須待 staging 部署成功後由 qa-lead 在 staging 重跑 TC-067／TC-009 至少 5 次連續全過。
  - **T-0038 合併 commit 的 trailer 位置瑕疵**：我在解完衝突後把「衝突解法」一段 `>>` 追加到訊息檔尾端，使 `Co-Authored-By` 不在最後一個段落。依「git 只准 append、禁止 `--amend`」鐵則不做修正，記錄於此。內容本身無誤。
  - **`/favicon.ico` 的缺陷編號未立**：T-0038 初審時我建議 qa-lead 立案（S4），實際上 Leader 直接開了 T-0043 修掉。若 qa-lead 需要一個缺陷編號串起追溯矩陣，須自行立案並直接標 closed；立案權不在我。
- 我做的假設與決策（含理由）：
  - **對 dev-fe「MIME type 別名」的表態：接受，且該爭議其實不存在。** dev-fe 擔心實測回 `image/vnd.microsoft.icon` 而驗收條文字面寫 `image/x-icon`，但任務卡 acceptance 第 1 條原文已括號寫明「回 200 與 image/x-icon（**或 image/vnd.microsoft.icon**）」，條文本身就允許兩者。`image/vnd.microsoft.icon` 是 IANA／`mime-db` 對 `.ico` 的官方登錄值，`@fastify/static` 走 `mime-types` 自然對應到它。**不另開卡改伺服器 MIME 設定**——那會為了字面一致去改 `src/plugins/static.ts`，屬於為規格誤讀而擴大改動範圍。
  - **對 dev-fe「純程式產生圖示」的表態：接受，但我不採信「因為我這樣寫所以沒抓第三方檔案」這種自述。** 我改以檔案長度的結構算式獨立佐證（見上），這是可被第三人重跑的證據。
  - **對 dev-fe「只加一行 `<link rel="icon">`」的表態：接受。** 瀏覽器對 `/favicon.ico` 的自動請求是內建行為，加不加 `<link>` 都會發生，所以這一行不是修復的必要條件；但它讓語意明確、且是最小變更（未加 `sizes`／`type`），符合任務卡 outputs 的「僅在需要時」條款。**不因它非必要而退回**。
  - **「已初審通過」不等於「可以少驗一次」**：T-0038 在 19:02 那輪已由我判通過待合併，但它當時的通過帶著一個明確的條件（favicon 404）。我沒有直接把它合進去了事，而是先合 T-0043、再在合併後的 main 上把 `-g "TC-067|TC-009"` 完整重跑 5 次，用 40/40 這個實際輸出解除條件。若我只合併不複驗，「條件已解除」就只是推論。
  - **合併順序不可對調**：若先合 T-0038 再合 T-0043，合併後的複驗就無法對應到「T-0038 的未過項被解除」這個因果，中間狀態也會留下一個 msedge TC-009 必紅的 commit。
  - **合併前檢查根目錄有他人未提交變更（`docs/specs/06_部署架構與CICD.md`、`tasks/T-0040-*.md`，plan-sd 的 T-0040 在進行中），確認兩卡 diff 與這些路徑零交集後才合併**，且全程只 `git add` 本次的任務卡、CHANGELOG 與本交接檔，未用 `git add -A`，避免把別人進行中的工作一起提交。
- 驗證方式（接手者如何確認我做的是對的：指令 + 我實際得到的輸出）：
  ```bash
  cd "C:/Users/excal/Projects/Claude/soft-dev-team-260919-wt/T-0043"
  git diff --name-only main...task/T-0043-favicon
  node -e "const b=require(\"fs\").readFileSync(\"public/favicon.ico\");const w=b[6],h=b[7];const e=22+40+w*h*4+(w*h/8);console.log(\"bytes=\"+b.length,\"hdr=\"+b[0]+b[1]+b[2]+b[3],\"w=\"+w,\"h=\"+h,\"bpp=\"+b.readUInt16LE(12),\"expected_raw=\"+e,\"match=\"+(b.length===e));"
  file public/favicon.ico
  ```
  ```text
  public/favicon.ico
  public/index.html
  tasks/T-0043-favicon.md
  worklog/handoff/20260920-1915-T0043-r1-dev-fe.md
  （src/ 與 tests/ 零命中）

  bytes=4286 hdr=0010 w=32 h=32 bpp=32 expected_raw=4286 match=true
  public/favicon.ico: MS Windows icon resource - 1 icon, 32x32, 32 bits/pixel
  ```
  ```bash
  npm run build; npm run lint; npm run test:unit
  ```
  ```text
  BUILD_EXIT=0
  LINT_EXIT=0
  UNIT_EXIT=0
  ℹ tests 76
  ℹ pass 76
  ℹ fail 0
  ```
  ```bash
  # compose（PORT=8083／POSTGRES_HOST_PORT=5435，.env 由 .env.example 複製後改兩個埠）
  docker compose -p t43rev up -d --build && docker compose -p t43rev ps
  ```
  ```text
  NAME           IMAGE                COMMAND                   SERVICE   CREATED         STATUS                   PORTS
  t43rev-app-1   t43rev-app           "docker-entrypoint.s…"   app       7 seconds ago   Up Less than a second    0.0.0.0:8083->8083/tcp
  t43rev-db-1    postgres:16-alpine   "docker-entrypoint.s…"   db        7 seconds ago   Up 6 seconds (healthy)   0.0.0.0:5435->5432/tcp
  ```
  ```bash
  curl -sS -o /dev/null -w "%{http_code}\n" http://localhost:8083/favicon.ico
  curl -u dev:dev -sS -o /dev/null -w "%{http_code} %{content_type} %{size_download}\n" http://localhost:8083/favicon.ico
  curl -u dev:dev -sS -o /tmp/fav_dl.ico http://localhost:8083/favicon.ico && cmp /tmp/fav_dl.ico public/favicon.ico && echo IDENTICAL
  ```
  ```text
  401
  200 image/vnd.microsoft.icon 4286
  IDENTICAL
  ```
  ```bash
  # SD 第 6 章：豁免路徑只認字面 /health（--path-as-is 避免 curl 自行正規化）
  for p in "/health" "/./health" "/%2e/health" "/%2E/health" "//health" "/health/" "/HEALTH" "/api/v1/todos" "/index.html" "/styles.css"; do
    echo "$p -> $(curl -s -o /dev/null -w "%{http_code}" --path-as-is "http://localhost:8083$p")"; done
  ```
  ```text
  /health -> 200
  /./health -> 401
  /%2e/health -> 401
  /%2E/health -> 401
  //health -> 401
  /health/ -> 401
  /HEALTH -> 401
  /api/v1/todos -> 401
  /index.html -> 401
  /styles.css -> 401
  ```
  ```bash
  # favicon 自身的路徑變體（含借道豁免路徑），任一繞過即為阻擋級
  for p in "/favicon.ico" "/./favicon.ico" "/%2e/favicon.ico" "//favicon.ico" "/FAVICON.ICO" "/health/../favicon.ico" "/%2e%2e/favicon.ico"; do
    echo "$p -> $(curl -s -o /dev/null -w "%{http_code}" --path-as-is "http://localhost:8083$p")"; done
  ```
  ```text
  /favicon.ico -> 401
  /./favicon.ico -> 401
  /%2e/favicon.ico -> 401
  //favicon.ico -> 401
  /FAVICON.ICO -> 401
  /health/../favicon.ico -> 401
  /%2e%2e/favicon.ico -> 401
  ```
  ```bash
  export BASE_URL=http://localhost:8083
  for i in 1 2 3 4 5; do npx playwright test -c tests/e2e/playwright.config.ts \
    --project=chromium-1280x800 --project=chromium-390x844 \
    --project=msedge-1280x800 --project=msedge-390x844 -g "TC-009"; done
  ```
  ```text
  ########## RUN 1 ##########  4 passed (5.3s)  RUN_1_EXIT=0
  ########## RUN 2 ##########  4 passed (4.4s)  RUN_2_EXIT=0
  ########## RUN 3 ##########  4 passed (5.1s)  RUN_3_EXIT=0
  ########## RUN 4 ##########  4 passed (5.5s)  RUN_4_EXIT=0
  ########## RUN 5 ##########  4 passed (4.5s)  RUN_5_EXIT=0
  # 每次 4 條皆為 ok：chromium-1280x800／chromium-390x844／msedge-1280x800／msedge-390x844
  # 的 tests\e2e\us001-create.e2e.test.ts:30:1 › TC-009
  ```
  ```bash
  # 根因消除的日誌佐證
  docker compose -p t43rev logs app | grep -c "Route not found"
  docker compose -p t43rev logs app | grep -o "\"url\":\"[^\"]*\"" | sort | uniq -c | grep favicon
  docker compose -p t43rev logs app | grep -o "\"statusCode\":[0-9]*" | sort | uniq -c
  ```
  ```text
  0
       14 "url":"/favicon.ico"
      153 "statusCode":200
       74 "statusCode":401
  （零筆 404、零筆 5xx；對照 T-0038 初審時同條件為 21 筆 404 全對應 /favicon.ico）
  ```
  ```bash
  # 合併（在主 repo 根目錄，非 worktree 內）
  cd "C:/Users/excal/Projects/Claude/soft-dev-team-260919"
  git merge --no-ff -F <msg_t43> task/T-0043-favicon
  git merge --no-ff -F <msg_t38> task/T-0038-fix-d017
  ```
  ```text
  Merge made by the "ort" strategy.
   public/favicon.ico                               | Bin 0 -> 4286 bytes
   public/index.html                                |   1 +
   tasks/T-0043-favicon.md                          |   4 +-
   worklog/handoff/20260920-1915-T0043-r1-dev-fe.md | 121 +++++++++++++++++++++++
   4 files changed, 124 insertions(+), 2 deletions(-)

  Auto-merging tasks/T-0038-fix-d017-error-message.md
  CONFLICT (content): Merge conflict in tasks/T-0038-fix-d017-error-message.md
  # 衝突內容只有 frontmatter 一行：
  #   <<<<<<< HEAD  updated: 2026-09-20T19:10:27+08:00
  #   =======       updated: 2026-09-20T18:45:08+08:00
  # 取 HEAD 版後 git add 該檔、git commit -F 完成合併
  [main d8a7427] T-0038: 合併 D-017 修正 — 前端錯誤訊息顯示時序競態
  ```
  ```bash
  # 合併後在 main 的獨立副本重建驗證
  git worktree add --detach "<根>-wt/merged-check" main
  cd "<根>-wt/merged-check"
  ls -l public/favicon.ico; grep -c "opSeq\|beginOp" public/assets/todo-store.js; grep -n "rel=\"icon\"" public/index.html
  npm ci; npm run build; npm run lint; npm run test:unit
  ```
  ```text
  -rw-r--r-- 1 excal 197609 4286 Sep 20 19:31 public/favicon.ico
  12
  7:    <link rel="icon" href="/favicon.ico" />
  NPMCI_EXIT=0  BUILD_EXIT=0  LINT_EXIT=0  UNIT_EXIT=0
  ℹ tests 78
  ℹ pass 78
  ℹ fail 0
  （78 = T-0043 基準的 76 + T-0038 新增的 2 條，兩卡內容皆在）
  ```
  ```bash
  docker compose -p mchk up -d --build
  export BASE_URL=http://localhost:8083
  for i in 1 2 3 4 5; do npx playwright test -c tests/e2e/playwright.config.ts \
    --project=chromium-1280x800 --project=chromium-390x844 \
    --project=msedge-1280x800 --project=msedge-390x844 -g "TC-067|TC-009"; done
  docker compose -p mchk logs app | grep -o "\"statusCode\":[0-9]*" | sort | uniq -c
  ```
  ```text
  ########## RUN 1 ##########  8 passed (6.8s)
  ########## RUN 2 ##########  8 passed (6.1s)
  ########## RUN 3 ##########  8 passed (6.3s)
  ########## RUN 4 ##########  8 passed (6.3s)
  ########## RUN 5 ##########  8 passed (6.5s)
  # 含 msedge-390x844 的 TC-009 與 TC-067 皆 ok；合計 40/40
      323 "statusCode":200
       20 "statusCode":201
       19 "statusCode":204
      114 "statusCode":401
  （零筆 404、零筆 5xx）
  ```
  ```bash
  # 合併後狀態下複驗 SD 第 6 章 17 條路徑（同上兩組指令，打 mchk 的 8083）
  ```
  ```text
  /health -> 200；其餘 9 條豁免變體與受保護路徑全 401
  /favicon.ico 無憑證 401、帶憑證 200 image/vnd.microsoft.icon、cmp IDENTICAL
  favicon 7 條路徑變體全 401
  （與合併前結果完全一致，合併未鬆動任何保護）
  ```
  ```bash
  # 清理（在主 repo 根目錄執行）
  docker compose -p t43rev down -v; docker compose -p mchk down -v; rm -f .env
  git worktree remove "<根>-wt/T-0043"; git worktree remove "<根>-wt/T-0038"
  git branch -d task/T-0043-favicon task/T-0038-fix-d017
  git worktree prune; git worktree list; git branch; docker ps
  ```
  ```text
  Deleted branch task/T-0043-favicon (was 938a759).
  Deleted branch task/T-0038-fix-d017 (was 1ab2a53).
  C:/Users/excal/Projects/Claude/soft-dev-team-260919           d8a7427 [main]
  C:/Users/excal/Projects/Claude/soft-dev-team-260919-wt/T-0039 0b0ba54 [task/T-0039-secret-pin-verify-retry]
  * main
  + task/T-0039-secret-pin-verify-retry
  （docker ps 無輸出＝無殘留容器；兩個 worktree 皆無未提交變更，未用 --force；兩個分支皆完全合併，未用 -D）
  ```
- 下一步建議：
  - **Leader**：在 `tasks/_done.md` 追加 T-0038 與 T-0043 兩列（派工時間取各卡 r1 交接檔 A 段開工時間：T-0038 為 2026-09-20T18:36、T-0043 為 2026-09-20T19:15:18；完成時間皆 2026-09-20T19:33:36），並把兩卡自 `tasks/_todo.md` 移除。
  - **使用者／Leader**：補授 `roles/secretmanager.viewer` 給 `github-deployer@pj002-509106.iam.gserviceaccount.com`（或設 `SECRET_VERSION_*` repository variables）解除 T-0039 阻塞；解除後重跑 `deploy-staging`，本次兩卡的程式即會上 staging。
  - **qa-lead**：staging 部署成功後於 staging 重跑 TC-067／TC-009 至少 5 次連續全過，才正式關閉 D-017；同時確認 staging 的 `/favicon.ico` 行為（T-0038 交接檔記載 AT r3 在 staging 的 msedge TC-009 失敗訊息是 `error-message ... Received: hidden`，不是 favicon 的 console error，本機與 staging 曾不一致，值得一併確認）。另建議評估是否補一條 TC 覆蓋 T-0038 帶來的新行為「寫入成功但被更新動作取代時清單暫不刷新」。
  - **T-0042（Release Notes 定版）** 依賴 T-0038，該依賴現已解除，可排入。
- 需要 Leader 裁決的事：
  - 無。（staging 部署卡在 T-0039 IAM 一事，Leader 2026-09-20 已裁決不計為本兩卡失敗，已依裁決於兩卡審核紀錄註明「待 IAM 授權後重跑部署即上 staging」。）

## 補記：推送結果（收尾後補，2026-09-20T19:40 左右）

- `git push origin main` **第 1 次失敗**，非權限問題、非提交內容問題：
  ```text
  fatal: unable to access "https://github.com/jsfan910/PJ002.git/":
  schannel: failed to receive handshake, SSL/TLS connection failed
  ```
  依「等待外部事件最多主動確認 2 次」的紀律，只重試一次即停。**第 2 次成功**：
  ```text
  To https://github.com/jsfan910/PJ002.git
     7274035..7dab35c  main -> main
  ```
- 推送範圍包含本次三個提交與並行的 T-0040 提交（`3e6b6b8`、`7dab35c`，plan-sd／Leader 於本卡收尾期間落在 main 上）。逐一驗證本卡三個提交確已在遠端：
  ```bash
  for c in 1107bbd d8a7427 55a20a0; do
    echo "$c -> $(git merge-base --is-ancestor $c origin/main && echo IN_ORIGIN || echo MISSING)"; done
  ```
  ```text
  1107bbd -> IN_ORIGIN   # merge T-0043
  d8a7427 -> IN_ORIGIN   # merge T-0038
  55a20a0 -> IN_ORIGIN   # 審核紀錄＋CHANGELOG＋本交接檔
  ```
- main 上的內容確認：`public/favicon.ico` 4286 bytes 在位、`todo-store.js` 有 3 處 `opSeq`、CHANGELOG `[Unreleased]` 最上方兩列為 T-0038 與 T-0043。
- **此次推送會觸發 `deploy-staging`，預期仍在第 8 步 `resolve secret versions` 以 `PERMISSION_DENIED` 紅燈**（T-0039 的 IAM 阻塞，Leader 已裁決不計為本兩卡失敗）。我未輪詢該 run；判讀留給 Leader 或待 IAM 授權後重跑。
