# T-0024 r1 — dev-tl（審核）— 工作紀錄與交接

<!-- 檔名：worklog/handoff/20260919-1227-T0024-r1-dev-tl.md -->

## A. 開工紀錄

- 開工時間：2026-09-19T12:27:27+08:00
- 角色／模型：dev-tl / Opus 5（claude-opus-5[1m]）
- 任務卡：tasks/T-0024-fix-auth-migrate.md（round 1）— **審核 T-0024**
- 讀取的輸入：
  - `tasks/T-0024-fix-auth-migrate.md`（acceptance 五條、驗收方式）
  - `docs/reports/20260919-1146-CR-E001.md` B-1／B-2／S-1／S-2 四列「修正建議」欄
  - `tasks/E-001-todo-app.md` 12:03:12 與 12:22:24 兩段 Leader 裁決（撤回 T-0013-③、coerceTypes 關閉、Dockerfile COPY migrations 追加授權）
  - `worklog/handoff/20260919-1204-T0024-r1-dev-be.md` B 段（含追加段）
  - 分支原始碼：`src/plugins/basic-auth.ts`、`src/server.ts`、`src/app.ts`、`src/schemas/todo-schema.ts`、`Dockerfile`、`tests/integration/startup-migrate.test.ts`
- 本輪計畫：在 worktree `…-wt/T-0024`（`task/T-0024-fix-auth-migrate`，HEAD `72558b2`）重跑 acceptance 每條；docker compose 乾淨環境驗自動 migration；判定並合併。

## B. 交接

- 完工時間／狀態：2026-09-19T12:35:00+08:00 / **done（初審通過、已合併 main）**

### 逐條判定 acceptance

| # | acceptance | 判定 | 證據 |
|---|---|---|---|
| 1 | B-1：豁免比對只剝除 `?`／`#`，不做正規化；四個點節段變體 401；`/health`、`/health?x=1` 200 | **通過** | 原始碼審閱（見下「B-1 原始碼確認」）＋ 真實容器 `curl --path-as-is` 六條（下方 §5） |
| 2 | S-2：豁免限 GET／HEAD；`POST /health` 未帶憑證 401 | **通過** | `isExemptPath(rawUrl, method)` 先擋 method；實測 `POST /health` → 401 |
| 3 | B-2：`server.ts` listen 前呼叫 `runMigrations`，失敗非 0 結束並印原因；未套用 migration 的 DB 啟動後 `GET /api/v1/todos` 不得 500 | **通過** | 乾淨 volume compose 啟動後 `\dt` 兩表存在、`GET /api/v1/todos` 200 `[]`（§4）；失敗路徑實測 exit 1 且未 listen（§6）；回歸測試 `tests/integration/startup-migrate.test.ts` 存在並通過 |
| 4 | S-1：`POST {"title":123}` 400、`{"isCompleted":null}` 400；既有 122 項測試仍全綠 | **通過** | §5 四個變體皆 400；75 unit + 56 integration = **131 全綠**（§1～§3、§7） |
| 5 | `git diff main...HEAD --stat` 只動 outputs | **通過** | §8，僅 outputs + 任務卡 + 交接檔 + Dockerfile（Leader 追加授權一行） |

### B-1 原始碼確認（豁免比對無任何正規化）

讀 `src/plugins/basic-auth.ts`：`extractPathname`／`new URL()` 已整段移除，改為

- `stripQueryAndHash(rawUrl)`：只用 `indexOf("?")`／`indexOf("#")` 取最小索引後 `slice`，**無 decodeURI、無 toLowerCase、無 normalize、無 replace、無正則**；
- `isExemptPath(rawUrl, method)`：`method !== "GET" && method !== "HEAD"` 直接 `false`，否則 `stripQueryAndHash(rawUrl) === "/health"` 完全相等比對；
- hook 內傳入 `request.url`（請求原文）與 `request.method`。

符合 SD §6.2「必須是完全相等的字串」硬性要求。

### 驗證輸出（全部在 `C:/Users/excal/Projects/Claude/soft-dev-team-260919-wt/T-0024`，HEAD `72558b2`）

#### §1 lint

```bash
npm run lint
```
```text
> todo-app@0.1.0 lint
> eslint . && tsc --noEmit -p tsconfig.json

LINT_EXIT=0
```

#### §2 build

```bash
npm run build
```
```text
> todo-app@0.1.0 build
> tsc -p tsconfig.json

BUILD_EXIT=0
```

#### §3 test:unit

```text
ℹ tests 75
ℹ suites 0
ℹ pass 75
ℹ fail 0
ℹ duration_ms 260.5376
```

#### §4 docker compose down -v 後 up --build（乾淨環境，驗自動 migration）

```bash
cp .env.example .env
docker compose down -v
docker compose up -d --build
docker logs t-0024-app-1
docker exec t-0024-db-1 psql -U dev -d todo -c "\dt"
curl -sS -u dev:dev -w "\nHTTP=%{http_code}\n" http://localhost:8080/api/v1/todos
```
```text
 Image t-0024-app Built
 Volume t-0024_todo-db-data Created
 Container t-0024-db-1 Healthy
 Container t-0024-app-1 Started

{"level":30,"time":1789791968138,"pid":1,"hostname":"6448ba4417da","msg":"Server listening at http://127.0.0.1:8080"}
{"level":30,"time":1789791968139,"pid":1,"hostname":"6448ba4417da","msg":"Server listening at http://172.18.0.3:8080"}

             List of relations
 Schema |       Name        | Type  | Owner
--------+-------------------+-------+-------
 public | schema_migrations | table | dev
 public | todos             | table | dev
(2 rows)

[]
HTTP=200
```

volume 為全新建立（`down -v` 後 `Volume t-0024_todo-db-data Created`），且容器內除 `node dist/server.js` 外沒有任何 migrate 步驟 → 兩張表只可能來自啟動時自動 migration。

#### §5 curl --path-as-is 六條 + POST /health + 型別脅迫（對 §4 的真實容器 8080）

```bash
for P in "/health" "/health?x=1" "/foo/../health" "/.%2e/health" "/%2e%2e/health" "/assets/%2e%2e/%2e%2e/health"; do
  printf "%-32s -> " "$P"; curl -sS --path-as-is -o /dev/null -w "%{http_code}\n" "http://127.0.0.1:8080$P"; done
curl -sS -X POST -o /dev/null -w "%{http_code}\n" "http://127.0.0.1:8080/health"
```
```text
/health                          -> 200
/health?x=1                      -> 200
/foo/../health                   -> 401
/.%2e/health                     -> 401
/%2e%2e/health                   -> 401
/assets/%2e%2e/%2e%2e/health     -> 401
POST /health                     -> 401
```

S-1 四個變體（皆帶合法憑證）：

```text
POST /api/v1/todos {"title":123}                 -> 400 {"code":"E_VALIDATION","message":"body/title must be string",...,"details":{"field":"title","rule":"type"}}
POST /api/v1/todos {"isCompleted":null}          -> 400 {"code":"E_VALIDATION","message":"body must have required property 'title'",...}
PATCH /api/v1/todos/{id} {"isCompleted":null}    -> 400 {"code":"E_VALIDATION","message":"body/isCompleted must be boolean",...,"details":{"field":"isCompleted","rule":"type"}}
PATCH /api/v1/todos/{id} {"title":123}           -> 400 {"code":"E_VALIDATION","message":"body/title must be string",...}
```

補充觀察（不影響判定）：`POST {"title":"ok","isCompleted":null}` 回 **201**。原因是 `CreateTodoRequestSchema` 的 `additionalProperties: false` 搭配 Fastify ajv 預設 `removeAdditional: true`，POST 的 `isCompleted` 屬**未宣告欄位**、在驗證前即被移除，並非型別脅迫；這與既有「夾帶 id／createdAt 一律忽略（BR-003、BR-012）」的行為與測試一致。CR S-1 所指的 `null → false` 脅迫只可能發生在宣告了 `isCompleted` 的 PATCH，該處已回 400。acceptance 第 4 條的字面情境（body 只含 `{"isCompleted":null}`）亦回 400。

#### §6 B-2 失敗路徑（migration 失敗 → 非 0 結束且不監聽）

```bash
DATABASE_URL="postgres://dev:dev@127.0.0.1:59999/nope" PORT=8099 node dist/server.js
```
```text
{"level":50,...,"err":{"type":"Error","message":"connect ECONNREFUSED 127.0.0.1:59999",...},"msg":"啟動時套用 migration 失敗，服務不會啟動監聽"}
EXIT=1

curl http://127.0.0.1:8099/health
curl: (7) Failed to connect to 127.0.0.1 port 8099 ... 000
```

#### §7 test:integration（對 §4 的 compose db，localhost:5432）

```bash
docker exec t-0024-db-1 psql -U dev -d todo -c "TRUNCATE todos;"
NODE_ENV=test PORT=8080 DATABASE_URL="postgres://dev:dev@localhost:5432/todo" \
  BASIC_AUTH_USER=dev BASIC_AUTH_PASSWORD=dev LOG_LEVEL=silent npm run test:integration
```
```text
✔ POST /api/v1/todos：title 送數字回 400（CR S-1：關閉 ajv coerceTypes，不得脅迫成字串後放行）
✔ PATCH /api/v1/todos/{id}：isCompleted 送 null 回 400（CR S-1…）
ℹ tests 56
ℹ pass 56
ℹ fail 0
ℹ duration_ms 1144.4574
```

（75 + 56 = **131 項全綠**，高於任務卡要求的既有 122 項。）

#### §8 git diff 範圍

```bash
git diff main...HEAD --stat
```
```text
 Dockerfile                                       |   5 +
 src/app.ts                                       |  10 +
 src/plugins/basic-auth.ts                        |  47 +++-
 src/server.ts                                    |  91 ++++++--
 tasks/T-0024-fix-auth-migrate.md                 |   5 +-
 tests/integration/auth-basic.test.ts             | 110 ++++++++-
 tests/integration/startup-migrate.test.ts        | 121 ++++++++++
 tests/integration/todos-api.test.ts              |  32 +++
 worklog/handoff/20260919-1204-T0024-r1-dev-be.md | 272 +++++++++++++++++++++++
 9 files changed, 655 insertions(+), 38 deletions(-)
```

程式碼檔全部在 outputs 內；`Dockerfile` 一行為 Leader 12:22:24 追加授權；`package.json`、`src/config.ts`、`src/repositories/*` 未動。**通過**。

#### §9 收尾清除

```bash
docker compose down -v      → Container/Volume/Network 全部 Removed
rm -f .env
docker image rm t-0024-app  → Untagged: t-0024-app:latest
docker ps                   → 只剩 t-0021-app-1、t-0021-db-1（qa-at 的 worktree，未受影響）
```

### 對 dev-be「假設與決策」逐條表態

| # | 決策 | 表態 | 理由 |
|---|---|---|---|
| 1 | 不把 `routes/todos.ts` 的連線池上移到 bootstrap，migration 另建臨時池跑完即 `end()` | **接受** | `routes/todos.ts` 不在本卡 outputs，不上移是正確的範圍控制。臨時池 `createPool(config)`→`runMigrations`→`finally pool.end()` 生命週期封閉，實測不影響服務池；單一池收斂如要做應另開卡（已寫入下一步建議） |
| 2 | `start()` 回傳已 listen 的 `FastifyInstance` | **接受** | 讓回歸測試走與 CLI 完全相同的啟動路徑（含 `applyMigrationsOrExit`），這正是 CR「否則此缺口會再次被掩蓋」要求的重點；`isMainModule` 守衛確保 import 無 side effect，已由測試行為佐證 |
| 3 | 四個點節段測試改用真實 TCP（`app.listen({port:0})` + `node:http`），不用 `app.inject()` | **接受，且是本卡的關鍵正確判斷** | 我獨立以 `curl --path-as-is` 對**真實容器**複驗四條皆 401（§5），與該測試同結論。`light-my-request` 會先正規化 request-target 的說法與現象一致（inject 永遠 200 → 無法重現漏洞）；此處若用 inject 等於寫一條恆綠的假測試 |
| 4 | 回歸測試用隔離 schema `startup_migrate_test`（`options=-c search_path=`）而非 DROP 共用表 | **接受** | `node --test` 多檔平行、且 `todos-api.test.ts` 假設 `public.todos` 存在，DROP 共用表會互撞。schema 隔離 + 測試前後 `DROP/CREATE SCHEMA CASCADE` 達成「完全未套用 migration」語意，且不觸碰 `public`；我重跑整批 56 條確認無互相干擾 |
| 5 | 手動驗收用獨立 `docker run` 的 Postgres 而非 compose（避開 5432 撞埠） | **接受（且本輪已用 compose 補驗）** | 當時 5432 被根目錄 compose 佔用，理由成立。本輪驗收時該容器已不存在，我直接以任務卡指定的 `docker compose down -v` + `up --build` 完整跑過（§4），乾淨環境路徑已由審核者親自覆蓋，不再依賴替代手法 |
| 6（追加） | Dockerfile 用 `COPY migrations ./migrations`（自 build context）而非 `COPY --from=builder` | **接受** | builder 階段從未 `COPY migrations`，照字面寫 `--from=builder` 會直接建置失敗；與既有 `COPY public ./public` 兩階段各自從 context 複製的寫法一致。§4 實測建置成功且容器未因 ENOENT 退出，等效性已證 |

### 判定

**全部通過 → status done，已合併 main（`--no-ff`）。**

### 下一步建議（給 qa-cr r2 與 qa-at r2）

1. **qa-cr r2 複審 B-1 時請用 `curl --path-as-is` 或 `node:http` 直送 request-target，不要用 `app.inject()`**：`light-my-request` 會先正規化點節段，用 inject 驗 B-1 會得到恆為「通過」的假訊號（本輪已由 dev-be 實證、dev-tl 以真實容器複驗）。
2. **`POST {"title":"ok","isCompleted":null}` 回 201 屬預期行為，非 S-1 殘留**：成因是 `removeAdditional`（未宣告欄位先被移除）而非型別脅迫，與 BR-003／BR-012「夾帶欄位一律忽略」一致。若 qa-cr 認為 POST 夾帶未知欄位應回 400，那是**新的規格議題**（04 規格未要求），請另開議題而非併入 S-1。
3. **qa-at r2 重跑整合測試時務必先備妥 Postgres**：`tests/integration/startup-migrate.test.ts` 無 DB 時會整批 skip 並印 `[SKIP]`，skip 不等於通過；請確認輸出為 `tests 56 / pass 56`。該檔另會佔用埠 `18024`（可用 `STARTUP_MIGRATE_TEST_PORT` 覆寫）與 schema `startup_migrate_test`。

### 需要 Leader 裁決的事

- 無。（「連線池上移 bootstrap」屬可選收斂，建議留到 P1 或由 Leader 視需要另開小卡，不阻擋本卡。）
