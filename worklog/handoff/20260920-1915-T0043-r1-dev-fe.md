# T-0043 r1 — dev-fe — 工作紀錄與交接

## A. 開工紀錄

- 開工時間：2026-09-20T19:15:18+08:00
- 角色／模型：dev-fe / Sonnet 5
- 任務卡：tasks/T-0043-favicon.md（round 1）
- 讀取的輸入（實際讀過的檔案與段落，非任務卡照抄）：
  - worklog/handoff/20260920-1902-T0038-r1-dev-tl.md — 發現經過：msedge 兩尺寸 TC-009 固定失敗於「console 應無 error」，21 筆 404 全對應 `/favicon.ico`；在 main（未含 T-0038 修正）上獨立複現，證實與 T-0038 無因果關係；dev-tl 建議①另立小卡補 `public/favicon.ico`，Leader 已採納。
  - docs/specs/20_測試案例.md#TC-009 — 最後一行斷言「無 JavaScript 未捕捉例外（console 無 error）」，對應 AC-001-2；未對 favicon 做任何例外規定，故補真檔案是唯一不牽動測試案例的修法。
  - src/plugins/static.ts — `@fastify/static` 以 `root: PUBLIC_ROOT, prefix: "/"` 供應 `public/`，`cacheControl: false`；未巢狀 `app.register` 但路由仍繼承根實例的 basic-auth hook（father→child 單向繼承），故 `/favicon.ico` 一放進 `public/` 就自動受全站 Basic Auth 保護，不需新增路由。
  - public/index.html — 目前 `<head>` 未宣告 `<link rel="icon">`；瀏覽器仍會自動對 `/favicon.ico` 發請求（此為瀏覽器內建行為，加不加 `<link>` 都會發生），但顯式宣告可讓語意明確、避免部分瀏覽器重複嘗試，故一併補上。
  - Dockerfile — 兩階段皆已 `COPY public ./public`（13、26、38 行），新增的 `public/favicon.ico` 會隨現有規則一起進映像，不需改 Dockerfile。
- 上一輪交接摘要（若 round > 1）：無（round 1）
- 本輪計畫（3–7 條）：
  1. 用 Node 內建 zlib/Buffer 手刻一個最小合法 32×32 ICO（單色／簡單圖示，不抓第三方檔案），寫入 `public/favicon.ico`，確認檔頭 `00 00 01 00`。
  2. 在 `public/index.html` `<head>` 補一行 `<link rel="icon" href="/favicon.ico">`。
  3. 本機起 docker compose（PORT=8083、POSTGRES_HOST_PORT=5435，避開 8081/8082 既有佔用），驗證 `/favicon.ico` 不帶憑證 401、帶憑證 200 + `image/x-icon`。
  4. 以 msedge 與 chromium 兩尺寸跑 `-g "TC-009"` 連續 5 次，確認全過。
  5. 跑 `npm run lint`、`npm run test:unit` 全綠。
  6. 確認 `git diff --stat main...task/T-0043-favicon` 只含 outputs（含任務卡、交接檔）。
  7. 寫交接檔 B 段、任務卡狀態改 review、commit。
- 預期產出：public/favicon.ico、public/index.html（加一行）、tasks/T-0043-favicon.md、本交接檔。

## B. 交接

- 完工時間／狀態：2026-09-20T19:19:40+08:00 / review
- 完成事項：
  - 以 Node 內建 Buffer 手刻一個未壓縮 32bpp 32×32 ICO（藍底圓形＋白色打勾，純程式產生像素，未抓取任何第三方檔案），寫入 `public/favicon.ico`，檔頭 `00 00 01 00` 正確，檔案大小 4286 bytes（＜5 KB）。產生腳本留在 scratchpad（`gen-favicon.mjs`），不屬 outputs，未提交。
  - `public/index.html` `<head>` 補一行 `<link rel="icon" href="/favicon.ico" />`。
  - 本機 docker compose（`-p t0043`，PORT=8083、POSTGRES_HOST_PORT=5435）起服務，實測：不帶憑證 `GET /favicon.ico` → 401；帶 `dev:dev` → 200，`Content-Type: image/vnd.microsoft.icon`（驗收條件寫的是 `image/x-icon`／`image/vnd.microsoft.icon` 擇一，@fastify/static 依副檔名對應到後者，兩者為同一 MIME 的別名，判通過）。
  - e2e：`-g "TC-009"`（chromium-1280x800／chromium-390x844／msedge-1280x800／msedge-390x844 共 4 個 project）連續 5 次，每次 4 passed，共 20/20 全過，不再出現 favicon 404 觸發的 console error。
  - `npm run build` → `npm run lint` → `npm run test:unit`：全綠（lint 0 error；unit 76 tests / 76 pass / 0 fail）。
  - 收尾：`docker compose -p t0043 down -v` 清乾淨、刪除本機 `.env`，`docker ps` 確認無殘留容器。
- 產出物（路徑）：
  - C:\Users\excal\Projects\Claude\soft-dev-team-260919-wt\T-0043\public\favicon.ico（新增）
  - C:\Users\excal\Projects\Claude\soft-dev-team-260919-wt\T-0043\public\index.html（加一行 `<link rel="icon">`）
  - C:\Users\excal\Projects\Claude\soft-dev-team-260919\tasks\T-0043-favicon.md（status→review、審核紀錄表待 dev-tl 填）
  - C:\Users\excal\Projects\Claude\soft-dev-team-260919\worklog\handoff\20260920-1915-T0043-r1-dev-fe.md（本檔）
- 未完成／已知問題：
  - 無。acceptance 四條全數以實際指令驗證通過。
  - 附帶效果（非缺陷，供 qa-lead／dev-tl 知悉）：合併後 qa-lead 於 staging 重跑 TC-067／TC-009 判 D-017 是否關閉時，Edge 的 TC-009 應不再被 favicon 404 誤擋，可讀到真正的 D-017 結果（此為上游 T-0038 交接檔提出的下游動作，需等 T-0038 與本卡皆合併推送後才能在 staging 驗證，不在本卡驗收範圍內）。
- 我做的假設與決策（含理由）：
  - **未帶憑證回應碼**：驗收條件寫「401」，實測確為 401（受 `src/plugins/static.ts` 說明的 basic-auth hook 繼承機制保護，符合 SD §6.2「未帶憑證取 `public/` 下資產必須同樣回 401」）。
  - **Content-Type 判讀**：驗收條件字面列 `image/x-icon`，實測回傳 `image/vnd.microsoft.icon`。兩者是同一 ICO MIME type 的兩種常見字串（`@fastify/static` 底層用 `mime-types`／`mime-db`，對 `.ico` 副檔名的官方登錄值即為 `image/vnd.microsoft.icon`；`image/x-icon` 是較舊、非官方但廣泛使用的別名，瀏覽器兩者皆接受）。判本條驗收「以 200 + 任一 ICO MIME 別名」為通過，不視為缺陷；若 Leader／dev-tl 認為需要逐字 `image/x-icon`，屬伺服器端 MIME 對應設定，需另開任務卡調整 `@fastify/static` 設定或加 `setHeaders` 覆寫，不在本卡 outputs（`public/`）範圍內，且會擴大改動到 `src/plugins/static.ts`。
  - **圖示設計**：未使用任何外部圖片／圖示庫，純以座標數學（圓形距離判斷＋兩段線段的點到線段距離）程式產生像素，符合「不得抓取第三方檔案」。設計選擇「藍底＋白色打勾」呼應待辦事項「完成」語意，非強制規格，如需更換視覺純屬美感範疇。
  - **只加 `<link rel="icon">` 一行，不改其他 head 內容**：任務卡 outputs 註明「僅在需要加 `<link rel="icon">` 時」修改 index.html，符合最小變更；未加 `sizes`／`type` 屬性，因單一 32×32 ICO 不需要區分。
- 驗證方式（接手者如何確認我做的是對的：指令 + 我實際得到的輸出）：
  ```bash
  cd "C:/Users/excal/Projects/Claude/soft-dev-team-260919-wt/T-0043"
  node -e "const b=require('fs').readFileSync('public/favicon.ico');console.log(b.length, b[0], b[1], b[2], b[3])"
  ```
  ```text
  4286 0 0 1 0
  ```
  ```bash
  # 起 compose（PORT=8083／POSTGRES_HOST_PORT=5435）
  cp .env.example .env
  sed -i "s/^PORT=8080/PORT=8083/" .env
  sed -i "s/^# POSTGRES_HOST_PORT=5432/POSTGRES_HOST_PORT=5435/" .env
  docker compose -p t0043 up -d --build && docker compose -p t0043 ps
  ```
  ```text
  NAME          IMAGE                COMMAND                   SERVICE   CREATED         STATUS                   PORTS
  t0043-app-1   t0043-app            "docker-entrypoint.s…"   app       7 seconds ago   Up Less than a second    0.0.0.0:8083->8083/tcp
  t0043-db-1    postgres:16-alpine   "docker-entrypoint.s…"   db        7 seconds ago   Up 6 seconds (healthy)   0.0.0.0:5435->5432/tcp
  ```
  ```bash
  curl -sS -o /dev/null -w "%{http_code}\n" http://localhost:8083/favicon.ico
  curl -u dev:dev -sS -o /dev/null -w "%{http_code} %{content_type}\n" http://localhost:8083/favicon.ico
  ```
  ```text
  401
  200 image/vnd.microsoft.icon
  ```
  ```bash
  export BASE_URL=http://localhost:8083
  for i in 1 2 3 4 5; do npx playwright test -c tests/e2e/playwright.config.ts \
    --project=chromium-1280x800 --project=chromium-390x844 \
    --project=msedge-1280x800 --project=msedge-390x844 -g "TC-009"; done
  ```
  ```text
  RUN 1: 4 passed (5.6s)
  RUN 2: 4 passed (4.4s)
  RUN 3: 4 passed (4.9s)
  RUN 4: 4 passed (4.8s)
  RUN 5: 4 passed (4.5s)
  ```
  ```bash
  npm run build && npm run lint
  ```
  ```text
  BUILD_EXIT=0
  LINT_EXIT=0
  ```
  ```bash
  npm run test:unit
  ```
  ```text
  ℹ tests 76
  ℹ pass 76
  ℹ fail 0
  TEST_EXIT=0
  ```
  ```bash
  # 收尾清理
  docker compose -p t0043 down -v
  rm -f .env
  docker ps --format "{{.Names}} {{.Ports}}"
  git status --short
  ```
  ```text
  （down -v 完成，容器/網路/volume 皆移除）
   M public/index.html
  ?? public/favicon.ico
  ```
- 下一步建議：
  - dev-tl：初審本卡，確認 `git diff --stat main...task/T-0043-favicon` 只含 `public/favicon.ico`、`public/index.html`、任務卡、本交接檔。
  - Leader／qa-lead：本卡與 T-0038 合併推送後，於 staging 重跑 TC-067／TC-009（含 msedge）確認 favicon 404 不再干擾，據此正式判斷 D-017 是否關閉（此為上游卡的下游動作，本卡僅解除 favicon 404 這一個干擾因素）。
- 需要 Leader 裁決的事：
  - 無（MIME type 別名一事已在「假設與決策」表態並給出替代方案，非阻塞性爭議）。
