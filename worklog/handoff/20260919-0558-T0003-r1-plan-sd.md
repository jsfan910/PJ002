# T-0003 r1 — plan-sd（審核）— 工作紀錄與交接

<!-- 檔名：worklog/handoff/20260919-0558-T0003-r1-plan-sd.md（開工時間，實查系統時間 date "+%Y%m%d-%H%M"） -->

## A. 開工紀錄

- 開工時間：2026-09-19T05:58:23+08:00
- 角色／模型：plan-sd / Claude Opus 5（1M context）
- 任務卡：tasks/T-0003-sa.md（round 1）
- **本次工作性質：審核 T-0003（系統分析書 SA）**，依 `.claude/skills/review-round/SKILL.md`。本人為該卡 `reviewer`。
- 開工前置檢查：`tasks/T-0003-sa.md` frontmatter `status: review`、`reviewer: plan-sd`、`round: 1`，符合審核條件。審核紀錄表為空（本輪為第一列）。
- 讀取的輸入（實際讀過的檔案與段落）：
  - `tasks/T-0003-sa.md`（全檔）—— 取得 5 條 acceptance 與「驗收方式」：審核者須**試著從領域模型畫 ERD、從 UC 列出 API 端點**，畫不出來即為退回點；並核對 traceability UC 欄無孤兒 US。
  - `worklog/handoff/20260919-0546-T0003-r1-plan-sa.md`（全檔，含 B 段全部驗證指令與 9 條假設與決策）—— 取得待重跑的指令清單與待表態的假設清單。
  - `docs/specs/02_系統分析書_SA.md`（全檔 739 行）—— 七章 ＋ 附錄 A/B。重點：第 2 章 14 則 UC 的「輸入／輸出」欄（端點推導來源）、第 3.1 節屬性表（ERD 推導來源）、第 5 章 32 條 BR、第 7 章 O-001~O-010。
  - `docs/specs/traceability.md`（全檔）—— UC 欄 13 列已填、孤兒檢查前兩項已成立。
  - `tasks/E-001-todo-app.md#Leader 裁決紀錄`（全檔）—— Q-001~Q-011 裁決、以及 2026-09-19T05:57:38+08:00 對 O-001~O-010 的裁決（O-005 健康檢查豁免；O-009 Gate 2 採 24 小時；O-001~O-004、O-006~O-008 接受 plan-sa 保守假設為預設，特別 O-004 同源、O-007 UUID）。此份裁決晚於 SA 完工，故**需確認 SA 內容與裁決無衝突**。
  - `docs/specs/01_需求規格書_SRS.md`（AC-009-1~6、AC-010-1~5、NFR-001~008 表、外部介面表）—— 用於反查 SA 是否擴大或縮小範圍。
  - `.claude/agents/plan-sd.md`（全檔）—— 「審核他人時（審 SA）」：試著從領域模型畫 ERD、從 UC 列端點，畫不出來即退回。
  - `.claude/skills/review-round/SKILL.md`、`.claude/skills/handoff/SKILL.md`（全檔）—— 審核步驟與判定規則。
- 上一輪交接摘要（若 round > 1）：無（round 1）。
- 本輪計畫（3–7 條）：
  1. 重跑 plan-sa 交接檔 B 段的全部驗證指令，比對輸出是否可重現（不採信「通過」二字）。
  2. 逐條判定 5 條 acceptance，附我自己跑出的證據。
  3. 依角色定義做**實質審核**：從第 3 章領域模型實際畫一次 ERD、從 14 則 UC 實際列一次 API 端點表，記錄畫不出／列不出之處。
  4. 對 plan-sa 交接檔「假設與決策」9 條逐條表態（接受／不接受 ＋ 理由），並比對 Leader 2026-09-19T05:57:38+08:00 的裁決。
  5. 判定 done／rework，更新任務卡 frontmatter 與審核紀錄表，commit。
- 預期產出：
  - `worklog/handoff/20260919-0558-T0003-r1-plan-sd.md`（本檔）
  - `tasks/T-0003-sa.md`（status 與審核紀錄表一列）

## B. 交接

- 完工時間／狀態：2026-09-19T06:02:11+08:00 / 審核完成，判定 **done**
- 完成事項：
  - 重跑 plan-sa 交接檔 B 段全部 5 組驗收指令 ＋ 追加檢查，輸出**全部可重現**（見下方「驗證方式」）。
  - 逐條判定 5 條 acceptance：**5/5 通過**。
  - 完成角色專屬的實質審核：**ERD 畫得出來**（2 實體、1 關聯、全部型別／必填／唯一性／不變量齊備）；**端點列得出來**（14 則 UC 全部可對應到模組與端點，無一則因資訊不足而卡住）。
  - 對 plan-sa 交接檔 9 條「假設與決策」逐條表態：**9 條全部接受**，且與 Leader 2026-09-19T05:57:38+08:00 的裁決無衝突。
  - `tasks/T-0003-sa.md`：`status: review` → `done`、`updated` 更新、審核紀錄表加一列。
- 產出物（路徑）：
  - `worklog/handoff/20260919-0558-T0003-r1-plan-sd.md`（本檔）
  - `tasks/T-0003-sa.md`（frontmatter ＋ 審核紀錄表）
- 未完成／已知問題：
  - 三項**小瑕疵**（不構成退回，已轉為 T-0004 的設計待辦，見「下一步建議」）：
    1. BR-005「依 createdAt 由新到舊排序」未定義**同秒並列時的次序**；SA 的時間範例 `2026-09-19T05:29:41Z` 為秒精度。500 筆待辦（NFR-007）若以腳本批次建立極易同秒碰撞，測試斷言會不穩定。→ 由我在 T-0004 的 DB 設計以「時間戳存毫秒（含）以上精度 ＋ 以 id 為次要排序鍵」收斂，不回退 SA。
    2. AC-009-5 的 Given 前提是「前端與 API 部署於**不同來源**」，而 Leader 對 O-004 裁決為「前端與 API **同源**」。兩者不是矛盾（同源時該 AC 的前提不成立），但測試團隊會需要一個明確說法。→ 由我在 T-0004 的 SD 第 6 章與 ADR 明確寫「同源部署，不啟用萬用字元 CORS；若未來跨來源則設具名來源 ＋ allow-credentials」，並在 traceability 註明 AC-009-5 的驗證方式。此為設計層收斂，非 SA 缺陷。
    3. 健康檢查端點的**路徑名稱**未定（SA 只說「由 plan-sd 定，SRS 示例 `GET /healthz`」）。→ 本就屬 SD 職責，非缺漏。
  - `traceability.md` 的 `SD 模組`、`API 端點`、`TC-ID` 三欄仍為 `—`。依分工正常（前兩欄由本人於 T-0004 補）。
- 我做的假設與決策（含理由）：
  - **判定為 done 而非 rework**。理由：角色定義給我的退回判準是「從領域模型畫不出 ERD、從 UC 列不出端點」。我實際各做了一次（見下方 R-1、R-2 兩節），**沒有任何一處因 SA 資訊不足而卡住**；上列三項小瑕疵皆屬「SD 本來就該決定的事」或「精度層級的補強」，用退回換一輪成本不划算，改以 T-0004 的設計收斂處理，並在此留痕。
  - **對 UC 數 14 > US 數 13（US-010 拆為 UC-010／UC-011）表示接受**。理由：從我的設計視角，UC-010 推導出的是「部署與環境檢查清單」，UC-011 推導出的是「一個具體端點 ＋ 一條中介層豁免規則」，兩者的設計產物完全不同；合成一則我會漏掉豁免規則。拆開是對設計有利的。
  - **對 SA 第 3.1 節「明確不存在的欄位」清單表示接受並將據以自我約束**。理由：這正是擋住我在 T-0004 順手加 `completedAt`、`dueDate` 的護欄。唯 `updatedAt` 依 O-008 與 Leader 裁決可加，我在 T-0004 會加但**不放進 API 回應契約**。
  - **不修改 SA 任何內容**。理由：我的本卡權限只有審核判定與任務卡 frontmatter／審核紀錄表；SA 是 T-0003 的 outputs，非我的 outputs。三項小瑕疵以「T-0004 設計收斂」處理，不需要 SA 改版。
- 驗證方式（接手者如何確認我做的是對的：指令 + 我實際得到的輸出）：

  ### 重跑 1／5 —— 每個 US 至少對應一個 UC；每個 UC 有主要流程與至少一個例外流程

  ```bash
  cd /c/Users/excal/Projects/Claude/soft-dev-team-260919
  grep -nE "^### UC-0[0-9][0-9] " docs/specs/02_系統分析書_SA.md
  grep -cE "^### UC-0[0-9][0-9] " docs/specs/02_系統分析書_SA.md
  grep -c "^- 主要流程" docs/specs/02_系統分析書_SA.md
  grep -c "^- 替代／例外流程：" docs/specs/02_系統分析書_SA.md
  grep -cE "^  - E[0-9]" docs/specs/02_系統分析書_SA.md
  diff <(grep -oE "US-0[0-9][0-9]" docs/specs/01_需求規格書_SRS.md | sort -u) <(grep -oE "US-0[0-9][0-9]" docs/specs/02_系統分析書_SA.md | sort -u) && echo "DIFF-EMPTY-OK"
  ```
  ```text
  === UC list ===
  61:### UC-001 新增待辦
  85:### UC-002 檢視待辦清單
  105:### UC-003 編輯待辦標題
  128:### UC-004 刪除待辦
  150:### UC-005 切換完成／未完成
  171:### UC-006 依狀態篩選清單
  192:### UC-007 檢視待辦建立時間
  210:### UC-008 於單一頁面完成全部待辦操作（操作情境）
  234:### UC-009 以 REST API 完成全部待辦操作
  259:### UC-010 於 staging 環境驗收（操作情境）
  282:### UC-011 監測服務存活
  302:### UC-012 使用者註冊（P1）
  325:### UC-013 使用者登入與登出（P1）
  350:### UC-014 以使用者身分隔離存取待辦（P1）
  UC total: 14
  main flow lines: 15
  exception sections: 14
  exception branches: 51
  === US set diff SRS vs SA ===
  DIFF-EMPTY-OK
  ```
  審核判讀：與 plan-sa 交接檔所載數字**完全一致**（14／15／14／51／DIFF-EMPTY-OK）。「主要流程行數 15」的差額我實際核到 UC-013 第 333、338 行為「主要流程（登入）」與「主要流程（登出）」兩段，屬刻意拆寫非重複計數，接受其解釋。**通過。**

  ### 重跑 2／5 —— 領域模型涵蓋 SRS 所有名詞

  ```bash
  sed -n "268,280p" docs/specs/01_需求規格書_SRS.md | grep -oE "^\| [^|]+ \|" | sed "s/^| //;s/ |$//"
  sed -n "/### 3.3/,/### 3.4/p" docs/specs/02_系統分析書_SA.md | grep -cE "^\| "
  grep -oE "^\*\*(Todo|User)[^*]*\*\*" docs/specs/02_系統分析書_SA.md
  grep -oE "^\| (CompletionStatus|FilterOption|AuthToken|StagingAccessCredential)[^|]*\|" docs/specs/02_系統分析書_SA.md
  ```
  ```text
  --- SRS ch7 nouns ---
  名詞
  待辦（Todo）
  完成狀態
  建立時間
  清單頁
  篩選
  P0 / P1 / P2
  staging
  JWT
  資料隔離
  UAT
  --- SA 3.3 rows ---
  11
  --- entities ---
  **Todo（待辦）**
  **User（使用者）**
  --- value objects ---
  | CompletionStatus（完成狀態） |
  | FilterOption（篩選） |
  | AuthToken（JWT） |
  | StagingAccessCredential |
  ```
  審核判讀：SRS §7 為 10 個名詞（11 − 表頭），SA §3.3 表格列數 11（表頭＋分隔列＋… 實際逐一核對 10 個名詞全部有歸屬列）。我逐字比對 §3.3 的左欄與 SRS §7 的名詞清單，**10 個名詞一一對得上，無遺漏、無捏造**。**通過。**

  ### 重跑 3／5 —— 業務規則表每條有來源 US 與違反時行為

  ```bash
  grep -cE "^\| BR-0[0-9][0-9] \|" docs/specs/02_系統分析書_SA.md
  grep -E "^\| BR-0[0-9][0-9] \|" docs/specs/02_系統分析書_SA.md | awk -F"|" "{print NF-2}" | sort | uniq -c
  grep -E "^\| BR-0[0-9][0-9] \|" docs/specs/02_系統分析書_SA.md | awk -F"|" "{ if (\$4 ~ /^[[:space:]]*$/ || \$5 ~ /^[[:space:]]*$/) print \$2 }"
  ```
  ```text
  BR count: 32
  --- field count per row ---
       32 4
  --- rows missing source US or violation behavior ---
  (end)
  ```
  審核判讀：32 條全為完整 4 欄，無任何一列缺「來源 US」或「違反時行為」。我另人工抽驗 BR-011、BR-016、BR-017、BR-023 四條（設計上最關鍵者），四條的違反時行為皆寫到可直接轉為測試斷言的程度。**通過。**

  ### 重跑 4／5 —— 外部介面清單存在（至少列出前端↔API）

  ```bash
  sed -n "/^## 6. 外部介面清單/,/^## 7./p" docs/specs/02_系統分析書_SA.md | grep -cE "^\| "
  grep -c "單頁前端 ↔ REST API" docs/specs/02_系統分析書_SA.md
  ```
  ```text
  --- ch6 table rows ---
  8
  FE-API interface: 1
  ```
  審核判讀：8 − 2（表頭＋分隔列）＝ 7 條介面，首條即「單頁前端 ↔ REST API」。**通過。**

  ### 重跑 5／5 —— traceability.md UC 欄已填

  ```bash
  sed -n "/^| US-ID | US 標題/,/^$/p" docs/specs/traceability.md
  awk -F"|" "/^\| US-0/ { gsub(/ /,\"\",\$5); if (\$5 == \"—\") c++ } END { print c+0 }" docs/specs/traceability.md
  ```
  ```text
  | US-ID | US 標題 | 優先級 | UC-ID | SD 模組 | API 端點 | TC-ID | 狀態 |
  |---|---|---|---|---|---|---|---|
  | US-001 | 新增待辦 | P0 | UC-001 | — | — | — | 待 SD／測試填 |
  | US-002 | 檢視待辦清單 | P0 | UC-002 | — | — | — | 待 SD／測試填 |
  | US-003 | 編輯待辦 | P0 | UC-003 | — | — | — | 待 SD／測試填 |
  | US-004 | 刪除待辦 | P0 | UC-004 | — | — | — | 待 SD／測試填 |
  | US-005 | 切換完成／未完成 | P0 | UC-005 | — | — | — | 待 SD／測試填 |
  | US-006 | 依狀態篩選 | P0 | UC-006 | — | — | — | 待 SD／測試填 |
  | US-007 | 顯示建立時間 | P0 | UC-007 | — | — | — | 待 SD／測試填 |
  | US-008 | 單頁前端操作 | P0 | UC-008 | — | — | — | 待 SD／測試填 |
  | US-009 | REST API | P0 | UC-009 | — | — | — | 待 SD／測試填 |
  | US-010 | staging 部署 | P0 | UC-010、UC-011 | — | — | — | 待 SD／測試填 |
  | US-011 | 使用者註冊 | P1 | UC-012 | — | — | — | 待 SD／測試填 |
  | US-012 | 使用者登入與登出 | P1 | UC-013 | — | — | — | 待 SD／測試填 |
  | US-013 | 使用者資料隔離 | P1 | UC-014 | — | — | — | 待 SD／測試填 |

  --- UC col still dash count (should be 0) ---
  0
  ```
  審核判讀：13 列 UC 欄無一為 `—`，無孤兒 US。**通過。**

  ### 追加檢查 —— AC 集合與章節完整性

  ```bash
  diff <(grep -oE "AC-0[0-9][0-9]-[0-9]" docs/specs/01_需求規格書_SRS.md | sort -u) <(grep -oE "AC-0[0-9][0-9]-[0-9]" docs/specs/02_系統分析書_SA.md | sort -u) && echo "AC-DIFF-EMPTY-OK"
  grep -nE "^## [0-9]\. " docs/specs/02_系統分析書_SA.md
  grep -c "^\`\`\`mermaid" docs/specs/02_系統分析書_SA.md
  ```
  ```text
  --- AC set diff SRS vs SA ---
  AC-DIFF-EMPTY-OK
  --- SA chapters ---
  24:## 1. 使用案例總覽
  56:## 2. 使用案例描述
  372:## 3. 領域模型
  490:## 4. 資料流程
  631:## 5. 業務規則
  673:## 6. 外部介面清單
  687:## 7. 未決事項（交給 SD 或升級）
  mermaid: 6
  ```
  審核判讀：SA 提及的 AC 集合與 SRS 的 62 條完全相同（既無遺漏亦無捏造 AC）；模板七章齊全；6 張圖。**通過。**

  ### R-1 角色專屬審核（一）：**實際從第 3 章領域模型畫一次 ERD**

  這是本卡「驗收方式」指定的退回判準。我不是看一眼就算，而是把 ERD 真的畫出來，畫的過程中卡住的地方才是退回點。

  ```text
  Todo                                        User (P1)
  ------------------------------------        ------------------------------------
  PK  id            UUID       NOT NULL  <--  PK  id            UUID       NOT NULL
      title         VARCHAR(200) NOT NULL         email         VARCHAR    NOT NULL  UNIQUE
                    CHECK(len(trim(title)) BETWEEN 1 AND 200)  password_hash VARCHAR  NOT NULL
      is_completed  BOOLEAN    NOT NULL DEFAULT false           created_at    TIMESTAMPTZ NOT NULL
      created_at    TIMESTAMPTZ NOT NULL  (immutable)
  FK  owner_id      UUID       NULL(P0) / NOT NULL(P1) -> User.id

  關聯：User 1 ---- 0..* Todo （owns，P1 起）
  不再建表：TodoList（SA §3.4 明示為查詢結果的名字，非持久化實體）—— 此句省掉我一張多餘的表。
  ```

  **逐欄可推導性檢查（畫得出來／畫不出來）：**

  | ERD 需要的資訊 | SA 有沒有給 | 出處 |
  |---|---|---|
  | 有哪些表 | ✅ 2 張（Todo、User）；並明示 TodoList 不建表 | §3.1、§3.4 |
  | 每欄型別 | ✅ 逐欄給（字串／布林／時間戳／識別碼） | §3.1 屬性表「型別」欄 |
  | 每欄可否為空 | ✅ 逐欄給，且 `ownerId` 明確標「P0 不存在／P1 起必填」 | §3.1「必填」欄 |
  | 預設值 | ✅ `isCompleted` 預設 false | §3.1、BR-006 |
  | 長度／值域約束 | ✅ title 去空白後 1~200 | §3.1、BR-001、BR-002、I-1 |
  | 唯一性約束 | ✅ `User.email` 全系統唯一（I-5） | §3.1、§3.4、BR-024 |
  | 不可變欄位 | ✅ `createdAt` 一經寫入不再改變（I-2、BR-003） | §3.4 |
  | 外鍵與基數 | ✅ `User 1 --> 0..* Todo`，I-4 每筆 Todo 恆有且僅有一個 ownerId | classDiagram、§3.4 |
  | 主鍵型別 | ✅ 由 O-007 指定為「不可猜測識別碼（UUID）」，Leader 已裁決採此預設 | §7 O-007 ＋ Epic 裁決 |
  | 列舉值域 | ✅ FilterOption 固定三值、CompletionStatus 兩態 | §3.2 |
  | 不該有的欄位 | ✅ 明列 description／completedAt／dueDate／priority／tags／deletedAt 皆不存在 | §3.1 |
  | 軟刪除或硬刪除 | ✅ 硬刪除，無 deletedAt | BR-008 |
  | **同秒排序次序（tie-break）** | ⚠️ **未定義**（見「未完成／已知問題」第 1 點） | BR-005 |

  **結論：ERD 畫得出來，13 項只有 1 項是精度層級的補強，且該項本來就落在 DB 設計（我的職責）。無退回點。**

  ### R-2 角色專屬審核（二）：**實際從 14 則 UC 列一次 API 端點**

  同上，這是把端點表真的列出來，列不出來的才是退回點。（下表為審核過程的推導結果，正式定案版見 T-0004 的 `docs/specs/04_API規格.yaml`。）

  | UC | 能推出的端點（能力層） | 推導依據（SA 的哪一欄） | 卡住了嗎 |
  |---|---|---|---|
  | UC-001 | 建立待辦：輸入 title → 輸出 todo，201 | §2 UC-001「輸入／輸出」＋ UC-009 第 7 步狀態碼 | 否 |
  | UC-002 | 查詢清單：無輸入 → 待辦集合，200 | UC-002「輸入／輸出」 | 否 |
  | UC-003 | 更新標題：id ＋ 新 title → 更新後 todo，200；BR-012 僅 title 可改 | UC-003「輸入／輸出」＋ BR-012 | 否 |
  | UC-004 | 刪除：id → 無內容；O-001 裁決為 **204** | UC-004 ＋ O-001 ＋ Epic 裁決 | 否 |
  | UC-005 | 變更完成狀態：id ＋ **目標狀態**（O-002 裁決為設定目標狀態，冪等） | UC-005「輸入／輸出」＋ O-002 ＋ Epic 裁決 | 否 |
  | UC-006 | 清單查詢的 status 篩選參數，三值，預設全部；O-003 裁決為**後端查詢** | UC-006 ＋ BR-010 ＋ O-003 | 否 |
  | UC-007 | 不需獨立端點：createdAt 隨清單／單筆回應一併回，ISO 8601 含時區 | UC-007「輸入／輸出」＋ BR-004 | 否 |
  | UC-008 | 不需 API 端點：對應前端靜態頁與前端狀態機（載入中、錯誤、篩選標示） | UC-008 主要流程 ＋ BR-028~BR-031 | 否 |
  | UC-009 | 查詢單筆：id → todo，200／404。**AC-009-1 明文含「查詢單筆」**，非 SA 自行加碼 | UC-009 第 3 步 ＋ SRS AC-009-1 | 否 |
  | UC-010 | 不需 API 端點：對應部署與環境檢查清單（HTTPS 導向、資料跨部署存續、README 可重現） | UC-010 主要流程 1~6 | 否 |
  | UC-011 | 健康檢查端點：無輸入 → 200；**唯一豁免 Basic Auth 的路徑** | UC-011 ＋ BR-017 ＋ O-005（Leader 已裁決豁免） | 否 |
  | UC-012 | 註冊（P1）：email ＋ password → 201 | UC-012「輸入／輸出」＋ BR-024 | 否 |
  | UC-013 | 登入（P1）：email ＋ password → JWT 24h；登出為用戶端清除（O-006 裁決無撤銷清單） | UC-013「輸入／輸出」＋ BR-021 ＋ O-006 | 否 |
  | UC-014 | 不需新端點：為全部待辦端點的授權橫切（先以身分過濾再套狀態篩選，BR-022） | UC-014 主要流程 2~3 ＋ BR-022、BR-023 | 否 |

  **另檢查端點設計必備的橫切資訊是否齊備：**

  | 設計必備 | SA 有沒有給 | 出處 |
  |---|---|---|
  | 錯誤回應結構 | ✅ 統一 JSON，至少含錯誤代碼與訊息 | BR-014 |
  | 各狀態碼語意 | ✅ 201／200／204／400／401／404／5xx 全給 | UC-009 第 7 步、BR-009、BR-016、BR-020 |
  | 內容型別 | ✅ `application/json` | UC-009、§6 |
  | 認證方式（P0／P1） | ✅ P0 Basic Auth 擋整站、P1 JWT | BR-016、BR-020 |
  | 錯誤訊息的洩漏禁令 | ✅ 不得含堆疊／SQL／內部路徑 | BR-026 |
  | CORS 規則 | ✅ 不得用 `*`；同源為 Leader 裁決的預設 | BR-015、O-004 ＋ Epic 裁決 |

  **結論：14 則 UC 全部列得出端點或明確歸屬到非端點模組（前端／部署），橫切資訊齊備。無退回點。**

  ### R-3 對 plan-sa 交接檔「假設與決策」9 條的逐條表態

  | # | plan-sa 的假設／決策 | 我的表態 | 理由 |
  |---|---|---|---|
  | 1 | US-010 拆為 UC-010（部署驗收）與 UC-011（存活監測） | **接受** | 兩者推導出的設計產物不同（環境檢查清單 vs 端點＋中介層豁免規則）；合寫我會漏掉豁免規則 |
  | 2 | UC-008／UC-010 以「操作序列 ＋ 環境前置條件 ＋ 可觀察狀態」描述 | **接受** | 我確實能從 UC-008 推出前端狀態機（載入中／錯誤／篩選標示／不整頁跳轉），從 UC-010 推出部署檢查清單。硬套資料流反而拿不到這些 |
  | 3 | 新增 BR-011（狀態變更後一律以目前生效篩選重新計算清單） | **接受** | 這條直接決定了前端變更後的重新渲染策略，且與 O-003「篩選在後端」相容（後端為事實來源，前端可即時過濾） |
  | 4 | BR-017 假設健康檢查端點豁免全站 Basic Auth | **接受** | Leader 已於 2026-09-19T05:57:38+08:00 正式裁決 O-005 為「豁免」，與此假設一致。我在 SD 第 6 章會明列其為**唯一未保護路徑** |
  | 5 | 領域模型明列「刻意不存在的欄位」 | **接受** | 這是擋住我自己擴大範圍的護欄。唯 `updatedAt` 依 O-008 與 Leader 裁決可加，我會加但不放進 API 回應契約 |
  | 6 | 第 7 章每項未決事項都給保守假設而非只列問題 | **接受** | Leader 已用「接受 plan-sa 保守假設為預設，改採他法須寫 ADR」把它制度化。這讓 T-0004 不需回頭問任何一題 |
  | 7 | O-001 假設刪除回 204、跨使用者存取回 404 | **接受** | 204 語意最精確；404 不洩漏識別碼是否存在。Leader 已裁決採此預設，我在 04_API規格.yaml 寫死這兩個值 |
  | 8 | O-007 假設識別碼採 UUID | **接受** | Leader 已明文裁決 O-007 UUID。P0 只有一層共享 Basic Auth，自增整數可被逐號列舉 |
  | 9 | 不動 SRS 與 01a（01a 狀態欄同步留給 plan-ba 的 T-0005） | **接受** | 符合「不改本卡 outputs 以外的檔案」；且 SA 第 5 章已內嵌裁決值，下游不需回查 01a |

  **9 條全部接受，且與 Leader 裁決無任何衝突。**

  ### 逐條核對任務卡 acceptance 的審核結論

  | # | acceptance | 審核結果 | 我的依據（自己跑出來的） |
  |---|---|---|---|
  | 1 | 每個 US 至少對應一個 UC；每個 UC 有主要流程與至少一個例外流程 | **通過** | 重跑 1：14 則 UC、14/14 有例外流程段（51 個分支）、US 集合 `DIFF-EMPTY-OK` |
  | 2 | 領域模型涵蓋 SRS 所有名詞（Todo、User 等） | **通過** | 重跑 2 ＋ R-1：SRS §7 的 10 個名詞於 §3.3 逐一歸屬；ERD 13 項必要資訊 12 項齊備，缺的 1 項屬 DB 設計精度 |
  | 3 | 業務規則表每條有來源 US 與違反時行為 | **通過** | 重跑 3：32 條全 4 欄無空欄；人工抽驗 BR-011／016／017／023 可直接轉測試斷言 |
  | 4 | 外部介面清單存在（至少列出前端↔API） | **通過** | 重跑 4：7 條介面，首條即前端↔REST API |
  | 5 | traceability.md UC 欄已填 | **通過** | 重跑 5：13 列 UC 欄無一為 `—` |

  **總判定：5/5 通過 ＋ ERD 畫得出來 ＋ 端點列得出來 → `status: done`。**

- 下一步建議：
  - **給我自己（T-0004，下一棒）**——本輪審核中發現、必須在 SD／DB／API 收斂的三件事：
    1. **排序 tie-break**：DB 的 `created_at` 採毫秒（含）以上精度，清單排序為 `ORDER BY created_at DESC, id DESC`，寫進 `05_資料庫設計.md` 與 `04_API規格.yaml` 的行為說明，讓測試斷言穩定（對應 BR-005、NFR-007 的 500 筆情境）。
    2. **CORS 與 AC-009-5**：SD 第 6 章與 ADR 明寫「前端與 API 同源（O-004 Leader 裁決），因此無跨來源請求；CORS 不設萬用字元，若未來需跨來源則設具名來源 ＋ allow-credentials」。並在 traceability 註明 AC-009-5 的驗證方式（同源下以「無 `Access-Control-Allow-Origin: *`」為判準）。
    3. **健康檢查路徑**：定名並在 SD 第 6 章明列為**唯一未保護路徑**（Leader 對 O-005 的裁決明文要求「明列」），部署設計中同步標註中介層／反向代理的豁免規則。
  - **給 plan-ba（T-0005）**：孤兒檢查第三項「無對應模組／端點的 US」將於 T-0004 後成立；另 Leader 已裁決 O-009，NFR-003 的量測方式需改寫為「Gate 2：24 小時採樣，每 5 分鐘一次，成功率 ≥ 99%；正式環境目標：連續 7 天」。
  - **給測試團隊**：O-001 已由 Leader 裁決收斂為單一值（刪除 204、跨使用者 404），可直接寫死斷言，不需再等 SD。
- 需要 Leader 裁決的事：**無**。plan-sa 上輪提出的兩項（O-005 健康檢查豁免、O-009 監測期）Leader 已於 2026-09-19T05:57:38+08:00 裁決完畢，本輪審核未新增需裁決事項。
