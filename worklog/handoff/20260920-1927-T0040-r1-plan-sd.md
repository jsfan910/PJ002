# T-0040 r1 — plan-sd — 工作紀錄與交接

<!-- 檔名：worklog/handoff/20260920-1927-T0040-r1-plan-sd.md（時間 = 開工時間，取實查系統時間） -->

## A. 開工紀錄

- 開工時間：2026-09-20T19:27:03+08:00
- 角色／模型：plan-sd / opus
- 任務卡：tasks/T-0040-spec-change-06-cron-301.md（round 1）
- 讀取的輸入（實際讀過的檔案與段落，非任務卡照抄）：
  - tasks/T-0040-spec-change-06-cron-301.md —（本卡 outputs 三檔、acceptance 五條、驗收指令四條；`depends_on` 已由 Leader 於 2026-09-20 解除對 T-0039 的依賴，改以 main 現況為準）
  - docs/specs/06_部署架構與CICD.md（全文 662 行）—（§2 資源表第 2 列「Google 前端 301 導向 HTTPS」、§2 WIF 列「授三個角色」、§3.2 deploy 列 `--set-secrets ...`、§3.2.1 六步設定末段「授服務帳號三個角色」、§3.3 cron 定位、§4.1 註「需額外授 secretmanager.secretAccessor」、§6.1 表「備援來源」兩列、§6.8 起源、§6.9／§6.9.1 T-0039 實作與 IAM 根因、§7 參數表「機密（Secret Manager 參照）」列仍寫 `:latest`、文末變更紀錄表三列）
  - docs/specs/adr/ADR-0005-雲端平台-CloudRun.md —（決定第 7 點「HTTP 請求由 Google 前端以 301 導向 HTTPS」；決定第 6 點保溫段落；後果「對其他文件的影響」）
  - docs/specs/24_缺陷清單.md#D-016 —（closed，規格文字待修；Leader 2026-09-19T17:30:10 裁決①接受 302 等效；文字建議「3xx（301 或 302 皆視為合規）且 Location 為對應的 https:// 網址」）
  - docs/reports/20260920-1806-測試總結-E001-r3.md#裁決事項 C、§7.2、R-13 —（24 小時窗內 cron `event=schedule` 實跑 8 次、理論約 258 次、達成率約 3%、8 次 conclusion 全 success；建議選項 1）
  - tasks/E-001-todo-app.md#2026-09-20T18:18:13+08:00 —（Leader 採選項 1：Gate 2 後開規格變更小卡修 06 §6.1）
  - docs/specs/01_需求規格書_SRS.md#NFR-002 —（`grep -n "301"` 無輸出；原文寫「觀察 3xx 導向」，**本卡不需修改 SRS**）
  - docs/specs/03_系統設計書_SD.md:308 —（NFR-002① 寫「HTTP 301 導向 HTTPS」與「`curl -sSI` 觀察 301」，**不在本卡 outputs**，列入下一步建議）
  - docs/specs/20_測試案例.md:156（TC-079）、:330 —（判準寫死 301，依任務卡 acceptance 第 5 條本卡不動，留給 qa-lead）
  - .github/workflows/deploy-staging.yml:84-124,194、scripts/deploy-staging.sh:52-112、infra/cloudrun-service.yaml:74-93 —（main 現況：三處皆已改為版本釘定；workflow 與腳本以 `resolve_version()` 查 `ENABLED` 最新版本，可由 `SECRET_VERSION_*` 覆寫；IaC 檔 `secretKeyRef.key` 為 `"<SECRET_VERSION>"` 佔位符）
  - worklog/handoff/20260920-1902-T0039-r2-dev-ops.md、docs/specs/06 §6.9.1 —（r1 真實 run 35506278351 紅燈根因：`github-deployer` 缺 `secretmanager.versions.list`，該權限只在 `roles/secretmanager.viewer`；`roles/secretmanager.secretAccessor` 只含 `secretmanager.versions.access`）
- 上一輪交接摘要（若 round > 1）：不適用（round 1）
- 本輪計畫（3–7 條）：
  1. 06 §6.1／§3.3：cron 定位由「備援來源」改為「保溫與人工抽查用，不具備援能力」，寫明理由（實測 8 次／理論 258 次）；NFR-003 主要來源明寫 uptime check；提出「第二個 uptime check 作為真備援」的建議與代價供 Leader 裁決，不自行決定。
  2. 06 §3.2／§4／§7：`--set-secrets` 由 `:latest` 改為版本釘定敘述（版本號來源、STEP_SUMMARY 記錄、`SECRET_VERSION_*` 覆寫變數名稱），與 main 現況一致。
  3. 06 §2 資源表：「301 導向」改「3xx（實測 302）」；WIF 列服務帳號角色清單補 `roles/secretmanager.viewer`（Leader 追加項），並說明用途；§3.2.1 六步設定與 §4.1 註同步。
  4. ADR-0005 決定第 7 點加附註：實測 302，判準改「3xx 且 Location 為 https」，引用 D-016 與 Leader 2026-09-19T17:30:10 裁決。
  5. 06 frontmatter `version: 0.3`、`updated` 更新；文末變更紀錄新增一列（含本卡編號與三項變更）。
  6. SRS 經實查無「301」字面，不修改；於本檔記錄證據。
  7. 執行任務卡「驗收方式」四條 grep／sed 指令，貼真實輸出進 B 段。
- 預期產出：docs/specs/06_部署架構與CICD.md（version 0.3）、docs/specs/adr/ADR-0005-雲端平台-CloudRun.md、本交接檔。

## B. 交接

- 完工時間／狀態：2026-09-20T19:33:47+08:00 / review
- 完成事項：
  - **【acceptance 1】cron 定位改寫**：`06` §3.3 標題由「NFR-003 的量測與保溫」改為「**保溫與人工抽查（不是 NFR-003 的量測來源，也不是備援來源）**」，全節重寫並新增「定位」與「為什麼改口」兩段，載明實測依據（T-0031 首次診斷 2h12m／0 次；T-0037 24 小時窗 **8 次／理論約 258 次，約 3%，8 次皆 success**）與成因（GitHub 平台排程延遲，無可修設定缺陷，見 §6.8.1）。§6.1 前言與監控表第 2 列同步（該列門檻欄改為「無門檻，樣本不進入 NFR-003 分母」）。§6.1 明寫 **NFR-003 正式且唯一量測來源為 Cloud Monitoring uptime check `todo-app-health`**。
  - **【acceptance 1】備援建議交 Leader，不自行決定**：新增 `06` §6.1.1「備援缺口與告警缺口」，以表格列出三個選項與代價——**A 新增第二個 uptime check**（Cloud Monitoring uptime check 每計費帳戶每月 100 萬次執行免費額度，現有 1 check × 4 地區 × 5 分鐘 ≈ 3.5 萬次／月，加一個仍為 US$0；代價為告警重複與多一個資源）、**B 定期人工核對**、**C 接受單點**；寫明 plan-sd 傾向 A 但**明示為傾向而非決定**。
  - **【acceptance 1 附帶】告警缺口一併提列**：§6.3 首條補「⚠️ T-0040 更正：此告警機制實質已失效」——cron 一天 8 次，「連續 3 次取樣全失敗」幾乎不被求值，**現況等同沒有自動告警**；§6.1.1 末段同步更正原文「`::error::` ＋ GitHub 寄信已經滿足」的前提。原文保留不刪。
  - **【acceptance 2】secret 版本釘定**：§3.2 表新增 `resolve secret versions` 階段列（含 `gcloud secrets versions list --filter="state=ENABLED" --sort-by="~createTime" --limit=1 --format="value(name)"` 全指令、寫入 `$GITHUB_STEP_SUMMARY`、`SECRET_VERSION_DATABASE_URL`／`SECRET_VERSION_BASIC_AUTH_USER`／`SECRET_VERSION_BASIC_AUTH_PASS` 三個覆寫變數名稱、查不到 ENABLED 版本即中止不回退 `:latest`）；原 `deploy` 與 `verify` 兩列依 T-0039 實作合併為單一「deploy ＋ verify」列並載明**一次自動重試**的觸發條件（④ 失敗才重試、③ 失敗不重試、上限 1 次無迴圈）；新增「為什麼 secret 參照要釘具體版本」整段（與映像 SHA 標籤同一邏輯、代價、緊急略過路徑）；§4.1 註改寫；§4.2 表補三個 `SECRET_VERSION_*` repository variables（標註為 variable 非 secret，因只是版本號）。
  - **【acceptance 2＋Leader 追加②】§7 三處參數同步表**「機密（Secret Manager 參照）」列由三處 `:latest` 改寫為版本釘定敘述，並在「現況」欄說明三處差異（workflow／腳本執行時查、IaC 檔留 `"<SECRET_VERSION>"` 佔位符由人套用前替換）**是本質差別而非飄移**；§7 維護規則補「第四處要一起檢查的是本文件」。
  - **【acceptance 2】§2 資源表 3xx**：「HTTP 請求由 Google 前端 301 導向 HTTPS」改為「以 **3xx（實測 302 Found）** 導向 HTTPS，**判準為『回 3xx 且 `Location` 為對應的 `https://` 網址』，不寫死單一狀態碼**」。
  - **【acceptance 3】ADR-0005 決定第 7 點附註**：載明 qa-at r3（2026-09-19T17:19）與 qa-lead T-0030（2026-09-19T17:32）兩次獨立實測皆為 `HTTP/1.1 302 Found`、判準改為 3xx＋`Location` 為 https、引用 D-016 與 **Leader 2026-09-19T17:30:10 裁決①**，並補「為什麼不改成寫死 302」的理由（狀態碼是不受團隊控制的平台行為，規格不對實作細節做承諾）與連帶影響清單。
  - **【Leader 追加①】`06` 新增 §2.1「部署服務帳號角色清單」**：服務帳號角色由**三個補列為五個**，逐項寫「用途（pipeline 哪一步需要）」與「少了會怎樣」；新增的 `roles/secretmanager.viewer` 對應 `secretmanager.versions.list`，並貼出 §6.9.1 已實查的權限差異（`secretAccessor` 只含 `secretmanager.versions.access`）與真實紅燈 run `35506278351`；附授權迴圈指令、核對指令、「**既有專案要補跑一次**」說明、以及「為什麼不自訂角色」的取捨。§2 資源表 WIF 列、§3.2.1 六步設定、§4.1 註三處同步改為指向 §2.1（單一事實來源）。
  - **【acceptance 4】** `06` frontmatter `version: 0.2 → 0.3`、`updated: 2026-09-20T19:27:03+08:00`；文末變更紀錄新增 0.3 一列（含本卡編號 T-0040、三項主變更＋兩項 Leader 追加、以及「未改動」四項的理由）。該列已以 awk 移至表尾維持時間順序。
  - **【acceptance 5】不改 `20_測試案例.md`**：TC-079 判準文字（`20_測試案例.md:156`、`:330`）未動，留給 qa-lead，已列下一步建議。
  - **ADR-0005 決定第 6 點**另補一則附註（保溫來源的前提更正），理由見「我做的假設與決策」第 3 點。
- 產出物（路徑）：
  - `docs/specs/06_部署架構與CICD.md`（version 0.3，733 行）
  - `docs/specs/adr/ADR-0005-雲端平台-CloudRun.md`（131 行，決定第 6、7 點各加一則附註）
  - `worklog/handoff/20260920-1927-T0040-r1-plan-sd.md`（本檔）
  - **未修改**：`docs/specs/01_需求規格書_SRS.md`（實查無「301」字面，見下方驗證）
- 未完成／已知問題：
  - **`03_系統設計書_SD.md:308`（§7 NFR-002①）仍寫「HTTP 301 導向 HTTPS」與驗證方式「`curl -sSI http://<staging>` 觀察 301」**——該檔**不在本卡 outputs**，依協作協定「不改 outputs 以外的檔案」未動。目前 `06`／ADR-0005 與 SD 之間存在狀態碼字面落差，需另開卡或由 Leader 併入下一張規格同步卡。已列下一步建議與需 Leader 裁決事項。
  - `20_測試案例.md` TC-079（:156）與 NFR-002 對照表（:330）判準仍寫死 301，依任務卡 acceptance 第 5 條刻意不動，待 qa-lead 於下一輪測試計畫同步卡處理。
  - §6.1.1 的三個選項**未做決定**，等 Leader 裁決；在裁決前，NFR-003 的量測與告警都是單點且沉默失效。
- 我做的假設與決策（含理由）：
  1. **`06` §6.8（T-0031 診斷紀錄）與 §6.6／§6.7／§6.9（實作紀錄）的歷史文字一律不改寫，只在 §6.8 開頭加一則「本節『降為備援』一語已由 T-0040 取代」的指向**。理由：這些節記錄的是當時的判讀與事件脈絡，改寫等於湮滅紀錄；但若完全不註記，讀者會在同一份文件讀到兩種定位。折衷是「歷史保留＋現行定位單一指向 §3.3／§6.1」。同一原則也套用在 §6.3 的告警條（保留原文，於其下加更正）。
  2. **`06` §6.9 與 §6.9.1 中「留待 T-0040 一併調整」的兩處待辦字句，改為「已於 T-0040（version 0.3）同步完成」**。這兩句是指向本卡的待辦標記，本卡完成後不更新就會變成假待辦。屬本卡 outputs 範圍內（同一檔）。
  3. **ADR-0005 決定第 6 點也加了一則附註**（任務卡 outputs 只寫「決定第 7 點附註」）。理由：第 6 點寫著「NFR-003 的量測工作流本身即為保溫來源」，這個前提在 cron 降級後已不成立；只改第 7 點會讓同一份 ADR 內部自相矛盾，而該附註仍在 outputs 所列的同一個檔案內、且不改動決定本身（結論「不引入 Cloud Scheduler」維持不變），只更正其理由。**若 Leader 認為逾越範圍，可退回本項單獨移除，不影響其餘變更。**
  4. **判準寫成「3xx＋`Location` 為 https」而非「302」**。理由：狀態碼是 Google 前端的行為，團隊零程式碼可改也無平台承諾；寫死任何單一碼都是讓規格對不受控的實作細節做承諾。此與 D-016 的文字建議一致。
  5. **`roles/secretmanager.viewer` 採預定義角色而非自訂角色**，並在 §2.1 寫明取捨（可維護性優先，若導入 prod 須重評）。理由：自訂角色的權限清單會隨 GCP API 演進失效，本案為 staging、單一專案、無真實個資。
  6. **`SECRET_VERSION_*` 歸類為 repository variable 而非 secret**。理由：其值只是版本號（如 `3`），不是機密；列為 secret 反而會讓它無法在 log 中顯示、增加除錯成本。此與 main 現況（`.github/workflows/deploy-staging.yml:84-86` 用 `${{ vars.* }}`）一致。
  7. **本卡未執行任何 GCP 指令、未讀取任何憑證值**。§2.1 的授權指令以 `<GCP_PROJECT_ID>` 佔位符書寫並明載「agent 不代為執行，屬使用者側的雲端安全設定」，符合 CLAUDE.md 安全鐵則。
- 驗證方式（接手者如何確認我做的是對的：指令 + 我實際得到的輸出）：

  **驗收指令 1（任務卡「驗收方式」第 1 條）—— `latest` 應只剩歷史紀錄與明確否定語**

  ```bash
  grep -n "latest" docs/specs/06_部署架構與CICD.md | grep -i "secret\|set-secrets" | cut -d: -f1 | tr "\n" " "
  ```
  ```text
  118 121 128 208 386 388 395 599 602 670 717 733
  ```
  逐行歸類（**無一行主張現行部署使用 `:latest`**）：
  - **明確否定語**（現行規範，寫的是「不得用／不再用／不回退到 `:latest`」）：118、121、128、208、717、733
  - **歷史紀錄**（T-0027 事件 §6.6／§6.7、T-0039 實作 §6.9／§6.9.1）：386、388、395、599、670
  - **已完成的待辦標記更新**：602（原「留待 T-0040 調整」→「已於 T-0040 同步完成」）

  **驗收指令 2（第 2 條）—— cron 不再被稱為備援來源**

  ```bash
  grep -n "備援" docs/specs/06_部署架構與CICD.md | cut -d: -f1 | tr "\n" " "
  ```
  ```text
  64 158 164 166 170 181 319 324 334 342 348 400 402 464 698 732 733
  ```
  逐行歸類（**現行文字無一處稱 cron 為備援來源**）：
  - **明確否定 cron 是備援**：158（§3.3 標題「也**不是**備援來源」）、164、166、170、319、324、400
  - **指 uptime check 的備援缺口／第二個 check**：181、334、342、348
  - **與 cron 無關**：64（`SECRET_VERSION_*` 的「備援路徑」）、698（同）、402／464（§6.8.1 T-0031 當時診斷原文，已由 400 的指向句標示被取代）
  - **歷史變更紀錄列**：732（T-0031 列，記錄當時的「降為備援」）、733（T-0040 列，記錄本次取代）

  ```bash
  sed -n "158p" docs/specs/06_部署架構與CICD.md
  ```
  ```text
  ### 3.3 `monitor-health.yml` — 保溫與人工抽查（**不是** NFR-003 的量測來源，也**不是**備援來源）
  ```

  **驗收指令 3（第 3 條）—— 三份文件的 301 字面**

  ```bash
  grep -n "301" docs/specs/06_部署架構與CICD.md docs/specs/adr/ADR-0005-雲端平台-CloudRun.md docs/specs/01_需求規格書_SRS.md | cut -d: -f1,2 | tr "\n" " "
  ```
  ```text
  docs/specs/06_部署架構與CICD.md:45 docs/specs/06_部署架構與CICD.md:378 docs/specs/06_部署架構與CICD.md:733 docs/specs/adr/ADR-0005-雲端平台-CloudRun.md:84 docs/specs/adr/ADR-0005-雲端平台-CloudRun.md:86 docs/specs/adr/ADR-0005-雲端平台-CloudRun.md:88 docs/specs/adr/ADR-0005-雲端平台-CloudRun.md:90
  ```
  逐行歸類（**無一處仍把 301 當成判準**）：
  - `06:45` — 本次改寫的那一列，判準已是「3xx（實測 302）」，「301」只出現在括號內的變更說明「原文寫死 301」
  - `06:378` — **誤中**：§6.5 的 GitHub run 連結 `.../runs/35430463010` 字串內含 `301`，非狀態碼
  - `06:733` — 變更紀錄 0.3 列的變更摘要
  - `ADR-0005:84/86/88/90` — 本次新增的附註本文（敘述「原文寫 301」、「301 或 302 皆視為合規」、理由、連帶影響）
  - **`01_需求規格書_SRS.md` 零筆**——實查其 NFR-002 驗證方式原文即為「`curl -I http://<staging>` 觀察 **3xx** 導向」，**本卡因此不修改 SRS**（符合任務卡 outputs「僅在含『301』字面時修正」）：

  ```bash
  grep -n "3xx\|301" docs/specs/01_需求規格書_SRS.md | cut -c1-120
  ```
  ```text
  225:| NFR-002 | 安全 | ①staging 對外一律 HTTPS，HTTP 請求需導向 HTTPS；②API 回應不得含堆疊追蹤、SQL、內部
  ```

  **驗收指令 4（第 4 條）—— version 0.3**

  ```bash
  sed -n "1,10p" docs/specs/06_部署架構與CICD.md
  ```
  ```text
  ---
  doc: DEPLOY
  title: 部署架構與 CI/CD
  epic: E-001
  version: 0.3           # T-0040 規格變更（cron 定位、secret 版本釘定、3xx 導向、IAM 角色補列），變更紀錄見文末
  status: frozen         # Gate 1 通過 2026-09-19，變更走「規格變更請求」任務卡
  author: plan-sd        # 設計階段由 plan-sd 起草；開發階段由 dev-ops 補實作細節
  reviewers: [dev-tl, dev-ops]
  updated: 2026-09-20T19:27:03+08:00
  ---
  ```

  **補充驗證 A —— 變更紀錄表依時間排序、0.3 在表尾**

  ```bash
  awk "NR>=728" docs/specs/06_部署架構與CICD.md | grep -n "^| 2026" | cut -c1-60
  ```
  ```text
  3:| 2026-09-19 | 0.1 | T-0004 | 初版。平台為 Render Web Ser
  4:| 2026-09-19 | **0.2** | **T-0010**（規格變更請求，使用
  5:| 2026-09-19 | 0.2（實作紀錄，未變更版本號） | **T-0031*
  6:| 2026-09-20 | **0.3** | **T-0040**（規格變更請求，Leader 2026
  ```

  **補充驗證 B —— 章節結構完整、新增 §2.1 與 §6.1.1**

  ```bash
  grep -n "^#\{2,4\} " docs/specs/06_部署架構與CICD.md | cut -c1-60 | head -25
  ```
  ```text
  22:## 1. 環境清單
  38:## 2. 雲端資源清單（依 ADR-0005）
  54:### 2.1 部署服務帳號角色清單（`github-deployer@<專案ID>
  100:## 3. Pipeline 階段（GitHub Actions）
  104:### 3.1 `ci.yml` — PR 與 push to main
  113:### 3.2 `deploy-staging.yml` — main 綠燈後
  130:#### 3.2.1 GCP 認證：Workload Identity Federation（主線）
  149:#### 3.2.2 備選：服務帳號 JSON 金鑰
  158:### 3.3 `monitor-health.yml` — 保溫與人工抽查（**不是** NF
  185:## 4. 環境變數清單（只列名稱與用途，值不入版控）
  189:### 4.1 應用執行期（Cloud Run 服務設定 ／ 本機 `.env`）
  210:### 4.2 CI/CD（GitHub repository secrets ／ variables）
  237:## 5. 回滾程序（必須實際演練過一次，記錄日期與結果）
  241:### 5.1 首選：Cloud Run revision 切流量（目標 < 2 分鐘）
  280:### 5.2 備選：以 git 回復並重新部署（目標 < 10 分鐘）
  289:### 5.3 資料庫層（僅在 migration 本身有誤時）
  295:### 5.4 演練要求
  315:## 6. 監控與告警
  317:### 6.1 監控項目
  334:#### 6.1.1 備援缺口與告警缺口：NFR-003 的量測來源目前
  350:### 6.2 採樣期（Leader 裁決 O-009）
  357:### 6.3 告警與已知誤報
  369:### 6.4 實作紀錄（T-0018）
  376:### 6.5 首次真實 run 紀錄（T-0027）
  381:### 6.6 WIF 修正後第二次真實 run（T-0027 續，2026-09-19 16:14）
  ```

  **補充驗證 C —— §2.1 角色表為 5 列（表頭＋分隔＋5 角色 ＝ 7 行 `|` 開頭）**

  ```bash
  awk "/^### 2.1/,/^\*\*授權指令/" docs/specs/06_部署架構與CICD.md | grep -c "^|"
  ```
  ```text
  7
  ```

  **補充驗證 D —— 規格文字與 main 現況（T-0039 實作）逐項對照**

  ```bash
  grep -n "set-secrets\|SECRET_VERSION\|secretmanager.viewer" .github/workflows/deploy-staging.yml scripts/deploy-staging.sh | cut -c1-110
  ```
  ```text
  .github/workflows/deploy-staging.yml:16:# SECRET_VERSION_BASIC_AUTH_USER／SECRET_VERSION_BASIC_AUTH_PASS 覆寫
  .github/workflows/deploy-staging.yml:84:          OVERRIDE_DATABASE_URL: ${{ vars.SECRET_VERSION_DATABASE_URL }}
  .github/workflows/deploy-staging.yml:85:          OVERRIDE_BASIC_AUTH_USER: ${{ vars.SECRET_VERSION_BASIC_AUTH_USER }}
  .github/workflows/deploy-staging.yml:86:          OVERRIDE_BASIC_AUTH_PASS: ${{ vars.SECRET_VERSION_BASIC_AUTH_PASS }}
  .github/workflows/deploy-staging.yml:112:              echo "::error::無法列出 secret ${secret_name} 的版本（gcloud 原始訊息：${out}）
  .github/workflows/deploy-staging.yml:194:              --set-secrets "DATABASE_URL=database-url:${DB_VERSION},BASIC_AUTH_USER=basic-
  scripts/deploy-staging.sh:112:  --set-secrets "DATABASE_URL=database-url:${DB_VERSION},BASIC_AUTH_USER=basic-auth-user:${PASS
  ```
  （截斷為顯示寬度所致；`06` §3.2 與 §7 的敘述即依此三處逐字對照撰寫，變數名稱與 `--set-secrets` 組法一致。）

- 下一步建議：
  1. **`03_系統設計書_SD.md:308`（§7 NFR-002①）的 301 字面需同步為 3xx**——含對策欄「HTTP 301 導向 HTTPS（ADR-0005）」與驗證方式欄「`curl -sSI http://<staging>` 觀察 301」兩處。非本卡 outputs，建議 Leader 併入下一張規格同步卡或追加本卡 outputs 後由 plan-sd 補做（**成本極低，兩處字串**）。
  2. **`20_測試案例.md` TC-079 判準（:156）與 NFR-002 對照表（:330）**由 qa-lead 於下一輪測試計畫同步卡改為「回 3xx（301 或 302 皆視為合規）且 `Location` 為對應的 `https://` 網址」——任務卡 acceptance 第 5 條明文由本卡註記、不代為修改。
  3. **`README.md`「部署與 secrets」一節**已由 T-0039 寫入 `roles/secretmanager.viewer` 的授權步驟；建議 dev-ops 或 dev-tl 核對其與 `06` §2.1 新表的五個角色**逐字一致**（尤其「既有專案要補跑一次」這句 README 是否也有）。
  4. **T-0039 的遠端待驗仍未解除**：使用者補授 `roles/secretmanager.viewer` 後，需觀察一次真實 `deploy-staging` run 是否全綠、`GITHUB_STEP_SUMMARY` 是否顯示三個版本號。`06` §6.9.1 已記錄根因，該條待驗由 dev-tl／Leader 追蹤。
  5. **`04_API規格.yaml` 未受本卡影響**，無須同步。
- 需要 Leader 裁決的事：
  1. **§6.1.1 的備援缺口三選項（A 新增第二個 uptime check／B 定期人工核對／C 接受單點）**——任務卡 acceptance 明文「不自行決定」，已備妥選項與代價（A 的費用實測仍為 US$0，代價是告警重複與多一個資源）。plan-sd 傾向 A。**同一裁決應一併涵蓋「本案目前沒有自動告警」這個缺口**（§6.3 更正條）。
  2. **`03_系統設計書_SD.md` 的 301 字面要不要現在修**——若 Leader 同意，最省的作法是直接追加本卡 outputs 讓 plan-sd 於 r2 一併補上（兩處字串）；否則會留下「`06`／ADR 已改、SD 未改」的落差，且 SD 是開發與測試都會引用的文件。
  3. **ADR-0005 決定第 6 點的附註是否逾越本卡授權**（任務卡 outputs 只寫第 7 點）——理由見「假設與決策」第 3 點，若認為逾越可單獨退回移除，不影響其餘變更。
