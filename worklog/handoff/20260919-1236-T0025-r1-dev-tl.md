# T-0025 r1 — dev-tl（審核）— 工作紀錄與交接

<!-- 檔名：worklog/handoff/20260919-1236-T0025-r1-dev-tl.md -->

## A. 開工紀錄

- 開工時間：2026-09-19T12:36:00+08:00
- 角色／模型：dev-tl / Opus 5（claude-opus-5[1m]）
- 任務卡：tasks/T-0025-fix-ops.md（round 1）— **審核 T-0025**
- 讀取的輸入：
  - `tasks/T-0025-fix-ops.md`（acceptance 七條、outputs 授權範圍）
  - `docs/reports/20260919-1146-CR-E001.md` S-3／S-4／S-7／S-9／S-10／S-11 六列「修正建議」欄
  - `tasks/E-001-todo-app.md` 12:03:12 與 12:22:24 兩段 Leader 裁決（package.json 三處授權、ci.yml lint 前補 build 由 dev-tl 收尾）
  - `worklog/handoff/20260919-1204-T0025-r1-dev-ops.md` B 段
  - 分支檔案：`package.json`、`tsconfig.test.json`、`.github/workflows/*.yml`、`docker-compose.yml`、`.env.example`、`README.md`、`docs/specs/06_部署架構與CICD.md`、`infra/cloudrun-service.yaml`、`scripts/deploy-staging.sh`
- 本輪計畫：在 worktree `…-wt/T-0025`（`task/T-0025-fix-ops`，HEAD `4328d36`）重跑 acceptance 每條；actionlint、compose 換埠、npm run dev 實測、06 參數表逐格對帳；判定並合併。

## B. 交接

- 完工時間／狀態：2026-09-19T12:45:00+08:00 / **done（初審通過、已合併 main）**

### 逐條判定 acceptance

| # | acceptance | 判定 | 證據 |
|---|---|---|---|
| S-3 | `run:` 內不再展開 secrets，全部經 env 後以 `"$VAR"` 引用；actionlint 零 finding | **通過** | §2 awk 掃描 `run:` 區塊 0 命中（全檔 7 處 `secrets.` 皆在 `with:`／`env:`）；§1 actionlint EXIT=0 無輸出 |
| S-7 | 去掉 `curl -f`，真實狀態碼進 CSV，只有連不上才 000 | **通過** | §3 `grep -c "curl -f" = 0`；唯一 curl 行為 `curl -sS -o /dev/null -w "%{http_code} %{time_total}" ... \|\| echo "000 0"` |
| S-10 | compose db 埠為 `"${POSTGRES_HOST_PORT:-5432}:5432"`，`.env.example` 加說明 | **通過** | §5 實際以 `POSTGRES_HOST_PORT=5437` 起服務，`docker ps` 顯示 `0.0.0.0:5437->5432/tcp`；`.env.example` 有註解段與註解掉的範例行 |
| S-11 | `npm run dev` 可用（`tsc -w` + `node --watch dist/server.js` 或等效）、`engines.node >= 22.18`、README 常用指令表 dev 有但書 | **通過（附裁決，見下「對 `npm run dev` 的裁決」）** | §4：`rm -rf dist` 後 `dev:build` 2 秒內產出 `dist/server.js`、`Found 0 errors. Watching for file changes.`；`dev:run` 起服務後 `/health` **HTTP=200 `{"status":"ok"}`**；`engines.node: ">=22.18"`；README 常用指令表已改為兩支並標「需開兩個終端機」 |
| S-4 | `tsconfig.test.json` 存在，`npm run lint` 同時檢查 `tests/**`；lint 全綠 | **通過** | §6：`tsc -p tsconfig.test.json --listFilesOnly` 列出 8 個 `tests/**/*.ts`；`npm run lint` 三段串接 EXIT=0 |
| S-9 | 06 新增三處參數對照表，內容與三處現況一致 | **通過** | §7 逐格對帳：region／port／cpu／memory／min-max／concurrency／timeout／env／secrets 逐字相符；兩處「飄移」標註屬實（`--allow-unauthenticated` 只在兩支 gcloud 指令、probe 只在 IaC 檔） |
| 全量 | lint、build、test:unit、test:integration 全綠；diff 只動 outputs | **通過（附一項範圍例外，已核准）** | §5 unit 75/75、integration 47/47；§8 diff 另含 `tests/**` 兩檔型別註記（S-4 的必然伴隨變更，見下「範圍例外」） |

### 對 `npm run dev` 的裁決（dev-tl 為 package.json 單一擁有者兼技術仲裁者）

實測 `npm run dev` 回 `npm error Missing script: "dev"`（§4）。dev-ops 將其拆成 `dev:build`（`tsc -w -p tsconfig.json`）與 `dev:run`（`node --watch dist/server.js`）兩支。

**裁決：接受，不視為不通過。** 理由：

1. CR S-11 的修正建議原文即為「`dev` 改為 `tsc -w` ＋ `node --watch dist/server.js` **兩段**」；任務卡 acceptance 的「或等效」涵蓋此寫法。
2. 專案未安裝 `concurrently`／`npm-run-all`，而 package.json 定案後不再新增相依（單一擁有者規約）。以 shell `&` 湊成單一 `dev` 只在 Git Bash 正確、PowerShell 語意不同，會製造「只在一種 shell 可用」的假象，違反硬前提 6。
3. S-11 的真正問題是「README 常用指令表列了一個必然失敗的指令」。現況 README 該列已改為 `npm run dev:build` ＋ `npm run dev:run` 並標明但書，倉庫內**不再有任何地方叫使用者執行 `npm run dev`**（已全檔確認），問題根因已消除；保留一個空殼 `dev` 反而會再製造一次同樣的陷阱。

**裁決後的紀錄義務**：本裁決寫入本檔與任務卡審核紀錄；後續任何文件不得再出現 `npm run dev`。

### 範圍例外（已核准）

`tests/integration/todo-repository.test.ts`（8 處 `(todo: { id: string })` 註記）與 `tests/unit/todo-repository.test.ts`（1 處 mock 泛型簽名由 `<T extends Record<string, unknown>>` 改為 `<T = Record<string, unknown>>`）不在任務卡 outputs 清單，但 S-4 的 acceptance 要求「`npm run lint` 含 tests 全綠」，不修這兩處就不可能成立（`TS7006`／結構不相容）。改動為**純型別註記**、不改任何斷言或執行邏輯，且 47＋75 項測試在修改後仍全綠。**核准為 S-4 的必然伴隨變更**，已在 dev-ops 交接檔「產出物」段登錄，非隱藏變更。

### 驗證輸出（全部在 `C:/Users/excal/Projects/Claude/soft-dev-team-260919-wt/T-0025`，HEAD `4328d36`）

#### §1 actionlint

```bash
docker run --rm -v "//c/Users/excal/Projects/Claude/soft-dev-team-260919-wt/T-0025://repo" -w //repo rhysd/actionlint:latest -color
```
```text
（無任何 finding 輸出）
ACTIONLINT_EXIT=0
```

#### §2 S-3：`run:` 區塊內不得出現 `secrets.`

```bash
grep -n "secrets\." .github/workflows/deploy-staging.yml
awk '/^[[:space:]]*run:/{inrun=1;next} inrun && /secrets\./{print FILENAME":"FNR": "$0; found=1} /^[[:space:]]*-[[:space:]]*name:/{inrun=0} END{if(!found) print "(no hits in run: blocks)"}' .github/workflows/deploy-staging.yml
```
```text
65:          workload_identity_provider: ${{ secrets.GCP_WIF_PROVIDER }}     ← with:
66:          service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}             ← with:
79:          DATABASE_URL: ${{ secrets.NEON_DATABASE_URL }}                  ← env:
80:          BASIC_AUTH_USER: ${{ secrets.STAGING_BASIC_AUTH_USER }}         ← env:
81:          BASIC_AUTH_PASSWORD: ${{ secrets.STAGING_BASIC_AUTH_PASSWORD }} ← env:
137:          BASIC_USER: ${{ secrets.STAGING_BASIC_AUTH_USER }}             ← env:（本卡新增）
138:          BASIC_PASS: ${{ secrets.STAGING_BASIC_AUTH_PASSWORD }}         ← env:（本卡新增）

(no hits in run: blocks)
```

verify 步驟現為 `-u "$BASIC_USER:$BASIC_PASS"`，與同檔 migrate 步驟寫法一致。

#### §3 S-7：monitor-health.yml

```bash
grep -n "curl" .github/workflows/monitor-health.yml
grep -c "curl -f" .github/workflows/monitor-health.yml
```
```text
47:            OUTPUT="$(curl -sS -o /dev/null -w "%{http_code} %{time_total}" "${STAGING_BASE_URL}/health" || echo "000 0")"
0
```

#### §4 S-11：`npm run dev` 系列實測（`rm -rf dist` 後由零編譯）

```bash
rm -rf dist
npm run dev            # 判定用：確認舊名稱是否仍存在
npm run dev:build      # 背景，另記錄輸出
# 輪詢 dist/server.js 出現
npm run dev:run        # 背景，PORT=8094
curl http://127.0.0.1:8094/health
```
```text
npm error Missing script: "dev"                （→ 見上「對 npm run dev 的裁決」）

dev:build produced dist/server.js = True (after 2 s)
12:31:20 - Found 0 errors. Watching for file changes.

HTTP=200 BODY={"status":"ok"}
{"level":30,...,"reqId":"req-1","req":{"method":"GET","url":"/health","host":"127.0.0.1:8094",...},"msg":"incoming request"}
{"level":30,...,"res":{"statusCode":200},"responseTime":2.926,"msg":"request completed"}

taskkill /PID <dev:run> /T /F  → SUCCESS（程序樹全數終止）
taskkill /PID <dev:build> /T /F → SUCCESS
```

`package.json` 覆核（Leader 授權三處）：

```diff
-    "node": ">=22"
+    "node": ">=22.18"
-    "dev": "node --watch src/server.ts",
+    "dev:build": "tsc -w -p tsconfig.json",
+    "dev:run": "node --watch dist/server.js",
-    "lint": "eslint . && tsc --noEmit -p tsconfig.json",
+    "lint": "eslint . && tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.test.json",
```

**覆核結論：核准。** 變更僅落在 `scripts.dev`（拆為兩支）、`scripts.lint`、`engines.node` 三處；`dependencies`／`devDependencies`／`package-lock.json`／其餘 script 一字未動（已逐行比對 diff）。

#### §5 S-10 + 全量測試（compose 換埠 5437／8096）

```bash
cp .env.example .env && echo "POSTGRES_HOST_PORT=5437" >> .env && sed -i "s/^PORT=8080/PORT=8096/" .env
docker compose up -d --build
docker ps --format "{{.Names}}\t{{.Ports}}"
```
```text
t-0025-app-1	0.0.0.0:8096->8096/tcp
t-0025-db-1	0.0.0.0:5437->5432/tcp     ← S-10 生效（容器內仍 5432，DATABASE_URL 不變）
```
```bash
DATABASE_URL="postgres://dev:dev@localhost:5437/todo" ... npm run migrate
DATABASE_URL="postgres://dev:dev@localhost:5437/todo" ... npm run test:integration
npm run test:unit
```
```text
已套用 migration：001

ℹ tests 47
ℹ pass 47
ℹ fail 0
ℹ duration_ms 985.4158

ℹ pass 75
ℹ fail 0
ℹ duration_ms 159.0827
```

#### §6 S-4：lint 涵蓋 tests

```bash
cat tsconfig.test.json
npx tsc -p tsconfig.test.json --listFilesOnly | grep "/tests/"
npm run build && npm run lint
```
```text
{ "extends": "./tsconfig.json", "compilerOptions": { "noEmit": true, "rootDir": ".",
  "allowJs": true, "checkJs": false, "allowImportingTsExtensions": true },
  "include": ["tests/**/*.ts"], "exclude": ["node_modules", "dist"] }

tests/integration/auth-basic.test.ts
tests/integration/error-contract.test.ts
tests/integration/health.test.ts
tests/integration/todo-repository.test.ts
tests/integration/todos-api.test.ts
tests/unit/error-handler.test.ts
tests/unit/todo-repository.test.ts
tests/unit/todo-service.test.ts

> eslint . && tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.test.json
LINT_EXIT=0
```

#### §7 S-9：參數表逐格對帳（實際讀三個檔案比對）

| 參數 | IaC 檔實值 | workflow 實值 | script 實值 | 表格所述 |
|---|---|---|---|---|
| 容器埠 | `containerPort: 8080` | `--port 8080` | `--port 8080` | 一致 ✓ |
| CPU／記憶體 | `cpu: "1"`／`memory: 512Mi` | `--cpu 1`／`--memory 512Mi` | 同左 | 一致 ✓ |
| min／max | `minScale: "0"`／`maxScale: "2"` | `--min-instances 0`／`--max-instances 2` | 同左 | 一致 ✓ |
| 並行／逾時 | `containerConcurrency: 80`／`timeoutSeconds: 60` | `--concurrency 80`／`--timeout 60s` | 同左 | 一致 ✓ |
| 非機密 env | `NODE_ENV`／`LOG_LEVEL`／`CORS_ALLOWED_ORIGINS` | `--set-env-vars "NODE_ENV=production,LOG_LEVEL=info,CORS_ALLOWED_ORIGINS="` | 同左 | 一致 ✓ |
| secrets | `database-url`／`basic-auth-user`／`basic-auth-pass`，`key: latest` | `--set-secrets "…:latest"` 三項 | 逐字相同 | 一致 ✓ |
| allow-unauthenticated | 無（只有 `ingress: all`） | 有 | 有 | 表格標「飄移」✓ 屬實 |
| probe | `startupProbe`＋`livenessProbe`（`/health`） | 無 flag | 無 flag | 表格標「飄移」✓ 屬實 |

06 既有 §1～§6 文字一字未動（diff 為純新增區塊）。

#### §8 git diff 範圍

```bash
git diff main...HEAD --stat
```
```text
 .env.example                                      |   5 +
 .github/workflows/deploy-staging.yml              |   4 +-
 .github/workflows/monitor-health.yml              |   2 +-
 README.md                                         |  10 +-
 docker-compose.yml                                |   2 +-
 docs/specs/06_部署架構與CICD.md                   |  24 +++
 package.json                                      |   7 +-
 tasks/T-0025-fix-ops.md                           |   4 +-
 tests/integration/todo-repository.test.ts         |  16 +-
 tests/unit/todo-repository.test.ts                |   2 +-
 tsconfig.test.json                                |  12 ++
 worklog/handoff/20260919-1204-T0025-r1-dev-ops.md | 192 ++++++++++++++++++++++
 12 files changed, 260 insertions(+), 20 deletions(-)
```

outputs 八項齊備；例外僅 `tests/**` 兩檔（見「範圍例外」）。`src/**` 一字未動。

#### §9 收尾清除

```text
docker compose down -v   → Container/Volume/Network 全部 Removed
rm -f .env
docker image rm t-0025-app → Deleted
docker ps                → 只剩 t-0021-app-1、t-0021-db-1（qa-at），未受影響
dev:build／dev:run 程序樹 → taskkill /T /F 全數 SUCCESS
```

#### §10 合併與合併後複驗

`task/T-0025-fix-ops` 與已合併的 T-0024 無檔案衝突（T-0024 未動 `package.json`／`tests/**/todo-repository*`），`git merge --no-ff` 一次成功。

**合併後必查項**（S-4 的 lint 現在會檢查 T-0024 新增的 `tests/integration/startup-migrate.test.ts`）：結果見「收尾 commit」的全量驗證（`npm run lint` EXIT=0，`tsconfig.test.json` 的 include 已涵蓋該新檔）。

### 對 dev-ops「假設與決策」逐條表態

| # | 決策 | 表態 | 理由 |
|---|---|---|---|
| 1 | `dev` 拆成 `dev:build`／`dev:run`，不用 shell `&` 湊單一 script | **接受** | 見上「對 `npm run dev` 的裁決」三點。以 dev-tl（package.json 擁有者）身分正式核准 |
| 2 | `tsconfig.test.json` 加 `allowJs: true`／`checkJs: false` | **接受，附但書** | 現況測試檔以 `../../dist/*.js` 匯入，主設定 `declaration: false` 無 `.d.ts`，不加會全面 `TS7016`。但書：這讓「自 dist 匯入的符號」退化為 `any`，S-4 實際換到的是**測試檔自身**的型別檢查（本輪修掉的 9 處正是這類）。要更完整需開 `declaration: true`，屬 src 建置設定變更，已列入下一步建議 |
| 3 | `tsconfig.test.json` 覆寫 `rootDir: "."` | **接受** | 主設定 `rootDir: "src"` 會讓 `tests/**` 觸發 `TS6059`；`noEmit` 下此檢查仍生效。已確認 `npm run build` 完全不讀本檔，對產物零影響 |
| 4 | mock 簽名 `<T extends …>` 改 `<T = …>` | **接受** | 與 `Queryable.query<T = Record<string, unknown>>` 對齊才能賦值；只改測試檔、未動 `src/`，75 項單元測試仍全綠 |
| 5 | 整合測試 callback 補 `(todo: { id: string })` 最小結構型別 | **接受** | 只用到 `.id`，最小註記即消除 `TS7006`；在 `dist` 匯入已退化為 `any` 的前提下，寫完整 `Todo` 型別拿不到額外檢查效益 |
| 6 | 06 第 7 節只記錄現況＋標註飄移，不代為修正兩項飄移 | **接受** | 任務卡明文「僅新增一節」，且兩項飄移是 ADR-0005 授權 dev-ops 的既有取捨。§7 已逐格查核表格內容屬實 |
| 7 | 本機驗證埠 5435／5436、8093／8094／8097，跑完即釋放 | **接受** | 我另以 5437／8096 重驗一次，過程中 `docker ps` 全程只見 `t-0021-*`（qa-at）未受影響，收尾後亦然 |
| 8 | `ci.yml` 的 `lint` job 補 `npm run build` 不由本卡做（不在 outputs） | **接受，且由我收尾** | 範圍判斷正確。Leader 已於 12:22:24 裁決由 dev-tl 在三卡合併後的收尾 commit 一併修，本輪照辦 |

### 判定

**全部通過 → status done，已合併 main（`--no-ff`）。**

### 下一步建議（給 qa-cr r2 與 qa-at r2）

1. **`npm run lint` 現在必須先 `npm run build`**（`tsconfig.test.json` 檢查的測試檔以 `dist/` 匯入）。qa-cr／qa-at 重跑時請用 `npm ci && npm run build && npm run lint` 的順序，否則會看到一堆 `TS2307` 假紅燈。`ci.yml` 的 `lint` job 已於收尾 commit 補上 `npm run build`。
2. **不要再用 `npm run dev`**（已拆為 `dev:build` + `dev:run` 兩個終端機）。若 qa-at 的 README 15 分鐘實測腳本裡還寫著 `npm run dev`，請同步改掉；README 本身已更新。
3. **S-4 的型別覆蓋有已知上限**：`tests/**` 自 `dist/` 匯入的符號型別為 `any`（`allowJs`＋無 `.d.ts`），所以「lint 綠」只代表測試檔自身無型別錯誤，不代表測試對 `src` API 的用法被型別檢查過。qa-cr r2 若要把 S-4 判為完全關閉，請以此為準；要根治需主 `tsconfig.json` 開 `declaration: true`（另開卡）。

### 需要 Leader 裁決的事

- 無。（`npm run dev` 改名一事已由 dev-tl 依 package.json 單一擁有者職權裁決並記錄於上；若 Leader 認為需保留 `dev` 名稱，請另開小卡。）
