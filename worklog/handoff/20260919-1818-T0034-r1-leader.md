# T-0034 r1 — leader（代理，Opus） — 工作紀錄與交接

## A. 開工紀錄

- 開工時間：2026-09-19T18:18:05+08:00
- 角色／模型：leader（Leader 代理） / opus
- 任務卡：tasks/T-0034-skills-v1.1.md（round 1）
- 讀取的輸入（實際讀過的檔案與段落，非任務卡照抄）：
  - .claude/agents/leader.md — Leader 職責、使用者介入點、禁止事項，確認本卡屬「模板／框架本身」故可由 Leader 代理直接改。
  - CLAUDE.md（專案）+ 全域 CLAUDE.md — 產出規則、收工規則、文件地圖、交接規則。
  - tasks/E-002-framework-v1.1.md#發現清單 — 本卡負責 F-02/04/05/06/12/14/17/19 的落點與措辭。
  - tasks/T-0034-skills-v1.1.md#acceptance — 八條驗收，逐條對應下方計畫。
  - .claude/skills/{dispatch,handoff,review-round,takeover,schedule-report}/SKILL.md — 五個 skill 現況，確認改動落點與既有行數。
  - scripts/extract-cards.sh — 輸出格式註解第 4 行與 echo 行，需加 phase 欄。
  - scripts/gen-schedule.mjs#L19-L38 — phaseOf() 與 cards.txt 解析，需加卡上 phase 優先序。
  - docs/schedule/E-001.json#phases/phaseById/phaseByTeam — 9 階段與覆寫表，確認回退可用、本次不需改。
  - tasks/E-001-todo-app.md#裁決紀錄 L168/L185 — UUID 格式錯回 400（提示詞誤寫 404）、部署階段順序以 06 §3.2 為準，兩例皆「規格優先於提示詞」。
- 上一輪交接摘要（若 round > 1）：不適用（r1）。
- 本輪計畫（3–7 條）：
  1. dispatch：三行提示詞定版 + 規格優先明文 + 開發卡 worktree 路徑行（F-12／F-06）；備援段精簡（F-05）。
  2. review-round：合併後 worktree remove / branch -d（F-02）；Leader 裁量 r2 升級模型並記錄於審核紀錄表（F-14）。
  3. handoff：commit 訊息寫檔 -F（F-04）；收尾前 grep 驗證 A/B 段時戳格式（F-19）。
  4. takeover：worktree 建立步驟（F-02）。
  5. extract-cards.sh 加 phase 欄、gen-schedule.mjs 讀卡上 phase（卡 → phaseById → phaseByTeam → team），schedule-report SKILL 同步說明（F-17）。
  6. 驗證：重跑抽取 + 產生器至 $TEMP，確認 30 卡／9 階段與現況一致；npx eslint 零錯誤。
  7. 收尾：填 B 段、任務卡 status: review、只 add 本卡 outputs + cards.txt + 卡 + 交接檔，commit -F。
- 預期產出：五個 SKILL.md、scripts/gen-schedule.mjs、scripts/extract-cards.sh、docs/schedule/cards.txt（重新抽取）、本交接檔、任務卡狀態更新。

## B. 交接

- 完工時間／狀態：2026-09-19T18:22:47+08:00 / review
- 完成事項（每項對應 F 編號）：
  - **F-12／F-06**（dispatch 步驟 5 + 新增「提示詞鐵則」段）：三行提示詞定版；開發卡在第一行後插入 worktree 路徑行（分支 `task/T-####-{slug}`，根目錄永遠 main）。新段明文三條：只指向不重述（不複製狀態碼／欄位／階段順序／閾值）、規格優先於提示詞（執行者依規格做並記於「假設與決策」，不回問）、提示詞寫錯由 Leader 追認於 Epic 裁決紀錄不退卡。措辭依 tasks/E-001-todo-app.md L168（UUID 格式錯回 400，提示詞誤寫 404）與 L185（部署階段順序以 06 §3.2 為準）兩則裁決。
  - **F-05**（dispatch 備援段）：保留 general-purpose 備援寫法與第一行加載角色檔的句子，三句縮成兩句。
  - **F-02**（review-round 步驟 5 done 分支）：合併後在主 repo 根目錄執行 `git worktree remove` → `git branch -d` → `git worktree prune`，並寫明兩個拒絕情境（未提交變更、分支未完全合併）的處理，禁止直接 `-D`。
  - **F-14**（review-round 新增步驟 6，原 6/7 順延為 7/8）：Leader 裁量可於 r2 提前升級模型，限「工具操作／環境誤判」類失敗（舉 E-001 三例：瀏覽器認證快取誤判、自我輪詢、指令語法踩環境限制）；必須記於審核紀錄表摘要「r2 升級模型 {舊}→{新}，理由：…」並同步改卡 `model`；加反例（規格理解錯／驗收條件有歧義／方向不對 = 回退重做，不是升級）。
  - **F-04**（handoff 步驟 6 Git）：多行 commit 訊息一律寫檔再 `-F`，不用多個 `-m`、不在 `-m` 內塞換行（本環境 Bash 不可含 ASCII 單引號）；補「只 add 自己卡的檔、禁 `add -A`／`--amend`／`reset`／`rebase`」。
  - **F-19**（handoff 新增步驟 5，原 5/6 順延為 6/7）：commit 前必做時戳格式驗證，附可直接跑的 grep 指令（`^- (開工時間|完工時間／狀態)：yyyy-mm-ddTHH:MM:SS+08:00`），要求兩行各一筆才算過，並說明失敗後果（T-0029 r1 該卡從甘特圖消失）與時間須 `date` 實查。
  - **F-02**（takeover 新增步驟 6，原 6 順延為 7）：開發卡動程式碼前先 `git worktree add ../{repo}-T#### -b task/T-####-{slug} main`；涵蓋卡上 `branch` 已有值改用既有分支、路徑殘留先 `prune`、把路徑與分支寫回卡 `branch` 與 A 段、之後一律用絕對路徑（agent 每次 Bash 呼叫工作目錄會重置）、文件卡不建 worktree。
  - **F-17**（extract-cards.sh、gen-schedule.mjs、schedule-report SKILL）：
    - `scripts/extract-cards.sh`：新增 `phase` 欄，位置在 `status` 之後（`id|title|team|role|status|phase|round|depends_on|created|updated|segs`），檔頭格式註解同步更新並說明缺值時的回退。
    - `scripts/gen-schedule.mjs`：解構多接一個 `phase`，`cards.push` 帶入 `phase`（空字串正規化為 `null`），`phaseOf()` 改為 `c.phase || phaseById || phaseByTeam || c.team` 並加註解。
    - `.claude/skills/schedule-report/SKILL.md`：輸入段的 frontmatter 欄位清單加 `phase`；設定檔說明改為「`phases[].key` 即卡上 `phase:` 可填的值，`phaseById` 只給沒有 phase 欄的舊卡」；步驟 2 改為「階段歸屬寫在任務卡，不另維護對照表」；規則段新增「階段歸屬優先序」一條。
- 產出物（路徑）：
  - .claude/skills/dispatch/SKILL.md（+11 −1）
  - .claude/skills/handoff/SKILL.md（+12 −2）
  - .claude/skills/review-round/SKILL.md（+12 −3）
  - .claude/skills/takeover/SKILL.md（+12 −1）
  - .claude/skills/schedule-report/SKILL.md（+4 −3）
  - scripts/extract-cards.sh（+4 −2）
  - scripts/gen-schedule.mjs（+4 −3）
  - docs/schedule/cards.txt（重新抽取，30 → 34 行，多出 E-002 四卡）
  - 每個 skill 淨增 ≤ 11 行，遠低於 60 行上限。
- 未完成／已知問題：
  - `scripts/extract-cards.sh` 沒有 Epic 過濾，掃 `tasks/T-*.md` 全部。E-002 四卡（T-0032～T-0035，`phase: framework`）已被抽進 `docs/schedule/cards.txt`；若之後拿這份 cards.txt 配 `docs/schedule/E-001.json` 重跑，會多出一個沒有標籤的 `framework` 群組（排序 99，置底）。本次未改 HTML 產出，`docs/專案時程表_20260919.html` 未動。
  - E-001 的 30 張卡目前都還沒有 `phase:` 欄（任務卡模板由 T-0033 負責，且本卡禁止改 T-00xx 卡），所以現階段 E-001 仍靠 `phaseById`／`phaseByTeam` 回退，行為與改動前完全相同。
- 我做的假設與決策（含理由）：
  - `phase` 欄放在 `status` 之後而非最後一欄：最後一欄是可變長度的 segs 串，插在它之前才不會讓解析位置隨資料浮動；卡片 frontmatter 的欄序也是 status → phase。
  - 卡上 `phase` 優先於設定檔 `phaseById`（而非相反）：F-17 的目的就是消掉「時程表要另外維護一份 phaseById」，設定檔若能覆蓋卡，等於留著兩份真相。
  - 空字串正規化為 `null`：`phase:` 欄缺席時 shell 會給空字串，空字串是 falsy 可直接落到回退，但顯式轉 `null` 讓嵌入 JSON 裡看得出「這張卡沒有 phase」而不是「有但空」。
  - `docs/schedule/cards.txt` 提交重新抽取後的 34 行版本，不手動刪掉 E-002 四卡：這個檔是產生器的輸入快照，手改就違反「產生器與抽取腳本是唯一事實來源」。Epic 過濾要不要做列為裁決項。
  - review-round 的 worktree 清理放在「判 done」分支而不是獨立段落：清理的觸發條件就是合併完成，跟 done 判定綁在一起才不會漏做。
- 驗證方式（接手者如何確認我做的是對的：指令 + 我實際得到的輸出）：
  ```bash
  # 1) 重新抽取（新增 phase 欄）
  bash scripts/extract-cards.sh docs/schedule/cards.txt
  # 2) 取 E-001 的 30 卡子集，跑產生器到暫存（不覆蓋 docs/ 既有報表）
  grep -vE "^T-003[2345]\|" docs/schedule/cards.txt > "$SP/cards-e001.txt"
  node scripts/gen-schedule.mjs docs/schedule/E-001.json "$SP/cards-e001.txt" --date 2026-09-19 --out "$SP/verify.html"
  # 3) 與基準（HEAD 版腳本 + HEAD 版 cards.txt）逐階段比對
  git show HEAD:scripts/gen-schedule.mjs > "$SP/gen-old.mjs"
  git show HEAD:docs/schedule/cards.txt > "$SP/cards-old.txt"
  node "$SP/gen-old.mjs" docs/schedule/E-001.json "$SP/cards-old.txt" --date 2026-09-19 --out "$SP/base.html"
  diff <(node "$SP/check.mjs" "$SP/base.html") <(node "$SP/check.mjs" "$SP/verify.html")
  # 4) lint
  npx eslint scripts/gen-schedule.mjs; echo "eslint-exit=$?"
  ```
  ```text
  extracted 34 cards -> docs/schedule/cards.txt
  30                                 # E-001 子集行數
  written .../verify.html 31667 bytes; 31 cards; range auto

  cards=31 phases=9                  # 30 卡 + extraCards 的 T-0000
  plan [規劃階段（規格包，Gate 1）] 8: T-0000,T-0002,T-0003,T-0004,T-0005,T-0006,T-0007,T-0010
  dev-plan [開發準備（WBS）] 1: T-0008
  qa-plan [測試準備（測試計畫、案例、追溯）] 2: T-0009,T-0019
  dev [開發（五批次）] 8: T-0011,T-0012,T-0013,T-0014,T-0015,T-0016,T-0017,T-0018
  qa [測試（CR / AT / UAT / 總結）] 4: T-0020,T-0021,T-0022,T-0023
  dev-fix [Code Review 修正] 3: T-0024,T-0025,T-0026
  staging [staging 部署] 1: T-0027
  qa-staging [staging 補驗（Gate 2 r2）] 3: T-0028,T-0029,T-0030
  ops [維運] 1: T-0031

  DIFF-EMPTY: 階段歸屬與基準完全一致   # 步驟 3 的 diff 無輸出

  eslint-exit=0
  ```
  優先序另做正向測試：把子集裡 T-0027（`phaseById: staging`）改成 `phase=ops`、T-0011（`phaseByTeam: dev`）改成 `phase=dev-fix` 後重跑，結果卡上值兩次都贏：
  ```text
  dev [開發（五批次）] 7: T-0012,...,T-0018        # T-0011 已移出
  dev-fix [Code Review 修正] 4: T-0011,T-0024,T-0025,T-0026
  ops [維運] 2: T-0027,T-0031                      # staging 群組因此清空消失
  ```
  （`$SP` = 本 session 暫存目錄；`check.mjs` 是暫存的小工具，讀 HTML 內嵌 `const DATA` 印出各階段卡號，非專案檔案。）
- 下一步建議：
  - Leader 審核：逐 F 項核對措辭，並抽 dispatch 的三行提示詞（含開發卡 worktree 行）對 2 個角色檔走一遍可理解性。
  - T-0033 的任務卡模板加上 `phase:` 欄後，之後新建的卡就會自動帶階段；E-001 既有 30 卡是否回頭補寫 `phase:` 可另開卡，不補也不影響（回退路徑仍在）。
- 需要 Leader 裁決的事：
  - `scripts/extract-cards.sh` 要不要加 Epic 過濾（例如依卡 frontmatter 的 `epic:` 欄，或接一個 `--epic E-001` 參數）？不加的話，跨 Epic 之後每次 `/schedule-report E-001` 都會把其他 Epic 的卡一起畫進去。本卡 acceptance 只要求 phase 欄，故未擅自擴張，列此待裁。
