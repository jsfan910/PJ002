# T-0033 r1 — leader（Leader 代理，Opus） — 工作紀錄與交接

<!-- 檔名時間 = 開工時間，取實查系統時間 2026-09-19T18:17:31+08:00 -->

## A. 開工紀錄

- 開工時間：2026-09-19T18:17:31+08:00
- 角色／模型：leader（Leader 代理） / opus
- 任務卡：tasks/T-0033-templates-v1.1.md（round 1）
- 讀取的輸入（實際讀過的檔案與段落）：
  - `.claude/agents/leader.md` —— Leader 職責、禁止事項；確認框架／模板本身由 Leader 撰寫不違反「不代寫規格」。
  - `CLAUDE.md`（專案）＋ 全域 CLAUDE.md —— 產出規則（docs/）、收工規則、交接規則。
  - `tasks/E-002-framework-v1.1.md#發現清單` —— 本卡負責 F-08/09/10/17/18/19/20/21/22 的落點描述。
  - `tasks/T-0033-templates-v1.1.md#acceptance` —— 8 條驗收；outputs 7 個檔（epic.md 為新增）。
  - `tasks/E-001-todo-app.md` —— Epic 結構（目標／原始需求／範圍 P0,P1,範圍外／限制／成功指標／任務卡表／Leader 裁決紀錄）與裁決紀錄的兩種格式（表格式、條列式）；F-22 原文出自 07:40 裁決第 6 項；F-08 出自 17:30 裁決「staging 測試卡錯開時段」。
  - `docs/reports/20260919-1410-Gate2-E001.md#3` —— Gate 判定選項表寫法：五段（核心爭議／證據觀點／選項 ≤3／建議+理由／推翻成本），且「判定方式」本身可作為裁決項①。
  - `docs/specs/06_部署架構與CICD.md#4`、`#6`、`#6.7`、`#6.8` —— 實際採用的 secret 注入方式（`--set-secrets`、`:latest` 的問題）、建議一（釘版本）與建議二（verify 失敗自動重建 revision）、uptime check 為 NFR-003 主要來源、cron 降備援。
  - `README.md#部署與-secrets`、`#GCP-指令在哪裡執行` —— Cloud Shell／本機 SDK 兩條路徑、`gcloud.cmd`、佔位符角括號、`<(echo)`、Secret 寫檔、管線量測不可靠。
  - `docs/templates/` 既有 7 檔（task_card、worklog_handoff、qa_07、spec_06、dev_11_README、gate_report）。
  - `.claude/skills/handoff/SKILL.md` —— 收尾步驟與回報五行格式。
- 上一輪交接摘要：無（round 1）。
- 本輪計畫：
  1. task_card.md 加 `phase:` 欄與 `branch` worktree 路徑慣例（F-17）。
  2. 新增 docs/templates/epic.md，依 E-001 結構抽象化（F-18）。
  3. worklog_handoff.md 兩個時戳欄位格式釘死並註明抽取契約（F-19/F-20）。
  4. qa_07 加「環境獨占與資料隔離」與「阻擋（環境未就緒）」固定寫法（F-08/F-22）。
  5. spec_06 §4 加 secret 釘版本 + verify 自動重建 revision；§6 加 uptime check 為主、CI cron 備援（F-09/F-21）。
  6. dev_11_README 加「指令在哪裡執行」骨架（F-10）。
  7. gate_report 第 3 章加「Gate 判定本身的選項表」骨架。
  8. 驗證：diff 統計、grep 檢查、epic.md 對齊 E-001。
- 預期產出：docs/templates/ 下 7 檔（1 新增 6 修改）、任務卡 status → review、本交接檔。

## B. 交接

- 完工時間／狀態：2026-09-19T18:20:43+08:00 / review
- 完成事項（每項對應 F 編號與落點行號）：

| F | 檔案 | 落點 | 改了什麼 |
|---|---|---|---|
| F-17 | `docs/templates/task_card.md` | frontmatter 第 8–14 行 | 新增 `phase:` 欄，註解列出 10 個常用值（plan / dev-plan / qa-plan / dev / qa / dev-fix / staging / qa-staging / ops / framework），並註明「必填：/schedule-report 以本欄分組，缺欄會退回另建對照表」 |
| F-17（F-02 連帶） | `docs/templates/task_card.md` | frontmatter 第 25–30 行 | `branch` 說明改為 worktree 路徑慣例：`git worktree add "<專案根目錄>-wt/T-0000" -b task/T-0000-slug`、所有操作在 worktree 內、根目錄永遠 main、合併後 `git worktree remove`、文件卡填 null |
| F-18 | `docs/templates/epic.md`（**新增**，75 行） | 全檔 | 依 `tasks/E-001-todo-app.md` 抽象化：frontmatter（id/title/status 六值/owner/created/updated）＋ 七章節「目標／使用者原始需求／範圍 P0,P1,範圍外／限制／成功指標／任務卡表／Leader 裁決紀錄」，章節名與 E-001 **完全對齊**（見下方驗證）。裁決紀錄段含格式契約註解：一次裁決一個 ISO 時戳小節、新的放最後、推翻要明寫「撤回 {時戳} 第 N 項」、事後更正用「更正」一行且舊文保留、成員只讀不寫；並給表格式與條列式兩種範例 |
| F-19 / F-20 | `docs/templates/worklog_handoff.md` | 檔首註解第 6–15 行、B 段第 33–36 行 | 檔首加「機器可讀欄位契約」區塊：明寫 A 段「開工時間：」與 B 段「完工時間／狀態：」由 `/schedule-report`（`scripts/gen-schedule.mjs`）與 `tasks/_done.md` 的派工／完成時間欄抽取，**不得改寫欄名或格式**；格式釘死 `yyyy-mm-ddTHH:MM:SS+08:00` 並列 4 個正／誤例；B 段補範例與收尾自檢 grep 指令 |
| F-08 | `docs/templates/qa_07_測試計畫.md` | 新增 §6.1「環境獨占與資料隔離」 | 獨占時段表（環境／卡／時段／可否平行）；資料隔離三條（只動自己本輪建立的資料、測試資料加 `{卡號}-{時戳}-` 前綴、需要空清單改用前綴篩選而非清空）；被他人清空記「阻擋（環境衝突）」不記不通過 |
| F-22 | `docs/templates/qa_07_測試計畫.md` | 新增 §6.2「環境未就緒時的結果記法（固定寫法，不得自創）」 | 固定寫 `` `阻擋（環境未就緒）` ``，不寫「不通過」「失敗」不留空；括號內註明缺什麼／誰能解／哪張卡補驗；阻擋自成一類不計入退出準則的不通過數；附六值結果對照表（通過／不通過／部分通過／阻擋（環境未就緒）／阻擋（工具）／不適用）與各自是否擋關 |
| F-21 | `docs/templates/spec_06_部署架構與CICD.md` | 新增 §4.1「Secret 注入與版本釘選（必填）」 | 建議釘具體版本（`NAME=secret:N`）而非 `latest`；仍用 latest 時至少把解析到的版本號寫進部署紀錄／pipeline summary；新增 secret 版本不會讓既有執行實例生效、須重新部署產生新 revision；**verify 失敗自動重建一次 revision 再重試**（上限 1 次、錯誤訊息完整保留避免掩蓋設定錯誤）；secret 值先落地成檔案再 `--data-file`、量位元組數量檔案不量管線；附「本專案採用」三列填表 |
| F-09 | `docs/templates/spec_06_部署架構與CICD.md` | 新增 §6.1「可用性採樣來源（必填）」、§6.2「採樣期與判讀」 | **主要來源必須是平台原生 uptime check**；**CI 排程 cron 只作備援與交叉比對，不作 Gate 判準分母**（理由：schedule 觸發無時效保證，零觸發會讓採樣期無限延後）；採樣起算時間以主要來源建立時間為準並同步 Epic 裁決紀錄；兩來源不一致以主要來源為準。附三列來源表（主要量測／備援／判讀成因）與採樣期填空、誤報判讀規則 |
| F-10 | `docs/templates/dev_11_README.md` | 新增「指令在哪裡執行」段 | 兩列環境對照表（雲端網頁 Shell／本機 SDK；專案指令用跨 shell npm script）＋「Windows 陷阱」七條：`.cmd` 後綴、佔位符連角括號一起替換（未替換不報錯而是把字面字串寫進設定）、`cmd.exe` 的 `echo` 不處理單引號、`<(echo)` 不存在須改暫存檔、secret 值用 `[System.IO.File]::WriteAllText`、量位元組先落地成檔、PowerShell 5.1 `Get-Content` 對 UTF-8 無 BOM 誤判改 `ReadAllLines()` |
| （gate_report 骨架） | `docs/templates/gate_report.md` | 第 3 章新增「① Gate 判定本身」 | 依 `docs/reports/20260919-1410-Gate2-E001.md` 第 3 章①寫法：擋關項未達標但 0 條不通過時，本項必列為第一個裁決項；五段結構（核心爭議／證據觀點／選項表／建議＋理由／推翻成本），選項表固定三列 A 阻擋待補驗、B 有條件通過（限定範圍）、C 不通過，各附做法與代價；無未達標項則整節刪除 |

- 產出物（路徑）：
  - `docs/templates/task_card.md`（+13／-1）
  - `docs/templates/epic.md`（新增，75 行）
  - `docs/templates/worklog_handoff.md`（+13）
  - `docs/templates/qa_07_測試計畫.md`（+35）
  - `docs/templates/spec_06_部署架構與CICD.md`（+39）
  - `docs/templates/dev_11_README.md`（+24）
  - `docs/templates/gate_report.md`（+18）
  - `tasks/T-0033-templates-v1.1.md`（status → review、updated、審核紀錄一列）
  - 本交接檔
- 未完成／已知問題：
  - `docs/templates/task_card.md` 加的 `phase:` 欄需要 **T-0034**（`scripts/gen-schedule.mjs`、schedule-report skill）實際去讀，本卡只定義欄位與值域，不改腳本（outputs 不重疊）。兩卡合併後請 Leader 確認值域字串一致（本卡用的 10 個值見上表）。
  - `docs/templates/spec_06` §6.1 的「平台原生 uptime check」刻意寫成平台無關措辭（只在括號內舉 Cloud Monitoring 為例），下一個 Epic 若換雲端平台不需改模板。
  - 既有 Epic `E-001`／`E-002` 未回頭套用新模板（本卡 acceptance 明文「不改任何 tasks/T-00xx 卡」，且 E-001 已是事實來源，改寫風險大於收益）。
- 我做的假設與決策（含理由）：
  1. **「每檔淨增不超過 60 行」只適用於既有檔案**：6 個既有模板最大淨增 39 行（spec_06），全部達標；新增的 `epic.md` 為 75 行全新檔（從 0 起算無「淨增」可言）。已先壓縮過一版（原 96 行 → 75 行：合併檔首註解、裁決紀錄格式契約由 7 條併為 4 條）。若 Leader 認為新檔也須 ≤ 60 行，可再砍掉裁決紀錄的第二個範例小節（條列式）降到約 68 行，但會失去「單一事件」的寫法示範。
  2. **模板維持「填空 + 註解」風格**：所有來自 E-001 的具體事實（GCP／Cloud Run／Neon／`basic-auth-user`／`jsfan910/PJ002`／具體時戳與數字）一律抽象化為「平台原生 uptime check」「雲端 CLI」「secret 值」等通用詞；E-001 只以 HTML 註解形式留下「為什麼有這條」的一句話（例：「CI 排程曾兩小時零次自動觸發（E-001 實際發生）」），讓後人知道規則來源又不綁死技術棧。
  3. **`qa_07` 的結果值域擴充為六值**：acceptance 只要求「阻擋（環境未就緒）」，但 E-001 實際還用過「阻擋（工具）」「部分通過」「不適用」；只定義一個值會讓執行者在其他情況又自創寫法，故一次把值域列全並標明是否擋關。
  4. **`gate_report` 的 Gate 判定節寫成「有未達標項才必列、否則整節刪除」**，而非無條件保留，避免每份 Gate 報告都硬塞一個不存在的爭議。
  5. **`task_card` 的 `phase` 預設值填 `plan`** 而非留空，與該檔其他欄位（`team: plan`、`role: plan-ba`）的示範值一致。
- 驗證方式（接手者如何確認）：

  ```bash
  cd /c/Users/excal/Projects/Claude/soft-dev-team-260919
  git diff --stat -- docs/templates/        # 既有 6 檔淨增行數（須每檔 ≤ 60）
  wc -l docs/templates/epic.md              # 新增檔行數
  grep -n "^phase:" docs/templates/task_card.md
  grep -n "worktree" docs/templates/task_card.md | head -3
  grep -n "開工時間：\|完工時間／狀態：" docs/templates/worklog_handoff.md
  grep -c "yyyy-mm-ddTHH:MM:SS+08:00" docs/templates/worklog_handoff.md
  # epic.md 章節與 E-001 對齊（去掉標題後的全形括號附註再比對）
  diff <(grep -o "^## [^（]*" tasks/E-001-todo-app.md) <(grep -o "^## [^（]*" docs/templates/epic.md)
  grep -n "環境獨占與資料隔離\|阻擋（環境未就緒）" docs/templates/qa_07_測試計畫.md
  grep -n "釘具體版本\|自動重建一次 revision\|平台原生 uptime check\|只作備援" docs/templates/spec_06_部署架構與CICD.md
  grep -n "指令在哪裡執行\|Windows 陷阱" docs/templates/dev_11_README.md
  grep -n "Gate 判定本身" docs/templates/gate_report.md
  ```

  ```text
  === diff stat ===（修改 epic.md 前的既有 6 檔統計，epic.md 為未追蹤新檔不列入）
   docs/templates/dev_11_README.md          | 24 ++++++++++++++++++++
   docs/templates/gate_report.md            | 18 +++++++++++++++
   docs/templates/qa_07_測試計畫.md         | 35 ++++++++++++++++++++++++++++
   docs/templates/spec_06_部署架構與CICD.md | 39 ++++++++++++++++++++++++++++++++
   docs/templates/task_card.md              | 13 ++++++++++-
   docs/templates/worklog_handoff.md        | 13 +++++++++++
   6 files changed, 141 insertions(+), 1 deletion(-)
  → 最大淨增 39 行（spec_06），六檔全部 ≤ 60，達標。

  === wc -l docs/templates/epic.md ===
     75 docs/templates/epic.md

  === F-17 grep phase ===
  8:phase: plan           # 本卡屬於哪個階段。常用值：

  === F-17 grep branch worktree ===
  25:branch: null          # 開發卡填 task/T-0000-slug，且**必須在 git worktree 內作業**：
  26:                      # git worktree add "<專案根目錄>-wt/T-0000" -b task/T-0000-slug
  27:                      # 所有檔案操作／npm／docker／測試／commit 都在該 worktree 內；

  === F-19/20 grep 時戳欄位 ===
  7:     A 段「開工時間：」與 B 段「完工時間／狀態：」兩欄由工具直接抽取：
  17:- 開工時間：yyyy-mm-ddTHH:MM:SS+08:00
  32:- 完工時間／狀態：yyyy-mm-ddTHH:MM:SS+08:00 / review | blocked | done
  34:       範例：- 完工時間／狀態：2026-09-19T19:04:02+08:00 / review -->
  35:  <!-- 填完本欄請自檢一次：grep -n "開工時間：\|完工時間／狀態：" 本檔，
  === 契約字串出現次數 ===
  4

  === epic.md 章節與 E-001 對齊 ===
  （diff 無輸出）章節完全對齊
  E-001：目標／使用者原始需求／範圍／限制／成功指標／任務卡／Leader 裁決紀錄
  epic.md：目標／使用者原始需求／範圍／限制／成功指標／任務卡／Leader 裁決紀錄

  === F-08/F-22 grep ===
  56:### 6.1 環境獨占與資料隔離
  77:- 環境（staging／外部服務／憑證）未就緒而無法執行的 TC，結果欄一律寫 **`阻擋（環境未就緒）`**，**不寫「不通過」、不寫「失敗」、不留空**。
  87:| 阻擋（環境未就緒） | 環境／憑證／外部相依未備妥，**沒有執行** | 阻擋，但不計入不通過 |

  === F-09/F-21 grep ===
  48:- **建議釘具體版本而非 `latest`**：…
  51:- **`verify` 失敗時自動重建一次 revision 再重試**：…
  57:| 版本策略 | （釘具體版本 ／ `latest` + 記錄版本號） | |
  76:- **主要來源必須是平台原生 uptime check**（…）
  77:- **CI 排程（cron）只作備援與交叉比對**，不作為 Gate 判準的分母。…
  83:| 平台原生 uptime check | **主要量測** | 每 5 分鐘、多檢查地區、打未受保護的健康檢查端點 | 成功率 ≥ 99% | yyyy-mm-ddTHH:MM:SS+08:00 |

  === F-10 grep ===
  27:## 指令在哪裡執行
  37:### Windows 陷阱（逐項寫明，不要只寫「請自行調整」）

  === gate_report grep ===
  24:### ① Gate 判定本身（當有擋關項未達標時，本項必列為第一個裁決項）
  ```

  範圍隔離驗證（`git status --porcelain` 實查）：本卡只新增／修改 `docs/templates/*` 7 檔、`tasks/T-0033-templates-v1.1.md`、本交接檔。工作區內同時出現的 `.claude/agents/*`、`.claude/skills/*`、`scripts/*`、`CLAUDE.md`、`docs/團隊協作計畫書_20260919.md`、`docs/schedule/cards.txt` 變更屬平行卡 T-0032／T-0034／T-0035，本卡**未碰、未 add**。commit 採逐檔 `git add`，未使用 `add -A`。

- 下一步建議：
  1. Leader 逐 F 項核對（上表已給行號落點），重點看 F-18 的 `epic.md` 是否還缺 E-001 有而模板沒抽象化的欄位。
  2. 與 **T-0034** 對齊 `phase` 值域：`scripts/gen-schedule.mjs` 讀卡上 `phase` 時的合法值需與本卡註解的 10 個值一致；若 T-0034 另訂值域，以哪邊為準請 Leader 裁定後由後手同步一處（不要兩處各寫一份）。
  3. 與 **T-0034** 對齊 F-19：handoff skill 收尾前的格式驗證（acceptance 寫「handoff skill 收尾前驗證格式」）應直接引用本卡在 `worklog_handoff.md` 檔首定的契約字串，不要重寫規則。
  4. 下一個 Epic 開卡時實地套用 `epic.md` 與新版 `task_card.md`，作為 v1.1 的真實驗證。
- 需要 Leader 裁決的事：
  1. **`epic.md` 75 行是否接受**（acceptance 的「每檔淨增不超過 60 行」對新增檔的適用方式）。若須 ≤ 60 行，指示要砍掉哪一段（建議砍裁決紀錄的第二個條列式範例）。
  2. **`phase` 值域的最終權威在哪一張卡**（本卡模板註解 vs T-0034 的 `gen-schedule.mjs`），避免兩處各存一份真相。
