# T-0039 r2（授權後複驗）— dev-tl — 工作紀錄與交接

<!-- 檔名：worklog/handoff/20260920-2006-T0039-r2-dev-tl.md（時間 = 開工時間，取實查系統時間） -->

## A. 開工紀錄

- 開工時間：2026-09-20T20:06:22+08:00
- 角色／模型：dev-tl / Opus 5 (1M context)
- 任務卡：tasks/T-0039-secret-pin-verify-retry.md（round 2，status: blocked）
- 讀取的輸入（實際讀過的檔案與段落，非任務卡照抄）：
  - tasks/T-0039-secret-pin-verify-retry.md#frontmatter —— 確認 status 為 blocked、blocked_reason 指向「缺 roles/secretmanager.viewer」，acceptance 1～5 已於 r2 通過、只剩第 6 條（真實 run 全綠＋STEP_SUMMARY 三個版本號）待勾核。
  - tasks/T-0039-secret-pin-verify-retry.md#審核紀錄 r2 列 —— 取得阻擋點全貌：程式已合併 main（merge commit `3345022`、推送 `bfe4693`），run `35507519302` 第 8 步 `resolve secret versions` 因 `PERMISSION_DENIED: secretmanager.versions.list` 失敗；worktree 與分支保留待複驗。
  - .claude/skills/review-round/SKILL.md#步驟 3~5 —— 本輪不採信交接檔文字，需自行重跑驗證；全過則 status: done、審核紀錄補列、清理 worktree 與分支（`remove` / `-d`，不得 `--force` / `-D`）。
  - docs/templates/worklog_handoff.md —— A/B 段欄位契約（時間格式 ISO 8601、欄名逐字相同）。
- 上一輪交接摘要（round > 1）：
  - 退回原因（r2 判 blocked）：唯一阻擋項為雲端 IAM 授權——部署服務帳號 `github-deployer@pj002-509106.iam.gserviceaccount.com` 缺 `secretmanager.versions.list`（在 `roles/secretmanager.viewer`），屬使用者的雲端安全設定，agent 不代為執行。本卡程式碼本身無可再改之處。
  - 本輪要修的點：無程式碼變更。僅在使用者完成授權後，重跑 deploy-staging 勾核 acceptance 第 6 條。
- 本輪計畫（3–7 條）：
  1. 以唯讀指令確認 `github-deployer@pj002-509106` 的 IAM 角色現況已含 `roles/secretmanager.viewer`。
  2. 重跑 deploy-staging：優先 `gh run rerun` 最新失敗 run（`35507519302`）；`gh` 未登入則以空 commit 推送 main 觸發，commit 訊息註明用途。
  3. 等該 run 結束（依環境鐵則最多主動確認 2 次），取 conclusion 與各步驟狀態。
  4. 確認 STEP_SUMMARY 顯示三個 secret 版本號、記錄新 revision 名稱。
  5. 實測 staging `/health` 回 200、`/`（未帶憑證）回 401。
  6. 全過 → 任務卡 `status: done`、`blocked_reason: null`、審核紀錄補一列；清理 worktree（`git worktree remove`）與分支（`git branch -d`）。仍失敗 → 維持 blocked 並貼實際錯誤。
- 預期產出：tasks/T-0039-secret-pin-verify-retry.md（status/blocked_reason/審核紀錄）、本交接檔。不得讀取或輸出任何 secret 值。

## B. 交接

- 完工時間／狀態：2026-09-20T20:24:41+08:00 / done
- 完成事項：
  - 以唯讀指令確認 `github-deployer@pj002-509106.iam.gserviceaccount.com` 的 IAM 角色已含 `roles/secretmanager.viewer`（較 r2 多一列）。未執行任何 IAM 變更指令。
  - `gh` CLI 本機未安裝，無法 `gh run rerun`；改以空 commit `854c843`（訊息註明用途與 gh 不可用）推送 main 觸發 CI #21 → workflow_run 觸發 Deploy Staging #21。
  - Deploy Staging run `35509795862` **全綠**（13 步全 success，無 failure annotation），r1／r2 兩度紅燈的第 8 步 `resolve secret versions` 這次 success。
  - 新 revision `todo-app-00017-qg4` 產生；其三個 `secretKeyRef` 為具體數字版本 `database-url:1`／`basic-auth-user:3`／`basic-auth-pass:1`，無 `latest`。
  - staging 實測 `GET /health` 200、`GET /` 401、`GET /api/v1/todos` 401；另實打 `/health` 豁免路徑變體與受保護路徑變體，確認無繞過。
  - 任務卡 `status: blocked → done`、`blocked_reason` 清為 `null`、`updated` 更新、審核紀錄補一列。
  - 清理：`git worktree remove`（worktree 乾淨）＋`git branch -d`（已確認完全併入 main）＋`git worktree prune`，未用 `--force`／`-D`。
- 產出物（路徑）：
  - `C:\Users\excal\Projects\Claude\soft-dev-team-260919\tasks\T-0039-secret-pin-verify-retry.md`（status／blocked_reason／updated／審核紀錄）
  - `C:\Users\excal\Projects\Claude\soft-dev-team-260919\worklog\handoff\20260920-2006-T0039-r2-dev-tl.md`（本檔）
  - 程式碼無變更（r2 已合併進 main，merge commit `3345022`）；本輪僅新增觸發用空 commit `854c843`
- 未完成／已知問題：
  - **STEP_SUMMARY 的渲染文字無法直接貼出**：GitHub job summary 對未登入訪客不渲染（run 頁 `document.body.innerText` 查無 `database-url`，頁面顯示 `Sign in`），`/actions/jobs/{id}/logs` 匿名回 **403**，`gh` CLI 未安裝，而本角色不得處理憑證登入。已改以等價證據勾核（見下方驗證方式第 4、5 組）。使用者若要肉眼確認，登入後開 https://github.com/jsfan910/PJ002/actions/runs/35509795862 即可看到「本次部署釘定的 Secret Manager 版本」表格。
  - `//health` 回 200（回的是 health 端點本身，body `{"status":"ok"}`）。同時實測 `//api/v1/todos`、`//` 皆 401，**受保護資源未被繞過**，故非阻擋級；此行為與本卡變更無因果（本卡未動認證中介層）。
- 我做的假設與決策（含理由）：
  - **以空 commit 觸發而非 `gh run rerun`**：`gh` 不在 PATH（見驗證方式第 2 組實際錯誤），且 Leader 提示詞已授權此備援路徑。空 commit 不含任何程式碼變更，不影響 main 內容，可追溯（訊息寫明用途）。
  - **acceptance 第 6 條判為通過**：條文要求「run 全綠且 STEP_SUMMARY 顯示三個版本號」。run 全綠已直接證實；三個版本號改以「產生該表格的第 8 步本身 success（r2 已用 mock gcloud 證明解析失敗或版本為空時該步必回非 0）」＋「該步算出的版本號實際綁進 revision `00017-qg4`，為 1／3／1」兩段證據勾核。此二者是同一組變數 `V_DB`／`V_USER`／`V_PASS` 的下游，比 summary 文字更接近事實源。**不以「應該有寫進去」放行，而是以 revision 的實際綁定值證明**。
  - **未動 CHANGELOG**：CHANGELOG 不在本卡 `outputs`，且 r2 合併時已追加 T-0039 一列；本輪無程式碼變更，不重複登錄。
  - **未讀取任何 secret 值**：全程只用 `gcloud run revisions describe`（顯示 `secretKeyRef` 的 name/key，不含值）與 `gcloud secrets versions list`；未執行 `gcloud secrets versions access`。
  - **一併推送了 Leader 未推送的看板 commit `dda8034`**：推送前 `git status -sb` 顯示 main 已 ahead 1（`看板：T-0039 解除阻塞，重跑部署中`）。觸發部署必須 push，無法只推自己的 commit；該 commit 只動 `tasks/_todo.md` 與每日交接檔，屬 Leader 既有工作成果，未做任何改寫。
- 驗證方式（接手者如何確認我做的是對的：指令 + 我實際得到的輸出）：

  **第 1 組：IAM 現況（唯讀）**
  ```powershell
  gcloud projects get-iam-policy pj002-509106 --flatten="bindings[].members" --format="table(bindings.role)" --filter="bindings.members:github-deployer@pj002-509106.iam.gserviceaccount.com"
  ```
  ```text
  ROLE
  roles/artifactregistry.writer
  roles/iam.serviceAccountUser
  roles/run.admin
  roles/secretmanager.secretAccessor
  roles/secretmanager.viewer
  ```
  （r2 時只有前四列；`roles/secretmanager.viewer` 為本次新增，即 `secretmanager.versions.list` 的來源。）

  **第 2 組：gh 不可用，改以空 commit 觸發**
  ```powershell
  gh auth status
  ```
  ```text
  gh : The term 'gh' is not recognized as the name of a cmdlet, function, script file, or operable program.
  ```
  ```bash
  git commit --allow-empty -F <訊息檔>
  git push origin main
  ```
  ```text
  [main 854c843] T-0039: 空 commit 觸發 deploy-staging 複驗（IAM 授權後）
  To https://github.com/jsfan910/PJ002.git
     5a3599d..854c843  main -> main
  ```

  **第 3 組：run 全綠與逐步驟狀態**
  ```bash
  curl -sS "https://api.github.com/repos/jsfan910/PJ002/actions/runs?head_sha=854c84301fb4619ac8fb1508fc9db9305bfa0eb7&per_page=30"
  curl -sS "https://api.github.com/repos/jsfan910/PJ002/actions/runs/35509795862/jobs"
  ```
  ```text
  RESULT id=35509795862 run_number=21 conclusion=success
  url=https://github.com/jsfan910/PJ002/actions/runs/35509795862

  job: deploy-staging  conclusion=success  (html_url .../job/106075596373)
    1 Set up job ............................................. success
    2 checkout ............................................... success
    3 setup-node ............................................. success
    4 npm ci ................................................. success
    5 npm run build .......................................... success
    6 auth（Workload Identity Federation） .................... success
    7 setup-gcloud ........................................... success
    8 resolve secret versions（釘具體版本，不用 :latest） ...... success   <= r1/r2 皆卡在此步
    9 gcloud auth configure-docker ........................... success
   10 migrate（NEON_DATABASE_URL，forward-only） .............. success
   11 docker build & push .................................... success
   12 gcloud run deploy ＋ verify（含一次自動重試） ........... success
   13 回填提示（首次部署後的網址） ........................... success
  ```
  run 頁 annotations（`document.body.innerText`）只有：
  ```text
  Annotations
  1 warning and 1 notice
  deploy-staging  Node.js 20 is deprecated. ...
  deploy-staging  "The ubuntu-latest label will migrate to Ubuntu 26 ..."
  ```
  **無任何 failure annotation。**

  **第 4 組：授權前後對照（證明 IAM 是唯一變數）**
  ```bash
  curl -sS "https://api.github.com/repos/jsfan910/PJ002/actions/runs/35509297796"          # #20, sha 5a3599d, 19:58
  curl -sS "https://api.github.com/repos/jsfan910/PJ002/check-runs/106074300687/annotations"
  ```
  ```text
  run_number 20 / conclusion "failure" / created_at 2026-09-20T11:58:29Z
  step 8 resolve secret versions ... failure；step 9~13 全 skipped
  annotation(failure): 無法列出 secret database-url 的版本（gcloud 原始訊息：ERROR:
    (gcloud.secrets.versions.list) PERMISSION_DENIED: Permission 'secretmanager.versions.list'
    denied on resource (or it may not exist). ...）
  ```
  同一份程式碼，授權前（#20）紅、授權後（#21）綠。

  **第 5 組：新 revision 與實際釘定的版本號（只取版本號，未讀值）**
  ```powershell
  gcloud run services list --format="table(metadata.name,metadata.labels.'cloud.googleapis.com/location',status.url)"
  gcloud run revisions list --service=todo-app --region=asia-east1 --format="table(metadata.name,metadata.creationTimestamp)" --sort-by="~metadata.creationTimestamp" --limit=3
  gcloud run revisions describe todo-app-00017-qg4 --region=asia-east1 --format="value[delimiter='%0A'](spec.containers[0].env)"
  ```
  ```text
  NAME      CLOUD.GOOGLEAPIS.COM/LOCATION  URL
  todo-app  asia-east1                     https://todo-app-dpevsdhdva-de.a.run.app

  NAME                CREATION_TIMESTAMP
  todo-app-00017-qg4  2026-09-20T12:10:28.130626Z   <= 本次部署新建
  todo-app-00016-tsg  2026-09-19T10:50:07.789332Z
  todo-app-00015-b7m  2026-09-19T10:40:57.005748Z

  {'name': 'DATABASE_URL',        'valueFrom': {'secretKeyRef': {'key': '1', 'name': 'database-url'}}}
  {'name': 'BASIC_AUTH_USER',     'valueFrom': {'secretKeyRef': {'key': '3', 'name': 'basic-auth-user'}}}
  {'name': 'BASIC_AUTH_PASSWORD', 'valueFrom': {'secretKeyRef': {'key': '1', 'name': 'basic-auth-pass'}}}
  ```
  三個 `key` 皆為具體數字（1／3／1），**無 `latest`**，與 r2 記錄的三個 secret ENABLED 最新版本一致。

  **第 6 組：STEP_SUMMARY 匿名不可讀（限制的實證）**
  ```bash
  curl -sS -w "\nHTTP=%{http_code}\n" -L -o job-log.txt "https://api.github.com/repos/jsfan910/PJ002/actions/jobs/106075596373/logs"
  # 瀏覽器（未登入）於 run 頁執行 document.body.innerText.indexOf("database-url")
  ```
  ```text
  HTTP=403
  {"hasSecret": -1, "len": 926, "snippet": "Skip to content\nNavigation Menu\nSign in\njsfan910\n/\nPJ002 ..."}
  ```
  匿名訪客看不到 job summary；`gh` 未安裝；本角色不處理憑證登入。故以第 5 組為等價證據。

  **第 7 組：staging 實測（部署後）**
  ```bash
  curl -s -o /dev/null -w "GET /health -> %{http_code}\n" https://todo-app-dpevsdhdva-de.a.run.app/health
  curl -s -o /dev/null -w "GET / (no creds) -> %{http_code}\n" https://todo-app-dpevsdhdva-de.a.run.app/
  curl -s -o /dev/null -w "GET /api/v1/todos (no creds) -> %{http_code}\n" https://todo-app-dpevsdhdva-de.a.run.app/api/v1/todos
  curl -s https://todo-app-dpevsdhdva-de.a.run.app/health
  ```
  ```text
  GET /health -> 200
  GET / (no creds) -> 401
  GET /api/v1/todos (no creds) -> 401
  {"status":"ok"}
  ```

  **第 8 組：豁免路徑變體與受保護路徑變體（dev-tl 安全判準）**
  ```bash
  B=https://todo-app-dpevsdhdva-de.a.run.app
  for p in "/./health" "/%2e/health" "//health" "/health/" "/HEALTH" "/health/../"; do curl -s -o /dev/null -w "%{http_code}\n" --path-as-is "$B$p"; done
  for p in "//api/v1/todos" "/./api/v1/todos" "//" "/health/../api/v1/todos" "/%2e%2e/api/v1/todos"; do curl -s -o /dev/null -w "%{http_code}\n" --path-as-is "$B$p"; done
  ```
  ```text
  /./health      -> 302
  /%2e/health    -> 302
  //health       -> 200   (body {"status":"ok"}，回的是 health 端點本身)
  /health/       -> 401
  /HEALTH        -> 401
  /health/../    -> 302

  //api/v1/todos             -> 401
  /./api/v1/todos            -> 302
  //                         -> 401
  /health/../api/v1/todos    -> 302
  /%2e%2e/api/v1/todos       -> 302
  ```
  **無任一變體可取得受保護資源，無阻擋級。**

  **第 9 組：清理（未用 --force／-D）**
  ```bash
  cd <worktree> && git status --short          # 空 = 無未提交變更
  git branch --merged main | grep T-0039       # + task/T-0039-secret-pin-verify-retry
  git worktree remove "…-wt/T-0039" && git branch -d task/T-0039-secret-pin-verify-retry && git worktree prune
  git worktree list && git branch --list "task/*"
  ```
  ```text
  Deleted branch task/T-0039-secret-pin-verify-retry (was 0b0ba54).
  --- after ---
  C:/Users/excal/Projects/Claude/soft-dev-team-260919 854c843 [main]
  （git branch --list "task/*" 無輸出）
  ```

- 下一步建議：
  - Leader 於 `tasks/_done.md` 追加一列（派工時間取 `worklog/handoff/20260920-1836-T0039-r1-dev-ops.md` A 段開工時間，完成時間 `2026-09-20T20:22:00+08:00`，回合 2）。
  - 使用者若要肉眼核對 STEP_SUMMARY 表格，登入 GitHub 後開 https://github.com/jsfan910/PJ002/actions/runs/35509795862 。純屬確認，不影響本卡判定。
  - T-0040（plan-sd 調整 06 規格文字）已在 main 上更新 06，可與本卡結果對齊後收斂；06 §6.9.1 目前記的是 r2 的失敗事實，建議由 T-0040 或後續維運卡追記「2026-09-20 補授 `roles/secretmanager.viewer` 後 run `35509795862` 全綠、revision `todo-app-00017-qg4` 釘定 1／3／1」一句，避免只留失敗版本。
  - 本卡只做機制，未做帳密輪換。使用者之後在 Secret Manager 新增版本後，下一次部署會自動釘到新的 ENABLED 最新版本，可用 `gcloud run revisions describe` 的 `secretKeyRef.key` 直接核對。
  - 建議把「本機 `gh` 未安裝，CI run 的複驗改以空 commit 觸發＋匿名 REST API 取狀態；job summary 匿名不可讀，改查 Cloud Run revision 的 `secretKeyRef`」沉澱為 CLAUDE.md 環境鐵則一條。
- 需要 Leader 裁決的事：
  - 無。
