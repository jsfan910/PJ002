# T-0018 r1 — dev-tl — 工作紀錄與交接

<!-- 檔名：worklog/handoff/20260919-1133-T0018-r1-dev-tl.md（時間 = 開工時間，取實查系統時間） -->
<!-- 規則：開工立即寫 A 段；完工或中止時寫 B 段。同一輪同一角色一個檔。 -->

## A. 開工紀錄

- 開工時間：2026-09-19T11:33:00+08:00
- 角色／模型：dev-tl / Claude Opus 5（1M context）
- 任務卡：tasks/T-0018-deploy-monitor.md（round 1）／**審核 T-0018**
- 讀取的輸入（實際讀過的檔案與段落）：
  - `.claude/agents/dev-tl.md`、`.claude/skills/review-round/SKILL.md` 全文
  - `tasks/T-0018-deploy-monitor.md` 全文（含分支上被補列的 outputs 八項）、acceptance 六條
  - `docs/specs/10_開發計畫_WBS.md` §0（硬前提 1~8，特別是第 5、6 條）、§1.8 WI-08 全文（含驗收方式草案 11 條指令、遠端待驗說明）、§2 批次 5、§5 共用檔案規約（`README.md` 由 WI-01 初版、WI-08 補部署章節）
  - `docs/specs/06_部署架構與CICD.md` **第 3 章全文**（3.1 ci、3.2 deploy-staging 五階段表與「為什麼 migrate 在 build & push 之前」、3.2.1 WIF 六步與 attribute condition 警告、3.2.2 備選金鑰、3.3 monitor-health），以及第 1 章環境清單、5.4 演練要求、第 6 章
  - `docs/specs/adr/ADR-0005-雲端平台-CloudRun.md`（本卡 IaC 去留的授權來源）
  - `tasks/E-001-todo-app.md` Leader 裁決紀錄（T-0010 四項、T-0011 三項、dev-tl-①②、T-0013-②③、T-0012-①、**T-0018 兩項**）
  - `worklog/handoff/20260919-0949-T0018-r1-dev-ops.md` B 段全文（6 條假設與決策、驗證輸出、5 條下一步建議、3 條需 Leader 裁決）
  - 受審產出全文：`infra/cloudrun-service.yaml`、`.github/workflows/deploy-staging.yml`、`.github/workflows/monitor-health.yml`、`scripts/verify-health.ts`、`scripts/deploy-staging.sh`、`scripts/rollback-staging.sh`、`README.md` 變更段落、`docs/specs/06_部署架構與CICD.md` 的三處 diff
- 上一輪交接摘要（若 round > 1）：不適用（round 1）
- 本輪計畫（3–7 條）：
  1. prettier YAML 檢查 ＋ actionlint（官方 Docker 映像）跑三個工作流
  2. 逐條重跑 WBS §1.8 驗收方式草案 11 條指令；憑證掃描（補強樣式）與真實專案 ID／金鑰樣式掃描
  3. `docker build`；`docker compose up` 後驗 `/health` 無憑證 200、`/api/v1/todos` 帶憑證 200、無憑證 401
  4. 逐字比對 `deploy-staging.yml` 階段順序與 06 §3.2；monitor 每 5 分鐘、不帶憑證、無查詢字串；WIF 為主、無長期金鑰；06 只動三處
  5. README 方式一與方式二各在乾淨 shell（Git Bash 與 PowerShell）逐字實測並記錄耗時
  6. 判定、合併、更新任務卡與審核紀錄表
- 預期產出：本審核交接檔、任務卡 `status: done` ＋ 審核紀錄一列、main 上的合併 commit

## B. 交接

- 完工時間／狀態：2026-09-19T11:40:00+08:00 / **done（初審通過，已合併 main）**

### 完成事項

- 逐條核對 acceptance 六條，全部通過（證據見下「驗證方式」§1～§9）。
- 對交接檔 6 條「假設與決策」逐條表態：**全部接受**。
- 判定 **done**，以 `git merge --no-ff` 合併 `task/T-0018-deploy-monitor` 至 main（commit `22018da`）。
- 三件事提交 Leader（皆不阻擋合併）：`scripts/*.sh` 與硬前提 6 的字面衝突、outputs 由開發者自行補列、派工提示詞的階段順序與 06 §3.2 不一致。見下「需要 Leader 裁決的事」。

### 產出物（路徑）

- 本審核交接檔 `worklog/handoff/20260919-1133-T0018-r1-dev-tl.md`
- `tasks/T-0018-deploy-monitor.md`（`status: done`、`updated`、審核紀錄表一列）
- main 合併 commit `22018da T-0018: 合併 staging 部署（Cloud Run）、健康監測與回滾程序（dev-tl 初審通過）`

### 未完成／已知問題

1. **被憑證阻擋的最後一步（不列為不通過，依 Leader 指示）**：實際 `gcloud` 部署、WIF 換證、Secret Manager 建立、Artifact Registry 推送、**回滾實機演練（06 §5.4）**、**NFR-003 的 24 小時採樣**，全部需要使用者先提供 GCP 專案與 GitHub repo。本卡已把離線可驗的部分做到最後一步。**使用者要做的事清單確實已列**：`README.md`「部署與 secrets（T-0018）」章節（六大步＋兩張 GitHub variables／secrets 表格＋倉庫可見性建議）、交接檔「下一步建議」1、2 與「需要 Leader 裁決的事」3。agent 未索取、未代填任何憑證值（已逐檔掃描確認，見 §4）。
2. **`scripts/deploy-staging.sh`／`scripts/rollback-staging.sh` 是 `.sh` 檔**，與 WBS §0 硬前提 6 的括號「（不得使用 `make`、`*.sh`、`/bin/bash` 專屬語法）」字面衝突。詳見「需要 Leader 裁決的事」1 —— 我以技術負責人身分判為**不阻擋**並說明理由，但請 Leader 追認。
3. **README「常用指令」表第 8 列仍寫「驗證 `/health`（尚未實作，由 T-0018 補上）」**，本卡已實作 `scripts/verify-health.ts`，該行已過時。屬合併後的文件不一致，由我於批次 5 收尾 commit 一併修正（含在目錄結構補 `infra/`、`scripts/`），不退回本卡。
4. **憑證掃描有 4 處結構性假陽性**（`id-token: write` 1 處、`--set-secrets "DATABASE_URL=database-url:latest,…"` 3 處，分別在 `deploy-staging.yml`、`scripts/deploy-staging.sh`、`README.md`）。**我逐行檢視確認全部是語法關鍵字或 Secret Manager 參照名稱，沒有任何值**；接受為已知假陽性（比照 Leader 裁決 dev-tl-② 對 `ci.yml` 佔位值的先例），並建議納入 qa-cr 檢查表的已知例外（見「下一步建議」）。

### 我做的假設與決策（含理由）

對 dev-ops 交接檔「假設與決策」1～6 的逐條表態：

1. **①（階段順序依 06 §3.2 逐字 `auth → migrate → build & push → deploy → verify`，而非派工提示詞的 `build → push → migrate → deploy`）：接受，且這是正確做法。** 我逐字讀了 06 §3.2 的五階段表與其下「**為什麼 migrate 在 build & push 之前**」整段（expand-contract 的 expand 階段：先 migrate 再部署，即使部署失敗舊程式碼在新 schema 上仍能運作），以及 WBS §1.8「階段順序固定：**migrate → deploy → verify**」。**兩份凍結文件一致，派工提示詞的摘要順序是誤寫**；依硬前提 1（規格凍結、規格優先）與 Leader 對 T-0016-① 的先例（提示詞與規格衝突時規格優先），dev-ops 依規格實作正確。已列入「需要 Leader 裁決的事」3 供追認。
2. **②（Secret Manager 命名 `database-url`／`basic-auth-user`／`basic-auth-pass`）：接受。** Leader 已於 2026-09-19T10:20:10 裁決「Secret Manager 命名歸 T-0018，接受」。命名刻意避開整字 `password`／`secret`／`database_url`／`token`，確實降低了掃描噪音（§4 顯示三個新 YAML 用 WBS 原始樣式掃描為**零命中**）。
3. **③（README PowerShell 的 `.env` 載入改用 `[System.IO.File]::ReadAllLines()`）：接受。** Leader 裁決 dev-tl-① 的原文是「PowerShell 逐行 `$env:` 設定」，dev-ops 的寫法仍是逐行 `Set-Item env:`，**意圖一致、實作更正確**：我在乾淨 PowerShell session 逐字執行該片段，六個變數全部正確載入（`PORT=8080 DATABASE_URL_SET=True BASIC_AUTH_USER=dev LOG_LEVEL=info NODE_ENV=development`），服務起得來、`/health` 回 200（§8）。且此寫法避免在 README 重列佔位值（否則會製造新的掃描命中與兩處維護）。
4. **④（`verify:health` 逾時本機 2s／60s、CI 覆寫 10s／300s）：接受。** 06 §3.2 verify 階段明文「每 10 秒一次、**上限 5 分鐘**」並附理由（Cloud Run 冷啟 1–3 秒）；WBS §1.8 的「上限 10 分鐘」是 Render 時代的殘留，06 後於 WBS 修訂且同為凍結文件，採 06 正確。`deploy-staging.yml` 第 130–131 行確實覆寫為 `"10"`／`"300"`。
5. **⑤（`infra/cloudrun-service.yaml` 定位為文件化 IaC，日常部署以 workflow 的 `gcloud run deploy` 參數為事實來源）：接受。** ADR-0005「後果」段落把此取捨授權給 dev-ops；理由（該檔無法自動代入 `${{ github.sha }}`）成立。**我逐項比對兩者參數：埠 8080、512Mi、CPU 1、minScale 0、maxScale 2、並行 80、逾時 60s、`NODE_ENV=production`／`LOG_LEVEL=info`／`CORS_ALLOWED_ORIGINS=""`、三個 Secret Manager 參照 —— 逐項一致，沒有漂移。**
6. **⑥（本機驗證改用 8081/8082/8083/8084 與 5433 埠以避開平行的 T-0017）：接受。** 該判斷在當時是對的（T-0017 確實佔用 8080/5432）。**但這也代表 dev-ops 沒能在 README 寫的 8080 埠上逐字驗證。** 我在 T-0017 已合併、容器已清空之後，**完全依 README 字面的 8080 埠重跑方式一與方式二各兩種 shell**，四次全部 `/health` 回 200（§8），補上了這個缺口。
7. **（我自己的決策）`scripts/*.sh` 不阻擋合併**：理由見「需要 Leader 裁決的事」1。
8. **（我自己的決策）遠端部署與 24 小時採樣不列為不通過**：依 Leader 派工指示與 WBS §1.8「本卡 review 時以本地驗收為準，遠端項目列入卡上『遠端待驗』清單交 Leader 追蹤」。

### 驗證方式（指令 + 實際輸出）

全部在 worktree `C:/Users/excal/Projects/Claude/soft-dev-team-260919-wt/T-0018`（分支 `task/T-0018-deploy-monitor`，commit `f8240aa`）執行。

**§1. prettier（YAML 可解析）**

```bash
npx --yes prettier@3 --check "infra/cloudrun-service.yaml" ".github/workflows/*.yml"; echo "PRETTIER_EXIT=$?"
```
```text
Checking formatting...
All matched files use Prettier code style!
PRETTIER_EXIT=0
```

**§2. actionlint（官方 Docker 映像，三個工作流一次過）**

```bash
MSYS_NO_PATHCONV=1 docker run --rm -v "//c/Users/excal/Projects/Claude/soft-dev-team-260919-wt/T-0018:/repo" -w //repo \
  rhysd/actionlint:latest -color .github/workflows/deploy-staging.yml .github/workflows/monitor-health.yml .github/workflows/ci.yml
echo "ACTIONLINT_EXIT=$?"
```
```text
（零 finding，無任何輸出）
ACTIONLINT_EXIT=0
```

**§3. WBS §1.8 驗收方式草案逐條重跑**

```bash
grep -nE "(PASSWORD|SECRET|DATABASE_URL|_KEY|_TOKEN)[[:space:]]*:[[:space:]]*[^$\{[:space:]]" infra/cloudrun-service.yaml .github/workflows/*.yml; echo "exit=$?"
grep -nE "(PASSWORD|SECRET|DATABASE_URL|_KEY|_TOKEN)[[:space:]]*:[[:space:]]*[^$\{[:space:]]" infra/cloudrun-service.yaml .github/workflows/deploy-staging.yml .github/workflows/monitor-health.yml; echo "exit=$?"
grep -n "/health" infra/cloudrun-service.yaml
grep -nE "workload_identity_provider|auth@v" .github/workflows/deploy-staging.yml
grep -nE "credentials_json|service_account_key|\.json$" .github/workflows/deploy-staging.yml; echo "exit=$?"
grep -n "artifactregistry\|Artifact Registry\|docker.pkg.dev" .github/workflows/deploy-staging.yml
grep -n "cron" .github/workflows/monitor-health.yml
grep -nE "(-u |Authorization|BASIC_AUTH)" .github/workflows/monitor-health.yml; echo "exit=$?"
grep -nE "/health\?" .github/workflows/monitor-health.yml; echo "exit=$?"
grep -n "migrate" .github/workflows/deploy-staging.yml | head -3
```
```text
=== 1. WBS 原始憑證樣式（全部 workflow + infra） ===
.github/workflows/ci.yml:69:          POSTGRES_PASSWORD: "ci"
.github/workflows/ci.yml:81:      DATABASE_URL: "postgres://ci:ci@localhost:5432/todo"
.github/workflows/ci.yml:83:      BASIC_AUTH_PASSWORD: "ci"
exit=0      ← 三筆全部來自 ci.yml（T-0011 既有、非本卡 outputs），Leader 裁決 dev-tl-② 已接受為本機/CI 佔位值

=== 2. 同樣式只掃本卡三個新 YAML ===
exit=1      ← 零命中

=== 3. /health in cloudrun-service.yaml ===
17:#   - startup probe 指向 GET /health（唯一未保護路徑，BR-017／O-005）
34:    # 整站，/health 為唯一豁免（ADR-0004）。
87:          # 路徑一律用完全等於 "/health" 的字串（不可有點節段變體，
91:              path: /health          ← startupProbe
99:              path: /health          ← livenessProbe
（兩處 probe 路徑都是完全等於 "/health" 的字串，無點節段變體，符合 T-0015 交接檔的提醒）

=== 4. WIF ===
63:        uses: google-github-actions/auth@v2
65:          workload_identity_provider: ${{ secrets.GCP_WIF_PROVIDER }}

=== 5. 長期服務帳號金鑰 ===
exit=1      ← 零命中（無 credentials_json／service_account_key／*.json）

=== 6. Artifact Registry ===
72:        run: gcloud auth configure-docker "${GCP_REGION}-docker.pkg.dev" --quiet
85:      # ---- build & push（Artifact Registry，SHA 標籤為事實來源） ----
88:          IMAGE: ${{ vars.GCP_REGION }}-docker.pkg.dev/${{ vars.GCP_PROJECT_ID }}/${{ vars.GCP_AR_REPOSITORY }}/todo-app
98:          IMAGE: ${{ vars.GCP_REGION }}-docker.pkg.dev/${{ vars.GCP_PROJECT_ID }}/${{ vars.GCP_AR_REPOSITORY }}/todo-app

=== 7. cron ===
16:    - cron: "*/5 * * * *"      ← 每 5 分鐘（06 §3.3、O-009 裁決）

=== 8. monitor 不帶憑證 ===
exit=1      ← 零命中（無 -u／Authorization／BASIC_AUTH）

=== 9. /health 不附查詢字串 ===
exit=1      ← 零命中

=== 10. migrate 在 deploy 之前 ===
4:# 階段順序固定：auth → migrate → build & push → deploy → verify
```

**§3b. `deploy-staging.yml` 階段順序逐字比對 06 §3.2**

```bash
grep -nE "^      - name:" .github/workflows/deploy-staging.yml
```
```text
43:      - name: checkout
46:      - name: setup-node
52:      - name: npm ci
55:      - name: npm run build                      ← migrate 讀 dist/（Leader 裁決 T-0013-②）
61:      - name: auth（Workload Identity Federation）   ← 06 §3.2 階段 1
68:      - name: setup-gcloud
71:      - name: gcloud auth configure-docker
75:      - name: migrate（NEON_DATABASE_URL，forward-only） ← 階段 2
86:      - name: docker build & push                  ← 階段 3
96:      - name: gcloud run deploy                    ← 階段 4
117:      - name: verify（取服務網址）                 ← 階段 5
127:      - name: verify（輪詢 /health 至 200）        ← npm run verify:health，覆寫 10s/300s
134:      - name: verify（帶憑證呼叫 /api/v1/todos 確認 200）
145:      - name: 回填提示（首次部署後的網址）
```
與 06 §3.2 的五階段表**逐字一致**（auth → migrate → build & push → deploy → verify），deploy 用 `${{ github.sha }}` 標籤而非 `latest`（06 明文要求），verify 覆寫為每 10 秒／上限 300 秒（06 §3.2「上限 5 分鐘」）。`workflow_run` 監聽的 `workflows: ["CI"]` 與 `ci.yml` 的 `name: CI` 對得上（已 `grep -n "^name:" .github/workflows/ci.yml` 確認）。

**§4. 憑證掃描（dev-tl 補強樣式）＋ 真實專案 ID／金鑰樣式掃描**

```bash
grep -nEi "(password|secret|database_url|api[_-]?key|token)[[:space:]]*[:=][[:space:]]*[\"]?[A-Za-z0-9/+_.-]{2,}" \
  infra/cloudrun-service.yaml .github/workflows/deploy-staging.yml .github/workflows/monitor-health.yml \
  scripts/*.sh scripts/verify-health.ts README.md docs/specs/06_部署架構與CICD.md; echo "exit=$?"
grep -nEi "(-----BEGIN|AIza[0-9A-Za-z_-]{20,}|ya29\.|projects/[0-9]{6,}|[0-9]{10,}-[a-z0-9]{20,}\.apps\.googleusercontent)" <同一組檔案>; echo "exit=$?"
git diff main...HEAD --name-only | grep -E "\.json$|key|credential"; echo "exit=$?"
```
```text
=== 補強樣式 ===
.github/workflows/deploy-staging.yml:26:  id-token: write # WIF 用：向 GCP 換取短期憑證
.github/workflows/deploy-staging.yml:114:  --set-secrets "DATABASE_URL=database-url:latest,BASIC_AUTH_USER=basic-auth-user:latest,BASIC_AUTH_PASSWORD=basic-auth-pass:latest"
scripts/deploy-staging.sh:67:            --set-secrets "DATABASE_URL=database-url:latest,BASIC_AUTH_USER=basic-auth-user:latest,BASIC_AUTH_PASSWORD=basic-auth-pass:latest"
README.md:210:  （同一行 --set-secrets 語法的說明文字）
exit=0

=== 真實專案 ID／金鑰樣式（PEM 標頭、AIza…、ya29.、projects/<純數字專案編號>、OAuth client id） ===
exit=1      ← 零命中，**倉庫中沒有任何真實專案 ID 或憑證值**

=== 變更檔案中是否有 .json／key／credential 檔 ===
exit=1      ← 零命中
```
**逐行判讀**：`id-token: write` 是 GitHub Actions 授予 OIDC token 的 permission 語法（值 `write` 是權限層級）；其餘三處是 `gcloud run deploy --set-secrets` 的 `<應用環境變數名>=<Secret Manager secret 名>:<版本>` 參照語法，右側 `database-url`／`basic-auth-user`／`basic-auth-pass` **是 secret 的名字，不是值**。左側 `BASIC_AUTH_PASSWORD` 等名稱由 06 §4.1 凍結、不可改名。**確認無任何憑證值，接受為結構性假陽性。**

**§5. `docs/specs/06_部署架構與CICD.md` 只動三處**

```bash
git diff main...HEAD -- "docs/specs/06_部署架構與CICD.md"
```
```text
三個 hunk：
  @@ -24,7 +24,7 @@   第 1 章環境清單 staging 列 → 「待首次部署後回填」＋觸發條件說明
  @@ -240,7 +240,7 @@  5.4 演練要求 → 演練紀錄 2026-09-19，「尚未實際演練（被憑證阻擋）」＋已備妥指令對照
  @@ -283,6 +283,13 @@  新增 6.4 實作紀錄（artifact 落點、Actions 額度選「公開倉庫」、24 小時採樣待遠端）
「變更紀錄」表未被觸碰；設計內容（第 2、3、4 章與 5.1~5.3）一字未改。
```
與任務卡 outputs 限定的三處**完全吻合**，全部是實作紀錄、沒有變更任何設計。

**§6. 本機 `docker build`**

```bash
docker build -t todo-app:staging .; echo "BUILD_EXIT=$?"
```
```text
#19 naming to docker.io/library/todo-app:staging done
#19 unpacking to docker.io/library/todo-app:staging done
#19 DONE 0.1s
BUILD_EXIT=0
```

**§7. compose 起服務後的三條權威驗證（`/health` 無憑證 200、`/api/v1/todos` 無憑證 401／帶憑證 200）**

```bash
cp .env.example .env
docker compose up -d --build
set -a; . ./.env; set +a
DATABASE_URL="postgres://dev:dev@localhost:5432/todo" npm run migrate
curl -sS -o /dev/null -w "health-no-cred=%{http_code}\n" http://localhost:8080/health
curl -sS http://localhost:8080/health
curl -sS -o /dev/null -w "api-no-cred=%{http_code}\n" http://localhost:8080/api/v1/todos
curl -sS -o /dev/null -w "api-with-cred=%{http_code}\n" -u dev:dev http://localhost:8080/api/v1/todos
npm run verify:health
```
```text
 Container t-0018-db-1 Healthy / Container t-0018-app-1 Started
已套用 migration：001
health-no-cred=200
{"status":"ok"}
api-no-cred=401
api-with-cred=200
> node scripts/verify-health.ts
[verify-health] 第 1 次：200 http://localhost:8080/health
```
（**這次 `npm run verify:health` 打的是本卡自己 build 的映像**，不像 dev-ops 那次是巧合命中 T-0017 的容器 —— T-0017 已合併、容器已 `down -v` 清空，8080 埠在本次啟動前經 `curl` 確認為 `000`。）

**§8. README 方式一與方式二 —— 在乾淨 shell 逐字實測，四種組合全部 200**

每次實測前都先 `docker compose down -v`、`rm -f .env`，方式二另加 `rm -rf node_modules dist`；**全部使用 README 字面的 8080 埠**（補上 dev-ops 因平行工作而改埠的缺口）。

方式一 — Git Bash：
```bash
cp .env.example .env && docker build -t todo-app:dev . && docker compose up -d
curl -sS -o /dev/null -w "%{http_code}\n" http://localhost:8080/health
curl -sS http://localhost:8080/health
```
```text
200
{"status":"ok"}
方式一 Git Bash 耗時：12 秒
```

方式一 — PowerShell：
```powershell
Copy-Item .env.example .env; docker build -t todo-app:dev .; docker compose up -d
Invoke-WebRequest -UseBasicParsing http://localhost:8080/health | Select-Object StatusCode
(Invoke-WebRequest -UseBasicParsing http://localhost:8080/health).Content
```
```text
StatusCode
----------
       200
{"status":"ok"}
方式一 PowerShell 耗時：00:00:11.0189572
```

方式二 — Git Bash（`rm -rf node_modules dist .env` 後從零開始）：
```bash
cp .env.example .env && npm ci && npm run build
set -a; . ./.env; set +a
npm start &                       # README 寫「另開一個終端機視窗執行下一步」，此處以背景執行等效
curl -sS http://localhost:8080/health
```
```text
{"status":"ok"}
code=200
方式二 Git Bash 耗時：8 秒
```

方式二 — PowerShell（`rm -rf node_modules dist .env` 後從零開始，逐字照抄 README 的 `ReadAllLines` 片段）：
```powershell
Copy-Item .env.example .env; npm ci; npm run build
[System.IO.File]::ReadAllLines("$PWD\.env") | Where-Object { $_ -match '^[A-Z_]+=' } | ForEach-Object {
  $name, $value = $_ -split '=', 2
  Set-Item -Path "env:$name" -Value $value
}
Start-Process node -ArgumentList "dist/server.js" ...   # npm start 的非阻塞等效
(Invoke-WebRequest -UseBasicParsing http://localhost:8080/health).Content
```
```text
PORT=8080 DATABASE_URL_SET=True BASIC_AUTH_USER=dev LOG_LEVEL=info NODE_ENV=development
{"status":"ok"}
200
方式二 PowerShell 耗時：00:00:08.7635320
```
**四種組合全部在 15 分鐘 DoD（NFR-008）內遠遠達標（最慢 12 秒）**，README 方式二的 `.env` 載入片段在兩種 shell 都逐字可用。

**§9. lint、單元測試、shell 語法檢查、只動 outputs**

```bash
npm run lint; echo "LINT_EXIT=$?"
npm run build && npm run test:unit
bash -n scripts/deploy-staging.sh && bash -n scripts/rollback-staging.sh
git diff main...HEAD --stat
git status --short
```
```text
> eslint . && tsc --noEmit -p tsconfig.json
LINT_EXIT=0

ℹ tests 51  ℹ pass 51  ℹ fail 0  ℹ duration_ms 148.8815
（此 worktree 的基底早於 T-0017 合併，故無前端 24 條；合併後的全量數字見批次 5 收尾 commit）

deploy-staging.sh: syntax OK
rollback-staging.sh: syntax OK

 .github/workflows/deploy-staging.yml              | 157 +++++++++++
 .github/workflows/monitor-health.yml              |  83 ++++++
 README.md                                         | 144 +++++++++-
 docs/specs/06_部署架構與CICD.md                   |  11 +-
 infra/cloudrun-service.yaml                       | 103 ++++++++
 scripts/deploy-staging.sh                         |  78 ++++++
 scripts/rollback-staging.sh                       |  66 +++++
 scripts/verify-health.ts                          |  62 +++++
 tasks/T-0018-deploy-monitor.md                    |   7 +-
 worklog/handoff/20260919-0949-T0018-r1-dev-ops.md | 309 ++++++++++++++++++++++
 10 files changed, 1014 insertions(+), 6 deletions(-)

git status --short → （無輸出；`.env`、`node_modules`、`dist`、實測產生的 log 皆已清除或在 .gitignore 內）
```
`docker-compose.yml`／`.env.example`／`package.json`／`src/`／`tests/`／`04_API規格.yaml` 皆未出現在變更清單。**唯一超出 Leader 原始 outputs 的是 `scripts/` 三檔**，見「需要 Leader 裁決的事」1、2。

**§10. 環境收尾**

```bash
docker compose down -v; docker ps --format "{{.Names}}"; curl --max-time 3 http://localhost:8080/health
```
```text
 Volume t-0018_todo-db-data Removed / Network t-0018_default Removed
（docker ps 無輸出）
port8080=000
```

**§11. 合併**

```bash
git merge --no-ff -F <訊息檔> task/T-0018-deploy-monitor
git log --oneline -2
```
```text
 10 files changed, 1014 insertions(+), 6 deletions(-)
22018da T-0018: 合併 staging 部署（Cloud Run）、健康監測與回滾程序（dev-tl 初審通過）
7adc6cb T-0017 審核：status done、審核紀錄、dev-tl 審核交接檔
```

### 遠端待驗清單（交 Leader 追蹤，非本卡不通過項）

| # | 項目 | 阻擋原因 | 解除條件 |
|---|---|---|---|
| 1 | `ci.yml` 四階段在真實 GitHub Actions 全綠 | 本 repo 無遠端 | 使用者提供 GitHub repo |
| 2 | `deploy-staging.yml` 實際跑完 auth→migrate→build&push→deploy→verify | 無 GCP 專案／WIF／Secret Manager／Artifact Registry | 使用者完成 README「部署與 secrets」六步 |
| 3 | `monitor-health.yml` 實際產出 artifact 與 24 小時 NFR-003 採樣 | `STAGING_BASE_URL` 尚無值 | 首次部署成功並回填 variable |
| 4 | 回滾實機演練（06 §5.4，**Gate 2 前置條件**） | 需已部署的 staging 服務與至少兩個 revision | 同 2 |
| 5 | `04_API規格.yaml` 的 staging `servers` 佔位符回填 | 需真實網址 | Leader 已裁決（T-0018-①）：首次部署後另開小卡 |

### 下一步建議

1. **給 Leader／使用者（最高優先）**：staging 相關的一切都卡在使用者的一次性設定上。清單與逐步指令在 `README.md`「部署與 secrets（T-0018）」。**特別提醒使用者：WIF 的 attribute condition 必須限定倉庫**（例如 `assertion.repository == "<owner>/<repo>"`），06 §3.2.1 明文警告「少了這一條，任何 GitHub 倉庫的工作流都能換到你的 GCP 憑證」—— 這是本專案唯一一個「設錯就是重大安全漏洞」的使用者步驟，建議 Leader 在交付時單獨口頭強調，並請 qa-cr 在使用者完成設定後逐字複檢這一行。
2. **給 qa-cr（程式碼審查，T-0020）**：憑證掃描樣式對本專案有**兩個結構性假陽性模式**，建議直接列入檢查表的「已知例外」而不是每卡重新論證：(a) GitHub Actions 的 `permissions: id-token: write`（WIF 必要語法）；(b) `gcloud run deploy --set-secrets "ENV_NAME=secret-name:version"`（Secret Manager 參照，右側是名字不是值）。加上 Leader 裁決 dev-tl-② 已接受的 `ci.yml` 佔位值，共三類。**其餘任何命中都必須當成真命中處理。**
3. **給 qa-at（P0 UAT，T-0022）／qa-lead**：staging 未就緒前，所有 staging 相關 TC 依 Leader 指示記「阻擋（環境未就緒）」。本機驗收可用 README 方式一（Git Bash 或 PowerShell 皆可，實測 11～12 秒起得來）。`npm run verify:health` 可直接當作「服務是否就緒」的等待指令（`BASE_URL`／`POLL_INTERVAL_SECONDS`／`POLL_TIMEOUT_SECONDS` 可覆寫）。
4. **給日後任何要在 PowerShell 讀 `.env` 的卡**：沿用 dev-ops 的發現 —— Windows PowerShell 5.1 的 `Get-Content` 對「UTF-8 無 BOM ＋ 含中文註解」的檔案會誤判編碼併行，必須用 `[System.IO.File]::ReadAllLines()`。我已逐字重跑確認 `ReadAllLines` 版本正確載入六個變數。
5. **給 Gate 2 前的收尾**：回滾演練（06 §5.4）是 Gate 2 的**前置條件**且目前是「尚未實際演練」，請 Leader 把它掛在 Gate 2 檢核表上，不要讓它隨著本卡 done 而消失在視線外。

### 需要 Leader 裁決的事

1. **`scripts/deploy-staging.sh`／`scripts/rollback-staging.sh` 是 `.sh` 檔，與 WBS §0 硬前提 6 的括號「不得使用 `make`、`*.sh`、`/bin/bash` 專屬語法」字面衝突 —— 請追認我的處理。**
   - 我判為**不阻擋合併**，理由三點：(a) 硬前提 6 的正文是「全部指令須可在 **Git Bash 或 PowerShell** 執行」，這兩支腳本在 Git Bash 可執行，且 README 明寫 `bash scripts/...` 的用法；(b) 專案的**標準指令**（build／test／lint／start／migrate／verify:health）全部仍是跨 shell 的 npm script，這兩支只是 `gcloud` 部署／回滾的選用輔助，其內容在 06 §5.1 與 README 都有等價的逐條指令，刪掉也不影響任何驗收；(c) WBS §1.8 要求「每個 run step 的實際指令都要有等價的本地 script」，而 `package.json` 由 T-0011 單一擁有且已凍結，dev-ops 無法新增 npm script，只能落在檔案層。
   - **若 Leader 認為硬前提 6 應嚴格執行**，建議的修法是把兩支改寫為 `scripts/*.mjs`（Node 可跨 shell 執行、不需 bash），另開一張小卡處理即可，**不需要退回本卡**。
2. **任務卡 `outputs` 由開發者自行補列三個 `scripts/*` 檔案，請追認。** `outputs` 依協定由 Leader 擁有。其中 `scripts/verify-health.ts` 實質上**無可迴避**（`package.json` 的 `verify:health` 由 T-0011 定義為 `node scripts/verify-health.ts`，該檔不存在則 WBS §1.8 的驗收指令 `npm run verify:health` 根本跑不起來），我視為在範圍內；另兩支 `.sh` 與第 1 點一併裁決。
3. **派工提示詞寫的階段順序「build → push → migrate → deploy」與凍結規格不符，請確認以規格為準。** 06 §3.2 的五階段表與其下「為什麼 migrate 在 build & push 之前」，以及 WBS §1.8「階段順序固定：migrate → deploy → verify」，都要求 **migrate 先於 build & push 與 deploy**（expand-contract 的 expand 階段）。dev-ops 依規格實作正確，我依硬前提 1 與 Leader 對 T-0016-① 的先例（提示詞誤寫時規格優先）判為通過。**若 Leader 其實想改變階段順序，那是規格變更，須走規格變更請求卡，不是開發卡能決定的。**
