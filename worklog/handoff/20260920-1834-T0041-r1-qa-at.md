# T-0041 r1 — qa-at — 工作紀錄與交接

<!-- 檔名：worklog/handoff/20260920-1834-T0041-r1-qa-at.md -->

## A. 開工紀錄

- 開工時間：2026-09-20T18:34:37+08:00
- 角色／模型：qa-at / sonnet
- 任務卡：tasks/T-0041-firefox-compat-rerun.md（round 1）
- 讀取的輸入（實際讀過的檔案與段落，非任務卡照抄）：
  - docs/specs/07_測試計畫.md 第 4 章退出準則第 10 項（NFR-004 相容性 6 組全過，待填 __/6）、第 7 章 R-5（NFR-004 改讀「最新版即可」，前一版不要求）
  - docs/specs/20_測試案例.md#TC-091（3 瀏覽器 × 2 尺寸，步驟與預期結果、備註引用 R-5）
  - docs/reports/20260919-1719-AT-E001-r3.md：grep 未命中 "firefox/E-B-09" 字面字串，改由 tasks/E-001-todo-app.md 裁決紀錄確認上游狀況（Firefox 於沙盒無法啟動，4/6 先行）
  - tests/e2e/playwright.config.ts：確認兩個 Firefox project 名稱為 `firefox-1280x800`、`firefox-390x844`；`use.launchOptions` 對 Chromium/Edge 加了 `--disable-http2 --disable-quic`（僅為 staging HTTP/2/QUIC 問題，對本機 compose 無影響）
  - README.md：`npm run test:e2e -- --project=...` 用法、`npx playwright install chromium` 提示（本卡需另跑 `npx playwright install firefox`）、`POSTGRES_HOST_PORT`／`PORT` 環境變數說明
  - docker-compose.yml：`app.ports` 用 `${PORT}:${PORT}`、`db.ports` 用 `${POSTGRES_HOST_PORT:-5432}:5432`，確認可用 `.env` 覆寫達成本卡要求的 8082／5434
  - tasks/E-001-todo-app.md：2026-09-19T12:32:40 裁決（Firefox 於非沙盒環境補跑列 Gate 2 後待辦）；2026-09-20 使用者 Gate 2 r2 裁決③ A 段「先清 Gate 2 後待辦」列出 T-0041 立即派工
- 上一輪交接摘要（若 round > 1）：不適用（round 1）
- 本輪計畫（3–7 條）：
  1. 建立獨立 `.env`（PORT=8082、POSTGRES_HOST_PORT=5434，其餘沿用 .env.example 佔位值），確認不與 T-0038（8081/5433）衝突
  2. `docker compose up -d` 起本機 app+db（用專屬 `-p` 專案名避免與其他卡的 compose 專案互撞）
  3. `npx playwright install firefox` 實際執行並記錄輸出；若失敗記錄完整錯誤與已嘗試替代方案
  4. 若 Firefox 可啟動：`npm run test:e2e -- --project=firefox-1280x800 --project=firefox-390x844`，貼 Playwright 摘要
  5. 依結果判定 TC-091：全過→traceability 主表該列狀態更新為 6/6；有失敗→逐條列產品缺陷／工具限制判斷，不自行立案
  6. `docker compose down` 清理，寫報告 `docs/reports/20260920-HHmm-AT-E001-r4-firefox.md`
  7. 收尾：填交接檔 B 段、任務卡 status、commit
- 預期產出：
  - docs/reports/20260920-HHmm-AT-E001-r4-firefox.md
  - docs/specs/traceability.md（僅 TC-091 所在列狀態欄，視結果而定）

## B. 交接

- 完工時間／狀態：2026-09-20T19:01:08+08:00 / blocked
- 完成事項：
  - 啟動本機 Docker Desktop（本機原本未在跑；第一次 `cmd /c start` 未生效，改直接執行 exe 才成功，過程中收到 Leader 18:55 的「停止輪詢」提醒，已即時 SendMessage 回報實際狀態並停止輪詢）
  - `docker compose -p t0041-firefox --env-file <scratchpad>/t0041.env up -d --build`：PORT=8082、POSTGRES_HOST_PORT=5434，與 T-0038（8081/5433）錯開；`curl -u dev:dev http://localhost:8082/health` 回 200
  - `npx playwright install firefox`（exit 0，二進位檔已存在）；`npx playwright install --force firefox`（重新下載 122 MiB，exit 0）排除「二進位檔損毀」假設
  - `npm run test:e2e -- --project=firefox-1280x800 --project=firefox-390x844`：68 條全數於 `browser.launch` 階段以 `Error: browserType.launch: spawn UNKNOWN` 失敗（18~34ms 內），與 AT r3 E-B-09 錯誤字面相同
  - 對照測試：同 session 同服務下 `--project=chromium-1280x800`（子集 4 條 TC）4/4 通過，證明本機服務／環境變數／launchOptions 皆非原因
  - 用 Node `child_process.spawn` 直接呼叫 `firefox.exe`（繞過 Playwright）重現 `Error: spawn UNKNOWN`（`errno: -4094, code: 'UNKNOWN'`），定位為 Windows CreateProcess 層級失敗，早於 Firefox 本身程式碼執行
  - 檢查系統版 Firefox：`C:\Program Files\Mozilla Firefox\firefox.exe`、`(x86)` 路徑、`where firefox` 皆查無安裝，無法以 `executablePath` 替代；Playwright 的 `channel` 參數本身也不支援一般 Firefox（僅 Chromium 系列）
  - 檢查 `PLAYWRIGHT_BROWSERS_PATH`：未設定，且此變數只影響安裝位置查找，不影響 CreateProcess 層級失敗，經重新下載驗證後判斷無需另行嘗試
  - 檢查 Windows Defender：即時保護為開啟狀態，但本 session 非系統管理員，無法查看/調整排除清單（`Get-MpPreference` 回 `Must be an administrator to view exclusions`）；調整資安設定亦不在本角色工具授權範圍內，僅記錄為可能成因，未實際變更任何系統/安全設定
  - 清理：`docker compose -p t0041-firefox down` + `docker volume rm t0041-firefox_todo-db-data`，未動到其他卡的容器/埠/資料
  - 寫報告、traceability 該列狀態欄補註、任務卡改 blocked
- 產出物（路徑）：
  - docs/reports/20260920-1901-AT-E001-r4-firefox.md
  - docs/specs/traceability.md（US-008 該列「狀態」欄，僅追加內容，未動其他欄／列）
- 未完成／已知問題：
  - NFR-004（TC-091）維持 4／6，Firefox 兩組仍待使用者在自己機器（非本工具沙盒）驗證，指令見報告「讓使用者在自己機器上執行的單一指令」一節
  - 未能定論 Windows Defender 是否為根因（無管理員權限查排除清單），僅提出為可能成因之一，供使用者在自己機器排查時參考
- 我做的假設與決策（含理由）：
  - 判定本次失敗為「工具／執行環境限制」而非「產品缺陷」：理由是同一 session、同一設定、同一服務下 Chromium 對照組正常，且底層錯誤發生在 Windows CreateProcess（早於 Firefox 任何程式碼執行），與應用程式碼／頁面邏輯無關；因此依任務卡「不自行立案缺陷（交 qa-lead）」，未建立缺陷單，僅在報告與 traceability 記錄判定與證據
  - 未修改任何系統或安全設定（Defender、服務啟動方式等），僅用一般使用者權限可用的診斷指令（因超出角色工具授權與任務卡範圍）
  - 未修改根目錄 `.env`（該檔為其他平行任務可能同時使用的共用檔案），改用 `--env-file` 指向 scratchpad 內的獨立 env 檔案，並用 `-p t0041-firefox` 專屬 compose 專案名，確保與 T-0038/T-0039 完全隔離
- 驗證方式（接手者如何確認我做的是對的：指令 + 我實際得到的輸出）：
  ```bash
  grep -n "firefox" tests/e2e/playwright.config.ts
  ```
  ```text
  62:      name: "firefox-1280x800",
  63:      use: { ...devices["Desktop Firefox"], viewport: { width: 1280, height: 800 } }
  66:      name: "firefox-390x844",
  67:      use: { ...devices["Desktop Firefox"], viewport: { width: 390, height: 844 } }
  ```
  ```bash
  grep -n "TC-091" docs/specs/traceability.md
  ```
  ```text
  84:| NFR-004 | 相容性 | FE-01 無框架、ES2020、flexbox 單一斷點、最小點擊區 44×44 | qa-at | TC-091 |
  （US-008 該列亦含 TC-091，內容已於本輪追加 T-0041 判定文字）
  ```
  ```bash
  # 完整 e2e 指令與輸出見報告「原始輸出」節；核心錯誤：
  # Error: browserType.launch: spawn UNKNOWN
  #   68 failed（firefox-1280x800 34 條 + firefox-390x844 34 條）
  ```
- 下一步建議：
  - qa-lead：判斷是否需為此環境限制另立 P1／S3 觀察項（不計入 Gate 2 擋關項），並在下一輪測試總結第 10 項寫「4／6，Firefox 待使用者自有機器驗證」
  - 使用者：於自己機器（非本工具沙盒）依報告指令跑一次，若通過則回填 traceability 為 6／6；若同樣失敗則升級為需排查該機器的資安/權限設定
  - dev-tl（T-0042）：Release Notes 已知限制欄可引用本報告
- 需要 Leader 裁決的事：
  - 無（本卡已依驗收條件的「若本環境仍無法啟動 Firefox」分支處理，改列 blocked 並附完整證據與替代方案；如需另行裁決是否要求使用者立即補測，留待 Leader 視排程決定）
