# T-0002 r1 — plan-sa — 審核紀錄與交接

<!-- 檔名：worklog/handoff/20260919-0541-T0002-r1-plan-sa.md（開工時間，實查系統時間） -->

## A. 開工紀錄

- 開工時間：2026-09-19T05:41:54+08:00
- 角色／模型：plan-sa / Claude Opus 5（1M context）
- 任務卡：tasks/T-0002-srs.md（round 1）
- 本次工作性質：**審核 T-0002**（依 `.claude/skills/review-round/SKILL.md`，審核者為本角色）
- 讀取的輸入（實際讀過的檔案與段落）：
  - `.claude/agents/plan-sa.md`（全檔）—— 取得本角色審核 SRS 的判準：每個 AC 問「能不能寫成一個測試案例」，找 US 之間的矛盾。
  - `.claude/skills/review-round/SKILL.md`（全檔）—— 取得審核一輪的六個步驟與判定規則（全過 done／不過 rework round+1／第 3 輪 blocked）。
  - `tasks/T-0002-srs.md`（全檔）—— 確認 `status: review`、`reviewer: plan-sa`、`round: 1`；取得 5 條 acceptance 與「驗收方式」。
  - `worklog/handoff/20260919-0529-T0002-r1-plan-ba.md`（A、B 段全部）—— 取得 6 項假設與決策、4 組驗證指令、11 項待裁決事項。
  - `docs/specs/01_需求規格書_SRS.md`（全檔，280 行）—— 逐條讀 13 則 US、62 條 AC、8 條 NFR、範圍外清單、外部介面表、名詞定義。
  - `docs/specs/01a_需求澄清紀錄.md`（全檔）—— Q-001~Q-011、A-001~A-006、N-001~N-005。
  - `docs/specs/traceability.md`（全檔）—— 主表 13 列、AC 對照表、NFR 追溯表、孤兒檢查。
  - `tasks/E-001-todo-app.md#範圍`、`#限制`、`#成功指標`、`#Leader 裁決紀錄` —— 比對 SRS 的 P0/P1 是否與 Epic 一致；取得 Q-001~Q-011 的裁決結果與 Q-008 選定 HTTP Basic Auth。
  - `docs/templates/worklog_handoff.md` —— 本檔格式。
- 上一輪交接摘要（若 round > 1）：無（round 1，本卡首次審核）。
- 本輪計畫（3–7 條）：
  1. 實際重跑 plan-ba 交接檔 B 段的 4 組驗證指令，貼真實輸出（不採信交接檔的「通過」二字）。
  2. 追加本角色專屬檢查：62 條 AC 是否全為 Given/When/Then 三段式、是否含不可測的模糊量詞、是否有多重期望值。
  3. 逐條判定 5 條 acceptance；對 plan-ba 的 6 項假設與決策逐條表態接受／不接受。
  4. 找 US 之間的矛盾（同一情境兩個 US 有不同預期）。
  5. 依 Leader 裁決結果（尤其 Q-008 Basic Auth）檢查是否與既有 AC 產生新衝突，衝突不退回 T-0002，改寫入下一步建議交 T-0003/T-0004。
  6. 判定並更新任務卡與審核紀錄表、commit。
- 預期產出：
  - 本審核交接檔
  - `tasks/T-0002-srs.md`（status、updated、審核紀錄表一列）

## B. 交接

- 完工時間／狀態：2026-09-19T05:44:12+08:00 / done（T-0002 審核通過）
- 完成事項：
  - 重跑 plan-ba 交接檔 B 段全部 4 組驗證指令，輸出與其所貼內容一致（見下方「驗證方式」）。
  - 追加 3 組本角色專屬檢查（AC 三段式覆蓋率、模糊量詞掃描、多重期望值掃描）。
  - 逐條判定 5 條 acceptance：**5 條全部通過**。
  - 對 plan-ba 的 6 項假設與決策逐條表態：**6 項全部接受**。
  - 執行 US 間矛盾檢查：**未發現實質矛盾**；找到 1 處措辭精確度不足（AC-005-1 未載明篩選前置條件），不構成退回，列入下一步建議。
  - 額外發現 1 項 Leader 裁決（Q-008 Basic Auth）與既有 AC-010-5／NFR-003／NFR-001 的交互衝突，屬 T-0004 設計範疇，已列入下一步建議與需裁決事項。
  - 判定 T-0002 `status: done`，`updated` 更新，審核紀錄表加一列。
- 產出物（路徑）：
  - `worklog/handoff/20260919-0541-T0002-r1-plan-sa.md`（本檔）
  - `tasks/T-0002-srs.md`（frontmatter `status: done`、`updated`；審核紀錄表 r1 一列）
- 未完成／已知問題：
  - `docs/specs/01a_需求澄清紀錄.md` 的 Q-001~Q-011 狀態欄仍為「待確認」，與 Epic 裁決紀錄（已全部裁決）不同步。依 Leader 指示此欄由 plan-ba 於 T-0005 統一更新，**本輪不因此退回**。
  - `docs/specs/traceability.md` 的 UC／SD 模組／API 端點／TC 四欄仍為 `—`，孤兒檢查三項本輪不成立。屬任務卡「初版」定義之正常狀態；UC 欄由本角色於 T-0003 立即回填。
- 我做的假設與決策（含理由）：
  - **判定「小瑕疵不退回」的界線**：我把「AC 可被寫成一個明確通過／失敗的測試案例」當作唯一退回判準。AC-009-3（刪除成功 200 **或** 204）與 AC-013-2（回應 404 **或** 403）雖有兩個可接受值，但測試案例可寫成「斷言回應碼屬於該集合」，仍可測，故**不退回**；收斂為單一值屬 API 設計決定，交 plan-sd。
  - **AC-005-1 與 AC-006-4 的關係判為「非矛盾」**：AC-005-1（切換為已完成後畫面以刪除線等方式呈現）未載明篩選前置條件，AC-006-4（在「未完成」篩選下切換完成即從清單移除）載明了。兩者的 Given 精確度不同而非期望值相反，且 Q-007 已裁決預設篩選為「全部」，AC-005-1 的隱含情境明確。判為措辭精確度問題，**不退回**，改由我在 T-0003 的 SA 以一條業務規則寫死「狀態變更後一律重新套用目前篩選」，並建議 T-0005 補強 AC-005-1 措辭。
  - **不因 Leader 裁決 Q-008 帶來的新衝突退回 T-0002**：衝突是裁決之後才產生的（SRS 撰寫時 Q-008 尚未裁決），不是 plan-ba 的交付缺陷；且解法落在部署設計層（plan-sd），SRS 正文不需改。
  - **NFR-003 的 7 天監測期我不判為不可測**：它有明確數值與量測方式，符合 acceptance 第 3 條「量化」的要求。7 天是否與 Gate 2 時程衝突屬排程問題，已寫入需裁決事項，不影響本卡判定。
- 驗證方式（接手者如何確認我做的是對的：指令 + 我實際得到的輸出）：

  **驗證組 1／4 —— US 與 AC 計數（重跑 plan-ba 指令）**
  ```bash
  cd /c/Users/excal/Projects/Claude/soft-dev-team-260919
  grep -n "^### US-" docs/specs/01_需求規格書_SRS.md
  grep -c "^### US-" docs/specs/01_需求規格書_SRS.md
  grep -o "AC-0[0-9][0-9]-[0-9]" docs/specs/01_需求規格書_SRS.md | sort -u | cut -d- -f2 | uniq -c
  grep -o "AC-0[0-9][0-9]-[0-9]" docs/specs/01_需求規格書_SRS.md | sort -u | wc -l
  ```
  ```text
  === 1. US 標題與優先級 ===
  79:### US-001 新增待辦（P0）
  90:### US-002 檢視待辦清單（P0）
  100:### US-003 編輯待辦（P0）
  111:### US-004 刪除待辦（P0）
  121:### US-005 切換完成／未完成（P0）
  131:### US-006 依狀態篩選（P0）
  143:### US-007 顯示建立時間（P0）
  153:### US-008 單頁前端操作（P0）
  164:### US-009 REST API（P0）
  176:### US-010 staging 部署（P0）
  187:### US-011 使用者註冊（P1）
  198:### US-012 使用者登入與登出（P1）
  209:### US-013 使用者資料隔離（P1）
  === US 總數 ===
  13
  === 2. 每個 US 的 AC 條數 ===
        5 001
        4 002
        5 003
        4 004
        4 005
        6 006
        4 007
        5 008
        6 009
        5 010
        5 011
        5 012
        4 013
  === AC 總數 ===
  62
  ```
  判讀：13 個 US-ID 唯一且連號、標題括號皆帶 P0/P1、每個 US 最少 4 條 AC、無 0 條者。與 plan-ba 所貼輸出**完全一致**。

  **驗證組 2／4 —— NFR 與範圍外（重跑 plan-ba 指令）**
  ```bash
  grep -o "^| NFR-00[0-9] | [^|]* |" docs/specs/01_需求規格書_SRS.md
  grep -c "^| NFR-00" docs/specs/01_需求規格書_SRS.md
  grep -n "範圍外" docs/specs/01_需求規格書_SRS.md | head -5
  ```
  ```text
  === 3. NFR 條目 ===
  | NFR-001 | 效能 |
  | NFR-002 | 安全 |
  | NFR-003 | 可用性 |
  | NFR-004 | 相容性 |
  | NFR-005 | 可維運性 |
  | NFR-006 | 資料持久性 |
  | NFR-007 | 容量 |
  | NFR-008 | 可安裝性 |
  NFR 總數:
  8
  === 4. 範圍外章節 ===
  56:### 2.2 範圍外（明確列出，避免下游猜測）
  65:- 以下為上列五項的延伸，一併列為範圍外，避免下游推論：
  98:- 備註／限制：本輪不分頁、不搜尋（範圍外）；清單筆數上限見 NFR-007。
  119:- 備註／限制：刪除為不可復原（無回收桶，範圍外）；硬刪除或軟刪除的實作語意見 Q-003。
  196:- 備註／限制：Email 驗證、密碼重設、第三方登入為範圍外。
  ```
  判讀：8 條 NFR，acceptance 要求的四類（效能 NFR-001／安全 NFR-002／可用性 NFR-003／相容性 NFR-004）齊備且各有數值門檻與量測方式；範圍外章節存在（§2.2）。與 plan-ba 所貼輸出**完全一致**。

  **驗證組 3／4 —— 澄清紀錄狀態欄與追溯矩陣一致性（重跑 plan-ba 指令）**
  ```bash
  grep -c "^| Q-0" docs/specs/01a_需求澄清紀錄.md
  grep -c "^| A-00" docs/specs/01a_需求澄清紀錄.md
  grep -E "^\| (Q|A)-0" docs/specs/01a_需求澄清紀錄.md | grep -cE "待確認|已確認"
  grep -c "^| US-0" docs/specs/traceability.md
  diff <(grep -o "US-0[0-9][0-9]" docs/specs/01_需求規格書_SRS.md | sort -u) <(grep -o "US-0[0-9][0-9]" docs/specs/traceability.md | sort -u) && echo "DIFF-EMPTY-OK"
  ```
  ```text
  === 待確認 Q 項數 ===
  11
  === 已確認 A 項數 ===
  6
  === 每列都有狀態欄的 Q+A 列數 ===
  17
  === trace US-0xx 起首列數 ===
  26
  === SRS 與 trace 的 US-ID 集合比對 ===
  DIFF-EMPTY-OK
  ```
  判讀：11 + 6 = 17 列，17 列全部帶狀態字樣，無缺狀態者；trace 26 列＝主表 13 + AC 對照表 13；SRS 與 trace 的 US-ID 集合 diff 為空，無多無缺。與 plan-ba 所貼輸出**完全一致**。

  **驗證組 4／4 —— 本角色追加檢查（AC 可測性）**
  ```bash
  # A. AC 總列數；B. 具備 Given/When/Then 三段的列數；C. 缺三段式者
  grep -cE "^  - AC-0[0-9][0-9]-[0-9]" docs/specs/01_需求規格書_SRS.md
  grep -cE "^  - AC-0[0-9][0-9]-[0-9].*Given .*When .*Then " docs/specs/01_需求規格書_SRS.md
  grep -nE "^  - AC-" docs/specs/01_需求規格書_SRS.md | grep -vE "Given .*When .*Then "
  # H. 模糊量詞掃描
  grep -nE "^  - AC-" docs/specs/01_需求規格書_SRS.md | grep -E "友善|美觀|快速|良好|適當|盡量|合理|簡單|等等"
  # I. 多重期望值掃描
  grep -nE "^  - AC-" docs/specs/01_需求規格書_SRS.md | grep -E "或 [0-9]"
  ```
  ```text
  === A. AC 總列數 ===
  62
  === B. 具備 Given/When/Then 三段的 AC 列數 ===
  62
  === C. 缺三段式的 AC 列（無輸出=全合格）===
  === D. 檢查完畢 ===

  === H. 模糊量詞（無輸出=無模糊量詞）===
  --- 無輸出 ---

  === I. 多重期望值 ===
  170:  - AC-009-3：Given 任一 API 回應，When 我檢視回應，Then 其 Content-Type 為 `application/json`，且 HTTP 狀態碼語意正確（建立成功 201、查詢成功 200、刪除成功 200 或 204、輸入驗證失敗 400、資源不存在 404、伺服器錯誤 5xx）。
  214:  - AC-013-2：Given A 已登入且知道 B 的某筆待辦識別碼，When A 以自己的憑證讀取、更新、切換或刪除該筆，Then 系統回應 404 或 403 且該筆資料未被改動。
  ```
  判讀：62 / 62 條 AC 皆為完整 Given/When/Then 三段式，缺項為 0；無任何「友善／美觀／快速／良好／適當」等不可測形容詞。2 條含兩個可接受回應碼，仍可寫成「斷言值屬於集合」的測試案例，判為可測（詳見上方「假設與決策」）。

  **驗證組 5／4（額外）—— Q-008 裁決與既有 AC 的交互**
  ```bash
  grep -n "健康檢查\|Basic\|存取保護" docs/specs/01_需求規格書_SRS.md tasks/E-001-todo-app.md
  ```
  ```text
  docs/specs/01_需求規格書_SRS.md:184:  - AC-010-5：Given staging 服務，When 我呼叫健康檢查端點，Then 回應 200 並可據以判斷服務存活（見 NFR-003）。
  docs/specs/01_需求規格書_SRS.md:185:- 備註／限制：…staging 是否需存取保護見 Q-008。
  docs/specs/01_需求規格書_SRS.md:225:| NFR-003 | 可用性 | staging 服務在連續 7 天監測期間，健康檢查端點成功率 ≥ 99%… |
  docs/specs/01_需求規格書_SRS.md:256:| Q-008 | P0 階段 staging 無登入機制，需要一層簡易存取保護… | 待確認 | 待 Leader 裁決 |
  tasks/E-001-todo-app.md:64:| Q-008 | **需要保護，選 HTTP Basic Auth** | …在應用層或反向代理層擋整站（頁面 + API）… |
  ```
  判讀：裁決要求 Basic Auth **擋整站（頁面 + API）**，而 AC-010-5 要求健康檢查端點「回應 200」。若健康檢查端點不豁免，未帶憑證的監測器會取得 401，AC-010-5 與 NFR-003 將永遠不通過。此為裁決後才浮現的設計約束，交 plan-sd 於 T-0004 處理（詳見下一步建議）。

  **逐條核對任務卡 acceptance 的判定**

  | # | acceptance | 判定 | 證據（我自己重跑的輸出） |
  |---|---|---|---|
  | 1 | 每個 US 有唯一 ID、優先級、至少一條 Given/When/Then 驗收條件 | **通過** | 驗證組 1：13 個唯一 US-ID、全帶 P0/P1、最少 4 條 AC；驗證組 4：62/62 條 AC 為完整三段式 |
  | 2 | P0 範圍完整對應 Epic 的 P0 清單，P1 對應 P1，範圍外清單存在 | **通過** | SRS §2.1 的 P0 七列逐項對回 Epic「範圍」的七項（CRUD／完成切換／狀態篩選／建立時間／單頁前端／REST API／staging），無多無缺；P1 三列對回 Epic 的註冊／登入 JWT／資料隔離；驗證組 2 第 4 段顯示 §2.2 範圍外章節存在且含 Epic 五項 + 衍生六類 |
  | 3 | 非功能需求至少 4 條且全部量化（效能、安全、可用性、相容性） | **通過** | 驗證組 2：8 條 ≥ 4 條；四個必要類別齊備；逐條讀過量測方式欄，NFR-001 有 P95/併發/秒數、NFR-003 有 99%/2016 次/60 秒、NFR-004 有 3 瀏覽器×2 尺寸、NFR-007 有 500 筆/2 秒/3 秒、NFR-008 有 15 分鐘，皆為數值判準 |
  | 4 | 澄清紀錄中每個假設都有狀態；待確認者列入交接檔「需要 Leader 裁決的事」 | **通過** | 驗證組 3：17 列全部有狀態字樣；plan-ba 交接檔「需要 Leader 裁決的事」列出 Q-001~Q-011 共 11 項並依急迫性分三批 |
  | 5 | traceability.md 已列出所有 US-ID | **通過** | 驗證組 3：`DIFF-EMPTY-OK`，SRS 與 trace 的 US-ID 集合完全相同 |

  **本角色專屬判準（角色定義「審核他人時」）**

  | 判準 | 結果 | 說明 |
  |---|---|---|
  | 每個 AC 能否寫成一個測試案例 | **通過** | 62 條逐條檢視：皆有明確前置條件、明確觸發動作、明確可觀察結果。AC-009-3／AC-013-2 有兩個可接受回應碼，仍可斷言集合成員，判為可測 |
  | US 之間是否有矛盾（同一情境不同預期） | **通過（無實質矛盾）** | 逐對檢查 US-001~US-013：US-002 全部列出 vs US-006 篩選（預設「全部」，Q-007 已裁決，無衝突）；US-005 切換 vs US-006 篩選（AC-005-1 未載明篩選前置條件，判為措辭精確度不足而非期望值相反）；US-007 建立時間不變 vs US-003 編輯／US-005 切換（AC-003-4、AC-005-4、AC-007-3 三處一致）；US-013 隔離 vs US-002/US-006（AC-013-4 明定先過濾身分再套篩選，無衝突） |
  | 範圍外是否明確 | **通過** | §2.2 明列 Epic 五項 + 六類衍生項，且各 US 備註欄重述邊界 |
  | NFR 是否量化 | **通過** | 8 條全部有數值門檻與量測方式，無「盡可能」式表述 |

  **對 plan-ba「我做的假設與決策」逐條表態**

  | # | plan-ba 的假設／決策 | 我的表態 | 理由 |
  |---|---|---|---|
  | 1 | 把 Epic 的 P0 七項拆成十則 US（CRUD 拆四則） | **接受** | 合併 CRUD 後無法對單一操作寫出可測 AC，也無法在追溯矩陣定位缺漏。拆分後每則仍在 Epic 範圍內，未新增能力，不構成擴大範圍。T-0003 的 UC 亦以此粒度建立 |
  | 2 | 不另立「登出」US，併入 US-012 的 AC-012-5 | **接受** | 登出是登入憑證生命週期的必要對偶，無登出等於憑證無法主動失效。且 Leader 已於 Epic 裁決紀錄 N-001 明文「接受，不視為擴大範圍」 |
  | 3 | SRS 全篇不指定技術棧，連資料庫類型都不寫，只要求持久化（NFR-006） | **接受** | 與 Epic「限制」明文一致（技術棧由 plan-sd 決定並寫 ADR）。NFR-006 以「重啟後 10 筆全數可查」表述，是行為而非技術，可測且不越權 |
  | 4 | NFR-001 效能門檻下修為 P95 < 500ms／10 併發 | **接受** | Leader 已於 Epic 裁決紀錄明文「接受」。從審核角度另補一點：免費額度雲端的冷啟動會使 300ms 門檻在 Gate 2 製造假性失敗，下修後仍為明確數值，可測性不受影響 |
  | 5 | 把 XSS 逸出、健康檢查端點、錯誤與載入狀態 UI 寫進需求（N-002~N-004） | **接受** | 三者皆為「既有需求可被驗收」的必要條件而非新功能：不逸出即 NFR-002 無法驗收；無健康檢查端點即 NFR-003 的成功率無從量測；請求失敗時畫面凍結即違反 Epic「要能用」。**但**健康檢查端點與 Leader 裁決的 Basic Auth 擋整站有交互衝突，須由 plan-sd 處理（見下一步建議） |
  | 6 | 明確把分頁與搜尋列入範圍外，改以 NFR-007（500 筆上限）處理規模 | **接受** | 這正是下游最容易自行推論而擴大範圍之處，先擋住是對的。NFR-007 給了 500 筆／2 秒／3 秒的明確替代判準，不會讓規模問題無解 |

  **判定結論：5 條 acceptance 全數通過，6 項假設全數接受，無實質矛盾與不可測 AC → T-0002 `status: done`。**

- 下一步建議：
  - **給 plan-ba（T-0005）** —— 以下三點為小瑕疵，不構成本輪退回，請於追溯矩陣定稿時一併處理：
    1. `01a_需求澄清紀錄.md` 的 Q-001~Q-011 狀態欄由「待確認」改為「已裁決（Leader，見 Epic 裁決紀錄 2026-09-19T05:38:38+08:00）」，並同步 SRS §6 表格的狀態欄；Q-008 的裁決值與假設不同（選定 HTTP Basic Auth 擋整站），須把裁決內容而非原假設寫入。
    2. AC-005-1 的 Given 建議補上篩選前置條件，例如「Given 我在『全部』篩選下且一筆待辦目前為未完成」，以與 AC-006-4 的情境明確區隔，讓測試團隊不需自行推論。
    3. SRS §3 各 US 備註中「見 Q-00x 裁決值，暫定…」的措辭，於 T-0005 可改為裁決後的定值敘述（例如 AC-001-3 的 200 字元已成定案），減少下游來回查表。
  - **給 plan-sd（T-0004）** —— 以下為 Leader 裁決 Q-008 後才浮現的設計約束，須在 SD 第 6 章與部署設計中明確回答，否則 P0 無法驗收：
    1. **健康檢查端點必須豁免 Basic Auth**，否則 AC-010-5（回應 200）與 NFR-003（成功率 ≥ 99%）永遠不通過。豁免方式（路徑白名單或平台內建探針）請寫進 ADR。
    2. **NFR-001 的負載測試與 NFR-003 的定時監測都需帶 Basic Auth 憑證**，請在部署文件中說明測試端如何取得與帶入，且憑證不得寫進倉庫（Epic 限制：憑證由使用者提供）。
    3. **AC-009-5（CORS 不用萬用字元）與 Basic Auth 併用**時，跨來源請求需以帶憑證模式發送，`Access-Control-Allow-Credentials` 與具名來源必須一併設定；請確認前後端同源或明確列出來源。
    4. **AC-010-4（未參與者依文件重現部署）**須把 Basic Auth 帳密的設定步驟寫入 README，但只寫「從平台 secrets 讀取」的流程，不得寫入實際值。
    5. AC-009-3 的「刪除成功 200 或 204」與 AC-013-2 的「404 或 403」請在 API 設計中各收斂為單一值並記錄理由，讓測試團隊寫死期望值。
  - **給本角色（T-0003，緊接著執行）**：以 US-001~US-013 為單位建 UC，UC 回填 `traceability.md` UC 欄；US-008（單頁前端）與 US-010（staging 部署）不是傳統資料流用例，需以操作情境描述，不得略過；以一條業務規則寫死「待辦狀態變更後一律以目前生效的篩選重新計算顯示清單」，一次解掉 AC-005-1／AC-006-4 的情境重疊。
  - **給測試團隊**：`traceability.md` 的「驗收條件對照」表已列出 62 條 AC 的編號範圍，可直接一 AC 一 TC 展開；注意 P0 的 UAT 需事先取得 staging 的 Basic Auth 憑證。
- 需要 Leader 裁決的事：
  1. **NFR-003 的「連續 7 天監測期」與 Gate 2 時程可能衝突**。本卡不因此退回（該 NFR 已量化且可測），但 Gate 2 若在 7 天內召開，此條將無法取得完整證據。建議 Leader 明確二選一：①Gate 2 接受較短採樣期（例如連續 24 小時、成功率 ≥ 99%）作為替代證據；②把 NFR-003 的完整驗證移到 Gate 2 之後的觀察期，不列為 Gate 2 阻擋項。
  2. **Q-008 裁決（Basic Auth 擋整站）對 AC-010-5／NFR-003 的影響已由我列為 plan-sd 的設計約束**。若 Leader 認為健康檢查端點不應豁免保護（例如不希望暴露任何未保護路徑），請改為裁決「NFR-003 的監測由帶憑證的排程器執行」，我會在 T-0003 的 SA 未決事項同步調整。此項在未收到不同指示前，我以「健康檢查端點豁免」為保守假設繼續推進。
