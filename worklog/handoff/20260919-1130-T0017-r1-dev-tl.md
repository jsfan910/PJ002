# T-0017 r1 — dev-tl — 工作紀錄與交接

<!-- 檔名：worklog/handoff/20260919-1130-T0017-r1-dev-tl.md（時間 = 開工時間，取實查系統時間） -->
<!-- 規則：開工立即寫 A 段；完工或中止時寫 B 段。同一輪同一角色一個檔。 -->

## A. 開工紀錄

- 開工時間：2026-09-19T11:30:05+08:00
- 角色／模型：dev-tl / Claude Opus 5（1M context）
- 任務卡：tasks/T-0017-todo-store-view.md（round 1）／**審核 T-0017**
- 讀取的輸入（實際讀過的檔案與段落）：
  - `.claude/agents/dev-tl.md` 全文 — 本角色職責、「審核他人時」規約（實際 checkout、實際跑、貼輸出）
  - `.claude/skills/review-round/SKILL.md` 全文 — 一輪審核的七個步驟、判定分支與審核紀錄表格式
  - `tasks/T-0017-todo-store-view.md` 全文 — outputs 四項、acceptance 五條、驗收方式
  - `docs/specs/10_開發計畫_WBS.md` §0（硬前提 1~8）、§1.7 WI-07 全文（單向資料流、BR-011/BR-010/BR-013/BR-004/BR-028/BR-030/BR-008/NFR-007、驗收方式草案六條 grep ＋瀏覽器實測五項）、§2 批次 5、§5 共用檔案規約
  - `docs/specs/01_需求規格書_SRS.md` US-001～US-008 的每條 AC
  - `tasks/E-001-todo-app.md` Leader 裁決紀錄（Q-001 200 字元、Q-003 硬刪除＋UI 二次確認、Q-004 本地時區 `yyyy-mm-dd HH:mm`、Q-005 新到舊、Q-007 篩選不保存預設全部、T-0014-① `index.html` 限一行進入點、T-0016-① UUID 格式錯誤 400、dev-tl-① README `.env` 載入、T-0018 兩項）
  - `worklog/handoff/20260919-0948-T0017-r1-dev-fe.md` B 段全文 — 完成事項、7 條假設與決策、驗證輸出、下一步建議
  - 受審程式碼全文：`public/assets/todo-store.js`、`public/assets/todo-view.js`、`public/index.html`
- 上一輪交接摘要（若 round > 1）：不適用（round 1）
- 本輪計畫（3–7 條）：
  1. 在 T-0017 worktree 實跑 lint、`node --test tests/unit/todo-store.test.mjs`、`npm run test:unit`
  2. 重跑 WBS §1.7 六條 grep 驗收指令；核對 `index.html` diff 只有一行、11 個 `data-testid` 未變、`api-client.js`／`styles.css` 未改
  3. `docker compose up -d --build` ＋ migration，實際用瀏覽器逐 US-001～US-008 每條 AC 操作
  4. 對交接檔 7 條「假設與決策」逐條表態
  5. 判定、合併、更新任務卡與審核紀錄表
- 預期產出：本審核交接檔、任務卡 `status: done` ＋ 審核紀錄一列、main 上的合併 commit

## B. 交接

- 完工時間／狀態：2026-09-19T11:32:00+08:00 / **done（初審通過，已合併 main）**

### 完成事項

- 逐條核對 acceptance 五條，全部通過（證據見下「驗證方式」§1～§8）。
- 逐條核對 US-001～US-008 的 AC（共 34 條），全部在**真實瀏覽器**（Chrome，`docker compose` 啟動的服務，Basic Auth `dev:dev`）操作到，無不通過項。
- 對交接檔 7 條「假設與決策」逐條表態，全部接受。
- 判定 **done**，以 `git merge --no-ff` 合併 `task/T-0017-todo-store-view` 至 main（commit `1ad255d`）。

### 產出物（路徑）

- 本審核交接檔 `worklog/handoff/20260919-1130-T0017-r1-dev-tl.md`
- `tasks/T-0017-todo-store-view.md`（`status: done`、`updated`、審核紀錄表一列）
- main 合併 commit `1ad255d T-0017: 合併前端狀態與呈現（單向資料流）（dev-tl 初審通過）`

### 未完成／已知問題

- 無阻擋級問題。四則小瑕疵（建議級）已寫入「下一步建議」，不阻擋合併。
- **審核環境的工具限制（非本卡缺陷，已實證）**：瀏覽器自動化工具送出的 Enter 鍵不會觸發 HTML form 的 implicit submission。我在同一頁面動態插入一個**與本卡程式碼無關的原生 `<form>` ＋ `<input>` ＋ `<button type=submit>`**，同樣按 Enter 也沒有觸發 submit（`implicitSubmitWorks:false`），證明是工具的合成鍵事件不走 default action，不是 `todo-view.js` 的問題。本卡的送出路徑改以滑鼠點擊「新增」按鈕驗證（AC-008-2 本就寫明「僅滑鼠點擊與文字輸入」）。**鍵盤 Enter 送出請 qa-at 以真人手動操作補驗一次。**

### 我做的假設與決策（含理由）

1. **對交接檔「假設與決策」①（未改 `styles.css`，改用行內 `style`）：接受。** 任務卡 `outputs` 明文只有四項且 acceptance 2 明文「不改本卡 outputs 以外的檔案」；角色協定亦禁止。dev-fe 以行內 `style`（引用既有 `:root` 變數）達成 44×44 點擊區與不橫向捲動，實測兩種尺寸都過（見 §8-3）。**技術負責人裁示：本卡不改 `styles.css` 正確，沒有範圍衝突。** 行內樣式的維護性問題列為建議級（見「下一步建議」4），不退回。
2. **②（`todo-view.js` 兼任 controller）：接受。** SD §3.2 的資料流圖未另立 controller 模組，任務卡 outputs 也沒有第三個檔案。檔內以註解把「事件綁定區塊」與「render 函式群」分界，單向資料流未被破壞（實測 `grep -n "fetch(" public/assets/todo-view.js` 無輸出，view 不呼叫 API）。
3. **③（`addInput.required = false`）：接受。** 只在執行期關掉原生檢查，`index.html` 原始檔未動（diff 只有一行 script，見 §5）。實測「完全空字串」與「三個空白字元」兩種情境都統一走 `error-message` 區顯示「標題不可為空白」（§8 AC-001-2），符合 AC-001-2「畫面上顯示可讀的錯誤訊息」的意圖。
4. **④（異動失敗時不自動重載清單）：接受。** 理由成立且可實證：`load()` 開頭就 `setState({error:null})`，若失敗後立刻自動重載，錯誤訊息會在同一 tick 被清掉，使用者看不到。實測 404 情境（§8 AC-004-4、AC-003-5）錯誤訊息確實留在畫面上且附「重試」按鈕，畫面不凍結。
5. **⑤（前端先 trim 後驗證 1~200 才送 API）：接受。** 與 T-0016 交接檔建議一致；實測 201 字元不產生任何 `POST`（§8 AC-001-3），避免了「原始字串 >200、trim 後 ≤200」的邊界。與後端驗證是兩層獨立防線，不是取代。
6. **⑥（`renderError` 每次重建重試按鈕）：接受。** `errorMessageEl.textContent = ...` 本就會清空子節點，重建是必要的；效能影響可忽略（清單才是熱點，已用 `DocumentFragment`）。
7. **⑦（本機驗證環境與收尾清理）：接受。** 我重跑時 `.env` 由 `.env.example` 複製、已在 `.gitignore`、未進版控（`git status --short` 全空，見 §5）；我自己的驗證也以 `docker compose down -v` 收尾並確認 8080 埠釋放（見 §9）。

### 驗證方式（指令 + 實際輸出）

全部在 worktree `C:/Users/excal/Projects/Claude/soft-dev-team-260919-wt/T-0017`（分支 `task/T-0017-todo-store-view`，commit `bae58e5`）執行。

**§1. lint**

```bash
npm run lint; echo "LINT_EXIT=$?"
```
```text
> todo-app@0.1.0 lint
> eslint . && tsc --noEmit -p tsconfig.json

LINT_EXIT=0
```

**§2. 本卡單元測試**

```bash
node --test tests/unit/todo-store.test.mjs
```
```text
（24 條全綠，節錄尾段）
✔ remove()：成功時呼叫 deleteTodo 並重新取清單 (0.1051ms)
✔ remove()：刪除不存在的資料（404）時設定 error，不得丟出未捕捉例外 (0.0636ms)
✔ retry()：以目前 filter 重新載入（供錯誤畫面的重試使用） (0.06ms)
✔ subscribe()：回傳的 unsubscribe 呼叫後不再收到通知 (0.0575ms)
ℹ tests 24
ℹ pass 24
ℹ fail 0
ℹ duration_ms 80.0111
```

**§3. 全專案單元測試**

```bash
npm run test:unit
```
```text
ℹ tests 75
ℹ pass 75
ℹ fail 0
ℹ duration_ms 164.8459
```

**§4. WBS §1.7 驗收方式草案六條 grep（逐字重跑）**

```bash
grep -rn "innerHTML" public/ ; echo "exit=$?"
grep -n "fetch(" public/assets/todo-view.js ; echo "exit=$?"
grep -nE "document\.|window\." public/assets/api-client.js ; echo "exit=$?"
grep -n "DocumentFragment" public/assets/todo-view.js
grep -n "Intl.DateTimeFormat" public/assets/todo-view.js
```
```text
=== innerHTML in public/ ===
exit=1
=== fetch( in todo-view.js ===
exit=1
=== document./window. in api-client.js ===
exit=1
=== DocumentFragment ===
13: *   - 清單以 `DocumentFragment` 組完整內容後單次插入（NFR-007），不做局部 DOM 補丁。
209:/** BR-028／NFR-007：以 DocumentFragment 組完整清單後單次插入，整段替換（BR-011）。 */
211:  const fragment = document.createDocumentFragment();
=== Intl.DateTimeFormat ===
12: *   - `createdAt` 以 `Intl.DateTimeFormat` 轉換為瀏覽器本地時區的 `yyyy-mm-dd HH:mm`（BR-004）。
52:const createdAtFormatter = new Intl.DateTimeFormat(undefined, {
```

另補一條「不得以 innerHTML 拼接使用者字串」的加強確認：`public/` 全目錄零命中（上面第一條），且瀏覽器端實測 `todo-list` 的 `innerHTML` 不含字面 `<script>` 標籤（見 §8 XSS 一列）。

**§5. 只動 outputs：`index.html` 一行、11 個 data-testid 未變、`api-client.js`／`styles.css` 未改**

```bash
git diff main...HEAD --stat
git diff main...HEAD -- public/index.html
git diff main...HEAD --name-only | grep -E "api-client|styles.css" ; echo "exit=$?"
git show main:public/index.html | grep -o "data-testid=\"[^\"]*\"" | sort
grep -o "data-testid=\"[^\"]*\"" public/index.html | sort
git status --short
```
```text
 public/assets/todo-store.js                      | 235 +++++++++++++++
 public/assets/todo-view.js                       | 288 ++++++++++++++++++
 public/index.html                                |   1 +
 tasks/T-0017-todo-store-view.md                  |   4 +-
 tests/unit/todo-store.test.mjs                   | 361 +++++++++++++++++++++++
 worklog/handoff/20260919-0948-T0017-r1-dev-fe.md | 282 ++++++++++++++++++
 6 files changed, 1169 insertions(+), 2 deletions(-)

diff --git a/public/index.html b/public/index.html
@@ -83,5 +83,6 @@
         <ul class="todo-list" data-testid="todo-list" aria-live="polite"></ul>
       </main>
     </div>
+    <script type="module" src="/assets/todo-view.js"></script>
   </body>
 </html>

=== api-client.js / styles.css changed? ===
exit=1                      ← 兩檔皆未出現在變更清單

=== data-testid main ===    === data-testid HEAD ===
add-todo-form               add-todo-form
add-todo-input              add-todo-input
add-todo-submit             add-todo-submit
app-shell                   app-shell
error-message               error-message
filter-controls             filter-controls
filter-option-active        filter-option-active
filter-option-all           filter-option-all
filter-option-completed     filter-option-completed
loading-indicator           loading-indicator
todo-list                   todo-list
（11 個，兩側逐字相同）

git status --short → （無輸出，工作區乾淨、.env 未進版控）
```

**§6. 憑證掃描（dev-tl 補強樣式，本卡新增檔案）**

```bash
grep -rnEi "(password|secret|database_url|api[_-]?key|token)[[:space:]]*[:=][[:space:]]*[\"]?[A-Za-z0-9/+_.-]{2,}" public/assets/todo-store.js public/assets/todo-view.js tests/unit/todo-store.test.mjs; echo "scan_exit=$?"
```
```text
scan_exit=1        ← 零命中
```

**§7. 起服務（`docker compose up -d --build`）＋ migration ＋ 基本煙霧測試**

```bash
docker compose up -d --build
set -a; . ./.env; set +a
DATABASE_URL="postgres://dev:dev@localhost:5432/todo" node dist/db/migrate.js
curl -sS -o /dev/null -w "health=%{http_code}\n" http://localhost:8080/health
curl -sS http://localhost:8080/health
curl -sS -o /dev/null -w "root-no-cred=%{http_code}\n" http://localhost:8080/
curl -sS -o /dev/null -w "root-with-cred=%{http_code}\n" -u dev:dev http://localhost:8080/
curl -sS -u dev:dev http://localhost:8080/api/v1/todos
```
```text
 Image t-0017-app Built
 Container t-0017-db-1 Started / Healthy
 Container t-0017-app-1 Started
已套用 migration：001
health=200
{"status":"ok"}
root-no-cred=401
root-with-cred=200
[]
```

**§8. 瀏覽器實測（Chrome，`http://localhost:8080`，Basic Auth `dev:dev`）—— US-001～US-008 逐 AC，審核者自己操作**

| AC | 操作 | 觀察到的結果 | 判定 |
|---|---|---|---|
| AC-001-1 | 輸入「␣␣買牛奶␣␣」，點「新增」 | `POST /api/v1/todos → 201` 後接 `GET ?status=all → 200`；清單出現「買牛奶」（前後空白已 trim），無整頁跳轉 | 通過 |
| AC-001-2 | 輸入三個空白字元，點「新增」 | 網路面板最後一筆請求不變（**無新 POST**）；`error-message` 顯示「標題不可為空白」 | 通過 |
| AC-001-3 | JS 移除 `maxlength` 後填 201 字元，點「新增」 | **無新 POST**；顯示「標題長度不可超過 200 字元」；清單筆數維持 2 | 通過 |
| AC-001-4 | 檢查新增後的項目 | `data-todo-id` 為 UUID（`a7cf6eab-0015-4037-981e-e9776be99157`、`c345899b-...`），`createdAt` 由伺服器產生 | 通過 |
| AC-001-5 | 新增成功後看輸入框 | `input.value === ""`，焦點回到輸入框 | 通過 |
| AC-002-1 | 清單有 2 筆時檢視 | 每筆有標題、完成核取方塊、`2026-09-19 11:23` 形式的建立時間 | 通過 |
| AC-002-2 | 清空所有待辦後檢視（首次開站即為空） | 顯示「目前沒有任何待辦事項，新增一筆開始吧。」，非錯誤、非空白 | 通過 |
| AC-002-3 | 新增 2 筆後重新載入頁面 | `GET ?status=all → 200`，2 筆仍在 | 通過 |
| AC-002-4 | 清單多筆時看順序 | `["<script>alert(1)</script>"(11:24), "買牛奶"(11:23)]` — **新到舊**（Q-005） | 通過 |
| AC-003-1 | 點「編輯」→ 改「買牛奶和麵包」→「儲存」 | `PATCH /api/v1/todos/c345899b... → 200` 後接 `GET → 200`；清單顯示新標題，無整頁重整 | 通過 |
| AC-003-2 | 編輯狀態改為三個空白字元後儲存 | 無 PATCH；`error-message` 顯示「標題不可為空白」；原標題「買牛奶」不變 | 通過 |
| AC-003-3 | 編輯狀態下改文字後點「取消」 | 無任何新請求；標題維持「買牛奶和麵包」；離開編輯狀態（`todo-item-edit-form` 消失） | 通過 |
| AC-003-4 | 編輯成功後檢視 | `createdAt` 仍為 `2026-09-19 11:23`、`isCompleted` 仍為 `false` | 通過 |
| AC-003-5 | 對已被他處刪除的項目按「編輯 → 儲存」 | 顯示「找不到這筆待辦，可能已被刪除。」＋「重試」；未新增資料、未當機 | 通過 |
| AC-004-1 | 點「刪除」，`confirm` 回 `true` | `DELETE → 204` 後接 `GET → 200`；該筆消失，筆數 2→1，無整頁重整 | 通過 |
| AC-004-2 | 點「刪除」，`confirm` 回 `false` | 攔截到的對話框文字＝「確定要刪除「買牛奶和麵包」嗎？此操作無法復原。」；**筆數維持 2**，未送出 DELETE | 通過（Q-003 二次確認存在且可取消） |
| AC-004-3 | 刪除後重新載入頁面 | 該筆不再出現（筆數 1） | 通過 |
| AC-004-4 | 對已被他處刪除的項目按「刪除 → 確認」 | 顯示「找不到這筆待辦，可能已被刪除。」；畫面未凍結、未拋未捕捉例外 | 通過 |
| AC-005-1 | 點未完成項目的核取方塊 | `PATCH → 200`；核取方塊打勾，標題 `text-decoration-line: line-through` | 通過 |
| AC-005-2 | 再點同一核取方塊 | **攔截到的請求本文為 `{"isCompleted":false}`** — 送的是使用者勾選後的**目標值**，不是取反運算（O-002）；刪除線消失 | 通過 |
| AC-005-3 | 切換完成後重新載入頁面 | `checked:[false,true]`、`lineThrough:["none","line-through"]` — 狀態保持 | 通過 |
| AC-005-4 | 切換完成後檢視 | 標題與 `createdAt`（`2026-09-19 11:23`）皆未變 | 通過 |
| AC-006-1 | 點「未完成」 | `GET ?status=active`；只剩未完成那筆 | 通過 |
| AC-006-2 | 點「已完成」 | `GET ?status=completed`；只剩「買牛奶和麵包」 | 通過 |
| AC-006-3 | 點「全部」／重新載入 | `GET ?status=all`；列出全部 | 通過 |
| AC-006-4 | 在「已完成」篩選下把唯一一筆改為未完成 | 該筆立即從目前清單移除（筆數 1→0），不需重新整理 | 通過 |
| AC-006-5 | 篩選結果為零筆 | 顯示「此篩選下沒有待辦事項。」（與「全部」的空狀態文案不同），非空白、非錯誤 | 通過 |
| AC-006-6 | 檢視篩選按鈕狀態 | `["all:false","active:true","completed:false"]` — 只有生效者 `aria-pressed="true"` | 通過 |
| AC-007-1～3 | 檢視建立時間；編輯／切換完成後再看 | `2026-09-19 11:23`、`2026-09-19 11:24`（本地時區 `yyyy-mm-dd HH:mm`，非相對時間）；編輯與切換後數值不變 | 通過 |
| AC-007-4 | （API 契約，非本卡範圍） | 由 T-0016 驗；實測回應 `createdAt:"2026-09-19T03:28:59.883Z"` 為含時區 ISO 8601 | 不適用／已由他卡涵蓋 |
| AC-008-1 | 開啟首頁 | 新增表單、清單、篩選控制在同一頁 | 通過 |
| AC-008-2 | 依序新增／編輯／切換／篩選／刪除 | 全程只用滑鼠點擊與文字輸入；網址一路維持 `http://localhost:8080/`，無整頁跳轉 | 通過 |
| AC-008-3 | `docker compose stop app` 後點「未完成」篩選 | 顯示「無法連線到伺服器，請檢查網路連線後重試。」＋「重試」按鈕；**先前資料仍在畫面上、篩選鈕仍可點（不凍結）**；`docker compose start app` 後點「重試」→ 錯誤區隱藏、清單恢復 | 通過 |
| AC-008-3（4xx） | 對已刪除項目操作 | 顯示「找不到這筆待辦，可能已被刪除。」（與網路失敗文案不同，依 `code` 分支） | 通過 |
| AC-008-4 | 以 MutationObserver 監看 `loading-indicator` 的 `hidden` 屬性 | `loadSeen:[true,false]` — 操作期間確實顯示過「載入中…」再隱藏 | 通過 |
| AC-008-5 | 375×812 與 1280×800 各操作一次 | 375：`scrollWidth 375 = clientWidth 375`（無橫向捲動），長標題換行不重疊，「編輯」「刪除」各 55×44、核取方塊外層 `<label>` 44×44、「新增」443×44；1280：`scrollWidth 1280 = clientWidth 1280` | 通過 |
| XSS（NFR-002⑤） | 新增標題 `<script>alert(1)</script>` | 清單原樣顯示該文字、**無彈窗**；`todo-list.innerHTML.includes("<script>") === false`（已被 `textContent` 逸出） | 通過 |

**§9. 收尾清理（審核者自己的環境）**

```bash
docker compose down -v
docker ps --format "{{.Names}}"
curl -sS -o /dev/null -w "health-after-down=%{http_code}\n" --max-time 3 http://localhost:8080/health
```
```text
 Container t-0017-app-1 Removed
 Container t-0017-db-1 Removed
 Volume t-0017_todo-db-data Removed
 Network t-0017_default Removed
（docker ps 無輸出）
health-after-down=000
```

**§10. 合併**

```bash
git merge --no-ff -F <訊息檔> task/T-0017-todo-store-view
git log --oneline -2
```
```text
Merge made by the 'ort' strategy.
 6 files changed, 1169 insertions(+), 2 deletions(-)
1ad255d T-0017: 合併前端狀態與呈現（單向資料流）（dev-tl 初審通過）
b53db53 E-001: 批次 5 進入審核
```

### 下一步建議

1. **給 qa-at（P0 UAT）／qa-cr**：**鍵盤 Enter 送出需真人補驗**。本輪實證瀏覽器自動化工具的合成 Enter 不觸發 form implicit submission（連動態插入的原生對照表單也一樣），因此「在輸入框按 Enter 新增」這條路徑本輪**未被驗證**，請以真人手動操作補一次（新增表單與編輯表單各一）。
2. **給 qa-at**：`data-testid="todo-item-toggle"` 掛在 `<input type=checkbox>` 上，該元素本身量得 **13×13**；44×44 的點擊區在它的外層 `<label>`。UI 測試若要驗「點擊區 ≥44px」，請量 `closest("label")`，不要量 testid 元素本身。另 `todo-item` 上有 `data-todo-id`（UUID）可精確定位。二次確認請用 `window.confirm` 注入（自動化工具預設 dismiss＝取消）。
3. **給 qa-cr（程式碼審查）**：兩點可讀性／健壯性觀察，皆非缺陷：(a) `todo-view.js` 的 `editingId` 是純畫面狀態，若編輯期間有其他來源觸發 store 更新（例如錯誤重試），`render()` 會重建編輯表單而把使用者已輸入但未儲存的文字還原成原標題——單人操作下不易觸發，但可列為 P1 的改善點；(b) `renderError` 對「本地驗證錯誤」（例如「標題不可為空白」）也一併附上「重試」按鈕，語意上重試只會重新取清單，建議日後區分 `kind === "validation"` 時不顯示重試鈕。
4. **給日後動樣式的卡**：本卡刻意不改 `styles.css`，動態元素改用行內 `style`（引用既有 `:root` 變數）。這在本卡範圍內是正確判斷，但長期而言樣式散落在 JS 字串常數中（`BUTTON_STYLE`／`DANGER_BUTTON_STYLE`／`PRIMARY_BUTTON_STYLE` 等）不利維護。建議 Gate 2 後開一張樣式收斂卡，把這些常數搬進 `styles.css` 的 class，並沿用相同變數命名；**本卡不退回**。
5. **給 P1（WI-P1-03 前端登入頁）**：`todo-view.js` 目前假設已通過站台層 Basic Auth，401 落在通用文案「驗證失敗，請重新整理頁面後再試」；導入 JWT 後 401 應改為導向登入頁。

### 需要 Leader 裁決的事

- 無。（dev-fe 提出的「styles.css 是否在範圍內」觀察，已由本審核以技術負責人身分裁示：不在範圍、行內樣式做法接受，見上「假設與決策」1，不需 Leader 介入。）
