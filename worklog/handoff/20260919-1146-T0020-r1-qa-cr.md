# T-0020 r1 — qa-cr — 工作紀錄與交接

## A. 開工紀錄

- 開工時間：2026-09-19T11:46:00+08:00
- 角色／模型：qa-cr / Opus 5
- 任務卡：tasks/T-0020-code-review.md（round 1）
- 讀取的輸入（實際讀過的檔案與段落，非任務卡照抄）：
  - tasks/T-0020-code-review.md —（審查對象為 main 合併後最終狀態；背景列出 10 項「特別檢查」清單）
  - .claude/agents/qa-cr.md —（審查重點順序、完成定義、收尾協定：直接在 main commit、只 add 本卡 outputs）
  - docs/templates/code_review_report.md —（報告格式：發現表 + 7 項檢查清單 + 嚴重度定義）
  - docs/templates/worklog_handoff.md —（A/B 段欄位）
  - docs/specs/03_系統設計書_SD.md#6 —（錯誤處理與安全規範，待讀）
  - docs/specs/04_API規格.yaml —（端點、狀態碼、schema，待讀）
  - docs/specs/05_資料庫設計.md —（欄位、約束、索引，待讀）
  - docs/specs/02_系統分析書_SA.md#5 —（業務規則 BR，待讀）
  - docs/specs/adr/ADR-0001~0005 —（語言框架、資料庫、雲端平台、認證與同源部署、Cloud Run，待讀）
  - worklog/handoff/*T0011~T0018* —（開發者與 dev-tl 的「假設與決策」「下一步建議」，待讀）
- 上一輪交接摘要（round 1，不適用）：
  - 退回原因：不適用
  - 本輪要修的點：不適用
- 本輪計畫（3–7 條）：
  1. 讀規格（SD#6、API yaml、DB 設計、SA#5 BR、ADR-0001~0005）與 T-0011～T-0018 全部交接檔，抽出可驗證的規格點。
  2. 逐檔審 src/**、public/**、tests/**、migrations/**、Dockerfile、docker-compose.yml、.github/workflows/**、infra/**、scripts/**，依「正確性 → 安全 → 規格符合 → 測試品質 → 可維護性」順序。
  3. 逐項比對任務卡「特別檢查」10 點（/health 豁免、Basic Auth 常數時間、redact、UUID 不洩漏、trim 早於長度檢查、204 無本文、篩選在後端、SQL 全參數化、Dockerfile 非 root 與 $PORT、workflow 無長期金鑰、migration forward-only）。
  4. 實跑 npm run lint / build / test:unit / test:integration（docker compose 起 Postgres，收尾 down），貼真實輸出核實交接檔宣稱。
  5. 用 dev-tl 補強樣式對全倉庫做憑證掃描並貼輸出；已知假陽性（ci.yml 佔位值、id-token: write、--set-secrets）列為例外，其餘視為真命中。
  6. 對 T-0011～T-0018 開發者「假設與決策」逐條表態接受／不接受。
  7. 產出 docs/reports/20260919-1146-CR-E001.md，結論寫明「阻擋級 = N」，N>0 時附修正卡切分建議。
- 預期產出：docs/reports/20260919-1146-CR-E001.md（不修改任何程式碼）

## B. 交接

- 完工時間／狀態：2026-09-19T11:59:16+08:00 / review

### 完成事項

- 逐檔審完受審範圍全部 40 個檔案（`src/**` 14、`public/**` 5、`tests/**` 10、`migrations/` 1、`Dockerfile`、`docker-compose.yml`、`.github/workflows/**` 3、`infra/` 1、`scripts/**` 3、`package.json`／`tsconfig.json`／`eslint.config.js`／`.env.example`／`.gitignore`／`README.md`），依「正確性 → 安全 → 規格符合 → 測試品質 → 可維護性」順序。
- 實跑 `npm run lint`、`npm run build`、`npm run test:unit`（75 項全綠）、`npm run test:integration`（47 項全綠），真實輸出貼入報告 §1～§3，**核實各卡交接檔的全綠宣稱屬實**。
- 以 dev-tl 補強樣式（含可選引號）對全部受追蹤檔案做憑證掃描，14 筆命中逐筆判讀，**真實憑證 0 筆**；另掃私鑰／長期服務帳號金鑰樣式零命中（報告 §4）。
- 逐項比對任務卡「特別檢查」10 點，找出 **2 個阻擋級**缺陷並各自實跑重現（報告 §5、§6）。
- 對 T-0011～T-0018 共 **40 條**開發者「假設與決策」逐條表態：接受 39、不接受 1（T-0015 第 4 條，即阻擋項 B-1）。
- 報告檢查清單 7 項全部打勾並附證據；結論「阻擋級 = 2」，每項附建議的修正卡切分（5 張卡的切法與驗收建議）。
- 全程**未修改任何程式碼**（`git status` 只有本卡三個產出檔）。

### 產出物（路徑）

- `docs/reports/20260919-1146-CR-E001.md`
- `tasks/T-0020-code-review.md`（`status: review`、`outputs` 填實檔名、`updated`）
- 本交接檔 `worklog/handoff/20260919-1146-T0020-r1-qa-cr.md`

### 未完成／已知問題

- **結論為退回（阻擋級 = 2）**，本卡的工作本身已完成；修正由 Leader 依報告最後一節建 dev 修正卡，不在本卡範圍。
- 收尾**未執行 `docker compose down`**：`soft-dev-team-260919-db-1`／`-app-1` 是平行作業 T-0022（qa-uat）啟動的，停掉會中斷他人服務（提示詞明令「不要殺別人的程序」）。本輪自建的資源已全數清除：驗證資料庫 `crcheck` 已 `DROP`、自用埠 8098／8099 已釋放、§7 抽驗時建立的那一筆待辦已 DELETE 清掉，`docker ps` 確認他人容器仍 Up（報告 §6 末有輸出）。**若 qa-uat 也結束，最後一位收尾者再 `docker compose down` 即可。**

### 我做的假設與決策（含理由）

1. **B-1（點節段繞過）判為阻擋級而非建議級。** 目前實際可達的只有 `@fastify/static` 的 403 與全域 404 表面，未取得任何受保護內容（路由比對走原始 URL，不存在「原始等於 `/api/v1/todos`、正規化等於 `/health`」的字串）。仍判阻擋的理由有三：① SD §6.2 把「豁免比對必須是完全相等的字串」明列為「實作硬性要求（**CR 判準**）」，這是規格直接授權 CR 擋下的項目；② 任務卡背景的「特別檢查」逐字寫了「**點節段**」，這正是本卡被要求抓的東西；③ 認證邊界目前只靠第三方 `send` 的穿越防護守住，防線在專案外部。修正成本約 5 行＋5 條測試，退回代價遠低於留著。
2. **B-2（migration 未自動套用）判為阻擋級。** 判準是嚴重度定義的「違反設計書」：`05_資料庫設計.md` §3 第 4 條與 SD §7 NFR-008 對策④皆明文「於應用啟動時自動執行／自動套用」。加上實測證明使用者照 README「方式一」走完拿到的是所有待辦 API 都 500 的服務，NFR-008「卡關點 0」不可能成立。T-0013 交接檔雖已自陳「尚未接進啟動流程」並寫入下一步建議，但那是**未完成的規格要求**，不因被記錄而變成可接受。
3. **不把 `.env.example` 的三筆掃描命中判為真命中。** 提示詞給的已知例外是三類（`ci.yml` 佔位值、`id-token: write`、`--set-secrets`），字面上 `.env.example` 不在內。但 `tasks/E-001-todo-app.md:102` 的 Leader 裁決 **D-01** 明文允許「`.env.example` 可含僅限本機 compose 的明顯佔位值」，且 T-0011 dev-tl 交接檔已據此判過一次。我採「裁決優先於清單未窮舉」，判為非真命中，同時開 S-8 建議 Leader 把它正式列為第 4 類例外並回寫 SD §6.2，讓下一輪不必再論證一次。`tests/integration/todo-repository.test.ts:37` 的 localhost 預設連線字串同屬此類。
4. **T-0016 決策 1（UUID 格式錯回 400）判為接受。** 開發者是在「提示詞單行敘述」與「三份凍結規格＋任務卡可執行驗收指令」衝突時選了後者，這與角色協定「規格就是合約」一致，且四份依據方向一致。我接受，但在修正卡切分中建議 Leader 補一筆裁決追認，把這個判斷從「開發者的合理推論」升級為「有紀錄的決定」。
5. **對既有 compose 服務（8080）做抽驗時，只做必要的寫入並立即清除。** §7 需要驗證 ajv 型別脅迫，必須實際 POST 一筆；建立後立刻 DELETE（同時順帶驗證 204 無本文），確保不污染 qa-uat 的 UAT 資料。B-1／B-2 的重現則完全不碰他人資料——另開埠 8099 與獨立資料庫 `crcheck`。
6. **憑證掃描樣式以 `grep -f` 從暫存檔讀入。** 環境限制「Bash 指令不可含 ASCII 單引號」，而補強樣式本身含 `["']?`；寫進暫存檔再 `grep -nEi -f` 可完整保留樣式，與直接內嵌等價。

### 驗證方式（接手者如何確認我做的是對的：指令 + 我實際得到的輸出）

完整輸出見報告 `docs/reports/20260919-1146-CR-E001.md` §1～§9（九段，皆為實跑貼上）。此處摘錄四段關鍵的：

```bash
npm run lint && npm run build && npm run test:unit
NODE_ENV=test PORT=8080 DATABASE_URL="postgres://dev:dev@localhost:5432/todo" \
  BASIC_AUTH_USER=dev BASIC_AUTH_PASSWORD=dev LOG_LEVEL=silent npm run test:integration
```
```text
# lint：無輸出，exit 0
# build：BUILD_EXIT=0
# test:unit      → ℹ tests 75 / ℹ pass 75 / ℹ fail 0 / duration_ms 161.7931
# test:integration → ℹ tests 47 / ℹ pass 47 / ℹ fail 0 / duration_ms 1046.333
```

```bash
# B-1 重現（未帶任何憑證，curl --path-as-is）
for P in "/foo/../health" "/.%2e/health" "/%2e%2e/health" "/assets/%2e%2e/%2e%2e/health" "/healthz-secret"; do
  printf "%-34s -> " "$P"; curl -sS --path-as-is -o /dev/null -w "%{http_code}\n" "http://localhost:8099$P"
done
```
```text
/foo/../health                     -> 403   ← 未帶憑證卻不是 401，已越過 Basic Auth hook
/.%2e/health                       -> 403
/%2e%2e/health                     -> 403
/assets/%2e%2e/%2e%2e/health       -> 403
/healthz-secret                    -> 401   ← 對照組，正確
```

```bash
# B-2 重現（乾淨資料庫 crcheck，未跑 migration）
curl -sS -o /dev/null -w "%{http_code}\n" http://localhost:8099/health
curl -sS -u dev:dev -w "\nHTTP=%{http_code}\n" http://localhost:8099/api/v1/todos
```
```text
200
{"code":"E_INTERNAL","message":"Internal Server Error","requestId":"req-2"}
HTTP=500
# 伺服器日誌同一筆：err.message = relation "todos" does not exist（code 42P01），
# 堆疊只進日誌、未進回應；日誌 reqId 與回應 requestId 同為 req-2
```

```bash
# 憑證掃描（補強樣式，全部受追蹤檔案）
git ls-files src public tests migrations infra scripts .github Dockerfile docker-compose.yml \
  package.json .env.example README.md eslint.config.js tsconfig.json \
  | tr "\n" "\0" | xargs -0 grep -nEi -f credscan.txt
```
```text
14 筆命中，逐筆判讀後真實憑證 0 筆：
  已知例外① ci.yml:69,81,83（CI 佔位值）
  已知例外② deploy-staging.yml:26（id-token: write）
  已知例外③ deploy-staging.yml:114 / scripts/deploy-staging.sh:67 / README.md:230（--set-secrets 名稱）
  D-01 許可  .env.example:21,23,29 / tests/integration/todo-repository.test.ts:37
  結構假陽性 src/config.ts:16（型別宣告）／auth-basic.test.ts:37,39／todos-api.test.ts:53（讀 env）
             ／error-handler.test.ts:110（刻意偽造的外洩字串，用來斷言不外洩）
另掃私鑰／AKIA／ghp_／AIza／BEGIN PRIVATE KEY：零命中
```

收尾環境確認：

```bash
netstat -ano | grep ":8099" | grep LISTENING || echo "8099 free"
docker exec soft-dev-team-260919-db-1 psql -U dev -d postgres -c "DROP DATABASE IF EXISTS crcheck"
docker ps --format "{{.Names}} {{.Status}}"
```
```text
8099 free
DROP DATABASE
soft-dev-team-260919-app-1 Up 4 minutes
soft-dev-team-260919-db-1 Up 4 minutes (healthy)      ← 他人（T-0022）的服務未受影響
```

### 下一步建議

1. **給 Leader**：依報告最後一節建 5 張修正卡。優先序建議 修正①（B-1，安全）＞ 修正②（B-2，NFR-008 直接不成立，會擋住 T-0022 UAT 的「照 README 裝起來」情境）＞ 修正③ ＞ 規格同步 ＞ 修正④（可延 P1）。
2. **給 Leader（需裁決）**：S-1 的 ajv 型別脅迫要「關閉 `coerceTypes`」還是「接受並在 04 規格註記」，請先定調再併入修正卡；另 T-0016 決策 1（UUID 格式錯 = 400）建議補一筆裁決追認。
3. **給修正卡的 dev-be**：修正②若採「在 `server.ts` 接 migration」方案，順手把 `createPool` 從 `routes/todos.ts` 上移到 bootstrap，可一併收斂 SD §3.1 BE-01 的職責劃分（報告 T-0016 決策 2 的但書），避免日後再改同一區。
4. **給 qa-lead（T-0020 審核者）**：抽查建議挑 B-1、B-2、S-11 三項——三項都有可一行重現的指令（報告 §5、§6、§9），不需要重建環境；重跑測試指令見 §1～§3。
5. **給後續 CR 與 qa-lead 檢查表**：WBS §1.5 那條 `grep -nE "startsWith|RegExp|\*" src/plugins/basic-auth.ts` 是**字面形狀檢查**，它讓 `basic-auth.ts` 通過了卻完全抓不到 B-1 這種「用標準 API 做正規化」的真實繞過。建議把該驗收改寫為行為斷言（四個點節段變體必須回 401），否則同一類缺陷會再次漏網。
6. **給最後一位收尾者**：`docker compose` 仍在跑（T-0022 的），待其結束後再 `docker compose down`。

### 需要 Leader 裁決的事

1. **S-1 ajv 型別脅迫的方向**：關閉 `coerceTypes: false`（嚴格符合 04 規格 `type: string`）或接受 Fastify 預設並在 04 規格註記。影響是否併入修正卡 ①。
2. **S-8 `.env.example` 佔位值**：請把 D-01 正式列為憑證掃描的第 4 類已知例外，並回寫進 SD §6.2 該行，避免每輪 CR 重新論證。
3. **T-0016 決策 1 的追認**：「UUID 格式錯回 400」目前只有開發者的推論與三份規格佐證，建議補一筆裁決紀錄。
