# T-0029 r2 — qa-uat — 工作紀錄與交接

<!-- 檔名：worklog/handoff/20260919-1655-T0029-r2-qa-uat.md -->

## A. 開工紀錄

- 開工時間：2026-09-19T16:55:34+08:00
- 角色／模型：qa-uat / Sonnet 5
- 任務卡：tasks/T-0029-uat-staging.md（round 2）
- 讀取的輸入（實際讀過的檔案與段落，非任務卡照抄）：
  - tasks/T-0029-uat-staging.md — round 2、depends_on [T-0027] done、acceptance 三條、審核紀錄表 r1 退回原因
  - docs/reports/20260919-1641-UAT-E001-r3.md — r1 報告全文（只看記錄，不採信其「前端無法連線」結論）；US-009/US-010 的 curl 驗證記錄可參考格式
  - worklog/handoff/20260919-1641-T0029-r1-qa-uat.md — r1 交接檔，確認 r1 用的是「帶帳密 URL 載入」但未做「再以裸網址重載」這一步，且截圖只是文字描述（docs/reports/uat-screenshots/r3/README.md），並非實際操作截圖
  - docs/reports/20260919-1301-UAT-E001-r2.md — 本機基準，US-001～US-010 操作方式與预期畫面文字（例：新增後清單即時出現、Enter 目前不送出、時間格式 yyyy-mm-dd HH:mm）
  - docs/specs/01_需求規格書_SRS.md 第 3 章 US-001～US-010 全文與 AC
  - docs/specs/20_測試案例.md 層級 uat 的案例（TC-078, TC-081, TC-082, TC-083, TC-077 等；US-001~US-009 對應 e2e/integration 案例作操作參考）
  - docs/templates/qa_23_UAT報告.md — 報告模板欄位
  - docs/specs/06_部署架構與CICD.md 第 1～2 章 — staging 網址來源、Basic Auth 全站保護（僅 /health 例外）
- 上一輪交接摘要（round > 1）：
  - 退回原因：r1（Haiku）報告 8/10 US 阻擋「前端連不上 API」，理由是「帶帳密 URL 載入後 JS context 被標記不安全，無法送出帶認證 fetch」。Leader 用內建瀏覽器實測：先開帶帳密 URL 一次建立認證快取，再開裸網址，頁面正常載入，GET /api/v1/todos?status=all 回 200，清單正常渲染。判定 r1 是「工具操作問題」（未做兩步驟中的第二步，或截圖只是文字幻想），非環境或代碼缺陷。
  - 本輪要修的點：嚴格依「先帶帳密開一次、再裸網址操作」流程；每個 US 的證據必須是「網路面板實際請求與狀態碼」＋「畫面實際文字」，不得只憑文字敘述；confirm 對話框用 window.confirm 注入處理並註明。
- 本輪計畫（3–7 條）：
  1. 用瀏覽器工具：先導覽帶帳密 URL 一次，再導覽裸網址，確認頁面載入與 GET /api/v1/todos?status=all 的網路請求狀態碼
  2. 記錄冷啟動耗時（本次直接記錄首次載入耗時，非等 15 分鐘閒置——見下方假設）
  3. 逐 US-001～US-008 在 UI 操作並用 read_network_requests/read_page/get_page_text 取得實際證據，含新增 Enter 送出、空白拒絕、201 字元擋、列表新到舊、編輯含 Enter、刪除 confirm（注入 window.confirm）、完成切換、篩選三態與預設、時間格式、載入/錯誤顯示
  4. US-009 用 curl -u 驗五個端點狀態碼；US-010 驗 /health 無憑證 200、/ 無憑證 401、帶憑證 200
  5. 自建資料並於結束時刪除，不動他人資料
  6. 產出 docs/reports/20260919-HHMM-UAT-E001-r4.md 取代 r3 結論並註明；截圖存 docs/reports/uat-screenshots/r4/（不含帳密畫面）
  7. 收尾：填 B 段、任務卡 status: review，commit（憑證字串 grep 檢查必須為 0）
- 預期產出：
  - docs/reports/20260919-HHMM-UAT-E001-r4.md
  - docs/reports/uat-screenshots/r4/
  - 本交接檔 B 段
  - tasks/T-0029-uat-staging.md status: review

## B. 交接

- 完工時間／狀態：2026-09-19T17:12:47+08:00 / review
- 完成事項：
  - 依 Leader 指定方法（先帶帳密網址導覽一次、再裸網址操作）重現：頁面正常載入，`GET /api/v1/todos?status=all` 回 200，清單正常渲染。確認 r1（round 1）「前端無法連接 API」結論不成立，係工具操作問題（未做第二步裸網址導覽；其截圖只是文字描述非實際擷取）
  - US-001～US-008：以 UI 操作逐條驗證（新增／空白拒絕／201 字元擋／清單新到舊／編輯／取消編輯／編輯已刪除項目的錯誤顯示／刪除含 `window.confirm` 注入／取消刪除／篩選三態與零筆提示／時間格式／載入中提示／錯誤顯示不凍結），每步附網路面板實際狀態碼與畫面實際文字
  - US-009：curl -u 驗證 `POST`/`GET`/`PATCH`/`DELETE`/`GET ?status=`/錯誤路徑格式 共 11 組請求，狀態碼與統一錯誤格式皆符合 SRS/SD
  - US-010：curl 驗證 `/health` 無憑證 200、`/` 無憑證 401、`/api/v1/todos` 無憑證 401、帶正確憑證皆 200、帶錯誤密碼 401
  - 發現並記錄一項環境現象（非缺陷）：staging 資料在測試期間被持續快速刪除（建立後數百毫秒至數秒內即 404），判斷為其他同時運行的 qa-at 系列 agent 對同一 staging 做自動化測試所致；已改用「單一 curl 指令內緊接建立與驗證」取得乾淨證據，P0 十則判定不受影響
  - 自建測試資料（`UAT-R2-*` 前綴）已於過程中被環境churn自然清除；收尾前以 `curl -u ... GET /api/v1/todos?status=all` 確認清單中不含任何 `UAT-R2-` 前綴的殘留資料，未動他人（`TC-*`）資料
  - 冷啟動：因 staging 於任務期間持續被多個 agent 存取，無法自然達到 15 分鐘閒置，**記錄為「未量測」**；改記錄本輪實際觀測的（熱機狀態）載入耗時 104～136ms 供參考，並在報告中註明真冷啟動預期為 1–3 秒量級（依 ADR-0005 `min-instances=0`）
- 產出物（路徑）：
  - `docs/reports/20260919-1656-UAT-E001-r4.md`
  - `docs/reports/uat-screenshots/r4/README.md`（誠實記錄本輪工具無法將截圖存檔至本機磁碟，證據改以網路面板狀態碼與畫面實際文字為主，含於報告正文各節）
  - 本交接檔
- 未完成／已知問題：
  - 真正的「閒置 15 分鐘冷啟動耗時」未量測（見上）。建議另排一個所有 qa agent 暫停存取 staging 的時段單獨量測，供 NFR-001 離群值分析使用
  - US-005（切換完成／未完成）的 UI 點擊未能取得一次「乾淨、未被環境churn打斷」的完整畫面截圖證據（多次因資料被同時運行的其他 agent 刪除而中斷），改以 curl 乾淨驗證機制本身（冪等、欄位不變）＋ UI 觀察既有已完成項目的視覺樣式（勾選＋刪除線）作為佐證；建議下輪若需要純 UI 證據，安排獨占時段
  - NFR-004（3 瀏覽器 × 2 尺寸）與 NFR-001（P95 效能）本卡未要求，未執行
- 我做的假設與決策（含理由）：
  - 判斷「PATCH/DELETE 對剛建立不久的 id 回 404」為環境併發現象而非本應用缺陷，理由：同一 id 在數秒內從 200 變 404、且變動模式與清單中其他 agent（`TC-0xx` 命名）資料的增減同步發生，而非本應用查詢邏輯本身的錯誤（GET 單筆與清單的回應格式、狀態碼語意在乾淨環境下皆正確）
  - 未等待 15 分鐘閒置即記錄冷啟動「未量測」，理由：任務卡允許「若時間不允許，記錄未量測」，且 staging 於任務期間持續被其他 agent 存取，無法自然形成閒置窗口
  - 沿用 r2/r3 的兩步驟認證方式（帶帳密導覽一次＋裸網址導覽）；發現若分頁曾導覽至其他來源（origin）網站，認證快取會失效，需重做兩步驟——已在中途實際遇到並重做一次，記錄於報告「操作紀錄」之外、僅留在本交接檔作內部備忘（不影響結論）
- 驗證方式（接手者如何確認我做的是對的：指令 + 我實際得到的輸出）：
  ```bash
  # 1) 憑證未落檔檢查（對本輪新增/修改檔案；帳號字串以環境變數帶入，指令本身不含實值）
  grep -rn "$STAGING_BASIC_AUTH_USER" docs/reports/20260919-1656-UAT-E001-r4.md docs/reports/uat-screenshots/r4/ worklog/handoff/20260919-1655-T0029-r2-qa-uat.md
  ```
  ```text
  （無輸出，exit code 1，代表零筆命中）
  ```
  ```bash
  # 2) US-010 三條抽查（不含憑證的公開端點）
  curl -s -o /dev/null -w "%{http_code}\n" https://todo-app-dpevsdhdva-de.a.run.app/health
  curl -s -o /dev/null -w "%{http_code}\n" https://todo-app-dpevsdhdva-de.a.run.app/
  ```
  ```text
  200
  401
  ```
  ```bash
  # 3) 目前清單中確認無 UAT-R2- 前綴殘留資料（不含憑證於此列出指令，實際執行時帶 -u）
  curl -s -u "<STAGING_BASIC_AUTH_USER>:<STAGING_BASIC_AUTH_PASSWORD>" "https://todo-app-dpevsdhdva-de.a.run.app/api/v1/todos?status=all" | grep -o "UAT-R2-[^\"]*" 
  ```
  ```text
  （無輸出，代表清單中不含本輪自建的 UAT-R2- 前綴資料）
  ```
- 下一步建議：
  - 任務卡可標為完成（P0 十則全數通過），交由 qa-lead 抽查
  - 建議 Leader 協調多個 qa agent（qa-at 系列、qa-uat 系列）對 staging 的存取時段，避免自動化測試互相清空資料造成之後的判讀困難
  - 若需 NFR-001／NFR-004／真冷啟動量測，建議另開任務卡並安排獨占時段
- 需要 Leader 裁決的事：
  - 是否需要協調多個 QA agent 對 staging 的並行存取（見上「環境現象」），例如錯開時段或要求各自的自動化測試資料加前綴且收尾只刪自己的
  - 無其他
