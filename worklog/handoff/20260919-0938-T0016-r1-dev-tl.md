# T-0016 r1 — dev-tl — 審核工作紀錄與交接

## A. 開工紀錄

- 開工時間：2026-09-19T09:38:00+08:00
- 角色／模型：dev-tl / opus
- 任務卡：tasks/T-0016-todo-api.md（round 1，status: review → 本次審核）
- 本次工作：**審核 T-0016「待辦 API 與業務規則」**（依 `.claude/skills/review-round/SKILL.md`）
- 讀取的輸入（實際讀過的檔案與段落）：
  - `.claude/agents/dev-tl.md` 全文 —— 審核他人時的要求：實際 checkout、實際跑、實際比對 OpenAPI，指令與輸出貼進審核交接檔
  - `.claude/skills/review-round/SKILL.md` 全文 —— 審核一輪的 7 個步驟與判定規則
  - `tasks/T-0016-todo-api.md` 全文 —— outputs 六項、acceptance 五條、驗收方式
  - `docs/specs/10_開發計畫_WBS.md` §0（硬前提 1~8）、§1.6（WI-06 全文含驗收方式草案）、§3.2（合併條件）、§5（共用檔案規約與 `src/app.ts` 錨點）
  - `docs/specs/04_API規格.yaml` 第 117~341 行（五個 todos 端點）、`components.parameters.TodoId`／`StatusFilter`、`components.schemas.Todo`／`CreateTodoRequest`／`UpdateTodoRequest`／`Error`、`components.responses.BadRequest`／`Unauthorized`／`NotFound`／`InternalServerError`
  - `tasks/E-001-todo-app.md` 最後一段 —— **Leader 裁決 2026-09-19T09:37:07+08:00：UUID 格式錯誤回 400 E_VALIDATION，規格優先，dev-be 依規格實作正確**
  - `worklog/handoff/20260919-0922-T0016-r1-dev-be.md` 全文（A 段、B 段、假設與決策 5 條、需 Leader 裁決 1 條）
  - 受審程式碼：`src/schemas/todo-schema.ts`、`src/services/todo-service.ts`、`src/routes/todos.ts`、`src/app.ts`；對照既有 `src/plugins/error-handler.ts`、`src/repositories/todo-repository.ts`
- 本輪計畫：
  1. 在 worktree `C:/Users/excal/Projects/Claude/soft-dev-team-260919-wt/T-0016`（commit `c88f9e8`）重跑 lint／build／unit／integration
  2. 實際起服務（`docker compose up -d db` + `node dist/server.js`），以 `curl -u` 逐端點驗狀態碼、schema、錯誤格式、邊界
  3. 與 `04_API規格.yaml` 逐項比對；檢查 BR 落點、`createdAt` 序列化、不在 register 內 `setErrorHandler`、repository 未被改、`app.ts` 只動錨點一行 + import
  4. `git diff main...HEAD --stat` 確認只動 outputs
  5. 對 dev-be 交接檔「假設與決策」逐條表態
  6. 判定並（若通過）在根目錄 main 執行 `git merge --no-ff`、更新任務卡與 CHANGELOG、清理 worktree
- 預期產出：本審核交接檔、任務卡 status/審核紀錄表、merge commit、收尾 commit（CHANGELOG）

## B. 交接

- 完工時間／狀態：2026-09-19T09:52:00+08:00 / **done（初審通過，已合併）**

### 完成事項

- 在 worktree 實際重跑 WBS §1.6 驗收指令與任務卡 acceptance 全部五條，**全部通過**（輸出見下方「驗證方式」）。
- 實際起服務以 `curl -u dev:dev` 逐端點驗證五個端點的狀態碼、回應 schema、錯誤格式與邊界，**與 `04_API規格.yaml` 逐項一致**。
- 判定：`status: done`，任務卡審核紀錄表加一列；執行 `git merge --no-ff task/T-0016-todo-api`；`CHANGELOG.md` 加一行；移除 worktree 與分支。

### acceptance 逐條判定

| # | acceptance | 判定 | 證據 |
|---|---|---|---|
| 1 | WBS §1.6「驗收方式草案」每條指令實際執行且結果符合 | **通過** | 驗證方式 §2、§4、§5：11 條 curl 全數與草案註記的期待值相同（201/200/200/200/204/404/400/400/400/400，`grep -c updatedAt` = 0）；另驗「201 字元回 400 而非 500」與「PATCH 夾帶 `id`／`createdAt` 被忽略且原值不變」 |
| 2 | `npm run lint` 與相關測試全綠；不改 outputs 以外檔案 | **通過** | 驗證方式 §1（lint exit 0）、§3（unit 51/51、integration 47/47）、§6（`git diff main...HEAD --stat` 只列 6 個 outputs ＋ 任務卡 ＋ 交接檔） |
| 3 | 五端點狀態碼、schema、錯誤碼與 04 逐項一致（刪除 204、設定目標狀態冪等、後端篩選、排序新到舊） | **通過** | 驗證方式 §4（DELETE 204 且無 `content-length`、PATCH 重送三次仍 `true` 且可設回 `false`、`status=active/completed/all` 篩選正確、清單 `createdAt` 遞減）、§5（四種錯誤本文皆為 `{code,message,requestId,details?}`，`code` 落在 Error schema 的 enum 內） |
| 4 | trim 早於長度檢查，標題上限 200 | **通過** | 驗證方式 §4「title 邊界」：`""`→400、`"   "`→400、1 字元→201、200 字元→201、201 字元→400（非 500）、前後帶空白且 trim 後 201 字元→400 E_VALIDATION；service 層 `validateAndTrimTitle` 先 `trim()` 再驗長度，早於任何交給 DB 的動作 |
| 5 | 單元測試覆蓋每個錯誤碼與邊界 | **通過** | 驗證方式 §3：`tests/unit/todo-service.test.ts` 18 條，含 `E_VALIDATION`（空字串／全空白／201 字元／trim 後 201 字元／PATCH 空 body）與 `E_NOT_FOUND`（get／update／delete 三入口），邊界 0/1/200/201 齊備 |

### 與 04_API規格.yaml 逐項比對（實測值 vs 規格）

| 端點 | 規格 | 實測 | 結果 |
|---|---|---|---|
| `POST /api/v1/todos` | 201 + `Todo`（4 欄，`isCompleted` 恆 false） | `201`，本文 `{"id","title","isCompleted":false,"createdAt"}` 恰 4 欄 | 一致 |
| `GET /api/v1/todos` | 200 + `Todo[]`，新到舊、後端篩選、空為 `[]` | `200`，`createdAt` 遞減；`active`／`completed`／`all` 篩選正確 | 一致 |
| `GET /api/v1/todos/{id}` | 200／404；非 UUID 400 | `200` / `404` / `not-a-uuid`→`400` | 一致（Leader 裁決：400） |
| `PATCH /api/v1/todos/{id}` | 200；設定目標狀態（冪等）；至少一欄否則 400；`createdAt` 不變；夾帶欄位忽略 | 重送 `true` 三次皆 `200` 且值恆 `true`；`false` 可設回；`{}`→400；`createdAt` 全程不變；夾帶 `id`／`createdAt` 被忽略 | 一致 |
| `DELETE /api/v1/todos/{id}` | 204 無本文；不存在 404，不建資料、不回 5xx | `204`，`size_download=0`，無 `content-length` 標頭；再 DELETE→`404`；刪除後 GET→`404` | 一致 |
| `StatusFilter` | enum 三值，預設 `all`，其他回 400 | 未帶 → 等同 `all`；`bogus`→`400 E_VALIDATION`，`details.rule=enum` | 一致 |
| `Error` schema | `{code,message,requestId,details?}`，`code` 在 enum 內，不含堆疊／SQL／路徑 | 四種錯誤本文逐一檢視，欄位與 enum 皆符合，無堆疊／SQL／內部路徑外洩 | 一致 |
| `Todo.createdAt` | `format: date-time`（RFC 3339 UTC） | `"2026-09-19T01:40:02.582Z"` | 一致 |
| `updatedAt` 不進契約（O-008） | 不得出現 | `grep -c "updatedAt"` = **0** | 一致 |
| `401` | 附 `WWW-Authenticate: Basic realm="staging"`，不含待辦資料 | 標頭存在，本文為 `E_UNAUTHORIZED` 錯誤物件 | 一致 |

### 結構檢查（初審項）

| 檢查項 | 結果 |
|---|---|
| BR 落點在 service 層 | **通過**。`trim` 後長度 1~200、PATCH 至少一欄、404 轉換全在 `todo-service.ts`；`routes/todos.ts` 只做 schema 與序列化，無任何業務判斷 |
| `createdAt` 序列化為 date-time | **通過**。repository 回 `Date`，`TodoResponseSchema` 以 `format: "date-time"` 由 `fast-json-stringify` 序列化；全鏈無第二處 `toISOString()`（`grep` 無命中） |
| 不在 register 內 `setErrorHandler` | **通過**。`grep "setErrorHandler\|setNotFoundHandler"` 在本卡三個 src 檔只命中一行**註解**，無實際呼叫 |
| repository 未被改 | **通過**。`git diff main...HEAD -- src/repositories/` 空輸出 |
| `app.ts` 只動錨點一行 + import | **通過**。`2 insertions(+), 0 deletions`：`import registerTodoRoutes` 一行 ＋ `[ANCHOR:routes]` 下 `registerTodoRoutes(app, config);` 一行；五個錨點順序未變（硬前提 8） |
| 倉庫無憑證值（硬前提 5） | **通過**。本卡五個檔掃描唯一命中為 `const PASSWORD = process.env.BASIC_AUTH_PASSWORD;`（變數宣告，非字面值） |
| 注入風險 | **無**。本卡不含任何 SQL；repository 未改，仍為 `$1/$2` 參數化 |
| 資源釋放 | **通過**。`registerTodoRoutes` 建池一次並掛 `onClose` 關池；整合測試 18 個 `app.close()` 對應 18 條測試，無連線洩漏 |

### 對 dev-be 交接檔「假設與決策」逐條表態

| # | 決策 | 表態 | 理由 |
|---|---|---|---|
| 1 | UUID 格式錯誤採 **400 E_VALIDATION**（不採派工提示詞的 404） | **接受** | 與 `04_API規格.yaml` `TodoId` 參數說明、WBS §1.6 驗收指令、SA／SD §5 第 6 點四處一致；且 **Leader 已於 2026-09-19T09:37:07+08:00 裁決「提示詞的 404 為誤寫，規格優先，dev-be 依規格實作正確」**（`tasks/E-001-todo-app.md`）。實測 `not-a-uuid`→400、`00000000-0000-4000-8000-000000000000`→404，分界正確 |
| 2 | 連線池建立與生命週期放在 `routes/todos.ts`，不改 `app.ts`／`server.ts` | **接受** | 本卡 outputs 不含 `server.ts`，`app.ts` 只允許錨點一行；以 `onClose` 綁 app 生命週期是在不越界前提下的正解。實測整合測試 18 次建／關 app 無連線洩漏、無 hang。**但列入下一步建議**：P1（WI-P1-02）會同時動 `todo-service.ts` 與 `app.ts`，屆時可評估是否把池上移到 `app.ts` 統一管理 |
| 3 | UUID 格式驗證留在 schema／ajv，service 不重複驗 | **接受** | 對應 SD §5 第 6 點「分界寫死」與 WBS §1.6「業務層再驗一次」只針對 `title` 的敘述。實測四個帶 `{id}` 的端點（GET／PATCH／DELETE）非 UUID 皆回 400 |
| 4 | PATCH「至少一欄」在 schema（`minProperties:1`）與 service 各驗一次 | **接受** | 與 SD §6.3 對 `title` 雙層驗證的精神一致，且讓單元測試可不經 Fastify 直接測到這條規則。兩層訊息不同但 `code` 同為 `E_VALIDATION`，不違反契約 |
| 5 | `createdAt` 全程維持 `Date`，只由 `TodoResponseSchema` 序列化 | **接受** | 依 T-0013 交接檔交辦執行，避免兩處格式化邏輯。實測回應為 `2026-09-19T01:40:02.582Z`，符合 `format: date-time` |

### 未完成／已知問題

- 無阻擋級問題。下列為**非阻擋的小瑕疵**，已寫入「下一步建議」：
  1. **原始字串 >200 但 trim 後 ≤200 的標題會在 schema 層被擋成 400**（實測：`"  " + "b"×200 + "  "`（原始 204 字元、trim 後 200 字元）回 `400`，訊息為 `body/title must NOT have more than 200 characters`）。這是**規格本身的雙層設計所致**，不是實作偏差：`04_API規格.yaml` 的 `CreateTodoRequest.title` 明定 `maxLength: 200`（作用於原始字串），WBS §1.6 亦明寫「API schema：`title` `minLength:1, maxLength:200`」。實作同時照做了 schema 層與業務層，行為與兩份規格逐字一致。風險層面 R-07 要求的「不得走 500」已達成（回 400 E_VALIDATION）。
  2. schema 層驗證失敗時 `message` 沿用 Fastify 原文（如 `body/title must NOT have more than 200 characters`），與 04 的範例訊息（`title must be at most 200 characters`）字面不同。`Error` schema 對 `message` 只要求 `type: string`，範例非契約，故不構成不一致；且不回射使用者輸入原文，無資訊洩漏。

### 我做的假設與決策（含理由）

1. **`docker build` 未重跑**。WBS §3.2 合併條件 1 明訂 build 階段為「`npm run build`（或 `docker build`）」，`npm run build` 已 exit 0；且 Leader 裁決 ②（2026-09-19T08:13）已將 docker build 驗證延後至 CI 與 T-0018。本卡未動 `Dockerfile`。
2. **`npx @redocly/cli lint` 未重跑**。本卡未改 `docs/specs/04_API規格.yaml`（`git diff` 佐證），該檔自 T-0011 起未變動；dev-be 於本輪已跑過並貼出輸出。不列入本次重跑範圍。
3. **收尾 commit 未一併修 `package.json`／`ci.yml`**。E-001 對 T-0013 ②、T-0012 ① 的兩項裁決（migrate 腳本、ci unit job 補 build）已於批次 2／3 的收尾 commit 完成，本次無待補項。
4. **測試資料清理**：驗證用的 Postgres 以 `docker compose down -v` 連同 volume 一併移除，不留殘餘容器、網路與資料。

### 驗證方式（指令 + 實際輸出）

**§1. lint／build（worktree `C:/Users/excal/Projects/Claude/soft-dev-team-260919-wt/T-0016`，commit `c88f9e8`）**

```bash
npm run lint
npm run build && ls dist/routes/todos.js dist/services/todo-service.js dist/schemas/todo-schema.js
```

```text
> todo-app@0.1.0 lint
> eslint . && tsc --noEmit -p tsconfig.json

LINT_EXIT=0

> todo-app@0.1.0 build
> tsc -p tsconfig.json

BUILD_EXIT=0
dist/routes/todos.js
dist/schemas/todo-schema.js
dist/services/todo-service.js
```

**§2. 起 Postgres 與 migration**

```bash
docker compose up -d db
docker inspect --format "{{.State.Health.Status}}" t-0016-db-1
NODE_ENV=development PORT=8080 DATABASE_URL=postgres://dev:dev@localhost:5432/todo BASIC_AUTH_USER=dev BASIC_AUTH_PASSWORD=dev LOG_LEVEL=info node dist/db/migrate.js
```

```text
 Container t-0016-db-1 Started
try1=healthy
無待套用的 migration（已是最新版本）。
```

**§3. 單元與整合測試**

```bash
node --test "tests/unit/todo-service.test.ts"
npm run test:unit
node --test "tests/integration/todos-api.test.ts"
npm run test:integration
```

```text
# node --test tests/unit/todo-service.test.ts
✔ listTodos：把 status 原樣轉給 repository，回傳 camelCase 列 (0.9322ms)
✔ createTodo：一般標題會先 trim 再交給 repository (0.6744ms)
✔ createTodo：空字串回 E_VALIDATION/400（BR-001） (0.379ms)
✔ createTodo：全空白字串回 E_VALIDATION/400（schema 的 minLength 擋不掉，業務層再驗一次） (0.0824ms)
✔ createTodo：剛好 1 字元（trim 後）視為合法 (0.1032ms)
✔ createTodo：剛好 200 字元（trim 後）視為合法 (0.0781ms)
✔ createTodo：201 字元（無前後空白）回 E_VALIDATION/400 而非 500 (0.0831ms)
✔ createTodo：trim 後才是 201 字元（原始字串因前後空白更長）回 400 而非 500（R-07 的核心情境） (0.0695ms)
✔ getTodoById：查得到即回傳 camelCase 物件 (0.1181ms)
✔ getTodoById：查無資料回 E_NOT_FOUND/404（BR-009） (0.1688ms)
✔ updateTodo：空 body（無 title 也無 isCompleted）回 E_VALIDATION/400（BR-012） (0.1273ms)
✔ updateTodo：只給 title 時 trim 後再驗證長度，isCompleted 傳 undefined 給 repository (0.1509ms)
✔ updateTodo：title 為全空白回 E_VALIDATION/400 (0.0938ms)
✔ updateTodo：只給 isCompleted=true（設定目標狀態，非 toggle） (0.1247ms)
✔ updateTodo：對已完成的待辦重送 isCompleted=true 仍是同一個結果（冪等，O-002） (0.1305ms)
✔ updateTodo：查無資料回 E_NOT_FOUND/404（BR-009，且不得因此建立資料） (0.0732ms)
✔ deleteTodo：刪除成功不丟例外（由路由層回 204） (0.1147ms)
✔ deleteTodo：查無資料回 E_NOT_FOUND/404（BR-009，不得回 5xx） (0.0839ms)
ℹ tests 18  ℹ pass 18  ℹ fail 0  ℹ duration_ms 103.0746

# npm run test:unit（全專案）
ℹ tests 51  ℹ pass 51  ℹ fail 0  ℹ duration_ms 145.8261

# node --test tests/integration/todos-api.test.ts
✔ POST /api/v1/todos：成功建立回 201，isCompleted 恆為 false（BR-006），不含 updatedAt（O-008） (190.524ms)
✔ POST /api/v1/todos：全空白標題回 400 E_VALIDATION（trim 早於長度檢查，schema 的 minLength 擋不掉） (15.935ms)
✔ POST /api/v1/todos：201 字元標題回 400 而非 500（R-07） (10.2456ms)
✔ POST /api/v1/todos：缺 title 回 400（schema required） (8.6948ms)
✔ POST /api/v1/todos：夾帶 id／createdAt 一律忽略，由伺服器產生（BR-003、BR-012） (22.6431ms)
✔ GET /api/v1/todos：新到舊排序（BR-005），且可依 status 篩選（O-003，後端做） (70.3589ms)
✔ GET /api/v1/todos?status=bogus：非三個合法值回 400 E_VALIDATION (8.9475ms)
✔ GET /api/v1/todos/{id}：查得到回 200 (22.6502ms)
✔ GET /api/v1/todos/{id}：格式合法但查無資料回 404（BR-009） (17.4259ms)
✔ GET /api/v1/todos/{id}：非 UUID 格式回 400（見檔頭「規格衝突說明」） (9.6925ms)
✔ PATCH /api/v1/todos/{id}：設定完成狀態為冪等（O-002），重送仍為同一結果 (23.1304ms)
✔ PATCH /api/v1/todos/{id}：只改標題，不動 createdAt 與 isCompleted（UC-003） (23.9398ms)
✔ PATCH /api/v1/todos/{id}：空 body 回 400（至少一個可更新欄位，BR-012） (21.9026ms)
✔ PATCH /api/v1/todos/{id}：夾帶 id／createdAt 被忽略，原值不變（BR-012） (23.278ms)
✔ PATCH /api/v1/todos/{id}：查無資料回 404 (18.983ms)
✔ DELETE /api/v1/todos/{id}：成功回 204 無本文（BR-008、O-001） (21.4698ms)
✔ DELETE /api/v1/todos/{id}：查無資料回 404，且不得因此建立資料（BR-009） (17.0883ms)
✔ 未帶憑證呼叫 /api/v1/todos 回 401，不回傳任何待辦資料 (9.3978ms)
ℹ tests 18  ℹ pass 18  ℹ fail 0  ℹ duration_ms 748.2474

# npm run test:integration（全專案）
ℹ tests 47  ℹ pass 47  ℹ fail 0  ℹ duration_ms 937.8929
```

**§4. 實際起服務，`curl -u` 逐端點驗（`node dist/server.js`，`-u dev:dev`）**

```bash
curl -sS -o /dev/null -w "health=%{http_code}\n" http://localhost:8080/health
curl -u dev:dev -sS -X POST http://localhost:8080/api/v1/todos -H "content-type: application/json" -d "{\"title\":\"  buy milk  \"}" -w "\nHTTP=%{http_code}\n" -D -
```

```text
health=200
{"status":"ok"}

# POST：201，回應恰 4 欄（無 updatedAt），title 已 trim，createdAt 為 date-time
{"id":"35e53856-46f7-4314-9675-90ca141180e5","title":"buy milk","isCompleted":false,"createdAt":"2026-09-19T01:40:02.582Z"}
HTTP=201
HTTP/1.1 201 Created
content-type: application/json; charset=utf-8
content-length: 123
```

```bash
B="-u dev:dev -sS -o /dev/null -w %{http_code}\n"
curl $B -X POST   .../api/v1/todos -d "{\"title\":\"buy milk\"}"
curl $B           .../api/v1/todos
curl $B           .../api/v1/todos/$ID
curl $B -X PATCH  .../api/v1/todos/$ID -d "{\"isCompleted\":true}"   # 兩次
```

```text
POST 201 期待201  -> 201
GET list 期待200  -> 200
GET byid 期待200  -> 200
PATCH 1 期待200   -> 200
PATCH 2 期待200   -> 200
--- PATCH 冪等後實際值 ---
{"id":"9bb7ffdd-357e-471b-b489-b6c8ecb89842","title":"T-0016 review A","isCompleted":true,"createdAt":"2026-09-19T01:40:13.964Z"}
```

```bash
# 列表排序（新到舊）與 status 篩選、updatedAt
curl -u dev:dev -sS ".../api/v1/todos"
curl -u dev:dev -sS ".../api/v1/todos?status=active|completed|all" | grep -c $ID
curl -u dev:dev -sS ".../api/v1/todos" | grep -c "updatedAt"
```

```text
--- 列表排序新到舊（前 3 筆）---
[{"id":"2e600f79-...","title":"buy milk","isCompleted":false,"createdAt":"2026-09-19T01:40:15.573Z"}
{"id":"720ebef8-...","title":"T-0016 review B","isCompleted":false,"createdAt":"2026-09-19T01:40:15.289Z"}
{"id":"9bb7ffdd-...","title":"T-0016 review A","isCompleted":true,"createdAt":"2026-09-19T01:40:13.964Z"}
--- status 篩選（ID 為已完成、ID2 為未完成）---
active    是否含已完成ID:   0
completed 是否含已完成ID:   1
all       是否含已完成ID:   1
active    是否含未完成ID2:  1
completed 是否含未完成ID2:  0
--- updatedAt 不進回應 ---
grep -c updatedAt = 0
```

```bash
# DELETE 204 無本文／再 DELETE 404／UUID 分界／非法 status／PATCH 空 body
curl -u dev:dev -sS -X DELETE .../api/v1/todos/$ID -w "HTTP=%{http_code} body_bytes=%{size_download}\n" -D -
```

```text
--- DELETE 204 無本文 ---
HTTP=204 body_bytes=0
content-length 標頭：（無 content-length，符合 204 無本文）
再 DELETE        期待404 -> 404
刪除後 GET       期待404 -> 404
UUID 格式錯      期待400 -> 400
格式對查無       期待404 -> 404
status=bogus     期待400 -> 400
PATCH 空body     期待400 -> 400
DELETE not-a-uuid 期待400 -> 400
PATCH  not-a-uuid 期待400 -> 400
```

```bash
# title 邊界 0 / 1 / 200 / 201（含 trim 情境）
T200PAD="  " + "b"×200 + "  "   # 原始 204 字元、trim 後 200
T201PAD="  " + "c"×201 + "  "   # 原始 205 字元、trim 後 201
```

```text
0 字元 ""        期待400 -> 400
全空白 "   "     期待400 -> 400
1 字元           期待201 -> 201
200 字元         期待201 -> 201
201 字元         期待400 -> 400
trim後200(204長) 期待201 -> 400   ← 見「未完成／已知問題」1（規格雙層設計所致，非實作偏差）
trim後201(205長) 期待400 -> 400
--- trim 後 201 的錯誤本文（400 E_VALIDATION，非 500）---
{"code":"E_VALIDATION","message":"body/title must NOT have more than 200 characters","requestId":"req-z","details":{"field":"title","rule":"maxLength"}}
```

```bash
# 錯誤格式是否符合 Error schema
```

```text
--- 404 本文 ---
{"code":"E_NOT_FOUND","message":"Todo not found","requestId":"req-10"}
--- 非 UUID 400 本文 ---
{"code":"E_VALIDATION","message":"params/todoId must match format \"uuid\"","requestId":"req-11","details":{"field":"todoId","rule":"format"}}
--- status=bogus 400 本文 ---
{"code":"E_VALIDATION","message":"querystring/status must be equal to one of the allowed values","requestId":"req-12","details":{"field":"status","rule":"enum"}}
--- PATCH 空 body 400 本文 ---
{"code":"E_VALIDATION","message":"body must NOT have fewer than 1 properties","requestId":"req-13","details":{"field":"(root)","rule":"minProperties"}}
--- 全空白標題 400 本文 ---
{"code":"E_VALIDATION","message":"title must not be empty after trimming","requestId":"req-14","details":{"field":"title","rule":"minLength"}}
--- 未帶憑證 401 ---
HTTP/1.1 401 Unauthorized
www-authenticate: Basic realm="staging"
content-type: application/json; charset=utf-8
```

```bash
# PATCH 設定目標狀態冪等、可設回、只改 title、夾帶欄位忽略
```

```text
建立：                    {"id":"d578ac3b-...","title":"idempotency-check","isCompleted":false,"createdAt":"2026-09-19T01:41:59.382Z"}
PATCH true 第1次：        {...,"isCompleted":true, "createdAt":"2026-09-19T01:41:59.382Z"}
PATCH true 第2次（冪等）： {...,"isCompleted":true, "createdAt":"2026-09-19T01:41:59.382Z"}
PATCH true 第3次：        {...,"isCompleted":true, "createdAt":"2026-09-19T01:41:59.382Z"}
PATCH false（可設回）：    {...,"isCompleted":false,"createdAt":"2026-09-19T01:41:59.382Z"}
PATCH 只改 title：        {...,"title":"renamed","isCompleted":false,"createdAt":"2026-09-19T01:41:59.382Z"}  ← 已 trim，createdAt 不變
PATCH 夾帶 id/createdAt： {"id":"d578ac3b-...","title":"kept","isCompleted":false,"createdAt":"2026-09-19T01:41:59.382Z"}  ← 原值不變
POST  夾帶 id/createdAt/isCompleted：{"id":"d08c06d3-...","title":"extra-fields","isCompleted":false,"createdAt":"2026-09-19T01:41:25.339Z"}  ← 一律忽略
```

**§5. 結構與範圍檢查**

```bash
git diff main...HEAD --stat
grep -rn "setErrorHandler\|setNotFoundHandler" src/routes/todos.ts src/services/todo-service.ts src/schemas/todo-schema.ts
git diff main...HEAD -- src/repositories/
git diff main...HEAD -- src/app.ts
grep -c "app.close()" tests/integration/todos-api.test.ts
grep -rnE "(PASSWORD|SECRET|DATABASE_URL)[[:space:]]*[:=][[:space:]]*[\"']?[A-Za-z0-9]" <本卡五個檔>
```

```text
 src/app.ts                                       |   2 +
 src/routes/todos.ts                              | 112 +++++
 src/schemas/todo-schema.ts                       | 110 +++++
 src/services/todo-service.ts                     | 121 ++++++
 tasks/T-0016-todo-api.md                         |   4 +-
 tests/integration/todos-api.test.ts              | 508 +++++++++++++++++++++++
 tests/unit/todo-service.test.ts                  | 243 +++++++++++
 worklog/handoff/20260919-0922-T0016-r1-dev-be.md | 212 ++++++++++
 8 files changed, 1310 insertions(+), 2 deletions(-)
（只有 outputs ＋ 任務卡 ＋ 交接檔，無越界）

# setErrorHandler 掃描：唯一命中是 routes/todos.ts:8 的「本檔不呼叫 setErrorHandler」註解，無實際呼叫
# src/repositories/ 差異：空輸出（repository 未被改）
# src/app.ts 差異：
+import registerTodoRoutes from "./routes/todos.js";
   // [ANCHOR:routes]          ← WI-06 於此註冊 BE-04
+  registerTodoRoutes(app, config);
（2 insertions，0 deletions；五個錨點順序未變）

# app.close() 次數：18（＝整合測試條數，無連線洩漏）
# 憑證掃描：tests/integration/todos-api.test.ts:53:const PASSWORD = process.env.BASIC_AUTH_PASSWORD;（變數宣告，非字面值）
```

**§6. 背景服務收尾**

```bash
docker compose down -v
curl --max-time 5 http://localhost:8080/health
```

```text
 Container t-0016-db-1 Removed
 Volume t-0016_todo-db-data Removed
 Network t-0016_default Removed
after_stop_health=000（服務已停，無殘留背景程序）
```

**§7. 合併與收尾（根目錄 main）**

```bash
git merge --no-ff task/T-0016-todo-api -m "T-0016: 合併待辦 API 與業務規則（dev-tl 初審通過）"
git worktree remove ../soft-dev-team-260919-wt/T-0016
git branch -d task/T-0016-todo-api
npm run build && npm run test:unit && npm run test:integration
```

```text
# git merge --no-ff task/T-0016-todo-api
Merge made by the 'ort' strategy.
 src/app.ts                                       |   2 +
 src/routes/todos.ts                              | 112 +++++
 src/schemas/todo-schema.ts                       | 110 +++++
 src/services/todo-service.ts                     | 121 ++++++
 tasks/T-0016-todo-api.md                         |   4 +-
 tests/integration/todos-api.test.ts              | 508 +++++++++++++++++++++++
 tests/unit/todo-service.test.ts                  | 243 +++++++++++
 worklog/handoff/20260919-0922-T0016-r1-dev-be.md | 212 ++++++++++
 8 files changed, 1310 insertions(+), 2 deletions(-)
MERGE_EXIT=0（無衝突）

# git worktree remove / git branch -d
WT_EXIT=0
C:/Users/excal/Projects/Claude/soft-dev-team-260919 c962692 [main]   ← 只剩根目錄，worktree 已移除
Deleted branch task/T-0016-todo-api (was c88f9e8).

# git log --oneline -3（合併後）
c962692 T-0016: 合併待辦 API 與業務規則（dev-tl 初審通過）
35fbc2e E-001: T-0016 進入審核；裁決 UUID 400
c88f9e8 T-0016: 待辦 API 與業務規則（BE-04/BE-05）
```

**§8. 合併後在根目錄 main 重跑（全綠）**

```bash
npm run build
npm run test:unit
docker compose up -d db && node dist/db/migrate.js
npm run test:integration
docker compose down
```

```text
> todo-app@0.1.0 build
> tsc -p tsconfig.json
（無錯誤輸出，exit 0）

# npm run test:unit
ℹ tests 51  ℹ pass 51  ℹ fail 0  ℹ duration_ms 151.5444

# docker compose up -d db
 Container soft-dev-team-260919-db-1 Started
db=healthy (try 1)
無待套用的 migration（已是最新版本）。

# npm run test:integration
✔ DELETE /api/v1/todos/{id}：查無資料回 404，且不得因此建立資料（BR-009） (17.7257ms)
✔ 未帶憑證呼叫 /api/v1/todos 回 401，不回傳任何待辦資料 (8.3159ms)
ℹ tests 47  ℹ pass 47  ℹ fail 0  ℹ duration_ms 937.8283

# docker compose down
 Container soft-dev-team-260919-db-1 Removed
 Network soft-dev-team-260919_default Removed
（docker ps 無輸出，無殘留背景服務）
```

### 合併紀錄

- merge commit：`c962692` — `T-0016: 合併待辦 API 與業務規則（dev-tl 初審通過）`（`--no-ff`，無衝突）
- 任務卡衝突處理：以分支為準，再改 `status: done`、`updated`、補審核紀錄表一列
- worktree：`git worktree remove` 完成；分支 `task/T-0016-todo-api` 已 `git branch -d`
- 收尾 commit：`T-0016 收尾：CHANGELOG`（只 add `CHANGELOG.md`）
- 根目錄重跑 build／test:unit／test:integration 全綠（輸出見 §8）

### 下一步建議

1. **給 T-0017（前端狀態與呈現）**：
   - `PATCH` 是**設定目標狀態**，前端請直接送目標值（`{"isCompleted": true}`／`false`），**不要送取反後的值**；重送同值為冪等，失敗重試可無腦重送。
   - **新增／編輯輸入框請設 `maxlength="200"` 並在送出前先 `trim()`**。原因見「未完成／已知問題」1：原始字串超過 200 字元（即使 trim 後正好 200）會被 schema 層擋成 400，前端先 trim 可完全避開這個邊界。
   - 篩選一律由後端做（`?status=all|active|completed`），前端不要自行過濾後當事實來源（BR-011 要求異動後以目前 filter 重新取清單並整份替換）。
   - 錯誤本文一律為 `{code, message, requestId, details?}`，請以 `code` 分支，不要 parse `message`（schema 層訊息為 Fastify 原文，日後可能隨版本變動）。
2. **給 T-0018（部署卡）**：
   - 連線池建立於 `routes/todos.ts`、隨 app `onClose` 關閉，`src/server.ts` 未被改動。Cloud Run 單實例 `max: 5`（T-0013 決定），若 staging 出現連線壓力，請於 `infra/cloudrun-service.yaml` 調整併發設定而非改程式碼（改程式碼須另開卡）。
   - `deploy-staging.yml` 的 verify 階段可直接沿用本次驗證的兩條：`GET /health` 回 200（不帶憑證）、`GET /api/v1/todos` 帶 secrets 憑證回 200。
3. **給 qa-cr／qa-lead**：
   - 建議把「原始字串 >200、trim 後 ≤200 的標題」列為**已知行為**（回 400）而非缺陷，依據見本檔「未完成／已知問題」1；若測試團隊認為應回 201，須走規格變更請求修改 `04_API規格.yaml` 的 `maxLength` 語意，不可直接改實作。
   - `tests/integration/todos-api.test.ts` 的清單／篩選斷言以「相對順序與 id 是否出現」為準、不假設資料表為空，因此可與其他整合測試共用同一資料庫；若要加更嚴格斷言，需另設測試資料隔離機制（交易 rollback），本卡刻意不做。
4. **給 P1（WI-P1-02）**：屆時 `todo-service.ts` 與 `app.ts` 都會再被改動，可一併評估把連線池上移到 `app.ts` 統一管理（本卡因 outputs 限制而放在 `routes/todos.ts`，見「假設與決策」表態 2）。

### 需要 Leader 裁決的事

- **無**。dev-be 提出的唯一裁決項（UUID 格式錯誤 400 vs 404）已由 Leader 於 2026-09-19T09:37:07+08:00 裁決為「400，規格優先」，本次審核實測與裁決一致，無新增待裁決事項。
