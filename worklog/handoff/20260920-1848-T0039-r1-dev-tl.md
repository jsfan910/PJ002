# T-0039 r1 — dev-tl（審核）— 工作紀錄與交接

## A. 開工紀錄

- 開工時間：2026-09-20T18:48:00+08:00
- 角色／模型：dev-tl / Opus 5 (1M context)
- 任務卡：tasks/T-0039-secret-pin-verify-retry.md（round 1，status: review）
- 本次工作：審核 T-0039（初審 + 合併）
- 讀取的輸入：
  - `tasks/T-0039-secret-pin-verify-retry.md` 全文（acceptance 六條、驗收方式、outputs 五項）
  - `worklog/handoff/20260920-1836-T0039-r1-dev-ops.md` B 段（完成事項、假設與決策 5 條、驗證方式 7 組）
  - `.claude/skills/review-round/SKILL.md`
  - 分支 `task/T-0039-secret-pin-verify-retry`（worktree `...-wt/T-0039`，commit 9984059）全部 diff
- 本輪計畫：
  1. 實跑任務卡「驗收方式」全部指令（diff 範圍、grep `secret:`、`bash -n`、`node -e` YAML）
  2. 逐行審 workflow 重試邏輯與 secret 版本解析；比對 SD 第 6 章安全判準（不得以平台行為當規格）
  3. 檢查倉庫無 secret 值、無 `versions access`
  4. 逐條判 acceptance、對 dev-ops 5 條假設與決策表態
  5. 通過 → 根目錄 `git merge --no-ff` → 更新 CHANGELOG → push main → 等 deploy-staging run 全綠且 STEP_SUMMARY 三版本號 → 清理 worktree 與分支
- 預期產出：任務卡審核紀錄與 status、CHANGELOG.md、main 合併 commit

## B. 交接

- 完工時間／狀態：2026-09-20T19:05:00+08:00 / 審核完成，判定 **rework**（round 1 → 2）
- 完成事項：
  - 實跑任務卡「驗收方式」全部指令（不採信 dev-ops 交接檔的結論，全部自行重跑）
  - 逐行審 `deploy-staging.yml` 的 `resolve secret versions` 與 `gcloud run deploy ＋ verify（含一次自動重試）` 兩個步驟
  - 合併 `task/T-0039-secret-pin-verify-retry` → main（`git merge --no-ff`，merge commit `05c9a1e`）、更新 `CHANGELOG.md`、推送 main（`6b27611`）
  - 追蹤實際觸發的 `deploy-staging` run → **紅燈**，查出根因並定出 r2 具體修法
- 產出物（路徑）：
  - `C:\Users\excal\Projects\Claude\soft-dev-team-260919\CHANGELOG.md`（新增 T-0039 一列）
  - `C:\Users\excal\Projects\Claude\soft-dev-team-260919\tasks\T-0039-secret-pin-verify-retry.md`（`status: rework`、`round: 2`、背景與限制 r2 追加段、審核紀錄 r1 列）
  - main 上的 `05c9a1e`（合併）、`6b27611`（CHANGELOG）
- 未完成／已知問題：
  - **main 的 `deploy-staging` 目前是紅的**（run `35506278351`）。staging 服務本身仍在跑合併前已部署的 revision，`/health`、`/api/v1/todos` 不受影響，**QA 卡（T-0041 等）可照常對 staging 操作**；受影響的只有「新的部署無法推上 staging」——這會擋住 T-0038（D-017 前端修正）合併後的 staging 生效。
  - IAM 授權（`roles/secretmanager.viewer`）屬使用者的雲端安全設定，**我依安全規則未代為執行**，寫進「需要 Leader 裁決的事」。
  - worktree `...-wt\T-0039` 與分支 `task/T-0039-secret-pin-verify-retry` **刻意保留未清理**，供 r2 續作。
- 我做的假設與決策（含理由）：
  1. **不 revert 合併**。理由：① staging 執行期沒有壞，壞的只有「下一次部署」；② `git revert -m 1` 之後同一條分支再合併回來不會帶回被 revert 的內容（merge base 已變），會給 r2 製造陷阱；③ r2 的修法已經定到逐行，修完再合併一次即可。若 Leader 認為「main 的 CI 徽章必須立刻恢復綠」優先於上述，再由 Leader 裁決 revert（列為裁決事項）。
  2. **r2 不升級模型（維持 sonnet）**。依 review-round SKILL 第 6 點判準：r1 的失敗不是工具操作踩坑，是「把本機擁有者身分的成功當成 CI 服務帳號會成功」的驗證設計缺口；r2 的修法已在審核紀錄逐條寫死，換模型不是成功的關鍵。
  3. **不自行改 workflow 修好它**。本卡 `outputs` 是 dev-ops 的，dev-tl 的角色是初審與合併；自己動手改會讓「誰為這段程式負責」失焦，也違反「不改本卡 outputs 以外的檔案」。
- 對 dev-ops 交接檔「假設與決策」5 條的逐條表態：
  1. **合併四個 step 為一個 bash 步驟** —— **接受**。GitHub Actions 的 step 之間確實無法表達「回到上一步重跑」，封裝成函式是唯一乾淨解；行為（輪詢 `/health`、帶憑證驗 `/api/v1/todos`、`STAGING_BASE_URL` 比對警告）逐項核對確實保留。副作用（run 頁面少了三個獨立 step 名稱、不易一眼看出卡在哪一段）可接受，因為步驟內有明確的 `echo "== … =="` 分段。
  2. **健康檢查失敗不觸發自動重試** —— **接受**。06 §6.7 建議二的情境確實限定「verify（帶憑證）」；`/health` 輪詢 5 分鐘都不過屬部署本身有問題，自動重試會掩蓋真缺陷。
  3. **repository variables 命名** —— **接受**。與 `GCP_PROJECT_ID` 等既有命名風格一致，且 README 明寫「只填版本號、不填值」。
  4. **§7 參數表與變更紀錄表本卡不動** —— **接受**。任務卡 outputs 明文限制，且已在 §6.9 留下待同步清單指向 T-0040，不構成無聲飄移。
  5. **本機 `gcloud secrets versions list` 唯讀驗證屬規則許可** —— **接受其「不違反安全規則」的部分**（只列版本號與狀態、未執行 `versions access`），但**不接受其作為「CI 會成功」的證據**：本機是專案擁有者身分，CI 是 WIF 服務帳號 `github-deployer@`，兩者權限集合不同，實測已證明 CI 端會 PERMISSION 失敗。這正是本輪退回的根因。
- 逐條判定 `acceptance`：

  | # | 條件（摘要） | 判定 | 證據 |
  |---|---|---|---|
  | 1 | `versions list` 解析版本、`--set-secrets` 改 `NAME=secret:N`、版本號寫 STEP_SUMMARY、可用 repository variable 覆寫且 README 只寫名稱用途 | **通過（靜態）** | workflow L81–125 `resolve secret versions` 步驟；L181 `--set-secrets "DATABASE_URL=database-url:${DB_VERSION},…"`；L117–125 寫入 `$GITHUB_STEP_SUMMARY` 表格；L84–86 `vars.SECRET_VERSION_*` 覆寫；README Variables 表三列只寫「只填版本號（例如 `2`），不得填 secret 值」 |
  | 2 | verify（帶憑證）失敗自動重新 deploy 並再驗一次、上限 1 次、STEP_SUMMARY 註明「已重試」 | **通過（靜態）** | workflow L211–230 `if verify_auth … else RETRIED=true; deploy_revision; verify_health; if ! verify_auth → ::error:: exit 1`，無迴圈故上限確為 1；L232–254「回填提示」依 `retried`／`retry_result` 輸出「已重試」兩種措辭 |
  | 3 | 三處參數一致、yaml 用 `<SECRET_VERSION>` 佔位符、README 同步表更新 | **通過** | `grep -n "set-secrets\|secretKeyRef\|key:"` 三檔輸出見下方指令區：workflow L181 與 `scripts/deploy-staging.sh` L103 字串逐字相同；`infra/cloudrun-service.yaml` L83/88/93 皆 `key: "<SECRET_VERSION>"`；README 新增「釘定 Secret Manager 版本與 verify 自動重試」一節 |
  | 4 | `bash -n` 通過、workflow 為合法 YAML 並貼輸出 | **通過** | `bash -n scripts/deploy-staging.sh` → `SYNTAX_OK`；`node -e` → `255 lines`；`npx --yes yaml-lint` → `√ YAML Lint successful.`；另自行下載 actionlint v1.7.12 → `ACTIONLINT_EXIT=0` |
  | 5 | 倉庫無 secret 值、不執行 `versions access`、交接檔只貼版本號與狀態 | **通過** | `grep -rnE "postgres://|postgresql://|BASIC_AUTH_PASSWORD=[^$\"]|versions access"` 於五個 outputs 檔零真實命中（README 命中的是本機 compose 用的 `postgres://dev:dev@…` 與 T-0027 既有的量測說明段，非本卡新增、非 staging 憑證）；`${{ secrets.* }}` 只出現在 L135–137、L159–160 的 `env:` 區塊，`run:` 內零命中（T-0025 CR S-3 規則未破）；dev-ops 交接檔貼的是 `NAME/STATE/CREATED` 三欄 |
  | 6 | **合併推送後 `deploy-staging` run 全綠且 STEP_SUMMARY 顯示三個版本號** | **不通過** | run `35506278351` `conclusion: failure`；第 8 步 `resolve secret versions` `conclusion: failure`，其後五步 `skipped`；STEP_SUMMARY 無任何版本號。根因見下方「根因實查」 |

- 驗證方式（接手者如何確認我做的是對的：指令 ＋ 我實際得到的輸出）：

  ```bash
  # 1) diff 範圍
  cd C:/Users/excal/Projects/Claude/soft-dev-team-260919-wt/T-0039
  git diff --stat main...task/T-0039-secret-pin-verify-retry
  ```
  ```text
   .github/workflows/deploy-staging.yml              | 185 ++++++++++++++++------
   README.md                                         |  19 ++-
   docs/specs/06_部署架構與CICD.md                   |  81 ++++++++++
   infra/cloudrun-service.yaml                       |  21 ++-
   scripts/deploy-staging.sh                         |  40 ++++-
   tasks/T-0039-secret-pin-verify-retry.md           |   4 +-
   worklog/handoff/20260920-1836-T0039-r1-dev-ops.md | 150 ++++++++++++++++++
   7 files changed, 443 insertions(+), 57 deletions(-)
  ```
  （五個 outputs ＋ 任務卡 ＋ 自己的交接檔，範圍正確；`src/**` 零改動，經 `git diff --name-only main...HEAD -- src/` 回空確認，故 SD §6.2 的 `/health` 唯一豁免比對邏輯不受本卡影響，本輪不需重測豁免路徑變體）

  ```bash
  # 2) workflow 內不得再出現 :latest 形式的 secret 參照
  grep -n "secret:" .github/workflows/deploy-staging.yml        # 零輸出
  grep -n "set-secrets\|secretKeyRef\|key:" .github/workflows/deploy-staging.yml scripts/deploy-staging.sh infra/cloudrun-service.yaml
  ```
  ```text
  .github/workflows/deploy-staging.yml:181:              --set-secrets "DATABASE_URL=database-url:${DB_VERSION},BASIC_AUTH_USER=basic-auth-user:${USER_VERSION},BASIC_AUTH_PASSWORD=basic-auth-pass:${PASS_VERSION}"
  scripts/deploy-staging.sh:103:  --set-secrets "DATABASE_URL=database-url:${DB_VERSION},BASIC_AUTH_USER=basic-auth-user:${USER_VERSION},BASIC_AUTH_PASSWORD=basic-auth-pass:${PASS_VERSION}"
  infra/cloudrun-service.yaml:83:                  key: "<SECRET_VERSION>"
  infra/cloudrun-service.yaml:88:                  key: "<SECRET_VERSION>"
  infra/cloudrun-service.yaml:93:                  key: "<SECRET_VERSION>"
  ```

  ```bash
  # 3) 語法層
  bash -n scripts/deploy-staging.sh && echo SYNTAX_OK
  node -e "const f=require(\"fs\").readFileSync(\".github/workflows/deploy-staging.yml\",\"utf8\");console.log(f.split(\"\\n\").length,\"lines\")"
  npx --yes yaml-lint .github/workflows/deploy-staging.yml infra/cloudrun-service.yaml
  # actionlint：我自行下載官方 release 二進位再跑一次（不採信 dev-ops 的 EXIT=0）
  bash dl.bash   # scripts/download-actionlint.bash，裝出 actionlint.exe v1.7.12
  actionlint.exe -color .github/workflows/deploy-staging.yml .github/workflows/ci.yml .github/workflows/monitor-health.yml ; echo "ACTIONLINT_EXIT=$?"
  ```
  ```text
  SYNTAX_OK
  255 lines
  √ YAML Lint successful.
  Done: 1.7.12 / installed by downloading from release page / built with go1.26.1 compiler for windows/amd64
  ACTIONLINT_EXIT=0
  ```

  ```bash
  # 4) 合併 ＋ 推送
  cd C:/Users/excal/Projects/Claude/soft-dev-team-260919
  git merge --no-ff task/T-0039-secret-pin-verify-retry -m "T-0039: 合併 — Secret Manager 釘具體版本、verify 失敗自動重試一次"
  git push origin main
  ```
  ```text
  Merge made by the "ort" strategy.  7 files changed, 443 insertions(+), 57 deletions(-)
  05c9a1e T-0039: 合併 — Secret Manager 釘具體版本、verify 失敗自動重試一次
  e948d54..6b27611  main -> main
  ```

  ```bash
  # 5) 追 deploy-staging run（gh CLI 本機未安裝，改用 GitHub public REST API）
  curl -sS "https://api.github.com/repos/jsfan910/PJ002/actions/workflows/deploy-staging.yml/runs?per_page=1"
  curl -sS "https://api.github.com/repos/jsfan910/PJ002/actions/runs/35506278351/jobs"
  ```
  ```text
  id=35506278351 | "head_sha": "6b27611519ac9e16fb134f32aaa443fbb6ffbff0" | run_number 17
  job "deploy-staging": "status": "completed", "conclusion": "failure"
    1 Set up job                                           success
    2 checkout                                             success
    3 setup-node                                           success
    4 npm ci                                               success
    5 npm run build                                        success
    6 auth（Workload Identity Federation）                 success
    7 setup-gcloud                                         success
    8 resolve secret versions（釘具體版本，不用 :latest）   failure   ← 本卡新增的步驟
    9 gcloud auth configure-docker                         skipped
   10 migrate（NEON_DATABASE_URL，forward-only）           skipped
   （其後 docker build & push／gcloud run deploy ＋ verify／回填提示 亦全部 skipped）
  ```

  ```bash
  # 6) 取失敗訊息（job logs 需 admin 權限：403；改用公開的 check-runs annotations）
  curl -sSL "https://api.github.com/repos/jsfan910/PJ002/actions/jobs/106066526502/logs"
  curl -sS "https://api.github.com/repos/jsfan910/PJ002/check-runs/106066526502/annotations"
  ```
  ```text
  {"message": "Must have admin rights to Repository.", "status": "403"}

  [failure] "Process completed with exit code 1."   (start_line 69)
  [warning] Node.js 20 is deprecated …（既有，與本卡無關）
  [notice]  ubuntu-latest 將於 2026-10-19 遷移至 Ubuntu 26（既有，與本卡無關）
  ```
  **關鍵判讀**：annotations 裡**沒有** workflow L109 那句自訂的 `::error::無法解析 secret 版本（…）` —— 代表流程根本沒走到那個空值檢查，是 `gcloud secrets versions list` 這條指令自己回非 0，被 `set -euo pipefail` 當場中止。
  （另：以瀏覽器開 `https://github.com/jsfan910/PJ002/actions/runs/35506278351/job/106066526502` 只得到 "Sign in to view logs"；我依安全規則**不輸入任何帳密登入**，改以上述公開 API 取證。）

  ```bash
  # 7) 根因實查：部署服務帳號到底有沒有列版本的權限
  gcloud projects get-iam-policy pj002-509106 --flatten="bindings[].members" \
    --format="table(bindings.role)" \
    --filter="bindings.members:github-deployer@pj002-509106.iam.gserviceaccount.com"
  ```
  ```text
  ROLE
  roles/artifactregistry.writer
  roles/iam.serviceAccountUser
  roles/run.admin
  roles/secretmanager.secretAccessor
  ```
  ```bash
  gcloud iam roles describe roles/secretmanager.secretAccessor --format="value(includedPermissions)"
  gcloud iam roles describe roles/secretmanager.viewer         --format="value(includedPermissions)"
  ```
  ```text
  resourcemanager.projects.get;resourcemanager.projects.list;secretmanager.versions.access

  resourcemanager.projects.get;resourcemanager.projects.list;secretmanager.locations.get;secretmanager.locations.list;secretmanager.secrets.get;secretmanager.secrets.getIamPolicy;secretmanager.secrets.list;secretmanager.secrets.listEffectiveTags;secretmanager.secrets.listTagBindings;secretmanager.versions.get;secretmanager.versions.list
  ```
  **根因結論**：`gcloud secrets versions list` 需要 `secretmanager.versions.list`，該權限**只在 `roles/secretmanager.viewer`**；部署服務帳號只有 `secretAccessor`（其權限集合僅 `secretmanager.versions.access`）。README L280／L294 建立服務帳號時授的四個角色也正是上面查到的四個，**T-0039 新增了一個需要第五種權限的步驟，卻沒有同步補這個角色**。r1 本機實測會過，是因為本機 gcloud 用的是專案擁有者帳號 `excalibur.star@gmail.com`，與 CI 的 WIF 服務帳號是不同身分。

  ```bash
  # 8) 佐證：secret 本身沒有 resource 層級的額外授權可補足（都是空的）
  for s in database-url basic-auth-user basic-auth-pass; do gcloud secrets get-iam-policy "$s" --project pj002-509106 --format="table(bindings.role,bindings.members)"; done
  ```
  ```text
  ROLE  MEMBERS      （三個 secret 皆為空，全部靠 project 層級綁定）
  ```

- 下一步建議：
  1. **Leader 轉請使用者執行一次 IAM 授權**（agent 依安全規則不代為修改雲端權限）：
     ```bash
     gcloud projects add-iam-policy-binding pj002-509106 \
       --member="serviceAccount:github-deployer@pj002-509106.iam.gserviceaccount.com" \
       --role="roles/secretmanager.viewer"
     ```
     這一步做完，r1 的程式碼其實就會過；但 r2 仍須補 README 與錯誤訊息（否則下一個人照 README 開新專案會再踩一次）。
  2. **替代路徑（不改 IAM）**：請使用者在 GitHub repository variables 直接填 `SECRET_VERSION_DATABASE_URL=1`、`SECRET_VERSION_BASIC_AUTH_USER=3`、`SECRET_VERSION_BASIC_AUTH_PASS=1`（值只是版本號，非機密；數字取自 2026-09-20 的實查）。填了之後 `resolve_version()` 會走 override 分支、完全不呼叫 `versions list`，deploy 立刻恢復綠。缺點是之後輪換帳密要手動改這個變數，失去「自動釘最新 ENABLED 版本」的好處。
  3. **T-0038 的合併順序**：T-0038（D-017 前端修正）合併後同樣需要 `deploy-staging` 綠才會進到 staging 給 QA 驗。建議 T-0039 r2 先落地、deploy 恢復綠，再合併 T-0038；否則 T-0038 的程式碼會停在 main 上不上 staging。
  4. T-0040（plan-sd）除了原本的 §7 參數表與變更紀錄表，請一併把「部署服務帳號需 `roles/secretmanager.viewer`」寫進 06 §2 的角色清單（目前 06 L157 只提到 `secretmanager.secretAccessor`）。
  5. 非阻擋級的小瑕疵（r2 可順手、不強制）：workflow L210–230 在「重試分支的 `verify_health` 失敗」這條路徑上，`retried=true` 還沒寫進 `$GITHUB_OUTPUT` 就被 `set -e` 中止，STEP_SUMMARY 會少掉「已重試」字樣；把 `echo "retried=true" >> "$GITHUB_OUTPUT"` 提前到 `RETRIED=true` 那一行旁邊即可。另 workflow 檔頭 L21–23 仍寫「本 repo 目前無遠端」，是 T-0018 留下的過期註解（實際 remote 為 `https://github.com/jsfan910/PJ002.git`），可順手更正。
  6. **另一個與本卡無關、但同一個 sha 上出現的紅燈（請 Leader 另開卡）**：`QA Tests (T-0021)` workflow 在 `6b27611` 上 **run #17 failure**（job `integration (tests/integration/qa)` 的「執行 tests/integration/qa（依序，避免共用資料庫互相干擾）」步驟 exit code 1，`e2e`／`perf` 兩個 job 因而 skipped），而前一個 sha `e948d54` 的 run #16 是 success。**已證明與 T-0039 無因果關係**：
     ```bash
     git diff --name-only e948d54 6b27611 -- src/ tests/ package.json package-lock.json migrations/
     ```
     ```text
     （空輸出——兩個 sha 之間程式碼、測試、相依套件、migration 零改動）
     ```
     兩次 run 跑的是**完全相同的程式碼與測試**，一綠一紅，因此是 flaky 或 runner 環境因素，不是本卡造成的回歸。job logs 需 repo admin 權限（公開 API 403），annotations 只有 `Process completed with exit code 1.`，**我沒有可讀的失敗細節**，無法在此判定是哪一條測試。建議 Leader 開一張 qa 卡由有權限的角色或使用者下載該 run 的 log 判讀，或在本機以 docker（本機 daemon 可用，`docker info` 回 `29.4.2`）重跑 `tests/integration/qa` 觀察是否可重現。
- 需要 Leader 裁決的事：
  1. **IAM 授權由誰執行**：`roles/secretmanager.viewer` 屬使用者 GCP 專案的安全設定，我依安全規則不代為修改。需 Leader 向使用者取得授權並由使用者執行（或明確指示改走「下一步建議 2」的 repository variables 路徑）。**在此之前 r2 就算改完程式碼，`deploy-staging` 仍然會紅。**
  2. **要不要 revert 合併**：我的判斷是不 revert（理由見「假設與決策 1」），但若 Leader 認為「main 必須隨時保持全綠」是硬性要求，revert 的決定權在 Leader。

