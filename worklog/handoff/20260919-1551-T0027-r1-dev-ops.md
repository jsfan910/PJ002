# T-0027 r1 — dev-ops — 工作紀錄與交接

<!-- 檔名：worklog/handoff/20260919-1551-T0027-r1-dev-ops.md（時間 = 開工時間，取實查系統時間） -->
<!-- 規則：開工立即寫 A 段；完工或中止時寫 B 段。同一輪同一角色一個檔。 -->

## A. 開工紀錄

- 開工時間：2026-09-19T15:51:00+08:00
- 角色／模型：dev-ops / Claude Sonnet 5
- 任務卡：tasks/T-0027-staging-deploy.md（round 1）
- 讀取的輸入（實際讀過的檔案與段落，非任務卡照抄）：
  - `tasks/T-0027-staging-deploy.md` 全文 —— outputs 四項、acceptance 五條、背景與限制
  - `worklog/handoff/20260919-1133-T0018-r1-dev-tl.md` 全文 —— T-0018 已把離線可驗部分做到最後一步，遠端待驗清單 5 項全部落在本卡；WIF attribute condition 是唯一「設錯即重大安全漏洞」的使用者步驟，需在報告中再次強調
  - `README.md`「部署與 secrets（T-0018）」整章 —— GCP 一次性設定六步、GitHub variables/secrets 兩表、首次部署後回填三處的位置與方法
  - `docs/specs/06_部署架構與CICD.md` 第 1 章（環境清單，staging 網址待回填）、第 3 章（三個 workflow 的階段表，含 3.2.1 WIF 六步與 attribute condition 警告）、第 5 章（回滾程序，5.4 演練要求：尚未實際演練，被憑證阻擋）、第 6 章（監控，6.4 monitor-health 實作紀錄）
  - `.github/workflows/deploy-staging.yml` 全文 —— 階段順序 auth→migrate→build&push→deploy→verify；verify 讀 `steps.service_url.outputs.url`（不是 STAGING_BASE_URL），因此 STAGING_BASE_URL 留空不會擋 verify；已有「回填提示」步驟印出網址到 GITHUB_STEP_SUMMARY
  - `.github/workflows/monitor-health.yml` 全文 —— cron 每 5 分鐘＋workflow_dispatch；STAGING_BASE_URL 為空時印 `::notice::` 並以 0/0 略過，不會誤判
  - `scripts/deploy-staging.sh`、`scripts/rollback-staging.sh` 全文 —— 本機等效指令，回滾腳本不帶參數列 revision、帶參數執行 update-traffic
  - `docs/specs/04_API規格.yaml` servers 區塊（第 44～71 行）—— staging 為佔位符 `https://todo-app-staging-{hash}-{region}.run.app`，待回填
- 上一輪交接摘要：不適用（round 1）
- 本輪計畫（3–7 條）：
  1. 瀏覽器開 Actions 頁確認 deploy-staging 最新 run 綠燈或紅燈，取得 run 網址與各階段狀態
  2. 綠燈：從 log／summary 取得 `*.run.app` 網址；紅燈：讀失敗 job log 判斷是 workflow 問題還是 GCP/GitHub 設定問題
  3. 網址到手後 curl 三條（/health 無憑證、/ 無憑證、/api/v1/todos 帶憑證，憑證看環境變數是否存在）
  4. 回填三處（06 第 1 章、04 servers；STAGING_BASE_URL 由使用者網頁設定，本 agent 只能列出要填的值）
  5. 回滾演練：檢查本機 gcloud 登入狀態，若已登入則實際跑 rollback-staging.sh；否則列指令請使用者執行
  6. 確認 monitor-health.yml 排程與至少 1 次成功採樣（手動 workflow_dispatch 需登入 GitHub，若不行則說明卡在哪）
  7. README 補「GCP 指令在哪裡執行」段；寫部署紀錄報告；收尾交接
- 預期產出：`docs/specs/06_部署架構與CICD.md`（三處）、`docs/specs/04_API規格.yaml`（servers）、`README.md`（補一段）、`docs/reports/20260919-*-部署紀錄-E001.md`、本交接檔 B 段、任務卡 status 更新

## B. 交接

- 完工時間／狀態：2026-09-19T16:05:00+08:00 / **blocked**
- 完成事項：
  - 用瀏覽器開 https://github.com/jsfan910/PJ002/actions 確認：`CI #2`、`QA Tests (T-0021) #2` 綠燈；`Deploy Staging` 真實觸發兩次（`#1` https://github.com/jsfan910/PJ002/actions/runs/35430432261、`#2` https://github.com/jsfan910/PJ002/actions/runs/35430463010），皆在 `auth`（WIF）階段失敗，錯誤訊息一致：`google-github-actions/auth failed with: ... "The given credential is rejected by the attribute condition."`
  - 用本機已登入的 `gcloud`（帳號 `excalibur.star@gmail.com`，未代登入，只查唯讀資訊）逐項排查：WIF provider `attribute-condition` 仍是字面佔位符 `assertion.repository == '<owner>/<repo>'`（**根因**）；SA `github-deployer` 的 `workloadIdentityUser` 綁定已正確指向 `jsfan910/PJ002`；專案層四個角色（`run.admin`／`artifactregistry.writer`／`iam.serviceAccountUser`／`secretmanager.secretAccessor`）皆已授予；Artifact Registry `todo-app` 存在（0MB，尚無映像）；Secret Manager 三個 secret 皆存在且有 `enabled` 版本；`gcloud run services list` 回傳空清單（從未有 revision 部署成功，回滾演練前提不成立）
  - 查 `monitor-health.yml`：0 workflow runs（GitHub 排程觸發延遲或需等待，非程式問題；agent 無 GitHub 登入無法手動 `workflow_dispatch`）
  - `README.md` 新增「GCP 指令在哪裡執行（T-0027 補充）」一節：Cloud Shell／本機 SDK 差異、Windows `gcloud.cmd`、佔位符替換（以本次真實踩到的 attribute-condition 案例佐證）、`cmd.exe` 的 `echo` 不需單引號、`<(echo …)` 改寫暫存檔、secret 值用 `[IO.File]::WriteAllText` 並以 `Format-Hex` 驗證無多餘換行
  - `docs/specs/06_部署架構與CICD.md`：第 1 章 staging 列更新為記錄本次根因與待辦（未填真實網址，因為沒有）；第 5.4 節回滾演練紀錄更新為記錄唯讀查詢結果與前提不成立的原因；新增 6.5「首次真實 run 紀錄」記錄兩次失敗 run 與 monitor-health 尚無 run 的狀態；`updated` 時間戳同步更新
  - `docs/reports/20260919-1551-部署紀錄-E001.md`：完整時間軸、各階段結果表、問題與解法、唯讀查詢佐證、acceptance 逐條對照、使用者後續要做的事（含確切修正指令，Git Bash 與 PowerShell 兩種寫法）
  - 任務卡 `status: blocked`，`blocked_reason` 填入根因摘要與參照文件路徑，`updated` 更新
- 產出物（路徑）：
  - `README.md`（worktree 內，新增段落見上）
  - `docs/specs/06_部署架構與CICD.md`（三處變更：第 1 章 staging 列、5.4、新增 6.5）
  - `docs/reports/20260919-1551-部署紀錄-E001.md`
  - `tasks/T-0027-staging-deploy.md`（`status: blocked`、`blocked_reason`、`updated`）
  - 本交接檔
  - **`docs/specs/04_API規格.yaml` 未變更**：沒有真實 staging 網址可回填，維持原佔位符，理由見下「我做的假設與決策」
- 未完成／已知問題：
  1. **staging 尚未部署成功**：所有下游驗收（curl 三條、三處回填、回滾演練、monitor-health 成功採樣）都因此無法完成，全部卡在同一個根因（見「需要 Leader 裁決的事」與報告）。
  2. **`monitor-health.yml` 尚無任何 run**：cron 已在 main 上生效（`*/5 * * * *`），但截至收尾時尚未被 GitHub 觸發過一次；agent 無 GitHub 帳密無法手動 `workflow_dispatch`。
  3. **`docs/specs/04_API規格.yaml` 的 servers staging 值仍是佔位符**：沒有真實網址可填，強行填入會製造假資料，故本卡未改動此檔（任務卡 outputs 雖列此檔，但只有在拿到真實網址後才有內容可填）。
- 我做的假設與決策（含理由）：
  1. **判定為「GCP 設定問題」而非「workflow 問題」，不修改 `deploy-staging.yml` 或任何 IAM 設定**。理由：`google-github-actions/auth` 的錯誤訊息明確指向 attribute-condition 拒絕，且用唯讀查詢直接讀到 provider 上的字面值仍是 `<owner>/<repo>`——這是 GCP 資源設定的資料錯誤，不是 workflow YAML 或程式邏輯錯誤；任務卡與角色協定都要求此類問題「列出使用者要做的事，不索取憑證」，且修改 WIF 的 attribute-condition 屬於安全性設定變更，agent 依規定不得代為執行。
  2. **用本機已登入的 `gcloud` 做唯讀查詢定位根因，但不執行任何會改變狀態的指令**（沒有跑 `update-oidc`、沒有跑 `deploy`、沒有跑 `secrets versions add`）。理由：使用者先前已完成 `gcloud auth login`（非本次代登入），任務卡與角色協定允許「若本機 gcloud 已登入」使用既有登入做屬於本卡驗收範圍的事（回滾演練），但修正 IAM 安全性設定是規則明文禁止的類別，即使有登入也不得做；因此只做 `describe`／`list`／`get-iam-policy` 這類唯讀動作，把修正指令留給使用者。
  3. **未嘗試用本機 `scripts/deploy-staging.sh` 手動部署一次來繞過 GitHub Actions 的失敗**。理由：T-0027 的驗收目的是證明「main 綠燈後 CI/CD 自動部署到 staging」這條路徑本身能跑通，若改用本機手動部署繞過，會掩蓋 Actions 端仍然失效的事實，且不符合任務卡「找出原因並提出修正」的意圖（應該是修正阻擋原因讓 Actions 自己跑通，而非另闢路徑）。
  4. **`04_API規格.yaml` 完全未動**，即使任務卡 outputs 列了這個檔案。理由：該檔案只能填「回填 staging 佔位符」這一件事，沒有真實網址就沒有內容可填；填入假網址或維持現狀但假裝「已檢查」都不誠實，因此選擇不動此檔並在報告與交接檔明確說明原因，留給下一輪（使用者修正 WIF 後）處理。
  5. **任務卡狀態標為 `blocked` 而非 `review`**。理由：acceptance 五條中僅第一條「找出紅燈原因並提出修正」完成，其餘四條（三條 curl、三處回填、回滾演練、monitor-health 採樣）皆因同一根因無法執行，符合角色協定「無法實際部署時...status: blocked + blocked_reason」的定義。
- 驗證方式（指令 + 實際輸出）：

  **1. Actions 頁面確認兩次 Deploy Staging 失敗（瀏覽器讀頁）**
  ```text
  Deploy Staging #2 (35430463010): Status Failure, 18s
  google-github-actions/auth failed with: failed to generate Google Cloud federated token
  for //iam.googleapis.com/***: {"error":"unauthorized_client","error_description":
  "The given credential is rejected by the attribute condition."}

  Deploy Staging #1 (35430432261): Status Failure, 21s
  （同一錯誤訊息）
  ```

  **2. 唯讀查詢 WIF provider attribute-condition（根因）**
  ```powershell
  & "$env:LOCALAPPDATA\Google\Cloud SDK\google-cloud-sdk\bin\gcloud.cmd" `
    iam workload-identity-pools providers describe github-provider `
    --project="pj002-509106" --location="global" --workload-identity-pool="github-pool" `
    --format="yaml(attributeCondition,attributeMapping,oidc)"
  ```
  ```text
  attributeCondition: assertion.repository == '<owner>/<repo>'
  attributeMapping:
    attribute.repository: assertion.repository
    google.subject: assertion.sub
  oidc:
    issuerUri: https://token.actions.githubusercontent.com
  ```

  **3. 排除其他可能原因（SA 綁定、角色、Artifact Registry、Secret Manager、Cloud Run 現況）**
  ```text
  gcloud iam service-accounts get-iam-policy github-deployer@pj002-509106.iam.gserviceaccount.com ...
  → roles/iam.workloadIdentityUser 綁定 principalSet .../attribute.repository/jsfan910/PJ002（正確）

  gcloud projects get-iam-policy pj002-509106 --filter="bindings.members:github-deployer@..." --format="table(bindings.role)"
  → roles/artifactregistry.writer / roles/iam.serviceAccountUser / roles/run.admin / roles/secretmanager.secretAccessor（四個皆已授予）

  gcloud artifacts repositories describe todo-app --project=pj002-509106 --location=asia-east1
  → projects/pj002-509106/locations/asia-east1/repositories/todo-app  DOCKER，Repository Size 0.000MB

  gcloud secrets list --project=pj002-509106
  → basic-auth-pass / basic-auth-user / database-url（三個皆存在）

  gcloud secrets versions list <name> --project=pj002-509106（三次，逐一）
  → database-url: 1 enabled / basic-auth-user: 2 enabled,1 disabled / basic-auth-pass: 1 enabled

  gcloud run services list --project=pj002-509106 --region=asia-east1
  → （空輸出，尚無任何 Cloud Run 服務）
  ```

  **4. monitor-health.yml 尚無 run**
  ```text
  https://github.com/jsfan910/PJ002/actions/workflows/monitor-health.yml
  → "This workflow has no runs yet."（收尾前重新查詢一次，結果相同）
  ```

  **5. staging 三條 curl 驗證**：**無法執行**，因為 `STAGING_BASE_URL` 不存在（沒有任何 Cloud Run 服務曾部署成功）。
- 下一步建議：
  1. **使用者先執行修正指令**（見報告「使用者後續要做的事」第 1 點），並用附帶的唯讀 `describe` 指令自行確認 `attributeCondition` 已變成 `assertion.repository == 'jsfan910/PJ002'`。
  2. 到 https://github.com/jsfan910/PJ002/actions/workflows/deploy-staging.yml 登入後 **Run workflow** 或對失敗 run 按 **Re-run all jobs**，重新觸發部署。
  3. 部署成功後，下一輪 dev-ops（或審核者）：從 run 的 `GITHUB_STEP_SUMMARY` 取網址 → 回填 GitHub variable `STAGING_BASE_URL`、`06 §1`、`04 servers` → 跑三條 curl → 執行回滾演練並記錄於 `06 §5.4`。
  4. `monitor-health.yml`：等 cron 自然觸發，或使用者登入 GitHub 後手動 `workflow_dispatch` 跑一次，確認至少 1 次成功採樣（`STAGING_BASE_URL` 填好之後才會是「成功」採樣，不是 0/0）。
  5. 建議使用者把 `C:\Users\excal\AppData\Local\Google\Cloud SDK\google-cloud-sdk\bin` 加入 PATH，之後可直接打 `gcloud`／`gcloud.cmd`，不需每次打完整路徑。
- 需要 Leader 裁決的事：
  1. **本卡因使用者一次性 GCP 設定的字元級錯誤（WIF attribute-condition 佔位符未替換）而整體 blocked，非 workflow 或程式碼問題。** 修正指令已備妥（Git Bash 與 PowerShell 兩種寫法，見報告與 README），但涉及安全性設定變更，agent 依規定不得代為執行，須由使用者本人執行後才能繼續。請 Leader 提示使用者盡快處理，以免阻塞 Gate 2（staging 可用瀏覽器操作、P0 UAT 全通過）與 NFR-003 24 小時採樣的時程。
  2. **`monitor-health.yml` 至今 0 run，晚於預期**：cron 已正確設定且在 main 上生效，但 GitHub 尚未觸發首次排程（可能延遲，也可能與 repo 剛建立有關）。若使用者修正 WIF 問題後仍長時間沒有任何 run，建議由使用者手動 `workflow_dispatch` 一次確認排程器本身正常，不需開新卡。
