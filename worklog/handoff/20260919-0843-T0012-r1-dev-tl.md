# T-0012 r1 — dev-tl — 審核紀錄與交接

## A. 開工紀錄

- 開工時間：2026-09-19T08:43:06+08:00
- 角色／模型：dev-tl / opus
- 任務卡：tasks/T-0012-error-handler.md（round 1）— 審核 T-0012（批次 2 第一張）
- 讀取的輸入（實際讀過的檔案與段落）：
  - tasks/T-0012-error-handler.md — outputs 五項、acceptance 四條、驗收方式
  - docs/specs/10_開發計畫_WBS.md §0（硬前提 1~8）、§1.2（WI-02 背景與限制＋驗收方式草案六條）、§5（共用檔案規約與 src/app.ts 錨點骨架）
  - docs/specs/03_系統設計書_SD.md 第 6 章（6.1 錯誤碼表、6.4 敏感資料、6.5 日誌等級）
  - docs/specs/04_API規格.yaml components.schemas.Error（第 696~736 行）與 responses.InternalServerError
  - tasks/E-001-todo-app.md 最後五段 Leader 裁決（特別是 2026-09-19T08:41:59 對 T-0012 兩項）
  - worklog/handoff/20260919-0822-T0012-r1-dev-be.md 全文（B 段：完成事項、假設與決策 5 條、驗證方式、需 Leader 裁決 2 條）
  - worktree 內 src/plugins/error-handler.ts、src/schemas/error-schema.ts 全文
- 上一輪交接摘要：無（round 1）
- 本輪計畫：
  1. 在 worktree C:/Users/excal/Projects/Claude/soft-dev-team-260919-wt/T-0012 重跑 WBS §1.2 六條驗收指令
  2. 重跑 npm run lint／build／test:unit／test:integration
  3. git diff main...HEAD --stat 檢查範圍
  4. 逐條比對 04_API規格.yaml Error 契約與 SD §6.1
  5. 對交接檔五條「假設與決策」逐條表態
  6. 判定 done／rework，通過則合併並更新任務卡
- 預期產出：本審核交接檔、任務卡 status 更新、合併 commit

## B. 交接

- 完工時間／狀態：2026-09-19T08:52:00+08:00 / done（初審通過，已合併 main）
- 完成事項：
  - 在 T-0012 worktree（HEAD `ef455ae`）實際重跑全部驗收指令，逐條核對 acceptance 四條，全數通過。
  - 逐欄比對 `src/schemas/error-schema.ts` 與 `04_API規格.yaml components.schemas.Error`：`code` 的 enum 五值逐字一致（E_VALIDATION／E_UNAUTHORIZED／E_NOT_FOUND／E_EMAIL_TAKEN／E_INTERNAL）；`ERROR_STATUS` 對照的 HTTP 狀態碼（400／401／404／409／500）與 SD §6.1 表格逐列一致；`ErrorBody` 只有 code／message／details?／requestId 四個欄位，`buildErrorBody` 以「不寫入欄位」而非「寫入 undefined」的方式實作，符合 `additionalProperties: false`；`AppError` 建構子與 `buildErrorBody` 兩處都強制 `E_INTERNAL` 不帶 details（雙重防線），符合「5xx 一律無 details」。
  - 確認 5xx 固定字面值：`INTERNAL_ERROR_MESSAGE = "Internal Server Error"`（error-handler.ts 第 36 行），errorHandler 內 500 路徑一律送此常數而非 `error.message`；堆疊只經 `request.log.error({ err: error })` 進日誌。
  - 確認 `requestId` 取自 `request.id`（＝pino `reqId`），整合測試「requestId 與 pino 的 reqId 為同一值」實跑通過。
  - 確認 `setNotFoundHandler` 已設，實際 curl 未知路徑回統一結構 404。
  - 確認 SD §6.5 日誌等級：`logByStatus` 5xx→error、4xx→warn。
  - 確認 src/app.ts 只新增兩行（錨點一行＋import 一行），五個 ANCHOR 註解數量與順序未動。
  - 合併：`git merge --no-ff task/T-0012-error-handler`，任務卡 status→done、審核紀錄表加一列；移除 worktree 與分支。
- 產出物（路徑）：
  - 本檔 worklog/handoff/20260919-0843-T0012-r1-dev-tl.md
  - tasks/T-0012-error-handler.md（status: done、審核紀錄表）
  - main 上的合併 commit
- 未完成／已知問題：
  - 無阻擋級問題。兩點小瑕疵已寫入「下一步建議」，不退回。
- acceptance 逐條判定：
  1. **WBS §1.2 驗收方式草案每條實際執行且結果符合** — 通過（六條指令輸出見下方「驗證方式」）。
  2. **npm run lint 與相關測試全綠；不改 outputs 以外檔案** — 通過。lint exit 0；unit 7/7、integration 8/8 全綠；`git diff main...HEAD --stat` 只有 outputs ＋任務卡＋交接檔。
  3. **錯誤回應格式與 04_API規格.yaml components.schemas.Error 逐欄一致** — 通過（逐欄比對見上）。
  4. **src/app.ts 只新增錨點處一行，其他行 diff 為零** — 通過（依 Leader 裁決 2026-09-19T08:41:59 第②條，import 一行不算；實測既有行 diff 為零）。
- 對交接檔「我做的假設與決策」逐條表態：
  1. **`registerErrorHandler` 直接對根 app 呼叫 setErrorHandler／setNotFoundHandler，不經 `app.register()`** — **接受**。理由正確：Fastify 的封裝會讓 `app.register()` 內設定的 error handler 只涵蓋該子情境，手足外掛（WI-05 auth／static、WI-06 routes）不繼承；改用直接呼叫既達成全域效果又不新增 `fastify-plugin` 相依（硬前提 7）。整合測試中「未經 register 包一層的測試路由同樣被接住」是有效證據。**給 WI-05／WI-06 的約束：不得在自己的子情境內再呼叫 setErrorHandler，否則會在該子情境覆蓋掉全域 handler。**
  2. **src/app.ts 多一行 import** — **接受**。與 Leader 裁決 2026-09-19T08:41:59 第②條一致（acceptance 意圖是「不動其他邏輯」，import 不算）。實測既有行零改動。
  3. **`error.validation` 只取第一條違規推導 `{ field, rule }`，不聚合** — **接受**。04_API規格.yaml 的 details 範例（`field`/`rule`）確為單一欄位形狀，聚合屬過度設計；若日後需要多欄位回報，走規格變更。
  4. **非 AppError 但帶 `.statusCode` 的例外依狀態碼對照，4xx 未列入表者歸 E_VALIDATION、非 4xx 一律 E_INTERNAL** — **接受（附一項提醒）**。fallback 方向保守正確（非 4xx 一律走最嚴格的 E_INTERNAL 路徑，不洩漏細節）。提醒：該 4xx 路徑會把 `error.message`（第三方套件產生的原文）直接放進回應。P0 範圍內只會由 `@fastify/basic-auth` 觸發（訊息為通用字串），風險可接受；已寫入「下一步建議」給 T-0015 實測確認。
  5. **`E_EMAIL_TAKEN`/409 只預留常數不主動丟出** — **接受**。與 WBS §1.2「P1，本卡可預留常數但不使用」逐字一致。
- 驗證方式（審核者實跑的指令與真實輸出）：

  ```bash
  cd C:/Users/excal/Projects/Claude/soft-dev-team-260919-wt/T-0012
  git log --oneline -2
  git status --short
  git diff main...HEAD --stat
  ```
  ```text
  ef455ae T-0012: 統一錯誤處理器與錯誤契約（WI-02）
  305d5bf E-001: 派工批次 2（T-0012/T-0013/T-0014）
  （git status --short 無輸出，工作區乾淨）

   src/app.ts                                       |   2 +
   src/plugins/error-handler.ts                     | 147 ++++++++++++++
   src/schemas/error-schema.ts                      |  78 ++++++++
   tasks/T-0012-error-handler.md                    |   4 +-
   tests/integration/error-contract.test.ts         | 157 +++++++++++++++
   tests/unit/error-handler.test.ts                 | 190 ++++++++++++++++++
   worklog/handoff/20260919-0822-T0012-r1-dev-be.md | 243 +++++++++++++++++++++++
   7 files changed, 819 insertions(+), 2 deletions(-)
  ```
  （只動 outputs ＋任務卡 ＋交接檔，符合 acceptance 第 2 條）

  ```bash
  npm run lint
  ```
  ```text
  > todo-app@0.1.0 lint
  > eslint . && tsc --noEmit -p tsconfig.json

  LINT_EXIT:0
  ```

  ```bash
  npm run build && ls dist/plugins dist/schemas
  ```
  ```text
  > todo-app@0.1.0 build
  > tsc -p tsconfig.json

  BUILD_EXIT:0
  dist/plugins:
  error-handler.js
  error-handler.js.map
  dist/schemas:
  error-schema.js
  error-schema.js.map
  ```

  ```bash
  node --test tests/unit/error-handler.test.ts
  ```
  ```text
  ✔ AppError(E_VALIDATION) 轉為 400，帶 details，requestId 等於 request.id (0.9843ms)
  ✔ AppError(E_NOT_FOUND) 轉為 404，無 details (0.0944ms)
  ✔ AppError(E_INTERNAL) 一律用固定訊息，即使建構時傳了自訂訊息與 details (0.1173ms)
  ✔ Fastify schema 驗證錯誤（error.validation）轉為 400 E_VALIDATION，details 只含欄位與規則 (0.1494ms)
  ✔ 未預期例外一律回 500 E_INTERNAL，固定訊息，不洩漏例外訊息或堆疊 (0.1248ms)
  ✔ 帶 statusCode 的一般錯誤（例如未來 basic-auth 丟出 401）依狀態碼對應到正確錯誤碼 (0.0778ms)
  ✔ notFoundHandler：未知路徑回統一結構的 404，含 requestId，不含 details (0.0828ms)
  ℹ tests 7
  ℹ pass 7
  ℹ fail 0
  ℹ duration_ms 108.598
  EXIT:0
  ```

  ```bash
  node --test tests/integration/error-contract.test.ts
  ```
  ```text
  ✔ 未知路徑：統一結構 404，含 requestId，不含 details／堆疊 (157.9307ms)
  ✔ AppError(E_VALIDATION)：400，details 逐欄一致（code/message/details/requestId） (12.3784ms)
  ✔ AppError(E_NOT_FOUND)：404，無 details 欄位 (8.7642ms)
  ✔ 未預期例外：500 固定訊息 Internal Server Error，不洩漏例外訊息／堆疊／SQL (8.5266ms)
  ✔ Fastify schema 驗證失敗：400 E_VALIDATION，details 只含欄位與規則，不回射輸入原文 (9.3692ms)
  ✔ requestId 與 pino 的 reqId 為同一值（NFR-005） (7.573ms)
  ℹ tests 6
  ℹ pass 6
  ℹ fail 0
  ℹ duration_ms 400.3182
  EXIT:0
  ```

  ```bash
  npm run test:unit
  npm run test:integration
  ```
  ```text
  ℹ tests 7  ℹ pass 7  ℹ fail 0  ℹ duration_ms 112.4182   UNIT_EXIT:0
  ℹ tests 8  ℹ pass 8  ℹ fail 0  ℹ duration_ms 423.8266   INT_EXIT:0
  ```
  （integration 8 條＝本卡 6 條 ＋ T-0011 既有 health 2 條，皆綠）

  ```bash
  # WBS §1.2 的 curl 兩條：先起服務
  netstat -ano | grep ":8080" | grep LISTENING        # 起服務前：無輸出
  cp .env.example .env
  set -a && . ./.env && set +a && node dist/server.js &
  netstat -ano | grep ":8080" | grep LISTENING
  curl -sS http://localhost:8080/no-such-path
  curl -sS -o /dev/null -w "%{http_code}\n" http://localhost:8080/no-such-path
  curl -sS http://localhost:8080/no-such-path | grep -o "\"requestId\""
  curl -sS http://localhost:8080/no-such-path | grep -cE "stack|at .*\.ts:"
  curl -sS http://localhost:8080/health
  ```
  ```text
    TCP    0.0.0.0:8080           0.0.0.0:0              LISTENING       33280
  {"code":"E_NOT_FOUND","message":"Route not found","requestId":"req-1"}
  404
  "requestId"
  0
  {"status":"ok"}
  ```
  （第 3 條 grep 有輸出＝404 走統一結構；第 4 條 grep -c 為 0＝無堆疊洩漏；health 未受影響）

  ```bash
  # 收尾：結束背景服務並確認埠已釋放
  # PowerShell: Stop-Process -Id 33280 -Force
  netstat -ano | grep ":8080" | grep LISTENING
  ```
  ```text
  PORT_CHECK_DONE（無 LISTENING 輸出，8080 已釋放）
  ```

  ```bash
  grep -n "redact" src/plugins/error-handler.ts
  grep -n "Internal Server Error" src/plugins/error-handler.ts
  grep -cE "ANCHOR:(error-handler|auth|static|routes|p1-auth-routes)" src/app.ts
  ```
  ```text
  21: * pino 的 `redact`（BR-016，避免 Authorization 標頭落日誌）已在
  33: * 供 dev-tl 逐字核對（WBS §1.2 驗收：`grep -n "Internal Server Error"
  36:export const INTERNAL_ERROR_MESSAGE = "Internal Server Error";
  5
  ```
  （redact 命中指標註解，實際設定在 src/app.ts 建構參數，我已閱讀該檔確認；五個錨點齊全）

  ```bash
  git diff main...HEAD -- src/app.ts
  ```
  ```text
  @@ -16,6 +16,7 @@
   import type { AppConfig } from "./config.js";
  +import registerErrorHandler from "./plugins/error-handler.js";
   import healthRoutes from "./routes/health.js";
  @@ -30,6 +31,7 @@ export function buildApp(config: AppConfig): FastifyInstance {
     // [ANCHOR:error-handler]   ← WI-02 於此註冊 BE-03（必須最先）
  +  registerErrorHandler(app);
     // [ANCHOR:auth]            ← WI-05 於此註冊 BE-02 全域 hook（必須早於 static 與 routes）
     app.register(healthRoutes); // WI-01 註冊 BE-07
  ```
  （只有兩行新增，零行修改／刪除；錨點順序未動）

  ```bash
  # 憑證掃描（T-0011 補強樣式）
  grep -rnEi "(password|secret|database_url|api[_-]?key|token)[[:space:]]*[:=][[:space:]]*[\"]?[A-Za-z0-9/+_.-]{2,}" src/plugins/error-handler.ts src/schemas/error-schema.ts tests/unit/error-handler.test.ts tests/integration/error-contract.test.ts
  ```
  ```text
  tests/unit/error-handler.test.ts:110:  const error = new AppError("E_INTERNAL", "DATABASE_URL=postgres://leaked", {
  ```
  （人工複核：這是測試用的假字串，斷言 E_INTERNAL 回應不會把它洩漏出去。不是真實憑證，不入任何設定檔。判定：非憑證入庫，**不阻擋**。）

- 下一步建議（小瑕疵，不退回；給後續卡）：
  1. **給 T-0015（WI-05）與 T-0016（WI-06）**：全域 handler 是直接掛在根實例上的（假設與決策①）。這兩張卡**不得在自己的 `app.register()` 子情境內再呼叫 `setErrorHandler`／`setNotFoundHandler`**，否則會在該子情境把全域 handler 蓋掉，造成「有些路徑回契約格式、有些不回」的分裂。要丟錯就 `throw new AppError(...)`。
  2. **給 T-0015**：error-handler 的 4xx fallback（`resolveCode` → 第 118 行）會把非 AppError 例外的 `error.message` 原文放進回應。請實測 `@fastify/basic-auth` 認證失敗時實際丟出的錯誤物件，確認訊息文字不含內部細節，且 `WWW-Authenticate: Basic realm="staging"` 標頭由 T-0015 自行加簽（本卡不負責）。若該外掛的訊息不理想，T-0015 應改為自行 `throw new AppError("E_UNAUTHORIZED", ...)`。
  3. **給 T-0016**：業務層 `title.trim()` 後的長度驗證請直接丟 `AppError("E_VALIDATION", "title must not be empty after trimming", { field: "title", rule: "minLength" })`，訊息逐字對齊 04_API規格.yaml 範例；不要依賴 schema 驗證 fallback（那條路徑的 message 是 ajv 的通用文字）。
  4. **給 qa-cr**：契約回歸測試可直接重用 tests/integration/error-contract.test.ts 的 `Object.keys().sort()` 斷言寫法，逐欄比對 additionalProperties:false。
- 需要 Leader 裁決的事：
  - 無。交接檔提的兩項（ci.yml unit job 缺 build、app.ts 多一行 import）Leader 已於 2026-09-19T08:41:59 裁決；ci.yml 的修正由我在批次 2 收尾 commit 一併處理。
