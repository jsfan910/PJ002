# T-0023 r1 — qa-lead — 工作紀錄與交接

<!-- 檔名：worklog/handoff/20260919-1329-T0023-r1-qa-lead.md（時間 = 開工時間，取實查系統時間 2026-09-19 13:29:30 +0800） -->
<!-- 規則：開工立即寫 A 段；完工或中止時寫 B 段。同一輪同一角色一個檔。 -->

## A. 開工紀錄

- 開工時間：2026-09-19T13:29:30+08:00
- 角色／模型：qa-lead / Claude Opus 5（1M context）
- 任務卡：tasks/T-0023-test-summary.md（round 1）
- 讀取的輸入（實際讀過的檔案與段落，非任務卡照抄）：
  - `.claude/agents/qa-lead.md` 全文 —「決定這個版本能不能過」；判斷依據只有退出準則、報告實際數據、缺陷清單；退回時每個缺陷須附重現步驟與對應 TC
  - `tasks/T-0023-test-summary.md` 全文 — outputs 三項、acceptance 四條、reviewer leader
  - `docs/specs/07_測試計畫.md` §4（退出準則 12 項，第 1~7 為擋關項）、§5（S1~S4 分級、5.2 阻擋級 CR 定義、5.3 缺陷成立要件）、§6.1（staging 未存在的註記）、§7（R-1~R-6 風險）
  - `docs/reports/20260919-1146-CR-E001.md` — CR r1 的 13 項（B-1、B-2、S-1~S-11）逐項檔名行號、說明與修正建議
  - `docs/reports/20260919-1309-CR-E001-r2.md` — 13/13 全部已修正、阻擋級 0；新增 N-1（建議級，migration 無互斥鎖）、N-2（已由 e7abe4f 關閉）、N-3 觀察
  - `docs/reports/20260919-1221-AT-E001.md`（r1）與 `docs/reports/20260919-1314-AT-E001-r2.md`（r2 取代 r1 數字）— lint 0、unit 76、integration 56、integration:qa 38 過／4 阻擋、e2e 136、perf PASS、README 方式二 14 秒 0 卡點；staging 依存 TC 清單
  - `docs/reports/20260919-1147-UAT-E001.md`（r1，D-FE-001）與 `docs/reports/20260919-1301-UAT-E001-r2.md`（r2，P0 10/10 通過）
  - `tasks/E-001-todo-app.md` §Leader 裁決紀錄全段 — 特別是 O-009（NFR-003 Gate 2 門檻改 24 小時採樣）、T-0009 裁決 6（staging 未就緒記「阻擋（環境未就緒）」非「不通過」）、12:03:12（撤回 T-0013-③）、12:18:14（D-FE-001 待重現，交 qa-lead 裁定）、13:12:54（T-0023 驗收須跑兩條 integration 指令）、13:15:01（CR r2 N-1 列 P1）
  - `worklog/handoff/20260919-1301-T0021-r1-dev-tl.md` B 段 — `test:integration` 已拆兩條、`tsconfig.e2e.json`、本機 compose 未關閉的理由
  - `docs/templates/qa_24_缺陷清單.md`、`docs/templates/qa_25_測試總結報告.md` — 欄位骨架
  - `docs/specs/20_測試案例.md`、`docs/specs/traceability.md` — TC 清單與主表狀態欄
- 上一輪交接摘要（round > 1）：不適用（round 1）
- 本輪計畫：
  1. 審核 T-0020（抽查 r2 指名的 B-1／B-2／S-1 行號存在且內容如述；重跑 r2 §2~§6 至少三條指令）
  2. 審核 T-0021（重跑 build／lint／test:unit／test:integration／compose 起服務後 test:integration:qa 與 test:e2e Chromium 核對數字；抽查 5 個 TC-ID 在 tests/ 存在）
  3. 審核 T-0022（抽查 2 個 US 的操作紀錄可依步驟重現；瀏覽器先帶帳密載入再用裸網址）
  4. 建 `docs/specs/24_缺陷清單.md`：CR r1 的 13 項（已修）、CR r2 N-1、UAT D-FE-001、AT 阻擋的 staging TC 逐一登錄
  5. 更新 `docs/specs/traceability.md` 主表「狀態」欄為測試結果
  6. 出 `docs/reports/20260919-HHMM-測試總結-E001.md`：退出準則表每格實際數字、建議、殘留風險、給 Leader 的裁決事項
- 預期產出：三卡審核紀錄、缺陷清單、traceability 狀態欄、測試總結報告、本交接檔

## B. 交接

- 完工時間／狀態：2026-09-19T14:00:00+08:00 / **review**（reviewer：leader）

### 完成事項

1. **審核三卡並寫入各卡審核紀錄表**（`status` 皆改 `done`）：
   - **T-0020（CR）**：抽查 r2 指名的行號四處（`basic-auth.ts:53-64`／`:70-75`、`server.ts:35-56`、`Dockerfile:43`、`app.ts:39-43`）**全部存在且內容如述**；重跑 r2 的 §1／§2／§3／§6／§7 **五段**（超過要求的三條），阻擋級 = 0 核實成立。
   - **T-0021（AT）**：依 Leader 13:12:54 裁決**跑足兩條 integration 指令**，build／lint／unit／integration／integration:qa／e2e **六項數字與報告完全一致**；抽查 TC-011／029／047／088／094 五個 TC-ID 皆存在且斷言對題；**補齊 AT r2 遺漏的 TC-094 子項**（500 筆下前端可互動時間）。
   - **T-0022（UAT）**：以「先帶帳密網址載入、再用裸網址操作」重現 **US-001 與 US-006**，與報告逐句相符；**裁定 D-FE-001 為工具限制、非產品缺陷**（Leader 12:18:14 交辦）。
2. **缺陷清單 `docs/specs/24_缺陷清單.md`**（新建）：CR r1 的 13 項（D-001 ~ D-013，全部 closed）、CR r2 N-1（D-014，S3 open，Leader 已裁 P1）、UAT D-FE-001（D-015，S4 wontfix ＋ 裁定理由）逐一登錄，欄位齊全；另以第 2 節登錄 9 筆「環境阻擋」（E-B-01 ~ E-B-09，依 Leader 裁決不計為缺陷）、第 3 節 6 項觀察。**開放 S1 = 0、開放 S2 = 0（實際值，非宣稱）**。
3. **`docs/specs/traceability.md`**：**只改主表「狀態」欄**（13 列）為測試結果，另同步 frontmatter 註解與第 16 行的一句說明（避免產生第二份真相）。其餘欄位、NFR 表、孤兒檢查表全部未動。
4. **測試總結 `docs/reports/20260919-1356-測試總結-E001.md`**（新建）：退出準則 12 項**每格皆有實際數字**；建議 **有條件可發布**；殘留風險 R-1 ~ R-8；給 Leader 的裁決事項 A／B／C 各為完整五段式。**staging 未部署的事實以引言區塊置於報告最前，並在第 1、6、12 項逐條標明「未執行」。**

### 產出物（路徑）

- `docs/specs/24_缺陷清單.md`（新建）
- `docs/reports/20260919-1356-測試總結-E001.md`（新建）
- `docs/specs/traceability.md`（主表「狀態」欄）
- `tasks/T-0020-code-review.md`、`tasks/T-0021-automated-tests.md`、`tasks/T-0022-uat.md`（審核紀錄表 ＋ `status: done` ＋ `updated`）
- `tasks/T-0023-test-summary.md`（`status: review`、`outputs` 的報告檔名填實、`updated`）
- 本交接檔

### 未完成／已知問題

1. **staging 從未部署** —— 退出準則第 1 項（P0 TC 通過率）與第 6 項（NFR-003 24 小時採樣）因此未達標，第 12 項（部署不可用時間）未執行。8 條 P0 TC 記「阻擋（環境未就緒）」：TC-078、079、080、081、084、089、090、105。**這不是我能解決的事，須使用者提供 GCP／GitHub 一次性設定。**
2. **NFR-004 相容性 4／6**：Firefox 兩個 e2e project 在本沙盒無法啟動（`browserType.launch: spawn UNKNOWN`），沿用 AT r1／r2 結論，Leader 已裁列 Gate 2 後補跑。
3. **unit 層 11 條 P0 TC 無法逐條追溯**：`tests/unit/**` 與 `tests/integration/*.test.ts` 未標 TC-ID（見裁決事項 C）。
4. **方式一（Docker Compose）的 README 計時未重測**：沿用 AT r1 的 27 秒／0 卡關點（T-0024 之後方式一的步驟未變更）。

### 我做的假設與決策（含理由）

1. **建議寫「有條件可發布」而非二擇一** —— 測試計畫 §4 只給「可發布／不可發布」，但本輪的唯二擋關缺口成因 100% 是環境未就緒、0 條不通過，兩個標籤都會誤導。折衷做法：主結論寫「有條件可發布」並列出四項條件，**同時明文附上「若必須二擇一則為不可發布」**，讓 Leader 兩種格式都能直接取用。
2. **D-FE-001 判 wontfix（非產品缺陷）** —— 不採信任何一方的文字，自己在瀏覽器複驗：先帶帳密載入、再以裸網址 `http://localhost:8093/` 操作，清單正常載入且 `location.href` 確認不含帳密。產品端無可重現偏差，依 §5.3 不成立為缺陷。
3. **把 8 條 staging 阻擋 TC 與 Firefox 另立「環境阻擋登錄」表，不混進缺陷表** —— 依 Leader 2026-09-19T07:40:06 裁決第 6 項「記阻擋不記不通過」；混進缺陷表會污染「開放 S1／S2」這個退出準則指標。但**仍逐條登錄且欄位齊全**，不是略過。
4. **自行補量 TC-094 的「前端可互動 < 3 秒」** —— 該子項是退出準則第 9 項的一半，AT r2 只量了 API 側。與其記「未量測」，不如當場量掉（502 筆、166 ms）。**這是補量不是代寫**，已在 T-0021 審核紀錄中標明來源為本卡。
5. **自用 compose 專案名 `qal23`、埠 8093／5436** —— 避免撞到他人；收尾已 `down -v` 並刪除 `.env.qa-lead-t0023`。
6. **`traceability.md` 動了 frontmatter 註解與第 16 行** —— 任務卡寫「只更新主表狀態欄」，但第 16 行原文寫「狀態欄由 T-0019 補填」，不改會與新內容互相矛盾（第二份真相）。改動限於一句說明，未觸及任何資料欄位。

### 驗證方式（接手者如何確認我做的是對的：指令 + 我實際得到的輸出）

環境：Windows 11、Node v24.15.0、Docker Desktop；自用 compose 專案 `qal23`、app 埠 `8093`、db 埠 `5436`。

#### 1) build / lint / unit

```bash
npm run build; npm run lint; npm run test:unit
```
```text
> todo-app@0.1.0 build
> tsc -p tsconfig.json
BUILD_EXIT=0

> todo-app@0.1.0 lint
> eslint . && tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.test.json && tsc --noEmit -p tsconfig.e2e.json
LINT_EXIT=0

ℹ tests 76
ℹ pass 76
ℹ fail 0
ℹ skipped 0
ℹ duration_ms 161.8524
UNIT_EXIT=0
```

#### 2) 兩條 integration 指令（Leader 13:12:54 裁決要求）

```bash
NODE_ENV=test PORT=8093 DATABASE_URL="postgres://dev:dev@localhost:5436/todo" \
  BASIC_AUTH_USER=dev BASIC_AUTH_PASSWORD=dev LOG_LEVEL=silent npm run test:integration
BASE_URL=http://localhost:8093 BASIC_AUTH_USER=dev BASIC_AUTH_PASSWORD=dev npm run test:integration:qa
```
```text
ℹ tests 56
ℹ pass 56
ℹ fail 0
ℹ skipped 0
INTEGRATION_EXIT=0

﹣ TC-079：HTTP 由平台自動 301 導向 HTTPS（Cloud Run 平台行為，本機無此層） # 阻擋（環境未就緒）
﹣ TC-080：一次新的部署後既有資料仍存在（需觸發真實 staging 部署）        # 阻擋（環境未就緒）
﹣ TC-084：Cloud Run revision 切流量回滾（需真實 staging 環境與回滾演練）  # 阻擋（環境未就緒）
ℹ tests 42
ℹ pass 38
ℹ fail 0
ℹ skipped 4
QA_INTEGRATION_EXIT=0
```

#### 3) e2e（Chromium × 2 尺寸）

```bash
BASE_URL=http://localhost:8093 BASIC_AUTH_USER=dev BASIC_AUTH_PASSWORD=dev \
  npm run test:e2e -- --project=chromium-1280x800 --project=chromium-390x844
```
```text
  ok 68 [chromium-390x844] › tests\e2e\us009-same-origin.e2e.test.ts:14:1 › TC-075：/api/v1/* 請求與頁面同源，且不含任何 OPTIONS 預檢 (311ms)
  68 passed (25.1s)
E2E_EXIT=0
```

#### 4) CR B-2 複驗：乾淨資料卷自動 migration

```bash
docker compose -p qal23 --env-file .env.qa-lead-t0023 up -d --build
curl -sS -u dev:dev -w "\nHTTP=%{http_code}\n" http://localhost:8093/api/v1/todos
docker exec qal23-db-1 psql -U dev -d todo -c "SELECT version, applied_at FROM schema_migrations"
```
```text
Volume qal23_todo-db-data  Created
Container qal23-db-1  Healthy
Container qal23-app-1 Started

[]
HTTP=200

 version |          applied_at
---------+-------------------------------
 001     | 2026-09-19 05:31:23.840727+00
```

#### 5) CR B-1／S-2 複驗

```bash
for P in /health "/health?x=1" "/foo/../health" "/.%2e/health" "/%2e%2e/health" \
         "/assets/%2e%2e/%2e%2e/health" "/HEALTH" "//health"; do
  printf "%-34s -> " "$P"; curl -sS --path-as-is -o /dev/null -w "%{http_code}\n" "http://localhost:8093$P"
done
for M in GET HEAD POST PUT PATCH DELETE OPTIONS; do
  printf "%-8s -> " "$M"; curl -sS -X "$M" -o /dev/null -w "%{http_code}\n" "http://localhost:8093/health"
done
```
```text
/health                            -> 200
/health?x=1                        -> 200
/foo/../health                     -> 401
/.%2e/health                       -> 401
/%2e%2e/health                     -> 401
/assets/%2e%2e/%2e%2e/health       -> 401
/HEALTH                            -> 401
//health                           -> 401

GET      -> 200
HEAD     -> 200
POST     -> 401
PUT      -> 401
PATCH    -> 401
DELETE   -> 401
OPTIONS  -> 401
```

#### 6) CR S-1 複驗

```text
POST {"title":123}            -> 400 {"code":"E_VALIDATION","message":"body/title must be string",
                                      "details":{"field":"title","rule":"type"}}
PATCH {"isCompleted":null}    -> 400 body/isCompleted must be boolean
PATCH {"isCompleted":"true"}  -> 400 body/isCompleted must be boolean
PATCH {"isCompleted":true}    -> 200 {"id":"b2212a17-a4e7-4076-b7c0-00a7cfecdf68","isCompleted":true}
```

#### 7) TC-ID 覆蓋抽查與總數

```bash
grep -roE "TC-[0-9]{3}" tests/ | sed -E "s/.*(TC-[0-9]{3})/\1/" | sort -u | wc -l
```
```text
79
（TC-004~011、013~018、021~029、031~037、040~047、049~058、060~076、079、080、082~088、090~094
 ——即應覆蓋清單 80 條扣掉明文不寫腳本的 TC-089；AT r2 報告寫「84 條」為計數筆誤，已記 OBS-4）

TC-011 → tests/e2e/us001-create.e2e.test.ts:63
TC-029 → tests/e2e/us003-edit.e2e.test.ts:61
TC-047 → tests/e2e/us005-toggle.e2e.test.ts:45
TC-088 → tests/e2e/nfr-xss.e2e.test.ts:18
TC-094 → tests/perf/tc094-nfr007-capacity.perf.mjs
```

#### 8) TC-094 補量（前端可互動 < 3 秒）

```text
建立 500 筆耗時 0.4 秒
5 次中位數：4.2ms（門檻 < 2000ms） → PASS
陣列長度（含既有資料）：502

# 瀏覽器（裸網址 http://localhost:8093/）
{ items: 502, sinceNavigationStartMs: 166, domContentLoadedMs: 38, waitedMs: 1 }
→ 導覽開始後 166 ms 時 502 筆已全部渲染完成（門檻 < 3000 ms） → PASS
```

#### 9) UAT 抽查（US-001／US-006）與 D-FE-001 裁定

```text
# 先 http://dev:dev@localhost:8093/ 載入一次，再以裸網址 http://localhost:8093/ 操作
location.href = "http://localhost:8093/"        ← 不含帳密
初始畫面：listitem "目前沒有任何待辦事項，新增一筆開始吧。"   ← 非 401

US-001：輸入「購買牛奶」→「新增」
  listitem: checkbox(未勾) / "購買牛奶" / "2026-09-19 13:53" / 編輯 / 刪除
  輸入框已清空、無整頁跳轉

排序：「寫系統設計書」(新) 在「購買牛奶」(舊) 之前          ← AC-002-4

US-005：勾選「購買牛奶」
  [{checked:false,title:"寫系統設計書",deco:"none"},
   {checked:true, title:"購買牛奶",    deco:"line-through"}]

US-006：「未完成」→ 只剩「寫系統設計書」
        「已完成」→ 只剩「購買牛奶」
        「全部」  → 兩筆皆在
```

#### 10) 收尾清除

```bash
docker compose -p qal23 --env-file .env.qa-lead-t0023 down -v
rm -f .env.qa-lead-t0023
docker ps; docker volume ls --filter name=qal23
```
```text
 Container qal23-db-1 Removed / Volume qal23_todo-db-data Removed / Network qal23_default Removed
docker ps → （無容器）
docker volume ls --filter name=qal23 → （無）
```

### 下一步建議

1. **給 Leader**：先處理測試總結第 6 節的裁決事項 **A**（Gate 2 判定方式），B、C 可一併裁但不擋 A。
2. **給使用者（經 Leader）**：唯一真正的阻塞是 **GCP 專案／計費帳戶／Artifact Registry 與 Cloud Run API 啟用／GitHub secrets（建議 WIF）** 這份一次性設定。設定到位後的順序：T-0018 部署 → 回滾演練 → `monitor-health.yml` 連續跑 **24 小時** → 補跑 8 條阻擋 TC ＋ staging UAT 十則 → 由 qa-lead 出「測試總結 r2」。
3. **Gate 2 後待辦（建議合併成一張 P1 卡）**：Firefox 兩組相容性補跑；`runMigrations` 加 advisory lock（D-014）並補一條「多實例並行冷啟」TC；`tests/unit` 與 dev 整合測試補 TC-ID 標註（OBS-3）；perf 改走專用空資料庫（OBS-6）。
4. **給 qa-at**：日後報告的覆蓋條數請以 `grep` 去重結果為準，不要手算區間（OBS-4）。

### 需要 Leader 裁決的事

三項，完整五段式見 `docs/reports/20260919-1356-測試總結-E001.md` 第 6 節：

- **A. Gate 2 的判定方式（staging 未就緒）** —— 建議「不通過（阻擋：環境未就緒）」並於結論首行載明「0 條不通過、0 個開放 S1／S2」；替代選項為「有條件通過」或「拆 Gate 2a／2b」。**這一項不裁決，Gate 2 報告寫不出來。**
- **B. NFR-004 的 Firefox 兩組（4／6）** —— 建議維持 2026-09-19T12:32:40 原裁決（列 Gate 2 後補跑）。
- **C. unit 層 11 條 P0 TC 無法逐條追溯（R-3）** —— 建議追認本輪判法（以 CR 檢查清單 ＋ 套件全綠佐證），補 TC-ID 標註列 P1。
