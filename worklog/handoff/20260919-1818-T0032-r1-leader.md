# T-0032 r1 — leader（代理・Opus） — 工作紀錄與交接

## A. 開工紀錄

- 開工時間：2026-09-19T18:18:35+08:00
- 角色／模型：leader（代理） / Claude Opus 5
- 任務卡：tasks/T-0032-agents-v1.1.md（round 1）
- 讀取的輸入（實際讀過的檔案與段落，非任務卡照抄）：
  - `.claude/agents/leader.md` — Leader 職責、使用者介入點三個、禁止三條；本卡要在此檔補 F-05 備援派工與 F-23 介入紀錄。
  - `CLAUDE.md` 全文 — 協作協定（狀態機、寫入權責、固定程序）與末段 5 條工作鐵則（單引號、python、git append、worktree、commit -F），即共用「環境注意」段的文字來源。
  - `tasks/E-002-framework-v1.1.md` — 25 項發現清單；確認本卡負責 F-01/03/04/05/07/08/10/11/13/15/16/23 與各自落點。
  - `tasks/T-0032-agents-v1.1.md` — acceptance 九條、淨增 ≤ 60 行限制、只改 outputs。
  - `tasks/E-001-todo-app.md` 全部 Leader 裁決紀錄 — 取得：①12:18 與 12:59 的 D-FE-001 成因（帶帳密網址建認證快取後改裸網址）②16:54／17:30 qa-uat haiku 兩輪誤判工具限制 ③12:03 CR B-1 點節段繞過 /health 豁免、dev-tl 初審放行與 qa-cr 判阻擋的差異 ④16:30／16:51 Leader 介入 `gcloud run services update --update-secrets` 的時間／指令／理由／revision 與事後更正 ⑤08:13-② 本機 Docker daemon 不可用的處置 ⑥17:30 qa 平行存取 staging 互刪資料。
  - `docs/templates/code_review_report.md` 末段 — 憑證掃描已知例外四類（qa-cr 引用對象）。
  - 12 個 `.claude/agents/*.md` — 既有結構：職責／必讀／必寫／完成定義／（角色專屬審核段）／通用協定（開工・收尾・禁止）；leader.md 例外，無通用協定。
  - `docs/templates/worklog_handoff.md` — 本檔格式與機器可讀欄位契約。
- 上一輪交接摘要（若 round > 1）：
  - 退回原因：不適用（r1）
  - 本輪要修的點：不適用
- 本輪計畫（3–7 條）：
  1. 寫一份共用「環境注意」文字（8 條：單引號／python／worktree 路徑／commit -F／只 add 自己的檔／Docker 備援／等待外部事件 2 次／回報五行），逐檔貼在「禁止」之後；同時把收尾第 4 步中已被共用段吸收的重複文字收斂成一行，以壓低淨增行數。
  2. qa-uat：frontmatter `model: haiku` → `sonnet`；加「瀏覽器認證與阻擋判定」段（F-07）。
  3. qa-at、qa-uat：加「共用環境的資料隔離」段（F-08，兩檔同文字）。
  4. dev-ops：加「Windows／PowerShell／cmd 常見陷阱」六條（F-10）。
  5. qa-cr：加「憑證掃描的已知例外」段，引用 CR 模板（F-11）。dev-tl：加「安全邊界判準」段（F-16）。
  6. leader.md：加「環境注意」＋「同 session 新建角色定義的備援派工」（F-05）＋「Leader 直接介入基礎設施或程式時的紀錄義務」（F-23）。
  7. 驗證：`git diff --numstat` 逐檔核 ≤ 60 行淨增；grep 12 檔皆有「環境注意」、qa-uat `model: sonnet`；用三行派工提示詞對 dev-be.md、qa-uat.md 做自我核對六問。
- 預期產出：`.claude/agents/` 12 檔、本交接檔、任務卡狀態 review。

## B. 交接

- 完工時間／狀態：2026-09-19T18:22:17+08:00 / review
- 完成事項（每項對應 E-002 的 F 編號）：
  - **F-01／F-03／F-04／F-13／F-15**：12 個角色檔全部新增「環境注意（每次必讀；開發機 Windows 11，Bash 工具是 Git Bash）」小節，**八條、12 檔逐字相同**（已用 diff 比對本文完全一致；leader.md 因無「通用協定」上層段落，標題層級為 `##`，其餘 11 檔為 `###`，位置皆在「禁止」之後）。八條內容：①Bash 不得含 ASCII 單引號（F-01）②不呼叫 python3／python（F-01）③開發卡一律在 `<專案根目錄>-wt/T-####` worktree 內作業、根目錄永遠 main（F-02 的角色檔側落點，與本卡 F-03 共用段合併）④多行 commit 訊息寫檔再 `-F`（F-04）⑤git 只准 append、只 add 本卡 outputs／任務卡／交接檔、禁 `add -A`／`--amend`／`reset`／`rebase`／`checkout -- <file>`（F-03）⑥Docker daemon 不可用時改離線等效驗證且不得記為失敗或缺陷（F-13）⑦等待外部事件最多主動確認 2 次，之後回報 Leader、不得反覆回報「等待中」（F-15）⑧回報固定五行（F-12 的角色檔側落點）。
  - **F-12（減法，支援 T-0034 的三行提示詞）**：11 檔「收尾」第 4 步原本重述 worktree／add 範圍／禁用指令，已收斂成一行並指向「環境注意」第 3～5 條，避免同一規則兩份真相；淨增行數因此壓到每檔 11 行。
  - **同 11 檔「收尾」第 3 步**補「標 `blocked` 時 `blocked_reason` 寫卡在哪／需要誰做什麼，佐證（指令與真實輸出、HTTP 狀態碼與 URL、錯誤訊息原文）貼交接檔；沒有佐證不得標 blocked」。原地改寫，不增行。此條為 F-07「阻擋須附證據」的通用化，使非 qa-uat 角色也有一致判準。
  - **F-07**：`qa-uat.md` frontmatter `model: haiku` → `model: sonnet`（依 E-002 裁決：平衡模式改 sonnet）；新增「瀏覽器認證程序與阻擋判定」段（在「完成定義」之後），寫入 E-001 12:59／13:17 的成因結論：先以帶帳密網址載入建立認證快取、之後一律用裸網址；同源 fetch 預設 `credentials: "same-origin"` 會帶認證，**缺 `credentials: "include"` 不是缺陷**；標阻擋前必須先自證非工具問題（重建快取＋API 等效驗證）；阻擋必須附網路面板的請求 URL（遮帳密）＋HTTP 狀態碼，否則改標「未完成（待補證據）」；401/403 先當認證快取問題、404/5xx 才可能是缺陷；同輪兩則以上想標阻擋即停手回報。
  - **F-08**：`qa-at.md`、`qa-uat.md` 各新增「共用環境的資料隔離」段（**兩檔逐字相同**）：只動自己建立的資料並加可辨識前綴、禁止全量刪除或清空、別人資料異動不記為缺陷、需獨占環境的動作回報 Leader 排時段。來源為 E-001 17:30「qa 並行存取 staging 互相刪資料」。
  - **F-10**：`dev-ops.md` 新增「Windows／PowerShell／cmd 常見陷阱」六條：①PowerShell 管線吃／補尾端換行且 `Format-Hex` 看不出，secret 一律 `--data-file=<檔案>`、以 Git Bash `wc -c` 驗位元組 ②secret 不用 echo／管線／環境變數傳遞，寫暫存檔用完刪 ③cmd 不認單引號，跨 shell 一律雙引號、README 兩種 shell 分開寫 ④佔位符尖括號 `<…>` 在 PowerShell／cmd 會被當重導向，須整串加引號並註明要換實值 ⑤PowerShell 呼叫 gcloud 要用 `gcloud.cmd` ⑥`<(echo …)` 不存在，改「寫檔→指向→刪檔」三步。另附 `gcloud auth configure-docker` 寫入 docker credHelpers 的前置條件提醒（E-001 16:30 紀錄）。
  - **F-11**：`qa-cr.md` 新增「憑證掃描的已知例外」段，明定判讀依據為 `docs/templates/code_review_report.md` 末段清單（列出目前四類但註明**以模板當下內容為準、本檔不複寫細目**）；不在清單者一律視為真命中；要新增例外類別須交 Leader 更新模板，不得自行認定。
  - **F-16**：`dev-tl.md` 新增「安全邊界判準（初審時）」段：一律以 SD 第 6 章判準，**不以「第三方已擋」放行**；豁免路徑只認字面路徑，點節段／百分號編碼／重複或尾端斜線／大小寫變體都要實測，能繞過即阻擋級；初審與 CR 是雙層不是二選一；兩造不一致時以較嚴一方為準並提 Leader。來源為 E-001 12:03 CR B-1 與 16:54 `//health` 回 401 兩起事實。
  - **F-05**：`leader.md` 新增「同 session 新建角色定義的備援派工」段：角色檔於 session 啟動載入，本 session 新建者叫不到 subagent_type；備援為通用 agent ＋「先完整讀 `.claude/agents/{role}.md` 並依該檔執行」第一行，其餘維持三行；不得把角色檔內容複製進提示詞。
  - **F-23**：`leader.md` 新增「直接介入基礎設施或程式時的紀錄義務」段：介入必須在該 Epic 裁決紀錄留「時間／逐字指令（憑證遮蔽）／理由／可回查的結果識別碼（revision、commit、run id）」四項；成果交回負責角色驗證；否定成員判斷要寫憑什麼否定；紀錄有誤另寫更正段不改舊段。來源為 E-001 16:30 與 16:51（update-secrets 建 revision、事後更正 00002／00003 寫反）。
- 產出物（路徑）：
  - `.claude/agents/plan-ba.md`、`plan-sa.md`、`plan-sd.md`、`dev-tl.md`、`dev-be.md`、`dev-fe.md`、`dev-ops.md`、`qa-lead.md`、`qa-cr.md`、`qa-at.md`、`qa-uat.md`、`leader.md`（共 12 檔）
  - `worklog/handoff/20260919-1818-T0032-r1-leader.md`（本檔）
- 未完成／已知問題：
  - 「環境注意」第 3 條的 worktree 規則同時是 F-02 的角色檔側落點；F-02 另外兩個落點（takeover／review-round skill、任務卡模板）屬 T-0034、T-0033，本卡未碰。
  - 角色檔改動要**下次啟動 session 後**原生 subagent_type 才保證吃到新內容（即本卡自己寫進 leader.md 的 F-05 規則）；本 session 內派工建議採備援寫法。
  - F-06「規格優先於派工提示詞」未寫進角色檔（落點在 T-0035 協定與 T-0034 dispatch），本卡刻意不寫以免產生第二份真相。
- 我做的假設與決策（含理由）：
  1. **共用段位置與層級**：11 檔放在「通用協定」的「禁止」之後（`###`）；`leader.md` 無「通用協定」上層段落，改放在其 `## 禁止` 之後並用 `##`。本文逐字相同，只有標題層級不同——理由是保持各檔既有標題階層正確，不為了字面一致而破壞結構。
  2. **把「收尾」第 4 步重述的 worktree／add／禁用指令收斂成一行指向環境注意**。理由：acceptance 要求「重複內容移到共用段」且每檔淨增 ≤ 60 行；同一規則寫兩處日後會不同步。
  3. **通用化「阻擋須附證據」到 11 檔的收尾第 3 步**（原地改寫不增行）。acceptance 只要求 qa-uat，但 F-15／F-13 也都會產生「標阻擋」的情境，判準一致才不會又出現 dev-tl 與 qa-cr 那種兩套標準。
  4. **qa-cr 的例外清單不複寫細目**，只引用模板並註明以模板為準。理由：模板是唯一事實來源（T-0033 可能還會改），角色檔複寫即第二份真相。
  5. **F-02 在角色檔只寫一次**（併入環境注意第 3 條），不另立段落。
- 驗證方式（接手者如何確認我做的是對的：指令 + 我實際得到的輸出）：
  ```bash
  # 1) 每檔淨增行數（acceptance：≤ 60）
  git diff --numstat -- .claude/agents/ | awk "{print \$3, \$1-\$2}"
  # 2) 12 檔都有「環境注意」小節
  grep -c "^#\+ 環境注意" .claude/agents/*.md
  # 3) qa-uat 模型
  grep -n "^model:" .claude/agents/qa-uat.md
  # 4) 12 檔「環境注意」本文逐字相同（以 plan-ba 為基準逐檔 diff）
  ```
  ```text
  # 1) 淨增行數（全部 ≤ 60）
  .claude/agents/dev-be.md 11
  .claude/agents/dev-fe.md 11
  .claude/agents/dev-ops.md 24
  .claude/agents/dev-tl.md 18
  .claude/agents/leader.md 26
  .claude/agents/plan-ba.md 11
  .claude/agents/plan-sa.md 11
  .claude/agents/plan-sd.md 11
  .claude/agents/qa-at.md 18
  .claude/agents/qa-cr.md 17
  .claude/agents/qa-lead.md 11
  .claude/agents/qa-uat.md 30

  # 2) 12 檔皆為 1（每檔一個「環境注意」標題）
  dev-be.md:1  dev-fe.md:1  dev-ops.md:1  dev-tl.md:1  leader.md:1  plan-ba.md:1
  plan-sa.md:1 plan-sd.md:1 qa-at.md:1   qa-cr.md:1   qa-lead.md:1 qa-uat.md:1

  # 3) qa-uat 模型
  4:model: sonnet

  # 4) 本文逐字比對結果
  IDENTICAL plan-sa / plan-sd / dev-tl / dev-be / dev-fe / dev-ops /
            qa-lead / qa-cr / qa-at / qa-uat / leader（以 plan-ba 為基準，diff 無輸出）
  標題行只有兩種（僅層級不同，文字相同）：
  ## 環境注意（每次必讀；開發機 Windows 11，Bash 工具是 Git Bash）
  ### 環境注意（每次必讀；開發機 Windows 11，Bash 工具是 Git Bash）
  ```

  **三行派工提示詞的自我核對**（acceptance 最後一條的「可理解性」驗證）

  假想提示詞（只有三行，不含任何環境說明）：
  ```text
  任務卡：tasks/T-0099-xxx.md
  依 .claude/agents/{role}.md 執行，規格以 docs/specs/ 凍結版為準。
  完成後回報五行。
  ```
  以最終版 `dev-be.md`（62 行）與 `qa-uat.md`（80 行）各自**只讀該檔**，回答六問：

  | 問題 | dev-be.md | qa-uat.md |
  |---|---|---|
  | worktree 路徑 | ✅ L57：`<專案根目錄>-wt/T-####`，且明寫「不在專案根目錄切分支」「根目錄永遠停在 main」 | ✅ L75（同文） |
  | commit -F | ✅ L58（並說明 `-m` 與 PowerShell here-string 為何不行） | ✅ L76 |
  | 禁 add -A | ✅ L59（連同 `--amend`／`reset`／`rebase`／`checkout --`，附「會移除別人 commit」的理由） | ✅ L77 |
  | Docker 不可用怎麼辦 | ✅ L60：不得記為失敗／缺陷，改離線等效驗證並在交接檔註明、列下一步建議 | ✅ L78 |
  | 阻擋要附什麼證據 | ✅ L42：指令與真實輸出／狀態碼與 URL／錯誤原文，無佐證不得標 blocked | ✅ L38（專屬：網路面板 URL＋狀態碼，且須先自證非工具問題）＋L60（通用） |
  | 回報格式 | ✅ L62 + 收尾第 5 步：`狀態｜產出路徑｜交接檔路徑｜需裁決事項｜下一步` | ✅ L80 + 收尾第 5 步 |

  結論：六問皆可**只靠角色檔**回答，不需提示詞補充。dev-be.md 未直接寫「Docker daemon 在哪一步會用到」，但第 6 條的處置方式足以判斷，不影響開工。
- 下一步建議：
  - Leader 審核（reviewer: leader）：逐 F 項核對落點；重點看 qa-uat 的認證程序是否與 E-001 12:59／13:17 的成因結論一致、dev-tl 的安全判準是否與 12:03 CR B-1 一致。
  - 與 T-0034 對齊：dispatch skill 的三行提示詞不得再重述環境限制與回報格式（已全部移入角色檔）；handoff skill 的 commit 段可直接指向角色檔「環境注意」第 4、5 條。
  - 與 T-0033 對齊：任務卡模板的 worktree 欄位與本檔第 3 條措辭需一致（`<專案根目錄>-wt/T-####`）。
  - 本 session 內若要立刻派工，用 leader.md 新增的備援寫法（通用 agent ＋「先讀角色檔」第一行）。
- 需要 Leader 裁決的事：
  - 無（本卡未觸及 outputs 以外檔案；未改任何 `tasks/T-00xx` 卡與 `docs/specs`）。

