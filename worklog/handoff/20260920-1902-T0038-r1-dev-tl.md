# T-0038 r1 — dev-tl — 工作紀錄與交接（初審）

## A. 開工紀錄

- 開工時間：2026-09-20T19:02:23+08:00
- 角色／模型：dev-tl / Opus 5 (1M context)
- 任務卡：tasks/T-0038-fix-d017-error-message.md（round 1，審核 T-0038）
- 讀取的輸入（實際讀過的檔案與段落，非任務卡照抄）：
  - tasks/T-0038-fix-d017-error-message.md — acceptance 5 條與「驗收方式」指令清單；outputs 三檔（todo-store.js／todo-view.js／tests/unit/todo-store.test.mjs）；reviewer=dev-tl、branch=task/T-0038-fix-d017。
  - worklog/handoff/20260920-1836-T0038-r1-dev-fe.md（分支版，全檔 148 行）— dev-fe 宣稱的根因（load() 的 requestSeq 只保護 load() 彼此、add()/updateTitle() 同步驗證失敗分支不佔號）、修正規則（opSeq／beginOp()/isStale()）、修正前 2 fail／修正後全綠的輸出、以及 acceptance 第 4 條（e2e 5 次連跑）因 Docker daemon 不可用未執行。
  - `git diff main...HEAD -- public/assets/todo-store.js`（分支實際變更）— 逐行核對 beginOp()/isStale() 的插入點：load() 158、add() 同步失敗分支與非同步分支、updateTitle()／setCompleted()／remove() 的 catch 與成功前檢查。
  - `git diff main...HEAD -- tests/unit/todo-store.test.mjs` — 新增 2 則測試（D-017／TC-009、D-017／TC-067）的實際斷言內容，確認不是空測試。
  - .claude/skills/review-round/SKILL.md — 審核一輪的步驟與判定規則（不採信交接檔的「通過」二字，只採信自己重跑的輸出）。
- 上一輪交接摘要（若 round > 1）：無（round 1，本卡第一次審核）
- 本輪計畫（3–7 條）：
  1. 在 worktree `<根>-wt/T-0038` 實跑 `npm ci`、`npm run lint`、`npm run test:unit`，貼真實輸出。
  2. 實跑「修正前會失敗」的反證：以 main 版 todo-store.js 搭配新測試檔，確認 2 則新測試真的紅，證明測試有效。
  3. Docker daemon 現已就緒：以 `PORT=8081 POSTGRES_HOST_PORT=5433` 起 compose（避開 T-0041 佔用的 8082／5434），補跑 e2e `-g "TC-067|TC-009"` 連續 5 次。
  4. 比對 `git diff --stat main...task/T-0038-fix-d017`，確認只動 outputs（含任務卡與交接檔）。
  5. 以 SD 第 6 章判準檢查安全面（本卡為前端狀態機修正，確認未觸及認證／豁免路徑、無硬編碼密鑰）。
  6. 逐條判 acceptance，對 dev-fe「假設與決策」逐條表態；寫審核紀錄。
  7. 本輪例外：即使通過也不合併、不推送（Leader 裁決：等 main 的 deploy 工作流恢復綠），卡維持 status: review。
- 預期產出：worklog/handoff/20260920-1902-T0038-r1-dev-tl.md（本檔）、tasks/T-0038-fix-d017-error-message.md（審核紀錄與 status）。

## B. 交接

- 完工時間／狀態：2026-09-20T19:10:27+08:00 / review
- 完成事項：
  - 在 worktree `<根>-wt/T-0038` 實跑 `npm ci`／`npm run lint`／`npm run test:unit`：全綠，78 條全過（含新增 2 條）。
  - **獨立複驗「修正前會失敗」**：不採信 dev-fe 交接檔的輸出，改以 scratchpad 隔離副本（`git show main:public/assets/todo-store.js` ＋ 分支版測試檔）重跑，2 條 D-017 測試確實紅；證明新測試不是空測試，確實釘住了 D-017 的時序。**過程中未改動 worktree 任何檔案**（收尾 `git status` 乾淨）。
  - **Docker daemon 本輪已就緒**（`docker version --format {{.Server.Version}}` → `29.4.2`），依提示以 `PORT=8081`／`POSTGRES_HOST_PORT=5433` 起 compose（專案名 `t0038`，避開 T-0041 的 8082／5434），補跑 acceptance 第 4 條的 e2e 5 次連跑（4 個 project：chromium／msedge × 1280x800／390x844，`-g "TC-067|TC-009"`）。
  - 結果：**每次 8 條中 6 過 2 敗**，5 次完全一致（不是間歇性）。失敗固定為 `[msedge-1280x800]`／`[msedge-390x844]` 的 **TC-009**，且**失敗點不是 D-017 的斷言**——`error-message` 可見、非空、`todo-item` count=0 三條 D-017 相關斷言 5 次全過；掛掉的是同一條 TC 最後一行「console 應無 error」，實際訊息為 `Failed to load resource: the server responded with a status of 404 (Not Found)`。
  - **追根**：對照應用日誌，本輪 21 筆 404 的 `reqId` 全部對應 `"url":"/favicon.ico"`（無其他 404）。即 Edge 會自動請求 `/favicon.ico`，而本專案沒有該路由／該靜態檔，回 404，瀏覽器 console 記一筆 error，撞上 TC-009 的零 console error 斷言。
  - **證明與本卡無因果關係**：另以 `git worktree add --detach` 拉出一份 **main（修正前）** 程式碼，獨立 compose（8083／5435）建置啟動，跑同一份測試檔的 msedge TC-009 → **一模一樣的 favicon 404 失敗**（2 條全敗）；同一基準的 chromium 兩尺寸 TC-067｜TC-009 → 4 條全過。故此失敗是 main 既有狀態，非 T-0038 造成。
  - 安全面（SD §6）：以實跑確認豁免路徑只認字面 `/health`——`/health`→200，`/./health`／`/%2e/health`／`//health`／`/health/`／`/HEALTH` 全部 401，受保護的 `/api/v1/todos` 亦為 401。本卡 diff 無硬編碼密鑰（掃 `password|secret|token=|api[_-]key|Bearer|BASIC_AUTH` 於新增行無命中；`token` 僅為序號變數名）。本機 `.env` 由 repo 內 `.env.example` 佔位值複製產生、已於收尾刪除，**全程未使用任何 staging 憑證**。
  - 變更範圍：`git diff --stat main...task/T-0038-fix-d017` 僅 4 檔＝outputs 2 檔（`todo-store.js`、`tests/unit/todo-store.test.mjs`）＋任務卡＋dev-fe 交接檔。`todo-view.js` 列在 outputs 但未改動（理由見下方表態）。
  - 依 Leader 本輪裁決：**通過但不合併、不推送**，任務卡維持 `status: review`、審核紀錄記「初審通過，待合併」。
- 產出物（路徑）：
  - C:\Users\excal\Projects\Claude\soft-dev-team-260919\worklog\handoff\20260920-1902-T0038-r1-dev-tl.md（本檔）
  - C:\Users\excal\Projects\Claude\soft-dev-team-260919\tasks\T-0038-fix-d017-error-message.md（status→review、審核紀錄表加一列）
- 未完成／已知問題：
  - **未合併、未推送**（Leader 本輪裁決：等 main 的 deploy-staging 工作流恢復綠燈再合併）。CHANGELOG 亦尚未更新（隨合併一併做）。
  - **新發現、與本卡無關的缺陷：`/favicon.ico` 回 404 使 TC-009 在 Edge 上必然失敗**。尚未立缺陷編號（立案權在 qa-lead），已寫進「需要 Leader 裁決的事」。
  - **D-017 本身尚未算關閉**：本機 5 次連跑證明修正有效且無回歸，但 D-017 是 **staging 才重現**的缺陷（缺陷清單第 50 列的重現步驟明寫「本機 docker compose／HTTP-1.1 從未重現，僅對 staging 重現」）。要正式關閉仍須合併推送後由 qa-lead 在 staging 重跑 TC-067／TC-009 至少 5 次連續全過。
- 我做的假設與決策（含理由）：
  - **對 dev-fe「根因」的表態：接受。** 我逐行核對 `git diff main...HEAD`，確認修正前 `load()` 的 `requestSeq` 只在 `load()` 內 `++` 與比對，`add()/updateTitle()/setCompleted()/remove()` 四個動作與 `add()/updateTitle()` 的同步驗證失敗分支確實完全不佔號；`load()` 成功分支的 `setState({ todos, loading: false, error: null })` 也確實無條件清 `error`。這是可被單元測試釘住的真實狀態轉移缺陷，不是「加了延遲」。
  - **對「修正後狀態轉移規則」的表態：接受。** `opSeq`／`beginOp()`／`isStale()` 把「誰有權寫入 loading／error／todos」收斂成單一單調遞增序號，語義一致（最新動作勝出），且同時補上 `add/updateTitle/setCompleted/remove` 彼此之間原本沒有的 staleness 保護。既有 76 條測試無回歸。
  - **對「不改 `todo-view.js`」的表態：接受。** 我讀過 `render()`／`renderError()`，確為純依 `state` 重繪；根因在 store。任務卡 outputs 列了該檔只是「允許改」的範圍，不是「必須改」。**不因此退回**。
  - **acceptance 第 4 條判「條件通過」而非退回 rework 的理由**：條文字面要求「5 次全數通過」，本輪 msedge 兩組的 TC-009 並未通過。但（a）失敗點與 D-017 無關，D-017 的三條斷言 5 次全過；（b）同一失敗在**未含本卡修正的 main** 上完全複現，因果上不可歸咎本卡；（c）修法需新增 favicon 路由或靜態檔，落在本卡 outputs 之外，強令 dev-fe 修即違反「不擴大範圍」與「不改 outputs 以外的檔案」。因此判本卡通過，另把 favicon 問題送 Leader／qa-lead 立案。這個判斷是「條件」而非「放行」：我在下方明列了它，未來任何人要主張 TC-009 在 Edge 上已通過，仍須先解掉 favicon 404。
  - **合併風險評估（供 Leader 決定合併時機）**：CI `qa-tests.yml:126` 只跑 `--project=chromium-1280x800 --project=chromium-390x844`，不跑 msedge；這兩組本輪在分支與 main 基準皆全過，故合併本卡不會因 favicon 問題讓 CI 轉紅。
  - **一個被我接受、但需記錄的行為改變**：`add()/updateTitle()/setCompleted()/remove()` 成功後若已被更新的動作取代（`isStale`），會直接回傳 `{ok:true, stale:true}` 而**不執行後續的 `load()`**。後果是「寫入已成功但清單暫時不刷新」，要到下一次載入才看得到。這是「最新動作勝出」規則的必然代價，替代方案（照舊無條件 `load()`）就是 D-017 本身。`todo-view.js:286-287` 只看 `result.ok`，所以輸入框仍會正確清空，無 UI 破綻。我接受此設計，但建議 qa-lead 評估是否補一條 TC。
- 驗證方式（接手者如何確認我做的是對的：指令 + 我實際得到的輸出）：
  ```bash
  cd "C:/Users/excal/Projects/Claude/soft-dev-team-260919-wt/T-0038"
  docker version --format "{{.Server.Version}}"
  ```
  ```text
  29.4.2
  ```
  ```bash
  npm ci && npm run lint
  ```
  ```text
  found 0 vulnerabilities
  > todo-app@0.1.0 lint
  > eslint . && tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.test.json && tsc --noEmit -p tsconfig.e2e.json
  LINT_EXIT=0
  ```
  ```bash
  npm run test:unit
  ```
  ```text
  ℹ tests 78
  ℹ suites 0
  ℹ pass 78
  ℹ fail 0
  ℹ cancelled 0
  ℹ skipped 0
  ℹ todo 0
  ℹ duration_ms 140.4091
  EXIT=0
  ```
  ```bash
  grep -c "D-017\|TC-067\|TC-009" tests/unit/todo-store.test.mjs
  grep -n "^test(\"D-017" tests/unit/todo-store.test.mjs
  ```
  ```text
  11
  203:test("D-017／TC-009：初始 load() 因延遲仍在飛行中時，同步驗證失敗的 error 不會被稍後才 resolve 的 load() 蓋回 null", async () => {
  236:test("D-017／TC-067：初始 load() 因延遲仍在飛行中時，新增失敗（後端 500）的 error 不會被稍後才 resolve 的 load() 蓋回 null", async () => {
  ```
  ```bash
  # 獨立複驗「修正前會紅」：在 scratchpad 隔離副本組合 main 版 store ＋ 分支版測試檔
  # （腳本見 scratchpad/prefix-check.sh；worktree 本身全程未被改動）
  git show main:public/assets/todo-store.js > $SB/public/assets/todo-store.js
  cd $SB && node --test tests/unit/todo-store.test.mjs
  ```
  ```text
  ✖ D-017／TC-009：初始 load() 因延遲仍在飛行中時，同步驗證失敗的 error 不會被稍後才 resolve 的 load() 蓋回 null (0.8108ms)
    AssertionError [ERR_ASSERTION]: Expected values to be strictly equal:
  ✖ D-017／TC-067：初始 load() 因延遲仍在飛行中時，新增失敗（後端 500）的 error 不會被稍後才 resolve 的 load() 蓋回 null (0.3053ms)
    AssertionError [ERR_ASSERTION]: Expected values to be strictly equal:
  ```
  ```bash
  # compose（PORT=8081／POSTGRES_HOST_PORT=5433，.env 由 .env.example 複製後覆寫兩個埠）
  docker compose -p t0038 up -d --build && docker compose -p t0038 ps
  ```
  ```text
  NAME          IMAGE                COMMAND                   SERVICE   CREATED   STATUS                  PORTS
  t0038-app-1   t0038-app            "docker-entrypoint.s…"   app       7s ago    Up Less than a second   0.0.0.0:8081->8081/tcp
  t0038-db-1    postgres:16-alpine   "docker-entrypoint.s…"   db        7s ago    Up 6 seconds (healthy)  0.0.0.0:5433->5432/tcp
  ```
  ```bash
  # 安全：豁免路徑只認字面 /health（SD §6）
  for p in "/health" "/./health" "/%2e/health" "//health" "/health/" "/HEALTH" "/api/v1/todos"; do
    echo "$p -> $(curl -s -o /dev/null -w "%{http_code}" --path-as-is "http://localhost:8081$p")"; done
  ```
  ```text
  /health -> 200
  /./health -> 401
  /%2e/health -> 401
  //health -> 401
  /health/ -> 401
  /HEALTH -> 401
  /api/v1/todos -> 401
  ```
  ```bash
  # acceptance 第 4 條：e2e 連跑 5 次（4 個 project）
  export BASE_URL=http://localhost:8081
  for i in 1 2 3 4 5; do
    npx playwright test -c tests/e2e/playwright.config.ts \
      --project=chromium-1280x800 --project=chromium-390x844 \
      --project=msedge-1280x800 --project=msedge-390x844 -g "TC-067|TC-009"; done
  ```
  ```text
  ########## RUN 1 ##########   2 failed / 6 passed (11.2s)   RUN_1_EXIT=1
  ########## RUN 2 ##########   2 failed / 6 passed (7.8s)    RUN_2_EXIT=1
  ########## RUN 3 ##########   2 failed / 6 passed (7.9s)    RUN_3_EXIT=1
  ########## RUN 4 ##########   2 failed / 6 passed (7.6s)    RUN_4_EXIT=1
  ########## RUN 5 ##########   2 failed / 6 passed (37.8s)   RUN_5_EXIT=1
  # 5 次的失敗名單完全相同：
  #   [msedge-1280x800] › tests\e2e\us001-create.e2e.test.ts:30:1 › TC-009：前端空白標題顯示可讀錯誤且不建立
  #   [msedge-390x844]  › tests\e2e\us001-create.e2e.test.ts:30:1 › TC-009：前端空白標題顯示可讀錯誤且不建立
  # 通過的 6 條＝chromium 兩尺寸 ×（TC-067、TC-009）＋ msedge 兩尺寸 × TC-067
  ```
  ```bash
  # 失敗細節：掛的不是 D-017 的斷言，是同一條 TC 最後一行的 console 檢查
  npx playwright test -c tests/e2e/playwright.config.ts --project=msedge-1280x800 -g "TC-009"
  ```
  ```text
  Error: console 應無 error：Failed to load resource: the server responded with a status of 404 (Not Found)
  expect(received).toHaveLength(expected)
  Expected length: 0
  Received length: 1
  Received array:  ["Failed to load resource: the server responded with a status of 404 (Not Found)"]
    45 |   await expect(page.getByTestId("error-message")).not.toBeEmpty();
    46 |   await expect(page.getByTestId("todo-item")).toHaveCount(0);
  > 47 |   expect(consoleErrors, `console 應無 error：${consoleErrors.join(" | ")}`).toHaveLength(0);
  # 第 44~46 行（error-message 可見／非空／todo-item 為 0）皆已通過才走到第 47 行
  ```
  ```bash
  # 404 是誰打的：把 404 的 reqId 對回 url
  docker compose -p t0038 logs app | grep "Route not found" | grep -o "req-[a-z0-9]*" | sort -u > /tmp/ids.txt
  docker compose -p t0038 logs app | grep "\"url\"" | grep -f /tmp/ids.txt | grep -o "\"url\":\"[^\"]*\"" | sort | uniq -c
  ```
  ```text
  404 reqIds: 21
       21 "url":"/favicon.ico"
  ```
  ```bash
  # 因果隔離：拉一份 main（修正前）獨立建置跑同一份測試
  git worktree add --detach $BASE main
  grep -c "opSeq\|beginOp" public/assets/todo-store.js   # → 0（確認是修正前版本）
  grep -c "requestSeq" public/assets/todo-store.js       # → 4
  docker compose -p t0038base up -d --build              # PORT=8083 / POSTGRES_HOST_PORT=5435
  BASE_URL=http://localhost:8083 npx playwright test -c tests/e2e/playwright.config.ts \
    --project=msedge-1280x800 --project=msedge-390x844 -g "TC-009"
  ```
  ```text
  ########## BASELINE(main) msedge TC-009 ##########
      Error: console 應無 error：Failed to load resource: the server responded with a status of 404 (Not Found)
      Received array:  ["Failed to load resource: the server responded with a status of 404 (Not Found)"]
      Error: console 應無 error：Failed to load resource: the server responded with a status of 404 (Not Found)
      Received array:  ["Failed to load resource: the server responded with a status of 404 (Not Found)"]
    2 failed
  ########## BASELINE(main) chromium TC-067|TC-009 ##########
    4 passed (2.5s)
  # 結論：main（無本卡修正）上 msedge TC-009 以完全相同的 favicon 404 失敗
  #       → 該失敗與 T-0038 無因果關係
  ```
  ```bash
  # CI 只跑 chromium，故合併不會因 favicon 問題讓 CI 轉紅
  grep -n "project=" .github/workflows/qa-tests.yml
  ```
  ```text
  126:        run: npm run test:e2e -- --project=chromium-1280x800 --project=chromium-390x844
  ```
  ```bash
  # 變更範圍與密鑰掃描
  git diff --stat main...task/T-0038-fix-d017
  git diff main...HEAD -- public/assets/todo-store.js tests/unit/todo-store.test.mjs \
    | grep -in "password\|secret\|token=\|api[_-]key\|Bearer \|BASIC_AUTH"
  git status --short
  ```
  ```text
   public/assets/todo-store.js                      |  79 ++++++++++--
   tasks/T-0038-fix-d017-error-message.md           |   4 +-
   tests/unit/todo-store.test.mjs                   |  82 +++++++++++++
   worklog/handoff/20260920-1836-T0038-r1-dev-fe.md | 147 +++++++++++++++++++++++
   4 files changed, 301 insertions(+), 11 deletions(-)
  （密鑰掃描無命中）
  （git status 無輸出＝工作區乾淨，驗證過程未污染 worktree）
  ```
  ```bash
  # 收尾清理：兩組 compose 皆 down -v、刪除本機 .env、移除臨時 worktree
  docker ps --format "{{.Names}} {{.Ports}}"    # → 無輸出
  git worktree list
  ```
  ```text
  C:/Users/excal/Projects/Claude/soft-dev-team-260919           5eb45db [main]
  C:/Users/excal/Projects/Claude/soft-dev-team-260919-wt/T-0038 1ab2a53 [task/T-0038-fix-d017]
  C:/Users/excal/Projects/Claude/soft-dev-team-260919-wt/T-0039 54e8117 [task/T-0039-secret-pin-verify-retry]
  ```
- 逐條 acceptance 判定：

  | # | 驗收條件 | 判定 | 證據 |
  |---|---|---|---|
  | 1 | 交接檔寫明根因（引用行號）與修正後狀態轉移規則，不得只寫「加了延遲」 | **通過** | dev-fe 交接檔「假設與決策」指出 main 版 `todo-store.js` 109／134／139／145 行的 `requestSeq` 僅覆蓋 `load()`，142 行無條件 `error: null`，171／192 行同步驗證失敗不佔號；我逐行核對 diff 屬實。修正規則另述為 `opSeq`／`beginOp()`／`isStale()` |
  | 2 | 新增單元測試重現時序，修正前失敗、修正後通過 | **通過** | 我用隔離副本獨立複驗：main 版 store ＋ 同一測試檔 → 2 條紅（AssertionError）；分支版 → 全綠。非空測試 |
  | 3 | `npm run lint`、`npm run test:unit` 全綠 | **通過** | LINT_EXIT=0；tests 78 / pass 78 / fail 0 |
  | 4 | 本機 compose（8081／5433）e2e 連跑 5 次全數通過，不改 tests/e2e 斷言或逾時 | **條件通過** | 5 次結果一致：6 過 2 敗。2 敗固定為 msedge 兩尺寸的 TC-009，失敗點是「console 應無 error」被 `/favicon.ico` 404 觸發，**非 D-017 斷言**（44~46 行皆過）。同一失敗在 main 基準完全複現 → 與本卡無因果關係。chromium 兩尺寸 ×（TC-067、TC-009）5 次全過；msedge 兩尺寸 TC-067 5 次全過。`git diff` 確認 tests/e2e 零改動 |
  | 5 | 不改 outputs 以外的檔案；未用 staging 憑證 | **通過** | diff 僅 outputs 2 檔＋任務卡＋交接檔；`.env` 由 repo 佔位值產生且已刪除，全程未觸及 staging 憑證或 Secret Manager |

- 下一步建議：
  - Leader：待 main 的 deploy-staging 恢復綠燈後，指示我執行合併（`git merge --no-ff task/T-0038-fix-d017`）、更新 CHANGELOG、`git worktree remove` 清理，再推送觸發 staging 部署。
  - qa-lead：（a）為 `/favicon.ico` 404 立缺陷（我建議 S4，非阻擋級，但它使 TC-009 在 Edge 上 100% 紅，會永久掩蓋 D-017 在 Edge 上的真實結果）；（b）合併推送後在 **staging** 重跑 TC-067／TC-009 至少 5 次連續全過，才正式關閉 D-017——本機 5 次全過不足以關閉一個本機從未重現的缺陷；（c）評估是否補一條 TC 覆蓋「寫入成功但被更新動作取代時清單暫不刷新」的新行為。
  - 對 favicon 的修法（若 Leader 決定修）：新增 `public/favicon.ico` 或一條回 204 的路由即可，屬 dev-fe／dev-be 小卡，**不應塞進 T-0038**。
- 需要 Leader 裁決的事：
  - **`/favicon.ico` 回 404 使 TC-009 在 Edge 上必然失敗，需獨立立案**。核心爭議：這不是 T-0038 造成的（main 基準完全複現），但只要不修，TC-009 在任何會自動抓 favicon 的瀏覽器上就是 100% 紅，日後 qa-lead 在 staging 重跑 TC-067／TC-009 判 D-017 是否關閉時，會被這個無關失敗擋住而讀不到真正的結果。證據：本機 21 筆 404 全對應 `/favicon.ico`；分支與 main 基準各自複現；CI 只跑 chromium 故目前不受影響。選項：①另立一張小卡補 `public/favicon.ico` 或回 204 的路由（代價：多一張卡、一次合併）；②由 qa-lead 立缺陷後列 P1，暫以 chromium 結果判 D-017（代價：Edge 的 D-017 證據始終缺一角）；③放寬 TC-009 的 console 斷言忽略 favicon（代價：改凍結測試案例，須走規格變更請求，且會弱化「無 console error」這條判準）。我的建議：①，因為它最便宜且讓 TC-009 恢復成能真正測 D-017 的測試。推翻成本：低，三個選項都可事後改採其他做法，不影響 T-0038 已驗證的修正本身。
  - **另附一則歷史對照（不需裁決，但請 Leader 留意）**：缺陷清單第 50 列記載 AT r3 在 **staging** 上 136 條僅 3 條失敗，且 msedge TC-009 當時是「時好時壞」、失敗訊息為 `error-message ... Received: hidden`，並非 favicon 的 console error。這表示 staging 環境下 TC-009 的 console 斷言當時沒被 favicon 觸發，與本機不一致。我無 staging 憑證、依安全規則未去實測 staging 的 `/favicon.ico` 行為，這個環境差異留給 qa-lead 在 staging 重跑時一併確認。
