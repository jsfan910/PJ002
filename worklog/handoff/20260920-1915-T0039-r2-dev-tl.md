# T-0039 r2 — dev-tl（初審） — 工作紀錄與交接

## A. 開工紀錄

- 開工時間：2026-09-20T19:15:00+08:00（實查系統時間 `date` → `2026-09-20 19:14:50 +0800`）
- 角色／模型：dev-tl / Opus 5 (1M context)
- 任務卡：`tasks/T-0039-secret-pin-verify-retry.md`（round 2，status: review）
- 本次工作：**審核 T-0039 第 2 輪**——實跑任務卡「驗收方式」與 r2 交接檔「驗證方式」全部指令（含第 4 組 IAM 現況唯讀讀取），逐條判定 6 條 acceptance；離線 5 條通過則合併、更新 CHANGELOG、推送 main，並觀察觸發的 `deploy-staging` run 結果後決定 done / blocked / rework。
- 讀取的輸入：
  - `tasks/T-0039-secret-pin-verify-retry.md` 全文（含 r1 審核紀錄四項具體修法）
  - `worklog/handoff/20260920-1902-T0039-r2-dev-ops.md`（r2 交接檔 A／B 段）
  - `worklog/handoff/20260920-1848-T0039-r1-dev-tl.md`（我上一輪的審核紀錄）
  - `.claude/skills/review-round/SKILL.md`、`.claude/agents/dev-tl.md`（角色定義）
  - 分支 `task/T-0039-secret-pin-verify-retry` 全部 diff
- 作業位置：審核與實跑在 worktree `C:\Users\excal\Projects\Claude\soft-dev-team-260919-wt\T-0039`；合併與推送在根目錄 `C:\Users\excal\Projects\Claude\soft-dev-team-260919`（main）。
- 安全邊界：本輪只執行**唯讀** gcloud 指令（`get-iam-policy`、`iam roles describe`、`secrets versions list`），**不執行任何 IAM 變更**、**不執行 `gcloud secrets versions access`**、不讀取任何 secret 值。
- 預期產出：任務卡 `status` 與審核紀錄表 r2 列、`CHANGELOG.md`、main 合併 commit、本交接檔 B 段。

## B. 交接

- 完工時間／狀態：2026-09-20T19:21:00+08:00 / 審核完成，判定 **blocked**（非 rework）。程式碼已通過初審並合併進 main；剩餘唯一阻擋項在使用者側的雲端 IAM 授權。
- 完成事項：
  1. 實跑任務卡「驗收方式」四條指令與 r2 交接檔「驗證方式」六組，**逐條判定 6 條 acceptance**：第 1～5 條通過、第 6 條不通過（原因在本卡程式碼之外）。
  2. **獨立重現** r1 根因的修復：以 mock `gcloud` 對 workflow 原檔抽出的 `resolve_version()` 跑四條路徑，證實「黑盒 exit 1」已變成可讀 `::error::`。
  3. 合併：`git merge --no-ff task/T-0039-secret-pin-verify-retry` → merge commit `3345022`；`CHANGELOG.md` 追加 T-0039 r2 一列；推送 `617a32f..bfe4693 main -> main`。
  4. 觀察推送觸發的真實 run：CI #18 成功 → `deploy-staging` run `35507519302` 紅燈於第 8 步，**但這次留下了可讀的失敗原因**（見下方驗證方式第 6 組）。
  5. 任務卡：`status: blocked`、`blocked_reason` 填「等待使用者 IAM 授權或填版本變數」並附 merge commit 與 run id；審核紀錄表加 r2 列。
  6. **worktree `…-wt/T-0039` 與分支 `task/T-0039-secret-pin-verify-retry` 依指示保留不清理**，待使用者授權後重跑勾核 acceptance 第 6 條。
- 逐條 acceptance 判定：

  | # | 條件 | 判定 | 證據 |
  |---|---|---|---|
  | 1 | 取最新 ENABLED 版本、`NAME=secret:N`、寫 STEP_SUMMARY、可用 repository variable 覆寫 | **通過** | workflow 81～141 行實讀：`resolve_version()` 用 `--filter="state=ENABLED" --sort-by="~createTime" --limit=1`，`--set-secrets "DATABASE_URL=database-url:${DB_VERSION},…"`，三個版本號寫入 `$GITHUB_STEP_SUMMARY` 表格；`OVERRIDE_*` 取自 `vars.SECRET_VERSION_*`；README 第 336～338 行列出三個 variable 名稱與「只填版本號、不得填 secret 值」 |
  | 2 | verify 失敗自動重部署重驗一次、上限 1 次、STEP_SUMMARY 註明「已重試」 | **通過** | workflow 164～263 行：`verify_auth` 失敗 → `RETRIED=true` → `deploy_revision`＋`verify_health`＋`verify_auth` 各一次 → 仍失敗才 `::error::` 並 `exit 1`；無迴圈；`retried`／`retry_result` 兩個 output 餵給「回填提示」步驟（`if: always() && …url != ''`），STEP_SUMMARY 印「**已重試**：…重試後通過／重試後仍失敗」 |
  | 3 | 三處參數一致、yaml 用 `<SECRET_VERSION>` 佔位符、README 同步表更新 | **通過** | `infra/cloudrun-service.yaml` 60～85 行三處 `secretKeyRef.key: "<SECRET_VERSION>"` 並附查版本號指令；workflow 與 `scripts/deploy-staging.sh` 的 `--set-secrets` 字串逐字比對一致；README 321／357／366／368 行同步表與說明已更新 |
  | 4 | `bash -n` 通過、workflow 為合法 YAML 並貼輸出 | **通過** | 見下方驗證方式第 3 組：`SYNTAX_OK`／`268 lines`／`√ YAML Lint successful.` |
  | 5 | 倉庫無 secret 值、不執行 `versions access`、貼只含版本號與狀態的 `versions list` 輸出 | **通過** | 見下方驗證方式第 4、5 組。`grep -rn "versions access"` 只命中 README 220／227 的既有 T-0027 量測範例（本卡 diff 未新增）；我本輪未執行 `versions access`；`versions list` 只用 `--format="table(name,state)"` |
  | 6 | 合併推送後 run 全綠且 STEP_SUMMARY 顯示三個版本號 | **不通過**（阻擋點在使用者側 IAM，非本卡程式碼） | run `35507519302` 第 8 步 `PERMISSION_DENIED`，第 9～13 步 skipped，STEP_SUMMARY 無版本號。見下方驗證方式第 6、7 組 |

- 對 r2 交接檔「假設與決策」的表態：
  1. `scripts/deploy-staging.sh` 一併套用同樣修法 —— **接受**。acceptance 第 3 條本就要求三處一致，且該檔 outputs 內本就含它；錯誤處理邏輯不一致本身就是風險。
  2. `infra/cloudrun-service.yaml` 本輪零改動 —— **接受**。我實讀該檔確認它不含任何 `gcloud` 呼叫，是靜態範本，與 r1 失敗無因果；強行改動才是擴大範圍。
  3. 不用 `gcloud secrets versions access` 做二次確認 —— **接受**，符合 CLAUDE.md 安全鐵則「agent 不讀取任何憑證值」。我本輪同樣未執行。
  4. README PowerShell 段改用 `gcloud.cmd` —— **接受**。位於本卡本就要改的同一段落內，屬最小修正。
  5. 不推翻 r1「不 revert」「不升級模型」兩項決策 —— **接受**，符合 Leader `54e8117` 裁決。
- 未完成／已知問題：
  - **acceptance 第 6 條未達成**，卡在使用者側 IAM 授權（詳見「需要 Leader 裁決的事」）。
  - **次要觀察（不阻擋、不退回）**：GitHub check-run annotation 的 message 長度有上限，本次 `::error::` 的後半段修復指引（「最常見原因：…可改用 repository variable …」）在 annotation 中被截斷，只有前半段的 gcloud 原始訊息可見；**完整訊息仍在 step log 內**。若日後要讓 annotation 一眼看完，可把修復指引改成第二則較短的 `::error::`。已寫入下一步建議，**不足以構成退回理由**——r1 要求的「留下可讀原因」已確實達成。
  - 三個 secret 目前 ENABLED 最新版本為 `database-url`→`1`、`basic-auth-user`→`3`、`basic-auth-pass`→`1`（只取版本號與狀態，未讀值），供使用者走 repository variable 路徑時直接填入。
  - **與本卡無因果的並行紅燈**：`bfe4693` 同時觸發的 `QA Tests (T-0021)` #18 也是 failure。這與 r1 審核交接檔（`20260920-1848-T0039-r1-dev-tl.md`）記載的 QA Tests #17 紅燈同源，是 main 上既有的 favicon 404 導致 msedge TC-009 失敗，已由 **T-0043** 處理，**與 T-0039 無關**，本輪未重複追查。
  - **推送紀律**：`104d5dc`（只改任務卡／CHANGELOG／交接檔）之後補了一筆空提交 `d919e9c` 帶 `[skip ci]`，讓本次推送的 HEAD 帶 skip 標記，避免文件性提交再觸發一次與 `35507519302` 完全相同的 PERMISSION_DENIED 紅燈。已實查確認 `d919e9c` 未產生任何新 run。（依工作鐵則禁用 `--amend`，故以追加提交而非改寫前一筆達成。）
- 我做的假設與決策（含理由）：
  1. **判 blocked 而非 rework**。理由：r1 指出的四件事全部改好且經我獨立重現；剩下的失敗因子是服務帳號缺 `secretmanager.versions.list`，再改程式也不會讓沒有該權限的身分列得出版本。依 CLAUDE.md 安全鐵則「agent 不讀取、不索取、不寫入任何憑證值／不代為執行使用者的雲端安全設定」，IAM 授權必須由使用者執行，符合 blocked 的定義（卡在別人做什麼）。
  2. **先合併再看 run**。理由：本次派工指示明確（離線 5 條通過即合併）；且不合併就不會觸發真實 run，acceptance 第 6 條永遠無法判定。合併風險可控：`deploy-staging` 失敗只影響 staging **新版本部署**，既有 revision 仍正常服務。
  3. **不把「annotation 被截斷」列為缺陷**。理由：acceptance 與 r1 修法要求的是「失敗時必須留下可讀的 `::error::` annotation，而不是只有 exit code」，本次 annotation 已明確寫出 `PERMISSION_DENIED: Permission 'secretmanager.versions.list' denied`，判準已滿足；截斷屬 GitHub 平台顯示上限，不是本卡產出的缺陷。
  4. **不代為執行任何 IAM 變更、不索取憑證**。本輪對 GCP 只下了 `projects get-iam-policy`／`iam roles describe`／`secrets versions list --format="table(name,state)"` 三種唯讀指令。
  5. **不升級模型**。r2 產出已達標，阻擋點不在能力。
- 驗證方式（接手者如何確認我做的是對的：指令 ＋ 我實際得到的輸出）：

  ```bash
  # 1) diff 範圍：只含 outputs ＋ 任務卡 ＋ r2 交接檔
  cd C:/Users/excal/Projects/Claude/soft-dev-team-260919-wt/T-0039
  git status --short   # （零輸出，工作區乾淨）
  git diff --stat main...task/T-0039-secret-pin-verify-retry
  ```
  ```text
   .github/workflows/deploy-staging.yml              |  35 ++++--
   README.md                                         |  26 +++-
   docs/specs/06_部署架構與CICD.md                   |  32 +++++
   scripts/deploy-staging.sh                         |  29 +++--
   tasks/T-0039-secret-pin-verify-retry.md           |   4 +-
   worklog/handoff/20260920-1902-T0039-r2-dev-ops.md | 143 ++++++++++++++++++++++
   6 files changed, 241 insertions(+), 28 deletions(-)
  ```

  ```bash
  # 2) workflow 內不得再有 :latest 形式的 secret 參照；殘留 latest 須逐行確認用途
  grep -n "secret:" .github/workflows/deploy-staging.yml            # exit=1（零命中）
  grep -n "latest" .github/workflows/deploy-staging.yml scripts/deploy-staging.sh infra/cloudrun-service.yaml
  ```
  ```text
  .github/workflows/deploy-staging.yml:13:# T-0039：Secret Manager 參照改釘具體版本（不再用 :latest），版本號   ← 註解
  .github/workflows/deploy-staging.yml:40:    runs-on: ubuntu-latest                                                ← runner 標籤
  .github/workflows/deploy-staging.yml:81:      - name: resolve secret versions（釘具體版本，不用 :latest）         ← 步驟名稱
  .github/workflows/deploy-staging.yml:160:          docker build -t "${IMAGE}:${{ github.sha }}" -t "${IMAGE}:latest" .  ← docker image tag
  .github/workflows/deploy-staging.yml:162:          docker push "${IMAGE}:latest"                                 ← docker image tag
  .github/workflows/deploy-staging.yml:164:      # ---- deploy（…部署一律用 SHA 標籤，不得用 latest）               ← 註解
  scripts/deploy-staging.sh:9 / 78 / 94 / 96：同上（註解、步驟訊息、docker image tag）
  infra/cloudrun-service.yaml:72：# key 一律填具體版本號，不用 "latest"                                    ← 註解
  ```
  **判讀**：零個 secret 參照使用 `:latest`，殘留皆為 docker image tag 與說明文字。

  ```bash
  # 3) 語法層
  bash -n scripts/deploy-staging.sh && echo SYNTAX_OK
  node -e "const f=require(\"fs\").readFileSync(\".github/workflows/deploy-staging.yml\",\"utf8\");console.log(f.split(\"\\n\").length,\"lines\")"
  npx --yes yaml-lint .github/workflows/deploy-staging.yml infra/cloudrun-service.yaml
  ```
  ```text
  SYNTAX_OK
  268 lines
  √ YAML Lint successful.
  ```

  ```bash
  # 4) 以部署服務帳號的實際權限集合驗證（唯讀；未執行任何 IAM 變更、未讀任何 secret 值）
  #    註：Git Bash 下這幾條「不要」加 MSYS_NO_PATHCONV=1——我先加了，gcloud 的 python
  #    launcher 反而把 lib 路徑解析成 C:\c\Users\... 而找不到 gcloud.py（exit 2）；拿掉即正常。
  gcloud projects get-iam-policy pj002-509106 --flatten="bindings[].members" \
    --format="table(bindings.role)" \
    --filter="bindings.members:github-deployer@pj002-509106.iam.gserviceaccount.com"
  gcloud iam roles describe roles/secretmanager.secretAccessor --format="value(includedPermissions)"
  gcloud iam roles describe roles/secretmanager.viewer         --format="value(includedPermissions)"
  ```
  ```text
  ROLE
  roles/artifactregistry.writer
  roles/iam.serviceAccountUser
  roles/run.admin
  roles/secretmanager.secretAccessor

  resourcemanager.projects.get;resourcemanager.projects.list;secretmanager.versions.access

  resourcemanager.projects.get;resourcemanager.projects.list;secretmanager.locations.get;secretmanager.locations.list;secretmanager.secrets.get;secretmanager.secrets.getIamPolicy;secretmanager.secrets.list;secretmanager.secrets.listEffectiveTags;secretmanager.secrets.listTagBindings;secretmanager.versions.get;secretmanager.versions.list
  ```
  **判讀**：與 r1／r2 一致——服務帳號仍缺 `roles/secretmanager.viewer`，即缺 `secretmanager.versions.list`。**IAM 未變更，屬預期**。

  ```bash
  # 5) secret 版本清單（只取版本號與狀態，未讀值；未執行 gcloud secrets versions access）
  for s in database-url basic-auth-user basic-auth-pass; do
    gcloud secrets versions list "$s" --filter="state=ENABLED" --sort-by="~createTime" --limit=1 --format="table(name,state)"
  done
  ```
  ```text
  -- database-url --      NAME  STATE
                          1     enabled
  -- basic-auth-user --   NAME  STATE
                          3     enabled
  -- basic-auth-pass --   NAME  STATE
                          1     enabled
  ```

  ```bash
  # 6) 獨立重現 r1 根因的修復：以 mock gcloud 跑 workflow 原檔抽出的 resolve_version()
  #    函式以 sed 從 .github/workflows/deploy-staging.yml 直接抽出（22 行），避免手抄失真；
  #    mock gcloud 與測試腳本置於 session scratchpad，未寫入倉庫。
  sed -n "/^          resolve_version() {/,/^          }$/p" .github/workflows/deploy-staging.yml | sed "s/^          //" > extracted_fn.sh
  bash tl_resolve_test.sh
  ```
  ```text
  ---- 抽出的函式行數 ----
  22
  ======== MOCK_MODE=denied ========
  before call
  ::error::無法列出 secret database-url 的版本（gcloud 原始訊息：ERROR: (gcloud.secrets.versions.list) PERMISSION_DENIED: Permission 'secretmanager.versions.list' denied for resource 'projects/pj002-509106/secrets/list'）。最常見原因：部署服務帳號缺少 secretmanager.versions.list 權限（需 roles/secretmanager.viewer，見 README「部署與 secrets」一節；既有專案要補跑一次 IAM 授權才會生效）；也可能是 secret 尚未建立。若暫時無法調整 IAM，可改用對應的 repository variable SECRET_VERSION_DATABASE_URL／SECRET_VERSION_BASIC_AUTH_USER／SECRET_VERSION_BASIC_AUTH_PASS 直接指定版本號略過此查詢
  EXIT=1
  ======== MOCK_MODE=empty ========
  before call
  ::error::secret database-url 沒有任何 state=ENABLED 的版本，請先在 Secret Manager 建立至少一個版本
  EXIT=1
  ======== MOCK_MODE=ok ========
  before call
  after call: V_DB=7
  EXIT=0
  ======== override 分支（不呼叫 gcloud） ========
  after call: V_DB=42
  EXIT=0
  ```
  **判讀**：四條路徑皆正確——失敗時**先印出可讀原因再結束**，而非 r1 的裸 `exit 1`；正常與 override 路徑不受影響。

  ```bash
  # 7) 合併、CHANGELOG、推送
  cd C:/Users/excal/Projects/Claude/soft-dev-team-260919      # 根目錄，main
  git merge --no-ff task/T-0039-secret-pin-verify-retry -m "Merge T-0039 r2: …"
  git add CHANGELOG.md worklog/handoff/20260920-1915-T0039-r2-dev-tl.md
  git commit -F <msg 檔>
  git push origin main
  git diff task/T-0039-secret-pin-verify-retry main -- .github/workflows/deploy-staging.yml scripts/deploy-staging.sh README.md docs/specs/06_部署架構與CICD.md
  ```
  ```text
  Merge made by the 'ort' strategy.   6 files changed, 241 insertions(+), 28 deletions(-)
  [main bfe4693] T-0039: 初審 r2 通過（離線 5 條），合併並記錄 CHANGELOG
  To https://github.com/jsfan910/PJ002.git
     617a32f..bfe4693  main -> main
  （最後一條 diff 零輸出＝main 內容與分支一致）

  merge commit 完整 sha：3345022eff624402b5f0ea69c43d7933bc5162ab
  ```

  ```bash
  # 8) 真實 run 結果（acceptance 第 6 條）。gh CLI 本機未安裝，改用 GitHub public REST API
  curl -sS "https://api.github.com/repos/jsfan910/PJ002/actions/runs/35507519302/jobs"
  ```
  ```text
  job 106069733881  deploy-staging  completed / failure
    1 Set up job                                     success
    2 checkout                                       success
    3 setup-node                                     success
    4 npm ci                                         success
    5 npm run build                                  success
    6 auth（Workload Identity Federation）            success
    7 setup-gcloud                                   success
    8 resolve secret versions（釘具體版本，不用 :latest）  FAILURE   ← 失敗點
    9 gcloud auth configure-docker                   skipped
   10 migrate（NEON_DATABASE_URL，forward-only）       skipped
   11 docker build & push                            skipped
   12 gcloud run deploy ＋ verify（含一次自動重試）      skipped
   13 回填提示（首次部署後的網址）                      skipped
  ```

  ```bash
  curl -sS "https://api.github.com/repos/jsfan910/PJ002/check-runs/106069733881/annotations"
  ```
  ```text
  [failure] "Process completed with exit code 1."
  [failure] "無法列出 secret database-url 的版本（gcloud 原始訊息：ERROR:
            (gcloud.secrets.versions.list) PERMISSION_DENIED: Permission
            'secretmanager.versions.list' denied on resource (or it may not exist).
            Remediate access with this Troubleshooter URL …"
  [warning] Node.js 20 is deprecated …（與本卡無關）
  [notice]  ubuntu-latest 將於 2026-10-19 起遷移至 Ubuntu 26 …（與本卡無關）
  ```
  **判讀（本輪最關鍵的一條）**：r1 的 annotation **只有** `Process completed with exit code 1.`；r2 多出一則寫明「無法列出 secret database-url 的版本 … `PERMISSION_DENIED: Permission 'secretmanager.versions.list' denied`」的 failure annotation。**r1 要求的修法已在真實 CI 上生效並得到驗證**。run 仍紅燈，但紅燈原因已從「不明」變成「服務帳號缺一個角色」，且該角色的授權屬使用者的雲端安全設定。

- 下一步建議：
  1. **Leader 轉請使用者擇一執行**（兩者擇一即可解除本卡 blocked）：
     - (a) 補授角色（建議，一勞永逸，之後輪換帳密只要加 secret 版本就會自動釘最新）：
       ```bash
       gcloud projects add-iam-policy-binding pj002-509106 \
         --member="serviceAccount:github-deployer@pj002-509106.iam.gserviceaccount.com" \
         --role="roles/secretmanager.viewer"
       ```
     - (b) 暫時略過查詢：在 GitHub repo 設三個 repository variables `SECRET_VERSION_DATABASE_URL=1`、`SECRET_VERSION_BASIC_AUTH_USER=3`、`SECRET_VERSION_BASIC_AUTH_PASS=1`（**只填版本號，不填 secret 值**）。代價：日後加新 secret 版本時要手動改這三個 variable，失去「自動釘最新」的好處。
  2. 使用者完成後，由 dev-tl 或 Leader 重跑 `deploy-staging`（可 `workflow_dispatch`，或在 main 推一個 commit 觸發 CI→deploy），確認全綠且 STEP_SUMMARY 出現三個版本號，即可把本卡改 `done`、清理 worktree 與分支：
     ```bash
     git worktree remove "C:/Users/excal/Projects/Claude/soft-dev-team-260919-wt/T-0039"
     git branch -d task/T-0039-secret-pin-verify-retry
     git worktree prune
     ```
  3. **給 T-0040（plan-sd）**：06 §2 的服務帳號角色清單需補 `roles/secretmanager.viewer`（README 與 §6.9.1 已落地文字，§2 規格文字仍待同步）；§7「三處必須同步的參數表」的「機密」列與變更紀錄表仍寫 `:latest`，一併調整。
  4. **可選的小改善（非本卡範圍）**：GitHub annotation 有長度上限，本次修復指引後半段被截斷。若希望 annotation 一眼看完，可把 `::error::` 拆成兩則（第一則只寫 gcloud 原始訊息、第二則只寫修復指引），列入日後維運小卡即可。
  5. **寫進工作鐵則的候選**（建議 Leader 收工時採納）：「**離線驗證只能證明語法與邏輯，不能證明權限**——凡新增需要雲端 API 呼叫的 CI 步驟，驗收必須額外比對『實際執行身分的角色 → 該角色 includedPermissions → 呼叫所需權限』，本機擁有者帳號成功不得作為 CI 會成功的證據。」（2026-09-20，T-0039 r1／r2 連兩輪紅燈於同一步驟）
- 需要 Leader 裁決的事：
  - **核心爭議**：T-0039 的程式碼已可上線且已合併進 main，但 staging **無法部署新版本**，因為部署服務帳號缺一個唯讀角色（`roles/secretmanager.viewer`）。這不是程式問題，是使用者雲端專案的 IAM 設定問題，agent 依安全鐵則不代為執行。既有 revision 仍正常服務，**不影響目前 staging 可用性與 Gate 2 的既有測試結果**，但在解除前任何新 commit 都進不了 staging。
  - **證據**：run `35507519302` 第 8 步 `PERMISSION_DENIED: Permission 'secretmanager.versions.list' denied`；`gcloud projects get-iam-policy` 顯示服務帳號只有四個角色；`gcloud iam roles describe` 顯示 `secretmanager.secretAccessor` 不含 `versions.list`、`secretmanager.viewer` 才含。
  - **選項**：(a) 請使用者補授 `roles/secretmanager.viewer`／代價：需使用者操作一次 GCP，約 1 分鐘；(b) 請使用者填三個 `SECRET_VERSION_*` repository variables／代價：需使用者操作一次 GitHub，且日後輪換帳密要手動改；(c) 退回本卡改用 `:latest`／代價：直接推翻使用者 Gate 2 r2 裁決③ A 與 06 §6.7 建議一，且重新踩回 T-0027 的「哪個 revision 讀到哪個版本不透明」問題。
  - **我的建議**：(a)。一次設定、之後輪換帳密自動生效，且本來就是 06 §6.7 建議一該有的權限基線。
  - **推翻成本**：低且可回頭。(a) 若使用者不願擴權，隨時可改走 (b)，不需改任何程式碼；(c) 需另開規格變更請求，成本最高，不建議。
