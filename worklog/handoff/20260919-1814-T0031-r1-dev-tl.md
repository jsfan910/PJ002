# T-0031 r1 — dev-tl — 審核與合併紀錄

<!-- 檔名：worklog/handoff/yyyymmdd-HHmm-T0031-r1-dev-tl.md（時間 = 開工時間，取實查系統時間） -->

## A. 開工紀錄

- 開工時間：2026-09-19T18:14:01+08:00
- 角色／模型：dev-tl / opus
- 任務卡：tasks/T-0031-monitor-ops.md（round 1，status: review）
- 工作內容：審核 T-0031（依 `.claude/skills/review-round/SKILL.md`），通過則於根目錄 main 合併
- 讀取的輸入：
  - `.claude/agents/dev-tl.md`、`.claude/skills/review-round/SKILL.md` — 角色與審核流程
  - `tasks/T-0031-monitor-ops.md`（根目錄）— acceptance 六條、驗收方式
  - `tasks/E-001-todo-app.md` 最後兩段 — 裁決 B／C、17:47:01 與 18:11:54 兩則紀錄
  - worktree `worklog/handoff/20260919-1750-T0031-r1-dev-ops.md` — A／B 段全文
  - worktree `docs/reports/20260919-1804-監測補強-E001.md` — 七節全文
  - worktree `docs/specs/06_部署架構與CICD.md` §6.1／§6.2／§6.8（新增）、`infra/uptime-check.sh`、`scripts/measure-deploy-downtime.sh`
- 本輪計畫：
  1. 實跑 gcloud 確認 uptime check 存在且參數如報告
  2. 重跑 06 §6.8.3 判讀指令一次，貼真實輸出
  3. 覆核 cron 診斷證據（GitHub API schedule run 數）
  4. 腳本可執行性（`bash -n`）、變更範圍（`git diff main...HEAD --stat`）、憑證掃描、06 僅動第 6 章
  5. 逐條判定 acceptance 六條，通過則合併、移除 worktree、更新 CHANGELOG、推送

## B. 交接

- 完工時間／狀態：2026-09-19T18:20:00+08:00 / done（已合併 main，merge commit `f0c05dd`）
- 判定：**r1 通過（done）**，acceptance 六條全數通過；合併後由 dev-tl 另開一個修正 commit 訂正 06 §6.8.4 一處貼錯的部署輸出（非阻擋級，見「假設與決策」）。

### 逐條 acceptance 判定（全部實跑，輸出如下）

**① GitHub cron 診斷：列出可能原因與證據，並記錄診斷當下 schedule 事件的 run 數 — 通過**

dev-tl 覆核實跑（2026-09-19T18:13+08:00，比 dev-ops 診斷時間晚 12 分鐘）：

```bash
curl -sS "https://api.github.com/repos/jsfan910/PJ002/actions/workflows/monitor-health.yml/runs?per_page=50" \
  | grep -oE "\"total_count\": *[0-9]+|\"event\": *\"[a-z_]+\"|\"created_at\": *\"[0-9T:Z-]+\""
```
```text
"total_count": 1
"event": "workflow_dispatch"
"created_at": "2026-09-19T08:46:26Z"
```

即：唯一的 1 次 run 是 `workflow_dispatch`（16:46:26+08:00 手動觸發），`event=schedule` 仍為 **0 次**；workflow 物件 `created_at=2026-09-19T07:49:44Z` 起算已逾 2 小時 24 分。dev-ops 在 06 §6.8.1 對四類可能原因（預設分支 `main`／workflow 路徑與 `state=active`／repo 公開且 `pushed_at` 極近／cron 語法 `*/5 * * * *`）逐項附 API 輸出排除，結論「無可修設定缺陷、判為 GitHub 對新註冊 workflow 的排程啟動延遲、不改 workflow」**接受**——四項證據齊備，且未做無證據支持的猜測性修改，符合任務卡 outputs 對 `monitor-health.yml`「僅在診斷出可修的設定問題時改」的條件。

**② Cloud Monitoring uptime check 建立（/health、5 分鐘、10 秒逾時、≥3 個位置、期望 200、記 check id 與建立時間）— 通過**

```bash
"C:/Users/excal/AppData/Local/Google/Cloud SDK/google-cloud-sdk/bin/gcloud.cmd" monitoring uptime list-configs \
  --project pj002-509106 \
  --format="table(name,displayName,period,timeout,selectedRegions.list(),httpCheck.path,httpCheck.port,httpCheck.useSsl,httpCheck.acceptedResponseStatusCodes.list(),monitoredResource.labels.host)"
```
```text
NAME                                                                  DISPLAY_NAME     PERIOD  TIMEOUT  SELECTED_REGIONS                         PATH     PORT  USE_SSL  ACCEPTED_RESPONSE_STATUS_CODES  HOST
projects/pj002-509106/uptimeCheckConfigs/todo-app-health-aMAlP5dfKv0  todo-app-health  300s    10s      ASIA_PACIFIC,USA_OREGON,USA_IOWA,EUROPE  /health  443   True     {'statusValue': 200}            todo-app-dpevsdhdva-de.a.run.app
```

六項參數與報告第 3 節、06 §6.8.2 完全一致：目標主機 `todo-app-dpevsdhdva-de.a.run.app` ＋ `/health` ＋ 443 ＋ SSL（＝`https://…/health`）✓、`period=300s`（5 分鐘）✓、`timeout=10s`✓、4 個檢查位置（≥ 3）✓、期望狀態碼 200 ✓、未設 `--username/--password`（不帶憑證）✓。check id `todo-app-health-aMAlP5dfKv0`、建立時間 `2026-09-19T09:55:07Z`＝`17:55:07+08:00` 已記於報告與 06；建立時間與下述首批樣本落在 09:59:20Z（建立後約 4 分鐘，符合 5 分鐘週期）互相佐證。

腳本可重跑性（任務卡 outputs 要求「可重跑」）：

```bash
bash -n infra/uptime-check.sh ; bash -n scripts/measure-deploy-downtime.sh
bash infra/uptime-check.sh list
bash infra/uptime-check.sh describe
```
```text
uptime-check.sh: syntax OK
measure-deploy-downtime.sh: syntax OK
NAME                                                                  DISPLAY_NAME     PERIOD  TIMEOUT  SELECTED_REGIONS
projects/pj002-509106/uptimeCheckConfigs/todo-app-health-aMAlP5dfKv0  todo-app-health  300s    10s      ASIA_PACIFIC,USA_OREGON,USA_IOWA,EUROPE
        "statusValue": 200
    "path": "/health",
      "host": "todo-app-dpevsdhdva-de.a.run.app",
  "name": "projects/pj002-509106/uptimeCheckConfigs/todo-app-health-aMAlP5dfKv0",
  "period": "300s",
  "timeout": "10s"
```

`list`／`describe` 兩個子命令實跑成功；`create` 子命令設計為「印出指令＋警告已建立過」，避免重跑產生第二個同名 check，接受此設計。

**③ 判讀指令：取 24 小時內成功率並實跑一次貼輸出 — 通過**

dev-tl 依 06 §6.8.3 原文指令重跑（僅把輸出改為統計，未改 filter 與 interval）：

```bash
GCLOUD="C:/Users/excal/AppData/Local/Google/Cloud SDK/google-cloud-sdk/bin/gcloud.cmd"
TOKEN=$("$GCLOUD" auth print-access-token)
END=$(date -u "+%Y-%m-%dT%H:%M:%SZ"); START=$(date -u -d "-24 hours" "+%Y-%m-%dT%H:%M:%SZ")
FILTER='metric.type="monitoring.googleapis.com/uptime_check/check_passed" AND metric.label.check_id="todo-app-health-aMAlP5dfKv0"'
curl -sS -G "https://monitoring.googleapis.com/v3/projects/pj002-509106/timeSeries" \
  -H "Authorization: Bearer ${TOKEN}" \
  --data-urlencode "filter=${FILTER}" --data-urlencode "interval.startTime=${START}" \
  --data-urlencode "interval.endTime=${END}" --data-urlencode "view=FULL"
```
```text
== window: 2026-09-18T10:13:29Z ~ 2026-09-19T10:13:29Z ==
== per-location counts ==
      1 "checker_location": "apac-singapore"
      1 "checker_location": "eur-belgium"
      1 "checker_location": "usa-iowa"
      1 "checker_location": "usa-oregon"
== boolValue tally ==
     70 "boolValue": true
== total points ==
70
```

指令可用且結果可統計：本窗 **70/70 ＝ 100%**（dev-ops 18:01 實跑時為 14/14，18 分鐘後增為 70 點，且 `apac-singapore` 已開始供樣——證實 dev-ops「asia-pacific 初期無資料屬正常」的判斷）。樣本數仍遠低於 Gate 2 的 288，正式判讀時間 `2026-09-20T17:55:07+08:00` 之後，此點 06 §6.2／§6.8.3 與報告第 6 節皆已寫明。

**④ 06 第 6 章更新（主要來源改 uptime check、cron 降備援、起算時間寫明）— 通過**

```bash
git diff main...HEAD -U0 -- docs/specs/06_部署架構與CICD.md | grep -E "^@@"
```
```text
@@ -9 +9 @@                 → frontmatter updated
@@ -265 +265 @@             → §6.1 導言（兩層改三層）
@@ -269 +269,2 @@           → §6.1 表格（正式量測列改 uptime check，新增備援列）
@@ -285,0 +287 @@           → §6.2 起算時間變更說明
@@ -326,0 +329,172 @@         → 新增 §6.8（六大段：cron 診斷／建立紀錄／判讀指令／TC-080-090 量測）
@@ -358,0 +533 @@           → 變更紀錄表加一列
```

第 6 章範圍為第 261～502 行（`## 6.` 至 `## 7.` 之前），所有內容變更均落在此範圍內；另兩處為 frontmatter `updated` 與文末變更紀錄表加列，屬每次修訂的必要同步，**不算越界**。實質內容核對：§6.1 表格「服務存活（正式量測，T-0031 起）」已改為 uptime check 並附完整參數，新增一列「服務存活（備援來源）」標明 `monitor-health.yml` 降級原因且註明「僅供交叉比對，不作為 Gate 判準」；§6.2 明寫起算由 `17:07:43+08:00` 改為 `17:55:07+08:00` 並附理由；§6.8.3 給出判讀規則（含 `checker_location` 維度區分單一地區網路問題與服務異常）。與 Leader 裁決 B 一致。

**⑤ TC-080：部署期間每 1 秒輪詢 /health 記錄不可用秒數 — 通過**

dev-ops 以 `gcloud run services update --update-env-vars DEPLOY_PROBE_T0031=…` 觸發新 revision 並同步量測，134 樣本全 200、最長連續不可用 0 秒。dev-tl 覆核該部署確實發生：

```bash
"…/gcloud.cmd" run revisions list --service todo-app --region asia-east1 --project pj002-509106 \
  --format="table(metadata.name,status.conditions[0].lastTransitionTime,spec.containers[0].env.len())"
```
```text
NAME                LAST_TRANSITION_TIME         ENV
todo-app-00010-dr7  2026-09-19T10:08:46.484179Z  6
todo-app-00009-cg7  2026-09-19T10:00:11.674475Z  6
todo-app-00008-kcs  2026-09-19T09:59:46.414312Z  7
todo-app-00007-jlz  2026-09-19T09:51:31.479844Z  6
todo-app-00006-jmx  2026-09-19T09:40:11.654990Z  6
…（00005-kld 08:49:37／00004-7p5 08:49:03／00003-lt2 08:33:27／00002-sn7 08:33:28／00001-tfq 08:29:11）
```

`todo-app-00008-kcs` 於 `09:59:46Z` 產生且**環境變數為 7 個**（其餘 revision 皆 6 個）——多出來的正是量測用的 `DEPLOY_PROBE_T0031`，證實「量測窗 09:59:17Z～10:03:16Z 內確實發生了一次真實的 revision 切換」，量測不是空跑。並行的 `todo-app-00009-cg7`（10:00:11Z，env 6）確為他卡操作，dev-ops 已在報告與 §6.8.4 誠實註記。0 秒不可用 < 60 秒門檻，通過；TC-080 資料層 id 比對不在本卡 acceptance 文字範圍內（見「假設與決策」第 2 條）。

**⑥ 不在任何檔案寫入憑證 — 通過（零真實命中）**

```bash
git diff main...HEAD | grep -nEi "password|passwd|secret|token|api[_-]?key|BEGIN [A-Z ]*PRIVATE KEY|AIza[0-9A-Za-z_-]{20,}|ya29\.[0-9A-Za-z_-]+|postgres(ql)?://[^ ]+:[^ @]+@"
```
```text
（命中 13 行，逐行判讀後全為非憑證）
- TOKEN=$("$GCLOUD" auth print-access-token) / -H "Authorization: Bearer ${TOKEN}"（×2 組）
  → 執行期以 shell 變數取短期 token，檔案中不含任何字面值
- 其餘皆為敘述文字：「不讀取憑證明文」「Secret Manager 參照」「STAGING_BASIC_AUTH_USER/PASSWORD secrets」（僅變數名）
- 無私鑰、無 API key、無連線字串、無 Basic Auth 帳密字面值
```

另：`--update-env-vars DEPLOY_PROBE_T0031=2026-09-19T09:59:43Z` 寫入 Cloud Run 的值為時間戳非機密，且該變數已隨後續 revision（00009／00010，env=6）自然消失，無殘留。

### 變更範圍檢查

```bash
git diff main...HEAD --stat
```
```text
 docs/reports/20260919-1804-監測補強-E001.md       | 150 ++++++++++++++++++
 docs/specs/06_部署架構與CICD.md                   | 181 +++++++++++++++++++++-
 infra/uptime-check.sh                             |  71 +++++++++
 scripts/measure-deploy-downtime.sh                |  57 +++++++
 tasks/T-0031-monitor-ops.md                       |   4 +-
 worklog/handoff/20260919-1750-T0031-r1-dev-ops.md |  78 ++++++++++
 6 files changed, 536 insertions(+), 5 deletions(-)
```

六個檔案全部落在任務卡 outputs ＋ 任務卡本身 ＋ 交接檔，**無越界**。`scripts/measure-deploy-downtime.sh` 未列在 outputs 字面清單內，但屬 acceptance 第 5 條（TC-080 每秒輪詢）的必要實作且為新增檔案、不與任何他卡 outputs 重疊，**接受**。`.github/workflows/monitor-health.yml` 未動（條件未成立），正確。

### 對 dev-ops「假設與決策」逐條表態

1. **不修改 `monitor-health.yml`** — **接受**。四項證據（分支／路徑與狀態／repo 活動／cron 語法）皆實查排除，無可歸因的設定缺陷；在沒有根因的情況下改 cron 偏移屬猜測性修改，且無法解釋 26 次排程視窗全數落空。符合角色規範「不做無證據支持的變更」。
2. **TC-080 範圍限定為 `/health` 輪詢** — **接受**。任務卡 acceptance 第 5 條原文即為「以每 1 秒輪詢 /health 記錄不可用秒數」，dev-ops 未擴大也未縮小；資料層 id 集合比對屬 `docs/specs/20_測試案例.md` TC-080 的完整步驟，需 Basic Auth 憑證明文，依第 6 條「不在任何檔案寫入憑證／不讀取憑證明文」本就不該由本卡執行。已列追蹤項交下一輪有憑證的測試卡（Leader 裁決 C 的追認結論不受影響）。
3. **量測改以 `gcloud run services update` 觸發而非等 dev-tl 推送** — **接受**。本卡在 review 階段無法預期合併時間，先取得真實量測輸出是對的；且 dev-tl 已用 revision 列表驗證該次部署真實存在（00008-kcs，env=7）。
4. **06 版本號不變（0.2），僅在變更紀錄表加列** — **接受**。本次屬第 6 章實作紀錄補寫（延續 6.4～6.7 慣例），未更動架構決策或流程；06 `status: frozen` 的規格變更須走「規格變更請求」卡，本次不觸發。NFR-003「主要來源」的改變是 Leader 裁決 B 的執行結果，非設計書決策的自行變更。

### 發現的問題（非阻擋級，dev-tl 於合併後以修正 commit 訂正）

- **06 §6.8.4 貼出的部署輸出有一行張冠李戴**：`Service [todo-app] revision [todo-app-00003-lt2] has been deployed and is serving 100 percent of traffic.`，但同節下一行寫新 revision 是 `todo-app-00008-kcs`。實查 revision 列表，`00003-lt2` 的 `lastTransitionTime` 是 `2026-09-19T08:33:27Z`（T-0027 回滾演練時期），與本次量測窗（09:59Z）無關，該行顯然是誤貼舊輸出。同段 `00008-kcs` 的 `lastTransitionTime` 寫 `…09:59:46.414475Z`，實際為 `…09:59:46.414312Z`（`475` 取自 `00009-cg7` 的 `…10:00:11.674475Z`）。
- 判定：**不構成阻擋級**——結論（0 秒不可用）與其他證據（134/134 回 200、00008-kcs env=7 確實產生）皆成立且經 dev-tl 獨立覆核，錯的只是貼上的字串；但 06 是規格文件，留著兩處與實況相反的證據會變成下一個人的假事實（T-0027 合併時已發生過同類問題）。依專案慣例「打錯就再開一個修正 commit」，由 dev-tl 於合併後訂正，不退回重跑 r2。
- 報告 `docs/reports/20260919-1804-監測補強-E001.md` 第 5 節**沒有**這行誤貼（只寫 `Deploying… Creating Revision…done. Done.` ＋新 revision `todo-app-00008-kcs`），故僅需修 06。

### 合併紀錄

```bash
git merge --no-ff task/T-0031-monitor-ops -F <訊息檔>
git worktree remove "C:/Users/excal/Projects/Claude/soft-dev-team-260919-wt/T-0031"
git branch -d task/T-0031-monitor-ops
```
```text
Merge made by the 'ort' strategy.
 docs/reports/20260919-1804-監測補強-E001.md       | 150 ++++++++++++++++++
 docs/specs/06_部署架構與CICD.md                   | 181 +++++++++++++++++++++-
 infra/uptime-check.sh                             |  71 +++++++++
 scripts/measure-deploy-downtime.sh                |  57 +++++++
 tasks/T-0031-monitor-ops.md                       |   4 +-
 worklog/handoff/20260919-1750-T0031-r1-dev-ops.md |  78 ++++++++++
 6 files changed, 536 insertions(+), 5 deletions(-)
Deleted branch task/T-0031-monitor-ops (was 5cd744c).
```

（任務卡無衝突：main 上的 T-0031 卡自建立後未再變動，分支的 `status: review` 直接併入，再由本次收尾 commit 改為 `done`。）

合併後收尾：任務卡 `status: done`／`updated: 2026-09-19T18:20:00+08:00`／審核紀錄表加一列；`CHANGELOG.md` 加一行；06 §6.8.4 訂正；本檔 B 段；`git push origin main`（觸發 `deploy-staging`，屬預期）。

### 推送紀錄（本檔完成後補記）

```bash
git push origin main
```
```text
To https://github.com/jsfan910/PJ002.git
   c4d9e52..4e69112  main -> main
```

推送後 GitHub Actions 立即起跑（實查 `/actions/runs`）：`CI`（head_sha `4e691122…`）與 `QA Tests (T-0021)` 皆 `in_progress`；`Deploy Staging` 走 `workflow_run` 監聽 CI 綠燈後觸發，屬預期行為，dev-tl 未等待其完成（本卡 acceptance 不含此次部署的結果，且 TC-080 已於 §6.8.4 以等效的 revision 切換量測完成）。**若 CI 或 Deploy Staging 轉紅，請以獨立維運卡處理，與本卡判定無關。**另留意：本次推送含 Leader 於合併期間寫入的 `815a8f0`／`01e1cbe` 兩個 commit（E-002 Phase 2 任務卡與看板去重），非本卡產物，一併推出。

推送後 5 分鐘內可順帶觀察 `monitor-health.yml` 是否因本次推送而開始自動觸發（若有，依 dev-ops 建議於 06 §6.8.1 補一行佐證「首次排程延遲」的診斷）。

- 產出物（路徑）：
  - `worklog/handoff/20260919-1814-T0031-r1-dev-tl.md`（本檔）
  - `tasks/T-0031-monitor-ops.md`（status done、審核紀錄表）
  - `CHANGELOG.md`（T-0031 一行）
  - `docs/specs/06_部署架構與CICD.md`（§6.8.4 兩處證據訂正，合併後修正 commit）
- 未完成／已知問題：
  - NFR-003 的 24 小時正式判讀未到時間：**判讀時間點 2026-09-20T17:55:07+08:00 之後**，由 qa-lead 重跑 06 §6.8.3 指令統計（本檔②③已證實指令可用、樣本正在累積，18:13 時 70/70）。
  - TC-080 資料層 id 集合直接比對仍未做（需憑證），待下一輪測試卡。
  - `monitor-health.yml` 的 cron 至本檔完成時仍 0 次自動觸發；本次 push main 之後若開始觸發，可回頭在 06 §6.8.1 補一行佐證。
- 我做的假設與決策（含理由）：
  - **以「修正 commit」而非 rework r2 處理 06 §6.8.4 的誤貼輸出**：錯誤僅是貼錯字串、不影響任何結論，且 dev-tl 已獨立取得正確證據可直接訂正；退回一輪的成本高於收益，也會延後 NFR-003 的採樣判讀時程。訂正內容與來源（`gcloud run revisions list` 實查輸出）寫在修正 commit 訊息與 06 該處。
  - **`scripts/measure-deploy-downtime.sh` 不在 outputs 字面清單仍接受**：它是 acceptance 第 5 條的必要實作，新增檔、不與他卡重疊。
  - **不在合併前後另跑一次 TC-080 量測**：任務卡標為「非必要」；本次 push 觸發的 `deploy-staging` 與 dev-ops 已量測的 revision 切換機制完全相同（Cloud Run startup probe 通過才切流量），再量一次不會產生新資訊，且會與其他卡的 staging 操作互相干擾（本卡量測期間已觀察到他卡的並行 revision）。
- 下一步建議：
  - qa-lead：2026-09-20T17:55:07+08:00 後重跑 06 §6.8.3 判讀指令，統計完整 24 小時窗，判定 Gate 2 第 6 項（門檻 288 次採樣成功率 ≥ 99%；注意分母為「各檢查地區資料點總數」，4 個地區 × 288 ≈ 1152 點，成功率以 `true` 佔比計）。
  - 下一輪測試卡：補 TC-080 資料層 id 集合比對（qa-at 以既有 `STAGING_BASIC_AUTH_USER/PASSWORD` secrets 執行）。
  - 維運改善小卡（低優先）：cron 分鐘偏移、Secret Manager 參照釘版本、verify 失敗自動重試（06 §6.7 兩項建議）——三項可併成一張卡。
- 需要 Leader 裁決的事：
  - 無。
