# T-0004 r1 — plan-sa — 工作紀錄與交接（審核 T-0004）

<!-- 檔名：worklog/handoff/20260919-0628-T0004-r1-plan-sa.md（開工時間，實查系統時間 date "+%Y%m%d-%H%M"） -->

## A. 開工紀錄

- 開工時間：2026-09-19T06:28:42+08:00
- 角色／模型：plan-sa / Claude Opus 5（1M context）
- 任務卡：tasks/T-0004-sd.md（round 1）
- **本次工作性質：審核 T-0004**（依 `.claude/skills/review-round/SKILL.md`）。本角色為該卡 `reviewer`，非執行者；不改任何 specs，只改任務卡 frontmatter／審核紀錄表與本檔。
- 開工前置檢查：`tasks/T-0004-sd.md` frontmatter `status: review`、`reviewer: plan-sa`、`round: 1`，與本角色相符，可審。
- 讀取的輸入（實際讀過的檔案與段落）：
  - `.claude/agents/plan-sa.md`（全檔）—— 角色定義與通用協定；`.claude/skills/review-round/SKILL.md`（全檔）—— 七步審核程序，第 3 步明載「審核者不採信交接檔的『通過』二字，只採信自己重跑得到的輸出」。
  - `tasks/T-0004-sd.md`（全檔）—— 6 條 acceptance、8 項 outputs、「驗收方式」段（核對每個 UC 有模組與端點、每條 BR 有落點、實跑 lint、檢查 ADR 選項）。
  - `worklog/handoff/20260919-0603-T0004-r1-plan-sd.md`（全檔，A 段與 B 段）—— 12 項假設與決策、6 條 acceptance 自驗輸出、給我的三個指定表態項。
  - `docs/specs/02_系統分析書_SA.md`（全檔 739 行，本角色自著）—— 第 2 章 14 則 UC、第 3 章領域模型與不變量 I-1~I-5、**第 5 章 32 條 BR**、第 6 章外部介面、**第 7 章 O-001~O-010**、附錄 A／B。
  - `docs/specs/01_需求規格書_SRS.md`（第 5 章外部介面、第 223~229 行 NFR-001／003／007 定義與量測方式）—— 用於核對 SD 第 7 章有無擅改 NFR 門檻。
  - `docs/specs/03_系統設計書_SD.md`（全檔 408 行，九章）、`docs/specs/04_API規格.yaml`（全檔 830 行）、`docs/specs/05_資料庫設計.md`（第 2、3 章）、`docs/specs/06_部署架構與CICD.md`（全檔 187 行）、`docs/specs/adr/ADR-0001~0004`（結構與選項表）、`docs/specs/traceability.md`（全檔）。
  - `tasks/E-001-todo-app.md#限制`（四條硬約束原文）—— 用於核對 acceptance 6。
  - `docs/templates/worklog_handoff.md` —— 本檔格式。
- 上一輪交接摘要（若 round > 1）：無（round 1）。
- 本輪計畫（3–7 條）：
  1. 實際重跑 plan-sd 交接檔 B 段的每一條驗證指令（含 `npx @redocly/cli lint`），貼真實輸出，不採信其「通過」二字。
  2. 逐條判定 6 條 acceptance，每條附自己跑出來的證據。
  3. 依任務卡「驗收方式」抽驗：每個 UC 有模組與端點；**抽驗至少 10 條 BR 的實際落點**（不只看 grep 命中，要看命中處寫了什麼）。
  4. 核對每個 NFR 有設計對策與驗證方式，並比對 SRS 原文確認門檻未被擅改。
  5. 核對 ADR 每份至少 2 個選項且有代價；核對技術選型對得上 Epic 四條硬約束。
  6. 核對設計硬約束七項（Basic Auth 擋整站／`/health` 唯一豁免、同源、UUID、刪除 204、跨使用者 404、設定目標狀態、篩選在後端）。
  7. 對 plan-sd 交接檔的 12 項「假設與決策」逐條表態；判定 done／rework；更新任務卡與審核紀錄表；commit。
- 預期產出：
  - `worklog/handoff/20260919-0628-T0004-r1-plan-sa.md`（本檔）
  - `tasks/T-0004-sd.md`（frontmatter `status`／`updated`、審核紀錄表加一列）

## B. 交接

- 完工時間／狀態：2026-09-19T06:31:41+08:00 / done（T-0004 判定通過）
- 完成事項：
  - 重跑 plan-sd 交接檔 B 段全部驗證指令，輸出與其所貼一致（含 redocly lint 的 1 warning）。
  - 6 條 acceptance 逐條判定，**全部通過**。
  - 抽驗 **13 條 BR**（超過要求的 10 條）的實際落點內容，非只看 grep 命中。另重跑 32 條 BR 的全覆蓋掃描並改為**逐條列出落在哪幾份文件**（plan-sd 原本只輸出「未被引用者：無」，資訊量不足以審核）。
  - 核對 14 則 UC 的模組與端點對應、8 條 NFR 的設計對策與驗證方式、4 份 ADR 的選項數與代價欄、Epic 四條硬約束、設計硬約束七項。
  - 比對 SRS 第 223 行原文，確認 SD 第 7 章 NFR-001 的「建立／更新／刪除 P95 < 800ms」**是 SRS 原文就有的門檻**，非 plan-sd 擅自放寬。
  - 對 plan-sd 的 12 項假設與決策逐條表態（見下）。
  - 任務卡 `tasks/T-0004-sd.md`：`status: review` → `done`，`updated` 更新，審核紀錄表加一列。
- 產出物（路徑）：
  - `worklog/handoff/20260919-0628-T0004-r1-plan-sa.md`（本檔）
  - `tasks/T-0004-sd.md`（frontmatter ＋ 審核紀錄表）
  - **未改動任何 `docs/specs/*`**（本角色為審核者，非執行者）。
- 未完成／已知問題：
  - `traceability.md` 的 `TC-ID` 欄仍為 `—`，孤兒檢查後兩項未成立。**依分工正常**（測試團隊補），不構成 T-0004 不過。
  - 回滾程序尚未演練（`06_部署架構與CICD.md` §5.4）。**依階段正常**（staging 尚未存在），已明確指派 dev-ops 於 OPS-03 後執行，為 Gate 2 前置條件。
  - 四項小瑕疵，皆**不影響 Gate 1**，寫入「下一步建議」交 T-0005 或 dev-tl，不退回。
- 我做的假設與決策（含理由）：
  - **判定標準採「acceptance ＋ 任務卡驗收方式」，不擴充**。理由：審核者加碼自己的偏好等於偷改驗收標準；SD 第 9 章五項開放問題中，Leader 已對 SD-03／SD-05 表態、SD-01／SD-02 屬 P1、SD-04 已明列風險，皆無阻擋 Gate 1 的成分。
  - **redocly 的 1 個 warning 不判為不通過**。理由：acceptance 1 的字面是「通過 lint」，實跑結果為 0 errors、exit code 0、`Your API description is valid`；`operation-4xx-response` 指 `GET /health` 無 4XX，而該端點無認證、無參數、不查資料庫，**不存在任何 4XX 情境**。為消 warning 而虛構一個不會發生的 4XX 回應會讓契約說謊。**接受 plan-sd 的保留決定。**
  - **UC-014 未出現在 SD 第 5 章端點表與 `04_API規格.yaml`，仍判 acceptance 2 通過**。理由：UC-014 是**橫切授權**而非獨立端點，其模組落點為 BE-05／BE-13（SD §3.1），端點落點由 `traceability.md` US-013 列明為「全部 `/api/v1/todos*` 端點」，且 SD §5 第 4 點與 §6.2 寫明實作方式（單一查詢同時完成授權與存在性檢查）。「至少一個模組與端點」的要求已滿足；缺的是一行標註，非缺設計。列為小瑕疵。
  - **對交接檔「假設與決策」的逐條表態**（12 項）：
    1. 多寫 ADR-0004（認證與同源部署）—— **接受**。Q-008／O-004／O-005 三者確有交互作用（不豁免則 NFR-003 永不通過；跨來源則預檢不帶 `Authorization` 會被 Basic Auth 擋成 401），拆散寫於三處讀者不會發現。這正是我在 SA 第 7 章 O-004／O-005 標「設計不當會使前端在 staging 上完全無法呼叫 API，屬阻擋級」的顧慮被正面處理。
    2. 選 Fastify 而非 Express —— **接受**。ADR-0001 選項表列了 4 個後端選項，缺點與成本欄齊全；訓練資料較少的代價已在「後果」承擔並要求開發卡附官方外掛名稱與版本。
    3. 選 Render ＋ Neon —— **接受**。ADR-0003 五個選項，淘汰理由（Fly.io／Cloud Run 建立帳號即需綁卡）直接對上 Epic「憑證與雲端帳號由使用者提供」。
    4. 不使用 ORM —— **接受**。代價（SQL injection 回到人）已以「禁止字串拼接 ＋ CR 以 repository 層有無樣板字串插值為單一判準」控制，判準可執行。
    5. `PATCH` 一個端點服務 UC-003 與 UC-005 —— **接受**。兩者都是「部分更新同一資源」；O-002「設定目標狀態」使其冪等，與我在 SA UC-005 的輸入定義相容。
    6. 採 `/api/v1` 前綴（SD-05）—— **接受，不視為擴大範圍**。與 Leader 表態一致：Epic 明載「將來可能接手機 App」，現在零成本。
    7. 排序 tie-break 收斂為 `created_at DESC, id DESC` —— **接受**。SA BR-005 只說「由新到舊」，同秒碰撞時無定序，這是對 BR-005 的**補強而非改寫**，且 `05_*.md` §2 索引與 `04_API規格.yaml` 的 `listTodos` description 兩處一致。
    8. AC-009-5 改以規範意圖驗證（兩條判準）—— **接受**。該 AC 的 Given 前提「部署於不同來源」在 O-004 同源裁決下確實不成立，但 BR-015 的規範意圖（不得萬用字元 CORS）仍須被驗；兩條判準（無 `Access-Control-Allow-Origin` 標頭、無跨來源預檢）可執行且已寫入 traceability「特殊驗證方式的 AC」表。**補充要求**：`CORS_ALLOWED_ORIGINS` 這個預留變數是唯一的回退風險，`06_*.md` §4.1 已寫「永遠不得填入 `*`」，足夠。
    9. `E_EMAIL_TAKEN` 採 409（SD-03）—— **接受**。與 Leader 表態一致：AC-009-3 的實質要求是語意正確，409 Conflict 對重複 Email 最準；屬 P1，Gate 1 不受影響。
    10. `/health` 不查資料庫 —— **接受**，且認為是本卡最好的設計判斷之一。它回答「程序活著」而非「整個系統健康」；若查資料庫，Neon 抖動會讓 Render 判定實例死亡而重啟，**反而降低** NFR-003。與 SA BR-017「不得回傳任何業務資料或內部細節」一致。
    11. `002` migration 含 `TRUNCATE TABLE todos;` —— **接受**。BR-032／Q-011 裁決「導入 P1 時直接清空」，SA 確實未指定執行者與時機；綁進同一個 migration 使其不可能被遺漏，且分開執行會讓 `ALTER ... SET NOT NULL` 失敗。屬 P1。
    12. `users.email` 用 `citext` —— **接受**。SA BR-024 只說「全系統唯一」，未指定大小寫敏感度；不做大小寫不敏感則唯一性可被 `Alice@` / `alice@` 繞過。此為對 BR-024 的保守補強，不改變裁決值。
    13. 建議倉庫設為公開以解 GitHub Actions 額度 —— **接受作為建議**（`06_*.md` §2 已給二選一並交 dev-ops 決定並記錄）。本角色不代 dev-ops 決定。
    14. 不改 SRS 與 SA —— **接受**。本卡 outputs 不含這兩份；O-009 的 NFR-003 量測期改寫由 plan-ba 於 T-0005 執行，與 Leader 對 O-009 的指派一致。
- 驗證方式（接手者如何確認我做的是對的：指令 + 我實際得到的輸出）：

  **驗收 1／6 —— OpenAPI 通過 lint（實際重跑，未採用替代方案）**

  ```bash
  cd /c/Users/excal/Projects/Claude/soft-dev-team-260919
  node --version && npx --version
  npx --yes @redocly/cli@latest lint docs/specs/04_API規格.yaml 2>&1 | tail -20; echo "EXIT=${PIPESTATUS[0]}"
  ```
  ```text
  v24.15.0
  11.12.1
  [1] docs\specs\04_API規格.yaml:97:7 at #/paths/~1health/get/responses

  Operation must have at least one `4XX` response.

  95 |   - health
  96 | security: []
  97 | responses:
  98 |   "200":
  99 |     description: 服務存活。

  Warning was generated by the operation-4xx-response rule.

  Reference: https://redocly.com/docs/cli/rules/oas/operation-4xx-response

  docs\specs\04_API規格.yaml: validated in 53ms

  Woohoo! Your API description is valid. 🎉
  You have 1 warning.

  EXIT=0
  ```
  判讀：`npx` 可用且下載未逾 2 分鐘，**使用任務卡指定的首選指令，未採用替代檢查**。結果為 **0 errors、exit code 0、`Your API description is valid`**，與 plan-sd 所貼一致。唯一 warning 已確認是 `GET /health` 的設計本身所致。**通過。**

  **驗收 2／6 —— 每個 UC 對應至少一個模組與端點；每個 NFR 有設計對策與驗證方式**

  ```bash
  diff <(grep -oE "UC-0[0-9][0-9]" docs/specs/02_系統分析書_SA.md | sort -u) <(grep -oE "UC-0[0-9][0-9]" docs/specs/03_系統設計書_SD.md | sort -u) && echo "UC-DIFF-EMPTY-OK"
  awk "NR>=110 && NR<=168" docs/specs/03_系統設計書_SD.md | grep -oE "UC-0[0-9]{2}" | sort -u | tr "\n" " "   # 第 3 章模組表
  awk "NR>=184 && NR<=215" docs/specs/03_系統設計書_SD.md | grep -oE "UC-0[0-9]{2}" | sort -u | tr "\n" " "   # 第 5 章端點表
  sed -n "/^## 7. 非功能設計/,/^## 8\./p" docs/specs/03_系統設計書_SD.md | grep -cE "^\| \*\*NFR-0"
  ```
  ```text
  UC-DIFF-EMPTY-OK
  第 3 章模組表：UC-001 UC-002 UC-004 UC-006 UC-007 UC-008 UC-009 UC-010 UC-011 UC-012 UC-013 UC-014
  第 5 章端點表：UC-001 UC-002 UC-003 UC-004 UC-005 UC-006 UC-007 UC-008 UC-009 UC-010 UC-011 UC-012 UC-013
  8
  ```
  判讀：
  - SD 與 SA 的 UC 集合**完全一致**（無遺漏、無捏造）。
  - 第 3 章模組表未出現 UC-003／UC-005 的字面，**是範圍寫法所致**：`BE-04`／`BE-05`／`BE-06` 三列皆寫 `UC-001~UC-007`，涵蓋此二者。人工核對確認。
  - 第 5 章端點表未出現 UC-014，**經人工核對為橫切授權**：模組落點 BE-05／BE-13（SD §3.1），端點落點見 `traceability.md` US-013 列「全部 `/api/v1/todos*` 端點（JWT 授權橫切；跨使用者回 404，O-001）」，SD §5 第 4 點與 §6.2 另有實作規格。「至少一個模組與端點」成立。
  - NFR 表 8 列齊全，逐列人工核對「設計對策」與「驗證方式」兩欄皆非空、且驗證方式為**可執行的指令或可計數的程序**（如 `autocannon -c 10 -d 60`、`grep -rn "innerHTML" public/`、3×2=6 組逐組記錄、建立 10 筆後重新部署比對 `id` 與 `title`），非「會注意」這類空話。
  - **另比對 SRS 原文防擅改門檻**：`grep -n "NFR-001" docs/specs/01_需求規格書_SRS.md` → 第 223 行原文即為「清單查詢 P95 < 500ms；**建立／更新／刪除 API P95 < 800ms**」。SD 第 7 章的 800ms **是 SRS 自己的門檻，非 plan-sd 放寬**。
  
  **通過。**

  **驗收 2 附加 —— 32 條 BR 逐條列出落點（本角色改寫 plan-sd 的檢查，使輸出可被審核）**

  plan-sd 的原指令只輸出「未被引用的 BR：無」，無法判斷落點是否合理。改為逐條列出落在哪幾份文件：

  ```bash
  for n in $(seq -w 1 32); do hits=$(grep -o "BR-0$n" docs/specs/03_系統設計書_SD.md docs/specs/04_API規格.yaml docs/specs/05_資料庫設計.md docs/specs/06_部署架構與CICD.md docs/specs/adr/*.md docs/specs/traceability.md 2>/dev/null | cut -d: -f1 | sort -u | ...); printf "BR-0%s : %s\n" "$n" "${hits:-*** MISSING ***}"; done
  ```
  ```text
  BR-001 : SD API DB ADR0001      BR-017 : SD API OPS ADR0001 ADR0003 ADR0004 TRC
  BR-002 : SD API DB ADR0001      BR-018 : SD API DB OPS
  BR-003 : SD API DB              BR-019 : SD API DB
  BR-004 : SD API ADR0002         BR-020 : SD API ADR0004
  BR-005 : SD API DB ADR0002      BR-021 : SD API OPS
  BR-006 : SD API DB              BR-022 : SD API DB ADR0002
  BR-007 : SD API DB              BR-023 : SD API DB ADR0002
  BR-008 : SD API DB              BR-024 : SD API DB ADR0002
  BR-009 : SD API DB              BR-025 : API OPS ADR0003
  BR-010 : SD API ADR0001         BR-026 : SD API DB ADR0001 ADR0004
  BR-011 : SD API ADR0001         BR-027 : SD API TRC
  BR-012 : SD API ADR0001         BR-028 : SD API
  BR-013 : SD API DB ADR0001      BR-029 : SD
  BR-014 : SD API OPS ADR0001     BR-030 : SD
  BR-015 : API OPS ADR0004        BR-031 : SD
  BR-016 : SD API DB OPS ADR0001 ADR0003 ADR0004   BR-032 : SD DB OPS
  ```
  判讀：**32 條全部有落點，無 `*** MISSING ***`**。BR-029／BR-030／BR-031 只落在 SD 屬正常 —— 這三條是純前端行為規則（錯誤呈現、載入中指示、響應式相容性），落點就該是 FE-01／FE-04 模組與 NFR-004 對策，不應出現在 API 契約或 DB 設計中。

  **驗收 2 附加 —— 抽驗 13 條 BR 的落點內容（不只看命中，看命中處寫了什麼）**

  ```bash
  for b in BR-002 BR-005 BR-008 BR-009 BR-011 BR-012 BR-013 BR-017 BR-022 BR-023 BR-029 BR-031 BR-032; do
    echo "=== $b ==="; grep -n "$b" docs/specs/03_系統設計書_SD.md docs/specs/05_資料庫設計.md | head -4; done
  ```

  | BR | SA 原文要求 | 設計文件實際寫的落點 | 判定 |
  |---|---|---|---|
  | BR-002 | 標題上限 200，前後端皆驗 | SD:122 `BE-05`「BR-002（≤200）」；SD:229 `E_VALIDATION` 400；SD:277 API schema `maxLength: 200`；SD:278 業務層 `trim()` 後**再驗一次**；DB:129 `CHECK (char_length(btrim(title)) BETWEEN 1 AND 200)` | **落點正確，且三層都有**（schema／業務層／DB CHECK）。DB 用 `btrim` 而非直接比長度，正是 BR-001「全空白」的正解 |
  | BR-005 | `createdAt` 由新到舊 | SD:122 `BE-05`（排序）；SD:123 `BE-06` `ORDER BY created_at DESC, id DESC`；DB:77 索引 `(created_at DESC, id DESC)`；DB:135 SQL 註解 | **落點正確**，且補上 SA 未定的 tie-break |
  | BR-008 | 硬刪除 ＋ 二次確認 | SD:143 `FE-04`「刪除前二次確認（BR-008）」；SD:179「刻意不存在 `deleted_at`（硬刪除）」；DB:69「出現此欄位即與裁決不符」；DB:225 SQL「硬刪除，回 204；影響列數為 0 即回 404」 | **落點正確**，UI 責任與資料責任分開落點 |
  | BR-009 | 不存在的 id 一律 404，且不得建資料／不得系統錯誤 | SD:122 `BE-05`；SD:231 `E_NOT_FOUND` 404；DB:225「影響列數為 0 即回 404」；`04_API規格.yaml` `responses/NotFound` 明載「不得因此建立新資料、不得回 5xx」 | **落點正確**，含「影響列數為 0」這個實作級判準 |
  | BR-011 | 異動後以**目前生效篩選**重算清單 | SD:134「單向資料流是 BR-011 不被寫成散落 DOM 補丁的唯一保障」；SD:142 `FE-03`「任何異動後以目前 filter 重新向後端取清單並**整份替換**」；SD:163 WBS「必須一次做完才驗得出來」 | **落點正確且比 SA 更進一步** —— SA 只規定行為，SD 指定了使該行為不可能被寫錯的架構（整份替換而非局部修補） |
  | BR-012 | 僅 title 可編輯，夾帶其他欄位一律忽略 | SD:121 `BE-04` `removeAdditional: true`；SD:206 明寫「是『移除』不是『拒絕』，符合 BR-012 的**忽略**用語」 | **落點正確**，且正確辨析了「忽略」vs「拒絕」的語意差 |
  | BR-013 | 輸入顯示一律逸出 | SD:143 `FE-04`「一律 `textContent`，禁止 `innerHTML`」；SD:281 CR 判準 `grep -rn "innerHTML" public/` 應無輸出；DB:257 4xx `details` 不回射輸入原文 | **落點正確**，且給了可執行的 CR 判準 |
  | BR-017 | `/health` 豁免 Basic Auth 且不回業務資料 | SD:249~255 未保護路徑表 ＋「**以上為全部。未保護路徑只有這一條。**」；SD:259「必須完全相等字串比對，禁前綴／萬用字元／正則」；SD:309 NFR-003 對策①②；`04_API規格.yaml` `/health` `security: []` ＋ `HealthStatus` schema 註明刻意不含版本號／commit／DB 狀態 | **落點正確**，且落實了 Leader 對 O-005 的「明列」要求 |
  | BR-022 | 先以身分過濾**再**套狀態篩選（順序不可顛倒） | SD:122 `BE-05`（P1 追加）；DB:78 索引 `(owner_id, created_at DESC, id DESC)`「**前導欄位為 `owner_id`**」；DB:185 SQL 註解「前導欄位必須是 owner_id」 | **落點正確**，且把「順序不可顛倒」翻譯成索引前導欄位這個可被 CR 驗證的形式 |
  | BR-023 | 跨使用者存取一律拒絕，且該筆不得被改動 | SD:209「以 `WHERE id = $1 AND owner_id = $2` 查詢，0 列即拋 `NotFoundError` —— **同一條查詢同時完成授權與存在性檢查**，不存在『先查到再判斷權限』的中間狀態」；SD:231 `E_NOT_FOUND`；DB:228 同上 | **落點正確且優於 SA 要求** —— SA 只規定「不得被改動」，SD 用單一查詢使該狀態**在結構上不可能發生**，而非靠開發者記得 |
  | BR-029 | 請求失敗須顯示可讀訊息、不得凍結 | SD:143 `FE-04`「錯誤訊息（BR-029）」；SD:142 `FE-03` 狀態含 `error` | **落點正確**（純前端規則，只落 SD 屬正常） |
  | BR-031 | 3 瀏覽器 × 2 尺寸皆可操作 | SD:140 `FE-01`「響應式版面滿足 1280×800 與 390×844（BR-031）」；SD:310 NFR-004「單一 `@media` 斷點 768px、最小點擊區 44×44 px、不使用橫向捲動」＋ 驗證方式為 6 組逐組記錄 | **落點正確**，且把「無需橫向捲動才能點擊」翻成具體的 44×44 與斷點數值 |
  | BR-032 | 導入 P1 直接清空，不做歸屬遷移 | SD:404 SD-02 開放問題；DB:175~183 `002` migration 內 `TRUNCATE TABLE todos;` 與 `SET NOT NULL` 同檔，附註「分開執行會讓 ALTER 失敗或留下違反 BR-023 的孤兒列」；OPS `06_*.md` §1 環境表「P1 導入時一次性 `TRUNCATE`」 | **落點正確**，補上了 SA 未指定的執行者與時機 |

  結論：13 條抽驗**全部落點正確**，且其中 BR-011、BR-022、BR-023 三條的設計比 SA 的規定更進一步 —— 不是複述規則，而是選了使違反該規則在結構上不可能發生的做法。**通過。**

  **驗收 3／6 —— 至少 3 份 ADR，每份有背景、選項、決定、後果；每份至少 2 個選項且有代價**

  ```bash
  ls -1 docs/specs/adr/*.md | wc -l
  for f in docs/specs/adr/*.md; do printf "%s : " "$(basename $f)"; grep -c -E "^## (背景|選項|決定|後果)$" "$f"; done
  for f in docs/specs/adr/*.md; do echo "===== $f ====="; awk "/^\| \*\*[A-E]\.|^\| [A-E]\. /{print NR\": \"substr(\$0,1,110)}" "$f"; done
  grep -nE "^\| 選項 \| 優點 \| 缺點 \| 成本 \|" docs/specs/adr/*.md
  ```
  ```text
  4
  ADR-0001-語言框架.md : 4
  ADR-0002-資料庫.md : 4
  ADR-0003-雲端平台.md : 4
  ADR-0004-認證與同源部署.md : 4

  ADR-0001 後端：A. Node+TS+Fastify 5 / B. Node+TS+Express 5 / C. Python+FastAPI / D. Java 21+Spring Boot   （4 個）
  ADR-0001 前端：A. 原生 HTML+CSS+ES2020 / B. React+Vite / C. htmx+伺服器端樣板                              （3 個）
  ADR-0002 資料庫產品：A. PostgreSQL 16(Neon) / B. SQLite+持久磁碟 / C. MongoDB Atlas M0 / D. 平台自帶免費 PG （4 個）
  ADR-0002 存取方式：A. pg+參數化+純 SQL migration / B. Prisma ORM / C. TypeORM／Sequelize                    （3 個）
  ADR-0003：A. Render+Neon / B. Fly.io / C. Cloudflare Workers+D1 / D. Cloud Run+Neon / E. Vercel／Netlify    （5 個）
  ADR-0004 議題一（Basic Auth 實作層）：A. 應用層 hook / B. 反向代理層 / C. 平台內建保護                        （3 個）
  ADR-0004 議題二（來源關係）：A. 同源單一服務 / B. 跨來源（靜態託管＋Render）                                （2 個）

  （全部 7 張選項表的表頭皆為：| 選項 | 優點 | 缺點 | 成本 |）
  ```
  判讀：**4 份 ADR（超過要求的 3 份）**，每份四段齊全；**每份至少 2 個選項**（最少的 ADR-0004 議題二為 2 個，最多 ADR-0003 為 5 個）；**每張選項表都有「缺點」與「成本」欄**，即每個選項都有代價，非只列優點。人工抽讀確認缺點欄寫的是實質代價（例：Fastify「訓練資料較少」、SQLite「資料與運算同生命週期」、Fly.io「建立帳號即需綁卡」、Cloudflare Workers「執行環境不是標準 Node」），非敷衍字樣。**通過。**

  **驗收 4／6 —— 部署設計含環境清單、pipeline 階段、環境變數名稱清單（無值）、回滾程序草案**

  ```bash
  grep -nE "^## [0-9]\. " docs/specs/06_部署架構與CICD.md
  sed -n "/^## 4. 環境變數清單/,/^## 5\./p" docs/specs/06_部署架構與CICD.md | grep -cE "^\| \`[A-Z]"
  grep -nE "(PASSWORD|SECRET|DATABASE_URL|AUTH_USER)[[:space:]]*[:=][[:space:]]*[A-Za-z0-9]" docs/specs/06_部署架構與CICD.md
  ```
  ```text
  20:## 1. 環境清單
  34:## 2. 雲端資源清單（依 ADR-0003）
  52:## 3. Pipeline 階段（GitHub Actions）
  90:## 4. 環境變數清單（只列名稱與用途，值不入版控）
  125:## 5. 回滾程序（必須實際演練過一次，記錄日期與結果）
  161:## 6. 監控與告警
  15
  （機密賦值掃描：無任何輸出）
  ```
  判讀：模板六章齊全。環境清單三環境（dev／staging／prod 標明不建立並說明理由）。Pipeline 三個工作流共 9 個階段，逐階段有「觸發／做什麼／失敗時」。環境變數 **15 個**（執行期 10 ＋ CI/CD 5），全部只有名稱與用途，機密賦值正則掃描**無輸出**。回滾程序為三層（平台層 < 2 分鐘／`git revert` < 10 分鐘／資料庫層只前進不回退）＋ §5.4 演練要求並指派 dev-ops。人工核對確認 `06_*.md` §3.3 的監測**刻意不帶憑證**，理由寫明「若帶憑證就等於沒驗證 BR-017 的豁免是否生效」—— 這正是我在 SA UC-011 E3 的顧慮。**通過。**

  **驗收 5／6 —— SD 第 8 章目錄結構與分支策略完整；traceability.md 模組與 API 欄已填**

  ```bash
  grep -nE "^## [0-9]\. " docs/specs/03_系統設計書_SD.md
  awk -F"|" "/^\| US-0/ { gsub(/ /,\"\",\$6); gsub(/ /,\"\",\$7); if (\$6==\"—\"||\$6==\"\") a++; if (\$7==\"—\"||\$7==\"\") b++; n++ } END { print \"列數:\" n \" SD模組欄空或破折:\" a+0 \" API端點欄空或破折:\" b+0 }" docs/specs/traceability.md
  ```
  ```text
  36:## 1. 架構總覽    90:## 2. 技術選型    110:## 3. 模組設計    171:## 4. 資料模型
  184:## 5. API 設計摘要    217:## 6. 錯誤處理與安全設計    303:## 7. 非功能設計    318:## 8. 目錄結構與分支策略    399:## 9. 開放問題
  列數:26 SD模組欄空或破折:13 API端點欄空或破折:13
  ```
  判讀：SD 模板九章齊全。第 8 章含**逐檔標註模組編號的目錄樹**（每個檔案後面標 `# BE-xx`／`# FE-xx`／`# OPS-xx`）、**外掛註冊順序六步並註明「順序錯了會出安全漏洞，不可調換」**（`basic-auth` 必須早於 `static` 與 routes）、分支策略五項（`main` 受保護、`task/T-####-slug`、dev-tl 合併、commit 訊息格式、禁止 commit 清單）。
  **awk 的 13／13 需說明**：`^| US-0` 在本檔命中**兩張表** —— 主追溯矩陣 13 列與第 52 行「驗收條件對照」表 13 列（後者只有 3 欄，故 `$6`／`$7` 為空被計入）。**人工逐列核對主追溯矩陣（第 24~36 行）確認 13 列的 `SD 模組` 與 `API 端點` 兩欄全部有實值、無一為 `—`**（例：US-008 為「`GET /`、`GET /assets/*`（靜態頁；本 US 無專屬 API 端點）」，US-012 為「`POST /api/v1/auth/login`（登出為用戶端清除 JWT，**無端點**，O-006）」—— 兩者皆註明性質而非留白）。**通過。**

  **驗收 6／6 —— 技術選型符合 Epic 限制（免費額度、可離線驗證、Windows 可執行）**

  ```bash
  sed -n "/^## 限制/,/^## 成功指標/p" tasks/E-001-todo-app.md
  for k in "204" "UUID" "同源" "設定目標狀態" "唯一未保護路徑" "PowerShell" "GitHub Actions" "docker"; do printf "%-18s SD:%s API:%s DB:%s OPS:%s\n" "$k" "$(grep -c "$k" ...)" ...; done
  grep -nE "\bmake\b|\.sh\b|/bin/bash" docs/specs/03_系統設計書_SD.md docs/specs/06_部署架構與CICD.md docs/specs/adr/*.md
  ```
  ```text
  Epic 限制原文：
  - 技術棧由 plan-sd 決定並寫 ADR；優先選「免費額度可部署、憑證可由使用者自行設定、可離線驗證（本機 docker / IaC plan）」。CI/CD 用 GitHub Actions。
  - 開發機為 Windows 11；所有指令須可在 Git Bash 或 PowerShell 執行。
  - 憑證與雲端帳號由使用者提供，agent 不得索取或代填。
  - 規模刻意小：後端 3–5 張卡、前端 1–2 張、DevOps 1–2 張。

  204                SD:5 API:4 DB:1 OPS:0
  UUID               SD:3 API:4 DB:4 OPS:0
  同源               SD:7 API:2 DB:0 OPS:3
  設定目標狀態       SD:2 API:3 DB:1 OPS:0
  唯一未保護路徑     SD:3 API:3 DB:0 OPS:1
  PowerShell         SD:2 API:0 DB:0 OPS:0
  GitHub Actions     SD:5 API:0 DB:0 OPS:10
  docker             SD:4 API:2 DB:0 OPS:4

  Unix 專屬工具掃描（make / *.sh / /bin/bash）：無任何輸出
  ```

  | Epic 限制 | 設計是否滿足 | 我核對到的證據 |
  |---|---|---|
  | 免費額度可部署 | **是** | `06_*.md` §2 費用表逐列 US$0，合計 US$0；ADR-0003「Render Free **不需綁信用卡**」為淘汰 Fly.io／Cloud Run 的決定性理由 |
  | 憑證由使用者自行設定，agent 不索取不代填 | **是** | `06_*.md` §4 只列 15 個名稱與用途；`render.yaml` 全部 `sync: false`；機密賦值正則掃描無輸出；§4 末「使用者一次性設定清單」明寫「全程不需信用卡；agent 不索取、不代填」 |
  | 可離線驗證（本機 docker / IaC plan） | **是** | `06_*.md` §1「本機與 staging 都以**同一份 Dockerfile** 建置」；`ci.yml` 的 build 階段即 `docker build`；`infra/render.yaml` 為宣告式 IaC，可離線做 YAML 檢查 |
  | Windows 11 Git Bash 或 PowerShell 可執行 | **是** | 全部指令為 `docker`／`npm`／`curl`／`git`／`npx`；**`make`、`*.sh`、`/bin/bash` 掃描無輸出**（本角色獨立重跑，非採信 plan-sd）；SD §7 NFR-008 與 `06_*.md` 要求 README 同時提供兩種 shell 寫法 |
  | CI/CD 用 GitHub Actions | **是** | `06_*.md` §3 三個工作流 `ci.yml`／`deploy-staging.yml`／`monitor-health.yml` |
  | 規模刻意小（後端 3–5、前端 1–2、DevOps 1–2） | **是** | SD §3.4 WBS 建議為後端 4 卡、前端 2 卡、DevOps 2 卡，**全部落在區間內**，且附並行建議與關鍵路徑 |

  **通過。**

  **附加 —— 設計硬約束七項逐條核對（任務卡「審核重點」第 ⑤ 項）**

  | 硬約束 | 核對結果 | 證據位置 |
  |---|---|---|
  | Basic Auth 擋整站，`/health` 為**唯一**豁免 | **符合** | SD:240 §6.2 標題；SD:249~255 未保護路徑表 ＋「以上為全部。未保護路徑只有這一條。」；SD:259 要求完全相等字串比對、**禁前綴／萬用字元／正則**；SD:260 明寫 hook 掛根實例、CR 須確認 `public/` 資產未帶憑證同樣回 401（即靜態檔也被擋）；`04_API規格.yaml` 全域 `security` ＋ 僅 `/health` 與 P1 `auth/*` 有 `security: []` |
  | 前端與 API 同源 | **符合** | SD §1.2 C4 L2 圖與說明「同一個 Node 程序下的兩個責任區」；`FE-02` 規定「組 `/api/v1/*` **相對路徑**，不寫死網域」；`CORS_ALLOWED_ORIGINS` 預設留空且註明「永遠不得填入 `*`」 |
  | 識別碼採 UUID | **符合** | DB:56 `id uuid PRIMARY KEY DEFAULT gen_random_uuid()` ＋ 理由（自增整數可逐號列舉）；`04_API規格.yaml` `Todo.id`／`TodoId` 參數皆 `format: uuid` |
  | 刪除成功 204 | **符合** | SD:198 端點表、SD:208 設計要點 3；`04_API規格.yaml` `deleteTodo` 僅 `"204": 無回應本文`（**無 200**） |
  | 跨使用者 404 | **符合** | SD:209 設計要點 4 ＋ 單一查詢實作；SD:231 `E_NOT_FOUND`；`04_API規格.yaml` `responses/NotFound` 明寫「**同樣回 404 而非 403**」 |
  | 狀態變更為**設定目標狀態** | **符合** | SD:205 設計要點 1「送 `{"isCompleted": true}` 是設定目標值，**冪等**」＋「**不提供 toggle 語意的端點**」；`04_API規格.yaml` `UpdateTodoRequest.isCompleted` 描述「這是設定值而非切換」 |
  | 篩選在後端 | **符合** | SD:207 設計要點 2「**篩選在後端 SQL 的 WHERE 做**（O-003），前端可即時過濾但**事實來源是後端**」；`BE-06` 職責欄同旨；`04_API規格.yaml` `listTodos` description 同旨 |

  **逐條核對任務卡 acceptance 的結論（本角色獨立重跑後的判定）**

  | # | acceptance | 我的判定 | 依據 |
  |---|---|---|---|
  | 1 | OpenAPI 通過 lint | **通過** | 本角色重跑 `npx @redocly/cli lint` → 0 errors、exit 0、`Your API description is valid`；1 warning 為 `/health` 無 4XX，屬設計本身，接受保留 |
  | 2 | 每個 UC 對應至少一個模組與端點；每個 NFR 有設計對策與驗證方式 | **通過** | UC 集合與 SA 完全一致；14 則 UC 皆有模組與端點（UC-014 為橫切授權，端點落點在 traceability）；8 條 NFR 兩欄齊全且驗證方式可執行；門檻與 SRS 原文一致 |
  | 3 | 至少 3 份 ADR，每份有背景、選項、決定、後果 | **通過** | 4 份、每份四段齊全；選項數 2~5 個；7 張選項表皆含「缺點」與「成本」欄 |
  | 4 | 部署設計含環境清單、pipeline 階段、環境變數名稱清單（無值）、回滾程序草案 | **通過** | 六章齊全；15 個變數僅名稱與用途；機密賦值掃描無輸出；回滾三層 ＋ 演練要求已指派 |
  | 5 | SD 第 8 章目錄結構與分支策略完整；traceability.md 模組與 API 欄已填 | **通過** | 九章齊全；目錄樹逐檔標模組編號 ＋ 外掛註冊順序（不可調換）＋ 分支策略五項；主追溯矩陣 13 列兩欄人工逐列核對全數有實值 |
  | 6 | 技術選型符合 Epic 限制 | **通過** | 六條限制逐條對上證據；Unix 專屬工具掃描無輸出（本角色獨立重跑） |

  **判定：6/6 全部通過 → `tasks/T-0004-sd.md` `status: done`。**

- 下一步建議：
  - **給 plan-ba（T-0005 定稿）**：
    1. **小瑕疵 ①：UC-014 未在 `04_API規格.yaml` 任一 description 標註**（其餘 13 則 UC 皆有）。落點已在 `traceability.md` US-013 與 SD §5 第 4 點，**不影響 Gate 1**；定稿時在 `getTodoById`／`updateTodo`／`deleteTodo` 三個 operation 的 description 各補一句「（P1）UC-014 以使用者身分隔離存取」即可，屬一行修補。
    2. **小瑕疵 ②：SD-02 的文字與 `05_*.md` 實際 SQL 對不上**。SD §9 SD-02 寫「做成 `002_*.sql` migration 的**第一行** `TRUNCATE todos;`」，但 `05_*.md` 的實際 SQL 中 `TRUNCATE TABLE todos;` 位於 `CREATE TABLE users` 之後（第 178 行）。**語意無差**（同一個 migration、同一個交易），但文字不一致會讓開發者以為有兩種寫法。定稿時把 SD-02 的「第一行」改為「同一個 migration 內、`ALTER ... SET NOT NULL` 之前」。
    3. 依 Leader 對 O-009 的裁決，把 SRS 的 NFR-003 量測方式改寫為「Gate 2：24 小時採樣，每 5 分鐘一次，成功率 ≥ 99%；正式環境目標：連續 7 天」。`06_*.md` §6.2 與 SD §7 已先行採用此值，SRS 是唯一尚未同步的一份。
    4. `traceability.md` 新增的「特殊驗證方式的 AC」表（5 條）請一併審核並定稿，特別是 AC-009-5 的替代判準 —— 本角色已表態接受，理由見上方假設表態第 8 項。
    5. 孤兒檢查第三、四項（US↔模組／端點雙向）已於本輪成立且通過；定稿時只需重跑 TC 相關的後兩項。
  - **給 dev-tl（開發階段拆 WBS）**：
    1. **小瑕疵 ③：`/health` 豁免的比對對象要寫清楚是 pathname，不是 `req.url`**。SD §6.2 的虛擬碼寫 `if (req.url === "/health") return;`，但 Node 的 `req.url` **含查詢字串** —— `/health?cb=1` 會不匹配而被擋成 401。方向上是 fail-secure（更嚴，不是漏洞），但若日後監測工作流加上 cache-buster 參數，會造成 **NFR-003 假失敗**且極難查。請在「後端④ 全站 Basic Auth 與豁免」卡的「背景與限制」寫明兩件事：**(a) 比對對象為 pathname（剝除 query 與 hash 後）；(b) 監測端呼叫 `/health` 時不得附加任何查詢字串**。兩者其一寫錯都會出事。
    2. **小瑕疵 ④：`title varchar(200)` 與 `CHECK(btrim(...))` 的錯誤路徑不同**。前後帶空白、總長 201~203 字元的標題會先被 `varchar(200)` 以 PostgreSQL `22001` 錯誤擋下，走到 500 而非 BR-002 的 400 `E_VALIDATION`。實務上 API schema 的 `maxLength: 200` 會先擋掉，**不會發生**；但請在「後端③ 待辦 API 與業務規則」卡註明「**API 層一律先 `trim()` 再驗長度，再交給 DB**」，避免開發者把 trim 放在 DB 之後。
    3. SD §3.4 的 8 張卡切法、並行建議與關鍵路徑（後端①→②→③→前端②）可直接採用，本角色核對後符合 Epic 的卡數區間。
    4. SD §8.1 末的**外掛註冊順序不可調換**（`basic-auth` 必須早於 `static` 與 routes，否則靜態檔不設防），請寫進後端④卡的「背景與限制」。
    5. ADR-0001「後果」要求開發卡須附官方外掛名稱與版本（`@fastify/basic-auth`、`@fastify/static`、P1 `@fastify/jwt`），避免開發者臨場自行發明中介層 —— 本角色同意此要求，請落實到卡上。
  - **給 dev-ops**：
    1. `06_*.md` §2 的 GitHub Actions 額度問題請二選一並記錄在卡上（文件建議設為公開倉庫）。本角色不代為決定。
    2. 回滾演練（§5.4）是 Gate 2 前置條件，OPS-03 完成後請執行並回填日期與結果。
  - **給測試團隊**：
    1. O-001 已收斂為單一值（刪除 204、跨使用者 404），斷言可寫死。
    2. 排序 tie-break 已定（`created_at DESC, id DESC`），NFR-007 的 500 筆情境順序為**決定性**，請勿寫成「順序不定」的寬鬆斷言。
    3. AC-010-5 的驗證**必須不帶憑證**呼叫 `/health`（帶憑證等於沒驗到 BR-017 的豁免），並同時驗 `GET /` 不帶憑證回 401，以證明只有一條路徑被豁免 —— `traceability.md` 特殊驗證表已寫死此兩條判準。
- 需要 Leader 裁決的事：**無**。
  SD 第 9 章的 SD-03（409）與 SD-05（`/api/v1` 前綴）Leader 已表態，本角色在審核中一致接受；SD-01、SD-02 屬 P1，SD-04 已明列風險。四項小瑕疵皆為文字或實作註記層級，已分派給 T-0005 與 dev-tl，不需升級。
