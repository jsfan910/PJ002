# T-0038 r1 — dev-fe — 工作紀錄與交接

## A. 開工紀錄

- 開工時間：2026-09-20T18:36:57+08:00
- 角色／模型：dev-fe / sonnet
- 任務卡：tasks/T-0038-fix-d017-error-message.md（round 1）
- 讀取的輸入（實際讀過的檔案與段落，非任務卡照抄）：
  - docs/specs/24_缺陷清單.md#D-017 — 現象：staging 下 TC-067／TC-009 間歇性失敗，`error-message` 有時維持 `hidden`；重現需真實網路延遲，本機 docker 從未重現；建議方向為檢視「錯誤訊息是否在下一次 render() 前被清掉」。
  - docs/specs/20_測試案例.md#TC-067、#TC-009 — TC-067：POST 500 時應顯示可讀錯誤且畫面不凍結、解除攔截後再點「新增」可不重整成功建立；TC-009：空白標題應顯示可讀錯誤、清單筆數不變、無 console error。
  - docs/specs/01_需求規格書_SRS.md#BR-029、#BR-030（透過 24_缺陷清單.md／20_測試案例.md 引用內容確認）— 後端失敗須顯示可讀錯誤（BR-029）、必填驗證失敗須顯示可讀錯誤（BR-030）。
  - public/assets/todo-store.js（全檔 249 行）— 找到根因：`load()`（現行 132-151 行）以模組層級 `requestSeq` 保護「先發後到」只針對其他 `load()` 呼叫；但 `add()/updateTitle()/setCompleted()/remove()`（現行 168-234 行）完全不使用這個序號保護，且 `add()/updateTitle()` 的**同步驗證失敗分支**（170-173、191-194 行）也不佔用序號。當頁面啟動時的初始 `todoStore.actions.load()`（todo-view.js:304）因真實網路延遲仍在飛行中，使用者這時觸發的 `add()` 錯誤（無論是同步驗證失敗還是非同步 API 失敗）先寫入 `state.error`；稍後那個較舊、仍在飛行中的初始 `load()` 才 resolve，其成功分支（142 行 `setState({ todos, loading: false, error: null })`）沒有任何依據可知道「已經有更新的錯誤要顯示」，於是無條件把 `error` 蓋回 `null`，導致 `error-message` 又被判定為 hidden。本機 docker compose 延遲極低，初始 load 幾乎必定早於使用者操作完成，因此本機幾乎不會重現；staging 的真實網路延遲讓這個競態視窗變得可觀察。
  - public/assets/todo-view.js（全檔 304 行）— 確認 `render()`（273-279 行）只是單純依 `state` 重繪，`renderError()`（249-264 行）本身無時序問題；問題完全在 store 的狀態轉移規則，view 端不需改動邏輯（僅在必要時可能小幅調整，但目前判斷不需要）。
  - tests/unit/todo-store.test.mjs（全檔 403 行）— 既有測試風格：以 `createMockApi()` 注入假 api-client、`collectStates()` 收集通知序列；既有「先發後到」測試（151-190 行）只涵蓋 `load()` vs `load()`，未涵蓋 `load()` vs `add()`/驗證失敗的跨動作競態，需在同檔新增對應案例（任務卡 outputs 指定新增進本檔，非另建新檔）。
  - tests/e2e/、playwright.config.ts — 待收尾階段確認 project 名稱與 TC-067/TC-009 測試檔位置（尚未讀取，e2e 驗證階段再讀）。
  - docs/reports/20260919-1719-AT-E001-r3.md — 待收尾階段核對 staging 失敗紀錄與重跑結果的原始敘述是否與缺陷清單一致（尚未讀取全文）。
- 上一輪交接摘要（若 round > 1）：無（round 1）
- 本輪計畫（3–7 條）：
  1. 在 `public/assets/todo-store.js` 引入共用的操作序號（`opSeq`／`beginOp()`/`isStale()`），取代只保護 `load()` 彼此的 `requestSeq`。
  2. 讓 `add()/updateTitle()` 的同步驗證失敗分支也佔用一個新序號（讓在飛行中的舊 `load()`／舊動作事後可判斷自己已過期而放棄寫入)。
  3. 讓 `add()/updateTitle()/setCompleted()/remove()` 的非同步失敗分支（catch）比照 `load()` 加上 staleness 檢查，避免任何過期的非同步結果覆蓋較新的狀態。
  4. 在 `tests/unit/todo-store.test.mjs` 新增至少一個測試：模擬「初始 `load()` delay 中 → `add()`（同步驗證失敗或非同步失敗）先設定 error → 較舊的 `load()` 才 resolve」，修正前應失敗、修正後應通過。
  5. 跑 `npm run lint`、`npm run test:unit`，確認全綠。
  6. 視需要（PORT=8081／POSTGRES_HOST_PORT=5433）起 docker compose 做 e2e 連跑 5 次 `-g "TC-067|TC-009"`；Docker 不可用時依角色檔離線替代驗證並註明。
  7. 填交接檔 B 段、任務卡 status、commit。
- 預期產出：`public/assets/todo-store.js`（修正）、`tests/unit/todo-store.test.mjs`（新增競態測試）；`public/assets/todo-view.js` 視根因分析結果可能不需改動（若最終判斷不需要，會在 B 段的假設與決策說明理由）。

## B. 交接

- 完工時間／狀態：2026-09-20T18:45:08+08:00 / review
- 完成事項：
  - 找到 D-017 精確根因並修正 `public/assets/todo-store.js`：所有會決定 `loading`／`error`／`todos` 的動作（`load()`、`add()/updateTitle()/setCompleted()/remove()` 含其同步驗證失敗分支）改為共用同一個單調遞增的操作序號（`opSeq`／`beginOp()`/`isStale()`），取代原本只保護 `load()` 彼此之間先發後到的 `requestSeq`。
  - 以 e2e 測試檔（`tests/e2e/us008-spa.e2e.test.ts` TC-067、`tests/e2e/us001-create.e2e.test.ts` TC-009）交叉核對，確認兩條 TC 在觸發使用者動作前的斷言（`empty-state` 可見／`todo-item` count=0）在初始清單為空時本來就會**立即成立、不會等待初始 `load()` 的網路回應完成**，因此使用者動作（新增／空白提交）與頁面啟動時的初始 `load()` 幾乎必然並行，證實根因情境確實會在這兩條 TC 的實際腳本下發生。
  - 在 `tests/unit/todo-store.test.mjs` 新增 2 個測試（`D-017／TC-009`、`D-017／TC-067`），以可控 resolve 的假 `listTodos` 模擬「初始 load() 因延遲仍在飛行中」，先觸發 `add()`（分別對應 TC-009 的同步驗證失敗、TC-067 的非同步 500 失敗）設定 error，再讓較舊的 `load()` resolve，驗證 `state.error` 不會被蓋回 `null`。已驗證：修正前 2 條測試失敗、修正後（含既有 25 條）全數通過（輸出見下方「驗證方式」）。
  - `npm run build`、`npm run lint`、`npm run test:unit`（78 條，含新增 2 條）全綠。
  - 確認未改動 `public/assets/todo-view.js`：`render()`／`renderError()` 只是單純依 `state` 重繪，沒有時序問題，根因完全在 store 的狀態轉移規則；本卡未產出該檔的變更（`git diff --stat` 只列 `todo-store.js`／`tasks/T-0038-*.md`／`tests/unit/todo-store.test.mjs` 三檔）。
  - 以 `docker compose config`（不需要 daemon）驗證 `PORT=8081`／`POSTGRES_HOST_PORT=5433` 兩個環境變數能正確覆蓋埠號，compose 檔語法無誤（輸出見下）。
- 產出物（路徑）：
  - C:\Users\excal\Projects\Claude\soft-dev-team-260919-wt\T-0038\public\assets\todo-store.js
  - C:\Users\excal\Projects\Claude\soft-dev-team-260919-wt\T-0038\tests\unit\todo-store.test.mjs
  - C:\Users\excal\Projects\Claude\soft-dev-team-260919-wt\T-0038\tasks\T-0038-fix-d017-error-message.md（status 更新）
- 未完成／已知問題：
  - **本機 docker compose 的 e2e 連跑 5 次（acceptance 第 4 條）未能執行**：本環境 Docker Desktop 進程存在但 daemon 未就緒（`docker info`／`docker version`（server 端）連續逾時無回應，等待超過本角色規定的 2 次確認上限），非本卡程式碼問題。已依角色檔「環境注意」第 6 條改跑離線等效驗證：`docker compose config`（不需要 daemon）確認 compose 檔語法正確、`PORT`／`POSTGRES_HOST_PORT` 覆蓋生效；實際跑 5 次 `--project=chromium-1280x800/chromium-390x844/msedge-1280x800/msedge-390x844 -g "TC-067|TC-009"` 仍待有可用 Docker daemon 的環境（例如 dev-tl 初審或另一台機器）補驗，任務卡背景本身也預期「本機從未重現，驗證以單元測試重現時序為主、e2e 連跑 5 次為輔」。
  - `tests/e2e/lib/api-helper.js`（TC-067／TC-009 用到的 `apiClearAll`）與 `tests/e2e/us001-create.e2e.test.ts`／`us008-spa.e2e.test.ts` 兩檔皆未修改（任務卡明確禁止修改 tests/e2e 的斷言或逾時，本卡也不需要改）。
- 我做的假設與決策（含理由）：
  - **根因（引用修正前行號，對應 main 分支 `public/assets/todo-store.js`）**：`load()`（132-151 行，見 `git show main:public/assets/todo-store.js`）以模組層級 `let requestSeq`（109 行）配合 `seq = ++requestSeq`（134 行）與 `if (seq !== requestSeq)`（139、145 行）只保護「`load()` 彼此之間先發後到」；但 `add()`（168-182 行）、`updateTitle()`（189-203 行）、`setCompleted()`（210-219 行）、`remove()`（225-234 行）完全不佔用或檢查 `requestSeq`，且 `add()/updateTitle()` 的**同步驗證失敗分支**（171、192 行的 `setState({ error: {...} })`）也不佔用。當頁面啟動時 `todoStore.actions.load()`（`public/assets/todo-view.js:304`）因真實網路延遲仍在飛行中，使用者這時觸發的 `add()` 錯誤（不論是 TC-009 的同步驗證失敗、還是 TC-067 的非同步 500 失敗）先把 `state.error` 設成可讀錯誤並通知 view 顯示；接著那個較舊、仍在飛行中的初始 `load()` 才 resolve，其成功分支（142 行 `setState({ todos, loading: false, error: null })`）沒有任何依據知道「已經有更新的錯誤要顯示」，於是無條件把 `error` 蓋回 `null`——這就是「請求失敗／驗證失敗 → 錯誤訊息設定 → 在下一次（不相關的）render 前被清除」的兩個事件。已用 `tests/e2e/us008-spa.e2e.test.ts:59-71`（TC-067）與 `tests/e2e/us001-create.e2e.test.ts:30-44`（TC-009）交叉核對：兩者觸發使用者動作前的斷言（`empty-state` 可見、`todo-item` count=0）在初始 `state.todos=[]` 時第一次 `render()` 就已成立，不需要等初始 `load()` 的網路回應，所以測試會在初始 `load()` 仍在飛行中時就送出使用者動作，與根因情境吻合。本機 docker compose 延遲趨近於零，初始 `load()` 幾乎必定早於使用者動作完成，因此 requestSeq 這個既有保護機制在本機幾乎不會被踩到；staging 的真實網路延遲才會讓這個競態視窗變得可觀察（約 2~4 成失敗率，與 AT r3 報告一致）。
  - **修正後的狀態轉移規則**：把「誰有權寫入目前 `loading`／`error`／`todos`」明確化為一個單調遞增的操作序號 `opSeq`（同檔新行 126-136）：任何會決定這三個欄位的動作，一律先呼叫 `beginOp()` 取得屬於自己的 token（`load()` 148 行、`add()` 同步驗證失敗分支 201 行、`add()` 非同步分支 205 行、`updateTitle()` 同理 231／235 行、`setCompleted()` 258 行、`remove()` 280 行）；非同步結果回來後先以 `isStale(token)` 確認「這段期間沒有更新的動作已經開始」才允許 `setState`（`load()` 的成功／失敗分支 167、173 行；`add()/updateTitle()/setCompleted()/remove()` 的 catch 分支與成功後呼叫 `load()` 前皆加了同樣的檢查）。只要有更新的動作已經開始（不論它自己是同步或非同步），較舊動作的非同步結果一律視為過期並直接放棄寫入（不呼叫 `setState`，因此也不會多觸發一次 `render()`），較新動作寫入的 `error`／`loading` 就不會被較舊、後到的結果覆蓋。這個規則同時修掉 D-017 本身、以及 `add()/updateTitle()/setCompleted()/remove()` 彼此之間原本完全沒有的 staleness 保護（原程式碼這幾個動作的 catch 分支無條件寫入，跟 D-017 是同一類缺陷，只是尚未被任何 TC 抓到），是一致的狀態轉移規則而非只針對這兩條 TC 的個案修補。
  - 決定不修改 `public/assets/todo-view.js`：確認 `render()`／`renderError()` 純粹依 `state` 重繪、無自身時序問題，任務卡 outputs 雖列了這個檔案，但依規格「規格優先於 Leader 提示詞」原則與實際根因分析，判斷不需要改動；若審核者認為仍需要對 view 端做防禦性調整（例如追加註解），請於審核意見中指出。
  - 決定新增的兩個測試放進既有 `tests/unit/todo-store.test.mjs`（任務卡 outputs 本就寫「新增重現競態的單元測試」於此檔，非另建新檔），並沿用既有測試的 `createMockApi()`／可控 resolve Promise 風格，與既有「先發後到」測試（`load()` vs `load()`）並列在同一組小節內，方便日後維護對照。
- 驗證方式（接手者如何確認我做的是對的：指令 + 我實際得到的輸出）：
  ```bash
  # 1) 修正前（回復 main 分支的 todo-store.js，只保留新測試）：確認 2 條新測試會失敗，
  #    證明它們真的重現了 D-017 的競態，而非空測試。
  cd C:\Users\excal\Projects\Claude\soft-dev-team-260919-wt\T-0038
  git show HEAD:public/assets/todo-store.js > public/assets/todo-store.js   # 暫時還原成修正前
  node --test "tests/unit/todo-store.test.mjs"
  ```
  ```text
  ✔ load()：先發後到時，只有最新一次的回應會寫入 state（S-5，CR-E001 請求序列化） (0.6424ms)
  ✖ D-017／TC-009：初始 load() 因延遲仍在飛行中時，同步驗證失敗的 error 不會被稍後才 resolve 的 load() 蓋回 null (2.2419ms)
  ✖ D-017／TC-067：初始 load() 因延遲仍在飛行中時，新增失敗（後端 500）的 error 不會被稍後才 resolve 的 load() 蓋回 null (0.5457ms)
  ...
  ℹ tests 27
  ℹ pass 25
  ℹ fail 2
  ✖ failing tests:
  test at tests\unit\todo-store.test.mjs:203:1
  ✖ D-017／TC-009：... AssertionError [ERR_ASSERTION]: Expected values to be strictly equal: + actual undefined - expected true
  test at tests\unit\todo-store.test.mjs:236:1
  ✖ D-017／TC-067：... AssertionError [ERR_ASSERTION]: Expected values to be strictly equal: + actual undefined - expected true
  ```
  ```bash
  # 2) 修正後（還原修正版 todo-store.js）：同一份測試檔全數通過。
  # （已將修正版內容還原至 public/assets/todo-store.js）
  node --test "tests/unit/todo-store.test.mjs"
  ```
  ```text
  ✔ D-017／TC-009：初始 load() 因延遲仍在飛行中時，同步驗證失敗的 error 不會被稍後才 resolve 的 load() 蓋回 null (0.296ms)
  ✔ D-017／TC-067：初始 load() 因延遲仍在飛行中時，新增失敗（後端 500）的 error 不會被稍後才 resolve 的 load() 蓋回 null (0.2563ms)
  ...
  ℹ tests 27
  ℹ pass 27
  ℹ fail 0
  ```
  ```bash
  # 3) 全量 lint 與單元測試（修正版）
  npm run build
  npm run lint
  npm run test:unit
  ```
  ```text
  # build：無輸出（tsc -p tsconfig.json 成功，exit 0）
  # lint：
  > todo-app@0.1.0 lint
  > eslint . && tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.test.json && tsc --noEmit -p tsconfig.e2e.json
  （無錯誤輸出，exit 0）
  # test:unit（節錄尾端）：
  ✔ D-017／TC-009：初始 load() 因延遲仍在飛行中時，同步驗證失敗的 error 不會被稍後才 resolve 的 load() 蓋回 null (0.5402ms)
  ✔ D-017／TC-067：初始 load() 因延遲仍在飛行中時，新增失敗（後端 500）的 error 不會被稍後才 resolve 的 load() 蓋回 null (0.4255ms)
  ...
  ℹ tests 78
  ℹ pass 78
  ℹ fail 0
  ```
  ```bash
  # 4) Docker daemon 可用性與離線替代驗證
  docker version
  ```
  ```text
  Client: Version: 29.4.2 ...
  failed to connect to the docker API at npipe:////./pipe/dockerDesktopLinuxEngine; check if the path is correct and if the daemon is running: open //./pipe/dockerDesktopLinuxEngine: The system cannot find the file specified.
  ```
  ```bash
  # docker info 與 docker info --format 皆逾時無回應（背景執行仍無輸出，已達 2 次確認上限，判定 daemon 不可用）
  PORT=8081 POSTGRES_HOST_PORT=5433 docker compose config
  ```
  ```text
  name: t-0038
  services:
    app:
      ...
      environment: {... PORT: "8081" ...}
      ports: [{target: 8081, published: "8081", protocol: tcp}]
    db:
      ...
      ports: [{target: 5432, published: "5433", protocol: tcp}]
  （完整輸出略，PORT／POSTGRES_HOST_PORT 覆蓋皆正確、compose 檔語法無誤，exit 0）
  ```
  ```bash
  # 5) 確認只改了 outputs 三檔
  git status
  git diff --stat
  ```
  ```text
  modified:   public/assets/todo-store.js
  modified:   tasks/T-0038-fix-d017-error-message.md
  modified:   tests/unit/todo-store.test.mjs
   public/assets/todo-store.js            | 79 ++++++++++++++++++++++++++++----
   tasks/T-0038-fix-d017-error-message.md |  4 +-
   tests/unit/todo-store.test.mjs         | 82 ++++++++++++++++++++++++++++++++++
   3 files changed, 154 insertions(+), 11 deletions(-)
  ```
- 下一步建議：
  - 補跑本機 docker compose 的 e2e 5 次連跑（`PORT=8081 POSTGRES_HOST_PORT=5433`，`--project=chromium-1280x800 --project=chromium-390x844 --project=msedge-1280x800 --project=msedge-390x844 -g "TC-067|TC-009"`）：需要一台 Docker daemon 正常運作的機器（本環境 Docker Desktop 進程存在但 daemon 未就緒）。若 5 次皆過，即可視為 acceptance 第 4 條完成；若仍有失敗，回頭檢視是否還有其他未涵蓋的競態路徑（例如 `setFilter()` 與 `add()` 的交錯）。
  - dev-tl 初審合併後推送觸發 staging 部署，qa-lead 依 D-017 建議在下一輪測試總結重跑 TC-067／TC-009（staging，至少 5 次）以正式關閉 D-017。
- 需要 Leader 裁決的事：
  - 無（Docker daemon 不可用屬環境限制，已依角色檔「環境注意」第 6 條處理，非規格衝突或需要決策的事項；若 dev-tl 審核時也遇到相同 Docker 限制，屬於同一個已知環境問題，非本卡新增風險）。
