# T-0013 r1 — dev-tl — 審核紀錄與交接

## A. 開工紀錄

- 開工時間：2026-09-19T08:47:00+08:00
- 角色／模型：dev-tl / opus
- 任務卡：tasks/T-0013-data-layer.md（round 1）— 審核 T-0013（批次 2 第二張）
- 讀取的輸入（實際讀過的檔案與段落）：
  - tasks/T-0013-data-layer.md — outputs 五項（＋Leader 裁決追加的單元測試）、acceptance 五條
  - docs/specs/10_開發計畫_WBS.md §0（硬前提）、§1.3（WI-03 背景與限制七條＋驗收方式草案七條＋「測試須覆蓋 500 筆 tie-break」）、§5
  - docs/specs/05_資料庫設計.md §3 全節：001 DDL 逐字稿、migration runner 五條行為規格、代表性查詢五條、阻擋級 CR 判準
  - tasks/E-001-todo-app.md 2026-09-19T08:39:11 對 T-0013 三項裁決
  - worklog/handoff/20260919-0822-T0013-r1-dev-be.md 全文（已知問題 4 條、假設與決策 6 條、需 Leader 裁決 2 條）
  - worktree 內 migrations/001_create_todos.sql、src/db/pool.ts、src/db/migrate.ts、src/repositories/todo-repository.ts 全文
- 上一輪交接摘要：無（round 1）
- 本輪計畫：
  1. 重跑 lint／build／test:unit
  2. 本機 Docker 若可用則起 compose Postgres 實跑整合測試與 psql 驗證（不可用則標 skip）
  3. 逐欄比對 001 DDL 與 05 §3、比對 repository SQL 與 05 §3 代表性查詢
  4. 跑阻擋級 CR 判準 grep
  5. git diff main...HEAD --stat 檢查範圍
  6. 對六條假設與決策表態，判定並合併
- 預期產出：本審核交接檔、任務卡 status 更新、合併 commit

## B. 交接

- 完工時間／狀態：2026-09-19T08:58:00+08:00 / done（初審通過，已合併 main）
- 完成事項：
  - **本機 Docker daemon 實測可用（29.4.2），整合測試未 skip，對真實 Postgres 16 全跑**。為取得獨立於開發者的第二份證據，我先 `docker compose down -v` 清空資料卷再重建，確認「首次套用 → 第二次 no-op」的冪等行為是真的跑出來的，而不是沿用既有資料。
  - 逐欄比對 `migrations/001_create_todos.sql` 與 `05_資料庫設計.md` §3 的 DDL：38 行逐字一致（含註解）。另以 `psql \d todos` 從**實際建出的資料庫**反向驗證：uuid 主鍵 + `gen_random_uuid()`、`varchar(200) NOT NULL`、`is_completed boolean NOT NULL DEFAULT false`、兩個 `timestamptz NOT NULL DEFAULT now()`、`owner_id uuid` 可為 NULL、索引 `idx_todos_created_at_desc btree (created_at DESC, id DESC)`、CHECK `todos_title_not_blank`、觸發器 `trg_todos_protect_created_at BEFORE UPDATE`——全部到位，與 05 §3 無差異。
  - 逐條比對 `src/repositories/todo-repository.ts` 的五段 SQL 與 05 §3「代表性查詢」：list（WHERE 三分支 + `ORDER BY created_at DESC, id DESC`）、create（`btrim($1)` + RETURNING）、update（雙 `COALESCE` + RETURNING）、delete、getById，語句與參數編號皆相符；狀態篩選確實在 SQL WHERE（O-003）；`snake_case → camelCase` 只在 `mapRow()` 一處（BE-06）。
  - 阻擋級 CR 判準：`grep -nF "${"` 對 repository 檔**零命中**（另以對照組證明 grep 樣式本身有效）。
  - `src/db/pool.ts` 確認 `max: 5`（SD §7 NFR-001 對策②）。
  - `src/db/migrate.ts` 對照 05 §3 runner 五條：① `CREATE TABLE IF NOT EXISTS schema_migrations (version text PRIMARY KEY, applied_at timestamptz NOT NULL DEFAULT now())` 逐字一致；② `readdirSync` 過濾 `.sql` 後 `.sort()`；③ 以 `pool.connect()` 取得單一 client 跑 `BEGIN`／檔案全文／`INSERT schema_migrations`／`COMMIT`，catch 內 `ROLLBACK` 並向上拋，`main()` 設 `process.exitCode = 1`；⑤ 無 `down`。第 ④ 條「應用啟動時自動執行」未接（見下方表態）。
  - 確認 `npm run migrate` 逐字執行確實失敗（`ERR_MODULE_NOT_FOUND: src/config.js`），與交接檔「已知問題」1 一致；依 Leader 裁決 2026-09-19T08:39:11 第②條，由我在批次 2 收尾 commit 修正 package.json 並在 README 註明。
  - 合併：`git merge --no-ff task/T-0013-data-layer`，任務卡 status→done、審核紀錄表加一列；移除 worktree 與分支。
- 產出物（路徑）：
  - 本檔 worklog/handoff/20260919-0847-T0013-r1-dev-tl.md
  - tasks/T-0013-data-layer.md（status: done、審核紀錄表）
  - main 上的合併 commit
- 未完成／已知問題：
  - 無阻擋級問題。`npm run migrate` 腳本修正與 README 註明由批次 2 收尾 commit 處理（Leader 已裁決）。其餘小瑕疵寫入「下一步建議」。
- acceptance 逐條判定：
  1. **WBS §1.3 驗收方式草案每條實際執行且結果符合** — 通過。七條指令（compose up、migrate ×2、整合測試、`${` grep、psql CHECK、psql schema_migrations）全跑，輸出見下。唯一偏離：`npm run migrate` 逐字執行失敗，改以功能等價的 `node dist/db/migrate.js` 取得結果——此為 package.json 腳本既有缺陷，Leader 已裁決由我修，非本卡缺陷。
  2. **npm run lint 與相關測試全綠；不改 outputs 以外的檔案** — 通過。lint exit 0、build exit 0、unit 15/15、integration 8/8；`git diff main...HEAD --stat` 只有 outputs（含 Leader 裁決①納入範圍的 `tests/unit/todo-repository.test.ts`）＋任務卡＋交接檔。
  3. **migration 與 05_資料庫設計.md 的 DDL 逐欄一致（UUID 主鍵、索引、約束）** — 通過（檔案逐字 ＋ `psql \d todos` 雙向驗證，見下）。
  4. **所有查詢參數化，無字串拼接 SQL** — 通過（`${` 零命中；全部值經 `values` 陣列以 `$1`/`$2`/`$3` 傳遞）。
  5. **整合測試對本機 compose Postgres 實跑** — 通過（**未 skip**；清空資料卷後重跑，8/8 綠）。
- 對交接檔「我做的假設與決策」逐條表態：
  1. **`setCompleted` 實作為 `updateTodo(db, id, { isCompleted })` 的薄封裝** — **接受**。05 §3 本來就只給一條 UPDATE 查詢；兩個入口共用同一條 SQL 符合 O-002「設定目標狀態而非 toggle」的冪等要求，也避免兩套 SQL 分歧。
  2. **`createdAt` 維持 `Date`，不在本層轉字串** — **接受**。BE-06 的職責只有命名轉換；型別格式化留給序列化層（BE-04）是正確的職責切分。**但這是跨層約定，必須讓 T-0016 知道**，已寫入「下一步建議」。
  3. **以最小介面 `Queryable` 注入而非直接依賴 `pg.Pool`** — **接受**。這是「無 DB 也能單元測試 SQL 組裝」的關鍵，且 `pg.Pool`／`PoolClient` 結構相容，不增加相依。
  4. **`migrate.ts` 用 `pool.connect()` 的單一 client 跑交易而非 `pool.query`** — **接受**，且這是必要的正確做法：`pool.query()` 每次可能取得不同連線，`BEGIN`／`COMMIT` 會落在不同連線上而完全失效。05 §3 第 3 條「單一交易內」靠這點才成立。
  5. **500 筆 tie-break 測試以顯式指定相同 `created_at` 灌入** — **接受**。真實時鐘下微秒級時間戳幾乎不會碰撞，靠自然時間測不到 tie-break；`created_at` 只在 `BEFORE UPDATE` 觸發器受保護，INSERT 顯式指定不違反任何約束。實跑 177ms 通過，證據有效。
  6. **新增 `tests/unit/todo-repository.test.ts` 超出原 outputs** — **接受**。Leader 已於 2026-09-19T08:39:11 第①條裁決併入本卡 outputs。內容確有獨立價值：15 條測試覆蓋純函式 SQL 組裝與參數順序，且把阻擋級 CR 判準做成自動化回歸防線（不只靠人工 grep），在 CI 無 DB 的 unit job 就能攔截。
  - 另對「已知問題」3（未接入啟動時自動 migrate）表態：**接受不做**。Leader 已於 2026-09-19T08:39:11 第③條裁決「不做啟動時自動 migrate，migrate 為部署 pipeline 獨立階段，由 T-0018 實作」。故 05 §3 runner 第 4 條在 P0 以「部署階段獨立執行」滿足，本卡無需補。
- 驗證方式（審核者實跑的指令與真實輸出）：

  ```bash
  cd C:/Users/excal/Projects/Claude/soft-dev-team-260919-wt/T-0013
  git log --oneline -2
  git status --short
  git diff main...HEAD --stat
  ```
  ```text
  7e0a086 T-0013: 資料層——連線池、migration runner、todo repository
  305d5bf E-001: 派工批次 2（T-0012/T-0013/T-0014）
  （git status --short 無輸出）

   migrations/001_create_todos.sql                  |  38 ++++
   src/db/migrate.ts                                | 132 +++++++++++++
   src/db/pool.ts                                   |  23 +++
   src/repositories/todo-repository.ts              | 179 +++++++++++++++++
   tasks/T-0013-data-layer.md                       |   5 +-
   tests/integration/todo-repository.test.ts        | 217 ++++++++++++++++++++
   tests/unit/todo-repository.test.ts               | 220 +++++++++++++++++++++
   worklog/handoff/20260919-0822-T0013-r1-dev-be.md | 241 +++++++++++++++++++++++
   8 files changed, 1053 insertions(+), 2 deletions(-)
  ```
  （只動 outputs ＋任務卡 ＋交接檔；未觸碰 package.json／src/app.ts／src/config.ts）

  ```bash
  npm run lint
  npm run build && ls dist/db dist/repositories
  ```
  ```text
  > eslint . && tsc --noEmit -p tsconfig.json
  LINT_EXIT:0

  > tsc -p tsconfig.json
  BUILD_EXIT:0
  dist/db:            migrate.js  migrate.js.map  pool.js  pool.js.map
  dist/repositories:  todo-repository.js  todo-repository.js.map
  ```

  ```bash
  npm run test:unit
  ```
  ```text
  ✔ 阻擋級 CR 判準：repository 原始碼內不得出現樣板字串插值 ${ (0.8195ms)
  ✔ buildListCommand：$1 帶 status，無論哪個 status 都只有一個參數 (0.4719ms)
  ✔ buildGetByIdCommand：$1 帶 id (0.0927ms)
  ✔ buildCreateCommand：$1 帶 title，SQL 對 title 做 btrim (0.9498ms)
  ✔ buildUpdateCommand：$1=id、$2=title、$3=isCompleted，缺的欄位為 null（COALESCE 保留原值） (0.1323ms)
  ✔ buildDeleteCommand：$1 帶 id (0.0878ms)
  ✔ listTodos：把 rows 轉成 camelCase，並把 status 原樣傳給 $1 (0.2138ms)
  ✔ getTodoById：0 列回傳 null（不存在） (0.1043ms)
  ✔ getTodoById：有列時回傳 camelCase 物件 (0.1059ms)
  ✔ createTodo：RETURNING 有列時回傳 camelCase 物件 (0.1549ms)
  ✔ createTodo：RETURNING 未回傳任何列時視為資料層異常並拋出 (0.1945ms)
  ✔ updateTodo：查無此 id（0 列）回傳 null (0.0935ms)
  ✔ setCompleted：委派給 updateTodo，只帶 isCompleted（title 為 null） (0.0721ms)
  ✔ deleteTodo：rowCount > 0 回傳 true (0.0904ms)
  ✔ deleteTodo：rowCount 為 0 或 null 回傳 false（不存在，非錯誤） (0.0916ms)
  ℹ tests 15  ℹ pass 15  ℹ fail 0  ℹ duration_ms 110.6829
  UNIT_EXIT:0
  ```

  ```bash
  docker info --format "{{.ServerVersion}}"
  ```
  ```text
  29.4.2
  ```
  （本機 Docker daemon **可用**，故整合測試實跑，不標 skip）

  ```bash
  cp .env.example .env
  docker compose down -v          # 先清空資料卷，確保「首次套用」是真的首次
  docker compose up -d db
  docker inspect --format="{{.State.Health.Status}}" t-0013-db-1
  ```
  ```text
   Volume t-0013_todo-db-data Removed
   Network t-0013_default Removed
   Container t-0013-db-1 Started
  healthy after 3
  ```

  ```bash
  # WBS §1.3 原文第 2、3 行是 npm run migrate —— 先逐字照做取得證據
  export DATABASE_URL="postgres://dev:dev@localhost:5432/todo"
  npm run migrate
  ```
  ```text
    code: 'ERR_MODULE_NOT_FOUND',
    url: 'file:///C:/Users/excal/Projects/Claude/soft-dev-team-260919-wt/T-0013/src/config.js'
  MIGRATE_NPM_EXIT:1
  ```
  （複現交接檔「已知問題」1。**非本卡缺陷**：`node src/db/migrate.ts` 是 WI-01 寫的腳本定義，Node 原生型別剝除不把 `.js` 匯入規格解析回 `.ts`。Leader 2026-09-19T08:39:11 第②條已裁決由 dev-tl 在批次 2 收尾修為 `node dist/db/migrate.js`。）

  ```bash
  set -a && . ./.env && set +a
  export DATABASE_URL="postgres://dev:dev@localhost:5432/todo"
  node dist/db/migrate.js    # 首次套用
  node dist/db/migrate.js    # 再跑一次應為 no-op（冪等）
  ```
  ```text
  --- run1 ---
  已套用 migration：001
  MIGRATE1_EXIT:0
  --- run2 ---
  無待套用的 migration（已是最新版本）。
  MIGRATE2_EXIT:0
  ```

  ```bash
  node --test "tests/integration/todo-repository.test.ts"
  ```
  ```text
  ✔ create → getById 讀回同一筆，欄位為 camelCase (17.3935ms)
  ✔ getById 查無資料回傳 null（由呼叫端轉 404） (1.1247ms)
  ✔ update：可只改標題、只改完成狀態，或兩者都改（COALESCE 保留未提供欄位） (6.7206ms)
  ✔ update：查無此 id 回傳 null (0.7848ms)
  ✔ delete：刪除存在的資料回 true，再刪一次回 false（不存在非錯誤） (3.852ms)
  ✔ list：status 篩選在 SQL WHERE 做（active／completed／all） (11.6566ms)
  ✔ list：500 筆同秒建立（同一 created_at）後排序仍為決定性（created_at DESC, id DESC，NFR-007） (177.485ms)
  ✔ DB 層第二道防線：全空白標題違反 CHECK 約束（即使繞過業務層直接呼叫 repository） (2.3202ms)
  ℹ tests 8  ℹ pass 8  ℹ fail 0  ℹ skipped 0  ℹ duration_ms 388.9063
  INT_EXIT:0
  ```
  （`skipped 0` —— 確實對真實 Postgres 跑，非 skip）

  ```bash
  docker compose exec -T db psql -U dev -d todo -c "INSERT INTO todos (title) VALUES ('   ');"
  docker compose exec -T db psql -U dev -d todo -c "SELECT version FROM schema_migrations;"
  docker compose exec -T db psql -U dev -d todo -c "\d todos"
  ```
  ```text
  ERROR:  new row for relation "todos" violates check constraint "todos_title_not_blank"
  DETAIL:  Failing row contains (c85aa24d-5e0c-484f-a9d1-9c740504d71d,    , f, 2026-09-19 00:47:48.997419+00, ...).

   version
  ---------
   001
  (1 row)

                                  Table "public.todos"
      Column    |           Type           | Collation | Nullable |      Default
  --------------+--------------------------+-----------+----------+-------------------
   id           | uuid                     |           | not null | gen_random_uuid()
   title        | character varying(200)   |           | not null |
   is_completed | boolean                  |           | not null | false
   created_at   | timestamp with time zone |           | not null | now()
   updated_at   | timestamp with time zone |           | not null | now()
   owner_id     | uuid                     |           |          |
  Indexes:
      "todos_pkey" PRIMARY KEY, btree (id)
      "idx_todos_created_at_desc" btree (created_at DESC, id DESC)
  Check constraints:
      "todos_title_not_blank" CHECK (char_length(btrim(title::text)) >= 1 AND char_length(btrim(title::text)) <= 200)
  Triggers:
      trg_todos_protect_created_at BEFORE UPDATE ON todos FOR EACH ROW EXECUTE FUNCTION todos_protect_created_at()
  ```
  （acceptance 第 3 條「UUID 主鍵、索引、約束」逐項對到；`BETWEEN 1 AND 200` 被 PostgreSQL 正規化為 `>= 1 AND <= 200`，語意相同）

  ```bash
  # 阻擋級 CR 判準（以固定字串 grep，另設對照組證明樣式有效）
  grep -nF "${" src/repositories/todo-repository.ts        # repository
  grep -nF "${" src/db/migrate.ts | head -5                # 對照組
  ```
  ```text
  --- repository ---
  REPO_EXIT:1   （無輸出＝零命中，判準通過）
  --- migrate.ts (對照組，應有命中) ---
  49:      throw new Error(`migration 檔名不符 NNN_描述.sql 格式：${filename}`);
  98:      throw new Error(`migration ${file.filename} 執行失敗，已 rollback：${reason}`);
  113:      console.log(`已套用 migration：${applied.join(", ")}`);
  MIG_EXIT:0
  ```
  （對照組證明 grep 樣式確實會命中 `${`，故 repository 的零命中是真的乾淨，不是樣式寫錯。migrate.ts 的三處命中皆在**錯誤訊息與 console.log**，不在任何 SQL 字串內——05 §3 的判準明文限定 repository 層，判定**不違反**。）

  ```bash
  # 收尾
  docker compose down -v
  rm -f .env
  docker ps --format "{{.Names}}"
  ```
  ```text
   Volume t-0013_todo-db-data Removed
   Network t-0013_default Removed
  （docker ps 無輸出，容器已全數移除；.env 未進版控）
  ```

- 下一步建議（小瑕疵，不退回；給後續卡）：
  1. **給 T-0016（WI-06）**：`TodoRow.createdAt` 是 `Date` 物件，不是字串（假設與決策②）。請在 route 的 response schema 以 `type: string, format: date-time` 讓 Fastify 序列化成 ISO 8601，**不要在 service 層再 `toISOString()` 一次**，否則會出現兩處格式化邏輯。另外 `updated_at`／`owner_id` 不在 `TodoRow` 內，API 契約也不回這兩欄，符合 04。
  2. **給 T-0016**：`deleteTodo` 回 `boolean`、`getTodoById`／`updateTodo` 回 `null` 代表不存在——請在 route 層轉成 `throw new AppError("E_NOT_FOUND", ...)`（T-0012 已提供），不要讓 `null` 流到回應。
  3. **給 T-0018（WI-08）**：Leader 裁決不做啟動時自動 migrate，migrate 是部署 pipeline 的獨立階段。請確保部署工作流的順序是 **build → migrate → deploy**，且 migrate 失敗即中止部署（runner 已以非 0 結束碼支援這點）。README 的 migrate 說明由我在批次 2 收尾 commit 先補一句，T-0018 再補完整部署章節。
  4. **給 qa-cr**：阻擋級 grep 判準建議照我的做法加一個「對照組」，確認 grep 樣式本身在該 shell 下真的會命中 `${`——WBS §1.3 原文的 `grep -nE "\$\{"` 在不同引號層級下有被吃掉跳脫字元而永遠零命中的風險（假陰性）。repository 內建的那條單元測試（程式化重讀原始碼斷言）不受 shell 跳脫影響，是更可靠的判準。
  5. **給 T-0018／qa-lead**：目前 `migrations/` 只有 001，runner 的「失敗即 rollback 並以非 0 結束」路徑**尚無自動化測試覆蓋**（整合測試只驗成功路徑與冪等）。建議在 T-0018 或測試卡補一條「刻意放一個語法錯誤的 migration → 期望非 0 結束且 schema_migrations 無該版本」的測試。
- 需要 Leader 裁決的事：
  - 無。交接檔提的兩項（單元測試納入 outputs、migrate 腳本歸屬）Leader 已於 2026-09-19T08:39:11 裁決；migrate 腳本與 README 由我在批次 2 收尾 commit 處理。
