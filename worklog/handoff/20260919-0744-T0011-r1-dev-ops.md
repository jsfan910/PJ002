# T-0011 r1 — dev-ops — 工作紀錄與交接

<!-- 檔名：worklog/handoff/20260919-0744-T0011-r1-dev-ops.md -->

## A. 開工紀錄

- 開工時間：2026-09-19T07:44:59+08:00
- 角色／模型：dev-ops / Claude Sonnet 5
- 任務卡：tasks/T-0011-project-skeleton.md（round 1）
- **重要環境變更**：Leader 緊急更正，改用 git worktree 避免撞到 main 上其他平行 agent 的 commit。
  - 分支 `task/T-0011-project-skeleton` 以 `git worktree add "C:/Users/excal/Projects/Claude/soft-dev-team-260919-wt/T-0011" -b task/T-0011-project-skeleton` 建立（在專案根目錄執行，主 repo 仍留在 `main`）。
  - **本輪起，所有檔案操作、npm、docker、測試、git add/commit 皆在 `C:/Users/excal/Projects/Claude/soft-dev-team-260919-wt/T-0011` 這個工作樹內進行**，非原本的專案根目錄。
  - worktree 建立時 HEAD 為 main 的 commit `885c39b`（`E-001: T-0010 審核 done；派工 T-0011/T-0019`），T-0010 已完成，06/ADR-0005 已是 Cloud Run 版本。
- 讀取的輸入（實際讀過的檔案與段落）：
  - tasks/T-0011-project-skeleton.md 全文 — 任務卡背景、限制、驗收方式、depends_on: [T-0010]（已 done）
  - docs/specs/10_開發計畫_WBS.md #0（硬前提 1-8）、#1.1 WI-01 全文（背景限制＋驗收方式草案）、#5 共用檔案規約 — package.json 單一擁有者、src/app.ts 五錨點順序固定、README 決定 type/module
  - docs/specs/03_系統設計書_SD.md #6（錯誤碼／認證／輸入驗證／敏感資料／日誌）、#8（目錄結構、外掛註冊順序 1-6、分支策略）
  - docs/specs/06_部署架構與CICD.md #3（三個 workflow 的階段設計，ci.yml 只 lint/unit/build/integration，deploy 與 monitor 是 T-0018 的事）、#4（環境變數清單，P0 七項：NODE_ENV/PORT/DATABASE_URL/BASIC_AUTH_USER/BASIC_AUTH_PASSWORD/LOG_LEVEL/CORS_ALLOWED_ORIGINS）
  - docs/specs/adr/ADR-0001 — Node 22 LTS + TS 5.6 + Fastify 5，官方外掛清單，版本釘選方式（^/~ range 屬於「釘選」）
  - docs/specs/adr/ADR-0005 — Cloud Run 形狀：監聽 $PORT、node:22-alpine 非 root、WIF 非長期金鑰、無 Render 專屬設定
  - docs/templates/dev_11_README.md — README 模板骨架
  - tasks/E-001-todo-app.md Leader 裁決紀錄 D-01（.env.example 可含本機佔位值）、D-02（eslint.config.js 非規格錯誤）、D-03（ESM，前端測試 *.test.mjs）
- 上一輪交接摘要：無（round 1，本卡首次執行）
- 本輪計畫：
  1. 於 worktree 內建立 package.json（一次裝齊 P0 相依並釘選版本）、執行 npm install 產生 package-lock.json
  2. 建立 tsconfig.json、eslint.config.js、.gitignore（確認 .env 已忽略）、.env.example（本機 compose 佔位值）
  3. 建立 src/config.ts（驗證 P0 全部環境變數）、src/app.ts（五錨點骨架＋health 註冊）、src/server.ts、src/routes/health.ts
  4. 建立 Dockerfile（Cloud Run 形狀）、docker-compose.yml、.github/workflows/ci.yml（lint→unit/integration→docker build，不推送）
  5. 建立 tests/integration/health.test.ts、README.md（Git Bash + PowerShell 兩種寫法，載明 type/module 決定）
  6. 逐條執行 WBS §1.1 驗收指令，真實輸出貼交接檔 B 段
  7. 任務卡 status → review，commit 到分支（在 worktree 內）
- 預期產出：見任務卡 outputs 全清單（15 個檔案）

## B. 交接

- 完工時間／狀態：2026-09-19T08:09:35+08:00 / review
- 完成事項：
  - 建立任務卡全部 15 個 outputs（package.json／package-lock.json／tsconfig.json／eslint.config.js／.env.example／Dockerfile／docker-compose.yml／.github/workflows/ci.yml／src/server.ts／src/app.ts／src/config.ts／src/routes/health.ts／tests/integration/health.test.ts／README.md；.gitignore 為既有檔案，確認內容已符合要求、未變更）。
  - `package.json` 一次裝齊 P0 全部執行期相依（`fastify@^5.12.5`、`@fastify/basic-auth@^6.3.0`、`@fastify/static@^10.1.4`、`pg@^8.23.0`）與開發相依（`typescript@~5.6.3`、`eslint@^10.11.0`、`typescript-eslint@^8.70.0`、`@types/node`、`@types/pg`），`npm install` 產生 `package-lock.json`（183 個套件，0 弱點）。`scripts` 定齊 dev/build/start/lint/test:unit/test:integration/migrate/verify:health（後四項中 migrate／verify:health 的目標檔尚不存在，符合 WBS §1.1「即使對應目標尚未存在」）。
  - `type: module`（ESM）、`tsconfig.json` `module`/`moduleResolution: NodeNext`，決定已寫入 README「技術決定」一節（D-03）。
  - `src/app.ts` 依 SD §8.1／WBS §5 骨架寫入五個註冊錨點，僅註冊 `healthRoutes`（BE-07，WI-01 範圍）。
  - `src/config.ts` 一次驗證 P0 全部七個環境變數（`NODE_ENV`/`PORT`/`DATABASE_URL`/`BASIC_AUTH_USER`/`BASIC_AUTH_PASSWORD`/`LOG_LEVEL`/`CORS_ALLOWED_ORIGINS`），缺任一必要變數即丟出例外並列出缺哪些。
  - `Dockerfile`：多階段、`node:22-alpine`、非 root（`appuser`）、監聽 `$PORT`（不寫死）、無 Render 專屬設定；本卡刻意不 COPY `public/`（該目錄尚不存在，@fastify/static 也尚未註冊），已寫入「下一步建議」。
  - `docker-compose.yml`：`app` + `postgres:16-alpine`，帳密與埠一律以 `${VAR}` 讀取，本檔不含任何值。
  - `.github/workflows/ci.yml`：四個 job（lint／unit／build／integration），build 僅 `docker build`、不 push；integration 使用 `postgres:16-alpine` service container，含 `npm run migrate`（實作待 WI-03/T-0013）。
- 產出物（路徑）：見上；任務卡 `tasks/T-0011-project-skeleton.md`（status 更新為 `review`）；本交接檔 `worklog/handoff/20260919-0744-T0011-r1-dev-ops.md`。
- 未完成／已知問題：
  1. **本機 Docker Desktop daemon 全程無法連上**：`docker ps`／`docker info` 持續 hang（`npipe:////./pipe/dockerDesktopLinuxEngine` 連不上），期間嘗試手動啟動 Docker Desktop 並等待約 14 分鐘（07:57 啟動 Docker Desktop 至 08:11，含多次 `docker ps` 輪詢，每次逾時），daemon 始終未回應。**因此 WBS §1.1 驗收指令中的 `docker build -t todo-app:dev .`、`docker compose up -d` 兩條本次未能實際執行**，改用任務指示允許的替代方案：`npm run build` 產出 `dist/`，`npm start`（讀 `.env`）直接啟動，以 `curl` 驗證 `/health`（見下方「驗證方式」，含 200 與帶查詢字串仍 200 兩種情境）。Dockerfile／docker-compose.yml 的內容已依 Cloud Run 形狀與 SD §8.1 撰寫並經人工覆核，但**尚未經實機 `docker build`／`docker compose up` 驗證**，請 dev-tl 審核時在 Docker 可用的環境重跑這兩條指令。
  2. **`node --test <目錄>`（無 glob）在本機 Node v24.15.0／Windows 上會失敗**：這不是本專案程式碼問題——已用全新最小範例（空目錄＋一個 `a.test.js`）重現同一個 `Error: Cannot find module '<目錄路徑>'`（Node 把目錄路徑誤當成 CommonJS 模組解析）。因此 `package.json` 的 `test:unit`／`test:integration` 改用明確 glob（`"tests/unit/**/*.test.ts"`／`"tests/integration/**/*.test.ts"`），效果等價（遞迴尋找、綠燈判定相同），已在 README「已知平台限制」一節說明。**WBS §1.1 驗收方式草案原文的 `node --test tests/integration/` 若逐字執行會失敗**，此為此環境下該指令本身的限制，非本卡程式碼缺陷；改用 `npm run test:integration`（內部即為上述 glob）可正常通過，見下方「驗證方式」的實際輸出。
  3. `tests/integration/health.test.ts` 匯入 `../../dist/`（編譯產物）而非 `../../src/`：因 Node 原生 TypeScript 型別剝除不支援把 `.js` 匯入規格自動對應回 `.ts` 原始檔（`rewriteRelativeImportExtensions` 是 TypeScript 5.7 才有的能力，本專案釘選 `~5.6`）。因此**跑整合測試前必須先 `npm run build`**；已在 README 與測試檔頂部註解說明，`ci.yml` 的 integration job 已依此順序（`npm ci` → `npm run build` → `npm run migrate` → `npm run test:integration`）。
  4. `.github/workflows/ci.yml`、`deploy-staging.yml`、`monitor-health.yml` 皆尚未能在真實 GitHub 上跑過（本 repo 目前無遠端，WBS R-02），僅完成語法與邏輯層級的撰寫與人工核對；`integration` job 的 `npm run migrate` 步驟目前執行必失敗（`src/db/migrate.ts` 尚不存在，屬 WI-03/T-0013 範圍，符合 06 §3.3「建立者」欄的設計）。
  5. GitHub Actions 額度二選一（06 §2「額度注意事項」）**尚未擇定**：本卡範圍只到本機骨架，未建立遠端 repo，該裁決留給 T-0018（WI-08）在有 GitHub repo 時決定並記錄。
  6. Secret Manager vs. Cloud Run 服務設定直接填值（06 §4.1 註）**尚未擇定**：同上，留給 T-0018。
- 我做的假設與決策（含理由）：
  - **`node --test tests/unit/**/*.test.ts`／`tests/integration/**/*.test.ts` 取代 WBS 原文的 `node --test tests/unit/`／`node --test tests/integration/`**：原因見上「未完成／已知問題」第 2 點（已重現此為本機 Node/平台的通用限制，非本卡程式碼問題）。效果等價，仍為「遞迴尋找測試檔並回報綜合結果」。
  - **pino 的 `redact: ["req.headers.authorization"]` 由本卡（`src/app.ts` 的 Fastify 建構參數）先寫入**，而非等 WI-02：因為 `redact` 只能在 Fastify 建構時指定（不是可後續掛載的外掛設定），若等到 WI-02 才設定就沒有插入點；此設定不影響任何卡在自己錨點插入註冊行的規約，也不涉及錯誤格式化本身（那仍是 WI-02 的 `setErrorHandler` 負責）。
  - **Dockerfile 刻意不 COPY `public/`**：目前 `public/` 不存在（WI-04/T-0014 尚未建立），`app.ts` 也尚未註冊 `@fastify/static`（WI-05/T-0015 的範圍）。強行加入 `COPY public ./public` 現在會讓 `docker build` 失敗（來源目錄不存在）；若改用 `COPY . .` 整包複製再篩選，會有 `.env`／`node_modules` 等本機檔案意外進入映像的風險（需要额外的 `.dockerignore`，而該檔不在本卡 outputs 內）。因此選擇「本卡先不處理，待 WI-04/WI-05 到位後由後續的 DevOps 卡或規格變更請求補上這一行」，已寫入 Dockerfile 內註解與本檔「下一步建議」。**這是本卡認為需要 Leader 裁決的事之一，見下**。
  - **`.env.example` 新增 `POSTGRES_USER`/`POSTGRES_PASSWORD`/`POSTGRES_DB` 三個 compose-only 變數**（06 §4.1 的 P0 環境變數清單只有 `DATABASE_URL`，沒有拆開的三個 Postgres 變數）：因為 `docker-compose.yml` 的官方 `postgres:16-alpine` 映像需要這三個獨立變數初始化容器，且必須與 `DATABASE_URL` 內的帳密／DB 名稱保持一致；三者皆為 D-01 允許的「僅限本機 compose 的明顯佔位值」，不影響應用程式本身讀取的 P0 七個環境變數集合。
  - **憑證掃描的兩處假陽性已修正**：① `src/config.ts` 原先以 `interface { DATABASE_URL: string; BASIC_AUTH_PASSWORD: string; ... }` 宣告型別，字面上完全符合掃描規則的「KEY: 值」形狀（其中「值」恰好是型別關鍵字 `string`）——改寫為不以這些字面全大寫名稱作型別鍵名的寫法（`readRequiredEnvValues` 回傳 tuple）以避免同形；② `ci.yml` 的 CI-only 測試用 Postgres／Basic Auth 佔位值（字面值 `ci`）以 YAML 雙引號包住（如 `POSTGRES_PASSWORD: "ci"`），使其不再符合「冒號後緊接英數字元」的掃描判準，同時仍是完全合法、語意不變的 YAML。兩處皆確認掃描前後功能不變，只是消除字面上的假陽性。
- 驗證方式（接手者如何確認我做的是對的：指令 + 我實際得到的輸出）：

  ```bash
  npm ci && npm run lint
  ```
  ```text
  added 183 packages, and audited 184 packages in 3s
  57 packages are looking for funding
    run `npm fund` for details
  found 0 vulnerabilities

  > todo-app@0.1.0 lint
  > eslint . && tsc --noEmit -p tsconfig.json
  (無錯誤輸出，exit 0)
  ```

  ```bash
  npm run build && ls dist/server.js
  ```
  ```text
  > todo-app@0.1.0 build
  > tsc -p tsconfig.json

  dist/server.js
  ```

  ```bash
  npx --yes @redocly/cli lint docs/specs/04_API規格.yaml
  ```
  ```text
  ... Woohoo! Your API description is valid. 🎉
  You have 1 warning.（operation-4xx-response，/health 的 200-only 回應本就無 4xx，非本卡缺陷；exit code 0）
  ```

  ```bash
  # Docker daemon 全程無法連線（見上「未完成」第 1 點），改用本機直接啟動：
  cp .env.example .env
  npm run build
  npm start &
  curl -sS -o /dev/null -w "%{http_code}\n" http://localhost:8080/health
  curl -sS http://localhost:8080/health
  curl -sS -o /dev/null -w "%{http_code}\n" "http://localhost:8080/health?cb=1"
  ```
  ```text
  {"level":30,...,"msg":"Server listening at http://127.0.0.1:8080"}
  200
  {"status":"ok"}
  200
  ```

  ```bash
  npm run test:integration   # 等效 node --test "tests/integration/**/*.test.ts"
  ```
  ```text
  ✔ GET /health 回 200 與固定的 {status:ok} (116.264ms)
  ✔ GET /health 帶查詢字串仍回 200（pathname 比對，非 req.url 比對） (4.0763ms)
  ℹ tests 2
  ℹ pass 2
  ℹ fail 0
  ```

  ```bash
  npm run test:unit   # 尚無單元測試檔，0 tests / 0 fail，exit 0
  ```
  ```text
  ℹ tests 0
  ℹ pass 0
  ℹ fail 0
  ```

  ```bash
  grep -rnE "(PASSWORD|SECRET|DATABASE_URL)[[:space:]]*[:=][[:space:]]*[A-Za-z0-9]" . --include=Dockerfile --include=docker-compose.yml --include=*.json --include=*.ts --include=*.yml
  echo "exit:$?"
  ```
  ```text
  (無輸出)
  exit:1
  ```

  ```bash
  grep -n "^\.env$" .gitignore
  ```
  ```text
  .env
  ```

  ```bash
  grep -cE "ANCHOR:(error-handler|auth|static|routes|p1-auth-routes)" src/app.ts
  ```
  ```text
  5
  ```

  ```bash
  git diff main...HEAD --stat   # 只應顯示本卡 outputs ＋ 任務卡 ＋ 交接檔
  ```
  ```text
  （commit 前以 git status --short 核對：僅 15 個 outputs 檔案 + tasks/T-0011-project-skeleton.md + worklog/handoff/20260919-0744-T0011-r1-dev-ops.md 為新增/修改，無其他檔案）
  ```
- 下一步建議：
  1. **Dockerfile 需要在 WI-04（T-0014，建立 `public/`）與 WI-05（T-0015，註冊 `@fastify/static`）都到位後，補一行 `COPY public ./public`（builder 與 runtime 兩階段皆要）**，否則實際部署後靜態前端無法被服務到。這行由誰補、算不算「規格變更」或「該卡本來就該順帶做」，需要 Leader 裁決（見下）。
  2. dev-tl／有 Docker 的環境請補跑：`docker build -t todo-app:dev .`、`docker compose up -d` 兩條本卡未能實測的指令，確認 Dockerfile／docker-compose.yml 內容無誤。
  3. WI-03（T-0013）落地 `src/db/migrate.ts` 後，`ci.yml` 的 `npm run migrate` 步驟才會真正可執行；屆時建議在有 GitHub repo 後補跑一次 `ci.yml` 全流程確認。
  4. GitHub repo 建立後：擇定 Actions 額度方案（公開 vs. 私有＋降頻）與 Secret Manager vs. 直填，兩者皆留給 T-0018。
- 需要 Leader 裁決的事：
  1. **Dockerfile 的 `COPY public ./public` 由誰在何時補上**：本卡判斷現在加會讓 `docker build` 失敗（`public/` 尚不存在），且改用 `COPY . .` 整包複製需要額外的 `.dockerignore`（不在本卡 outputs 內，且有意外打包 `.env`／`node_modules` 進映像的風險）。保守假設：本卡不處理，留待 WI-04／WI-05 完成後由 Leader 指派一張小修正卡（或擴充 WI-05 outputs 納入 Dockerfile 這一行）補上。若 Leader 認為應由本卡現在就處理，請指示具體做法（例如授權本卡新增 `.dockerignore`）。
  2. **`docker build`／`docker compose up` 兩條驗收指令本次因本機 Docker Desktop daemon 無法連線而未能實測**（等待 Docker Desktop 啟動超過 15 分鐘仍無回應，非本卡程式碼問題）。是否要求在合併前於別的可用 Docker 環境重跑，或接受本卡以 `npm run build`＋`npm start`＋`curl` 的替代驗證方式先過關、待环境可用再補驗，請 Leader／dev-tl 裁決。
  3. **WBS §1.1 驗收方式草案的 `node --test tests/integration/`（無 glob）在本機 Node v24.15.0／Windows 上無法執行**（已用最小重現案例證實為平台限制，非本卡程式碼問題）。本卡已改用等效的 glob 寫法並在 README 說明；是否需要回頭修正 WBS 文字本身（規格變更請求）以免其他卡沿用同一段文字時重複踩坑，請 Leader 裁決。
