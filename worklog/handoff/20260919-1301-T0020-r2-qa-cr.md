# T-0020 r2 — qa-cr — 工作紀錄與交接

<!-- 檔名：worklog/handoff/20260919-1301-T0020-r2-qa-cr.md（時間 = 開工時間，取實查系統時間） -->
<!-- 規則：開工立即寫 A 段；完工或中止時寫 B 段。同一輪同一角色一個檔。 -->

## A. 開工紀錄

- 開工時間：2026-09-19T13:01:02+08:00
- 角色／模型：qa-cr / opus
- 任務卡：tasks/T-0020-code-review.md（round 2）
- 讀取的輸入（實際讀過的檔案與段落，非任務卡照抄）：
  - `.claude/agents/qa-cr.md` —（本角色通用協定：開工四步、收尾五步、只 add 本卡 outputs／任務卡／交接檔）
  - `docs/reports/20260919-1146-CR-E001.md` —（r1 的 13 項發現原文：B-1／B-2 與 S-1～S-11 的檔案:行號、說明、修正建議，作為 r2 逐項複審的基準）
  - `tasks/E-001-todo-app.md` 最後五段 Leader 裁決 —（撤回 T-0013-③ 改為規格優先做啟動時自動 migration；S-1 關閉 ajv coerceTypes；S-8 改列 CR 模板已知例外第 4 類不改凍結 SD；修正卡切分 T-0024／25／26；修正卡合併完成 main `4b96b20`，dev 腳本拆 `dev:build`／`dev:run` 經追認）
  - `tasks/T-0024-fix-auth-migrate.md`、`tasks/T-0025-fix-ops.md`、`tasks/T-0026-fix-frontend.md` —（三張修正卡的 outputs 與 acceptance，以及 dev-tl r1 審核紀錄列）
  - `worklog/handoff/20260919-1227-T0024-r1-dev-tl.md`、`-1236-T0025-r1-dev-tl.md`、`-1246-T0026-r1-dev-tl.md` —（dev-tl 的審核證據與三項範圍例外核准：Dockerfile COPY migrations、package.json 三處、tests 型別註記）
  - `docs/templates/code_review_report.md` —（已新增「憑證掃描已知例外」四類清單，第 4 類為 D-01 本機佔位值，對應 r1 的 S-8）
  - main `6df63f1` 上的修正後檔案：`src/plugins/basic-auth.ts`、`src/server.ts`、`src/app.ts`、`Dockerfile`、`docker-compose.yml`、`package.json`、`tsconfig.test.json`、`.github/workflows/*.yml`、`public/assets/todo-store.js`、`todo-view.js`、`tests/integration/startup-migrate.test.ts`、`auth-basic.test.ts`
- 上一輪交接摘要（round 2）：
  - 退回原因（r1）：阻擋級 2 項 —— B-1 `/health` 豁免比對用 `new URL().pathname`，WHATWG 正規化點節段與 `%2e`，`/foo/../health` 等四個變體未帶憑證即越過 Basic Auth hook；B-2 migration runner 未接進應用啟動流程，依 README 方式一裝起來的服務所有待辦 API 回 500。另建議級 11 項（S-1～S-11）。
  - 本輪要修的點：**不重審全量**，只複審 r1 這 13 項是否確實修正（逐項標「已修正／部分修正／未修正」附證據），並檢查修正是否引入新問題（回歸）。B-1 必須用 `curl --path-as-is`／`node:http` 直送 request-target 對**真實容器**驗證（`app.inject` 會正規化，得到假綠燈）；B-2 必須 `docker compose down -v` 後 `up --build` 驗容器啟動即自動 migration；確認 `startup-migrate.test.ts` 在有 DB 時真的執行而非 skip。
- 本輪計畫（7 條）：
  1. 讀 main 上修正後的全部相關檔案與三份 dev-tl 審核交接檔，逐項對照 r1 發現的「修正建議」欄。
  2. `npm run build` → `npm run lint`（順序依提示詞，build 先於 lint）。
  3. 起自用埠的獨立 compose 專案（`POSTGRES_HOST_PORT`／`PORT` 皆用自用埠），`down -v` 後 `up --build` 驗 B-2：乾淨 volume 啟動即自動建表、`GET /api/v1/todos` 回 200 `[]`。
  4. 對該真實容器以 `curl --path-as-is` 與 `node:http` 直送原始 request-target，驗 B-1 四個點節段變體與 S-2 `POST /health` 皆 401，且 `/health`、`/health?x=1` 仍 200。
  5. 跑 unit 與 integration 全量，確認 `startup-migrate.test.ts` 真的執行（不是 skip），integration 應為 56/56。
  6. 逐項驗 S-1～S-11 的修正證據（靜態閱讀 + 能實跑的就實跑），並掃回歸（新引入問題）。
  7. 產出 `docs/reports/20260919-HHMM-CR-E001-r2.md`，結論阻擋級 = N；收尾填 B 段、任務卡 `status: review`、commit 三個檔。
- 預期產出：`docs/reports/20260919-HHMM-CR-E001-r2.md`（r2 專屬報告，不改 r1 報告；不修改任何程式碼）

## B. 交接

- 完工時間／狀態：2026-09-19T13:16:00+08:00 / review
- 完成事項：
  - 逐項複審 r1 的 13 項發現（B-1、B-2、S-1～S-11），**全部判定「已修正」**，無「部分修正」「未修正」；每項附檔案:行號與實跑或讀碼證據。
  - B-1 以**真實容器**（獨立 compose 專案 `crr2`，app 8097／db 5439）驗證：`curl --path-as-is` 送 13 種 request-target，r1 的四個繞過變體由 403 全部變 401；另以 `node:http`／`net` 直送 8 種畸形 request-target（絕對式、`;` 參數、`#` 片段、`%23`、`/..;/`）尋找新繞過，未發現。
  - B-2 以 `docker compose -p crr2 down -v` → `up -d --build` 的**全新 volume** 驗證：未執行任何 migrate 指令，容器啟動即自動建表，`GET /api/v1/todos` 回 200 `[]`；`schema_migrations.applied_at` 早於 `Server listening` 60ms；失敗路徑（DB 連不上）記錄原因後 `NODE_EXIT=1` 且埠未監聽。
  - 確認 `tests/integration/startup-migrate.test.ts` 在有 DB 時**真的執行**（`skipped 0`，該測試 959.27ms 通過），不是 skip 的假綠燈。
  - `npm run build`（0）→ `npm run lint`：13:02 對 committed `beb3f0b` 為**紅**（24 錯誤全在 `tests/e2e/*`），13:12 dev-tl 收尾 commit `e7abe4f` 後重跑為**綠**（build 0、lint 0）。
  - 回歸檢查：新增 2 項建議級（N-1、N-2）＋ 1 項觀察（N-3），詳見報告；憑證掃描（含 T-0021 新進檔案）真實憑證 0 筆；`innerHTML` 等五個樣式對 `public/`＋`src/` 仍 NO MATCH。
  - 核實三張修正卡 dev-tl 審核交接檔的宣稱，全部屬實（unit 由 75→76 是 T-0026 併入 store 測試所致，非矛盾）。
- 產出物（路徑）：
  - `docs/reports/20260919-1309-CR-E001-r2.md`（r2 複審報告；r1 報告 `docs/reports/20260919-1146-CR-E001.md` 未修改）
  - `tasks/T-0020-code-review.md`（outputs 追加 r2 報告、`status: review`、`updated`）
  - 本交接檔
- 未完成／已知問題：
  - N-1（建議級，未修）：`runMigrations` 無互斥鎖，多實例同時對**全新資料庫**冷啟時，其中一方會因 `schema_migrations` 的 PK 衝突而 `exit(1)`。建議列 P1 或與 `002` migration 同卡處理，不必為此開 P0 修正卡。
  - N-3（觀察）：`T-0024` acceptance 寫「`POST {"isCompleted":null}` 回 400」與實際 201 不符；成因是 `removeAdditional` 先移除未宣告欄位（既有且合規的行為），非型別脅迫復發。S-1 在 `PATCH` 與兩處 `title` 皆確實回 400。
- 我做的假設與決策（含理由）：
  1. **N-2（lint 紅燈）不列阻擋級**。理由：成因不在 T-0024～T-0026 的修正，而在 S-4（lint 擴及 `tests/`）與 T-0021 合併（`tests/e2e`）的交互；且我在本輪結束前已實查 dev-tl 的收尾 commit `e7abe4f` 內含三處修法（`tsconfig.e2e.json`、`tsconfig.test.json` 的 `exclude`、`package.json` 的 `lint`），對該 commit 重跑 build 與 lint 皆為 0。把已關閉的事項記為阻擋會製造無謂的修正卡；但紅燈事實與時間點仍完整寫進報告 N-2 存查。
  2. **N-1 列建議級而非阻擋級**。理由：部署工作流的 migrate 階段先於 deploy 執行（Leader 保留的雙重保險），實例啟動時 `runMigrations` 幾乎恆為 no-op；`max-instances 2` 且 Cloud Run 會重試。屬「三個月後會咬人」的結構問題，記錄但不擋本輪。
  3. **S-1 判定為已修正**（儘管 T-0024 acceptance 的字面有一條不符）。理由見 N-3：`POST` 的 201 來自 `removeAdditional`，與 r1 指出的 `coerceTypes` 缺陷無關，且符合 `04_API規格.yaml` 的 `CreateTodoRequest` 只宣告 `title`。
  4. **只複審修正處與回歸，不重做 40 條「假設與決策」表態與全量規格比對**。理由：Leader 2026-09-19T12:03:12 裁決明文「修正卡合併後 T-0020 開 r2 只複審修正處」，且 r1 的那些結論未被本次修正觸及。檢查清單該兩項標「不適用」並寫明理由。
  5. **自用資源隔離**：用獨立 compose 專案名 `crr2` 與自用埠 8097／5439（`POSTGRES_HOST_PORT`），全程未碰他人正在使用的 `soft-dev-team-260919-*`（8080／5432）與其資料；收尾 `down -v` 清除，`docker ps` 確認他人服務不受影響。
- 驗證方式（接手者如何確認我做的是對的：指令 + 我實際得到的輸出）：
  ```bash
  # 1) 乾淨環境自動 migration（B-2）——自用埠，勿用 8080/5432
  printf "NODE_ENV=development\nPORT=8097\nPOSTGRES_HOST_PORT=5439\nDATABASE_URL=postgres://dev:dev@db:5432/todo\nPOSTGRES_USER=dev\nPOSTGRES_PASSWORD=dev\nPOSTGRES_DB=todo\nBASIC_AUTH_USER=dev\nBASIC_AUTH_PASSWORD=dev\nLOG_LEVEL=info\nCORS_ALLOWED_ORIGINS=\n" > /tmp/cr.env
  docker compose -p crr2 --env-file /tmp/cr.env down -v
  docker compose -p crr2 --env-file /tmp/cr.env up -d --build
  curl -sS -u dev:dev -w "\nHTTP=%{http_code}\n" http://localhost:8097/api/v1/todos
  docker exec crr2-db-1 psql -U dev -d todo -c "SELECT version, applied_at FROM schema_migrations"

  # 2) 豁免邊界（B-1／S-2）——必須 --path-as-is，且對真實容器不可用 app.inject
  for P in /health "/health?x=1" "/foo/../health" "/.%2e/health" "/%2e%2e/health" "/assets/%2e%2e/%2e%2e/health"; do
    printf "%-34s -> " "$P"; curl -sS --path-as-is -o /dev/null -w "%{http_code}\n" "http://localhost:8097$P"
  done
  for M in GET HEAD POST PUT PATCH DELETE OPTIONS; do
    printf "%-8s -> " "$M"; curl -sS -X "$M" -o /dev/null -w "%{http_code}\n" "http://localhost:8097/health"
  done

  # 3) migration 失敗即非 0 結束且不監聽
  DATABASE_URL="postgres://dev:dev@localhost:5999/nodb" PORT=18099 NODE_ENV=development \
    BASIC_AUTH_USER=dev BASIC_AUTH_PASSWORD=dev LOG_LEVEL=silent node dist/server.js; echo "NODE_EXIT=$?"

  # 4) build/lint/測試
  npm run build && npm run lint
  npm run test:unit
  NODE_ENV=test PORT=8097 DATABASE_URL="postgres://dev:dev@localhost:5439/todo" \
    BASIC_AUTH_USER=dev BASIC_AUTH_PASSWORD=dev LOG_LEVEL=silent npm run test:integration

  # 5) 收尾
  docker compose -p crr2 --env-file /tmp/cr.env down -v
  ```
  ```text
  # 1) 乾淨環境（Volume crr2_todo-db-data Removed 之後重建）
  []
  HTTP=200
   version |          applied_at
  ---------+-------------------------------
   001     | 2026-09-19 05:03:20.601436+00      ← 早於 Server listening（05:03:20.661Z）

  # 2) 豁免邊界
  /health                            -> 200
  /health?x=1                        -> 200
  /foo/../health                     -> 401     ← r1 為 403（已越過認證）
  /.%2e/health                       -> 401     ← r1 為 403
  /%2e%2e/health                     -> 401     ← r1 為 403
  /assets/%2e%2e/%2e%2e/health       -> 401     ← r1 為 403
  GET -> 200 / HEAD -> 200 / POST -> 401 / PUT -> 401 / PATCH -> 401 / DELETE -> 401 / OPTIONS -> 401

  # 3) migration 失敗路徑
  {"level":50,...,"code":"ECONNREFUSED"...,"msg":"啟動時套用 migration 失敗，服務不會啟動監聽"}
  NODE_EXIT=1
  netstat | grep 18099 → 18099 free

  # 4) build/lint（13:12，對 dev-tl 收尾 commit e7abe4f）
  BUILD_EXIT=0
  > eslint . && tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.test.json && tsc --noEmit -p tsconfig.e2e.json
  LINT_EXIT=0
  （同一指令 13:02 對 beb3f0b 為 LINT_EXIT=2，24 個錯誤全在 tests/e2e/*，見報告 N-2）

  # 4) 測試
  test:unit         ℹ tests 76  ℹ pass 76  ℹ fail 0  ℹ skipped 0
  test:integration  ℹ tests 56  ℹ pass 56  ℹ fail 0  ℹ skipped 0
    其中 ✔ 以與 src/server.ts 相同的啟動路徑（start()）啟動，對未套用 migration 的資料庫，
         GET /api/v1/todos 回 200 []（CR B-2，非 500） (959.2696ms)   ← 確認非 skip

  # 5) 收尾
  Container crr2-db-1 Removed / Volume crr2_todo-db-data Removed / Network crr2_default Removed
  docker ps → soft-dev-team-260919-app-1（8080）、soft-dev-team-260919-db-1（5432）  ← 他人服務未受影響
  ```
- 下一步建議：
  1. qa-lead 依任務卡「驗收方式」抽查 3 個發現的行號（建議抽 B-1 `src/plugins/basic-auth.ts:53-75`、B-2 `src/server.ts:35-56`、S-1 `src/app.ts:39-43`），並重跑上列指令 1～4。
  2. N-1 列入 P1 待辦或與 `002` migration 同卡：`runMigrations` 加 `pg_advisory_lock`，或 `INSERT ... ON CONFLICT (version) DO NOTHING` 後重新確認版本。
  3. 日後在 `tests/` 新增需要不同型別環境的測試，須比照 `tsconfig.e2e.json` 的三處同步做法（新 tsconfig ＋ `tsconfig.test.json` 的 `exclude` ＋ `lint` 串接）同一 commit 改齊，否則會重演 N-2 的紅燈。
  4. 寫驗收條件時，型別嚴格性一律取 `PATCH` 案例（`POST` 的未宣告欄位會先被 `removeAdditional` 移除）。
- 需要 Leader 裁決的事：
  - 無。（N-1 建議列 P1 待辦、N-3 為寫驗收條件的經驗，兩者皆不需裁決即可推進；T-0020 結論阻擋級 = 0）
