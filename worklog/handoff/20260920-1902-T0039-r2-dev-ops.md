# T-0039 r2 — dev-ops — 工作紀錄與交接

## A. 開工紀錄

- 開工時間：2026-09-20T19:02:00+08:00
- 角色／模型：dev-ops / Sonnet 5
- 任務卡：tasks/T-0039-secret-pin-verify-retry.md（round 2，status: rework → in_progress）
- 本次工作：依 dev-tl r1 審核紀錄逐條修法（見任務卡「審核紀錄」r1 列與「背景與限制」r2 追加段）
- 讀取的輸入：
  - `tasks/T-0039-secret-pin-verify-retry.md` 全文（含 r1 審核紀錄四項具體修法、r2 追加背景）
  - `worklog/handoff/20260920-1848-T0039-r1-dev-tl.md`（r1 審核完整交接檔，根因實查與逐條判定）
  - `.github/workflows/deploy-staging.yml`、`README.md`（IAM 角色迴圈、釘版本一節）、`docs/specs/06_部署架構與CICD.md` §6.9
  - `.claude/agents/dev-ops.md`
- worktree：`C:\Users\excal\Projects\Claude\soft-dev-team-260919-wt\T-0039`（分支 `task/T-0039-secret-pin-verify-retry`，已 fast-forward 合併 main 至 `54e8117`，無衝突）
- 本輪計畫（對應 r1 審核紀錄四項）：
  1. `.github/workflows/deploy-staging.yml` 的 `resolve_version()`：不讓 `gcloud secrets versions list` 非 0 被 `set -e` 吞掉，改為顯式捕捉 stdout+stderr 並判斷，失敗時輸出可讀 `::error::` annotation
  2. `README.md`：IAM 角色迴圈（Bash／PowerShell 各一處）補 `roles/secretmanager.viewer`；「釘定 Secret Manager 版本」一節補「需要 `secretmanager.versions.list` 權限，既有專案需補跑一次 `add-iam-policy-binding`」說明
  3. `docs/specs/06_部署架構與CICD.md` §6.9：補記本次失敗事實（run 35506278351 紅燈、根因）與「本機 gcloud 身分 ≠ CI WIF 服務帳號身分」的教訓
  4. 交接檔驗證方式：額外貼「以部署服務帳號實際權限集合驗證」的唯讀證據（`gcloud iam roles describe`／`get-iam-policy`），不再以本機擁有者帳號的成功當證據
  - **不執行任何 IAM 變更指令**（`add-iam-policy-binding` 屬使用者雲端安全設定，Leader 已裁決 IAM 交使用者），只在 README 寫步驟；驗證 CI 身分權限只用讀取指令
- 預期產出：`.github/workflows/deploy-staging.yml`、`README.md`、`docs/specs/06_部署架構與CICD.md`（僅 §6.9 追記）三檔修改；本交接檔 B 段

## B. 交接

- 完工時間／狀態：2026-09-20T19:11:00+08:00 / 完成，任務卡 `status: review`（round 2）
- 完成事項：
  1. `.github/workflows/deploy-staging.yml` 的 `resolve_version()`：改為先把 `gcloud secrets versions list` 的 stdout+stderr 收進變數 `out` 再判斷（`if ! out="$(...)"; then …; return 1; fi`），失敗時輸出含 gcloud 原始訊息與修復指引（需 `roles/secretmanager.viewer`，或改填 `SECRET_VERSION_*` repository variable 略過查詢）的 `::error::` annotation；`out` 為空（secret 存在但無 `ENABLED` 版本）另有獨立 `::error::`。三個呼叫端 `V_DB`／`V_USER`／`V_PASS` 都改成 `"$(resolve_version …)" || exit 1`，避免命令替代賦值被 `set -e` 直接中止而不留訊息（r1 根因）。
  2. `scripts/deploy-staging.sh` 同步套用相同修法（維持與 workflow 三處參數一致的既有要求），錯誤訊息改為一般 stderr（本機腳本無 `::error::` annotation 語義）。
  3. `README.md`：
     - Bash／PowerShell 兩處建立服務帳號的角色迴圈都補上 `roles/secretmanager.viewer`，並在迴圈上方加註解說明為何需要這個角色（`secretmanager.secretAccessor` 只有 `versions.access`、沒有 `versions.list`）、舊專案要補跑一次。
     - 「釘定 Secret Manager 版本與 verify 自動重試」一節第 1 點新增「前置權限（必讀）」子段，寫明所需權限、補跑指令、失敗時的行為（`PERMISSION_DENIED`、後續步驟全部 skipped），並註明 IAM 授權屬使用者雲端專案安全設定、需使用者自行執行。
     - 順手修正 PowerShell 迴圈呼叫改用 `gcloud.cmd`（CLAUDE.md 工作鐵則「PowerShell 裡呼叫 gcloud 要用 gcloud.cmd」，剛好在同一段落內）。
  4. `docs/specs/06_部署架構與CICD.md` 新增 `#### 6.9.1 r1 真實 run 紅燈事件與根因`：記載 run `35506278351` 紅燈事實、根因（`gcloud projects get-iam-policy`／`gcloud iam roles describe` 兩組實查輸出）、「本機 gcloud 身分 ≠ CI WIF 服務帳號身分」教訓、r2 修法摘要。`§1～§6.8`、`§6.9`（不含新增的 `6.9.1`）文字未動。
  5. 任務卡 frontmatter：`status: review`（round 2 維持不變）、`updated` 更新。
- 產出物（路徑，皆在 worktree `C:\Users\excal\Projects\Claude\soft-dev-team-260919-wt\T-0039` 內，尚未 commit，留給下一步 commit）：
  - `.github/workflows/deploy-staging.yml`
  - `scripts/deploy-staging.sh`
  - `README.md`
  - `docs/specs/06_部署架構與CICD.md`
  - `tasks/T-0039-secret-pin-verify-retry.md`
  - 本檔 `worklog/handoff/20260920-1902-T0039-r2-dev-ops.md`
- 未完成／已知問題：
  - **IAM 授權（`roles/secretmanager.viewer`）尚未執行**（本輪重新以唯讀指令確認，見下方「驗證方式」第 4 組，部署服務帳號目前仍只有四個角色，缺 `secretmanager.viewer`）。依 Leader 2026-09-20 19:01 裁決（commit `54e8117`：「IAM 交使用者」）與角色檔安全規則，**agent 不執行、不索取任何 IAM 變更指令**，已把可執行的指令與其用途完整寫進 README，等待使用者自行執行或改填 `SECRET_VERSION_*` repository variables 覆寫。
  - **在 IAM 授權完成（或改走 repository variables 路徑）之前，合併推送後的真實 `deploy-staging` run 預期仍會在 `resolve secret versions` 步驟失敗**——差別是這次會留下可讀的 `::error::` annotation（本輪已用模擬測試驗證，見下方「驗證方式」第 5 組），不會再是只有「Process completed with exit code 1.」的黑盒失敗。
  - acceptance 第 6 條（合併推送後 run 全綠）不在本卡 dev-ops 職責內判定，由 dev-tl 於審核紀錄勾核；本輪離線能驗證的第 1～5 條已全部完成並附證據。
  - worktree 與分支依 r1 交接檔指示保留未清理，續由 dev-tl 合併。
- 我做的假設與決策（含理由）：
  1. **`scripts/deploy-staging.sh` 一併套用同樣的 set -e 修法**，雖然 r1 審核紀錄的四項具體修法只點名 workflow 檔，但 acceptance 第 3 條要求「scripts/deploy-staging.sh 與 infra/cloudrun-service.yaml 與 workflow 三處參數一致」，且本機腳本存在完全相同的「命令替代賦值被 set -e 吞掉」邏輯缺陷（雖然本機執行者通常是專案擁有者、觸發機率較低，但錯誤處理邏輯不一致本身就是風險）。判斷：這是回應「三處一致」既有驗收條件的必要延伸，不是擴大範圍；outputs 本就含此檔。
  2. **不新增 `infra/cloudrun-service.yaml` 的改動**：該檔本輪 diff 為 0。理由：它是「文件化 IaC，非自動套用」的靜態範本，用 `<SECRET_VERSION>` 佔位符表達，本身不含可執行的 `gcloud secrets versions list` 呼叫，r1 的失敗與本檔無關，且 r1 審核紀錄的四項修法未提及此檔，強行改動反而違反「不擴大範圍」。
  3. **不執行、不建議使用者用 `gcloud secrets versions access` 之類的讀值指令做二次確認**：本輪僅用 `gcloud projects get-iam-policy`／`gcloud iam roles describe` 兩個純角色查詢指令佐證權限缺口，未讀取任何 secret 值，符合 CLAUDE.md 安全鐵則與角色檔「不索取、不代填憑證」。
  4. **README 的 PowerShell 段落改用 `gcloud.cmd`**：屬本卡本來就會touch 的同一段落（IAM 角色迴圈）之內的最小修正，對應角色檔「Windows／PowerShell 常見陷阱」第 5 條，不算擴大 outputs 範圍。
  5. **不重新推翻 r1「不 revert 合併」與「不升級模型」兩項決策**：Leader 已在 `54e8117` 裁決「不 revert、r2 重派、IAM 交使用者」，本輪延續執行，未再另行判斷。
- 驗證方式（接手者如何確認我做的是對的：指令 ＋ 我實際得到的輸出）：

  ```bash
  # 1) diff 範圍：只含 outputs ＋ 任務卡 ＋ 自己的交接檔
  cd C:/Users/excal/Projects/Claude/soft-dev-team-260919-wt/T-0039
  git status --short
  git diff --stat
  ```
  ```text
   M .github/workflows/deploy-staging.yml
   M README.md
   M docs/specs/06_部署架構與CICD.md
   M scripts/deploy-staging.sh
   M tasks/T-0039-secret-pin-verify-retry.md
  ?? worklog/handoff/20260920-1902-T0039-r2-dev-ops.md

   .github/workflows/deploy-staging.yml    | 35 ++++++++++++++++++++++-----------
   README.md                               | 26 +++++++++++++++++++-----
   docs/specs/06_部署架構與CICD.md         | 32 ++++++++++++++++++++++++++++++
   scripts/deploy-staging.sh               | 29 +++++++++++++++++----------
   tasks/T-0039-secret-pin-verify-retry.md |  4 ++--
   5 files changed, 98 insertions(+), 28 deletions(-)
  ```

  ```bash
  # 2) workflow 內不得再出現 :latest 形式的 secret 參照
  grep -n "secret:" .github/workflows/deploy-staging.yml
  ```
  ```text
  （零輸出，確認不存在）
  ```

  ```bash
  # 3) 語法層
  bash -n scripts/deploy-staging.sh && echo SYNTAX_OK
  node -e "const f=require('fs').readFileSync('.github/workflows/deploy-staging.yml','utf8');console.log(f.split('\n').length,'lines')"
  npx --yes yaml-lint .github/workflows/deploy-staging.yml infra/cloudrun-service.yaml
  ```
  ```text
  SYNTAX_OK
  268 lines
  √ YAML Lint successful.
  ```

  ```bash
  # 4) 以部署服務帳號的實際權限集合驗證（唯讀指令，符合 r1 要求④；
  #    未執行、未索取任何 IAM 變更或 secret 值讀取指令）
  gcloud projects get-iam-policy pj002-509106 --flatten="bindings[].members" \
    --format="table(bindings.role)" \
    --filter="bindings.members:github-deployer@pj002-509106.iam.gserviceaccount.com"
  gcloud iam roles describe roles/secretmanager.viewer --format="value(includedPermissions)"
  ```
  ```text
  ROLE
  roles/artifactregistry.writer
  roles/iam.serviceAccountUser
  roles/run.admin
  roles/secretmanager.secretAccessor

  resourcemanager.projects.get;resourcemanager.projects.list;secretmanager.locations.get;secretmanager.locations.list;secretmanager.secrets.get;secretmanager.secrets.getIamPolicy;secretmanager.secrets.list;secretmanager.secrets.listEffectiveTags;secretmanager.secrets.listTagBindings;secretmanager.versions.get;secretmanager.versions.list
  ```
  **判讀**：部署服務帳號目前仍缺 `roles/secretmanager.viewer`（即仍缺 `secretmanager.versions.list`），與 r1 實查結果一致——**IAM 尚未變更，屬預期中**（Leader 裁決交由使用者執行，本輪不代為執行）。這組輸出證明「本輪程式碼修正解決的是『失敗時看不到原因』，不是『讓這次部署立刻變綠』」，兩者是不同問題，避免混為一談。

  ```bash
  # 5) 模擬驗證 resolve_version() 的錯誤路徑確實會印出可讀 ::error::（而非被 set -e 吞掉）
  #    以 mock gcloud 模擬 PERMISSION_DENIED，抽出與 workflow 內完全相同的函式邏輯單獨執行
  #    （測試腳本寫在 session scratchpad，不在倉庫內，路徑僅供本輪重現）
  bash "C:\Users\excal\AppData\Local\Temp\claude\...\scratchpad\test_resolve.sh"; echo "SCRIPT_EXIT=$?"
  ```
  ```text
  before call
  ::error::無法列出 secret database-url 的版本（gcloud 原始訊息：ERROR: (gcloud.secrets.versions.list) PERMISSION_DENIED: Permission 'secretmanager.versions.list' denied）。範例修復指引
  SCRIPT_EXIT=1
  ```
  （另以模擬「gcloud 成功回傳版本號」與「override 分支」各跑一次，確認正常路徑不受影響，兩者皆正確印出對應版本號、EXIT=0；測試腳本置於 session scratchpad，未寫入倉庫）

  ```bash
  # 6) 倉庫中不含 secret 值、不新增 versions access 呼叫
  grep -rn "versions access" .github/workflows/deploy-staging.yml scripts/deploy-staging.sh infra/cloudrun-service.yaml README.md docs/specs/06_部署架構與CICD.md
  ```
  ```text
  README.md:220 / 227：既有的 T-0027 量測範例（`gcloud secrets versions access latest --secret=… --out-file=…`），非本卡新增，本卡未新增、未執行 `versions access`
  ```

- 下一步建議：
  1. **dev-tl 合併前，請先確認 IAM 現況**（見上方驗證方式第 4 組）：若使用者尚未執行 `roles/secretmanager.viewer` 授權、也未填 `SECRET_VERSION_*` repository variables，合併推送後的真實 run **預期仍會在 `resolve secret versions` 步驟失敗**，但這次會留下明確的 `::error::` annotation 與修復指引，不再是黑盒的 `exit code 1`。建議 dev-tl 合併後先看這次失敗訊息是否確如預期（驗證本輪修法真的有效），而非直接判失敗。
  2. 若使用者已完成 IAM 授權或填好 `SECRET_VERSION_*`，合併推送後應可看到 `resolve secret versions` 步驟成功、`GITHUB_STEP_SUMMARY` 出現三個版本號，流程接續 `verify`。
  3. T-0040（plan-sd）除原本待辦外，請一併確認 06 §2 服務帳號角色清單需補 `roles/secretmanager.viewer`（r1 交接檔下一步建議 4 已提過，本輪 README／§6.9.1 已落地文字，§2 本身的規格文字仍待 T-0040 同步，不在本卡 outputs 範圍）。
  4. T-0039 完成後，若使用者已完成 IAM 授權，建議請使用者本次一併確認 `github-deployer@` 服務帳號目前的四個角色是否也一併補齊（部分舊環境可能是分批建立），避免下次又漏一個角色。
- 需要 Leader 裁決的事：無（IAM 授權「由誰執行」已於 `54e8117` 裁決為使用者；本輪未發現新的裁決點）。

