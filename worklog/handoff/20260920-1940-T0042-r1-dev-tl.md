# T-0042 r1 — dev-tl — 工作紀錄與交接

<!-- 檔名：worklog/handoff/20260920-1940-T0042-r1-dev-tl.md -->

## A. 開工紀錄

- 開工時間：2026-09-20T19:40:02+08:00
- 角色／模型：dev-tl / opus
- 任務卡：tasks/T-0042-unit-tcid-release-notes.md（round 1）
- 讀取的輸入（實際讀過的檔案與段落，非任務卡照抄）：
  - `docs/reports/20260920-1806-測試總結-E001-r3.md` §1 退出準則表（12 項的最終數字）、§1.1 105 條分解（通過 101／部分通過 4：TC-009、TC-067、TC-080、TC-091）、§R-6（unit 11 條無法逐條追溯，沿用 Leader 2026-09-19T14:06:43 裁決 C）
  - `docs/reports/20260920-1822-Gate2-E001-r2.md` 第 2 章（三項應先知道的事）、第 3 章①②③、末段「使用者裁決（2026-09-20T18:35+08:00）」——① A 有條件通過、② **B 追認 TC-080 間接證據改判通過（第 1 項 102／105 ＝ 97.14%）**、③ A 先清 Gate 2 後待辦
  - `docs/specs/20_測試案例.md` 第 33、34、35、49、61、62、77、90、91、105、121 行（TC-001／002／003／012／019／020／030／038／039／048／059 的前置條件、步驟、預期結果逐字）
  - `tests/unit/todo-service.test.ts`（18 條）、`tests/unit/todo-repository.test.ts`（15 條）、`tests/unit/todo-store.test.mjs`（27 條）、`api-client.test.mjs`、`error-handler.test.ts` 全文——逐條比對哪一個 `test()` 對應哪一條 TC，以及各 TC 的子斷言是否真的被斷言
  - `src/schemas/todo-schema.ts:30-40`（`StatusFilterQuerystringSchema` 的 `enum: ["all","active","completed"]` ＋ `default: "all"`）與 `src/services/todo-service.ts:61-63`（`listTodos` 為純轉發、**不驗 status**）——確認 TC-048「非法值拋 ValidationError」在後端落在 schema 層而非 service 層
  - `docs/specs/24_缺陷清單.md` 第 17、20、47、50 行（D-014、D-017 現況：兩者 open／S3／列 P1；D-017 的關閉條件為「staging 重跑 TC-067／TC-009 至少 5 次連續全過」）
  - `docs/specs/14_ReleaseNotes.md`（v0.1.0 草案全文，`status: draft`、7 條已知問題、staging 未建立的假設已過時）、`docs/templates/dev_14_ReleaseNotes.md`（三段式骨架）
  - `CHANGELOG.md`（`[Unreleased]` 內已有 T-0038／T-0043／T-0039／T-0039 r2 四列，尚無版本標記）
  - `worklog/handoff/20260920-1834-T0041-r1-qa-at.md` B 段（Firefox `spawn UNKNOWN` 定位在 Windows CreateProcess，Chromium 對照組 4/4 通過，判工具限制非產品缺陷，NFR-004 維持 4／6）
  - `worklog/handoff/20260920-1915-T0039-r2-dev-tl.md` 相關結論已濃縮於 CHANGELOG `[Unreleased]` 的 T-0039 r2 列（r2 機制已在真實 run `35507519302` 上驗證生效，但仍紅燈於 `resolve secret versions`，等使用者授 `roles/secretmanager.viewer` 或填 `SECRET_VERSION_*`）
- 上一輪交接摘要（若 round > 1）：不適用（round 1）
- 本輪計畫（3–7 條）：
  1. 在 worktree `…-wt/T-0042` 內作業（分支 `task/T-0042-tcid-release-notes`），根目錄保持 main
  2. 為 11 條 unit 層 TC 在 `tests/unit/**` 標註 TC-ID：**只動測試名稱字串與註解，不動任何 `assert`**；每個檔頭加一張「TC-ID 對應表」，逐條註明「完全覆蓋／部分覆蓋（未覆蓋的子斷言是哪一句）」，不以「有標到就算追溯」蒙混
  3. 跑 `npm run build`（service 測試讀 `dist/`）→ `npm run test:unit`，確認 76 條不減少且全綠（T-0038 已把 unit 由 76 升為 78，實跑為準）
  4. 改寫 `docs/specs/14_ReleaseNotes.md` 為 v0.1.0 定版：功能清單對應 US-001～US-010、staging 網址（不含憑證）、Gate 2 判定「有條件通過（2026-09-20，使用者裁決）」與三項條件、已知限制（D-014／D-017 現況、NFR-004 依 T-0041、NFR-007 前端子項僅本機、T-0039 部署綠燈待 IAM）、回滾指引指向 06 §5
  5. `CHANGELOG.md`：`[Unreleased]` 收斂為 `## [0.1.0] - 2026-09-20`（日期取實查系統時間），確認 T-0038／T-0039 條目在內
  6. 在 **main（根目錄）** 建 annotated tag `v0.1.0`（只建不刪），推送與否留 Leader 決定並在 B 段註明
  7. 收尾：B 段貼真實輸出、任務卡 `status: review`、`git commit -F`
- 預期產出：
  - `tests/unit/todo-service.test.ts`、`tests/unit/todo-repository.test.ts`、`tests/unit/todo-store.test.mjs`（TC-ID 標註）
  - `docs/specs/14_ReleaseNotes.md`
  - `CHANGELOG.md`
  - annotated tag `v0.1.0`

## B. 交接

- 完工時間／狀態：2026-09-20T19:58:40+08:00 / review
- 完成事項：
  1. **worktree 內作業**：`git worktree add …-wt/T-0042 -b task/T-0042-tcid-release-notes`，根目錄全程停在 main。worktree 無 `node_modules`，先 `npm ci`（186 packages，0 vulnerabilities）才跑得動 build／test——**未跑 `npm ci` 前的 `npm run test:unit` 會出現 53/55、2 個檔案 fail 的假象**（`tsc` 不存在 → `dist/` 未產生 → 讀 `dist/` 的兩個測試檔整檔失敗），接手者若看到這個數字請先確認有沒有裝依賴
  2. **11 條 unit 層 TC-ID 補標**（關閉 R-6）：`tests/unit/todo-service.test.ts`（10 條測試加前綴）、`todo-repository.test.ts`（7 條）、`todo-store.test.mjs`（2 條）共 **21 個 `test()` 名稱**加上 `[TC-xxx]` 前綴，三個檔頭各補一張「TC-ID 對應表」
  3. **對應表逐條標「完全覆蓋／部分覆蓋」並寫明沒被斷言到的是哪一句**（下方「假設與決策」有完整清單），不以「有標到就算追溯」充數
  4. **未改動任何 `assert`**：以 `git diff -U0 tests/unit/ | grep "^[+-]" | …` 濾掉註解與 `test(` 名稱行後**輸出為空**（見「驗證方式」），即三個測試檔的可執行程式碼一行未動
  5. `docs/specs/14_ReleaseNotes.md` 由 `draft` 改 `released` 並全文改寫（US-001～US-010 功能表、staging 網址不含憑證、Gate 2 三項條件 C-1～C-3 與現況、已知限制 KL-1～KL-11、回滾指引指向 06 第 5 章、相關文件索引）
  6. `CHANGELOG.md` 收斂：新增版本標記小節 `## [0.1.0] - 2026-09-20`（日期取實查系統時間 2026-09-20T19:50:41+08:00），原 `[Unreleased]` 下的 18 條全部歸入該版本並補 T-0042 自身條目；`[Unreleased]` 留空供 P1 累積；檔頭補一句版本標記的格式說明
  7. **合併回 main 並建 annotated tag `v0.1.0`**（只建不刪，**未推送**）——實際輸出見「驗證方式」末段
- 產出物（路徑）：
  - `tests/unit/todo-service.test.ts`、`tests/unit/todo-repository.test.ts`、`tests/unit/todo-store.test.mjs`
  - `docs/specs/14_ReleaseNotes.md`
  - `CHANGELOG.md`
  - annotated tag `v0.1.0`（在 `main` 上，local only）
- 未完成／已知問題：
  - **tag `v0.1.0` 未推送至 origin**。任務卡明寫「推送 tag 與否留 Leader 決定」，故本卡只在本機建立。推送指令：`git push origin v0.1.0`（刪除需 `git push origin :refs/tags/v0.1.0`，但任務卡限定「只建不刪」）
  - **Release Notes 的三項已知限制皆為「引用他卡結論」而非本卡實測**：D-017 現況引 T-0038 交接檔與缺陷清單第 50 列、Firefox 4／6 引 T-0041 報告、staging 部署阻塞引 T-0039 r2 交接檔與 CHANGELOG。本卡**未重跑** e2e、未打 staging、未碰任何雲端資源
  - **TC-001／TC-003／TC-019／TC-039／TC-048／TC-059 六條為「部分覆蓋」**，各自沒被斷言到的子句已逐條寫在測試檔頭表格與下方「下一步建議」。本卡受 outputs 限制（「只加 TC-ID，不改斷言」）未補測
  - `docs/specs/traceability.md` 未更新：不在本卡 outputs 內。R-6 的正式關閉（測試總結第 1 項的 unit 11 條由「整體佐證」改「逐條追溯」）需 qa-lead 在下一輪總結認定
- 我做的假設與決策（含理由）：
  - **標註形式選 `[TC-xxx]` 測試名稱前綴**，而非只寫在註解：`node:test` 的輸出會逐條印出測試名稱，前綴讓「跑一次測試」本身就是追溯證據；且 `grep -rn "TC-001" tests/unit` 一行即可定位，符合任務卡驗收指令的形式。既有 T-0038 留下的 `D-017／TC-009：` 前綴形式**未改動**（不在本卡要標的 11 條內），已在 `todo-store.test.mjs` 檔頭說明為何有兩種前綴
  - **老實標「部分覆蓋」而不是含糊帶過**。11 條裡有 6 條的預期結果含 unit 層根本驗不到或現行測試沒驗的子句，逐條如下：
    - **TC-001**：「repository 的寫入方法**零次**被呼叫」未斷言（測試只驗例外的 code／statusCode，沒檢查 `db.calls.length === 0`）；三組輸入中 `"\t\n "` 未單獨測
    - **TC-002**：**完全覆蓋**（步驟 3 由「先 trim 再交給 repository」＋「trim 後才 201 仍回 400」兩條夾擊）
    - **TC-003**：「`id` 為 UUID 格式」「`createdAt` 為建立當下時間」unit 層驗不到——兩者由 DDL 的 `gen_random_uuid()`／`DEFAULT now()` 產生，mock 回什麼就是什麼；但「**不存在 `completedAt`**」（BR-007）已被 `assert.deepEqual` 的四欄位完整比對釘住，多一個欄位就會紅
    - **TC-012**：**完全覆蓋**（`assert.match(command.text, /ORDER BY created_at DESC, id DESC/)`）
    - **TC-019**：`""` 未單獨測（與 `"   "` 走同一條 `validateAndTrimTitle`，見 `src/services/todo-service.ts:92-99`）；「repository 更新方法零次被呼叫」同樣未斷言
    - **TC-020／TC-030／TC-038**：**完全覆蓋**（TC-030 的 `assert.equal(command.text, "DELETE FROM todos WHERE id = $1")` 是字面完全相等，任何軟刪除寫法都會紅）
    - **TC-039**：「異動欄位只含 `is_completed`」已斷言；「回傳物件 `title`／`createdAt` 不變、無 `completedAt`」在 service 層只斷言了 `result.isCompleted === true`，完整四欄位比對在 repository 測試
    - **TC-048**：步驟 2（三值皆被接受）完全覆蓋；**步驟 1（不帶 `status` 等同 `all`）與步驟 3（`foo`／`ALL`／空字串拋 ValidationError）在後端不落在 `todo-service`**——`src/services/todo-service.ts:61-63` 的 `listTodos` 是純轉發、零驗證，實際落點是 `src/schemas/todo-schema.ts:30-40` 的 `StatusFilterQuerystringSchema`（`enum: ["all","active","completed"]` ＋ `default: "all"`），屬路由 schema／整合層。unit 層改標在前端 store 的兩條等效斷言（`FILTERS 常數固定三值，預設為 all`／`setFilter()：非法值不呼叫 API`），並在檔頭明寫「**不是**宣稱後端 service 層已驗 status」
    - **TC-059**：「非本地化字串、非 epoch 數字」已被 `deepEqual` 釘住（任何 `toLocaleString()`／`getTime()` 改寫都會紅）；「字串以 `Z` 或 `+00:00` 結尾」未斷言——轉換函式回傳的是 `Date` 物件，ISO 字串化發生在 `fast-json-stringify` 對 `format: "date-time"` 的序列化（依據為 `src/schemas/todo-schema.ts:22-26` 的檔頭說明，該處已註明實測過 `node_modules/fast-json-stringify/lib/serializer.js` 的行為）
  - **Release Notes 的 Gate 2 條件以使用者 2026-09-20T18:35 的三項裁決為準**，而非 Gate 報告建議欄的原文：①A 有條件通過、**②B 追認 TC-080 間接證據改判通過（第 1 項 101／105 → 102／105 ＝ 97.14%）**、③A 先清 Gate 2 後待辦。因此 Release Notes 與 CHANGELOG 一律寫 **102／105**、剩餘 3 條（不是測試總結 r3 當時的 4 條）
  - **Release Notes 明寫「現在打開 staging 看到的仍是修正前的版本」**。D-017 的修正已合併 main，但 staging 部署被 T-0039 的 IAM 缺口擋住，若只寫「已修正」會讓讀者以為線上已經好了。這是面向使用者的文件，寧可寫得難看也不能讓人誤判
  - **不寫任何憑證**：Release Notes 只給 staging 網址，帳密一律寫「請向專案負責人索取」；同時把 Gate 2 條件 C-2「發布前輪換帳密」列進升級注意事項
  - **合併回 main 由本卡執行**：任務卡 acceptance 第 4 條要求「**main 上**建 annotated tag」，且 `## 驗收方式` 的四道指令（`grep` 迴圈、`npm run test:unit`、`git tag -l`、`sed` 讀 Release Notes）是 Leader 在**根目錄**執行——根目錄永遠停在 main，因此這四道指令要成立，內容必須先在 main 上。故依 dev-tl「執行合併」的職責，先合併再打 tag。本卡的 reviewer 是 leader，審核在合併後的 main 上進行
  - **`npm ci` 產生的 `node_modules/`、`dist/` 未進版控**（`.gitignore` 既有規則），`git status` 乾淨，只有 6 個改動檔 ＋ 1 個新交接檔
- 驗證方式（接手者如何確認我做的是對的：指令 + 我實際得到的輸出）：

  **① 任務卡 acceptance 第 1 條前半：11 條 TC-ID 各自的出現次數**（任務卡「驗收方式」原文照跑）

  ```bash
  for tc in TC-001 TC-002 TC-003 TC-012 TC-019 TC-020 TC-030 TC-038 TC-039 TC-048 TC-059; do printf "%s " "$tc"; grep -rc "$tc" tests/unit | awk -F: "{s+=\$2} END {print s}"; done
  ```
  ```text
  TC-001 3
  TC-002 5
  TC-003 6
  TC-012 4
  TC-019 2
  TC-020 5
  TC-030 4
  TC-038 3
  TC-039 6
  TC-048 8
  TC-059 5
  ```
  11 條**全部 ≥ 2**（次數含檔頭對應表與測試名稱）。**零次的一條都沒有。**

  **② 只看 `test()` 名稱的命中（排除註解，證明不是只寫在表格裡）**

  ```bash
  grep -rn "^test(\"\[TC-" tests/unit | sed "s/, async.*//;s/, () =>.*//"
  ```
  ```text
  tests/unit/todo-repository.test.ts:111:test("[TC-012][TC-048] buildListCommand：$1 帶 status，無論哪個 status 都只有一個參數"
  tests/unit/todo-repository.test.ts:133:test("[TC-020][TC-039] buildUpdateCommand：$1=id、$2=title、$3=isCompleted，缺的欄位為 null（COALESCE 保留原值）"
  tests/unit/todo-repository.test.ts:144:test("[TC-030] buildDeleteCommand：$1 帶 id"
  tests/unit/todo-repository.test.ts:155:test("[TC-059] listTodos：把 rows 轉成 camelCase，並把 status 原樣傳給 $1"
  tests/unit/todo-repository.test.ts:180:test("[TC-003][TC-059] getTodoById：有列時回傳 camelCase 物件"
  tests/unit/todo-repository.test.ts:193:test("[TC-003] createTodo：RETURNING 有列時回傳 camelCase 物件"
  tests/unit/todo-repository.test.ts:216:test("[TC-039] setCompleted：委派給 updateTodo，只帶 isCompleted（title 為 null）"
  tests/unit/todo-service.test.ts:112:test("[TC-048] listTodos：把 status 原樣轉給 repository，回傳 camelCase 列"
  tests/unit/todo-service.test.ts:133:test("[TC-002] createTodo：一般標題會先 trim 再交給 repository"
  tests/unit/todo-service.test.ts:141:test("[TC-001] createTodo：空字串回 E_VALIDATION/400（BR-001）"
  tests/unit/todo-service.test.ts:146:test("[TC-001] createTodo：全空白字串回 E_VALIDATION/400（schema 的 minLength 擋不掉，業務層再驗一次）"
  tests/unit/todo-service.test.ts:159:test("[TC-002] createTodo：剛好 200 字元（trim 後）視為合法"
  tests/unit/todo-service.test.ts:169:test("[TC-002] createTodo：201 字元（無前後空白）回 E_VALIDATION/400 而非 500"
  tests/unit/todo-service.test.ts:174:test("[TC-002] createTodo：trim 後才是 201 字元（原始字串因前後空白更長）回 400 而非 500（R-07 的核心情境）"
  tests/unit/todo-service.test.ts:206:test("[TC-020] updateTodo：只給 title 時 trim 後再驗證長度，isCompleted 傳 undefined 給 repository"
  tests/unit/todo-service.test.ts:214:test("[TC-019] updateTodo：title 為全空白回 E_VALIDATION/400"
  tests/unit/todo-service.test.ts:219:test("[TC-038][TC-039] updateTodo：只給 isCompleted=true（設定目標狀態，非 toggle）"
  tests/unit/todo-service.test.ts:228:test("[TC-038] updateTodo：對已完成的待辦重送 isCompleted=true 仍是同一個結果（冪等，O-002）"
  tests/unit/todo-service.test.ts:252:test("[TC-030] deleteTodo：刪除成功不丟例外（由路由層回 204）"
  tests/unit/todo-store.test.mjs:302:test("[TC-048] setFilter()：非法值不呼叫 API，回 ok:false"
  tests/unit/todo-store.test.mjs:313:test("[TC-048] FILTERS 常數固定三值，預設為 all（BR-010）"
  ```
  共 **21 條測試名稱**，11 個 TC-ID 全數出現在名稱層級（不只是註解）。

  **③ acceptance 第 1 條後半：`npm run test:unit` 全綠且案例數不減少**

  改動前（`npm ci` ＋ `npm run build` 後的基準）：
  ```text
  ℹ tests 78
  ℹ pass 78
  ℹ fail 0
  ℹ duration_ms 143.3448
  ```
  改動後：
  ```bash
  npm run build && npm run test:unit
  ```
  ```text
  ℹ tests 78
  ℹ suites 0
  ℹ pass 78
  ℹ fail 0
  ℹ cancelled 0
  ℹ skipped 0
  ℹ todo 0
  ℹ duration_ms 1886.6013
  ```
  **78 → 78，一條不減、零失敗。**（78 = T-0038 由 76 加上 2 條 D-017 時序測試後的現況）

  **④ 證明「只加 TC-ID，不改斷言」**

  ```bash
  git diff -U0 tests/unit/ | grep "^[+-]" | grep -v "^[+-][+-]" | grep -v "^[+-] \*" | grep -v "^[+-]test(" | grep -v "^[+-]/\*\*" | grep -v "^[+-] \*/"
  ```
  ```text
  （空輸出）
  ```
  濾掉註解行（` *`、`/**`、` */`）與 `test(` 名稱行之後，三個測試檔的 diff **沒有任何一行剩下**，即可執行程式碼與所有 `assert` 一字未動。

  **⑤ lint（含對 `tests/**` 跑 tsc）**

  ```bash
  npm run lint
  ```
  ```text
  > todo-app@0.1.0 lint
  > eslint . && tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.test.json && tsc --noEmit -p tsconfig.e2e.json

  LINT_EXIT=0
  ```

  **⑥ acceptance 第 5 條：diff 範圍只含 outputs ＋ 任務卡 ＋ 交接檔**

  ```bash
  git status --short && git diff --stat
  ```
  ```text
   M CHANGELOG.md
   M docs/specs/14_ReleaseNotes.md
   M tasks/T-0042-unit-tcid-release-notes.md
   M tests/unit/todo-repository.test.ts
   M tests/unit/todo-service.test.ts
   M tests/unit/todo-store.test.mjs
  ?? worklog/handoff/20260920-1940-T0042-r1-dev-tl.md

   CHANGELOG.md                            |  12 +++
   docs/specs/14_ReleaseNotes.md           | 135 ++++++++++++++++++++++----------
   tasks/T-0042-unit-tcid-release-notes.md |   4 +-
   tests/unit/todo-repository.test.ts      |  30 +++++--
   tests/unit/todo-service.test.ts         |  46 ++++++++---
   tests/unit/todo-store.test.mjs          |  18 ++++-
   6 files changed, 182 insertions(+), 63 deletions(-)
  ```
  `src/`、`public/`、`.github/`、`infra/`、其他 `docs/specs/*` **零改動**。

  **⑦ CHANGELOG 結構（acceptance 第 3 條）**

  ```bash
  grep -n "^## \[" CHANGELOG.md; grep -c "^- T-0038 \|^- T-0039 " CHANGELOG.md
  ```
  ```text
  7:## [Unreleased]
  11:## [0.1.0] - 2026-09-20 — E-001 待辦事項 Web 應用 P0 交付
  3
  ```
  （`3` ＝ T-0038 一條 ＋ T-0039 兩條「T-0039 維運…」「T-0039 r2 維運…」，皆已歸入 `[0.1.0]` 節之下；日期 `2026-09-20` 取自實查 `date` 輸出 `2026-09-20T19:50:41+08:00`）

  **⑧ 合併與 tag（acceptance 第 4 條）** —— 實際輸出見本檔最末「合併與 tag 實際輸出」一節（該節於合併完成後以 main 上的第二個 commit 補入，因為這些輸出在分支 commit 的當下還不存在）。
- 下一步建議：
  - **qa-lead**：R-6 可否正式關閉由你認定。本卡達成的是「11 條 TC-ID 在 unit 層逐條可 grep 到、且對應到具體測試」；**但其中 6 條為部分覆蓋**（TC-001、TC-003、TC-019、TC-039、TC-048、TC-059），未覆蓋的子句已逐條寫在三個測試檔頭的對應表。若你認為 R-6 要求的是「每條 TC 的每個子斷言都在 unit 層被驗」，那本卡只完成了追溯、沒完成補測，請開一張 P1 補測卡；若要求的是「可逐條追溯」，則 R-6 可關
  - **P1 補測清單**（若要補齊，建議一張卡做完）：① TC-001／TC-019 加 `assert.equal(db.calls.length, 0)`；② TC-002 補一條「前後空白 ＋ 200 字元」的直接案例；③ TC-039 在 service 層補一條回傳物件的四欄位 `deepEqual`；④ TC-048 若要在後端 unit 層驗「非法 status 被拒」，須先決定驗證要不要下放到 `todo-service`（目前刻意只放在 schema 層，**這是設計決定，改它要走規格變更請求，不該由測試卡自行改**）
  - **Leader**：tag `v0.1.0` 已在 main 上建好但**未推送**，請決定是否 `git push origin v0.1.0`
  - **使用者**：Gate 2 條件 C-2「發布前輪換 staging Basic Auth 帳密」尚未執行；KL-5 的 `roles/secretmanager.viewer` 授權是解開「D-017 修正上 staging → D-017 正式關閉 → 第 1 項升到 104／105」這條鏈的唯一鑰匙
- 需要 Leader 裁決的事：
  - **tag `v0.1.0` 是否推送至 origin**。本卡依任務卡指示只在本機建立 annotated tag，未推送。推送後 tag 即對外可見且依慣例不再變動；不推送則 CI／GitHub Releases 看不到這個版本。建議推送（v0.1.0 的內容已定版，Gate 2 判定也已由使用者做出，沒有會讓版本內容再變的未決事項；`main` 上的三個 commit 皆已在 origin）
  - **R-6 的關閉標準**（見「下一步建議」第一條）：是「可逐條追溯」還是「每個子斷言都在 unit 層被驗」。兩者差一張 P1 補測卡。建議採前者並把補測列 P1——後者會逼著把 `status` 驗證從 schema 層下放到 service 層，那是為了測試去改設計
