# T-0028 r1 — qa-at — 工作紀錄與交接

<!-- 檔名：worklog/handoff/20260919-1642-T0028-r1-qa-at.md（時間 = 開工時間，取實查系統時間） -->

## A. 開工紀錄

- 開工時間：2026-09-19T16:42:38+08:00
- 角色／模型：qa-at / sonnet（Claude Sonnet 5）
- 任務卡：tasks/T-0028-at-staging.md（round 1）
- 讀取的輸入（實際讀過的檔案與段落，非任務卡照抄）：
  - docs/reports/20260919-1314-AT-E001-r2.md — 本機基準：unit 76、integration 56、integration:qa 38/4skip、e2e 136、perf 4+1 全PASS；staging 相依 5 條 TC（079/080/084/089/090）本機一律「阻擋（環境未就緒）」
  - docs/specs/20_測試案例.md — TC-078/079/080/081/084/089/090/105 全文（步驟、判準、自動化欄）；TC-078/081/105 為 uat 類（自動化：否），不在 qa-at 範圍
  - docs/specs/06_部署架構與CICD.md §3.3、§5、§6 — monitor-health 設計、回滾程序 5.1/5.4、告警與冷啟判讀規則
  - .github/workflows/monitor-health.yml — 採樣邏輯：cron */5、連續3次、CSV 欄位、artifact 保留 90 天
  - README.md「測試分層」— test:integration 與 test:integration:qa 差異
  - tasks/T-0027-staging-deploy.md 與其分支 task/T-0027-staging-deploy 上的 docs/reports/20260919-1551-部署紀錄-E001.md、docs/specs/06 §5.4（未合併進 main，但為 T-0027 review 版本的真實部署與回滾演練紀錄，含 revision 名稱與耗時）
  - tasks/_todo.md 引言區 — 目前進行中/審核中狀態；dev-tl 正合併 T-0027 到 main
- 上一輪交接摘要：round=1，無上一輪
- 本輪計畫（3–7 條）：
  1. worktree 內 npm ci、npm run build
  2. 對 staging（BASE_URL 由提示詞給）暖身 /health，跑 test:integration:qa、test:e2e（Chromium+Edge）、test:perf
  3. 8 條原阻擋 TC 逐條給結果：TC-079/080/084/089/090（qa-at 範圍，用真實 staging 測或引用 T-0027 部署/回滾紀錄）；TC-078/081/105（uat 類，非 qa-at 範圍，標明交 T-0029）
  4. 利用 dev-tl 正在合併 T-0027 觸發的重新部署事件，機會性量測 TC-080（部署後資料仍存在）與 TC-090（部署不可用時間）
  5. 查 GitHub Actions monitor-health 的採樣 CSV，計算 NFR-003 現況
  6. 憑證掃描後 commit
- 預期產出：docs/reports/20260919-HHMM-AT-E001-r3.md（時間依實際完工調整）；本交接檔 B 段

## B. 交接

- 完工時間／狀態：2026-09-19T17:26:49+08:00 / review
- 完成事項：
  - 對 staging（`https://todo-app-dpevsdhdva-de.a.run.app`）跑完 `test:integration:qa`（42 條：pass 36、fail 1、skip 5）、`test:e2e`（Chromium×2＋Edge×2，136 條：pass 133、fail 3，皆間歇性）、`test:perf`（NFR-001 四段全 PASS、NFR-007 全 PASS），並清空測試自建資料。
  - 8 條原阻擋 TC 逐條給結果：TC-079 失敗（實測 302 非 301，規格覆核建議）；TC-080 以間接證據佐證（直接量測因本卡自身操作疏失未能取得乾淨對照，已誠實記錄）；TC-084 通過（引用 T-0027 真實回滾演練）；TC-089 採樣中；TC-090 通過（真實部署事件期間 0 秒不可用）；TC-078／081／105 標「不適用，非 qa-at 範圍」。
  - NFR-003 現況查明：monitor-health.yml 目前 1 個 run（2 個 attempt，第 1 次失敗、第 2 次 repo owner 手動 re-run 後成功），採樣中，未達 24 小時判讀門檻，已附 qa-lead 之後的判讀指令。
  - `tests/e2e/playwright.config.ts` 小修：加 `--disable-http2 --disable-quic` 解決本執行環境對 staging（Cloud Run／Google Frontend）用 HTTP/2／QUIC 導覽逾時的問題（環境差異，非斷言變更）。
  - 報告：`docs/reports/20260919-1719-AT-E001-r3.md`。
- 產出物（路徑）：
  - docs/reports/20260919-1719-AT-E001-r3.md
  - tests/e2e/playwright.config.ts（小修，見上）
  - tasks/T-0028-at-staging.md（status → review）
  - worklog/handoff/20260919-1642-T0028-r1-qa-at.md（本檔）
- 未完成／已知問題：
  - **TC-080 未取得直接的「同批資料跨部署前後比對」乾淨證據**：本卡嘗試建立基準資料後，因忘記部署事件恰與 `test:integration:qa`（其中 TC-014 會清空全部 staging 資料）重疊執行，基準資料在部署前就被清空。已改用間接證據（TC-090 顯示部署期間 `/health` 零中斷、T-0027 回滾演練顯示 revision 切換不動資料層、NFR-006 架構設計）佐證，但非任務卡驗收方式字面要求的「直接同批比對」。建議下一次真實部署事件時，qa-lead 安排單獨時段（不與其他清空型測試同時）補做一次。
  - **e2e TC-067／TC-009 間歇性失敗**（約 2~4 成機率，`error-message` 有時不顯示）：多次重跑（含完全乾淨、無並行污染的單獨執行）皆會偶發重現，非本卡環境修正造成。已列為缺陷候選（報告「觀察與缺陷候選」第 2 項），尚未經 qa-lead 編號，需另開缺陷單並指派 dev-fe 調查前端錯誤顯示的時序邏輯。
  - **TC-079 規格與平台行為不一致**（302 vs 規格認定之 301）：需 qa-lead／plan-sd 裁決是否修訂 `20_測試案例.md`／`06_部署架構與CICD.md` 的判準文字，而非視為應用缺陷（Cloud Run 平台行為，團隊無程式碼可改）。
  - **NFR-003 尚未達 24 小時判讀門檻**：monitor-health.yml 排程觸發延遲明顯（main 推送後超過 1 小時仍只有 1 個 workflow_dispatch run），cron 是否已正常啟動待觀察；qa-lead 需在累積足夠 run 後依報告內「下次判讀指令」重算。
  - 我本輪操作疏失（已在報告「重要操作紀事」誠實記錄，不重複於此，僅列出指路）：第 3 點（忘記停止背景重複 e2e 程序，已修正並改採乾淨結果）、第 4 點（TC-014 清空資料連帶影響 TC-080 基準）。
- 我做的假設與決策（含理由）：
  - **TC-078／081／105 判定「不適用，非 qa-at 範圍」**：三者在 `20_測試案例.md` 標「自動化：否」且分類為 uat，任務卡角色檔（`.claude/agents/qa-at.md`）職責僅涵蓋 integration／e2e／perf 自動化，UAT 類由 T-0029（qa-uat）執行；仍在報告與交接檔逐條列出以滿足任務卡「8 條逐條有結果」的字面要求，但不擴大範圍代做。
  - **TC-084 不重複演練，直接引用 T-0027 真實紀錄**：任務卡明文「以 T-0027 的回滾演練紀錄...佐證」，且 T-0027 該次演練（切到 `todo-app-00002-sn7` 再切回 `todo-app-00003-lt2`）是對同一 staging 服務、同一批 revision 的真實操作，時效性與相關性皆足夠，重複演練除了消耗一次不必要的流量切換外無額外資訊價值。
  - **e2e config 小修（`--disable-http2 --disable-quic`）判定為「環境差異」而非「測試碼邏輯錯誤」**：用最小重現腳本（獨立 node 腳本，非 playwright test）逐一排除 QUIC／HTTP2／reload 三種情境，確認純屬本沙盒環境對 Cloud Run／Google Frontend 的瀏覽器網路層問題（`curl`／`node fetch` 對同網址皆正常），符合任務卡「僅在需要以 BASE_URL 參數化或修正環境差異時小修」的授權範圍。
  - **perf 測試殘留資料（714 筆）判定為「本卡自己的測試資料」並主動清空**：核對後確認 500 筆為 TC-094 直接建立、214 筆為 NFR-001 高併發 `createAndCleanup()` 殘留，皆非其他角色資料；雖然 `tc094-nfr007-capacity.perf.mjs` 原始設計是「保留供後續 e2e 量測沿用」，但本卡未接續做該前端量測且處於多角色共用 staging 情境，優先遵守提示詞「用自己建立的資料並清理」的明文指示，全數清空。
- 驗證方式（接手者如何確認我做的是對的：指令 + 我實際得到的輸出）：
  ```bash
  # 1. staging integration:qa 真實輸出（節錄）
  BASE_URL=https://todo-app-dpevsdhdva-de.a.run.app STAGING_BASE_URL=https://todo-app-dpevsdhdva-de.a.run.app \
    STAGING_BASIC_AUTH_USER=*** STAGING_BASIC_AUTH_PASSWORD=*** npm run test:integration:qa
  ```
  ```text
  ℹ tests 42
  ℹ pass 36
  ℹ fail 1
  ℹ skipped 5
  ```
  ```bash
  # 2. e2e 最終乾淨執行（完整見報告，本處為彙總行）
  npm run test:e2e -- --project=chromium-1280x800 --project=chromium-390x844 --project=msedge-1280x800 --project=msedge-390x844
  ```
  ```text
  3 failed
    [msedge-1280x800] TC-067
    [msedge-390x844] TC-009
    [msedge-390x844] TC-067
  133 passed (3.2m)
  ```
  ```bash
  # 3. TC-079 實測（手動 curl，未含憑證）
  curl -sSI "http://todo-app-dpevsdhdva-de.a.run.app/health"
  ```
  ```text
  HTTP/1.1 302 Found
  location: https://todo-app-dpevsdhdva-de.a.run.app/health
  server: Google Frontend
  ```
  ```bash
  # 4. TC-090 部署期間零中斷（節錄，完整 228 筆見報告）
  awk -F, 'NR>1 && $2!="" {c++; if($2!=200) f++; if($3+0>max) max=$3} END {print "samples="c, "non200="f+0, "max_time="max}' /tmp/tc090-poll.csv
  ```
  ```text
  samples=228 non200=0 max_time=4.194953
  ```
  ```bash
  # 5. 收尾清空驗證
  curl -sS -u "$STAGING_BASIC_AUTH_USER:$STAGING_BASIC_AUTH_PASSWORD" "https://todo-app-dpevsdhdva-de.a.run.app/api/v1/todos"
  ```
  ```text
  []
  ```
  ```bash
  # 6. 憑證掃描（commit 前，掃本卡新增／修改檔案）
  cd worktree; grep -inE "pj002user|pj002pass" tasks/T-0028-at-staging.md tests/e2e/playwright.config.ts docs/reports/20260919-1719-AT-E001-r3.md worklog/handoff/20260919-1642-T0028-r1-qa-at.md
  ```
  ```text
  （無輸出，exit code 1，即無命中）
  ```
- 下一步建議：
  - qa-lead：抽查 2 條原阻擋 TC 的證據（建議挑 TC-084、TC-090，皆有完整原始輸出可核對）；重算 NFR-003（累積更多 monitor-health run 後）；對 TC-079、e2e TC-067/TC-009 兩項觀察裁決是否開缺陷單／修訂規格。
  - 下一次真實部署事件時，安排與清空型測試（`test:integration:qa`、`apiClearAll` 相關 e2e）分開的獨立時段，補做 TC-080 直接同批資料比對。
  - 若 24 小時後 monitor-health 累積足夠 run，依本報告「NFR-003 現況」章節的判讀指令重算。
- 需要 Leader 裁決的事：
  - TC-079 判準文字（301 vs 3xx 皆可）是否修訂 `20_測試案例.md`／`06_部署架構與CICD.md`，或維持現狀並記為已知落差。
  - e2e TC-067／TC-009 間歇性失敗是否指派 dev-fe 立案調查（本卡評估為 S3，非阻擋，但影響 BR-029／BR-030 判準的穩定性）。
