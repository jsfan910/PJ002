# T-0007 r1 — plan-sd — 工作紀錄與交接

<!-- 檔名：worklog/handoff/20260919-0652-T0007-r1-plan-sd.md（時間 = 開工時間，取實查系統時間） -->
<!-- 規則：開工立即寫 A 段；完工或中止時寫 B 段。同一輪同一角色一個檔。 -->

## A. 開工紀錄

- 開工時間：2026-09-19T06:52:51+08:00
- 角色／模型：plan-sd / opus
- 任務卡：tasks/T-0007-sd-fix.md（round 1）
- 讀取的輸入（實際讀過的檔案與段落，非任務卡照抄）：
  - docs/reports/20260919-0637-覆蓋度審核-E001.md#4 —— D-1、D-2 的問題、證據與建議修法原文；D-1 要求補標三個 operation，D-2 要求把 SD-02 的「第一行」改成與 `05_資料庫設計.md` 實際 SQL 位置一致的措辭；D-3／D-4 不在本卡範圍。
  - docs/specs/04_API規格.yaml（含 frontmatter 註解、`info.version`、第 206~330 行三個 operation 的 description）—— 確認 `getTodoById`／`updateTodo`／`deleteTodo` 的 description 皆為 `|` 區塊，末段為條列，補句可直接接在條列末尾；確認 `grep -c UC-014` 目前為 0。
  - docs/specs/03_系統設計書_SD.md#9（第 404 行 SD-02 列）與 frontmatter —— SD-02 現行「建議做成 `002_*.sql` migration 的第一行 `TRUNCATE todos;`」，`version: 0.1`。
  - docs/templates/worklog_handoff.md、.claude/skills/handoff/SKILL.md —— 交接檔格式與收尾步驟。
- 上一輪交接摘要（若 round > 1）：不適用（round 1）。
- 本輪計畫（3–7 條）：
  1. 在 `04_API規格.yaml` 的 `getTodoById`／`updateTodo`／`deleteTodo` 三個 operation description 末尾各補一句「（P1）UC-014：以使用者身分隔離存取，跨使用者回 404」。
  2. `04_API規格.yaml` `info.version` 由 `0.1.0` 進版為 `0.1.1`（純文字修正，patch）。
  3. 修 `03_系統設計書_SD.md` 第 9 章 SD-02 列：「第一行」改為「同一個 migration 內、`ALTER ... SET NOT NULL` 之前」。
  4. `03_系統設計書_SD.md` frontmatter `version` 由 `0.1` 進版為 `0.2`、`updated` 更新。
  5. 執行 `npx @redocly/cli lint docs/specs/04_API規格.yaml` 確認 0 errors；執行 `grep -c UC-014`；`git diff --stat` 確認只動兩檔。
  6. 依 /handoff 收尾：填 B 段、任務卡 `status: review` + `updated`、在 main commit（只 add 兩份 spec、任務卡、本交接檔）。
- 預期產出：docs/specs/04_API規格.yaml、docs/specs/03_系統設計書_SD.md 兩檔的純文字修正 + frontmatter 進版；本交接檔。

## B. 交接

- 完工時間／狀態：2026-09-19T06:54:16+08:00 / review
- 完成事項：
  - D-1：`docs/specs/04_API規格.yaml` 的 `getTodoById`（第 220 行）、`updateTodo`（第 264 行）、`deleteTodo`（第 321 行）三個 operation 的 `description` 末尾各補一條「- （P1）UC-014：以使用者身分隔離存取，跨使用者回 404。」，`grep -c UC-014` 由 0 變 3。
  - D-1 附帶：`info.version` `0.1.0` → `0.1.1`（純文字修正，semver patch）。
  - D-2：`docs/specs/03_系統設計書_SD.md` 第 9 章第 404 行 SD-02 列，「migration 的第一行 `TRUNCATE todos;`」改為「migration 內、**`ALTER ... SET NOT NULL` 之前**的 `TRUNCATE todos;`」。
  - D-2 附帶：SD frontmatter `version` `0.1` → `0.2`、`updated` → `2026-09-19T06:52:51+08:00`。
  - 任務卡 `tasks/T-0007-sd-fix.md`：`status: review`、`updated` 更新。
- 產出物（路徑）：
  - docs/specs/04_API規格.yaml
  - docs/specs/03_系統設計書_SD.md
  - worklog/handoff/20260919-0652-T0007-r1-plan-sd.md（本檔）
- 未完成／已知問題：
  - 覆蓋度審核報告第 4 章的 **D-3**（SD 第 3 章模組表「對應 UC」欄比 `traceability.md` 窄 6 處）**不在本卡 acceptance 內，未處理**。D-4 報告已判定「不需修改」。若 Leader 要收斂 D-3，須另開卡。
  - `npx @redocly/cli lint` 有 **1 個 warning**（`operation-4xx-response`：`GET /health` 無 4XX 回應）。此為本卡修改前既有狀態、非本次引入；acceptance 只要求 0 errors，已滿足。`GET /health` 依 Leader 裁決 O-005 為唯一未保護路徑且僅回 200，刻意不設 4xx。
- 我做的假設與決策（含理由）：
  - 補句放在各 operation `description` 條列的**最末一條**，而非首行。理由：檔頭註解（第 3 行）要求「description 首行標註對應的 UC-ID」指的是該端點的**主 UC**（`getTodoById` 為 UC-009／UC-007、`updateTodo` 為 UC-003／UC-005、`deleteTodo` 為 UC-004）；UC-014 是 P1 的橫切關注點，放首行會蓋掉主 UC 的可讀性。任務卡只要求「各補一句」，未指定位置。
  - 版號進位規則：SD 沿用專案 x.y 慣例 `0.1` → `0.2`（對照 `01_需求規格書_SRS.md` 已為 `0.2`）；OpenAPI `info.version` 為 semver，純文字修正取 patch `0.1.0` → `0.1.1`。兩檔規則不同是因為 `info.version` 受 OpenAPI 規範約束，非筆誤。
  - `getTodoById` 原本已在條列中完整描述「跨使用者回 404」的語意，本次補句在語意上與其重複。仍照任務卡補上，理由是 acceptance 的驗收方式是 `grep -c UC-014` 得 3，目的是讓 UC-014 可被機械化追溯，而非補充語意。
  - SD-02 措辭保留後半句「與加 `owner_id NOT NULL` 同一個 migration」未刪。理由：本卡限定純文字小修、不擴大；刪除屬於改寫句構，超出「把『第一行』改為 …」的範圍。
- 驗證方式（接手者如何確認我做的是對的：指令 + 我實際得到的輸出）：
  ```bash
  npx @redocly/cli lint docs/specs/04_API規格.yaml
  ```
  ```text
  [1] docs\specs\04_API規格.yaml:97:7 at #/paths/~1health/get/responses

  Operation must have at least one `4XX` response.

  95 |   - health
  96 | security: []
  97 | responses:
  98 |   "200":
  99 |     description: 服務存活。

  Warning was generated by the operation-4xx-response rule.

  Reference: https://redocly.com/docs/cli/rules/oas/operation-4xx-response

  docs\specs\04_API規格.yaml: validated in 52ms

  Woohoo! Your API description is valid. 🎉
  You have 1 warning.
  ```
  （0 errors，符合 acceptance；1 warning 為既有狀態，說明見「未完成／已知問題」。）

  ```bash
  grep -c UC-014 docs/specs/04_API規格.yaml
  grep -n UC-014 docs/specs/04_API規格.yaml
  ```
  ```text
  3
  220:        - （P1）UC-014：以使用者身分隔離存取，跨使用者回 404。
  264:        - （P1）UC-014：以使用者身分隔離存取，跨使用者回 404。
  321:        - （P1）UC-014：以使用者身分隔離存取，跨使用者回 404。
  ```

  ```bash
  grep -n "SD-02" docs/specs/03_系統設計書_SD.md
  ```
  ```text
  404:| SD-02 | **P1 資料清空的執行者與時機**：BR-032／Q-011 裁決「導入 P1 時直接清空 staging 既有待辦資料」，但未指定由誰在哪一步執行 | 建議做成 `002_*.sql` migration 內、**`ALTER ... SET NOT NULL` 之前**的 `TRUNCATE todos;`，與加 `owner_id NOT NULL` 同一個 migration，使其不可能被遺漏。**屬 P1，Gate 1 不受影響** | dev-ops（P1） |
  ```

  ```bash
  git diff --stat
  ```
  ```text
   docs/specs/01_需求規格書_SRS.md | 29 +++++++++++++++--------------
   docs/specs/03_系統設計書_SD.md  |  6 +++---
   docs/specs/04_API規格.yaml      |  5 ++++-
   3 files changed, 22 insertions(+), 18 deletions(-)
  ```
  （`01_需求規格書_SRS.md` 為**另一個 agent 平行進行中的工作**，不屬本卡，未 `git add`、未觸碰。本卡實際變更為 SD 3 行〔version、updated、SD-02〕與 API 4 行〔version 1 行、UC-014 補句 3 行〕。）
- 下一步建議：
  - Leader 覆核本卡兩項後，若同意即可與 T-0005 一併收進 Gate 1 規格凍結。
  - D-3 若要處理，建議採報告的 (a) 方案（補齊 SD 第 3 章模組表「對應 UC」欄），另開一張 plan-sd 小修卡；本卡刻意不含。
- 需要 Leader 裁決的事：
  - 無。（D-3 是否開卡屬「下一步建議」，不阻擋本卡驗收。）
