# T-0045 r1 — dev-tl（初審・第一階段／離線部分） — 工作紀錄與交接

## A. 開工紀錄

- 開工時間：2026-09-20T20:59:37+08:00
- 角色／模型：dev-tl / Opus 5 (1M context)
- 任務卡：tasks/T-0045-traffic-pinned-fix.md（round 1，status 仍 in_progress）
- 本次範圍：**初審第一階段（離線部分）**。Leader 指示：acceptance 第 1、2、3、5 條 + 06 §6.10／事故報告草稿；第 4 條（真實部署重測 TC-090）由 Leader 推送 main 後 dev-ops 於同一 worktree 補量測結果，不在本次判定範圍。
- 讀取的輸入：
  - tasks/T-0045-traffic-pinned-fix.md（acceptance 6 條、驗收方式指令、outputs 清單）
  - .claude/skills/review-round/SKILL.md（審核流程；本次依 Leader 指示只做離線部分，不清理 worktree／分支、卡維持 in_progress）
  - worklog/handoff/20260920-2042-T0045-r1-dev-ops.md（A 段；B 段尚未填）
  - 分支 task/T-0045-traffic-pinned-fix（最新 commit 67a2802）之 diff 與各檔內容
- 本輪計畫：
  1. 實跑 `gcloud run services describe` 唯讀指令，確認流量已復原到 latestReady。
  2. grep 三個腳本／workflow 的 to-latest／流量檢查實作，逐行讀邏輯。
  3. 跑 actionlint（或等效 YAML lint）與 `bash -n` 語法檢查。
  4. 核對 acceptance 1、2、3、5 與 06 §6.10、事故報告內容是否與實查事實一致。
  5. 通過則在根目錄 `git merge --no-ff` 合併進 main 並更新 CHANGELOG；不 push、不清理 worktree／分支、卡維持 in_progress。
- 預期產出：本交接檔、（通過時）main 上的合併 commit 與 CHANGELOG.md 更新。

## B. 交接

- 收尾時間：2026-09-20T21:05+08:00
- 判定：**初審第一階段不通過（不合併）**。阻擋級 2 項（下方 R-1、R-2），均在本卡 outputs 內、修正成本低。
- 卡狀態：維持 `in_progress`（dev-ops 仍在本卡上、真實部署重測未做），**本次不改狀態、不改 round、不清理 worktree／分支**，依 Leader 指示回報要改哪裡。

### 驗證方式（實際指令與真實輸出）

#### 1. 服務端流量狀態（acceptance 第 1 條，唯讀）

```bash
GCLOUD="C:/Users/excal/AppData/Local/Google/Cloud SDK/google-cloud-sdk/bin/gcloud.cmd"
MSYS_NO_PATHCONV=1 "$GCLOUD" run services describe todo-app --region asia-east1 --project pj002-509106 \
  --format="yaml(spec.traffic,status.traffic,status.latestReadyRevisionName,status.latestCreatedRevisionName)"
```
```yaml
spec:
  traffic:
  - latestRevision: true
    percent: 100
status:
  latestCreatedRevisionName: todo-app-00018-xq4
  latestReadyRevisionName: todo-app-00018-xq4
  traffic:
  - latestRevision: true
    percent: 100
    revisionName: todo-app-00018-xq4
```
→ 流量確已復原，100% 在 latestReady＝latestCreated＝`todo-app-00018-xq4`，與 acceptance 第 1 條預期一致。

#### 2. revision Ready 狀態逐一核對（用於檢驗 pipeline 判準是否成立）

```bash
MSYS_NO_PATHCONV=1 "$GCLOUD" run revisions list --service todo-app --region asia-east1 --project pj002-509106 \
  --format="table(metadata.name,status.conditions[0].status,metadata.creationTimestamp,status.observedGeneration)" --limit 20
```
```text
NAME                STATUS  CREATION_TIMESTAMP           OBSERVED_GENERATION
todo-app-00018-xq4  True    2026-09-20T12:19:12.835503Z  1
todo-app-00017-qg4  True    2026-09-20T12:10:28.130626Z  1
todo-app-00016-tsg  True    2026-09-19T10:50:07.789332Z  1
...（00001～00015 略，STATUS 皆 True）
todo-app-00001-tfq  True    2026-09-19T08:10:06.761522Z  1
```
→ 18 個 revision 的 Ready 條件皆為 `True`。與 dev-ops 於 06 §6.10.3 貼出的「流量釘死時 `latestReadyRevisionName` 仍停在 `todo-app-00003-lt2`」並存，**證實 `latestReadyRevisionName` 在 Cloud Run 的實際語義並非 Knative 文件所述的「最新 Ready 的 revision」，而是一個未文件化的平台行為**。此點對 R-1 的判定為關鍵：dev-ops 的觀察屬實（非捏造），但把它當成判準仍不可靠，理由見 R-1。

#### 3. grep 三個腳本的 to-latest／流量檢查

```bash
grep -n "to-latest\|latestCreatedRevisionName\|latestReadyRevisionName\|traffic" \
  .github/workflows/deploy-staging.yml scripts/deploy-staging.sh scripts/rollback-staging.sh
```
重點節錄（完整輸出見審核過程）：
- `deploy-staging.yml:227-233` `verify_traffic()` 判定式為 `[ "${latest_ready}" = "${expected_revision}" ]`，**其中 `expected_revision` 來自 `status.latestCreatedRevisionName`**；`status.traffic` 僅在 `:242/:244` 的 warning／error 訊息裡以 `get_traffic_table` 印出，**不參與任何判定**。
- `scripts/deploy-staging.sh:127-139` 同一模式：`if [ "${LATEST_READY}" != "${NEW_REVISION}" ]`，`status.traffic` 只在失敗分支印出。
- `scripts/rollback-staging.sh:3,18-22,34-37,74-82,96,116` `--to-latest` 模式、檔頭警語、用法說明、執行後提醒皆齊備。

#### 4. 語法檢查

```bash
for f in scripts/deploy-staging.sh scripts/rollback-staging.sh scripts/measure-deploy-downtime.sh; do bash -n "$f" && echo "OK: $f"; done
```
```text
OK: scripts/deploy-staging.sh
OK: scripts/rollback-staging.sh
OK: scripts/measure-deploy-downtime.sh
```

```bash
npx --yes actionlint .github/workflows/deploy-staging.yml
```
```text
npm error could not determine executable to run
```
→ actionlint 本機不可用（npx 找不到可執行檔）。改以兩項等效驗證替代：

```bash
npx --yes yaml@2 valid < .github/workflows/deploy-staging.yml && echo "YAML valid OK (exit $?)"
```
```text
YAML valid OK (exit 0)
```
```bash
sed -n "170,320p" .github/workflows/deploy-staging.yml | sed "s/^          //" > /tmp/runblk.sh; bash -n /tmp/runblk.sh
```
```text
run block bash -n OK
```
→ YAML 結構與 run 區塊 shell 語法皆正確。dev-ops 於 06 §6.10.4 另貼有 `actionlint.exe -color ...` 零 finding 的輸出（其本機有安裝執行檔），與本次替代驗證結論不衝突。

#### 5. 替代判準可行性實測（用於確認 R-1 的修正建議可執行，非空話）

```bash
MSYS_NO_PATHCONV=1 "$GCLOUD" run services describe todo-app --region asia-east1 --project pj002-509106 \
  --format="table[no-heading](status.traffic.revisionName,status.traffic.percent)"
```
```text
['todo-app-00018-xq4']  [100]
```
```bash
MSYS_NO_PATHCONV=1 "$GCLOUD" run services describe todo-app --region asia-east1 --project pj002-509106 \
  --format="csv[no-heading](status.traffic.revisionName,status.traffic.percent)"
```
```text
todo-app-00018-xq4,100
```
→ 現有 `get_traffic_table()` 用的 `table[no-heading]` 格式輸出帶方括號與引號（`['todo-app-00018-xq4']  [100]`），**不可直接解析**；`csv[no-heading]` 輸出乾淨可解析。R-1 的修正建議即以 `csv[no-heading]` 為準。

#### 6. outputs 範圍與 secret 掃描（acceptance 第 6 條）

```bash
git diff --stat main...task/T-0045-traffic-pinned-fix
```
```text
 .github/workflows/deploy-staging.yml              |  75 +++++++++++
 README.md                                         |   9 +-
 docs/reports/20260920-2042-流量釘死事故-E001.md   | 130 +++++++++++++++++++
 docs/specs/06_部署架構與CICD.md                   | 144 +++++++++++++++++++++-
 scripts/deploy-staging.sh                         |  31 ++++-
 scripts/rollback-staging.sh                       |  94 ++++++++++----
 tasks/T-0045-traffic-pinned-fix.md                |   4 +-
 worklog/handoff/20260920-2042-T0045-r1-dev-ops.md |  28 +++++
 8 files changed, 486 insertions(+), 29 deletions(-)
```
→ 全部落在任務卡 outputs ＋任務卡本身＋自己的交接檔，無越界。

```bash
git diff main...HEAD | grep -n -iE "BASIC_AUTH_PASS(WORD)?=|DATABASE_URL=postgres|npg_|postgresql://" | grep -v "secrets\.\|SECRET_VERSION\|set-secrets\|\${"
```
```text
（無輸出）
```
→ 未見明文 secret 值。第 6 條通過。

#### 7. 06 章節結構核對（發現 R-2）

```bash
grep -n "^## " docs/specs/06_部署架構與CICD.md | tail -6     # 分支
```
```text
## 5. 回滾程序（必須實際演練過一次，記錄日期與結果）
## 6. 監控與告警
## 變更紀錄
```
```bash
git show main:docs/specs/06_部署架構與CICD.md | grep -n "^## " | tail -6   # main
```
```text
237:## 5. 回滾程序（必須實際演練過一次，記錄日期與結果）
315:## 6. 監控與告警
713:## 7. 三處必須同步的參數表（T-0025，CR S-9，實作紀錄）
737:## 變更紀錄
```
→ **分支上 `## 7.` 整章標題消失**。以 `sed -n "838,848p"` 核對分支內文，確認參數表內容仍在，但直接接在 §6.10 的「事故報告全文另見…」之後，已被併吞為 §6.10 的附屬段落。

### 逐條判定（acceptance）

| # | 條目 | 判定 | 依據 |
|---|---|---|---|
| 1 | 先量測再復原、貼前後 describe、貼最長連續非 200 秒數 | **通過** | 驗證 1 實查 100% 在 `00018-xq4`；06 §6.10.3 貼有復原前後 yaml 與 152/152 樣本皆 200、最長連續非 200 = 0 |
| 2 | pipeline 讀 `latestCreatedRevisionName` 與 `status.traffic`，未拿到 100% 流量即自動 `--to-latest` 並重驗；verify 確認 `latestReadyRevisionName == 接流量的 revision` | **不通過（R-1，阻擋級）** | 驗證 3、5 |
| 3 | rollback 加 `--to-latest` 與檔頭警語；README 同步 | **通過**（惟 README 內對判準的敘述隨 R-1 一併修正） | 驗證 3；README diff 已含三種用法與 ⚠️ 警語 |
| 4 | 真實部署重測 TC-090 | **不在本次範圍** | 依 Leader 指示，由 Leader 推送 main 觸發後 dev-ops 補；06 §6.10.3 與事故報告已留佔位段落 |
| 5 | 06 §6.10 事故時間軸／影響範圍／復原與兩次量測／pipeline 修法；事故報告同內容並含給 qa-lead 的清單 | **通過內容、不通過結構（R-2，阻擋級）** | 內容詳實且與實查一致（時間軸、影響範圍、重驗清單 6 列齊備，TC-090／TC-080／D-017／TC-009 皆列「需重驗」，P0＋NFR-003 列「仍然有效」）；但插入動作刪掉了 `## 7.` 章標題，見驗證 7 |
| 6 | 不讀取／不輸出 secret；只 add outputs | **通過** | 驗證 6 |

### 阻擋級退回項（具體要改哪裡）

**R-1：流量驗證用的是代理指標，不是 acceptance 指定的 `status.traffic`；且「100%」從未被檢查。**

- 位置：`.github/workflows/deploy-staging.yml` 的 `verify_traffic()`（約 227–233 行）與 `ensure_traffic_on_latest()`（235–248 行）；`scripts/deploy-staging.sh` 127–144 行；連帶 06 §6.10.3「關鍵發現」段、§6.10.4「修法」段、§變更紀錄 T-0045 列、README「本機等效指令」`scripts/deploy-staging.sh` 那一條。
- 事實：判定式是 `latestReadyRevisionName == latestCreatedRevisionName`。`status.traffic` 從頭到尾不參與判定，只在失敗訊息裡印出。acceptance 第 2 條要求的是「讀 `status.traffic`，若新 revision 未拿到 **100%** 流量則…」以及「確認 `latestReadyRevisionName == **接流量的 revision**」——「接流量的 revision」只能從 `status.traffic` 取得，實作把它替換成 `latestCreatedRevisionName`，「接流量」這層語義整個消失。
- 為什麼不能放行（三點，任一點都足夠）：
  1. **百分比不檢查＝有實際漏判**。`rollback-staging.sh` 允許把流量釘到任意 revision，未來若做 canary／分流（例如 `--to-revisions LATEST=50,<old>=50`），`latestReadyRevisionName` 仍會等於新 revision，判準通過，但新 revision 只有 50% 流量，acceptance 要的 100% 沒達到，D-018「驗證失效」以另一種形式留著。
  2. **判準依賴未文件化的平台行為**。本次實查已證明：00004～00018 全部 `Ready=True`，而釘死期間 `latestReadyRevisionName` 卻停在 `00003-lt2`——這與 Knative 對該欄位的公開語義（「最新一個 Ready 條件為 True 的 revision」）相反。dev-ops 的觀察屬實，但那是 Cloud Run 目前實作的副作用，不是規格。角色定義的安全邊界判準同型原則：**第三方平台行為不是規格，換一個平台版本或部署形態就沒了**。本卡的全部價值就是「讓 pipeline 真的偵測得到流量沒切」，判準底座不能是觀察。
  3. **修正成本極低，沒有不做的理由**。資料本來就在手上，`csv[no-heading]` 格式已於驗證 5 實測可解析。
- 具體改法（兩處同步，維持既有 `ensure_traffic_on_latest` 的重試結構不變）：
  1. 把 `get_traffic_table()` 的格式由 `table[no-heading]` 改為 `csv[no-heading](status.traffic.revisionName,status.traffic.percent)`（`table` 格式輸出 `['todo-app-00018-xq4']  [100]`，帶方括號與引號，無法解析）。
  2. `verify_traffic()` 改為直接從 `status.traffic` 求該 revision 的流量百分比並要求等於 100，例如：
     ```bash
     verify_traffic() {
       local expected_revision="$1" table pct latest_ready
       table="$(get_traffic_table)"                    # csv: revisionName,percent
       pct="$(echo "${table}" | awk -F, -v r="${expected_revision}" '$1==r {s+=$2} END {print s+0}')"
       latest_ready="$(get_latest_ready_revision)"
       echo "status.traffic=$(echo "${table}" | tr '\n' ';')；${expected_revision} 取得 ${pct}%；latestReadyRevisionName=${latest_ready}"
       [ "${pct}" = "100" ] && [ "${latest_ready}" = "${expected_revision}" ]
     }
     ```
     即：**主判準為 `status.traffic` 中該 revision 拿到 100%**，`latestReadyRevisionName` 比對降為併行的輔助條件（正好對應 acceptance 「同時確認」的字面要求）。
  3. STEP_SUMMARY 的「流量」欄改為填入實際讀到的 `pct`，不要寫死 `100%`（現行成功分支是硬寫 `| 100% |`，等於把未經證實的值寫進報告）。
  4. 06 §6.10.3「關鍵發現」段與 §6.10.4「修法」段、§變更紀錄 T-0045 列、README 該條敘述同步改寫：把 `latestReadyRevisionName` 的角色從「**可靠依據**」降為「輔助佐證（Cloud Run 實測觀察，非文件化保證）」，主判準明寫 `status.traffic` 的百分比。06 是 frozen 規格文件，後人會照著引用，措辭必須精確。

**R-2：06 第 7 章標題被刪除，整章被併吞進 §6.10。**

- 位置：`docs/specs/06_部署架構與CICD.md`，§6.10 尾端「事故報告全文另見 `docs/reports/20260920-2042-流量釘死事故-E001.md`。」之後。
- 事實：main 有 `## 7. 三處必須同步的參數表（T-0025，CR S-9，實作紀錄）`（第 713 行），分支已不存在該標題，其上方的 `---` 分隔線也一併消失；參數表內容仍在，但現在掛在 §6.10 底下。
- 為什麼是阻擋級：06 為凍結規格，§7 被 CR S-9 引用；章節消失會讓後續引用失效，也會讓「三處參數同步表」在目錄與交叉引用中找不到。這不是排版偏好，是文件事實被破壞。
- 具體改法：在該行之後補回
  ```markdown

  ---

  ## 7. 三處必須同步的參數表（T-0025，CR S-9，實作紀錄）
  ```
  使參數表回到第 7 章之下，§6.10 到此結束。

### 對 dev-ops 交接檔「假設與決策」的表態

dev-ops 交接檔 B 段尚未填寫（卡仍 `in_progress`，真實部署重測未做），本輪無「假設與決策」可逐條表態。惟其寫入 06 §6.10.3／§6.10.4 與兩支腳本註解中的核心技術主張——「`status.latestReadyRevisionName` 會跟隨目前實際接流量的 revision，故比對它與 `latestCreatedRevisionName` 即為判斷流量是否切過去的**可靠依據**」——**不接受**：觀察本身經本輪實查佐證為真（驗證 2），但它是平台未文件化行為，且無法覆蓋「流量百分比非 100」的情形，不足以作為規格級判準。理由與改法見 R-1。

### 未解決問題／風險

- R-1 若不修而直接上線，pipeline 對「分流／canary」型的流量異常仍然是盲的；本次事故的同型風險只擋掉了一種形態。
- acceptance 第 4 條（真實部署重測 TC-090）尚未執行，06 §6.10.3 與事故報告皆為佔位段落。建議 R-1、R-2 修完後再由 Leader 推送 main 觸發，一次部署同時驗證「修正後的流量檢查邏輯在 CI 真的會跑」與 TC-090 門檻——若先推送再改判準，那一次 run 證明不了修正後的邏輯。

### 需要 Leader 裁決的事

- **是否接受本輪不合併**：Leader 原指示「通過則合併」。本輪判定不通過，故未執行 `git merge`，main 未變動，CHANGELOG 未更新。R-1、R-2 皆在本卡 outputs 內，dev-ops 於同一 worktree 續修即可，不需新卡。
- **真實部署觸發的時點**：建議改為「R-1／R-2 修完後再推送」，理由見上。若 Leader 因時程考量要先推送，本輪判定不變，但那次 run 的 STEP_SUMMARY 只能證明舊判準跑得動，不能作為 acceptance 第 2 條的通過證據。

### 下一步建議

1. dev-ops 於 worktree `C:\Users\excal\Projects\Claude\soft-dev-team-260919-wt\T-0045` 內依 R-1、R-2 修正並追加 commit（禁止 amend／reset）。
2. 修完後 Leader 推送 main 觸發真實部署，dev-ops 背景量測並補 06 §6.10.3 與事故報告的佔位段落（acceptance 第 4 條）。
3. dev-ops 填 B 段、卡轉 `review`，dev-tl 做初審第二階段（含真實 run 的 STEP_SUMMARY 與 TC-090 量測），通過後合併並更新 CHANGELOG。
4. 後續（非本卡）：`scripts/rollback-staging.sh` 的釘定模式可考慮在執行後把「必須還原」寫進一個顯式待辦（例如輸出一行可複製的還原指令並要求操作者回貼），但這屬於流程強化，需另開卡，本卡不擴大範圍。

---

## B2. 複審（初審第一階段・第二次）—— dev-ops 追加 commit `78dca59` 後

- 複審時間：2026-09-20T21:12:33 ～ 21:16+08:00
- 對象：`78dca59 T-0045: 修正 r1 初審阻擋項 R-1／R-2（流量判準改讀 status.traffic、補回 06 §7 標題）`
- 增量範圍（`git diff --stat 67a2802..78dca59`）：`deploy-staging.yml` 45 行、`README.md` 2 行、事故報告 4 行、`06` 10 行、`scripts/deploy-staging.sh` 39 行，共 5 檔 64+ / 36-，全在 outputs 內。
- **判定：通過。已合併進 main。**

### R-1 複驗（流量判準）

`deploy-staging.yml` 的 `verify_traffic()`（229–238 行）與 `scripts/deploy-staging.sh`（130–142 行）兩處同步改為：

```bash
verify_traffic() {
  local expected_revision="$1"
  local table pct latest_ready
  table="$(get_traffic_table)"
  pct="$(echo "${table}" | awk -F, -v r="${expected_revision}" '$1==r {s+=$2} END {print s+0}')"
  latest_ready="$(get_latest_ready_revision)"
  echo "status.traffic=$(echo "${table}" | tr '\n' ';')；${expected_revision} 取得 ${pct}%；latestReadyRevisionName=${latest_ready}（輔助佐證）"
  [ "${pct}" = "100" ] && [ "${latest_ready}" = "${expected_revision}" ]
}
```

逐項核對我 r1 開的四點改法：

| 改法 | 落實 | 證據 |
|---|---|---|
| ① `get_traffic_table()` 改 `csv[no-heading]` | 是 | `deploy-staging.yml:210`、`scripts/deploy-staging.sh:132-134` |
| ② 主判準改為 `status.traffic` 百分比 = 100，`latestReadyRevisionName` 降為併行輔助條件 | 是 | 上列程式碼；本機腳本為 `if [ "${TRAFFIC_PCT}" != "100" ] \|\| [ "${LATEST_READY}" != "${NEW_REVISION}" ]` |
| ③ STEP_SUMMARY 改填實際讀到的值，不寫死 `100%` | 是 | 新增 `get_traffic_percent()`（268–271 行）；成功分支由 `\| 100% \|` 改為 `\| $(get_traffic_percent "${NEW_REVISION}")% \|`，失敗分支同步，表頭改為「流量（status.traffic 實際值）」 |
| ④ 06／README 措辭把 `latestReadyRevisionName` 由「可靠依據」降為「輔助佐證（平台未文件化行為）」 | 是 | 06 §6.10.3「關鍵發現」、§6.10.4「修法」、§3.2 條目、§變更紀錄 T-0045 列、README「本機等效指令」、事故報告 D-018 段，共 6 處同步改寫 |

**判準邏輯三情境實測**（`awk -F, -v r="todo-app-00018-xq4" "\$1==r {s+=\$2} END {print s+0}"`）：

```text
--- 情境A 正常：單列 100% ---      輸入 todo-app-00018-xq4,100
100
--- 情境B 事故重演：流量全在舊 revision ---   輸入 todo-app-00003-lt2,100
0
--- 情境C 分流 50/50（r1 判準會漏判者）---    輸入 todo-app-00018-xq4,50 / todo-app-00003-lt2,50
50
```
→ A 放行、B 擋下、C **擋下**。情境 C 正是 r1 判準會誤放行的那一類，修正確實達到目的。

**對真實服務端對端實跑判準**：

```bash
REV="$(... --format="value(status.latestCreatedRevisionName)")"
PCT="$(... --format="csv[no-heading](status.traffic.revisionName,status.traffic.percent)" | awk -F, -v r="$REV" "\$1==r {s+=\$2} END {print s+0}")"
READY="$(... --format="value(status.latestReadyRevisionName)")"
```
```text
NEW_REVISION=todo-app-00018-xq4 / TRAFFIC_PCT=100 / LATEST_READY=todo-app-00018-xq4
判準結果：PASS（流量已在新 revision 100%）
```

### R-2 複驗（06 章節結構）

```bash
grep -n "^## " docs/specs/06_部署架構與CICD.md | tail -6     # 分支
```
```text
100:## 3. Pipeline 階段（GitHub Actions）
185:## 4. 環境變數清單（只列名稱與用途，值不入版控）
237:## 5. 回滾程序（必須實際演練過一次，記錄日期與結果）
315:## 6. 監控與告警
845:## 7. 三處必須同步的參數表（T-0025，CR S-9，實作紀錄）
869:## 變更紀錄
```
→ 與 main 的章節結構一致（`## 3/4/5/6/7/變更紀錄`），`---` 分隔線亦已補回，§6.10 在 §7 之前正常收尾。

### 語法與範圍複驗

```bash
for f in scripts/deploy-staging.sh scripts/rollback-staging.sh scripts/measure-deploy-downtime.sh; do bash -n "$f"; done
```
```text
OK: scripts/deploy-staging.sh
OK: scripts/rollback-staging.sh
OK: scripts/measure-deploy-downtime.sh
```
```bash
npx --yes yaml@2 valid < .github/workflows/deploy-staging.yml
sed -n "170,330p" .github/workflows/deploy-staging.yml | sed "s/^          //" > /tmp/runblk2.sh; bash -n /tmp/runblk2.sh
```
```text
YAML valid OK (exit 0)
run block bash -n OK
```
（`actionlint` 本機 npx 仍不可用，同 r1 以 YAML valid ＋ run 區塊 `bash -n` 替代；dev-ops 本機有 actionlint 執行檔，其零 finding 輸出記於 06 §6.10.4。）

增量 secret 掃描無輸出；`git diff --stat main...task/T-0045-traffic-pinned-fix` 仍為 8 檔，全在 outputs＋任務卡＋dev-ops 自己的交接檔內。

### 合併

```bash
git merge --no-ff task/T-0045-traffic-pinned-fix -F <訊息檔>
```
```text
Auto-merging README.md
Auto-merging docs/specs/06_部署架構與CICD.md
CONFLICT (content): Merge conflict in docs/specs/06_部署架構與CICD.md
```

**衝突內容僅 frontmatter `updated` 一行**（T-0044 的 `20:26:13` vs 本卡的 `20:55:08`），如任務卡「背景與限制」預告，由 dev-tl 處理。解法：取 dev-ops 最後實際修改 06 的時間 `2026-09-20T21:12:07+08:00`（`git log -1 --format=%ad 78dca59`），註解改為涵蓋 T-0044＋T-0045 兩筆實作紀錄。此舉一併解掉下方「非阻擋級瑕疵」所記的時戳未更新問題。

合併後核對：

```bash
grep -n "^<<<<<<<\|^=======$\|^>>>>>>>" docs/specs/06_部署架構與CICD.md   # → 無殘留
grep -n "^## \|^### 6.1.1\|^### 6.9\|^### 6.10" docs/specs/06_部署架構與CICD.md | tail -10
```
```text
315:## 6. 監控與告警
598:### 6.9 實作紀錄：secret 釘版本與 verify 自動重試（T-0039，2026-09-20 18:43）
713:### 6.10 事故紀錄：staging 流量自 09-19 回滾演練後釘死在 `todo-app-00003-lt2`（T-0045，2026-09-20）
856:## 7. 三處必須同步的參數表（T-0025，CR S-9，實作紀錄）
880:## 變更紀錄
```
→ T-0044 的 §6.1.1（334 行）、§6.3 告警條（367 行）、§6.9.1 內容經核對**完整保留**，未被本次合併覆蓋。合併後再跑一次 `bash -n`＋`yaml valid`，皆 OK。

```text
[main 5b28b13] T-0045: 合併 — staging 流量釘死事故復原、pipeline 加驗流量 100%、rollback 加 --to-latest 還原模式
```

CHANGELOG.md `## [Unreleased]` 置頂新增 T-0045 一條（含事故經過、影響 15 個 revision／28 小時 11 分、復原量測、pipeline 修法、r1 兩項退回與 r2 修正的三情境實測證據、給 qa-lead 的重驗清單摘要、合併衝突處理、以及「第 4 條未做、卡維持 in_progress」的明確標註）。

**依 Leader 指示未執行**：`git push`（main 現領先 `origin/main` 17 個 commit）、`git worktree remove`、`git branch -d`。`git worktree list` 確認 `...-wt/T-0045` 仍在 `78dca59 [task/T-0045-traffic-pinned-fix]`，dev-ops 可直接續用同一 worktree 補量測。任務卡 `status` 維持 `in_progress`、`round` 未動、審核紀錄表待本卡真正收尾（第二階段）時再填。

### 非阻擋級瑕疵（已於合併時一併處理，記錄備查）

- `78dca59` 改了 `docs/specs/06_部署架構與CICD.md` 卻未更新 frontmatter `updated`（仍為 `67a2802` 時的 `20:55:08`）。本可退回，但考量 dev-ops 稍後補真實部署量測時必然再改 06、且合併衝突正落在這一行，已於解衝突時直接更正為 `21:12:07` 並加註。**提醒 dev-ops：補完 §6.10.3 佔位段落時記得再更新一次 `updated`。**

### 複審後仍未解決／風險

- acceptance 第 4 條（真實部署重測 TC-090）仍未執行；06 §6.10.3 與事故報告的該段仍是佔位文字。**這是本卡唯一剩下的未完成項。**
- 修正後的流量檢查邏輯**尚未在 CI 實際跑過一次**。離線驗證（三情境 awk ＋ 對真實服務端對端實跑判準 ＋ 語法檢查）只能證明邏輯與語法正確，依 06 §6.9.1 的教訓，**本機 gcloud 身分（專案擁有者）≠ CI 的 WIF 服務帳號 `github-deployer@`**；本次新增的呼叫仍是 `run services describe`／`update-traffic`，前者 CI 既有步驟已在用，後者為新增呼叫，理論上 `roles/run.admin` 已涵蓋，但**未經 CI 實測**。真實部署那一次 run 需一併確認此點。

### 需要 Leader 裁決的事（複審後更新）

- **無新增裁決項**。r1 提出的兩項均已解決：①不合併 → 現已修正並合併；②真實部署觸發時點 → 已依建議在修正後才合併，Leader 可隨時推送 main 觸發，那次 run 即可同時驗證「修正後的判準在 CI 真的會跑」與 TC-090 門檻。

### 下一步（複審後更新）

1. Leader 推送 main（含本次合併 `5b28b13`）觸發 `deploy-staging` 真實 run。
2. dev-ops 在同一 worktree 背景執行 `scripts/measure-deploy-downtime.sh`（≥300 秒、1 秒一次）全程輪詢 `/health`，補上 06 §6.10.3 與事故報告的佔位段落：run 連結與結論、STEP_SUMMARY 的「部署流量檢查」revision 與百分比、最長連續非 200 秒數（TC-090 門檻 < 60 秒），並更新 06 frontmatter `updated`。
3. dev-ops 填 r1 交接檔 B 段、任務卡轉 `status: review`。
4. dev-tl 做初審第二階段（只需核第 4 條與新增內容），通過後填審核紀錄表、任務卡轉 `done`，並在 CHANGELOG 該條追記真實部署量測結果；Leader 登錄 `tasks/_done.md` 並決定清理 worktree 與分支的時點。

---

## B3. 初審第二階段（真實部署重測）—— dev-ops 追加 commit `bb9e679` 後

- 複審時間：2026-09-20T21:28:25 ～ 21:34+08:00
- 對象：`bb9e679 T-0045: 補真實部署重測結果（TC-090 通過），卡轉 review`
- 增量範圍（`git diff --stat 78dca59..HEAD`）：事故報告 35 行、`06` 46 行、任務卡 4 行、dev-ops 交接檔 83 行，共 4 檔，全在 outputs 內。
- **判定：通過。acceptance 六條全數達成，卡判 `done`，已合併（`f015ea1`）並推送。**

### acceptance 第 4 條複驗（真實部署重測 TC-090）

審核原則：不採信交接檔的數字，只採信自己能唯讀取得的證據。GitHub job log 需登入（見下），故改以「多條獨立可查證據互相咬合」的方式驗證。

**① run 結論（匿名 GitHub API，repo 為公開）**

```bash
curl -sS "https://api.github.com/repos/jsfan910/PJ002/actions/runs/35513187463"
```
```json
{ "id": 35513187463, "name": "Deploy Staging",
  "head_branch": "main",
  "head_sha": "95bc4cafde3f2581e20cb06845e1933df141e874",
  "run_number": 23, "event": "workflow_run",
  "status": "completed", "conclusion": "success" }
```

**② 步驟逐一核對**（`/actions/runs/35513187463/jobs`）：13 個步驟 `conclusion` 全為 `success`。關鍵步驟時窗：

```text
"name": "docker build & push"                          13:21:13Z → 13:21:50Z  success
"name": "gcloud run deploy ＋ verify（含一次自動重試）"   13:21:50Z → 13:22:19Z  success
"name": "回填提示（首次部署後的網址）"                    13:22:19Z → 13:22:19Z  success
```
job 全程 `13:20:40Z → 13:22:22Z`。

**③ 此 run 跑的是不是 R-1 修正後的程式碼**（本輪最關鍵的一項，直接回答 r1 交接檔列的風險）

```bash
git show 95bc4ca:.github/workflows/deploy-staging.yml | grep -n "pct=\|\[ \"\${pct}\" = \"100\" \]\|csv\[no-heading\]"
```
```text
210:  gcloud run services describe ... --format="csv[no-heading](status.traffic.revisionName,status.traffic.percent)"
235:  pct="$(echo "${table}" | awk -F, -v r="${expected_revision}" '$1==r {s+=$2} END {print s+0}')"
238:  [ "${pct}" = "100" ] && [ "${latest_ready}" = "${expected_revision}" ]
```
```bash
git merge-base --is-ancestor 95bc4ca main   # → 是（95bc4ca 為 main 祖先）
```
→ run 的 `head_sha` 正是我上一輪合併後推送的 `95bc4ca`，其 workflow 判定式確為修正後版本。**兩項 r1 遺留風險同時解除**：(a) 修正後的流量檢查邏輯已在 CI 實跑；(b) CI 的 WIF 服務帳號 `github-deployer@` 有權執行新增的 `run services describe --format=csv` 呼叫（該步驟 `success`，未出現 §6.9.1 那類權限紅燈）。

**④ 服務端現況（本機唯讀 gcloud）**

```bash
MSYS_NO_PATHCONV=1 "$GCLOUD" run services describe todo-app --region asia-east1 --project pj002-509106 \
  --format="yaml(spec.traffic,status.traffic,status.latestReadyRevisionName,status.latestCreatedRevisionName)"
```
```yaml
spec:
  traffic:
  - latestRevision: true
    percent: 100
status:
  latestCreatedRevisionName: todo-app-00019-k5q
  latestReadyRevisionName: todo-app-00019-k5q
  traffic:
  - latestRevision: true
    percent: 100
    revisionName: todo-app-00019-k5q
```
```bash
... --format="csv[no-heading](status.traffic.revisionName,status.traffic.percent)"
```
```text
todo-app-00019-k5q,100
```
```bash
curl -sS -o /dev/null -w "HTTP %{http_code}\n" https://todo-app-dpevsdhdva-de.a.run.app/health
```
```text
HTTP 200
```
→ 以 pipeline 自己的主判準格式複跑，結果與 `verify_traffic()` 的通過條件完全對應。

**⑤ revision 時序咬合**

```bash
MSYS_NO_PATHCONV=1 "$GCLOUD" run revisions list --service todo-app --region asia-east1 \
  --project pj002-509106 --format="table(metadata.name,status.conditions[0].status,metadata.creationTimestamp)" --limit 3
```
```text
NAME                STATUS  CREATION_TIMESTAMP
todo-app-00019-k5q  True    2026-09-20T13:21:55.672504Z
todo-app-00018-xq4  True    2026-09-20T12:19:12.835503Z
todo-app-00017-qg4  True    2026-09-20T12:10:28.130626Z
```
→ `00019-k5q` 建立於 `13:21:55Z`，**落在「`gcloud run deploy ＋ verify`」步驟的 `13:21:50Z～13:22:19Z` 時窗內**，證實該 revision 確由這一次 run 產生，而非他處。

**⑥ 量測涵蓋性**：dev-ops 量測窗 `13:17:39Z～13:22:38Z`（226 樣本）**完整涵蓋** run 全程 `13:20:01Z～13:22:23Z`，226/226 皆 200、最長連續非 200 = 0 秒 < 60 秒門檻。量測窗與 run 窗的包含關係可由 ① 的 `run_started_at`／`updated_at` 與 §6.10.3 所載量測起訖時間直接核對，兩者皆為獨立可查值。

**⑦ job log 不可讀（誠實記載）**

```bash
curl -sSL "https://api.github.com/repos/jsfan910/PJ002/actions/jobs/106084598845/logs" -w "HTTP %{http_code}\n"
```
```text
HTTP 403
```
→ 原始 log 需登入。依安全鐵則「agent 不讀取、不索取任何憑證值」，**未索取 token**，改以上述 ①～⑥ 的證據鏈佐證。dev-ops 交接檔亦主動記載此限制，未假稱讀過 log，這點如實。

### acceptance 逐條最終判定

| # | 條目 | 判定 |
|---|---|---|
| 1 | 先量測再復原、貼前後 describe 與最長連續非 200 秒數 | 通過（r1 已驗：152/152、0 秒；流量復原至 `latestRevision: true`） |
| 2 | pipeline 讀 `status.traffic`，未達 100% 自動 `--to-latest` 並重驗；verify 同時確認 `latestReadyRevisionName` | 通過（r1 退回後於 `78dca59` 修正，本階段③證實已在 CI 實跑） |
| 3 | rollback 加 `--to-latest` 與檔頭警語；README 同步 | 通過（r1 已驗） |
| 4 | 真實部署重測、貼 run 全綠與 revision／流量／最長連續非 200 秒數 | **通過**（本階段①～⑥） |
| 5 | 06 §6.10 事故紀錄與事故報告，含給 qa-lead 的清單 | 通過（內容完整，兩份正式清單已更新為「已重驗，通過」；三處交叉引用措辭滯後見下） |
| 6 | 不讀取／不輸出 secret；只 add outputs | 通過（增量 secret 掃描無輸出；`git diff --stat main...HEAD` 僅 4 檔且全在 outputs 內） |

### 對 dev-ops 交接檔「假設與決策」的表態

1. 「判準改為主判準讀 `status.traffic`、`latestReadyRevisionName` 降為輔助佐證」——**接受**（即採納 r1 的 R-1 改法，理由一致）。
2. 「『以空 commit 觸發真實部署』改為由 Leader 於根目錄推送 main」——**接受**。根目錄當時由 dev-tl 佔用（正在合併 T-0044／T-0045），dev-ops 依工作鐵則不得在根目錄操作；實際效果（一次真實部署＋全程量測）與 acceptance 第 4 條要求等同，且由本階段③證實觸發的 sha 與 workflow 版本皆正確。
3. 「全程未讀取／未輸出 secret；`rollback --to-latest` 實跑時未匯出 Basic Auth 環境變數，故帶憑證那一項驗證依設計跳過」——**接受**，與安全鐵則一致；`git diff` secret 掃描亦無輸出佐證。

### 非阻擋級殘留（不退回，已記錄並指派後續）

06 有三處交叉引用在 §6.10.3 補完後未同步更新，仍寫「待補」：

| 行 | 現行文字 | 問題 |
|---|---|---|
| 717（§6.10.2） | 「TC-090 需以本次事故復原後的真實部署重新量測（見 6.10.3 **待補**）」 | 6.10.3 已補完 |
| 791（§6.10.3） | 「TC-090 重測留待下方『**待補：真實部署重測**』段落」 | 該標題已改為「真實部署重測（TC-090，2026-09-20 21:18～21:22）」，被引用的標題名已不存在 |
| 843（§6.10.4） | 「仍待 6.10.3『**待補**』段落的真實部署補證」 | 同上，補證已完成 |

**判為非阻擋級的理由**：這三處是敘述段落內的失效交叉引用，不是失實的技術主張；而**真正供下游判斷的兩份正式重驗清單（06 §6.10.4 第 883 行、事故報告第 144 行）皆已正確標示「已重驗，通過」**，qa-lead 依清單作業不會被誤導。為三句措辭退回一輪、成本與風險不成比例。**指派**：plan-sd 的 P1 規格同步卡本就要動 06 §5.1，請一併順修這三處（已寫入下方下一步與回報）。

### 合併與清理

```bash
git merge --no-ff task/T-0045-traffic-pinned-fix -F <訊息檔>
```
衝突同上一階段，仍只在 06 frontmatter `updated` 一行（我上階段解衝突寫入的 `21:12:07` vs 分支的 `21:24:51`）。取分支較新值 `2026-09-20T21:24:51+08:00`，註解補上「含真實部署重測 TC-090」。合併後核對：無衝突標記殘留；章節結構 `## 4/5/6 → ### 6.10（713）→ ## 7（894）→ ## 變更紀錄（918）` 正確；`bash -n`（兩支腳本）與 `yaml valid`（workflow）皆 OK。

```text
[main f015ea1] T-0045: 合併剩餘 commit — 真實部署重測 TC-090 通過（226/226 樣本 0 秒不可用）
```

任務卡：`status: review → done`，`updated: 2026-09-20T21:31:49+08:00`，審核紀錄表補兩列（r1 第一階段「退回後修正」、r1 第二階段「done」）。CHANGELOG 的 T-0045 條目改寫：移除「尚未完成／佔位文字／維持 in_progress」段，改為真實部署重測的實測數據、CI 實跑證據與非阻擋級殘留說明。

**本次依 Leader 指示推送 main 並清理 worktree 與分支**（與前兩階段不同）。

### 未解決問題／風險（移交 qa-lead 與後續卡）

- **`update-traffic --to-latest` 自動補救分支未經 CI 實跑**：本次部署走的是「流量已正常」路徑，`ensure_traffic_on_latest()` 於第 1 次 `verify_traffic` 即回真，補救分支未被觸發。該分支目前僅有離線三情境實測（r1 B2 段）與程式碼審查佐證。要實測需刻意把流量釘死再部署一次，**本卡不做**（會再次讓 staging 服務舊版本，風險大於收益）；建議列為日後回滾演練的固定收尾項目——演練釘死流量後，直接觀察下一次部署是否被 pipeline 自動修正，等於免費取得該分支的實跑證據。
- TC-080（`/api/v1/todos` 資料筆數直接比對）需有憑證者執行，本卡與本次審核皆依安全鐵則未讀取憑證。
- D-017、TC-009 favicon 需在流量已復原的 `00019-k5q` 上重新驗證（含 T-0038／T-0043 的修正）。

### 需要 Leader 裁決的事

- **無**。

### 下一步

1. Leader 於 `tasks/_done.md` 追加 T-0045 一列（派工時間＝r1 交接檔 A 段 `2026-09-20T20:42:05+08:00`，完成時間＝`2026-09-20T21:31:49+08:00`，回合 1）。
2. qa-lead 依 06 §6.10.4／事故報告的重驗清單安排 TC-080（需憑證）、D-017、TC-009 favicon 重驗，並據此更新測試總結與退出準則第 12 項（TC-090 已可標為通過）。
3. plan-sd 的 P1 規格同步卡：把「回滾演練後必須 `--to-latest` 還原」補進 06 §5.1 正式流程文字，**並順修上述三處「待補」交叉引用**。
4. 日後回滾演練把「觀察下一次部署是否被 pipeline 自動修正」納入收尾步驟，以取得補救分支的 CI 實跑證據。

