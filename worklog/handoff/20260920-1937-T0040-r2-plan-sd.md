# T-0040 r2 — plan-sd — 工作紀錄與交接

<!-- 檔名：worklog/handoff/20260920-1937-T0040-r2-plan-sd.md（時間 = 開工時間，取實查系統時間） -->

## A. 開工紀錄

- 開工時間：2026-09-20T19:37:52+08:00
- 角色／模型：plan-sd / opus
- 任務卡：tasks/T-0040-spec-change-06-cron-301.md（round 2）
- 讀取的輸入（實際讀過的檔案與段落，非任務卡照抄）：
  - tasks/T-0040-spec-change-06-cron-301.md（`status: rework`、`round: 2`）—（outputs 第 4 項為 r2 追加：`docs/specs/03_系統設計書_SD.md`「僅 §7 NFR-002① 一列」；acceptance 第 1 條為 r2 新增條款；審核紀錄表 r1 列已由 Leader 填畢）
  - 任務卡「審核紀錄」r1 列 —（退回原因＝**範圍擴充，非失敗**：acceptance 1～5 全過、Leader 追加兩項到位、假設 7 條全接受含 ADR 第 6 點附註；裁決①備援缺口採 §6.1.1 選項 A＋告警政策，列 Gate 2 後維運卡由 dev-ops 實作，**本卡不處理**；裁決②SD:308 補改＝本輪工作；裁決③接受 ADR 第 6 點附註）
  - docs/specs/03_系統設計書_SD.md:306-309（§7 NFR 對策表）—（第 308 列 NFR-002 的對策欄①寫「將 HTTP **301** 導向 HTTPS（ADR-0005）」、驗證方式欄①寫「`curl -sSI http://<staging>` 觀察 **301**」，兩處即本輪要改的字面）
  - docs/specs/03_系統設計書_SD.md:1-10（frontmatter）—（現為 `version: 0.3`、`updated: 2026-09-19T07:30:22+08:00`）
  - docs/specs/03_系統設計書_SD.md:410-417（變更紀錄表）—（已有 T-0010 兩列；**發現既有異常**：第 416 行是一列 `SD-05` 的殘留列，誤落在本表中，屬既有缺陷，非本輪授權範圍，列入下一步建議）
  - docs/specs/06_部署架構與CICD.md:45（r1 已改的 §2 資源表列）、docs/specs/adr/ADR-0005-雲端平台-CloudRun.md:80-90（r1 已加的決定第 7 點附註）—（作為 r2 用字對齊的基準：判準為「回 3xx 且 `Location` 為對應的 `https://` 網址」，實測 302 Found）
- 上一輪交接摘要（round > 1）：
  - 退回原因：**非失敗**。Leader 審核 r1 五條 acceptance 全過、七條假設全接受，改 `rework` 純為**範圍擴充**——把 r1 已在交接檔「下一步建議 1」與「需 Leader 裁決 2」提出的 `03_系統設計書_SD.md:308` 狀態碼落差，追加進本卡 outputs 由本輪補做。
  - 本輪要修的點：**只有一處**——SD §7 NFR-002① 的對策欄與驗證方式欄的「301」字面，改為「3xx 且 `Location` 為對應的 `https://` 網址」，用字與 `06` §2、ADR-0005 決定第 7 點附註三方一致；另依任務卡指示更新 SD frontmatter `updated`、變更紀錄表加一列。
- 本輪計畫（3–7 條）：
  1. 改 SD 第 308 列對策欄①：「將 HTTP 301 導向 HTTPS」→「將 HTTP 請求以 3xx（實測 302）導向 HTTPS」，並載明判準不寫死單一狀態碼。
  2. 改同列驗證方式欄①：「`curl -sSI http://<staging>` 觀察 301」→ 觀察 **3xx 狀態碼且 `Location` 為對應的 `https://` 網址**，補上可直接照抄的指令。
  3. SD frontmatter `updated` 更新為本輪實查時間（`version` 維持 0.3：本次為單列字面同步，不是新的設計決策——**此為本輪假設，見 B 段**）。
  4. SD 文末變更紀錄表新增一列（日期、version、任務卡 T-0040、變更摘要）。
  5. **不動 SD 其他任何一列**（Leader 明示「只動這一處」）；不動 `20_測試案例.md`（仍由 qa-lead 處理）；不動 `06`／ADR-0005（r1 已定稿）。
  6. 實跑 `grep -n "301" docs/specs/03_系統設計書_SD.md` 等驗證指令，貼真實輸出進 B 段。
- 預期產出：docs/specs/03_系統設計書_SD.md（第 308 列、frontmatter `updated`、變更紀錄一列）、本交接檔。

## B. 交接

- 完工時間／狀態：2026-09-20T19:39:03+08:00 / review
- 完成事項：
  - **【r2 acceptance 第 1 條】SD §7 NFR-002① 狀態碼字面同步**（`docs/specs/03_系統設計書_SD.md` 第 308 列，一列內兩欄）：
    - **對策欄①**：「Cloud Run 為 `*.run.app` 自動配發憑證並將 HTTP **301** 導向 HTTPS（ADR-0005）」→「…並由 Google 前端將明文 HTTP 請求以 **3xx（實測 302 Found）** 導向 HTTPS（ADR-0005 決定第 7 點及其 T-0040 附註）…**判準為「回 3xx 且 `Location` 為對應的 `https://` 網址」，不寫死單一狀態碼**」，並附不寫死的理由與依據（D-016、Leader 2026-09-19T17:30:10 裁決①）。
    - **驗證方式欄①**：「`curl -sSI http://<staging>` 觀察 **301** ＋ 瀏覽器檢視憑證」→「`curl -sSI "http://${STAGING_BASE_URL#https://}/health"` **觀察首行為 3xx**（301 或 302 皆合規）**且 `location` 標頭為對應的 `https://` 網址** ＋ 瀏覽器檢視憑證」。指令改為可直接照抄執行的形式（原文的 `<staging>` 是佔位符，執行前還要人工替換；現與 `20_測試案例.md` TC-079 的取值寫法一致）。
  - **SD frontmatter**：`updated` 由 `2026-09-19T07:30:22+08:00` 改為 `2026-09-20T19:37:52+08:00`；`version` **維持 0.3**（理由見「假設與決策」第 1 點），並在該行註解補記本次同步範圍。
  - **SD 文末變更紀錄新增一列**（`2026-09-20 ／ 0.3（字面同步，未變更版本號）／ T-0040 r2`）：寫明改了哪兩欄、改前改後文字、依據（D-016＋兩次獨立實測 302＋Leader 裁決①）、三方用字一致的對照對象（`06` §2、ADR-0005 決定第 7 點附註）、不寫死單一狀態碼的理由、以及「未新增或變更任何設計決策故 version 不進位、`status` 維持 `frozen`」與「TC-079 由 qa-lead 處理不在本卡範圍」兩項邊界聲明。
  - **嚴格遵守「只動這一處」**：`git diff -U0` 顯示本輪對 SD 只有 4 個 hunk ——第 5 行（version 註解）、第 9 行（`updated`）、第 308 行（本體變更）、第 416 行（新增變更紀錄列），合計 4 插入 3 刪除。**SD 其餘 400 餘列一字未動**；`06_部署架構與CICD.md`、`adr/ADR-0005-雲端平台-CloudRun.md`（r1 已定稿）、`20_測試案例.md`、`01_需求規格書_SRS.md` 本輪皆未觸碰。
  - **Leader 裁決①（備援缺口採選項 A ＋ 告警政策，列 Gate 2 後維運卡由 dev-ops 實作）已知悉，本卡不處理**，`06` §6.1.1 的三選項敘述維持原樣（它記錄的是提案與代價，裁決結果應由該維運卡落地時回填，本卡不代填以免與尚未存在的實作產生第二份真相）。此點列入下一步建議。
- 產出物（路徑）：
  - `C:\Users\excal\Projects\Claude\soft-dev-team-260919\docs\specs\03_系統設計書_SD.md`（第 308 列、frontmatter 第 5／9 行、變更紀錄新增第 416 列）
  - `C:\Users\excal\Projects\Claude\soft-dev-team-260919\worklog\handoff\20260920-1937-T0040-r2-plan-sd.md`（本檔）
  - **r1 已定稿、r2 未再變更**：`docs/specs/06_部署架構與CICD.md`（v0.3）、`docs/specs/adr/ADR-0005-雲端平台-CloudRun.md`
- 未完成／已知問題：
  - **SD 變更紀錄表有一列既有的錯置資料**：`docs/specs/03_系統設計書_SD.md` 原第 416 行（本輪新增一列後為第 417 行）是一列 `| SD-05 | **`/api/v1` 版本前綴**：SRS 未要求版本化 | … |`，屬第 9 章「待確認設計決策」表的列，卻落在文末「變更紀錄」表內（欄位語意完全對不上：第 1 欄應為日期卻是 `SD-05`）。**這是既有缺陷、非本輪造成**（`git log` 可證本輪 diff 未觸及該行），且 Leader 明示「只動這一處」，故**未修正**。已列下一步建議。
  - `20_測試案例.md` TC-079（:156）與 NFR-002 對照表（:330）判準仍寫死 301，依任務卡 acceptance 末條由 qa-lead 於下一輪測試計畫同步卡處理。至此「301 → 3xx」的四份文件中，**三份（`06`、ADR-0005、SD）已同步，剩 `20_測試案例.md` 一份**；`01_需求規格書_SRS.md` 原文即為「3xx」無須改。
- 我做的假設與決策（含理由）：
  1. **SD `version` 維持 0.3、不進位為 0.4。** 理由：本次只把一個**已經錯的事實陳述**改成對的（狀態碼字面），**未新增、未變更任何設計決策**，NFR-002① 的設計對策（平台提供 HTTPS、團隊零程式碼）與驗證意圖（明文 HTTP 不得直接服務、必須被導到 HTTPS）一字未變。此處理與 SD 自身先例一致——文末變更紀錄第 2 列「T-0010 追加修正」同樣標註「0.3（不進版，Leader 裁決）」；也與 `06` 對 T-0031 實作紀錄的處理（「0.2（實作紀錄，未變更版本號）」）一致。**若 Leader 認為凍結文件的任何字面變更都應進版，改為 0.4 是單點變更（frontmatter 一行＋變更紀錄一格）。**
  2. **驗證指令順手改為可直接執行的形式**（`curl -sSI "http://${STAGING_BASE_URL#https://}/health"`），而非保留原本的 `http://<staging>` 佔位符。理由：本欄的用途就是給測試者照著跑，佔位符會讓每個人各自替換、也容易漏掉「要去掉 `https://` 前綴」這一步；且此寫法與 `20_測試案例.md` TC-079 現有步驟逐字相同，兩份文件對得起來。**這仍在「改驗證方式」的授權範圍內**，但屬超出「只改狀態碼字面」的最小改動，故明列於此供審核者判斷。
  3. **不回填 Leader 裁決①的結果到 `06` §6.1.1。** 理由：該節現在記錄的是「三個選項與各自代價」，是一份完整的提案；裁決結果要落地成「已建立第二個 uptime check `todo-app-health-backup` ＋ 告警政策」這類事實陳述，應由 Gate 2 後那張維運卡在**實作完成時**回填（如同 §6.8.2 回填 uptime check 的建立輸出）。現在就寫「採選項 A」而實際尚未建立，會讓規格再次跑在事實前面——這正是本卡要收斂的那一類問題。
  4. **SD 變更紀錄新列插在 T-0010 兩列之後、錯置的 SD-05 列之前**，使三列真實紀錄依時間連續，錯置列被留在表尾最顯眼處。理由：若插在最後，錯置列會夾在兩列真實紀錄中間，更難被發現與清理。
- 驗證方式（接手者如何確認我做的是對的：指令 + 我實際得到的輸出）：

  **驗收 1（r2 acceptance 第 1 條）—— SD 的 `301` 只剩脈絡引用，不再是判準**

  ```bash
  grep -n "301" docs/specs/03_系統設計書_SD.md | cut -c1-200
  ```
  ```text
  308:| **NFR-002** 安全<br>五項 | ①HTTPS：Cloud Run 為 `*.run.app` 自動配發憑證，並由 Google 前端將明文 HTTP 請求以 **3xx（實測 302 Found）** 導向 HTTPS（ADR-0005 決定
  416:| 2026-09-20 | 0.3（字面同步，未變更版本號） | **T-0040 r2**（規格變更請求，Leader 2026-09-20 審核 r1 後追加 outputs） | **只動第 7 章 NFR-002① 一列**，把 HTTP
  ```
  兩筆皆非判準：308 是本次改寫後的那一列，`301` 只出現在「301 或 302 皆合規」的合規範圍說明中；416 是本次新增的變更紀錄列（記錄「由寫死的 301 改為 3xx」）。**改寫前的原文為**（`git show` 取自本輪 commit 的父版本）：

  ```bash
  git show HEAD:docs/specs/03_系統設計書_SD.md | sed -n "308p" | cut -c1-150
  ```
  ```text
  | **NFR-002** 安全<br>五項 | ①HTTPS：Cloud Run 為 `*.run.app` 自動配發憑證並將 HTTP 301 導向 HTTPS（ADR-0005），團隊零程式�
  ```
  （末字為 `cut -c1-150` 在多位元組字元中間截斷所致，非檔案內容有誤。）

  **此指令於本輪 commit 前執行，當時 `git rev-parse --short HEAD` ＝ `9343c0f`**——**注意：這不是我 r1 的 commit `3e6b6b8`**，其間有其他平行任務卡的 commit 進入 `main`（本卡為文件卡、在根目錄 main 作業，HEAD 會隨他人提交前進，屬正常）。`9343c0f` 仍保有改寫前的 SD 內容，因為那些 commit 未觸及本檔。**本輪 commit 之後**，要取回改寫前的版本請改用 `git log -1 --format=%H -- docs/specs/03_系統設計書_SD.md` 找出本輪 commit 再看其父版本，或直接 `git show 9343c0f:docs/specs/03_系統設計書_SD.md | sed -n "308p"`；**不要用 `HEAD~1`**，平行提交會讓它指到不相干的 commit。

  **驗收 2 —— 「只動這一處」：本輪對 SD 的完整 diff 範圍**

  ```bash
  git diff -U0 docs/specs/03_系統設計書_SD.md | grep "^@@"
  ```
  ```text
  @@ -5 +5 @@ epic: E-001
  @@ -9 +9 @@ reviewers: [plan-sa, dev-tl]
  @@ -308 +308 @@ BE-02 的全域 onRequest hook：
  @@ -415,0 +416 @@ BE-02 的全域 onRequest hook：
  ```
  ```bash
  git diff --numstat docs/specs/03_系統設計書_SD.md
  ```
  ```text
  4	3	docs/specs/03_系統設計書_SD.md
  ```
  即：frontmatter 兩行（`version` 註解、`updated`）＋ 第 308 列本體 ＋ 新增第 416 列變更紀錄，**無第五處**。

  **驗收 3 —— frontmatter `updated` 已更新、`version`／`status` 維持**

  ```bash
  sed -n "1,10p" docs/specs/03_系統設計書_SD.md
  ```
  ```text
  ---
  doc: SD
  title: 系統設計書
  epic: E-001
  version: 0.3           # T-0010 規格變更（雲端平台改 GCP Cloud Run）；T-0040 r2 同步 §7 NFR-002① 導向狀態碼字面，未動設計決策故版本號不進位。變更紀錄見文末
  status: frozen         # Gate 1 通過 2026-09-19，變更走「規格變更請求」任務卡
  author: plan-sd
  reviewers: [plan-sa, dev-tl]
  updated: 2026-09-20T19:37:52+08:00
  ---
  ```

  **驗收 4 —— 三方用字一致（SD ／ `06` §2 ／ ADR-0005 決定第 7 點附註）**

  ```bash
  grep -n "3xx" docs/specs/03_系統設計書_SD.md docs/specs/06_部署架構與CICD.md "docs/specs/adr/ADR-0005-雲端平台-CloudRun.md" | cut -c1-110
  ```
  ```text
  docs/specs/03_系統設計書_SD.md:308:| **NFR-002** 安全<br>五項 | ①HTTPS：Cloud Run 為 `*.run.app`
  docs/specs/03_系統設計書_SD.md:416:| 2026-09-20 | 0.3（字面同步，未變更版本號） | **T-0040 r
  docs/specs/06_部署架構與CICD.md:5:version: 0.3           # T-0040 規格變更（cron 定位、secret 版
  docs/specs/06_部署架構與CICD.md:45:| **Cloud Run 自動 TLS 憑證＋網址** | `*.run.app` 平台憑證
  docs/specs/06_部署架構與CICD.md:733:| 2026-09-20 | **0.3** | **T-0040**（規格變更請求，Leader 202
  docs/specs/adr/ADR-0005-雲端平台-CloudRun.md:82:7. **HTTPS（AC-010-2／BR-025）**：Cloud Run 為 `*.run
  docs/specs/adr/ADR-0005-雲端平台-CloudRun.md:88:   > **判準據此改為：回 `3xx`（301 或 302 皆視
  docs/specs/adr/ADR-0005-雲端平台-CloudRun.md:90:   > 連帶影響：`06_部署架構與CICD.md` §2 資源
  ```
  三份文件的判準用語逐字比對（截斷為顯示寬度所致，完整字串見各檔）：

  ```bash
  grep -o "判準為「回 3xx 且 \`Location\` 為對應的 \`https://\` 網址」" docs/specs/03_系統設計書_SD.md docs/specs/06_部署架構與CICD.md
  ```
  ```text
  docs/specs/03_系統設計書_SD.md:判準為「回 3xx 且 `Location` 為對應的 `https://` 網址」
  docs/specs/06_部署架構與CICD.md:判準為「回 3xx 且 `Location` 為對應的 `https://` 網址」
  ```
  （ADR-0005:86 為同義的敘述句「**判準據此改為：回 `3xx`（301 或 302 皆視為合規）且 `Location` 為對應的 `https://` 網址。**」，因是 ADR 的附註行文而非規格條目，用語一致、句式不同。）

  **驗收 5 —— r1 產出未被本輪動到**

  ```bash
  git status --short docs/specs/
  ```
  ```text
   M docs/specs/03_系統設計書_SD.md
  ```
  即 `docs/specs/` 下本輪只有 SD 一個檔案有未提交變更，`06`／ADR-0005 已於 r1 commit `3e6b6b8` 定稿且未再修改（該指令於本輪 commit 前執行）。

- 下一步建議：
  1. **`20_測試案例.md` TC-079（:156）與 NFR-002 對照表（:330）**由 qa-lead 改為「回 3xx（301 或 302 皆視為合規）且 `Location` 為對應的 `https://` 網址」。**這是「301 → 3xx」這件事的最後一塊**，改完 D-016 即可從「規格文字待修」完全落地。
  2. **SD 變更紀錄表的錯置 SD-05 列**（現第 417 行）建議移回第 9 章「待確認設計決策」表，或由 Leader 判定是否直接刪除（該列內容在第 9 章應已有對應列，需先比對是否重複）。**屬既有缺陷、成本極低（移動一行），但需要一張授權改 SD 其他章節的卡**。
  3. **Leader 裁決①的落地**：Gate 2 後維運卡（dev-ops）建立第二個 uptime check 與告警政策後，**回填 `06` §6.1.1**（比照 §6.8.2 回填 uptime check 建立輸出的做法：貼實際 `gcloud monitoring uptime create` 輸出、check id、建立時間），並把 §6.3「現況等同沒有自動告警」的更正條改為告警政策的實際設定。本卡刻意不預先回填，理由見「假設與決策」第 3 點。
  4. **T-0039 的遠端待驗仍未解除**：使用者補授 `roles/secretmanager.viewer`（`06` §2.1 第 5 項）後，需觀察一次真實 `deploy-staging` run 是否全綠、`GITHUB_STEP_SUMMARY` 是否顯示三個 secret 版本號。由 dev-tl／Leader 追蹤。
  5. **`T-0042 Release Notes`（下游）**撰寫時可直接引用 `06` v0.3 的變更紀錄列與本列 SD 變更紀錄，兩者已寫成可直接摘錄的形式。
- 需要 Leader 裁決的事：
  1. **SD `version` 要不要進位為 0.4**（本輪維持 0.3，理由見「假設與決策」第 1 點，並援引 SD 與 `06` 各自的先例）。若 Leader 認為凍結文件的任何字面變更都應進版，改動成本為 frontmatter 一行＋變更紀錄一格，可於審核時直接指示。
  2. **驗證指令是否接受改為可直接執行的形式**（`curl -sSI "http://${STAGING_BASE_URL#https://}/health"`，取代原本帶 `<staging>` 佔位符的寫法）。這超出「只改狀態碼字面」的最小改動，若 Leader 認為應嚴格最小化，可退回為只改狀態碼、保留原佔位符寫法。
